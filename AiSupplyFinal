WorldLoaded = function()
	p1 = Player.GetPlayer("Multi0")
	p2 = Player.GetPlayer("Multi1")
	p3 = Player.GetPlayer("Multi2")
	p4 = Player.GetPlayer("Multi3")
	p5 = Player.GetPlayer("Multi4")
	p6 = Player.GetPlayer("Multi5")
	p7 = Player.GetPlayer("Multi6")
	p8 = Player.GetPlayer("Multi7")
	p9 = Player.GetPlayer("Multi8")
	p10 = Player.GetPlayer("Multi9")
	p11 = Player.GetPlayer("Multi10")
	p12 = Player.GetPlayer("Multi11")
	p13 = Player.GetPlayer("Multi12")
	p14 = Player.GetPlayer("Multi13")
	p15 = Player.GetPlayer("Multi14")
	p16 = Player.GetPlayer("Multi15")

	p1cash()
	p2cash()
	p3cash()
	p4cash()
	p5cash()
	p6cash()
	p7cash()
	p8cash()
	p9cash()
	p10cash()
	p11cash()
	p12cash()
	p13cash()
	p14cash()
	p15cash()
	p16cash()

	p1building()
	p2building()
	p3building()
	p4building()
	p5building()
	p6building()
	p7building()
	p8building()
	p9building()
	p10building()
	p11building()
	p12building()
	p13building()
	p14building()
	p15building()
	p16building()

	p1vehicle()
	p2vehicle()
	p3vehicle()
	p4vehicle()
	p5vehicle()
	p6vehicle()
	p7vehicle()
	p8vehicle()
	p9vehicle()
	p10vehicle()
	p11vehicle()
	p12vehicle()
	p13vehicle()
	p14vehicle()
	p15vehicle()
	p16vehicle()

	p1infantry()
	p2infantry()
	p3infantry()
	p4infantry()
	p5infantry()
	p6infantry()
	p7infantry()
	p8infantry()
	p9infantry()
	p10infantry()
	p11infantry()
	p12infantry()
	p13infantry()
	p14infantry()
	p15infantry()
	p16infantry()

end

p1cash = function()
	if p1 and p1.IsBot then
		citybottruck = p1.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p2 and p1.IsAlliedWith(p2) then
				fact = p2.GetActorsByType("fact")
				pyle = p2.GetActorsByType("pyle")
				hand = p2.GetActorsByType("hand")
				afld = p2.GetActorsByType("afld")
				weap = p2.GetActorsByType("weap")
				hpad = p2.GetActorsByType("hpad")
				rafact = p2.GetActorsByType("rafact")
				tent = p2.GetActorsByType("tent")
				barr = p2.GetActorsByType("barr")
				raweapa = p2.GetActorsByType("raweapa")
				raweap = p2.GetActorsByType("raweap")
				rahpad = p2.GetActorsByType("rahpad")
				raafld = p2.GetActorsByType("raafld")
				hq = p2.GetActorsByType("2100hq")
				fy1 = p2.GetActorsByType("2100fy1")
				fy2 = p2.GetActorsByType("2100fy2")
				sccommandcenter = p2.GetActorsByType("sccommandcenter")
				scbarracks = p2.GetActorsByType("scbarracks")
				scfactory = p2.GetActorsByType("scfactory")
				scstarport = p2.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p3 and p1.IsAlliedWith(p3) then
				fact = p3.GetActorsByType("fact")
				pyle = p3.GetActorsByType("pyle")
				hand = p3.GetActorsByType("hand")
				afld = p3.GetActorsByType("afld")
				weap = p3.GetActorsByType("weap")
				hpad = p3.GetActorsByType("hpad")
				rafact = p3.GetActorsByType("rafact")
				tent = p3.GetActorsByType("tent")
				barr = p3.GetActorsByType("barr")
				raweapa = p3.GetActorsByType("raweapa")
				raweap = p3.GetActorsByType("raweap")
				rahpad = p3.GetActorsByType("rahpad")
				raafld = p3.GetActorsByType("raafld")
				hq = p3.GetActorsByType("2100hq")
				fy1 = p3.GetActorsByType("2100fy1")
				fy2 = p3.GetActorsByType("2100fy2")
				sccommandcenter = p3.GetActorsByType("sccommandcenter")
				scbarracks = p3.GetActorsByType("scbarracks")
				scfactory = p3.GetActorsByType("scfactory")
				scstarport = p3.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p4 and p1.IsAlliedWith(p4) then
				fact = p4.GetActorsByType("fact")
				pyle = p4.GetActorsByType("pyle")
				hand = p4.GetActorsByType("hand")
				afld = p4.GetActorsByType("afld")
				weap = p4.GetActorsByType("weap")
				hpad = p4.GetActorsByType("hpad")
				rafact = p4.GetActorsByType("rafact")
				tent = p4.GetActorsByType("tent")
				barr = p4.GetActorsByType("barr")
				raweapa = p4.GetActorsByType("raweapa")
				raweap = p4.GetActorsByType("raweap")
				rahpad = p4.GetActorsByType("rahpad")
				raafld = p4.GetActorsByType("raafld")
				hq = p4.GetActorsByType("2100hq")
				fy1 = p4.GetActorsByType("2100fy1")
				fy2 = p4.GetActorsByType("2100fy2")
				sccommandcenter = p4.GetActorsByType("sccommandcenter")
				scbarracks = p4.GetActorsByType("scbarracks")
				scfactory = p4.GetActorsByType("scfactory")
				scstarport = p4.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p5 and p1.IsAlliedWith(p5) then
				fact = p5.GetActorsByType("fact")
				pyle = p5.GetActorsByType("pyle")
				hand = p5.GetActorsByType("hand")
				afld = p5.GetActorsByType("afld")
				weap = p5.GetActorsByType("weap")
				hpad = p5.GetActorsByType("hpad")
				rafact = p5.GetActorsByType("rafact")
				tent = p5.GetActorsByType("tent")
				barr = p5.GetActorsByType("barr")
				raweapa = p5.GetActorsByType("raweapa")
				raweap = p5.GetActorsByType("raweap")
				rahpad = p5.GetActorsByType("rahpad")
				raafld = p5.GetActorsByType("raafld")
				hq = p5.GetActorsByType("2100hq")
				fy1 = p5.GetActorsByType("2100fy1")
				fy2 = p5.GetActorsByType("2100fy2")
				sccommandcenter = p5.GetActorsByType("sccommandcenter")
				scbarracks = p5.GetActorsByType("scbarracks")
				scfactory = p5.GetActorsByType("scfactory")
				scstarport = p5.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p6 and p1.IsAlliedWith(p6) then
				fact = p6.GetActorsByType("fact")
				pyle = p6.GetActorsByType("pyle")
				hand = p6.GetActorsByType("hand")
				afld = p6.GetActorsByType("afld")
				weap = p6.GetActorsByType("weap")
				hpad = p6.GetActorsByType("hpad")
				rafact = p6.GetActorsByType("rafact")
				tent = p6.GetActorsByType("tent")
				barr = p6.GetActorsByType("barr")
				raweapa = p6.GetActorsByType("raweapa")
				raweap = p6.GetActorsByType("raweap")
				rahpad = p6.GetActorsByType("rahpad")
				raafld = p6.GetActorsByType("raafld")
				hq = p6.GetActorsByType("2100hq")
				fy1 = p6.GetActorsByType("2100fy1")
				fy2 = p6.GetActorsByType("2100fy2")
				sccommandcenter = p6.GetActorsByType("sccommandcenter")
				scbarracks = p6.GetActorsByType("scbarracks")
				scfactory = p6.GetActorsByType("scfactory")
				scstarport = p6.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p7 and p1.IsAlliedWith(p7) then
				fact = p7.GetActorsByType("fact")
				pyle = p7.GetActorsByType("pyle")
				hand = p7.GetActorsByType("hand")
				afld = p7.GetActorsByType("afld")
				weap = p7.GetActorsByType("weap")
				hpad = p7.GetActorsByType("hpad")
				rafact = p7.GetActorsByType("rafact")
				tent = p7.GetActorsByType("tent")
				barr = p7.GetActorsByType("barr")
				raweapa = p7.GetActorsByType("raweapa")
				raweap = p7.GetActorsByType("raweap")
				rahpad = p7.GetActorsByType("rahpad")
				raafld = p7.GetActorsByType("raafld")
				hq = p7.GetActorsByType("2100hq")
				fy1 = p7.GetActorsByType("2100fy1")
				fy2 = p7.GetActorsByType("2100fy2")
				sccommandcenter = p7.GetActorsByType("sccommandcenter")
				scbarracks = p7.GetActorsByType("scbarracks")
				scfactory = p7.GetActorsByType("scfactory")
				scstarport = p7.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p8 and p1.IsAlliedWith(p8) then
				fact = p8.GetActorsByType("fact")
				pyle = p8.GetActorsByType("pyle")
				hand = p8.GetActorsByType("hand")
				afld = p8.GetActorsByType("afld")
				weap = p8.GetActorsByType("weap")
				hpad = p8.GetActorsByType("hpad")
				rafact = p8.GetActorsByType("rafact")
				tent = p8.GetActorsByType("tent")
				barr = p8.GetActorsByType("barr")
				raweapa = p8.GetActorsByType("raweapa")
				raweap = p8.GetActorsByType("raweap")
				rahpad = p8.GetActorsByType("rahpad")
				raafld = p8.GetActorsByType("raafld")
				hq = p8.GetActorsByType("2100hq")
				fy1 = p8.GetActorsByType("2100fy1")
				fy2 = p8.GetActorsByType("2100fy2")
				sccommandcenter = p8.GetActorsByType("sccommandcenter")
				scbarracks = p8.GetActorsByType("scbarracks")
				scfactory = p8.GetActorsByType("scfactory")
				scstarport = p8.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p9 and p1.IsAlliedWith(p9) then
				fact = p9.GetActorsByType("fact")
				pyle = p9.GetActorsByType("pyle")
				hand = p9.GetActorsByType("hand")
				afld = p9.GetActorsByType("afld")
				weap = p9.GetActorsByType("weap")
				hpad = p9.GetActorsByType("hpad")
				rafact = p9.GetActorsByType("rafact")
				tent = p9.GetActorsByType("tent")
				barr = p9.GetActorsByType("barr")
				raweapa = p9.GetActorsByType("raweapa")
				raweap = p9.GetActorsByType("raweap")
				rahpad = p9.GetActorsByType("rahpad")
				raafld = p9.GetActorsByType("raafld")
				hq = p9.GetActorsByType("2100hq")
				fy1 = p9.GetActorsByType("2100fy1")
				fy2 = p9.GetActorsByType("2100fy2")
				sccommandcenter = p9.GetActorsByType("sccommandcenter")
				scbarracks = p9.GetActorsByType("scbarracks")
				scfactory = p9.GetActorsByType("scfactory")
				scstarport = p9.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p10 and p1.IsAlliedWith(p10) then
				fact = p10.GetActorsByType("fact")
				pyle = p10.GetActorsByType("pyle")
				hand = p10.GetActorsByType("hand")
				afld = p10.GetActorsByType("afld")
				weap = p10.GetActorsByType("weap")
				hpad = p10.GetActorsByType("hpad")
				rafact = p10.GetActorsByType("rafact")
				tent = p10.GetActorsByType("tent")
				barr = p10.GetActorsByType("barr")
				raweapa = p10.GetActorsByType("raweapa")
				raweap = p10.GetActorsByType("raweap")
				rahpad = p10.GetActorsByType("rahpad")
				raafld = p10.GetActorsByType("raafld")
				hq = p10.GetActorsByType("2100hq")
				fy1 = p10.GetActorsByType("2100fy1")
				fy2 = p10.GetActorsByType("2100fy2")
				sccommandcenter = p10.GetActorsByType("sccommandcenter")
				scbarracks = p10.GetActorsByType("scbarracks")
				scfactory = p10.GetActorsByType("scfactory")
				scstarport = p10.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p11 and p1.IsAlliedWith(p11) then
				fact = p11.GetActorsByType("fact")
				pyle = p11.GetActorsByType("pyle")
				hand = p11.GetActorsByType("hand")
				afld = p11.GetActorsByType("afld")
				weap = p11.GetActorsByType("weap")
				hpad = p11.GetActorsByType("hpad")
				rafact = p11.GetActorsByType("rafact")
				tent = p11.GetActorsByType("tent")
				barr = p11.GetActorsByType("barr")
				raweapa = p11.GetActorsByType("raweapa")
				raweap = p11.GetActorsByType("raweap")
				rahpad = p11.GetActorsByType("rahpad")
				raafld = p11.GetActorsByType("raafld")
				hq = p11.GetActorsByType("2100hq")
				fy1 = p11.GetActorsByType("2100fy1")
				fy2 = p11.GetActorsByType("2100fy2")
				sccommandcenter = p11.GetActorsByType("sccommandcenter")
				scbarracks = p11.GetActorsByType("scbarracks")
				scfactory = p11.GetActorsByType("scfactory")
				scstarport = p11.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p12 and p1.IsAlliedWith(p12) then
				fact = p12.GetActorsByType("fact")
				pyle = p12.GetActorsByType("pyle")
				hand = p12.GetActorsByType("hand")
				afld = p12.GetActorsByType("afld")
				weap = p12.GetActorsByType("weap")
				hpad = p12.GetActorsByType("hpad")
				rafact = p12.GetActorsByType("rafact")
				tent = p12.GetActorsByType("tent")
				barr = p12.GetActorsByType("barr")
				raweapa = p12.GetActorsByType("raweapa")
				raweap = p12.GetActorsByType("raweap")
				rahpad = p12.GetActorsByType("rahpad")
				raafld = p12.GetActorsByType("raafld")
				hq = p12.GetActorsByType("2100hq")
				fy1 = p12.GetActorsByType("2100fy1")
				fy2 = p12.GetActorsByType("2100fy2")
				sccommandcenter = p12.GetActorsByType("sccommandcenter")
				scbarracks = p12.GetActorsByType("scbarracks")
				scfactory = p12.GetActorsByType("scfactory")
				scstarport = p12.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p13 and p1.IsAlliedWith(p13) then
				fact = p13.GetActorsByType("fact")
				pyle = p13.GetActorsByType("pyle")
				hand = p13.GetActorsByType("hand")
				afld = p13.GetActorsByType("afld")
				weap = p13.GetActorsByType("weap")
				hpad = p13.GetActorsByType("hpad")
				rafact = p13.GetActorsByType("rafact")
				tent = p13.GetActorsByType("tent")
				barr = p13.GetActorsByType("barr")
				raweapa = p13.GetActorsByType("raweapa")
				raweap = p13.GetActorsByType("raweap")
				rahpad = p13.GetActorsByType("rahpad")
				raafld = p13.GetActorsByType("raafld")
				hq = p13.GetActorsByType("2100hq")
				fy1 = p13.GetActorsByType("2100fy1")
				fy2 = p13.GetActorsByType("2100fy2")
				sccommandcenter = p13.GetActorsByType("sccommandcenter")
				scbarracks = p13.GetActorsByType("scbarracks")
				scfactory = p13.GetActorsByType("scfactory")
				scstarport = p13.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p14 and p1.IsAlliedWith(p14) then
				fact = p14.GetActorsByType("fact")
				pyle = p14.GetActorsByType("pyle")
				hand = p14.GetActorsByType("hand")
				afld = p14.GetActorsByType("afld")
				weap = p14.GetActorsByType("weap")
				hpad = p14.GetActorsByType("hpad")
				rafact = p14.GetActorsByType("rafact")
				tent = p14.GetActorsByType("tent")
				barr = p14.GetActorsByType("barr")
				raweapa = p14.GetActorsByType("raweapa")
				raweap = p14.GetActorsByType("raweap")
				rahpad = p14.GetActorsByType("rahpad")
				raafld = p14.GetActorsByType("raafld")
				hq = p14.GetActorsByType("2100hq")
				fy1 = p14.GetActorsByType("2100fy1")
				fy2 = p14.GetActorsByType("2100fy2")
				sccommandcenter = p14.GetActorsByType("sccommandcenter")
				scbarracks = p14.GetActorsByType("scbarracks")
				scfactory = p14.GetActorsByType("scfactory")
				scstarport = p14.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p15 and p1.IsAlliedWith(p15) then
				fact = p15.GetActorsByType("fact")
				pyle = p15.GetActorsByType("pyle")
				hand = p15.GetActorsByType("hand")
				afld = p15.GetActorsByType("afld")
				weap = p15.GetActorsByType("weap")
				hpad = p15.GetActorsByType("hpad")
				rafact = p15.GetActorsByType("rafact")
				tent = p15.GetActorsByType("tent")
				barr = p15.GetActorsByType("barr")
				raweapa = p15.GetActorsByType("raweapa")
				raweap = p15.GetActorsByType("raweap")
				rahpad = p15.GetActorsByType("rahpad")
				raafld = p15.GetActorsByType("raafld")
				hq = p15.GetActorsByType("2100hq")
				fy1 = p15.GetActorsByType("2100fy1")
				fy2 = p15.GetActorsByType("2100fy2")
				sccommandcenter = p15.GetActorsByType("sccommandcenter")
				scbarracks = p15.GetActorsByType("scbarracks")
				scfactory = p15.GetActorsByType("scfactory")
				scstarport = p15.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p16 and p1.IsAlliedWith(p16) then
				fact = p16.GetActorsByType("fact")
				pyle = p16.GetActorsByType("pyle")
				hand = p16.GetActorsByType("hand")
				afld = p16.GetActorsByType("afld")
				weap = p16.GetActorsByType("weap")
				hpad = p16.GetActorsByType("hpad")
				rafact = p16.GetActorsByType("rafact")
				tent = p16.GetActorsByType("tent")
				barr = p16.GetActorsByType("barr")
				raweapa = p16.GetActorsByType("raweapa")
				raweap = p16.GetActorsByType("raweap")
				rahpad = p16.GetActorsByType("rahpad")
				raafld = p16.GetActorsByType("raafld")
				hq = p16.GetActorsByType("2100hq")
				fy1 = p16.GetActorsByType("2100fy1")
				fy2 = p16.GetActorsByType("2100fy2")
				sccommandcenter = p16.GetActorsByType("sccommandcenter")
				scbarracks = p16.GetActorsByType("scbarracks")
				scfactory = p16.GetActorsByType("scfactory")
				scstarport = p16.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p1cash()
	end)
end
p2cash = function()
	if p2 and p2.IsBot then
		citybottruck = p2.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p3 and p2.IsAlliedWith(p3) then
				fact = p3.GetActorsByType("fact")
				pyle = p3.GetActorsByType("pyle")
				hand = p3.GetActorsByType("hand")
				afld = p3.GetActorsByType("afld")
				weap = p3.GetActorsByType("weap")
				hpad = p3.GetActorsByType("hpad")
				rafact = p3.GetActorsByType("rafact")
				tent = p3.GetActorsByType("tent")
				barr = p3.GetActorsByType("barr")
				raweapa = p3.GetActorsByType("raweapa")
				raweap = p3.GetActorsByType("raweap")
				rahpad = p3.GetActorsByType("rahpad")
				raafld = p3.GetActorsByType("raafld")
				hq = p3.GetActorsByType("2100hq")
				fy1 = p3.GetActorsByType("2100fy1")
				fy2 = p3.GetActorsByType("2100fy2")
				sccommandcenter = p3.GetActorsByType("sccommandcenter")
				scbarracks = p3.GetActorsByType("scbarracks")
				scfactory = p3.GetActorsByType("scfactory")
				scstarport = p3.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p4 and p2.IsAlliedWith(p4) then
				fact = p4.GetActorsByType("fact")
				pyle = p4.GetActorsByType("pyle")
				hand = p4.GetActorsByType("hand")
				afld = p4.GetActorsByType("afld")
				weap = p4.GetActorsByType("weap")
				hpad = p4.GetActorsByType("hpad")
				rafact = p4.GetActorsByType("rafact")
				tent = p4.GetActorsByType("tent")
				barr = p4.GetActorsByType("barr")
				raweapa = p4.GetActorsByType("raweapa")
				raweap = p4.GetActorsByType("raweap")
				rahpad = p4.GetActorsByType("rahpad")
				raafld = p4.GetActorsByType("raafld")
				hq = p4.GetActorsByType("2100hq")
				fy1 = p4.GetActorsByType("2100fy1")
				fy2 = p4.GetActorsByType("2100fy2")
				sccommandcenter = p4.GetActorsByType("sccommandcenter")
				scbarracks = p4.GetActorsByType("scbarracks")
				scfactory = p4.GetActorsByType("scfactory")
				scstarport = p4.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p5 and p2.IsAlliedWith(p5) then
				fact = p5.GetActorsByType("fact")
				pyle = p5.GetActorsByType("pyle")
				hand = p5.GetActorsByType("hand")
				afld = p5.GetActorsByType("afld")
				weap = p5.GetActorsByType("weap")
				hpad = p5.GetActorsByType("hpad")
				rafact = p5.GetActorsByType("rafact")
				tent = p5.GetActorsByType("tent")
				barr = p5.GetActorsByType("barr")
				raweapa = p5.GetActorsByType("raweapa")
				raweap = p5.GetActorsByType("raweap")
				rahpad = p5.GetActorsByType("rahpad")
				raafld = p5.GetActorsByType("raafld")
				hq = p5.GetActorsByType("2100hq")
				fy1 = p5.GetActorsByType("2100fy1")
				fy2 = p5.GetActorsByType("2100fy2")
				sccommandcenter = p5.GetActorsByType("sccommandcenter")
				scbarracks = p5.GetActorsByType("scbarracks")
				scfactory = p5.GetActorsByType("scfactory")
				scstarport = p5.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p6 and p2.IsAlliedWith(p6) then
				fact = p6.GetActorsByType("fact")
				pyle = p6.GetActorsByType("pyle")
				hand = p6.GetActorsByType("hand")
				afld = p6.GetActorsByType("afld")
				weap = p6.GetActorsByType("weap")
				hpad = p6.GetActorsByType("hpad")
				rafact = p6.GetActorsByType("rafact")
				tent = p6.GetActorsByType("tent")
				barr = p6.GetActorsByType("barr")
				raweapa = p6.GetActorsByType("raweapa")
				raweap = p6.GetActorsByType("raweap")
				rahpad = p6.GetActorsByType("rahpad")
				raafld = p6.GetActorsByType("raafld")
				hq = p6.GetActorsByType("2100hq")
				fy1 = p6.GetActorsByType("2100fy1")
				fy2 = p6.GetActorsByType("2100fy2")
				sccommandcenter = p6.GetActorsByType("sccommandcenter")
				scbarracks = p6.GetActorsByType("scbarracks")
				scfactory = p6.GetActorsByType("scfactory")
				scstarport = p6.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p7 and p2.IsAlliedWith(p7) then
				fact = p7.GetActorsByType("fact")
				pyle = p7.GetActorsByType("pyle")
				hand = p7.GetActorsByType("hand")
				afld = p7.GetActorsByType("afld")
				weap = p7.GetActorsByType("weap")
				hpad = p7.GetActorsByType("hpad")
				rafact = p7.GetActorsByType("rafact")
				tent = p7.GetActorsByType("tent")
				barr = p7.GetActorsByType("barr")
				raweapa = p7.GetActorsByType("raweapa")
				raweap = p7.GetActorsByType("raweap")
				rahpad = p7.GetActorsByType("rahpad")
				raafld = p7.GetActorsByType("raafld")
				hq = p7.GetActorsByType("2100hq")
				fy1 = p7.GetActorsByType("2100fy1")
				fy2 = p7.GetActorsByType("2100fy2")
				sccommandcenter = p7.GetActorsByType("sccommandcenter")
				scbarracks = p7.GetActorsByType("scbarracks")
				scfactory = p7.GetActorsByType("scfactory")
				scstarport = p7.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p8 and p2.IsAlliedWith(p8) then
				fact = p8.GetActorsByType("fact")
				pyle = p8.GetActorsByType("pyle")
				hand = p8.GetActorsByType("hand")
				afld = p8.GetActorsByType("afld")
				weap = p8.GetActorsByType("weap")
				hpad = p8.GetActorsByType("hpad")
				rafact = p8.GetActorsByType("rafact")
				tent = p8.GetActorsByType("tent")
				barr = p8.GetActorsByType("barr")
				raweapa = p8.GetActorsByType("raweapa")
				raweap = p8.GetActorsByType("raweap")
				rahpad = p8.GetActorsByType("rahpad")
				raafld = p8.GetActorsByType("raafld")
				hq = p8.GetActorsByType("2100hq")
				fy1 = p8.GetActorsByType("2100fy1")
				fy2 = p8.GetActorsByType("2100fy2")
				sccommandcenter = p8.GetActorsByType("sccommandcenter")
				scbarracks = p8.GetActorsByType("scbarracks")
				scfactory = p8.GetActorsByType("scfactory")
				scstarport = p8.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p9 and p2.IsAlliedWith(p9) then
				fact = p9.GetActorsByType("fact")
				pyle = p9.GetActorsByType("pyle")
				hand = p9.GetActorsByType("hand")
				afld = p9.GetActorsByType("afld")
				weap = p9.GetActorsByType("weap")
				hpad = p9.GetActorsByType("hpad")
				rafact = p9.GetActorsByType("rafact")
				tent = p9.GetActorsByType("tent")
				barr = p9.GetActorsByType("barr")
				raweapa = p9.GetActorsByType("raweapa")
				raweap = p9.GetActorsByType("raweap")
				rahpad = p9.GetActorsByType("rahpad")
				raafld = p9.GetActorsByType("raafld")
				hq = p9.GetActorsByType("2100hq")
				fy1 = p9.GetActorsByType("2100fy1")
				fy2 = p9.GetActorsByType("2100fy2")
				sccommandcenter = p9.GetActorsByType("sccommandcenter")
				scbarracks = p9.GetActorsByType("scbarracks")
				scfactory = p9.GetActorsByType("scfactory")
				scstarport = p9.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p10 and p2.IsAlliedWith(p10) then
				fact = p10.GetActorsByType("fact")
				pyle = p10.GetActorsByType("pyle")
				hand = p10.GetActorsByType("hand")
				afld = p10.GetActorsByType("afld")
				weap = p10.GetActorsByType("weap")
				hpad = p10.GetActorsByType("hpad")
				rafact = p10.GetActorsByType("rafact")
				tent = p10.GetActorsByType("tent")
				barr = p10.GetActorsByType("barr")
				raweapa = p10.GetActorsByType("raweapa")
				raweap = p10.GetActorsByType("raweap")
				rahpad = p10.GetActorsByType("rahpad")
				raafld = p10.GetActorsByType("raafld")
				hq = p10.GetActorsByType("2100hq")
				fy1 = p10.GetActorsByType("2100fy1")
				fy2 = p10.GetActorsByType("2100fy2")
				sccommandcenter = p10.GetActorsByType("sccommandcenter")
				scbarracks = p10.GetActorsByType("scbarracks")
				scfactory = p10.GetActorsByType("scfactory")
				scstarport = p10.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p11 and p2.IsAlliedWith(p11) then
				fact = p11.GetActorsByType("fact")
				pyle = p11.GetActorsByType("pyle")
				hand = p11.GetActorsByType("hand")
				afld = p11.GetActorsByType("afld")
				weap = p11.GetActorsByType("weap")
				hpad = p11.GetActorsByType("hpad")
				rafact = p11.GetActorsByType("rafact")
				tent = p11.GetActorsByType("tent")
				barr = p11.GetActorsByType("barr")
				raweapa = p11.GetActorsByType("raweapa")
				raweap = p11.GetActorsByType("raweap")
				rahpad = p11.GetActorsByType("rahpad")
				raafld = p11.GetActorsByType("raafld")
				hq = p11.GetActorsByType("2100hq")
				fy1 = p11.GetActorsByType("2100fy1")
				fy2 = p11.GetActorsByType("2100fy2")
				sccommandcenter = p11.GetActorsByType("sccommandcenter")
				scbarracks = p11.GetActorsByType("scbarracks")
				scfactory = p11.GetActorsByType("scfactory")
				scstarport = p11.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p12 and p2.IsAlliedWith(p12) then
				fact = p12.GetActorsByType("fact")
				pyle = p12.GetActorsByType("pyle")
				hand = p12.GetActorsByType("hand")
				afld = p12.GetActorsByType("afld")
				weap = p12.GetActorsByType("weap")
				hpad = p12.GetActorsByType("hpad")
				rafact = p12.GetActorsByType("rafact")
				tent = p12.GetActorsByType("tent")
				barr = p12.GetActorsByType("barr")
				raweapa = p12.GetActorsByType("raweapa")
				raweap = p12.GetActorsByType("raweap")
				rahpad = p12.GetActorsByType("rahpad")
				raafld = p12.GetActorsByType("raafld")
				hq = p12.GetActorsByType("2100hq")
				fy1 = p12.GetActorsByType("2100fy1")
				fy2 = p12.GetActorsByType("2100fy2")
				sccommandcenter = p12.GetActorsByType("sccommandcenter")
				scbarracks = p12.GetActorsByType("scbarracks")
				scfactory = p12.GetActorsByType("scfactory")
				scstarport = p12.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p13 and p2.IsAlliedWith(p13) then
				fact = p13.GetActorsByType("fact")
				pyle = p13.GetActorsByType("pyle")
				hand = p13.GetActorsByType("hand")
				afld = p13.GetActorsByType("afld")
				weap = p13.GetActorsByType("weap")
				hpad = p13.GetActorsByType("hpad")
				rafact = p13.GetActorsByType("rafact")
				tent = p13.GetActorsByType("tent")
				barr = p13.GetActorsByType("barr")
				raweapa = p13.GetActorsByType("raweapa")
				raweap = p13.GetActorsByType("raweap")
				rahpad = p13.GetActorsByType("rahpad")
				raafld = p13.GetActorsByType("raafld")
				hq = p13.GetActorsByType("2100hq")
				fy1 = p13.GetActorsByType("2100fy1")
				fy2 = p13.GetActorsByType("2100fy2")
				sccommandcenter = p13.GetActorsByType("sccommandcenter")
				scbarracks = p13.GetActorsByType("scbarracks")
				scfactory = p13.GetActorsByType("scfactory")
				scstarport = p13.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p14 and p2.IsAlliedWith(p14) then
				fact = p14.GetActorsByType("fact")
				pyle = p14.GetActorsByType("pyle")
				hand = p14.GetActorsByType("hand")
				afld = p14.GetActorsByType("afld")
				weap = p14.GetActorsByType("weap")
				hpad = p14.GetActorsByType("hpad")
				rafact = p14.GetActorsByType("rafact")
				tent = p14.GetActorsByType("tent")
				barr = p14.GetActorsByType("barr")
				raweapa = p14.GetActorsByType("raweapa")
				raweap = p14.GetActorsByType("raweap")
				rahpad = p14.GetActorsByType("rahpad")
				raafld = p14.GetActorsByType("raafld")
				hq = p14.GetActorsByType("2100hq")
				fy1 = p14.GetActorsByType("2100fy1")
				fy2 = p14.GetActorsByType("2100fy2")
				sccommandcenter = p14.GetActorsByType("sccommandcenter")
				scbarracks = p14.GetActorsByType("scbarracks")
				scfactory = p14.GetActorsByType("scfactory")
				scstarport = p14.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p15 and p2.IsAlliedWith(p15) then
				fact = p15.GetActorsByType("fact")
				pyle = p15.GetActorsByType("pyle")
				hand = p15.GetActorsByType("hand")
				afld = p15.GetActorsByType("afld")
				weap = p15.GetActorsByType("weap")
				hpad = p15.GetActorsByType("hpad")
				rafact = p15.GetActorsByType("rafact")
				tent = p15.GetActorsByType("tent")
				barr = p15.GetActorsByType("barr")
				raweapa = p15.GetActorsByType("raweapa")
				raweap = p15.GetActorsByType("raweap")
				rahpad = p15.GetActorsByType("rahpad")
				raafld = p15.GetActorsByType("raafld")
				hq = p15.GetActorsByType("2100hq")
				fy1 = p15.GetActorsByType("2100fy1")
				fy2 = p15.GetActorsByType("2100fy2")
				sccommandcenter = p15.GetActorsByType("sccommandcenter")
				scbarracks = p15.GetActorsByType("scbarracks")
				scfactory = p15.GetActorsByType("scfactory")
				scstarport = p15.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p16 and p2.IsAlliedWith(p16) then
				fact = p16.GetActorsByType("fact")
				pyle = p16.GetActorsByType("pyle")
				hand = p16.GetActorsByType("hand")
				afld = p16.GetActorsByType("afld")
				weap = p16.GetActorsByType("weap")
				hpad = p16.GetActorsByType("hpad")
				rafact = p16.GetActorsByType("rafact")
				tent = p16.GetActorsByType("tent")
				barr = p16.GetActorsByType("barr")
				raweapa = p16.GetActorsByType("raweapa")
				raweap = p16.GetActorsByType("raweap")
				rahpad = p16.GetActorsByType("rahpad")
				raafld = p16.GetActorsByType("raafld")
				hq = p16.GetActorsByType("2100hq")
				fy1 = p16.GetActorsByType("2100fy1")
				fy2 = p16.GetActorsByType("2100fy2")
				sccommandcenter = p16.GetActorsByType("sccommandcenter")
				scbarracks = p16.GetActorsByType("scbarracks")
				scfactory = p16.GetActorsByType("scfactory")
				scstarport = p16.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p1 and p2.IsAlliedWith(p1) then
				fact = p1.GetActorsByType("fact")
				pyle = p1.GetActorsByType("pyle")
				hand = p1.GetActorsByType("hand")
				afld = p1.GetActorsByType("afld")
				weap = p1.GetActorsByType("weap")
				hpad = p1.GetActorsByType("hpad")
				rafact = p1.GetActorsByType("rafact")
				tent = p1.GetActorsByType("tent")
				barr = p1.GetActorsByType("barr")
				raweapa = p1.GetActorsByType("raweapa")
				raweap = p1.GetActorsByType("raweap")
				rahpad = p1.GetActorsByType("rahpad")
				raafld = p1.GetActorsByType("raafld")
				hq = p1.GetActorsByType("2100hq")
				fy1 = p1.GetActorsByType("2100fy1")
				fy2 = p1.GetActorsByType("2100fy2")
				sccommandcenter = p1.GetActorsByType("sccommandcenter")
				scbarracks = p1.GetActorsByType("scbarracks")
				scfactory = p1.GetActorsByType("scfactory")
				scstarport = p1.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p2cash()
	end)
end
p3cash = function()
	if p3 and p3.IsBot then
		citybottruck = p3.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p4 and p3.IsAlliedWith(p4) then
				fact = p4.GetActorsByType("fact")
				pyle = p4.GetActorsByType("pyle")
				hand = p4.GetActorsByType("hand")
				afld = p4.GetActorsByType("afld")
				weap = p4.GetActorsByType("weap")
				hpad = p4.GetActorsByType("hpad")
				rafact = p4.GetActorsByType("rafact")
				tent = p4.GetActorsByType("tent")
				barr = p4.GetActorsByType("barr")
				raweapa = p4.GetActorsByType("raweapa")
				raweap = p4.GetActorsByType("raweap")
				rahpad = p4.GetActorsByType("rahpad")
				raafld = p4.GetActorsByType("raafld")
				hq = p4.GetActorsByType("2100hq")
				fy1 = p4.GetActorsByType("2100fy1")
				fy2 = p4.GetActorsByType("2100fy2")
				sccommandcenter = p4.GetActorsByType("sccommandcenter")
				scbarracks = p4.GetActorsByType("scbarracks")
				scfactory = p4.GetActorsByType("scfactory")
				scstarport = p4.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p5 and p3.IsAlliedWith(p5) then
				fact = p5.GetActorsByType("fact")
				pyle = p5.GetActorsByType("pyle")
				hand = p5.GetActorsByType("hand")
				afld = p5.GetActorsByType("afld")
				weap = p5.GetActorsByType("weap")
				hpad = p5.GetActorsByType("hpad")
				rafact = p5.GetActorsByType("rafact")
				tent = p5.GetActorsByType("tent")
				barr = p5.GetActorsByType("barr")
				raweapa = p5.GetActorsByType("raweapa")
				raweap = p5.GetActorsByType("raweap")
				rahpad = p5.GetActorsByType("rahpad")
				raafld = p5.GetActorsByType("raafld")
				hq = p5.GetActorsByType("2100hq")
				fy1 = p5.GetActorsByType("2100fy1")
				fy2 = p5.GetActorsByType("2100fy2")
				sccommandcenter = p5.GetActorsByType("sccommandcenter")
				scbarracks = p5.GetActorsByType("scbarracks")
				scfactory = p5.GetActorsByType("scfactory")
				scstarport = p5.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p6 and p3.IsAlliedWith(p6) then
				fact = p6.GetActorsByType("fact")
				pyle = p6.GetActorsByType("pyle")
				hand = p6.GetActorsByType("hand")
				afld = p6.GetActorsByType("afld")
				weap = p6.GetActorsByType("weap")
				hpad = p6.GetActorsByType("hpad")
				rafact = p6.GetActorsByType("rafact")
				tent = p6.GetActorsByType("tent")
				barr = p6.GetActorsByType("barr")
				raweapa = p6.GetActorsByType("raweapa")
				raweap = p6.GetActorsByType("raweap")
				rahpad = p6.GetActorsByType("rahpad")
				raafld = p6.GetActorsByType("raafld")
				hq = p6.GetActorsByType("2100hq")
				fy1 = p6.GetActorsByType("2100fy1")
				fy2 = p6.GetActorsByType("2100fy2")
				sccommandcenter = p6.GetActorsByType("sccommandcenter")
				scbarracks = p6.GetActorsByType("scbarracks")
				scfactory = p6.GetActorsByType("scfactory")
				scstarport = p6.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p7 and p3.IsAlliedWith(p7) then
				fact = p7.GetActorsByType("fact")
				pyle = p7.GetActorsByType("pyle")
				hand = p7.GetActorsByType("hand")
				afld = p7.GetActorsByType("afld")
				weap = p7.GetActorsByType("weap")
				hpad = p7.GetActorsByType("hpad")
				rafact = p7.GetActorsByType("rafact")
				tent = p7.GetActorsByType("tent")
				barr = p7.GetActorsByType("barr")
				raweapa = p7.GetActorsByType("raweapa")
				raweap = p7.GetActorsByType("raweap")
				rahpad = p7.GetActorsByType("rahpad")
				raafld = p7.GetActorsByType("raafld")
				hq = p7.GetActorsByType("2100hq")
				fy1 = p7.GetActorsByType("2100fy1")
				fy2 = p7.GetActorsByType("2100fy2")
				sccommandcenter = p7.GetActorsByType("sccommandcenter")
				scbarracks = p7.GetActorsByType("scbarracks")
				scfactory = p7.GetActorsByType("scfactory")
				scstarport = p7.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p8 and p3.IsAlliedWith(p8) then
				fact = p8.GetActorsByType("fact")
				pyle = p8.GetActorsByType("pyle")
				hand = p8.GetActorsByType("hand")
				afld = p8.GetActorsByType("afld")
				weap = p8.GetActorsByType("weap")
				hpad = p8.GetActorsByType("hpad")
				rafact = p8.GetActorsByType("rafact")
				tent = p8.GetActorsByType("tent")
				barr = p8.GetActorsByType("barr")
				raweapa = p8.GetActorsByType("raweapa")
				raweap = p8.GetActorsByType("raweap")
				rahpad = p8.GetActorsByType("rahpad")
				raafld = p8.GetActorsByType("raafld")
				hq = p8.GetActorsByType("2100hq")
				fy1 = p8.GetActorsByType("2100fy1")
				fy2 = p8.GetActorsByType("2100fy2")
				sccommandcenter = p8.GetActorsByType("sccommandcenter")
				scbarracks = p8.GetActorsByType("scbarracks")
				scfactory = p8.GetActorsByType("scfactory")
				scstarport = p8.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p9 and p3.IsAlliedWith(p9) then
				fact = p9.GetActorsByType("fact")
				pyle = p9.GetActorsByType("pyle")
				hand = p9.GetActorsByType("hand")
				afld = p9.GetActorsByType("afld")
				weap = p9.GetActorsByType("weap")
				hpad = p9.GetActorsByType("hpad")
				rafact = p9.GetActorsByType("rafact")
				tent = p9.GetActorsByType("tent")
				barr = p9.GetActorsByType("barr")
				raweapa = p9.GetActorsByType("raweapa")
				raweap = p9.GetActorsByType("raweap")
				rahpad = p9.GetActorsByType("rahpad")
				raafld = p9.GetActorsByType("raafld")
				hq = p9.GetActorsByType("2100hq")
				fy1 = p9.GetActorsByType("2100fy1")
				fy2 = p9.GetActorsByType("2100fy2")
				sccommandcenter = p9.GetActorsByType("sccommandcenter")
				scbarracks = p9.GetActorsByType("scbarracks")
				scfactory = p9.GetActorsByType("scfactory")
				scstarport = p9.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p10 and p3.IsAlliedWith(p10) then
				fact = p10.GetActorsByType("fact")
				pyle = p10.GetActorsByType("pyle")
				hand = p10.GetActorsByType("hand")
				afld = p10.GetActorsByType("afld")
				weap = p10.GetActorsByType("weap")
				hpad = p10.GetActorsByType("hpad")
				rafact = p10.GetActorsByType("rafact")
				tent = p10.GetActorsByType("tent")
				barr = p10.GetActorsByType("barr")
				raweapa = p10.GetActorsByType("raweapa")
				raweap = p10.GetActorsByType("raweap")
				rahpad = p10.GetActorsByType("rahpad")
				raafld = p10.GetActorsByType("raafld")
				hq = p10.GetActorsByType("2100hq")
				fy1 = p10.GetActorsByType("2100fy1")
				fy2 = p10.GetActorsByType("2100fy2")
				sccommandcenter = p10.GetActorsByType("sccommandcenter")
				scbarracks = p10.GetActorsByType("scbarracks")
				scfactory = p10.GetActorsByType("scfactory")
				scstarport = p10.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p11 and p3.IsAlliedWith(p11) then
				fact = p11.GetActorsByType("fact")
				pyle = p11.GetActorsByType("pyle")
				hand = p11.GetActorsByType("hand")
				afld = p11.GetActorsByType("afld")
				weap = p11.GetActorsByType("weap")
				hpad = p11.GetActorsByType("hpad")
				rafact = p11.GetActorsByType("rafact")
				tent = p11.GetActorsByType("tent")
				barr = p11.GetActorsByType("barr")
				raweapa = p11.GetActorsByType("raweapa")
				raweap = p11.GetActorsByType("raweap")
				rahpad = p11.GetActorsByType("rahpad")
				raafld = p11.GetActorsByType("raafld")
				hq = p11.GetActorsByType("2100hq")
				fy1 = p11.GetActorsByType("2100fy1")
				fy2 = p11.GetActorsByType("2100fy2")
				sccommandcenter = p11.GetActorsByType("sccommandcenter")
				scbarracks = p11.GetActorsByType("scbarracks")
				scfactory = p11.GetActorsByType("scfactory")
				scstarport = p11.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p12 and p3.IsAlliedWith(p12) then
				fact = p12.GetActorsByType("fact")
				pyle = p12.GetActorsByType("pyle")
				hand = p12.GetActorsByType("hand")
				afld = p12.GetActorsByType("afld")
				weap = p12.GetActorsByType("weap")
				hpad = p12.GetActorsByType("hpad")
				rafact = p12.GetActorsByType("rafact")
				tent = p12.GetActorsByType("tent")
				barr = p12.GetActorsByType("barr")
				raweapa = p12.GetActorsByType("raweapa")
				raweap = p12.GetActorsByType("raweap")
				rahpad = p12.GetActorsByType("rahpad")
				raafld = p12.GetActorsByType("raafld")
				hq = p12.GetActorsByType("2100hq")
				fy1 = p12.GetActorsByType("2100fy1")
				fy2 = p12.GetActorsByType("2100fy2")
				sccommandcenter = p12.GetActorsByType("sccommandcenter")
				scbarracks = p12.GetActorsByType("scbarracks")
				scfactory = p12.GetActorsByType("scfactory")
				scstarport = p12.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p13 and p3.IsAlliedWith(p13) then
				fact = p13.GetActorsByType("fact")
				pyle = p13.GetActorsByType("pyle")
				hand = p13.GetActorsByType("hand")
				afld = p13.GetActorsByType("afld")
				weap = p13.GetActorsByType("weap")
				hpad = p13.GetActorsByType("hpad")
				rafact = p13.GetActorsByType("rafact")
				tent = p13.GetActorsByType("tent")
				barr = p13.GetActorsByType("barr")
				raweapa = p13.GetActorsByType("raweapa")
				raweap = p13.GetActorsByType("raweap")
				rahpad = p13.GetActorsByType("rahpad")
				raafld = p13.GetActorsByType("raafld")
				hq = p13.GetActorsByType("2100hq")
				fy1 = p13.GetActorsByType("2100fy1")
				fy2 = p13.GetActorsByType("2100fy2")
				sccommandcenter = p13.GetActorsByType("sccommandcenter")
				scbarracks = p13.GetActorsByType("scbarracks")
				scfactory = p13.GetActorsByType("scfactory")
				scstarport = p13.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p14 and p3.IsAlliedWith(p14) then
				fact = p14.GetActorsByType("fact")
				pyle = p14.GetActorsByType("pyle")
				hand = p14.GetActorsByType("hand")
				afld = p14.GetActorsByType("afld")
				weap = p14.GetActorsByType("weap")
				hpad = p14.GetActorsByType("hpad")
				rafact = p14.GetActorsByType("rafact")
				tent = p14.GetActorsByType("tent")
				barr = p14.GetActorsByType("barr")
				raweapa = p14.GetActorsByType("raweapa")
				raweap = p14.GetActorsByType("raweap")
				rahpad = p14.GetActorsByType("rahpad")
				raafld = p14.GetActorsByType("raafld")
				hq = p14.GetActorsByType("2100hq")
				fy1 = p14.GetActorsByType("2100fy1")
				fy2 = p14.GetActorsByType("2100fy2")
				sccommandcenter = p14.GetActorsByType("sccommandcenter")
				scbarracks = p14.GetActorsByType("scbarracks")
				scfactory = p14.GetActorsByType("scfactory")
				scstarport = p14.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p15 and p3.IsAlliedWith(p15) then
				fact = p15.GetActorsByType("fact")
				pyle = p15.GetActorsByType("pyle")
				hand = p15.GetActorsByType("hand")
				afld = p15.GetActorsByType("afld")
				weap = p15.GetActorsByType("weap")
				hpad = p15.GetActorsByType("hpad")
				rafact = p15.GetActorsByType("rafact")
				tent = p15.GetActorsByType("tent")
				barr = p15.GetActorsByType("barr")
				raweapa = p15.GetActorsByType("raweapa")
				raweap = p15.GetActorsByType("raweap")
				rahpad = p15.GetActorsByType("rahpad")
				raafld = p15.GetActorsByType("raafld")
				hq = p15.GetActorsByType("2100hq")
				fy1 = p15.GetActorsByType("2100fy1")
				fy2 = p15.GetActorsByType("2100fy2")
				sccommandcenter = p15.GetActorsByType("sccommandcenter")
				scbarracks = p15.GetActorsByType("scbarracks")
				scfactory = p15.GetActorsByType("scfactory")
				scstarport = p15.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p16 and p3.IsAlliedWith(p16) then
				fact = p16.GetActorsByType("fact")
				pyle = p16.GetActorsByType("pyle")
				hand = p16.GetActorsByType("hand")
				afld = p16.GetActorsByType("afld")
				weap = p16.GetActorsByType("weap")
				hpad = p16.GetActorsByType("hpad")
				rafact = p16.GetActorsByType("rafact")
				tent = p16.GetActorsByType("tent")
				barr = p16.GetActorsByType("barr")
				raweapa = p16.GetActorsByType("raweapa")
				raweap = p16.GetActorsByType("raweap")
				rahpad = p16.GetActorsByType("rahpad")
				raafld = p16.GetActorsByType("raafld")
				hq = p16.GetActorsByType("2100hq")
				fy1 = p16.GetActorsByType("2100fy1")
				fy2 = p16.GetActorsByType("2100fy2")
				sccommandcenter = p16.GetActorsByType("sccommandcenter")
				scbarracks = p16.GetActorsByType("scbarracks")
				scfactory = p16.GetActorsByType("scfactory")
				scstarport = p16.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p1 and p3.IsAlliedWith(p1) then
				fact = p1.GetActorsByType("fact")
				pyle = p1.GetActorsByType("pyle")
				hand = p1.GetActorsByType("hand")
				afld = p1.GetActorsByType("afld")
				weap = p1.GetActorsByType("weap")
				hpad = p1.GetActorsByType("hpad")
				rafact = p1.GetActorsByType("rafact")
				tent = p1.GetActorsByType("tent")
				barr = p1.GetActorsByType("barr")
				raweapa = p1.GetActorsByType("raweapa")
				raweap = p1.GetActorsByType("raweap")
				rahpad = p1.GetActorsByType("rahpad")
				raafld = p1.GetActorsByType("raafld")
				hq = p1.GetActorsByType("2100hq")
				fy1 = p1.GetActorsByType("2100fy1")
				fy2 = p1.GetActorsByType("2100fy2")
				sccommandcenter = p1.GetActorsByType("sccommandcenter")
				scbarracks = p1.GetActorsByType("scbarracks")
				scfactory = p1.GetActorsByType("scfactory")
				scstarport = p1.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p2 and p3.IsAlliedWith(p2) then
				fact = p2.GetActorsByType("fact")
				pyle = p2.GetActorsByType("pyle")
				hand = p2.GetActorsByType("hand")
				afld = p2.GetActorsByType("afld")
				weap = p2.GetActorsByType("weap")
				hpad = p2.GetActorsByType("hpad")
				rafact = p2.GetActorsByType("rafact")
				tent = p2.GetActorsByType("tent")
				barr = p2.GetActorsByType("barr")
				raweapa = p2.GetActorsByType("raweapa")
				raweap = p2.GetActorsByType("raweap")
				rahpad = p2.GetActorsByType("rahpad")
				raafld = p2.GetActorsByType("raafld")
				hq = p2.GetActorsByType("2100hq")
				fy1 = p2.GetActorsByType("2100fy1")
				fy2 = p2.GetActorsByType("2100fy2")
				sccommandcenter = p2.GetActorsByType("sccommandcenter")
				scbarracks = p2.GetActorsByType("scbarracks")
				scfactory = p2.GetActorsByType("scfactory")
				scstarport = p2.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p3cash()
	end)
end
p4cash = function()
	if p4 and p4.IsBot then
		citybottruck = p4.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p5 and p4.IsAlliedWith(p5) then
				fact = p5.GetActorsByType("fact")
				pyle = p5.GetActorsByType("pyle")
				hand = p5.GetActorsByType("hand")
				afld = p5.GetActorsByType("afld")
				weap = p5.GetActorsByType("weap")
				hpad = p5.GetActorsByType("hpad")
				rafact = p5.GetActorsByType("rafact")
				tent = p5.GetActorsByType("tent")
				barr = p5.GetActorsByType("barr")
				raweapa = p5.GetActorsByType("raweapa")
				raweap = p5.GetActorsByType("raweap")
				rahpad = p5.GetActorsByType("rahpad")
				raafld = p5.GetActorsByType("raafld")
				hq = p5.GetActorsByType("2100hq")
				fy1 = p5.GetActorsByType("2100fy1")
				fy2 = p5.GetActorsByType("2100fy2")
				sccommandcenter = p5.GetActorsByType("sccommandcenter")
				scbarracks = p5.GetActorsByType("scbarracks")
				scfactory = p5.GetActorsByType("scfactory")
				scstarport = p5.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p6 and p4.IsAlliedWith(p6) then
				fact = p6.GetActorsByType("fact")
				pyle = p6.GetActorsByType("pyle")
				hand = p6.GetActorsByType("hand")
				afld = p6.GetActorsByType("afld")
				weap = p6.GetActorsByType("weap")
				hpad = p6.GetActorsByType("hpad")
				rafact = p6.GetActorsByType("rafact")
				tent = p6.GetActorsByType("tent")
				barr = p6.GetActorsByType("barr")
				raweapa = p6.GetActorsByType("raweapa")
				raweap = p6.GetActorsByType("raweap")
				rahpad = p6.GetActorsByType("rahpad")
				raafld = p6.GetActorsByType("raafld")
				hq = p6.GetActorsByType("2100hq")
				fy1 = p6.GetActorsByType("2100fy1")
				fy2 = p6.GetActorsByType("2100fy2")
				sccommandcenter = p6.GetActorsByType("sccommandcenter")
				scbarracks = p6.GetActorsByType("scbarracks")
				scfactory = p6.GetActorsByType("scfactory")
				scstarport = p6.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p7 and p4.IsAlliedWith(p7) then
				fact = p7.GetActorsByType("fact")
				pyle = p7.GetActorsByType("pyle")
				hand = p7.GetActorsByType("hand")
				afld = p7.GetActorsByType("afld")
				weap = p7.GetActorsByType("weap")
				hpad = p7.GetActorsByType("hpad")
				rafact = p7.GetActorsByType("rafact")
				tent = p7.GetActorsByType("tent")
				barr = p7.GetActorsByType("barr")
				raweapa = p7.GetActorsByType("raweapa")
				raweap = p7.GetActorsByType("raweap")
				rahpad = p7.GetActorsByType("rahpad")
				raafld = p7.GetActorsByType("raafld")
				hq = p7.GetActorsByType("2100hq")
				fy1 = p7.GetActorsByType("2100fy1")
				fy2 = p7.GetActorsByType("2100fy2")
				sccommandcenter = p7.GetActorsByType("sccommandcenter")
				scbarracks = p7.GetActorsByType("scbarracks")
				scfactory = p7.GetActorsByType("scfactory")
				scstarport = p7.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p8 and p4.IsAlliedWith(p8) then
				fact = p8.GetActorsByType("fact")
				pyle = p8.GetActorsByType("pyle")
				hand = p8.GetActorsByType("hand")
				afld = p8.GetActorsByType("afld")
				weap = p8.GetActorsByType("weap")
				hpad = p8.GetActorsByType("hpad")
				rafact = p8.GetActorsByType("rafact")
				tent = p8.GetActorsByType("tent")
				barr = p8.GetActorsByType("barr")
				raweapa = p8.GetActorsByType("raweapa")
				raweap = p8.GetActorsByType("raweap")
				rahpad = p8.GetActorsByType("rahpad")
				raafld = p8.GetActorsByType("raafld")
				hq = p8.GetActorsByType("2100hq")
				fy1 = p8.GetActorsByType("2100fy1")
				fy2 = p8.GetActorsByType("2100fy2")
				sccommandcenter = p8.GetActorsByType("sccommandcenter")
				scbarracks = p8.GetActorsByType("scbarracks")
				scfactory = p8.GetActorsByType("scfactory")
				scstarport = p8.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p9 and p4.IsAlliedWith(p9) then
				fact = p9.GetActorsByType("fact")
				pyle = p9.GetActorsByType("pyle")
				hand = p9.GetActorsByType("hand")
				afld = p9.GetActorsByType("afld")
				weap = p9.GetActorsByType("weap")
				hpad = p9.GetActorsByType("hpad")
				rafact = p9.GetActorsByType("rafact")
				tent = p9.GetActorsByType("tent")
				barr = p9.GetActorsByType("barr")
				raweapa = p9.GetActorsByType("raweapa")
				raweap = p9.GetActorsByType("raweap")
				rahpad = p9.GetActorsByType("rahpad")
				raafld = p9.GetActorsByType("raafld")
				hq = p9.GetActorsByType("2100hq")
				fy1 = p9.GetActorsByType("2100fy1")
				fy2 = p9.GetActorsByType("2100fy2")
				sccommandcenter = p9.GetActorsByType("sccommandcenter")
				scbarracks = p9.GetActorsByType("scbarracks")
				scfactory = p9.GetActorsByType("scfactory")
				scstarport = p9.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p10 and p4.IsAlliedWith(p10) then
				fact = p10.GetActorsByType("fact")
				pyle = p10.GetActorsByType("pyle")
				hand = p10.GetActorsByType("hand")
				afld = p10.GetActorsByType("afld")
				weap = p10.GetActorsByType("weap")
				hpad = p10.GetActorsByType("hpad")
				rafact = p10.GetActorsByType("rafact")
				tent = p10.GetActorsByType("tent")
				barr = p10.GetActorsByType("barr")
				raweapa = p10.GetActorsByType("raweapa")
				raweap = p10.GetActorsByType("raweap")
				rahpad = p10.GetActorsByType("rahpad")
				raafld = p10.GetActorsByType("raafld")
				hq = p10.GetActorsByType("2100hq")
				fy1 = p10.GetActorsByType("2100fy1")
				fy2 = p10.GetActorsByType("2100fy2")
				sccommandcenter = p10.GetActorsByType("sccommandcenter")
				scbarracks = p10.GetActorsByType("scbarracks")
				scfactory = p10.GetActorsByType("scfactory")
				scstarport = p10.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p11 and p4.IsAlliedWith(p11) then
				fact = p11.GetActorsByType("fact")
				pyle = p11.GetActorsByType("pyle")
				hand = p11.GetActorsByType("hand")
				afld = p11.GetActorsByType("afld")
				weap = p11.GetActorsByType("weap")
				hpad = p11.GetActorsByType("hpad")
				rafact = p11.GetActorsByType("rafact")
				tent = p11.GetActorsByType("tent")
				barr = p11.GetActorsByType("barr")
				raweapa = p11.GetActorsByType("raweapa")
				raweap = p11.GetActorsByType("raweap")
				rahpad = p11.GetActorsByType("rahpad")
				raafld = p11.GetActorsByType("raafld")
				hq = p11.GetActorsByType("2100hq")
				fy1 = p11.GetActorsByType("2100fy1")
				fy2 = p11.GetActorsByType("2100fy2")
				sccommandcenter = p11.GetActorsByType("sccommandcenter")
				scbarracks = p11.GetActorsByType("scbarracks")
				scfactory = p11.GetActorsByType("scfactory")
				scstarport = p11.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p12 and p4.IsAlliedWith(p12) then
				fact = p12.GetActorsByType("fact")
				pyle = p12.GetActorsByType("pyle")
				hand = p12.GetActorsByType("hand")
				afld = p12.GetActorsByType("afld")
				weap = p12.GetActorsByType("weap")
				hpad = p12.GetActorsByType("hpad")
				rafact = p12.GetActorsByType("rafact")
				tent = p12.GetActorsByType("tent")
				barr = p12.GetActorsByType("barr")
				raweapa = p12.GetActorsByType("raweapa")
				raweap = p12.GetActorsByType("raweap")
				rahpad = p12.GetActorsByType("rahpad")
				raafld = p12.GetActorsByType("raafld")
				hq = p12.GetActorsByType("2100hq")
				fy1 = p12.GetActorsByType("2100fy1")
				fy2 = p12.GetActorsByType("2100fy2")
				sccommandcenter = p12.GetActorsByType("sccommandcenter")
				scbarracks = p12.GetActorsByType("scbarracks")
				scfactory = p12.GetActorsByType("scfactory")
				scstarport = p12.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p13 and p4.IsAlliedWith(p13) then
				fact = p13.GetActorsByType("fact")
				pyle = p13.GetActorsByType("pyle")
				hand = p13.GetActorsByType("hand")
				afld = p13.GetActorsByType("afld")
				weap = p13.GetActorsByType("weap")
				hpad = p13.GetActorsByType("hpad")
				rafact = p13.GetActorsByType("rafact")
				tent = p13.GetActorsByType("tent")
				barr = p13.GetActorsByType("barr")
				raweapa = p13.GetActorsByType("raweapa")
				raweap = p13.GetActorsByType("raweap")
				rahpad = p13.GetActorsByType("rahpad")
				raafld = p13.GetActorsByType("raafld")
				hq = p13.GetActorsByType("2100hq")
				fy1 = p13.GetActorsByType("2100fy1")
				fy2 = p13.GetActorsByType("2100fy2")
				sccommandcenter = p13.GetActorsByType("sccommandcenter")
				scbarracks = p13.GetActorsByType("scbarracks")
				scfactory = p13.GetActorsByType("scfactory")
				scstarport = p13.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p14 and p4.IsAlliedWith(p14) then
				fact = p14.GetActorsByType("fact")
				pyle = p14.GetActorsByType("pyle")
				hand = p14.GetActorsByType("hand")
				afld = p14.GetActorsByType("afld")
				weap = p14.GetActorsByType("weap")
				hpad = p14.GetActorsByType("hpad")
				rafact = p14.GetActorsByType("rafact")
				tent = p14.GetActorsByType("tent")
				barr = p14.GetActorsByType("barr")
				raweapa = p14.GetActorsByType("raweapa")
				raweap = p14.GetActorsByType("raweap")
				rahpad = p14.GetActorsByType("rahpad")
				raafld = p14.GetActorsByType("raafld")
				hq = p14.GetActorsByType("2100hq")
				fy1 = p14.GetActorsByType("2100fy1")
				fy2 = p14.GetActorsByType("2100fy2")
				sccommandcenter = p14.GetActorsByType("sccommandcenter")
				scbarracks = p14.GetActorsByType("scbarracks")
				scfactory = p14.GetActorsByType("scfactory")
				scstarport = p14.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p15 and p4.IsAlliedWith(p15) then
				fact = p15.GetActorsByType("fact")
				pyle = p15.GetActorsByType("pyle")
				hand = p15.GetActorsByType("hand")
				afld = p15.GetActorsByType("afld")
				weap = p15.GetActorsByType("weap")
				hpad = p15.GetActorsByType("hpad")
				rafact = p15.GetActorsByType("rafact")
				tent = p15.GetActorsByType("tent")
				barr = p15.GetActorsByType("barr")
				raweapa = p15.GetActorsByType("raweapa")
				raweap = p15.GetActorsByType("raweap")
				rahpad = p15.GetActorsByType("rahpad")
				raafld = p15.GetActorsByType("raafld")
				hq = p15.GetActorsByType("2100hq")
				fy1 = p15.GetActorsByType("2100fy1")
				fy2 = p15.GetActorsByType("2100fy2")
				sccommandcenter = p15.GetActorsByType("sccommandcenter")
				scbarracks = p15.GetActorsByType("scbarracks")
				scfactory = p15.GetActorsByType("scfactory")
				scstarport = p15.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p16 and p4.IsAlliedWith(p16) then
				fact = p16.GetActorsByType("fact")
				pyle = p16.GetActorsByType("pyle")
				hand = p16.GetActorsByType("hand")
				afld = p16.GetActorsByType("afld")
				weap = p16.GetActorsByType("weap")
				hpad = p16.GetActorsByType("hpad")
				rafact = p16.GetActorsByType("rafact")
				tent = p16.GetActorsByType("tent")
				barr = p16.GetActorsByType("barr")
				raweapa = p16.GetActorsByType("raweapa")
				raweap = p16.GetActorsByType("raweap")
				rahpad = p16.GetActorsByType("rahpad")
				raafld = p16.GetActorsByType("raafld")
				hq = p16.GetActorsByType("2100hq")
				fy1 = p16.GetActorsByType("2100fy1")
				fy2 = p16.GetActorsByType("2100fy2")
				sccommandcenter = p16.GetActorsByType("sccommandcenter")
				scbarracks = p16.GetActorsByType("scbarracks")
				scfactory = p16.GetActorsByType("scfactory")
				scstarport = p16.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p1 and p4.IsAlliedWith(p1) then
				fact = p1.GetActorsByType("fact")
				pyle = p1.GetActorsByType("pyle")
				hand = p1.GetActorsByType("hand")
				afld = p1.GetActorsByType("afld")
				weap = p1.GetActorsByType("weap")
				hpad = p1.GetActorsByType("hpad")
				rafact = p1.GetActorsByType("rafact")
				tent = p1.GetActorsByType("tent")
				barr = p1.GetActorsByType("barr")
				raweapa = p1.GetActorsByType("raweapa")
				raweap = p1.GetActorsByType("raweap")
				rahpad = p1.GetActorsByType("rahpad")
				raafld = p1.GetActorsByType("raafld")
				hq = p1.GetActorsByType("2100hq")
				fy1 = p1.GetActorsByType("2100fy1")
				fy2 = p1.GetActorsByType("2100fy2")
				sccommandcenter = p1.GetActorsByType("sccommandcenter")
				scbarracks = p1.GetActorsByType("scbarracks")
				scfactory = p1.GetActorsByType("scfactory")
				scstarport = p1.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p2 and p4.IsAlliedWith(p2) then
				fact = p2.GetActorsByType("fact")
				pyle = p2.GetActorsByType("pyle")
				hand = p2.GetActorsByType("hand")
				afld = p2.GetActorsByType("afld")
				weap = p2.GetActorsByType("weap")
				hpad = p2.GetActorsByType("hpad")
				rafact = p2.GetActorsByType("rafact")
				tent = p2.GetActorsByType("tent")
				barr = p2.GetActorsByType("barr")
				raweapa = p2.GetActorsByType("raweapa")
				raweap = p2.GetActorsByType("raweap")
				rahpad = p2.GetActorsByType("rahpad")
				raafld = p2.GetActorsByType("raafld")
				hq = p2.GetActorsByType("2100hq")
				fy1 = p2.GetActorsByType("2100fy1")
				fy2 = p2.GetActorsByType("2100fy2")
				sccommandcenter = p2.GetActorsByType("sccommandcenter")
				scbarracks = p2.GetActorsByType("scbarracks")
				scfactory = p2.GetActorsByType("scfactory")
				scstarport = p2.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p3 and p4.IsAlliedWith(p3) then
				fact = p3.GetActorsByType("fact")
				pyle = p3.GetActorsByType("pyle")
				hand = p3.GetActorsByType("hand")
				afld = p3.GetActorsByType("afld")
				weap = p3.GetActorsByType("weap")
				hpad = p3.GetActorsByType("hpad")
				rafact = p3.GetActorsByType("rafact")
				tent = p3.GetActorsByType("tent")
				barr = p3.GetActorsByType("barr")
				raweapa = p3.GetActorsByType("raweapa")
				raweap = p3.GetActorsByType("raweap")
				rahpad = p3.GetActorsByType("rahpad")
				raafld = p3.GetActorsByType("raafld")
				hq = p3.GetActorsByType("2100hq")
				fy1 = p3.GetActorsByType("2100fy1")
				fy2 = p3.GetActorsByType("2100fy2")
				sccommandcenter = p3.GetActorsByType("sccommandcenter")
				scbarracks = p3.GetActorsByType("scbarracks")
				scfactory = p3.GetActorsByType("scfactory")
				scstarport = p3.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p4cash()
	end)
end
p5cash = function()
	if p5 and p5.IsBot then
		citybottruck = p5.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p6 and p5.IsAlliedWith(p6) then
				fact = p6.GetActorsByType("fact")
				pyle = p6.GetActorsByType("pyle")
				hand = p6.GetActorsByType("hand")
				afld = p6.GetActorsByType("afld")
				weap = p6.GetActorsByType("weap")
				hpad = p6.GetActorsByType("hpad")
				rafact = p6.GetActorsByType("rafact")
				tent = p6.GetActorsByType("tent")
				barr = p6.GetActorsByType("barr")
				raweapa = p6.GetActorsByType("raweapa")
				raweap = p6.GetActorsByType("raweap")
				rahpad = p6.GetActorsByType("rahpad")
				raafld = p6.GetActorsByType("raafld")
				hq = p6.GetActorsByType("2100hq")
				fy1 = p6.GetActorsByType("2100fy1")
				fy2 = p6.GetActorsByType("2100fy2")
				sccommandcenter = p6.GetActorsByType("sccommandcenter")
				scbarracks = p6.GetActorsByType("scbarracks")
				scfactory = p6.GetActorsByType("scfactory")
				scstarport = p6.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p7 and p5.IsAlliedWith(p7) then
				fact = p7.GetActorsByType("fact")
				pyle = p7.GetActorsByType("pyle")
				hand = p7.GetActorsByType("hand")
				afld = p7.GetActorsByType("afld")
				weap = p7.GetActorsByType("weap")
				hpad = p7.GetActorsByType("hpad")
				rafact = p7.GetActorsByType("rafact")
				tent = p7.GetActorsByType("tent")
				barr = p7.GetActorsByType("barr")
				raweapa = p7.GetActorsByType("raweapa")
				raweap = p7.GetActorsByType("raweap")
				rahpad = p7.GetActorsByType("rahpad")
				raafld = p7.GetActorsByType("raafld")
				hq = p7.GetActorsByType("2100hq")
				fy1 = p7.GetActorsByType("2100fy1")
				fy2 = p7.GetActorsByType("2100fy2")
				sccommandcenter = p7.GetActorsByType("sccommandcenter")
				scbarracks = p7.GetActorsByType("scbarracks")
				scfactory = p7.GetActorsByType("scfactory")
				scstarport = p7.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p8 and p5.IsAlliedWith(p8) then
				fact = p8.GetActorsByType("fact")
				pyle = p8.GetActorsByType("pyle")
				hand = p8.GetActorsByType("hand")
				afld = p8.GetActorsByType("afld")
				weap = p8.GetActorsByType("weap")
				hpad = p8.GetActorsByType("hpad")
				rafact = p8.GetActorsByType("rafact")
				tent = p8.GetActorsByType("tent")
				barr = p8.GetActorsByType("barr")
				raweapa = p8.GetActorsByType("raweapa")
				raweap = p8.GetActorsByType("raweap")
				rahpad = p8.GetActorsByType("rahpad")
				raafld = p8.GetActorsByType("raafld")
				hq = p8.GetActorsByType("2100hq")
				fy1 = p8.GetActorsByType("2100fy1")
				fy2 = p8.GetActorsByType("2100fy2")
				sccommandcenter = p8.GetActorsByType("sccommandcenter")
				scbarracks = p8.GetActorsByType("scbarracks")
				scfactory = p8.GetActorsByType("scfactory")
				scstarport = p8.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p9 and p5.IsAlliedWith(p9) then
				fact = p9.GetActorsByType("fact")
				pyle = p9.GetActorsByType("pyle")
				hand = p9.GetActorsByType("hand")
				afld = p9.GetActorsByType("afld")
				weap = p9.GetActorsByType("weap")
				hpad = p9.GetActorsByType("hpad")
				rafact = p9.GetActorsByType("rafact")
				tent = p9.GetActorsByType("tent")
				barr = p9.GetActorsByType("barr")
				raweapa = p9.GetActorsByType("raweapa")
				raweap = p9.GetActorsByType("raweap")
				rahpad = p9.GetActorsByType("rahpad")
				raafld = p9.GetActorsByType("raafld")
				hq = p9.GetActorsByType("2100hq")
				fy1 = p9.GetActorsByType("2100fy1")
				fy2 = p9.GetActorsByType("2100fy2")
				sccommandcenter = p9.GetActorsByType("sccommandcenter")
				scbarracks = p9.GetActorsByType("scbarracks")
				scfactory = p9.GetActorsByType("scfactory")
				scstarport = p9.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p10 and p5.IsAlliedWith(p10) then
				fact = p10.GetActorsByType("fact")
				pyle = p10.GetActorsByType("pyle")
				hand = p10.GetActorsByType("hand")
				afld = p10.GetActorsByType("afld")
				weap = p10.GetActorsByType("weap")
				hpad = p10.GetActorsByType("hpad")
				rafact = p10.GetActorsByType("rafact")
				tent = p10.GetActorsByType("tent")
				barr = p10.GetActorsByType("barr")
				raweapa = p10.GetActorsByType("raweapa")
				raweap = p10.GetActorsByType("raweap")
				rahpad = p10.GetActorsByType("rahpad")
				raafld = p10.GetActorsByType("raafld")
				hq = p10.GetActorsByType("2100hq")
				fy1 = p10.GetActorsByType("2100fy1")
				fy2 = p10.GetActorsByType("2100fy2")
				sccommandcenter = p10.GetActorsByType("sccommandcenter")
				scbarracks = p10.GetActorsByType("scbarracks")
				scfactory = p10.GetActorsByType("scfactory")
				scstarport = p10.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p11 and p5.IsAlliedWith(p11) then
				fact = p11.GetActorsByType("fact")
				pyle = p11.GetActorsByType("pyle")
				hand = p11.GetActorsByType("hand")
				afld = p11.GetActorsByType("afld")
				weap = p11.GetActorsByType("weap")
				hpad = p11.GetActorsByType("hpad")
				rafact = p11.GetActorsByType("rafact")
				tent = p11.GetActorsByType("tent")
				barr = p11.GetActorsByType("barr")
				raweapa = p11.GetActorsByType("raweapa")
				raweap = p11.GetActorsByType("raweap")
				rahpad = p11.GetActorsByType("rahpad")
				raafld = p11.GetActorsByType("raafld")
				hq = p11.GetActorsByType("2100hq")
				fy1 = p11.GetActorsByType("2100fy1")
				fy2 = p11.GetActorsByType("2100fy2")
				sccommandcenter = p11.GetActorsByType("sccommandcenter")
				scbarracks = p11.GetActorsByType("scbarracks")
				scfactory = p11.GetActorsByType("scfactory")
				scstarport = p11.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p12 and p5.IsAlliedWith(p12) then
				fact = p12.GetActorsByType("fact")
				pyle = p12.GetActorsByType("pyle")
				hand = p12.GetActorsByType("hand")
				afld = p12.GetActorsByType("afld")
				weap = p12.GetActorsByType("weap")
				hpad = p12.GetActorsByType("hpad")
				rafact = p12.GetActorsByType("rafact")
				tent = p12.GetActorsByType("tent")
				barr = p12.GetActorsByType("barr")
				raweapa = p12.GetActorsByType("raweapa")
				raweap = p12.GetActorsByType("raweap")
				rahpad = p12.GetActorsByType("rahpad")
				raafld = p12.GetActorsByType("raafld")
				hq = p12.GetActorsByType("2100hq")
				fy1 = p12.GetActorsByType("2100fy1")
				fy2 = p12.GetActorsByType("2100fy2")
				sccommandcenter = p12.GetActorsByType("sccommandcenter")
				scbarracks = p12.GetActorsByType("scbarracks")
				scfactory = p12.GetActorsByType("scfactory")
				scstarport = p12.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p13 and p5.IsAlliedWith(p13) then
				fact = p13.GetActorsByType("fact")
				pyle = p13.GetActorsByType("pyle")
				hand = p13.GetActorsByType("hand")
				afld = p13.GetActorsByType("afld")
				weap = p13.GetActorsByType("weap")
				hpad = p13.GetActorsByType("hpad")
				rafact = p13.GetActorsByType("rafact")
				tent = p13.GetActorsByType("tent")
				barr = p13.GetActorsByType("barr")
				raweapa = p13.GetActorsByType("raweapa")
				raweap = p13.GetActorsByType("raweap")
				rahpad = p13.GetActorsByType("rahpad")
				raafld = p13.GetActorsByType("raafld")
				hq = p13.GetActorsByType("2100hq")
				fy1 = p13.GetActorsByType("2100fy1")
				fy2 = p13.GetActorsByType("2100fy2")
				sccommandcenter = p13.GetActorsByType("sccommandcenter")
				scbarracks = p13.GetActorsByType("scbarracks")
				scfactory = p13.GetActorsByType("scfactory")
				scstarport = p13.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p14 and p5.IsAlliedWith(p14) then
				fact = p14.GetActorsByType("fact")
				pyle = p14.GetActorsByType("pyle")
				hand = p14.GetActorsByType("hand")
				afld = p14.GetActorsByType("afld")
				weap = p14.GetActorsByType("weap")
				hpad = p14.GetActorsByType("hpad")
				rafact = p14.GetActorsByType("rafact")
				tent = p14.GetActorsByType("tent")
				barr = p14.GetActorsByType("barr")
				raweapa = p14.GetActorsByType("raweapa")
				raweap = p14.GetActorsByType("raweap")
				rahpad = p14.GetActorsByType("rahpad")
				raafld = p14.GetActorsByType("raafld")
				hq = p14.GetActorsByType("2100hq")
				fy1 = p14.GetActorsByType("2100fy1")
				fy2 = p14.GetActorsByType("2100fy2")
				sccommandcenter = p14.GetActorsByType("sccommandcenter")
				scbarracks = p14.GetActorsByType("scbarracks")
				scfactory = p14.GetActorsByType("scfactory")
				scstarport = p14.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p15 and p5.IsAlliedWith(p15) then
				fact = p15.GetActorsByType("fact")
				pyle = p15.GetActorsByType("pyle")
				hand = p15.GetActorsByType("hand")
				afld = p15.GetActorsByType("afld")
				weap = p15.GetActorsByType("weap")
				hpad = p15.GetActorsByType("hpad")
				rafact = p15.GetActorsByType("rafact")
				tent = p15.GetActorsByType("tent")
				barr = p15.GetActorsByType("barr")
				raweapa = p15.GetActorsByType("raweapa")
				raweap = p15.GetActorsByType("raweap")
				rahpad = p15.GetActorsByType("rahpad")
				raafld = p15.GetActorsByType("raafld")
				hq = p15.GetActorsByType("2100hq")
				fy1 = p15.GetActorsByType("2100fy1")
				fy2 = p15.GetActorsByType("2100fy2")
				sccommandcenter = p15.GetActorsByType("sccommandcenter")
				scbarracks = p15.GetActorsByType("scbarracks")
				scfactory = p15.GetActorsByType("scfactory")
				scstarport = p15.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p16 and p5.IsAlliedWith(p16) then
				fact = p16.GetActorsByType("fact")
				pyle = p16.GetActorsByType("pyle")
				hand = p16.GetActorsByType("hand")
				afld = p16.GetActorsByType("afld")
				weap = p16.GetActorsByType("weap")
				hpad = p16.GetActorsByType("hpad")
				rafact = p16.GetActorsByType("rafact")
				tent = p16.GetActorsByType("tent")
				barr = p16.GetActorsByType("barr")
				raweapa = p16.GetActorsByType("raweapa")
				raweap = p16.GetActorsByType("raweap")
				rahpad = p16.GetActorsByType("rahpad")
				raafld = p16.GetActorsByType("raafld")
				hq = p16.GetActorsByType("2100hq")
				fy1 = p16.GetActorsByType("2100fy1")
				fy2 = p16.GetActorsByType("2100fy2")
				sccommandcenter = p16.GetActorsByType("sccommandcenter")
				scbarracks = p16.GetActorsByType("scbarracks")
				scfactory = p16.GetActorsByType("scfactory")
				scstarport = p16.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p1 and p5.IsAlliedWith(p1) then
				fact = p1.GetActorsByType("fact")
				pyle = p1.GetActorsByType("pyle")
				hand = p1.GetActorsByType("hand")
				afld = p1.GetActorsByType("afld")
				weap = p1.GetActorsByType("weap")
				hpad = p1.GetActorsByType("hpad")
				rafact = p1.GetActorsByType("rafact")
				tent = p1.GetActorsByType("tent")
				barr = p1.GetActorsByType("barr")
				raweapa = p1.GetActorsByType("raweapa")
				raweap = p1.GetActorsByType("raweap")
				rahpad = p1.GetActorsByType("rahpad")
				raafld = p1.GetActorsByType("raafld")
				hq = p1.GetActorsByType("2100hq")
				fy1 = p1.GetActorsByType("2100fy1")
				fy2 = p1.GetActorsByType("2100fy2")
				sccommandcenter = p1.GetActorsByType("sccommandcenter")
				scbarracks = p1.GetActorsByType("scbarracks")
				scfactory = p1.GetActorsByType("scfactory")
				scstarport = p1.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p2 and p5.IsAlliedWith(p2) then
				fact = p2.GetActorsByType("fact")
				pyle = p2.GetActorsByType("pyle")
				hand = p2.GetActorsByType("hand")
				afld = p2.GetActorsByType("afld")
				weap = p2.GetActorsByType("weap")
				hpad = p2.GetActorsByType("hpad")
				rafact = p2.GetActorsByType("rafact")
				tent = p2.GetActorsByType("tent")
				barr = p2.GetActorsByType("barr")
				raweapa = p2.GetActorsByType("raweapa")
				raweap = p2.GetActorsByType("raweap")
				rahpad = p2.GetActorsByType("rahpad")
				raafld = p2.GetActorsByType("raafld")
				hq = p2.GetActorsByType("2100hq")
				fy1 = p2.GetActorsByType("2100fy1")
				fy2 = p2.GetActorsByType("2100fy2")
				sccommandcenter = p2.GetActorsByType("sccommandcenter")
				scbarracks = p2.GetActorsByType("scbarracks")
				scfactory = p2.GetActorsByType("scfactory")
				scstarport = p2.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p3 and p5.IsAlliedWith(p3) then
				fact = p3.GetActorsByType("fact")
				pyle = p3.GetActorsByType("pyle")
				hand = p3.GetActorsByType("hand")
				afld = p3.GetActorsByType("afld")
				weap = p3.GetActorsByType("weap")
				hpad = p3.GetActorsByType("hpad")
				rafact = p3.GetActorsByType("rafact")
				tent = p3.GetActorsByType("tent")
				barr = p3.GetActorsByType("barr")
				raweapa = p3.GetActorsByType("raweapa")
				raweap = p3.GetActorsByType("raweap")
				rahpad = p3.GetActorsByType("rahpad")
				raafld = p3.GetActorsByType("raafld")
				hq = p3.GetActorsByType("2100hq")
				fy1 = p3.GetActorsByType("2100fy1")
				fy2 = p3.GetActorsByType("2100fy2")
				sccommandcenter = p3.GetActorsByType("sccommandcenter")
				scbarracks = p3.GetActorsByType("scbarracks")
				scfactory = p3.GetActorsByType("scfactory")
				scstarport = p3.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p4 and p5.IsAlliedWith(p4) then
				fact = p4.GetActorsByType("fact")
				pyle = p4.GetActorsByType("pyle")
				hand = p4.GetActorsByType("hand")
				afld = p4.GetActorsByType("afld")
				weap = p4.GetActorsByType("weap")
				hpad = p4.GetActorsByType("hpad")
				rafact = p4.GetActorsByType("rafact")
				tent = p4.GetActorsByType("tent")
				barr = p4.GetActorsByType("barr")
				raweapa = p4.GetActorsByType("raweapa")
				raweap = p4.GetActorsByType("raweap")
				rahpad = p4.GetActorsByType("rahpad")
				raafld = p4.GetActorsByType("raafld")
				hq = p4.GetActorsByType("2100hq")
				fy1 = p4.GetActorsByType("2100fy1")
				fy2 = p4.GetActorsByType("2100fy2")
				sccommandcenter = p4.GetActorsByType("sccommandcenter")
				scbarracks = p4.GetActorsByType("scbarracks")
				scfactory = p4.GetActorsByType("scfactory")
				scstarport = p4.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p5cash()
	end)
end
p6cash = function()
	if p6 and p6.IsBot then
		citybottruck = p6.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p7 and p6.IsAlliedWith(p7) then
				fact = p7.GetActorsByType("fact")
				pyle = p7.GetActorsByType("pyle")
				hand = p7.GetActorsByType("hand")
				afld = p7.GetActorsByType("afld")
				weap = p7.GetActorsByType("weap")
				hpad = p7.GetActorsByType("hpad")
				rafact = p7.GetActorsByType("rafact")
				tent = p7.GetActorsByType("tent")
				barr = p7.GetActorsByType("barr")
				raweapa = p7.GetActorsByType("raweapa")
				raweap = p7.GetActorsByType("raweap")
				rahpad = p7.GetActorsByType("rahpad")
				raafld = p7.GetActorsByType("raafld")
				hq = p7.GetActorsByType("2100hq")
				fy1 = p7.GetActorsByType("2100fy1")
				fy2 = p7.GetActorsByType("2100fy2")
				sccommandcenter = p7.GetActorsByType("sccommandcenter")
				scbarracks = p7.GetActorsByType("scbarracks")
				scfactory = p7.GetActorsByType("scfactory")
				scstarport = p7.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p8 and p6.IsAlliedWith(p8) then
				fact = p8.GetActorsByType("fact")
				pyle = p8.GetActorsByType("pyle")
				hand = p8.GetActorsByType("hand")
				afld = p8.GetActorsByType("afld")
				weap = p8.GetActorsByType("weap")
				hpad = p8.GetActorsByType("hpad")
				rafact = p8.GetActorsByType("rafact")
				tent = p8.GetActorsByType("tent")
				barr = p8.GetActorsByType("barr")
				raweapa = p8.GetActorsByType("raweapa")
				raweap = p8.GetActorsByType("raweap")
				rahpad = p8.GetActorsByType("rahpad")
				raafld = p8.GetActorsByType("raafld")
				hq = p8.GetActorsByType("2100hq")
				fy1 = p8.GetActorsByType("2100fy1")
				fy2 = p8.GetActorsByType("2100fy2")
				sccommandcenter = p8.GetActorsByType("sccommandcenter")
				scbarracks = p8.GetActorsByType("scbarracks")
				scfactory = p8.GetActorsByType("scfactory")
				scstarport = p8.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p9 and p6.IsAlliedWith(p9) then
				fact = p9.GetActorsByType("fact")
				pyle = p9.GetActorsByType("pyle")
				hand = p9.GetActorsByType("hand")
				afld = p9.GetActorsByType("afld")
				weap = p9.GetActorsByType("weap")
				hpad = p9.GetActorsByType("hpad")
				rafact = p9.GetActorsByType("rafact")
				tent = p9.GetActorsByType("tent")
				barr = p9.GetActorsByType("barr")
				raweapa = p9.GetActorsByType("raweapa")
				raweap = p9.GetActorsByType("raweap")
				rahpad = p9.GetActorsByType("rahpad")
				raafld = p9.GetActorsByType("raafld")
				hq = p9.GetActorsByType("2100hq")
				fy1 = p9.GetActorsByType("2100fy1")
				fy2 = p9.GetActorsByType("2100fy2")
				sccommandcenter = p9.GetActorsByType("sccommandcenter")
				scbarracks = p9.GetActorsByType("scbarracks")
				scfactory = p9.GetActorsByType("scfactory")
				scstarport = p9.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p10 and p6.IsAlliedWith(p10) then
				fact = p10.GetActorsByType("fact")
				pyle = p10.GetActorsByType("pyle")
				hand = p10.GetActorsByType("hand")
				afld = p10.GetActorsByType("afld")
				weap = p10.GetActorsByType("weap")
				hpad = p10.GetActorsByType("hpad")
				rafact = p10.GetActorsByType("rafact")
				tent = p10.GetActorsByType("tent")
				barr = p10.GetActorsByType("barr")
				raweapa = p10.GetActorsByType("raweapa")
				raweap = p10.GetActorsByType("raweap")
				rahpad = p10.GetActorsByType("rahpad")
				raafld = p10.GetActorsByType("raafld")
				hq = p10.GetActorsByType("2100hq")
				fy1 = p10.GetActorsByType("2100fy1")
				fy2 = p10.GetActorsByType("2100fy2")
				sccommandcenter = p10.GetActorsByType("sccommandcenter")
				scbarracks = p10.GetActorsByType("scbarracks")
				scfactory = p10.GetActorsByType("scfactory")
				scstarport = p10.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p11 and p6.IsAlliedWith(p11) then
				fact = p11.GetActorsByType("fact")
				pyle = p11.GetActorsByType("pyle")
				hand = p11.GetActorsByType("hand")
				afld = p11.GetActorsByType("afld")
				weap = p11.GetActorsByType("weap")
				hpad = p11.GetActorsByType("hpad")
				rafact = p11.GetActorsByType("rafact")
				tent = p11.GetActorsByType("tent")
				barr = p11.GetActorsByType("barr")
				raweapa = p11.GetActorsByType("raweapa")
				raweap = p11.GetActorsByType("raweap")
				rahpad = p11.GetActorsByType("rahpad")
				raafld = p11.GetActorsByType("raafld")
				hq = p11.GetActorsByType("2100hq")
				fy1 = p11.GetActorsByType("2100fy1")
				fy2 = p11.GetActorsByType("2100fy2")
				sccommandcenter = p11.GetActorsByType("sccommandcenter")
				scbarracks = p11.GetActorsByType("scbarracks")
				scfactory = p11.GetActorsByType("scfactory")
				scstarport = p11.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p12 and p6.IsAlliedWith(p12) then
				fact = p12.GetActorsByType("fact")
				pyle = p12.GetActorsByType("pyle")
				hand = p12.GetActorsByType("hand")
				afld = p12.GetActorsByType("afld")
				weap = p12.GetActorsByType("weap")
				hpad = p12.GetActorsByType("hpad")
				rafact = p12.GetActorsByType("rafact")
				tent = p12.GetActorsByType("tent")
				barr = p12.GetActorsByType("barr")
				raweapa = p12.GetActorsByType("raweapa")
				raweap = p12.GetActorsByType("raweap")
				rahpad = p12.GetActorsByType("rahpad")
				raafld = p12.GetActorsByType("raafld")
				hq = p12.GetActorsByType("2100hq")
				fy1 = p12.GetActorsByType("2100fy1")
				fy2 = p12.GetActorsByType("2100fy2")
				sccommandcenter = p12.GetActorsByType("sccommandcenter")
				scbarracks = p12.GetActorsByType("scbarracks")
				scfactory = p12.GetActorsByType("scfactory")
				scstarport = p12.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p13 and p6.IsAlliedWith(p13) then
				fact = p13.GetActorsByType("fact")
				pyle = p13.GetActorsByType("pyle")
				hand = p13.GetActorsByType("hand")
				afld = p13.GetActorsByType("afld")
				weap = p13.GetActorsByType("weap")
				hpad = p13.GetActorsByType("hpad")
				rafact = p13.GetActorsByType("rafact")
				tent = p13.GetActorsByType("tent")
				barr = p13.GetActorsByType("barr")
				raweapa = p13.GetActorsByType("raweapa")
				raweap = p13.GetActorsByType("raweap")
				rahpad = p13.GetActorsByType("rahpad")
				raafld = p13.GetActorsByType("raafld")
				hq = p13.GetActorsByType("2100hq")
				fy1 = p13.GetActorsByType("2100fy1")
				fy2 = p13.GetActorsByType("2100fy2")
				sccommandcenter = p13.GetActorsByType("sccommandcenter")
				scbarracks = p13.GetActorsByType("scbarracks")
				scfactory = p13.GetActorsByType("scfactory")
				scstarport = p13.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p14 and p6.IsAlliedWith(p14) then
				fact = p14.GetActorsByType("fact")
				pyle = p14.GetActorsByType("pyle")
				hand = p14.GetActorsByType("hand")
				afld = p14.GetActorsByType("afld")
				weap = p14.GetActorsByType("weap")
				hpad = p14.GetActorsByType("hpad")
				rafact = p14.GetActorsByType("rafact")
				tent = p14.GetActorsByType("tent")
				barr = p14.GetActorsByType("barr")
				raweapa = p14.GetActorsByType("raweapa")
				raweap = p14.GetActorsByType("raweap")
				rahpad = p14.GetActorsByType("rahpad")
				raafld = p14.GetActorsByType("raafld")
				hq = p14.GetActorsByType("2100hq")
				fy1 = p14.GetActorsByType("2100fy1")
				fy2 = p14.GetActorsByType("2100fy2")
				sccommandcenter = p14.GetActorsByType("sccommandcenter")
				scbarracks = p14.GetActorsByType("scbarracks")
				scfactory = p14.GetActorsByType("scfactory")
				scstarport = p14.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p15 and p6.IsAlliedWith(p15) then
				fact = p15.GetActorsByType("fact")
				pyle = p15.GetActorsByType("pyle")
				hand = p15.GetActorsByType("hand")
				afld = p15.GetActorsByType("afld")
				weap = p15.GetActorsByType("weap")
				hpad = p15.GetActorsByType("hpad")
				rafact = p15.GetActorsByType("rafact")
				tent = p15.GetActorsByType("tent")
				barr = p15.GetActorsByType("barr")
				raweapa = p15.GetActorsByType("raweapa")
				raweap = p15.GetActorsByType("raweap")
				rahpad = p15.GetActorsByType("rahpad")
				raafld = p15.GetActorsByType("raafld")
				hq = p15.GetActorsByType("2100hq")
				fy1 = p15.GetActorsByType("2100fy1")
				fy2 = p15.GetActorsByType("2100fy2")
				sccommandcenter = p15.GetActorsByType("sccommandcenter")
				scbarracks = p15.GetActorsByType("scbarracks")
				scfactory = p15.GetActorsByType("scfactory")
				scstarport = p15.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p16 and p6.IsAlliedWith(p16) then
				fact = p16.GetActorsByType("fact")
				pyle = p16.GetActorsByType("pyle")
				hand = p16.GetActorsByType("hand")
				afld = p16.GetActorsByType("afld")
				weap = p16.GetActorsByType("weap")
				hpad = p16.GetActorsByType("hpad")
				rafact = p16.GetActorsByType("rafact")
				tent = p16.GetActorsByType("tent")
				barr = p16.GetActorsByType("barr")
				raweapa = p16.GetActorsByType("raweapa")
				raweap = p16.GetActorsByType("raweap")
				rahpad = p16.GetActorsByType("rahpad")
				raafld = p16.GetActorsByType("raafld")
				hq = p16.GetActorsByType("2100hq")
				fy1 = p16.GetActorsByType("2100fy1")
				fy2 = p16.GetActorsByType("2100fy2")
				sccommandcenter = p16.GetActorsByType("sccommandcenter")
				scbarracks = p16.GetActorsByType("scbarracks")
				scfactory = p16.GetActorsByType("scfactory")
				scstarport = p16.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p1 and p6.IsAlliedWith(p1) then
				fact = p1.GetActorsByType("fact")
				pyle = p1.GetActorsByType("pyle")
				hand = p1.GetActorsByType("hand")
				afld = p1.GetActorsByType("afld")
				weap = p1.GetActorsByType("weap")
				hpad = p1.GetActorsByType("hpad")
				rafact = p1.GetActorsByType("rafact")
				tent = p1.GetActorsByType("tent")
				barr = p1.GetActorsByType("barr")
				raweapa = p1.GetActorsByType("raweapa")
				raweap = p1.GetActorsByType("raweap")
				rahpad = p1.GetActorsByType("rahpad")
				raafld = p1.GetActorsByType("raafld")
				hq = p1.GetActorsByType("2100hq")
				fy1 = p1.GetActorsByType("2100fy1")
				fy2 = p1.GetActorsByType("2100fy2")
				sccommandcenter = p1.GetActorsByType("sccommandcenter")
				scbarracks = p1.GetActorsByType("scbarracks")
				scfactory = p1.GetActorsByType("scfactory")
				scstarport = p1.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p2 and p6.IsAlliedWith(p2) then
				fact = p2.GetActorsByType("fact")
				pyle = p2.GetActorsByType("pyle")
				hand = p2.GetActorsByType("hand")
				afld = p2.GetActorsByType("afld")
				weap = p2.GetActorsByType("weap")
				hpad = p2.GetActorsByType("hpad")
				rafact = p2.GetActorsByType("rafact")
				tent = p2.GetActorsByType("tent")
				barr = p2.GetActorsByType("barr")
				raweapa = p2.GetActorsByType("raweapa")
				raweap = p2.GetActorsByType("raweap")
				rahpad = p2.GetActorsByType("rahpad")
				raafld = p2.GetActorsByType("raafld")
				hq = p2.GetActorsByType("2100hq")
				fy1 = p2.GetActorsByType("2100fy1")
				fy2 = p2.GetActorsByType("2100fy2")
				sccommandcenter = p2.GetActorsByType("sccommandcenter")
				scbarracks = p2.GetActorsByType("scbarracks")
				scfactory = p2.GetActorsByType("scfactory")
				scstarport = p2.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p3 and p6.IsAlliedWith(p3) then
				fact = p3.GetActorsByType("fact")
				pyle = p3.GetActorsByType("pyle")
				hand = p3.GetActorsByType("hand")
				afld = p3.GetActorsByType("afld")
				weap = p3.GetActorsByType("weap")
				hpad = p3.GetActorsByType("hpad")
				rafact = p3.GetActorsByType("rafact")
				tent = p3.GetActorsByType("tent")
				barr = p3.GetActorsByType("barr")
				raweapa = p3.GetActorsByType("raweapa")
				raweap = p3.GetActorsByType("raweap")
				rahpad = p3.GetActorsByType("rahpad")
				raafld = p3.GetActorsByType("raafld")
				hq = p3.GetActorsByType("2100hq")
				fy1 = p3.GetActorsByType("2100fy1")
				fy2 = p3.GetActorsByType("2100fy2")
				sccommandcenter = p3.GetActorsByType("sccommandcenter")
				scbarracks = p3.GetActorsByType("scbarracks")
				scfactory = p3.GetActorsByType("scfactory")
				scstarport = p3.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p4 and p6.IsAlliedWith(p4) then
				fact = p4.GetActorsByType("fact")
				pyle = p4.GetActorsByType("pyle")
				hand = p4.GetActorsByType("hand")
				afld = p4.GetActorsByType("afld")
				weap = p4.GetActorsByType("weap")
				hpad = p4.GetActorsByType("hpad")
				rafact = p4.GetActorsByType("rafact")
				tent = p4.GetActorsByType("tent")
				barr = p4.GetActorsByType("barr")
				raweapa = p4.GetActorsByType("raweapa")
				raweap = p4.GetActorsByType("raweap")
				rahpad = p4.GetActorsByType("rahpad")
				raafld = p4.GetActorsByType("raafld")
				hq = p4.GetActorsByType("2100hq")
				fy1 = p4.GetActorsByType("2100fy1")
				fy2 = p4.GetActorsByType("2100fy2")
				sccommandcenter = p4.GetActorsByType("sccommandcenter")
				scbarracks = p4.GetActorsByType("scbarracks")
				scfactory = p4.GetActorsByType("scfactory")
				scstarport = p4.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p5 and p6.IsAlliedWith(p5) then
				fact = p5.GetActorsByType("fact")
				pyle = p5.GetActorsByType("pyle")
				hand = p5.GetActorsByType("hand")
				afld = p5.GetActorsByType("afld")
				weap = p5.GetActorsByType("weap")
				hpad = p5.GetActorsByType("hpad")
				rafact = p5.GetActorsByType("rafact")
				tent = p5.GetActorsByType("tent")
				barr = p5.GetActorsByType("barr")
				raweapa = p5.GetActorsByType("raweapa")
				raweap = p5.GetActorsByType("raweap")
				rahpad = p5.GetActorsByType("rahpad")
				raafld = p5.GetActorsByType("raafld")
				hq = p5.GetActorsByType("2100hq")
				fy1 = p5.GetActorsByType("2100fy1")
				fy2 = p5.GetActorsByType("2100fy2")
				sccommandcenter = p5.GetActorsByType("sccommandcenter")
				scbarracks = p5.GetActorsByType("scbarracks")
				scfactory = p5.GetActorsByType("scfactory")
				scstarport = p5.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p6cash()
	end)
end
p7cash = function()
	if p7 and p7.IsBot then
		citybottruck = p7.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p8 and p7.IsAlliedWith(p8) then
				fact = p8.GetActorsByType("fact")
				pyle = p8.GetActorsByType("pyle")
				hand = p8.GetActorsByType("hand")
				afld = p8.GetActorsByType("afld")
				weap = p8.GetActorsByType("weap")
				hpad = p8.GetActorsByType("hpad")
				rafact = p8.GetActorsByType("rafact")
				tent = p8.GetActorsByType("tent")
				barr = p8.GetActorsByType("barr")
				raweapa = p8.GetActorsByType("raweapa")
				raweap = p8.GetActorsByType("raweap")
				rahpad = p8.GetActorsByType("rahpad")
				raafld = p8.GetActorsByType("raafld")
				hq = p8.GetActorsByType("2100hq")
				fy1 = p8.GetActorsByType("2100fy1")
				fy2 = p8.GetActorsByType("2100fy2")
				sccommandcenter = p8.GetActorsByType("sccommandcenter")
				scbarracks = p8.GetActorsByType("scbarracks")
				scfactory = p8.GetActorsByType("scfactory")
				scstarport = p8.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p9 and p7.IsAlliedWith(p9) then
				fact = p9.GetActorsByType("fact")
				pyle = p9.GetActorsByType("pyle")
				hand = p9.GetActorsByType("hand")
				afld = p9.GetActorsByType("afld")
				weap = p9.GetActorsByType("weap")
				hpad = p9.GetActorsByType("hpad")
				rafact = p9.GetActorsByType("rafact")
				tent = p9.GetActorsByType("tent")
				barr = p9.GetActorsByType("barr")
				raweapa = p9.GetActorsByType("raweapa")
				raweap = p9.GetActorsByType("raweap")
				rahpad = p9.GetActorsByType("rahpad")
				raafld = p9.GetActorsByType("raafld")
				hq = p9.GetActorsByType("2100hq")
				fy1 = p9.GetActorsByType("2100fy1")
				fy2 = p9.GetActorsByType("2100fy2")
				sccommandcenter = p9.GetActorsByType("sccommandcenter")
				scbarracks = p9.GetActorsByType("scbarracks")
				scfactory = p9.GetActorsByType("scfactory")
				scstarport = p9.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p10 and p7.IsAlliedWith(p10) then
				fact = p10.GetActorsByType("fact")
				pyle = p10.GetActorsByType("pyle")
				hand = p10.GetActorsByType("hand")
				afld = p10.GetActorsByType("afld")
				weap = p10.GetActorsByType("weap")
				hpad = p10.GetActorsByType("hpad")
				rafact = p10.GetActorsByType("rafact")
				tent = p10.GetActorsByType("tent")
				barr = p10.GetActorsByType("barr")
				raweapa = p10.GetActorsByType("raweapa")
				raweap = p10.GetActorsByType("raweap")
				rahpad = p10.GetActorsByType("rahpad")
				raafld = p10.GetActorsByType("raafld")
				hq = p10.GetActorsByType("2100hq")
				fy1 = p10.GetActorsByType("2100fy1")
				fy2 = p10.GetActorsByType("2100fy2")
				sccommandcenter = p10.GetActorsByType("sccommandcenter")
				scbarracks = p10.GetActorsByType("scbarracks")
				scfactory = p10.GetActorsByType("scfactory")
				scstarport = p10.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p11 and p7.IsAlliedWith(p11) then
				fact = p11.GetActorsByType("fact")
				pyle = p11.GetActorsByType("pyle")
				hand = p11.GetActorsByType("hand")
				afld = p11.GetActorsByType("afld")
				weap = p11.GetActorsByType("weap")
				hpad = p11.GetActorsByType("hpad")
				rafact = p11.GetActorsByType("rafact")
				tent = p11.GetActorsByType("tent")
				barr = p11.GetActorsByType("barr")
				raweapa = p11.GetActorsByType("raweapa")
				raweap = p11.GetActorsByType("raweap")
				rahpad = p11.GetActorsByType("rahpad")
				raafld = p11.GetActorsByType("raafld")
				hq = p11.GetActorsByType("2100hq")
				fy1 = p11.GetActorsByType("2100fy1")
				fy2 = p11.GetActorsByType("2100fy2")
				sccommandcenter = p11.GetActorsByType("sccommandcenter")
				scbarracks = p11.GetActorsByType("scbarracks")
				scfactory = p11.GetActorsByType("scfactory")
				scstarport = p11.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p12 and p7.IsAlliedWith(p12) then
				fact = p12.GetActorsByType("fact")
				pyle = p12.GetActorsByType("pyle")
				hand = p12.GetActorsByType("hand")
				afld = p12.GetActorsByType("afld")
				weap = p12.GetActorsByType("weap")
				hpad = p12.GetActorsByType("hpad")
				rafact = p12.GetActorsByType("rafact")
				tent = p12.GetActorsByType("tent")
				barr = p12.GetActorsByType("barr")
				raweapa = p12.GetActorsByType("raweapa")
				raweap = p12.GetActorsByType("raweap")
				rahpad = p12.GetActorsByType("rahpad")
				raafld = p12.GetActorsByType("raafld")
				hq = p12.GetActorsByType("2100hq")
				fy1 = p12.GetActorsByType("2100fy1")
				fy2 = p12.GetActorsByType("2100fy2")
				sccommandcenter = p12.GetActorsByType("sccommandcenter")
				scbarracks = p12.GetActorsByType("scbarracks")
				scfactory = p12.GetActorsByType("scfactory")
				scstarport = p12.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p13 and p7.IsAlliedWith(p13) then
				fact = p13.GetActorsByType("fact")
				pyle = p13.GetActorsByType("pyle")
				hand = p13.GetActorsByType("hand")
				afld = p13.GetActorsByType("afld")
				weap = p13.GetActorsByType("weap")
				hpad = p13.GetActorsByType("hpad")
				rafact = p13.GetActorsByType("rafact")
				tent = p13.GetActorsByType("tent")
				barr = p13.GetActorsByType("barr")
				raweapa = p13.GetActorsByType("raweapa")
				raweap = p13.GetActorsByType("raweap")
				rahpad = p13.GetActorsByType("rahpad")
				raafld = p13.GetActorsByType("raafld")
				hq = p13.GetActorsByType("2100hq")
				fy1 = p13.GetActorsByType("2100fy1")
				fy2 = p13.GetActorsByType("2100fy2")
				sccommandcenter = p13.GetActorsByType("sccommandcenter")
				scbarracks = p13.GetActorsByType("scbarracks")
				scfactory = p13.GetActorsByType("scfactory")
				scstarport = p13.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p14 and p7.IsAlliedWith(p14) then
				fact = p14.GetActorsByType("fact")
				pyle = p14.GetActorsByType("pyle")
				hand = p14.GetActorsByType("hand")
				afld = p14.GetActorsByType("afld")
				weap = p14.GetActorsByType("weap")
				hpad = p14.GetActorsByType("hpad")
				rafact = p14.GetActorsByType("rafact")
				tent = p14.GetActorsByType("tent")
				barr = p14.GetActorsByType("barr")
				raweapa = p14.GetActorsByType("raweapa")
				raweap = p14.GetActorsByType("raweap")
				rahpad = p14.GetActorsByType("rahpad")
				raafld = p14.GetActorsByType("raafld")
				hq = p14.GetActorsByType("2100hq")
				fy1 = p14.GetActorsByType("2100fy1")
				fy2 = p14.GetActorsByType("2100fy2")
				sccommandcenter = p14.GetActorsByType("sccommandcenter")
				scbarracks = p14.GetActorsByType("scbarracks")
				scfactory = p14.GetActorsByType("scfactory")
				scstarport = p14.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p15 and p7.IsAlliedWith(p15) then
				fact = p15.GetActorsByType("fact")
				pyle = p15.GetActorsByType("pyle")
				hand = p15.GetActorsByType("hand")
				afld = p15.GetActorsByType("afld")
				weap = p15.GetActorsByType("weap")
				hpad = p15.GetActorsByType("hpad")
				rafact = p15.GetActorsByType("rafact")
				tent = p15.GetActorsByType("tent")
				barr = p15.GetActorsByType("barr")
				raweapa = p15.GetActorsByType("raweapa")
				raweap = p15.GetActorsByType("raweap")
				rahpad = p15.GetActorsByType("rahpad")
				raafld = p15.GetActorsByType("raafld")
				hq = p15.GetActorsByType("2100hq")
				fy1 = p15.GetActorsByType("2100fy1")
				fy2 = p15.GetActorsByType("2100fy2")
				sccommandcenter = p15.GetActorsByType("sccommandcenter")
				scbarracks = p15.GetActorsByType("scbarracks")
				scfactory = p15.GetActorsByType("scfactory")
				scstarport = p15.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p16 and p7.IsAlliedWith(p16) then
				fact = p16.GetActorsByType("fact")
				pyle = p16.GetActorsByType("pyle")
				hand = p16.GetActorsByType("hand")
				afld = p16.GetActorsByType("afld")
				weap = p16.GetActorsByType("weap")
				hpad = p16.GetActorsByType("hpad")
				rafact = p16.GetActorsByType("rafact")
				tent = p16.GetActorsByType("tent")
				barr = p16.GetActorsByType("barr")
				raweapa = p16.GetActorsByType("raweapa")
				raweap = p16.GetActorsByType("raweap")
				rahpad = p16.GetActorsByType("rahpad")
				raafld = p16.GetActorsByType("raafld")
				hq = p16.GetActorsByType("2100hq")
				fy1 = p16.GetActorsByType("2100fy1")
				fy2 = p16.GetActorsByType("2100fy2")
				sccommandcenter = p16.GetActorsByType("sccommandcenter")
				scbarracks = p16.GetActorsByType("scbarracks")
				scfactory = p16.GetActorsByType("scfactory")
				scstarport = p16.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p1 and p7.IsAlliedWith(p1) then
				fact = p1.GetActorsByType("fact")
				pyle = p1.GetActorsByType("pyle")
				hand = p1.GetActorsByType("hand")
				afld = p1.GetActorsByType("afld")
				weap = p1.GetActorsByType("weap")
				hpad = p1.GetActorsByType("hpad")
				rafact = p1.GetActorsByType("rafact")
				tent = p1.GetActorsByType("tent")
				barr = p1.GetActorsByType("barr")
				raweapa = p1.GetActorsByType("raweapa")
				raweap = p1.GetActorsByType("raweap")
				rahpad = p1.GetActorsByType("rahpad")
				raafld = p1.GetActorsByType("raafld")
				hq = p1.GetActorsByType("2100hq")
				fy1 = p1.GetActorsByType("2100fy1")
				fy2 = p1.GetActorsByType("2100fy2")
				sccommandcenter = p1.GetActorsByType("sccommandcenter")
				scbarracks = p1.GetActorsByType("scbarracks")
				scfactory = p1.GetActorsByType("scfactory")
				scstarport = p1.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p2 and p7.IsAlliedWith(p2) then
				fact = p2.GetActorsByType("fact")
				pyle = p2.GetActorsByType("pyle")
				hand = p2.GetActorsByType("hand")
				afld = p2.GetActorsByType("afld")
				weap = p2.GetActorsByType("weap")
				hpad = p2.GetActorsByType("hpad")
				rafact = p2.GetActorsByType("rafact")
				tent = p2.GetActorsByType("tent")
				barr = p2.GetActorsByType("barr")
				raweapa = p2.GetActorsByType("raweapa")
				raweap = p2.GetActorsByType("raweap")
				rahpad = p2.GetActorsByType("rahpad")
				raafld = p2.GetActorsByType("raafld")
				hq = p2.GetActorsByType("2100hq")
				fy1 = p2.GetActorsByType("2100fy1")
				fy2 = p2.GetActorsByType("2100fy2")
				sccommandcenter = p2.GetActorsByType("sccommandcenter")
				scbarracks = p2.GetActorsByType("scbarracks")
				scfactory = p2.GetActorsByType("scfactory")
				scstarport = p2.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p3 and p7.IsAlliedWith(p3) then
				fact = p3.GetActorsByType("fact")
				pyle = p3.GetActorsByType("pyle")
				hand = p3.GetActorsByType("hand")
				afld = p3.GetActorsByType("afld")
				weap = p3.GetActorsByType("weap")
				hpad = p3.GetActorsByType("hpad")
				rafact = p3.GetActorsByType("rafact")
				tent = p3.GetActorsByType("tent")
				barr = p3.GetActorsByType("barr")
				raweapa = p3.GetActorsByType("raweapa")
				raweap = p3.GetActorsByType("raweap")
				rahpad = p3.GetActorsByType("rahpad")
				raafld = p3.GetActorsByType("raafld")
				hq = p3.GetActorsByType("2100hq")
				fy1 = p3.GetActorsByType("2100fy1")
				fy2 = p3.GetActorsByType("2100fy2")
				sccommandcenter = p3.GetActorsByType("sccommandcenter")
				scbarracks = p3.GetActorsByType("scbarracks")
				scfactory = p3.GetActorsByType("scfactory")
				scstarport = p3.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p4 and p7.IsAlliedWith(p4) then
				fact = p4.GetActorsByType("fact")
				pyle = p4.GetActorsByType("pyle")
				hand = p4.GetActorsByType("hand")
				afld = p4.GetActorsByType("afld")
				weap = p4.GetActorsByType("weap")
				hpad = p4.GetActorsByType("hpad")
				rafact = p4.GetActorsByType("rafact")
				tent = p4.GetActorsByType("tent")
				barr = p4.GetActorsByType("barr")
				raweapa = p4.GetActorsByType("raweapa")
				raweap = p4.GetActorsByType("raweap")
				rahpad = p4.GetActorsByType("rahpad")
				raafld = p4.GetActorsByType("raafld")
				hq = p4.GetActorsByType("2100hq")
				fy1 = p4.GetActorsByType("2100fy1")
				fy2 = p4.GetActorsByType("2100fy2")
				sccommandcenter = p4.GetActorsByType("sccommandcenter")
				scbarracks = p4.GetActorsByType("scbarracks")
				scfactory = p4.GetActorsByType("scfactory")
				scstarport = p4.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p5 and p7.IsAlliedWith(p5) then
				fact = p5.GetActorsByType("fact")
				pyle = p5.GetActorsByType("pyle")
				hand = p5.GetActorsByType("hand")
				afld = p5.GetActorsByType("afld")
				weap = p5.GetActorsByType("weap")
				hpad = p5.GetActorsByType("hpad")
				rafact = p5.GetActorsByType("rafact")
				tent = p5.GetActorsByType("tent")
				barr = p5.GetActorsByType("barr")
				raweapa = p5.GetActorsByType("raweapa")
				raweap = p5.GetActorsByType("raweap")
				rahpad = p5.GetActorsByType("rahpad")
				raafld = p5.GetActorsByType("raafld")
				hq = p5.GetActorsByType("2100hq")
				fy1 = p5.GetActorsByType("2100fy1")
				fy2 = p5.GetActorsByType("2100fy2")
				sccommandcenter = p5.GetActorsByType("sccommandcenter")
				scbarracks = p5.GetActorsByType("scbarracks")
				scfactory = p5.GetActorsByType("scfactory")
				scstarport = p5.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p6 and p7.IsAlliedWith(p6) then
				fact = p6.GetActorsByType("fact")
				pyle = p6.GetActorsByType("pyle")
				hand = p6.GetActorsByType("hand")
				afld = p6.GetActorsByType("afld")
				weap = p6.GetActorsByType("weap")
				hpad = p6.GetActorsByType("hpad")
				rafact = p6.GetActorsByType("rafact")
				tent = p6.GetActorsByType("tent")
				barr = p6.GetActorsByType("barr")
				raweapa = p6.GetActorsByType("raweapa")
				raweap = p6.GetActorsByType("raweap")
				rahpad = p6.GetActorsByType("rahpad")
				raafld = p6.GetActorsByType("raafld")
				hq = p6.GetActorsByType("2100hq")
				fy1 = p6.GetActorsByType("2100fy1")
				fy2 = p6.GetActorsByType("2100fy2")
				sccommandcenter = p6.GetActorsByType("sccommandcenter")
				scbarracks = p6.GetActorsByType("scbarracks")
				scfactory = p6.GetActorsByType("scfactory")
				scstarport = p6.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p7cash()
	end)
end
p8cash = function()
	if p8 and p8.IsBot then
		citybottruck = p8.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p9 and p8.IsAlliedWith(p9) then
				fact = p9.GetActorsByType("fact")
				pyle = p9.GetActorsByType("pyle")
				hand = p9.GetActorsByType("hand")
				afld = p9.GetActorsByType("afld")
				weap = p9.GetActorsByType("weap")
				hpad = p9.GetActorsByType("hpad")
				rafact = p9.GetActorsByType("rafact")
				tent = p9.GetActorsByType("tent")
				barr = p9.GetActorsByType("barr")
				raweapa = p9.GetActorsByType("raweapa")
				raweap = p9.GetActorsByType("raweap")
				rahpad = p9.GetActorsByType("rahpad")
				raafld = p9.GetActorsByType("raafld")
				hq = p9.GetActorsByType("2100hq")
				fy1 = p9.GetActorsByType("2100fy1")
				fy2 = p9.GetActorsByType("2100fy2")
				sccommandcenter = p9.GetActorsByType("sccommandcenter")
				scbarracks = p9.GetActorsByType("scbarracks")
				scfactory = p9.GetActorsByType("scfactory")
				scstarport = p9.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p10 and p8.IsAlliedWith(p10) then
				fact = p10.GetActorsByType("fact")
				pyle = p10.GetActorsByType("pyle")
				hand = p10.GetActorsByType("hand")
				afld = p10.GetActorsByType("afld")
				weap = p10.GetActorsByType("weap")
				hpad = p10.GetActorsByType("hpad")
				rafact = p10.GetActorsByType("rafact")
				tent = p10.GetActorsByType("tent")
				barr = p10.GetActorsByType("barr")
				raweapa = p10.GetActorsByType("raweapa")
				raweap = p10.GetActorsByType("raweap")
				rahpad = p10.GetActorsByType("rahpad")
				raafld = p10.GetActorsByType("raafld")
				hq = p10.GetActorsByType("2100hq")
				fy1 = p10.GetActorsByType("2100fy1")
				fy2 = p10.GetActorsByType("2100fy2")
				sccommandcenter = p10.GetActorsByType("sccommandcenter")
				scbarracks = p10.GetActorsByType("scbarracks")
				scfactory = p10.GetActorsByType("scfactory")
				scstarport = p10.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p11 and p8.IsAlliedWith(p11) then
				fact = p11.GetActorsByType("fact")
				pyle = p11.GetActorsByType("pyle")
				hand = p11.GetActorsByType("hand")
				afld = p11.GetActorsByType("afld")
				weap = p11.GetActorsByType("weap")
				hpad = p11.GetActorsByType("hpad")
				rafact = p11.GetActorsByType("rafact")
				tent = p11.GetActorsByType("tent")
				barr = p11.GetActorsByType("barr")
				raweapa = p11.GetActorsByType("raweapa")
				raweap = p11.GetActorsByType("raweap")
				rahpad = p11.GetActorsByType("rahpad")
				raafld = p11.GetActorsByType("raafld")
				hq = p11.GetActorsByType("2100hq")
				fy1 = p11.GetActorsByType("2100fy1")
				fy2 = p11.GetActorsByType("2100fy2")
				sccommandcenter = p11.GetActorsByType("sccommandcenter")
				scbarracks = p11.GetActorsByType("scbarracks")
				scfactory = p11.GetActorsByType("scfactory")
				scstarport = p11.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p12 and p8.IsAlliedWith(p12) then
				fact = p12.GetActorsByType("fact")
				pyle = p12.GetActorsByType("pyle")
				hand = p12.GetActorsByType("hand")
				afld = p12.GetActorsByType("afld")
				weap = p12.GetActorsByType("weap")
				hpad = p12.GetActorsByType("hpad")
				rafact = p12.GetActorsByType("rafact")
				tent = p12.GetActorsByType("tent")
				barr = p12.GetActorsByType("barr")
				raweapa = p12.GetActorsByType("raweapa")
				raweap = p12.GetActorsByType("raweap")
				rahpad = p12.GetActorsByType("rahpad")
				raafld = p12.GetActorsByType("raafld")
				hq = p12.GetActorsByType("2100hq")
				fy1 = p12.GetActorsByType("2100fy1")
				fy2 = p12.GetActorsByType("2100fy2")
				sccommandcenter = p12.GetActorsByType("sccommandcenter")
				scbarracks = p12.GetActorsByType("scbarracks")
				scfactory = p12.GetActorsByType("scfactory")
				scstarport = p12.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p13 and p8.IsAlliedWith(p13) then
				fact = p13.GetActorsByType("fact")
				pyle = p13.GetActorsByType("pyle")
				hand = p13.GetActorsByType("hand")
				afld = p13.GetActorsByType("afld")
				weap = p13.GetActorsByType("weap")
				hpad = p13.GetActorsByType("hpad")
				rafact = p13.GetActorsByType("rafact")
				tent = p13.GetActorsByType("tent")
				barr = p13.GetActorsByType("barr")
				raweapa = p13.GetActorsByType("raweapa")
				raweap = p13.GetActorsByType("raweap")
				rahpad = p13.GetActorsByType("rahpad")
				raafld = p13.GetActorsByType("raafld")
				hq = p13.GetActorsByType("2100hq")
				fy1 = p13.GetActorsByType("2100fy1")
				fy2 = p13.GetActorsByType("2100fy2")
				sccommandcenter = p13.GetActorsByType("sccommandcenter")
				scbarracks = p13.GetActorsByType("scbarracks")
				scfactory = p13.GetActorsByType("scfactory")
				scstarport = p13.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p14 and p8.IsAlliedWith(p14) then
				fact = p14.GetActorsByType("fact")
				pyle = p14.GetActorsByType("pyle")
				hand = p14.GetActorsByType("hand")
				afld = p14.GetActorsByType("afld")
				weap = p14.GetActorsByType("weap")
				hpad = p14.GetActorsByType("hpad")
				rafact = p14.GetActorsByType("rafact")
				tent = p14.GetActorsByType("tent")
				barr = p14.GetActorsByType("barr")
				raweapa = p14.GetActorsByType("raweapa")
				raweap = p14.GetActorsByType("raweap")
				rahpad = p14.GetActorsByType("rahpad")
				raafld = p14.GetActorsByType("raafld")
				hq = p14.GetActorsByType("2100hq")
				fy1 = p14.GetActorsByType("2100fy1")
				fy2 = p14.GetActorsByType("2100fy2")
				sccommandcenter = p14.GetActorsByType("sccommandcenter")
				scbarracks = p14.GetActorsByType("scbarracks")
				scfactory = p14.GetActorsByType("scfactory")
				scstarport = p14.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p15 and p8.IsAlliedWith(p15) then
				fact = p15.GetActorsByType("fact")
				pyle = p15.GetActorsByType("pyle")
				hand = p15.GetActorsByType("hand")
				afld = p15.GetActorsByType("afld")
				weap = p15.GetActorsByType("weap")
				hpad = p15.GetActorsByType("hpad")
				rafact = p15.GetActorsByType("rafact")
				tent = p15.GetActorsByType("tent")
				barr = p15.GetActorsByType("barr")
				raweapa = p15.GetActorsByType("raweapa")
				raweap = p15.GetActorsByType("raweap")
				rahpad = p15.GetActorsByType("rahpad")
				raafld = p15.GetActorsByType("raafld")
				hq = p15.GetActorsByType("2100hq")
				fy1 = p15.GetActorsByType("2100fy1")
				fy2 = p15.GetActorsByType("2100fy2")
				sccommandcenter = p15.GetActorsByType("sccommandcenter")
				scbarracks = p15.GetActorsByType("scbarracks")
				scfactory = p15.GetActorsByType("scfactory")
				scstarport = p15.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p16 and p8.IsAlliedWith(p16) then
				fact = p16.GetActorsByType("fact")
				pyle = p16.GetActorsByType("pyle")
				hand = p16.GetActorsByType("hand")
				afld = p16.GetActorsByType("afld")
				weap = p16.GetActorsByType("weap")
				hpad = p16.GetActorsByType("hpad")
				rafact = p16.GetActorsByType("rafact")
				tent = p16.GetActorsByType("tent")
				barr = p16.GetActorsByType("barr")
				raweapa = p16.GetActorsByType("raweapa")
				raweap = p16.GetActorsByType("raweap")
				rahpad = p16.GetActorsByType("rahpad")
				raafld = p16.GetActorsByType("raafld")
				hq = p16.GetActorsByType("2100hq")
				fy1 = p16.GetActorsByType("2100fy1")
				fy2 = p16.GetActorsByType("2100fy2")
				sccommandcenter = p16.GetActorsByType("sccommandcenter")
				scbarracks = p16.GetActorsByType("scbarracks")
				scfactory = p16.GetActorsByType("scfactory")
				scstarport = p16.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p1 and p8.IsAlliedWith(p1) then
				fact = p1.GetActorsByType("fact")
				pyle = p1.GetActorsByType("pyle")
				hand = p1.GetActorsByType("hand")
				afld = p1.GetActorsByType("afld")
				weap = p1.GetActorsByType("weap")
				hpad = p1.GetActorsByType("hpad")
				rafact = p1.GetActorsByType("rafact")
				tent = p1.GetActorsByType("tent")
				barr = p1.GetActorsByType("barr")
				raweapa = p1.GetActorsByType("raweapa")
				raweap = p1.GetActorsByType("raweap")
				rahpad = p1.GetActorsByType("rahpad")
				raafld = p1.GetActorsByType("raafld")
				hq = p1.GetActorsByType("2100hq")
				fy1 = p1.GetActorsByType("2100fy1")
				fy2 = p1.GetActorsByType("2100fy2")
				sccommandcenter = p1.GetActorsByType("sccommandcenter")
				scbarracks = p1.GetActorsByType("scbarracks")
				scfactory = p1.GetActorsByType("scfactory")
				scstarport = p1.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p2 and p8.IsAlliedWith(p2) then
				fact = p2.GetActorsByType("fact")
				pyle = p2.GetActorsByType("pyle")
				hand = p2.GetActorsByType("hand")
				afld = p2.GetActorsByType("afld")
				weap = p2.GetActorsByType("weap")
				hpad = p2.GetActorsByType("hpad")
				rafact = p2.GetActorsByType("rafact")
				tent = p2.GetActorsByType("tent")
				barr = p2.GetActorsByType("barr")
				raweapa = p2.GetActorsByType("raweapa")
				raweap = p2.GetActorsByType("raweap")
				rahpad = p2.GetActorsByType("rahpad")
				raafld = p2.GetActorsByType("raafld")
				hq = p2.GetActorsByType("2100hq")
				fy1 = p2.GetActorsByType("2100fy1")
				fy2 = p2.GetActorsByType("2100fy2")
				sccommandcenter = p2.GetActorsByType("sccommandcenter")
				scbarracks = p2.GetActorsByType("scbarracks")
				scfactory = p2.GetActorsByType("scfactory")
				scstarport = p2.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p3 and p8.IsAlliedWith(p3) then
				fact = p3.GetActorsByType("fact")
				pyle = p3.GetActorsByType("pyle")
				hand = p3.GetActorsByType("hand")
				afld = p3.GetActorsByType("afld")
				weap = p3.GetActorsByType("weap")
				hpad = p3.GetActorsByType("hpad")
				rafact = p3.GetActorsByType("rafact")
				tent = p3.GetActorsByType("tent")
				barr = p3.GetActorsByType("barr")
				raweapa = p3.GetActorsByType("raweapa")
				raweap = p3.GetActorsByType("raweap")
				rahpad = p3.GetActorsByType("rahpad")
				raafld = p3.GetActorsByType("raafld")
				hq = p3.GetActorsByType("2100hq")
				fy1 = p3.GetActorsByType("2100fy1")
				fy2 = p3.GetActorsByType("2100fy2")
				sccommandcenter = p3.GetActorsByType("sccommandcenter")
				scbarracks = p3.GetActorsByType("scbarracks")
				scfactory = p3.GetActorsByType("scfactory")
				scstarport = p3.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p4 and p8.IsAlliedWith(p4) then
				fact = p4.GetActorsByType("fact")
				pyle = p4.GetActorsByType("pyle")
				hand = p4.GetActorsByType("hand")
				afld = p4.GetActorsByType("afld")
				weap = p4.GetActorsByType("weap")
				hpad = p4.GetActorsByType("hpad")
				rafact = p4.GetActorsByType("rafact")
				tent = p4.GetActorsByType("tent")
				barr = p4.GetActorsByType("barr")
				raweapa = p4.GetActorsByType("raweapa")
				raweap = p4.GetActorsByType("raweap")
				rahpad = p4.GetActorsByType("rahpad")
				raafld = p4.GetActorsByType("raafld")
				hq = p4.GetActorsByType("2100hq")
				fy1 = p4.GetActorsByType("2100fy1")
				fy2 = p4.GetActorsByType("2100fy2")
				sccommandcenter = p4.GetActorsByType("sccommandcenter")
				scbarracks = p4.GetActorsByType("scbarracks")
				scfactory = p4.GetActorsByType("scfactory")
				scstarport = p4.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p5 and p8.IsAlliedWith(p5) then
				fact = p5.GetActorsByType("fact")
				pyle = p5.GetActorsByType("pyle")
				hand = p5.GetActorsByType("hand")
				afld = p5.GetActorsByType("afld")
				weap = p5.GetActorsByType("weap")
				hpad = p5.GetActorsByType("hpad")
				rafact = p5.GetActorsByType("rafact")
				tent = p5.GetActorsByType("tent")
				barr = p5.GetActorsByType("barr")
				raweapa = p5.GetActorsByType("raweapa")
				raweap = p5.GetActorsByType("raweap")
				rahpad = p5.GetActorsByType("rahpad")
				raafld = p5.GetActorsByType("raafld")
				hq = p5.GetActorsByType("2100hq")
				fy1 = p5.GetActorsByType("2100fy1")
				fy2 = p5.GetActorsByType("2100fy2")
				sccommandcenter = p5.GetActorsByType("sccommandcenter")
				scbarracks = p5.GetActorsByType("scbarracks")
				scfactory = p5.GetActorsByType("scfactory")
				scstarport = p5.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p6 and p8.IsAlliedWith(p6) then
				fact = p6.GetActorsByType("fact")
				pyle = p6.GetActorsByType("pyle")
				hand = p6.GetActorsByType("hand")
				afld = p6.GetActorsByType("afld")
				weap = p6.GetActorsByType("weap")
				hpad = p6.GetActorsByType("hpad")
				rafact = p6.GetActorsByType("rafact")
				tent = p6.GetActorsByType("tent")
				barr = p6.GetActorsByType("barr")
				raweapa = p6.GetActorsByType("raweapa")
				raweap = p6.GetActorsByType("raweap")
				rahpad = p6.GetActorsByType("rahpad")
				raafld = p6.GetActorsByType("raafld")
				hq = p6.GetActorsByType("2100hq")
				fy1 = p6.GetActorsByType("2100fy1")
				fy2 = p6.GetActorsByType("2100fy2")
				sccommandcenter = p6.GetActorsByType("sccommandcenter")
				scbarracks = p6.GetActorsByType("scbarracks")
				scfactory = p6.GetActorsByType("scfactory")
				scstarport = p6.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p7 and p8.IsAlliedWith(p7) then
				fact = p7.GetActorsByType("fact")
				pyle = p7.GetActorsByType("pyle")
				hand = p7.GetActorsByType("hand")
				afld = p7.GetActorsByType("afld")
				weap = p7.GetActorsByType("weap")
				hpad = p7.GetActorsByType("hpad")
				rafact = p7.GetActorsByType("rafact")
				tent = p7.GetActorsByType("tent")
				barr = p7.GetActorsByType("barr")
				raweapa = p7.GetActorsByType("raweapa")
				raweap = p7.GetActorsByType("raweap")
				rahpad = p7.GetActorsByType("rahpad")
				raafld = p7.GetActorsByType("raafld")
				hq = p7.GetActorsByType("2100hq")
				fy1 = p7.GetActorsByType("2100fy1")
				fy2 = p7.GetActorsByType("2100fy2")
				sccommandcenter = p7.GetActorsByType("sccommandcenter")
				scbarracks = p7.GetActorsByType("scbarracks")
				scfactory = p7.GetActorsByType("scfactory")
				scstarport = p7.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p8cash()
	end)
end
p9cash = function()
	if p9 and p9.IsBot then
		citybottruck = p9.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p10 and p9.IsAlliedWith(p10) then
				fact = p10.GetActorsByType("fact")
				pyle = p10.GetActorsByType("pyle")
				hand = p10.GetActorsByType("hand")
				afld = p10.GetActorsByType("afld")
				weap = p10.GetActorsByType("weap")
				hpad = p10.GetActorsByType("hpad")
				rafact = p10.GetActorsByType("rafact")
				tent = p10.GetActorsByType("tent")
				barr = p10.GetActorsByType("barr")
				raweapa = p10.GetActorsByType("raweapa")
				raweap = p10.GetActorsByType("raweap")
				rahpad = p10.GetActorsByType("rahpad")
				raafld = p10.GetActorsByType("raafld")
				hq = p10.GetActorsByType("2100hq")
				fy1 = p10.GetActorsByType("2100fy1")
				fy2 = p10.GetActorsByType("2100fy2")
				sccommandcenter = p10.GetActorsByType("sccommandcenter")
				scbarracks = p10.GetActorsByType("scbarracks")
				scfactory = p10.GetActorsByType("scfactory")
				scstarport = p10.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p11 and p9.IsAlliedWith(p11) then
				fact = p11.GetActorsByType("fact")
				pyle = p11.GetActorsByType("pyle")
				hand = p11.GetActorsByType("hand")
				afld = p11.GetActorsByType("afld")
				weap = p11.GetActorsByType("weap")
				hpad = p11.GetActorsByType("hpad")
				rafact = p11.GetActorsByType("rafact")
				tent = p11.GetActorsByType("tent")
				barr = p11.GetActorsByType("barr")
				raweapa = p11.GetActorsByType("raweapa")
				raweap = p11.GetActorsByType("raweap")
				rahpad = p11.GetActorsByType("rahpad")
				raafld = p11.GetActorsByType("raafld")
				hq = p11.GetActorsByType("2100hq")
				fy1 = p11.GetActorsByType("2100fy1")
				fy2 = p11.GetActorsByType("2100fy2")
				sccommandcenter = p11.GetActorsByType("sccommandcenter")
				scbarracks = p11.GetActorsByType("scbarracks")
				scfactory = p11.GetActorsByType("scfactory")
				scstarport = p11.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p12 and p9.IsAlliedWith(p12) then
				fact = p12.GetActorsByType("fact")
				pyle = p12.GetActorsByType("pyle")
				hand = p12.GetActorsByType("hand")
				afld = p12.GetActorsByType("afld")
				weap = p12.GetActorsByType("weap")
				hpad = p12.GetActorsByType("hpad")
				rafact = p12.GetActorsByType("rafact")
				tent = p12.GetActorsByType("tent")
				barr = p12.GetActorsByType("barr")
				raweapa = p12.GetActorsByType("raweapa")
				raweap = p12.GetActorsByType("raweap")
				rahpad = p12.GetActorsByType("rahpad")
				raafld = p12.GetActorsByType("raafld")
				hq = p12.GetActorsByType("2100hq")
				fy1 = p12.GetActorsByType("2100fy1")
				fy2 = p12.GetActorsByType("2100fy2")
				sccommandcenter = p12.GetActorsByType("sccommandcenter")
				scbarracks = p12.GetActorsByType("scbarracks")
				scfactory = p12.GetActorsByType("scfactory")
				scstarport = p12.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p13 and p9.IsAlliedWith(p13) then
				fact = p13.GetActorsByType("fact")
				pyle = p13.GetActorsByType("pyle")
				hand = p13.GetActorsByType("hand")
				afld = p13.GetActorsByType("afld")
				weap = p13.GetActorsByType("weap")
				hpad = p13.GetActorsByType("hpad")
				rafact = p13.GetActorsByType("rafact")
				tent = p13.GetActorsByType("tent")
				barr = p13.GetActorsByType("barr")
				raweapa = p13.GetActorsByType("raweapa")
				raweap = p13.GetActorsByType("raweap")
				rahpad = p13.GetActorsByType("rahpad")
				raafld = p13.GetActorsByType("raafld")
				hq = p13.GetActorsByType("2100hq")
				fy1 = p13.GetActorsByType("2100fy1")
				fy2 = p13.GetActorsByType("2100fy2")
				sccommandcenter = p13.GetActorsByType("sccommandcenter")
				scbarracks = p13.GetActorsByType("scbarracks")
				scfactory = p13.GetActorsByType("scfactory")
				scstarport = p13.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p14 and p9.IsAlliedWith(p14) then
				fact = p14.GetActorsByType("fact")
				pyle = p14.GetActorsByType("pyle")
				hand = p14.GetActorsByType("hand")
				afld = p14.GetActorsByType("afld")
				weap = p14.GetActorsByType("weap")
				hpad = p14.GetActorsByType("hpad")
				rafact = p14.GetActorsByType("rafact")
				tent = p14.GetActorsByType("tent")
				barr = p14.GetActorsByType("barr")
				raweapa = p14.GetActorsByType("raweapa")
				raweap = p14.GetActorsByType("raweap")
				rahpad = p14.GetActorsByType("rahpad")
				raafld = p14.GetActorsByType("raafld")
				hq = p14.GetActorsByType("2100hq")
				fy1 = p14.GetActorsByType("2100fy1")
				fy2 = p14.GetActorsByType("2100fy2")
				sccommandcenter = p14.GetActorsByType("sccommandcenter")
				scbarracks = p14.GetActorsByType("scbarracks")
				scfactory = p14.GetActorsByType("scfactory")
				scstarport = p14.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p15 and p9.IsAlliedWith(p15) then
				fact = p15.GetActorsByType("fact")
				pyle = p15.GetActorsByType("pyle")
				hand = p15.GetActorsByType("hand")
				afld = p15.GetActorsByType("afld")
				weap = p15.GetActorsByType("weap")
				hpad = p15.GetActorsByType("hpad")
				rafact = p15.GetActorsByType("rafact")
				tent = p15.GetActorsByType("tent")
				barr = p15.GetActorsByType("barr")
				raweapa = p15.GetActorsByType("raweapa")
				raweap = p15.GetActorsByType("raweap")
				rahpad = p15.GetActorsByType("rahpad")
				raafld = p15.GetActorsByType("raafld")
				hq = p15.GetActorsByType("2100hq")
				fy1 = p15.GetActorsByType("2100fy1")
				fy2 = p15.GetActorsByType("2100fy2")
				sccommandcenter = p15.GetActorsByType("sccommandcenter")
				scbarracks = p15.GetActorsByType("scbarracks")
				scfactory = p15.GetActorsByType("scfactory")
				scstarport = p15.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p16 and p9.IsAlliedWith(p16) then
				fact = p16.GetActorsByType("fact")
				pyle = p16.GetActorsByType("pyle")
				hand = p16.GetActorsByType("hand")
				afld = p16.GetActorsByType("afld")
				weap = p16.GetActorsByType("weap")
				hpad = p16.GetActorsByType("hpad")
				rafact = p16.GetActorsByType("rafact")
				tent = p16.GetActorsByType("tent")
				barr = p16.GetActorsByType("barr")
				raweapa = p16.GetActorsByType("raweapa")
				raweap = p16.GetActorsByType("raweap")
				rahpad = p16.GetActorsByType("rahpad")
				raafld = p16.GetActorsByType("raafld")
				hq = p16.GetActorsByType("2100hq")
				fy1 = p16.GetActorsByType("2100fy1")
				fy2 = p16.GetActorsByType("2100fy2")
				sccommandcenter = p16.GetActorsByType("sccommandcenter")
				scbarracks = p16.GetActorsByType("scbarracks")
				scfactory = p16.GetActorsByType("scfactory")
				scstarport = p16.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p1 and p9.IsAlliedWith(p1) then
				fact = p1.GetActorsByType("fact")
				pyle = p1.GetActorsByType("pyle")
				hand = p1.GetActorsByType("hand")
				afld = p1.GetActorsByType("afld")
				weap = p1.GetActorsByType("weap")
				hpad = p1.GetActorsByType("hpad")
				rafact = p1.GetActorsByType("rafact")
				tent = p1.GetActorsByType("tent")
				barr = p1.GetActorsByType("barr")
				raweapa = p1.GetActorsByType("raweapa")
				raweap = p1.GetActorsByType("raweap")
				rahpad = p1.GetActorsByType("rahpad")
				raafld = p1.GetActorsByType("raafld")
				hq = p1.GetActorsByType("2100hq")
				fy1 = p1.GetActorsByType("2100fy1")
				fy2 = p1.GetActorsByType("2100fy2")
				sccommandcenter = p1.GetActorsByType("sccommandcenter")
				scbarracks = p1.GetActorsByType("scbarracks")
				scfactory = p1.GetActorsByType("scfactory")
				scstarport = p1.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p2 and p9.IsAlliedWith(p2) then
				fact = p2.GetActorsByType("fact")
				pyle = p2.GetActorsByType("pyle")
				hand = p2.GetActorsByType("hand")
				afld = p2.GetActorsByType("afld")
				weap = p2.GetActorsByType("weap")
				hpad = p2.GetActorsByType("hpad")
				rafact = p2.GetActorsByType("rafact")
				tent = p2.GetActorsByType("tent")
				barr = p2.GetActorsByType("barr")
				raweapa = p2.GetActorsByType("raweapa")
				raweap = p2.GetActorsByType("raweap")
				rahpad = p2.GetActorsByType("rahpad")
				raafld = p2.GetActorsByType("raafld")
				hq = p2.GetActorsByType("2100hq")
				fy1 = p2.GetActorsByType("2100fy1")
				fy2 = p2.GetActorsByType("2100fy2")
				sccommandcenter = p2.GetActorsByType("sccommandcenter")
				scbarracks = p2.GetActorsByType("scbarracks")
				scfactory = p2.GetActorsByType("scfactory")
				scstarport = p2.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p3 and p9.IsAlliedWith(p3) then
				fact = p3.GetActorsByType("fact")
				pyle = p3.GetActorsByType("pyle")
				hand = p3.GetActorsByType("hand")
				afld = p3.GetActorsByType("afld")
				weap = p3.GetActorsByType("weap")
				hpad = p3.GetActorsByType("hpad")
				rafact = p3.GetActorsByType("rafact")
				tent = p3.GetActorsByType("tent")
				barr = p3.GetActorsByType("barr")
				raweapa = p3.GetActorsByType("raweapa")
				raweap = p3.GetActorsByType("raweap")
				rahpad = p3.GetActorsByType("rahpad")
				raafld = p3.GetActorsByType("raafld")
				hq = p3.GetActorsByType("2100hq")
				fy1 = p3.GetActorsByType("2100fy1")
				fy2 = p3.GetActorsByType("2100fy2")
				sccommandcenter = p3.GetActorsByType("sccommandcenter")
				scbarracks = p3.GetActorsByType("scbarracks")
				scfactory = p3.GetActorsByType("scfactory")
				scstarport = p3.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p4 and p9.IsAlliedWith(p4) then
				fact = p4.GetActorsByType("fact")
				pyle = p4.GetActorsByType("pyle")
				hand = p4.GetActorsByType("hand")
				afld = p4.GetActorsByType("afld")
				weap = p4.GetActorsByType("weap")
				hpad = p4.GetActorsByType("hpad")
				rafact = p4.GetActorsByType("rafact")
				tent = p4.GetActorsByType("tent")
				barr = p4.GetActorsByType("barr")
				raweapa = p4.GetActorsByType("raweapa")
				raweap = p4.GetActorsByType("raweap")
				rahpad = p4.GetActorsByType("rahpad")
				raafld = p4.GetActorsByType("raafld")
				hq = p4.GetActorsByType("2100hq")
				fy1 = p4.GetActorsByType("2100fy1")
				fy2 = p4.GetActorsByType("2100fy2")
				sccommandcenter = p4.GetActorsByType("sccommandcenter")
				scbarracks = p4.GetActorsByType("scbarracks")
				scfactory = p4.GetActorsByType("scfactory")
				scstarport = p4.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p5 and p9.IsAlliedWith(p5) then
				fact = p5.GetActorsByType("fact")
				pyle = p5.GetActorsByType("pyle")
				hand = p5.GetActorsByType("hand")
				afld = p5.GetActorsByType("afld")
				weap = p5.GetActorsByType("weap")
				hpad = p5.GetActorsByType("hpad")
				rafact = p5.GetActorsByType("rafact")
				tent = p5.GetActorsByType("tent")
				barr = p5.GetActorsByType("barr")
				raweapa = p5.GetActorsByType("raweapa")
				raweap = p5.GetActorsByType("raweap")
				rahpad = p5.GetActorsByType("rahpad")
				raafld = p5.GetActorsByType("raafld")
				hq = p5.GetActorsByType("2100hq")
				fy1 = p5.GetActorsByType("2100fy1")
				fy2 = p5.GetActorsByType("2100fy2")
				sccommandcenter = p5.GetActorsByType("sccommandcenter")
				scbarracks = p5.GetActorsByType("scbarracks")
				scfactory = p5.GetActorsByType("scfactory")
				scstarport = p5.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p6 and p9.IsAlliedWith(p6) then
				fact = p6.GetActorsByType("fact")
				pyle = p6.GetActorsByType("pyle")
				hand = p6.GetActorsByType("hand")
				afld = p6.GetActorsByType("afld")
				weap = p6.GetActorsByType("weap")
				hpad = p6.GetActorsByType("hpad")
				rafact = p6.GetActorsByType("rafact")
				tent = p6.GetActorsByType("tent")
				barr = p6.GetActorsByType("barr")
				raweapa = p6.GetActorsByType("raweapa")
				raweap = p6.GetActorsByType("raweap")
				rahpad = p6.GetActorsByType("rahpad")
				raafld = p6.GetActorsByType("raafld")
				hq = p6.GetActorsByType("2100hq")
				fy1 = p6.GetActorsByType("2100fy1")
				fy2 = p6.GetActorsByType("2100fy2")
				sccommandcenter = p6.GetActorsByType("sccommandcenter")
				scbarracks = p6.GetActorsByType("scbarracks")
				scfactory = p6.GetActorsByType("scfactory")
				scstarport = p6.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p7 and p9.IsAlliedWith(p7) then
				fact = p7.GetActorsByType("fact")
				pyle = p7.GetActorsByType("pyle")
				hand = p7.GetActorsByType("hand")
				afld = p7.GetActorsByType("afld")
				weap = p7.GetActorsByType("weap")
				hpad = p7.GetActorsByType("hpad")
				rafact = p7.GetActorsByType("rafact")
				tent = p7.GetActorsByType("tent")
				barr = p7.GetActorsByType("barr")
				raweapa = p7.GetActorsByType("raweapa")
				raweap = p7.GetActorsByType("raweap")
				rahpad = p7.GetActorsByType("rahpad")
				raafld = p7.GetActorsByType("raafld")
				hq = p7.GetActorsByType("2100hq")
				fy1 = p7.GetActorsByType("2100fy1")
				fy2 = p7.GetActorsByType("2100fy2")
				sccommandcenter = p7.GetActorsByType("sccommandcenter")
				scbarracks = p7.GetActorsByType("scbarracks")
				scfactory = p7.GetActorsByType("scfactory")
				scstarport = p7.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p8 and p9.IsAlliedWith(p8) then
				fact = p8.GetActorsByType("fact")
				pyle = p8.GetActorsByType("pyle")
				hand = p8.GetActorsByType("hand")
				afld = p8.GetActorsByType("afld")
				weap = p8.GetActorsByType("weap")
				hpad = p8.GetActorsByType("hpad")
				rafact = p8.GetActorsByType("rafact")
				tent = p8.GetActorsByType("tent")
				barr = p8.GetActorsByType("barr")
				raweapa = p8.GetActorsByType("raweapa")
				raweap = p8.GetActorsByType("raweap")
				rahpad = p8.GetActorsByType("rahpad")
				raafld = p8.GetActorsByType("raafld")
				hq = p8.GetActorsByType("2100hq")
				fy1 = p8.GetActorsByType("2100fy1")
				fy2 = p8.GetActorsByType("2100fy2")
				sccommandcenter = p8.GetActorsByType("sccommandcenter")
				scbarracks = p8.GetActorsByType("scbarracks")
				scfactory = p8.GetActorsByType("scfactory")
				scstarport = p8.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p9cash()
	end)
end
p10cash = function()
	if p10 and p10.IsBot then
		citybottruck = p10.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p11 and p10.IsAlliedWith(p11) then
				fact = p11.GetActorsByType("fact")
				pyle = p11.GetActorsByType("pyle")
				hand = p11.GetActorsByType("hand")
				afld = p11.GetActorsByType("afld")
				weap = p11.GetActorsByType("weap")
				hpad = p11.GetActorsByType("hpad")
				rafact = p11.GetActorsByType("rafact")
				tent = p11.GetActorsByType("tent")
				barr = p11.GetActorsByType("barr")
				raweapa = p11.GetActorsByType("raweapa")
				raweap = p11.GetActorsByType("raweap")
				rahpad = p11.GetActorsByType("rahpad")
				raafld = p11.GetActorsByType("raafld")
				hq = p11.GetActorsByType("2100hq")
				fy1 = p11.GetActorsByType("2100fy1")
				fy2 = p11.GetActorsByType("2100fy2")
				sccommandcenter = p11.GetActorsByType("sccommandcenter")
				scbarracks = p11.GetActorsByType("scbarracks")
				scfactory = p11.GetActorsByType("scfactory")
				scstarport = p11.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p12 and p10.IsAlliedWith(p12) then
				fact = p12.GetActorsByType("fact")
				pyle = p12.GetActorsByType("pyle")
				hand = p12.GetActorsByType("hand")
				afld = p12.GetActorsByType("afld")
				weap = p12.GetActorsByType("weap")
				hpad = p12.GetActorsByType("hpad")
				rafact = p12.GetActorsByType("rafact")
				tent = p12.GetActorsByType("tent")
				barr = p12.GetActorsByType("barr")
				raweapa = p12.GetActorsByType("raweapa")
				raweap = p12.GetActorsByType("raweap")
				rahpad = p12.GetActorsByType("rahpad")
				raafld = p12.GetActorsByType("raafld")
				hq = p12.GetActorsByType("2100hq")
				fy1 = p12.GetActorsByType("2100fy1")
				fy2 = p12.GetActorsByType("2100fy2")
				sccommandcenter = p12.GetActorsByType("sccommandcenter")
				scbarracks = p12.GetActorsByType("scbarracks")
				scfactory = p12.GetActorsByType("scfactory")
				scstarport = p12.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p13 and p10.IsAlliedWith(p13) then
				fact = p13.GetActorsByType("fact")
				pyle = p13.GetActorsByType("pyle")
				hand = p13.GetActorsByType("hand")
				afld = p13.GetActorsByType("afld")
				weap = p13.GetActorsByType("weap")
				hpad = p13.GetActorsByType("hpad")
				rafact = p13.GetActorsByType("rafact")
				tent = p13.GetActorsByType("tent")
				barr = p13.GetActorsByType("barr")
				raweapa = p13.GetActorsByType("raweapa")
				raweap = p13.GetActorsByType("raweap")
				rahpad = p13.GetActorsByType("rahpad")
				raafld = p13.GetActorsByType("raafld")
				hq = p13.GetActorsByType("2100hq")
				fy1 = p13.GetActorsByType("2100fy1")
				fy2 = p13.GetActorsByType("2100fy2")
				sccommandcenter = p13.GetActorsByType("sccommandcenter")
				scbarracks = p13.GetActorsByType("scbarracks")
				scfactory = p13.GetActorsByType("scfactory")
				scstarport = p13.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p14 and p10.IsAlliedWith(p14) then
				fact = p14.GetActorsByType("fact")
				pyle = p14.GetActorsByType("pyle")
				hand = p14.GetActorsByType("hand")
				afld = p14.GetActorsByType("afld")
				weap = p14.GetActorsByType("weap")
				hpad = p14.GetActorsByType("hpad")
				rafact = p14.GetActorsByType("rafact")
				tent = p14.GetActorsByType("tent")
				barr = p14.GetActorsByType("barr")
				raweapa = p14.GetActorsByType("raweapa")
				raweap = p14.GetActorsByType("raweap")
				rahpad = p14.GetActorsByType("rahpad")
				raafld = p14.GetActorsByType("raafld")
				hq = p14.GetActorsByType("2100hq")
				fy1 = p14.GetActorsByType("2100fy1")
				fy2 = p14.GetActorsByType("2100fy2")
				sccommandcenter = p14.GetActorsByType("sccommandcenter")
				scbarracks = p14.GetActorsByType("scbarracks")
				scfactory = p14.GetActorsByType("scfactory")
				scstarport = p14.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p15 and p10.IsAlliedWith(p15) then
				fact = p15.GetActorsByType("fact")
				pyle = p15.GetActorsByType("pyle")
				hand = p15.GetActorsByType("hand")
				afld = p15.GetActorsByType("afld")
				weap = p15.GetActorsByType("weap")
				hpad = p15.GetActorsByType("hpad")
				rafact = p15.GetActorsByType("rafact")
				tent = p15.GetActorsByType("tent")
				barr = p15.GetActorsByType("barr")
				raweapa = p15.GetActorsByType("raweapa")
				raweap = p15.GetActorsByType("raweap")
				rahpad = p15.GetActorsByType("rahpad")
				raafld = p15.GetActorsByType("raafld")
				hq = p15.GetActorsByType("2100hq")
				fy1 = p15.GetActorsByType("2100fy1")
				fy2 = p15.GetActorsByType("2100fy2")
				sccommandcenter = p15.GetActorsByType("sccommandcenter")
				scbarracks = p15.GetActorsByType("scbarracks")
				scfactory = p15.GetActorsByType("scfactory")
				scstarport = p15.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p16 and p10.IsAlliedWith(p16) then
				fact = p16.GetActorsByType("fact")
				pyle = p16.GetActorsByType("pyle")
				hand = p16.GetActorsByType("hand")
				afld = p16.GetActorsByType("afld")
				weap = p16.GetActorsByType("weap")
				hpad = p16.GetActorsByType("hpad")
				rafact = p16.GetActorsByType("rafact")
				tent = p16.GetActorsByType("tent")
				barr = p16.GetActorsByType("barr")
				raweapa = p16.GetActorsByType("raweapa")
				raweap = p16.GetActorsByType("raweap")
				rahpad = p16.GetActorsByType("rahpad")
				raafld = p16.GetActorsByType("raafld")
				hq = p16.GetActorsByType("2100hq")
				fy1 = p16.GetActorsByType("2100fy1")
				fy2 = p16.GetActorsByType("2100fy2")
				sccommandcenter = p16.GetActorsByType("sccommandcenter")
				scbarracks = p16.GetActorsByType("scbarracks")
				scfactory = p16.GetActorsByType("scfactory")
				scstarport = p16.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p1 and p10.IsAlliedWith(p1) then
				fact = p1.GetActorsByType("fact")
				pyle = p1.GetActorsByType("pyle")
				hand = p1.GetActorsByType("hand")
				afld = p1.GetActorsByType("afld")
				weap = p1.GetActorsByType("weap")
				hpad = p1.GetActorsByType("hpad")
				rafact = p1.GetActorsByType("rafact")
				tent = p1.GetActorsByType("tent")
				barr = p1.GetActorsByType("barr")
				raweapa = p1.GetActorsByType("raweapa")
				raweap = p1.GetActorsByType("raweap")
				rahpad = p1.GetActorsByType("rahpad")
				raafld = p1.GetActorsByType("raafld")
				hq = p1.GetActorsByType("2100hq")
				fy1 = p1.GetActorsByType("2100fy1")
				fy2 = p1.GetActorsByType("2100fy2")
				sccommandcenter = p1.GetActorsByType("sccommandcenter")
				scbarracks = p1.GetActorsByType("scbarracks")
				scfactory = p1.GetActorsByType("scfactory")
				scstarport = p1.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p2 and p10.IsAlliedWith(p2) then
				fact = p2.GetActorsByType("fact")
				pyle = p2.GetActorsByType("pyle")
				hand = p2.GetActorsByType("hand")
				afld = p2.GetActorsByType("afld")
				weap = p2.GetActorsByType("weap")
				hpad = p2.GetActorsByType("hpad")
				rafact = p2.GetActorsByType("rafact")
				tent = p2.GetActorsByType("tent")
				barr = p2.GetActorsByType("barr")
				raweapa = p2.GetActorsByType("raweapa")
				raweap = p2.GetActorsByType("raweap")
				rahpad = p2.GetActorsByType("rahpad")
				raafld = p2.GetActorsByType("raafld")
				hq = p2.GetActorsByType("2100hq")
				fy1 = p2.GetActorsByType("2100fy1")
				fy2 = p2.GetActorsByType("2100fy2")
				sccommandcenter = p2.GetActorsByType("sccommandcenter")
				scbarracks = p2.GetActorsByType("scbarracks")
				scfactory = p2.GetActorsByType("scfactory")
				scstarport = p2.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p3 and p10.IsAlliedWith(p3) then
				fact = p3.GetActorsByType("fact")
				pyle = p3.GetActorsByType("pyle")
				hand = p3.GetActorsByType("hand")
				afld = p3.GetActorsByType("afld")
				weap = p3.GetActorsByType("weap")
				hpad = p3.GetActorsByType("hpad")
				rafact = p3.GetActorsByType("rafact")
				tent = p3.GetActorsByType("tent")
				barr = p3.GetActorsByType("barr")
				raweapa = p3.GetActorsByType("raweapa")
				raweap = p3.GetActorsByType("raweap")
				rahpad = p3.GetActorsByType("rahpad")
				raafld = p3.GetActorsByType("raafld")
				hq = p3.GetActorsByType("2100hq")
				fy1 = p3.GetActorsByType("2100fy1")
				fy2 = p3.GetActorsByType("2100fy2")
				sccommandcenter = p3.GetActorsByType("sccommandcenter")
				scbarracks = p3.GetActorsByType("scbarracks")
				scfactory = p3.GetActorsByType("scfactory")
				scstarport = p3.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p4 and p10.IsAlliedWith(p4) then
				fact = p4.GetActorsByType("fact")
				pyle = p4.GetActorsByType("pyle")
				hand = p4.GetActorsByType("hand")
				afld = p4.GetActorsByType("afld")
				weap = p4.GetActorsByType("weap")
				hpad = p4.GetActorsByType("hpad")
				rafact = p4.GetActorsByType("rafact")
				tent = p4.GetActorsByType("tent")
				barr = p4.GetActorsByType("barr")
				raweapa = p4.GetActorsByType("raweapa")
				raweap = p4.GetActorsByType("raweap")
				rahpad = p4.GetActorsByType("rahpad")
				raafld = p4.GetActorsByType("raafld")
				hq = p4.GetActorsByType("2100hq")
				fy1 = p4.GetActorsByType("2100fy1")
				fy2 = p4.GetActorsByType("2100fy2")
				sccommandcenter = p4.GetActorsByType("sccommandcenter")
				scbarracks = p4.GetActorsByType("scbarracks")
				scfactory = p4.GetActorsByType("scfactory")
				scstarport = p4.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p5 and p10.IsAlliedWith(p5) then
				fact = p5.GetActorsByType("fact")
				pyle = p5.GetActorsByType("pyle")
				hand = p5.GetActorsByType("hand")
				afld = p5.GetActorsByType("afld")
				weap = p5.GetActorsByType("weap")
				hpad = p5.GetActorsByType("hpad")
				rafact = p5.GetActorsByType("rafact")
				tent = p5.GetActorsByType("tent")
				barr = p5.GetActorsByType("barr")
				raweapa = p5.GetActorsByType("raweapa")
				raweap = p5.GetActorsByType("raweap")
				rahpad = p5.GetActorsByType("rahpad")
				raafld = p5.GetActorsByType("raafld")
				hq = p5.GetActorsByType("2100hq")
				fy1 = p5.GetActorsByType("2100fy1")
				fy2 = p5.GetActorsByType("2100fy2")
				sccommandcenter = p5.GetActorsByType("sccommandcenter")
				scbarracks = p5.GetActorsByType("scbarracks")
				scfactory = p5.GetActorsByType("scfactory")
				scstarport = p5.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p6 and p10.IsAlliedWith(p6) then
				fact = p6.GetActorsByType("fact")
				pyle = p6.GetActorsByType("pyle")
				hand = p6.GetActorsByType("hand")
				afld = p6.GetActorsByType("afld")
				weap = p6.GetActorsByType("weap")
				hpad = p6.GetActorsByType("hpad")
				rafact = p6.GetActorsByType("rafact")
				tent = p6.GetActorsByType("tent")
				barr = p6.GetActorsByType("barr")
				raweapa = p6.GetActorsByType("raweapa")
				raweap = p6.GetActorsByType("raweap")
				rahpad = p6.GetActorsByType("rahpad")
				raafld = p6.GetActorsByType("raafld")
				hq = p6.GetActorsByType("2100hq")
				fy1 = p6.GetActorsByType("2100fy1")
				fy2 = p6.GetActorsByType("2100fy2")
				sccommandcenter = p6.GetActorsByType("sccommandcenter")
				scbarracks = p6.GetActorsByType("scbarracks")
				scfactory = p6.GetActorsByType("scfactory")
				scstarport = p6.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p7 and p10.IsAlliedWith(p7) then
				fact = p7.GetActorsByType("fact")
				pyle = p7.GetActorsByType("pyle")
				hand = p7.GetActorsByType("hand")
				afld = p7.GetActorsByType("afld")
				weap = p7.GetActorsByType("weap")
				hpad = p7.GetActorsByType("hpad")
				rafact = p7.GetActorsByType("rafact")
				tent = p7.GetActorsByType("tent")
				barr = p7.GetActorsByType("barr")
				raweapa = p7.GetActorsByType("raweapa")
				raweap = p7.GetActorsByType("raweap")
				rahpad = p7.GetActorsByType("rahpad")
				raafld = p7.GetActorsByType("raafld")
				hq = p7.GetActorsByType("2100hq")
				fy1 = p7.GetActorsByType("2100fy1")
				fy2 = p7.GetActorsByType("2100fy2")
				sccommandcenter = p7.GetActorsByType("sccommandcenter")
				scbarracks = p7.GetActorsByType("scbarracks")
				scfactory = p7.GetActorsByType("scfactory")
				scstarport = p7.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p8 and p10.IsAlliedWith(p8) then
				fact = p8.GetActorsByType("fact")
				pyle = p8.GetActorsByType("pyle")
				hand = p8.GetActorsByType("hand")
				afld = p8.GetActorsByType("afld")
				weap = p8.GetActorsByType("weap")
				hpad = p8.GetActorsByType("hpad")
				rafact = p8.GetActorsByType("rafact")
				tent = p8.GetActorsByType("tent")
				barr = p8.GetActorsByType("barr")
				raweapa = p8.GetActorsByType("raweapa")
				raweap = p8.GetActorsByType("raweap")
				rahpad = p8.GetActorsByType("rahpad")
				raafld = p8.GetActorsByType("raafld")
				hq = p8.GetActorsByType("2100hq")
				fy1 = p8.GetActorsByType("2100fy1")
				fy2 = p8.GetActorsByType("2100fy2")
				sccommandcenter = p8.GetActorsByType("sccommandcenter")
				scbarracks = p8.GetActorsByType("scbarracks")
				scfactory = p8.GetActorsByType("scfactory")
				scstarport = p8.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p9 and p10.IsAlliedWith(p9) then
				fact = p9.GetActorsByType("fact")
				pyle = p9.GetActorsByType("pyle")
				hand = p9.GetActorsByType("hand")
				afld = p9.GetActorsByType("afld")
				weap = p9.GetActorsByType("weap")
				hpad = p9.GetActorsByType("hpad")
				rafact = p9.GetActorsByType("rafact")
				tent = p9.GetActorsByType("tent")
				barr = p9.GetActorsByType("barr")
				raweapa = p9.GetActorsByType("raweapa")
				raweap = p9.GetActorsByType("raweap")
				rahpad = p9.GetActorsByType("rahpad")
				raafld = p9.GetActorsByType("raafld")
				hq = p9.GetActorsByType("2100hq")
				fy1 = p9.GetActorsByType("2100fy1")
				fy2 = p9.GetActorsByType("2100fy2")
				sccommandcenter = p9.GetActorsByType("sccommandcenter")
				scbarracks = p9.GetActorsByType("scbarracks")
				scfactory = p9.GetActorsByType("scfactory")
				scstarport = p9.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p10cash()
	end)
end
p11cash = function()
	if p11 and p11.IsBot then
		citybottruck = p11.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p12 and p11.IsAlliedWith(p12) then
				fact = p12.GetActorsByType("fact")
				pyle = p12.GetActorsByType("pyle")
				hand = p12.GetActorsByType("hand")
				afld = p12.GetActorsByType("afld")
				weap = p12.GetActorsByType("weap")
				hpad = p12.GetActorsByType("hpad")
				rafact = p12.GetActorsByType("rafact")
				tent = p12.GetActorsByType("tent")
				barr = p12.GetActorsByType("barr")
				raweapa = p12.GetActorsByType("raweapa")
				raweap = p12.GetActorsByType("raweap")
				rahpad = p12.GetActorsByType("rahpad")
				raafld = p12.GetActorsByType("raafld")
				hq = p12.GetActorsByType("2100hq")
				fy1 = p12.GetActorsByType("2100fy1")
				fy2 = p12.GetActorsByType("2100fy2")
				sccommandcenter = p12.GetActorsByType("sccommandcenter")
				scbarracks = p12.GetActorsByType("scbarracks")
				scfactory = p12.GetActorsByType("scfactory")
				scstarport = p12.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p13 and p11.IsAlliedWith(p13) then
				fact = p13.GetActorsByType("fact")
				pyle = p13.GetActorsByType("pyle")
				hand = p13.GetActorsByType("hand")
				afld = p13.GetActorsByType("afld")
				weap = p13.GetActorsByType("weap")
				hpad = p13.GetActorsByType("hpad")
				rafact = p13.GetActorsByType("rafact")
				tent = p13.GetActorsByType("tent")
				barr = p13.GetActorsByType("barr")
				raweapa = p13.GetActorsByType("raweapa")
				raweap = p13.GetActorsByType("raweap")
				rahpad = p13.GetActorsByType("rahpad")
				raafld = p13.GetActorsByType("raafld")
				hq = p13.GetActorsByType("2100hq")
				fy1 = p13.GetActorsByType("2100fy1")
				fy2 = p13.GetActorsByType("2100fy2")
				sccommandcenter = p13.GetActorsByType("sccommandcenter")
				scbarracks = p13.GetActorsByType("scbarracks")
				scfactory = p13.GetActorsByType("scfactory")
				scstarport = p13.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p14 and p11.IsAlliedWith(p14) then
				fact = p14.GetActorsByType("fact")
				pyle = p14.GetActorsByType("pyle")
				hand = p14.GetActorsByType("hand")
				afld = p14.GetActorsByType("afld")
				weap = p14.GetActorsByType("weap")
				hpad = p14.GetActorsByType("hpad")
				rafact = p14.GetActorsByType("rafact")
				tent = p14.GetActorsByType("tent")
				barr = p14.GetActorsByType("barr")
				raweapa = p14.GetActorsByType("raweapa")
				raweap = p14.GetActorsByType("raweap")
				rahpad = p14.GetActorsByType("rahpad")
				raafld = p14.GetActorsByType("raafld")
				hq = p14.GetActorsByType("2100hq")
				fy1 = p14.GetActorsByType("2100fy1")
				fy2 = p14.GetActorsByType("2100fy2")
				sccommandcenter = p14.GetActorsByType("sccommandcenter")
				scbarracks = p14.GetActorsByType("scbarracks")
				scfactory = p14.GetActorsByType("scfactory")
				scstarport = p14.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p15 and p11.IsAlliedWith(p15) then
				fact = p15.GetActorsByType("fact")
				pyle = p15.GetActorsByType("pyle")
				hand = p15.GetActorsByType("hand")
				afld = p15.GetActorsByType("afld")
				weap = p15.GetActorsByType("weap")
				hpad = p15.GetActorsByType("hpad")
				rafact = p15.GetActorsByType("rafact")
				tent = p15.GetActorsByType("tent")
				barr = p15.GetActorsByType("barr")
				raweapa = p15.GetActorsByType("raweapa")
				raweap = p15.GetActorsByType("raweap")
				rahpad = p15.GetActorsByType("rahpad")
				raafld = p15.GetActorsByType("raafld")
				hq = p15.GetActorsByType("2100hq")
				fy1 = p15.GetActorsByType("2100fy1")
				fy2 = p15.GetActorsByType("2100fy2")
				sccommandcenter = p15.GetActorsByType("sccommandcenter")
				scbarracks = p15.GetActorsByType("scbarracks")
				scfactory = p15.GetActorsByType("scfactory")
				scstarport = p15.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p16 and p11.IsAlliedWith(p16) then
				fact = p16.GetActorsByType("fact")
				pyle = p16.GetActorsByType("pyle")
				hand = p16.GetActorsByType("hand")
				afld = p16.GetActorsByType("afld")
				weap = p16.GetActorsByType("weap")
				hpad = p16.GetActorsByType("hpad")
				rafact = p16.GetActorsByType("rafact")
				tent = p16.GetActorsByType("tent")
				barr = p16.GetActorsByType("barr")
				raweapa = p16.GetActorsByType("raweapa")
				raweap = p16.GetActorsByType("raweap")
				rahpad = p16.GetActorsByType("rahpad")
				raafld = p16.GetActorsByType("raafld")
				hq = p16.GetActorsByType("2100hq")
				fy1 = p16.GetActorsByType("2100fy1")
				fy2 = p16.GetActorsByType("2100fy2")
				sccommandcenter = p16.GetActorsByType("sccommandcenter")
				scbarracks = p16.GetActorsByType("scbarracks")
				scfactory = p16.GetActorsByType("scfactory")
				scstarport = p16.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p1 and p11.IsAlliedWith(p1) then
				fact = p1.GetActorsByType("fact")
				pyle = p1.GetActorsByType("pyle")
				hand = p1.GetActorsByType("hand")
				afld = p1.GetActorsByType("afld")
				weap = p1.GetActorsByType("weap")
				hpad = p1.GetActorsByType("hpad")
				rafact = p1.GetActorsByType("rafact")
				tent = p1.GetActorsByType("tent")
				barr = p1.GetActorsByType("barr")
				raweapa = p1.GetActorsByType("raweapa")
				raweap = p1.GetActorsByType("raweap")
				rahpad = p1.GetActorsByType("rahpad")
				raafld = p1.GetActorsByType("raafld")
				hq = p1.GetActorsByType("2100hq")
				fy1 = p1.GetActorsByType("2100fy1")
				fy2 = p1.GetActorsByType("2100fy2")
				sccommandcenter = p1.GetActorsByType("sccommandcenter")
				scbarracks = p1.GetActorsByType("scbarracks")
				scfactory = p1.GetActorsByType("scfactory")
				scstarport = p1.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p2 and p11.IsAlliedWith(p2) then
				fact = p2.GetActorsByType("fact")
				pyle = p2.GetActorsByType("pyle")
				hand = p2.GetActorsByType("hand")
				afld = p2.GetActorsByType("afld")
				weap = p2.GetActorsByType("weap")
				hpad = p2.GetActorsByType("hpad")
				rafact = p2.GetActorsByType("rafact")
				tent = p2.GetActorsByType("tent")
				barr = p2.GetActorsByType("barr")
				raweapa = p2.GetActorsByType("raweapa")
				raweap = p2.GetActorsByType("raweap")
				rahpad = p2.GetActorsByType("rahpad")
				raafld = p2.GetActorsByType("raafld")
				hq = p2.GetActorsByType("2100hq")
				fy1 = p2.GetActorsByType("2100fy1")
				fy2 = p2.GetActorsByType("2100fy2")
				sccommandcenter = p2.GetActorsByType("sccommandcenter")
				scbarracks = p2.GetActorsByType("scbarracks")
				scfactory = p2.GetActorsByType("scfactory")
				scstarport = p2.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p3 and p11.IsAlliedWith(p3) then
				fact = p3.GetActorsByType("fact")
				pyle = p3.GetActorsByType("pyle")
				hand = p3.GetActorsByType("hand")
				afld = p3.GetActorsByType("afld")
				weap = p3.GetActorsByType("weap")
				hpad = p3.GetActorsByType("hpad")
				rafact = p3.GetActorsByType("rafact")
				tent = p3.GetActorsByType("tent")
				barr = p3.GetActorsByType("barr")
				raweapa = p3.GetActorsByType("raweapa")
				raweap = p3.GetActorsByType("raweap")
				rahpad = p3.GetActorsByType("rahpad")
				raafld = p3.GetActorsByType("raafld")
				hq = p3.GetActorsByType("2100hq")
				fy1 = p3.GetActorsByType("2100fy1")
				fy2 = p3.GetActorsByType("2100fy2")
				sccommandcenter = p3.GetActorsByType("sccommandcenter")
				scbarracks = p3.GetActorsByType("scbarracks")
				scfactory = p3.GetActorsByType("scfactory")
				scstarport = p3.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p4 and p11.IsAlliedWith(p4) then
				fact = p4.GetActorsByType("fact")
				pyle = p4.GetActorsByType("pyle")
				hand = p4.GetActorsByType("hand")
				afld = p4.GetActorsByType("afld")
				weap = p4.GetActorsByType("weap")
				hpad = p4.GetActorsByType("hpad")
				rafact = p4.GetActorsByType("rafact")
				tent = p4.GetActorsByType("tent")
				barr = p4.GetActorsByType("barr")
				raweapa = p4.GetActorsByType("raweapa")
				raweap = p4.GetActorsByType("raweap")
				rahpad = p4.GetActorsByType("rahpad")
				raafld = p4.GetActorsByType("raafld")
				hq = p4.GetActorsByType("2100hq")
				fy1 = p4.GetActorsByType("2100fy1")
				fy2 = p4.GetActorsByType("2100fy2")
				sccommandcenter = p4.GetActorsByType("sccommandcenter")
				scbarracks = p4.GetActorsByType("scbarracks")
				scfactory = p4.GetActorsByType("scfactory")
				scstarport = p4.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p5 and p11.IsAlliedWith(p5) then
				fact = p5.GetActorsByType("fact")
				pyle = p5.GetActorsByType("pyle")
				hand = p5.GetActorsByType("hand")
				afld = p5.GetActorsByType("afld")
				weap = p5.GetActorsByType("weap")
				hpad = p5.GetActorsByType("hpad")
				rafact = p5.GetActorsByType("rafact")
				tent = p5.GetActorsByType("tent")
				barr = p5.GetActorsByType("barr")
				raweapa = p5.GetActorsByType("raweapa")
				raweap = p5.GetActorsByType("raweap")
				rahpad = p5.GetActorsByType("rahpad")
				raafld = p5.GetActorsByType("raafld")
				hq = p5.GetActorsByType("2100hq")
				fy1 = p5.GetActorsByType("2100fy1")
				fy2 = p5.GetActorsByType("2100fy2")
				sccommandcenter = p5.GetActorsByType("sccommandcenter")
				scbarracks = p5.GetActorsByType("scbarracks")
				scfactory = p5.GetActorsByType("scfactory")
				scstarport = p5.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p6 and p11.IsAlliedWith(p6) then
				fact = p6.GetActorsByType("fact")
				pyle = p6.GetActorsByType("pyle")
				hand = p6.GetActorsByType("hand")
				afld = p6.GetActorsByType("afld")
				weap = p6.GetActorsByType("weap")
				hpad = p6.GetActorsByType("hpad")
				rafact = p6.GetActorsByType("rafact")
				tent = p6.GetActorsByType("tent")
				barr = p6.GetActorsByType("barr")
				raweapa = p6.GetActorsByType("raweapa")
				raweap = p6.GetActorsByType("raweap")
				rahpad = p6.GetActorsByType("rahpad")
				raafld = p6.GetActorsByType("raafld")
				hq = p6.GetActorsByType("2100hq")
				fy1 = p6.GetActorsByType("2100fy1")
				fy2 = p6.GetActorsByType("2100fy2")
				sccommandcenter = p6.GetActorsByType("sccommandcenter")
				scbarracks = p6.GetActorsByType("scbarracks")
				scfactory = p6.GetActorsByType("scfactory")
				scstarport = p6.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p7 and p11.IsAlliedWith(p7) then
				fact = p7.GetActorsByType("fact")
				pyle = p7.GetActorsByType("pyle")
				hand = p7.GetActorsByType("hand")
				afld = p7.GetActorsByType("afld")
				weap = p7.GetActorsByType("weap")
				hpad = p7.GetActorsByType("hpad")
				rafact = p7.GetActorsByType("rafact")
				tent = p7.GetActorsByType("tent")
				barr = p7.GetActorsByType("barr")
				raweapa = p7.GetActorsByType("raweapa")
				raweap = p7.GetActorsByType("raweap")
				rahpad = p7.GetActorsByType("rahpad")
				raafld = p7.GetActorsByType("raafld")
				hq = p7.GetActorsByType("2100hq")
				fy1 = p7.GetActorsByType("2100fy1")
				fy2 = p7.GetActorsByType("2100fy2")
				sccommandcenter = p7.GetActorsByType("sccommandcenter")
				scbarracks = p7.GetActorsByType("scbarracks")
				scfactory = p7.GetActorsByType("scfactory")
				scstarport = p7.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p8 and p11.IsAlliedWith(p8) then
				fact = p8.GetActorsByType("fact")
				pyle = p8.GetActorsByType("pyle")
				hand = p8.GetActorsByType("hand")
				afld = p8.GetActorsByType("afld")
				weap = p8.GetActorsByType("weap")
				hpad = p8.GetActorsByType("hpad")
				rafact = p8.GetActorsByType("rafact")
				tent = p8.GetActorsByType("tent")
				barr = p8.GetActorsByType("barr")
				raweapa = p8.GetActorsByType("raweapa")
				raweap = p8.GetActorsByType("raweap")
				rahpad = p8.GetActorsByType("rahpad")
				raafld = p8.GetActorsByType("raafld")
				hq = p8.GetActorsByType("2100hq")
				fy1 = p8.GetActorsByType("2100fy1")
				fy2 = p8.GetActorsByType("2100fy2")
				sccommandcenter = p8.GetActorsByType("sccommandcenter")
				scbarracks = p8.GetActorsByType("scbarracks")
				scfactory = p8.GetActorsByType("scfactory")
				scstarport = p8.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p9 and p11.IsAlliedWith(p9) then
				fact = p9.GetActorsByType("fact")
				pyle = p9.GetActorsByType("pyle")
				hand = p9.GetActorsByType("hand")
				afld = p9.GetActorsByType("afld")
				weap = p9.GetActorsByType("weap")
				hpad = p9.GetActorsByType("hpad")
				rafact = p9.GetActorsByType("rafact")
				tent = p9.GetActorsByType("tent")
				barr = p9.GetActorsByType("barr")
				raweapa = p9.GetActorsByType("raweapa")
				raweap = p9.GetActorsByType("raweap")
				rahpad = p9.GetActorsByType("rahpad")
				raafld = p9.GetActorsByType("raafld")
				hq = p9.GetActorsByType("2100hq")
				fy1 = p9.GetActorsByType("2100fy1")
				fy2 = p9.GetActorsByType("2100fy2")
				sccommandcenter = p9.GetActorsByType("sccommandcenter")
				scbarracks = p9.GetActorsByType("scbarracks")
				scfactory = p9.GetActorsByType("scfactory")
				scstarport = p9.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p10 and p11.IsAlliedWith(p10) then
				fact = p10.GetActorsByType("fact")
				pyle = p10.GetActorsByType("pyle")
				hand = p10.GetActorsByType("hand")
				afld = p10.GetActorsByType("afld")
				weap = p10.GetActorsByType("weap")
				hpad = p10.GetActorsByType("hpad")
				rafact = p10.GetActorsByType("rafact")
				tent = p10.GetActorsByType("tent")
				barr = p10.GetActorsByType("barr")
				raweapa = p10.GetActorsByType("raweapa")
				raweap = p10.GetActorsByType("raweap")
				rahpad = p10.GetActorsByType("rahpad")
				raafld = p10.GetActorsByType("raafld")
				hq = p10.GetActorsByType("2100hq")
				fy1 = p10.GetActorsByType("2100fy1")
				fy2 = p10.GetActorsByType("2100fy2")
				sccommandcenter = p10.GetActorsByType("sccommandcenter")
				scbarracks = p10.GetActorsByType("scbarracks")
				scfactory = p10.GetActorsByType("scfactory")
				scstarport = p10.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p11cash()
	end)
end
p12cash = function()
	if p12 and p12.IsBot then
		citybottruck = p12.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p13 and p12.IsAlliedWith(p13) then
				fact = p13.GetActorsByType("fact")
				pyle = p13.GetActorsByType("pyle")
				hand = p13.GetActorsByType("hand")
				afld = p13.GetActorsByType("afld")
				weap = p13.GetActorsByType("weap")
				hpad = p13.GetActorsByType("hpad")
				rafact = p13.GetActorsByType("rafact")
				tent = p13.GetActorsByType("tent")
				barr = p13.GetActorsByType("barr")
				raweapa = p13.GetActorsByType("raweapa")
				raweap = p13.GetActorsByType("raweap")
				rahpad = p13.GetActorsByType("rahpad")
				raafld = p13.GetActorsByType("raafld")
				hq = p13.GetActorsByType("2100hq")
				fy1 = p13.GetActorsByType("2100fy1")
				fy2 = p13.GetActorsByType("2100fy2")
				sccommandcenter = p13.GetActorsByType("sccommandcenter")
				scbarracks = p13.GetActorsByType("scbarracks")
				scfactory = p13.GetActorsByType("scfactory")
				scstarport = p13.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p14 and p12.IsAlliedWith(p14) then
				fact = p14.GetActorsByType("fact")
				pyle = p14.GetActorsByType("pyle")
				hand = p14.GetActorsByType("hand")
				afld = p14.GetActorsByType("afld")
				weap = p14.GetActorsByType("weap")
				hpad = p14.GetActorsByType("hpad")
				rafact = p14.GetActorsByType("rafact")
				tent = p14.GetActorsByType("tent")
				barr = p14.GetActorsByType("barr")
				raweapa = p14.GetActorsByType("raweapa")
				raweap = p14.GetActorsByType("raweap")
				rahpad = p14.GetActorsByType("rahpad")
				raafld = p14.GetActorsByType("raafld")
				hq = p14.GetActorsByType("2100hq")
				fy1 = p14.GetActorsByType("2100fy1")
				fy2 = p14.GetActorsByType("2100fy2")
				sccommandcenter = p14.GetActorsByType("sccommandcenter")
				scbarracks = p14.GetActorsByType("scbarracks")
				scfactory = p14.GetActorsByType("scfactory")
				scstarport = p14.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p15 and p12.IsAlliedWith(p15) then
				fact = p15.GetActorsByType("fact")
				pyle = p15.GetActorsByType("pyle")
				hand = p15.GetActorsByType("hand")
				afld = p15.GetActorsByType("afld")
				weap = p15.GetActorsByType("weap")
				hpad = p15.GetActorsByType("hpad")
				rafact = p15.GetActorsByType("rafact")
				tent = p15.GetActorsByType("tent")
				barr = p15.GetActorsByType("barr")
				raweapa = p15.GetActorsByType("raweapa")
				raweap = p15.GetActorsByType("raweap")
				rahpad = p15.GetActorsByType("rahpad")
				raafld = p15.GetActorsByType("raafld")
				hq = p15.GetActorsByType("2100hq")
				fy1 = p15.GetActorsByType("2100fy1")
				fy2 = p15.GetActorsByType("2100fy2")
				sccommandcenter = p15.GetActorsByType("sccommandcenter")
				scbarracks = p15.GetActorsByType("scbarracks")
				scfactory = p15.GetActorsByType("scfactory")
				scstarport = p15.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p16 and p12.IsAlliedWith(p16) then
				fact = p16.GetActorsByType("fact")
				pyle = p16.GetActorsByType("pyle")
				hand = p16.GetActorsByType("hand")
				afld = p16.GetActorsByType("afld")
				weap = p16.GetActorsByType("weap")
				hpad = p16.GetActorsByType("hpad")
				rafact = p16.GetActorsByType("rafact")
				tent = p16.GetActorsByType("tent")
				barr = p16.GetActorsByType("barr")
				raweapa = p16.GetActorsByType("raweapa")
				raweap = p16.GetActorsByType("raweap")
				rahpad = p16.GetActorsByType("rahpad")
				raafld = p16.GetActorsByType("raafld")
				hq = p16.GetActorsByType("2100hq")
				fy1 = p16.GetActorsByType("2100fy1")
				fy2 = p16.GetActorsByType("2100fy2")
				sccommandcenter = p16.GetActorsByType("sccommandcenter")
				scbarracks = p16.GetActorsByType("scbarracks")
				scfactory = p16.GetActorsByType("scfactory")
				scstarport = p16.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p1 and p12.IsAlliedWith(p1) then
				fact = p1.GetActorsByType("fact")
				pyle = p1.GetActorsByType("pyle")
				hand = p1.GetActorsByType("hand")
				afld = p1.GetActorsByType("afld")
				weap = p1.GetActorsByType("weap")
				hpad = p1.GetActorsByType("hpad")
				rafact = p1.GetActorsByType("rafact")
				tent = p1.GetActorsByType("tent")
				barr = p1.GetActorsByType("barr")
				raweapa = p1.GetActorsByType("raweapa")
				raweap = p1.GetActorsByType("raweap")
				rahpad = p1.GetActorsByType("rahpad")
				raafld = p1.GetActorsByType("raafld")
				hq = p1.GetActorsByType("2100hq")
				fy1 = p1.GetActorsByType("2100fy1")
				fy2 = p1.GetActorsByType("2100fy2")
				sccommandcenter = p1.GetActorsByType("sccommandcenter")
				scbarracks = p1.GetActorsByType("scbarracks")
				scfactory = p1.GetActorsByType("scfactory")
				scstarport = p1.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p2 and p12.IsAlliedWith(p2) then
				fact = p2.GetActorsByType("fact")
				pyle = p2.GetActorsByType("pyle")
				hand = p2.GetActorsByType("hand")
				afld = p2.GetActorsByType("afld")
				weap = p2.GetActorsByType("weap")
				hpad = p2.GetActorsByType("hpad")
				rafact = p2.GetActorsByType("rafact")
				tent = p2.GetActorsByType("tent")
				barr = p2.GetActorsByType("barr")
				raweapa = p2.GetActorsByType("raweapa")
				raweap = p2.GetActorsByType("raweap")
				rahpad = p2.GetActorsByType("rahpad")
				raafld = p2.GetActorsByType("raafld")
				hq = p2.GetActorsByType("2100hq")
				fy1 = p2.GetActorsByType("2100fy1")
				fy2 = p2.GetActorsByType("2100fy2")
				sccommandcenter = p2.GetActorsByType("sccommandcenter")
				scbarracks = p2.GetActorsByType("scbarracks")
				scfactory = p2.GetActorsByType("scfactory")
				scstarport = p2.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p3 and p12.IsAlliedWith(p3) then
				fact = p3.GetActorsByType("fact")
				pyle = p3.GetActorsByType("pyle")
				hand = p3.GetActorsByType("hand")
				afld = p3.GetActorsByType("afld")
				weap = p3.GetActorsByType("weap")
				hpad = p3.GetActorsByType("hpad")
				rafact = p3.GetActorsByType("rafact")
				tent = p3.GetActorsByType("tent")
				barr = p3.GetActorsByType("barr")
				raweapa = p3.GetActorsByType("raweapa")
				raweap = p3.GetActorsByType("raweap")
				rahpad = p3.GetActorsByType("rahpad")
				raafld = p3.GetActorsByType("raafld")
				hq = p3.GetActorsByType("2100hq")
				fy1 = p3.GetActorsByType("2100fy1")
				fy2 = p3.GetActorsByType("2100fy2")
				sccommandcenter = p3.GetActorsByType("sccommandcenter")
				scbarracks = p3.GetActorsByType("scbarracks")
				scfactory = p3.GetActorsByType("scfactory")
				scstarport = p3.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p4 and p12.IsAlliedWith(p4) then
				fact = p4.GetActorsByType("fact")
				pyle = p4.GetActorsByType("pyle")
				hand = p4.GetActorsByType("hand")
				afld = p4.GetActorsByType("afld")
				weap = p4.GetActorsByType("weap")
				hpad = p4.GetActorsByType("hpad")
				rafact = p4.GetActorsByType("rafact")
				tent = p4.GetActorsByType("tent")
				barr = p4.GetActorsByType("barr")
				raweapa = p4.GetActorsByType("raweapa")
				raweap = p4.GetActorsByType("raweap")
				rahpad = p4.GetActorsByType("rahpad")
				raafld = p4.GetActorsByType("raafld")
				hq = p4.GetActorsByType("2100hq")
				fy1 = p4.GetActorsByType("2100fy1")
				fy2 = p4.GetActorsByType("2100fy2")
				sccommandcenter = p4.GetActorsByType("sccommandcenter")
				scbarracks = p4.GetActorsByType("scbarracks")
				scfactory = p4.GetActorsByType("scfactory")
				scstarport = p4.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p5 and p12.IsAlliedWith(p5) then
				fact = p5.GetActorsByType("fact")
				pyle = p5.GetActorsByType("pyle")
				hand = p5.GetActorsByType("hand")
				afld = p5.GetActorsByType("afld")
				weap = p5.GetActorsByType("weap")
				hpad = p5.GetActorsByType("hpad")
				rafact = p5.GetActorsByType("rafact")
				tent = p5.GetActorsByType("tent")
				barr = p5.GetActorsByType("barr")
				raweapa = p5.GetActorsByType("raweapa")
				raweap = p5.GetActorsByType("raweap")
				rahpad = p5.GetActorsByType("rahpad")
				raafld = p5.GetActorsByType("raafld")
				hq = p5.GetActorsByType("2100hq")
				fy1 = p5.GetActorsByType("2100fy1")
				fy2 = p5.GetActorsByType("2100fy2")
				sccommandcenter = p5.GetActorsByType("sccommandcenter")
				scbarracks = p5.GetActorsByType("scbarracks")
				scfactory = p5.GetActorsByType("scfactory")
				scstarport = p5.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p6 and p12.IsAlliedWith(p6) then
				fact = p6.GetActorsByType("fact")
				pyle = p6.GetActorsByType("pyle")
				hand = p6.GetActorsByType("hand")
				afld = p6.GetActorsByType("afld")
				weap = p6.GetActorsByType("weap")
				hpad = p6.GetActorsByType("hpad")
				rafact = p6.GetActorsByType("rafact")
				tent = p6.GetActorsByType("tent")
				barr = p6.GetActorsByType("barr")
				raweapa = p6.GetActorsByType("raweapa")
				raweap = p6.GetActorsByType("raweap")
				rahpad = p6.GetActorsByType("rahpad")
				raafld = p6.GetActorsByType("raafld")
				hq = p6.GetActorsByType("2100hq")
				fy1 = p6.GetActorsByType("2100fy1")
				fy2 = p6.GetActorsByType("2100fy2")
				sccommandcenter = p6.GetActorsByType("sccommandcenter")
				scbarracks = p6.GetActorsByType("scbarracks")
				scfactory = p6.GetActorsByType("scfactory")
				scstarport = p6.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p7 and p12.IsAlliedWith(p7) then
				fact = p7.GetActorsByType("fact")
				pyle = p7.GetActorsByType("pyle")
				hand = p7.GetActorsByType("hand")
				afld = p7.GetActorsByType("afld")
				weap = p7.GetActorsByType("weap")
				hpad = p7.GetActorsByType("hpad")
				rafact = p7.GetActorsByType("rafact")
				tent = p7.GetActorsByType("tent")
				barr = p7.GetActorsByType("barr")
				raweapa = p7.GetActorsByType("raweapa")
				raweap = p7.GetActorsByType("raweap")
				rahpad = p7.GetActorsByType("rahpad")
				raafld = p7.GetActorsByType("raafld")
				hq = p7.GetActorsByType("2100hq")
				fy1 = p7.GetActorsByType("2100fy1")
				fy2 = p7.GetActorsByType("2100fy2")
				sccommandcenter = p7.GetActorsByType("sccommandcenter")
				scbarracks = p7.GetActorsByType("scbarracks")
				scfactory = p7.GetActorsByType("scfactory")
				scstarport = p7.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p8 and p12.IsAlliedWith(p8) then
				fact = p8.GetActorsByType("fact")
				pyle = p8.GetActorsByType("pyle")
				hand = p8.GetActorsByType("hand")
				afld = p8.GetActorsByType("afld")
				weap = p8.GetActorsByType("weap")
				hpad = p8.GetActorsByType("hpad")
				rafact = p8.GetActorsByType("rafact")
				tent = p8.GetActorsByType("tent")
				barr = p8.GetActorsByType("barr")
				raweapa = p8.GetActorsByType("raweapa")
				raweap = p8.GetActorsByType("raweap")
				rahpad = p8.GetActorsByType("rahpad")
				raafld = p8.GetActorsByType("raafld")
				hq = p8.GetActorsByType("2100hq")
				fy1 = p8.GetActorsByType("2100fy1")
				fy2 = p8.GetActorsByType("2100fy2")
				sccommandcenter = p8.GetActorsByType("sccommandcenter")
				scbarracks = p8.GetActorsByType("scbarracks")
				scfactory = p8.GetActorsByType("scfactory")
				scstarport = p8.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p9 and p12.IsAlliedWith(p9) then
				fact = p9.GetActorsByType("fact")
				pyle = p9.GetActorsByType("pyle")
				hand = p9.GetActorsByType("hand")
				afld = p9.GetActorsByType("afld")
				weap = p9.GetActorsByType("weap")
				hpad = p9.GetActorsByType("hpad")
				rafact = p9.GetActorsByType("rafact")
				tent = p9.GetActorsByType("tent")
				barr = p9.GetActorsByType("barr")
				raweapa = p9.GetActorsByType("raweapa")
				raweap = p9.GetActorsByType("raweap")
				rahpad = p9.GetActorsByType("rahpad")
				raafld = p9.GetActorsByType("raafld")
				hq = p9.GetActorsByType("2100hq")
				fy1 = p9.GetActorsByType("2100fy1")
				fy2 = p9.GetActorsByType("2100fy2")
				sccommandcenter = p9.GetActorsByType("sccommandcenter")
				scbarracks = p9.GetActorsByType("scbarracks")
				scfactory = p9.GetActorsByType("scfactory")
				scstarport = p9.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p10 and p12.IsAlliedWith(p10) then
				fact = p10.GetActorsByType("fact")
				pyle = p10.GetActorsByType("pyle")
				hand = p10.GetActorsByType("hand")
				afld = p10.GetActorsByType("afld")
				weap = p10.GetActorsByType("weap")
				hpad = p10.GetActorsByType("hpad")
				rafact = p10.GetActorsByType("rafact")
				tent = p10.GetActorsByType("tent")
				barr = p10.GetActorsByType("barr")
				raweapa = p10.GetActorsByType("raweapa")
				raweap = p10.GetActorsByType("raweap")
				rahpad = p10.GetActorsByType("rahpad")
				raafld = p10.GetActorsByType("raafld")
				hq = p10.GetActorsByType("2100hq")
				fy1 = p10.GetActorsByType("2100fy1")
				fy2 = p10.GetActorsByType("2100fy2")
				sccommandcenter = p10.GetActorsByType("sccommandcenter")
				scbarracks = p10.GetActorsByType("scbarracks")
				scfactory = p10.GetActorsByType("scfactory")
				scstarport = p10.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p11 and p12.IsAlliedWith(p11) then
				fact = p11.GetActorsByType("fact")
				pyle = p11.GetActorsByType("pyle")
				hand = p11.GetActorsByType("hand")
				afld = p11.GetActorsByType("afld")
				weap = p11.GetActorsByType("weap")
				hpad = p11.GetActorsByType("hpad")
				rafact = p11.GetActorsByType("rafact")
				tent = p11.GetActorsByType("tent")
				barr = p11.GetActorsByType("barr")
				raweapa = p11.GetActorsByType("raweapa")
				raweap = p11.GetActorsByType("raweap")
				rahpad = p11.GetActorsByType("rahpad")
				raafld = p11.GetActorsByType("raafld")
				hq = p11.GetActorsByType("2100hq")
				fy1 = p11.GetActorsByType("2100fy1")
				fy2 = p11.GetActorsByType("2100fy2")
				sccommandcenter = p11.GetActorsByType("sccommandcenter")
				scbarracks = p11.GetActorsByType("scbarracks")
				scfactory = p11.GetActorsByType("scfactory")
				scstarport = p11.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p12cash()
	end)
end
p13cash = function()
	if p13 and p13.IsBot then
		citybottruck = p13.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p14 and p13.IsAlliedWith(p14) then
				fact = p14.GetActorsByType("fact")
				pyle = p14.GetActorsByType("pyle")
				hand = p14.GetActorsByType("hand")
				afld = p14.GetActorsByType("afld")
				weap = p14.GetActorsByType("weap")
				hpad = p14.GetActorsByType("hpad")
				rafact = p14.GetActorsByType("rafact")
				tent = p14.GetActorsByType("tent")
				barr = p14.GetActorsByType("barr")
				raweapa = p14.GetActorsByType("raweapa")
				raweap = p14.GetActorsByType("raweap")
				rahpad = p14.GetActorsByType("rahpad")
				raafld = p14.GetActorsByType("raafld")
				hq = p14.GetActorsByType("2100hq")
				fy1 = p14.GetActorsByType("2100fy1")
				fy2 = p14.GetActorsByType("2100fy2")
				sccommandcenter = p14.GetActorsByType("sccommandcenter")
				scbarracks = p14.GetActorsByType("scbarracks")
				scfactory = p14.GetActorsByType("scfactory")
				scstarport = p14.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p15 and p13.IsAlliedWith(p15) then
				fact = p15.GetActorsByType("fact")
				pyle = p15.GetActorsByType("pyle")
				hand = p15.GetActorsByType("hand")
				afld = p15.GetActorsByType("afld")
				weap = p15.GetActorsByType("weap")
				hpad = p15.GetActorsByType("hpad")
				rafact = p15.GetActorsByType("rafact")
				tent = p15.GetActorsByType("tent")
				barr = p15.GetActorsByType("barr")
				raweapa = p15.GetActorsByType("raweapa")
				raweap = p15.GetActorsByType("raweap")
				rahpad = p15.GetActorsByType("rahpad")
				raafld = p15.GetActorsByType("raafld")
				hq = p15.GetActorsByType("2100hq")
				fy1 = p15.GetActorsByType("2100fy1")
				fy2 = p15.GetActorsByType("2100fy2")
				sccommandcenter = p15.GetActorsByType("sccommandcenter")
				scbarracks = p15.GetActorsByType("scbarracks")
				scfactory = p15.GetActorsByType("scfactory")
				scstarport = p15.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p16 and p13.IsAlliedWith(p16) then
				fact = p16.GetActorsByType("fact")
				pyle = p16.GetActorsByType("pyle")
				hand = p16.GetActorsByType("hand")
				afld = p16.GetActorsByType("afld")
				weap = p16.GetActorsByType("weap")
				hpad = p16.GetActorsByType("hpad")
				rafact = p16.GetActorsByType("rafact")
				tent = p16.GetActorsByType("tent")
				barr = p16.GetActorsByType("barr")
				raweapa = p16.GetActorsByType("raweapa")
				raweap = p16.GetActorsByType("raweap")
				rahpad = p16.GetActorsByType("rahpad")
				raafld = p16.GetActorsByType("raafld")
				hq = p16.GetActorsByType("2100hq")
				fy1 = p16.GetActorsByType("2100fy1")
				fy2 = p16.GetActorsByType("2100fy2")
				sccommandcenter = p16.GetActorsByType("sccommandcenter")
				scbarracks = p16.GetActorsByType("scbarracks")
				scfactory = p16.GetActorsByType("scfactory")
				scstarport = p16.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p1 and p13.IsAlliedWith(p1) then
				fact = p1.GetActorsByType("fact")
				pyle = p1.GetActorsByType("pyle")
				hand = p1.GetActorsByType("hand")
				afld = p1.GetActorsByType("afld")
				weap = p1.GetActorsByType("weap")
				hpad = p1.GetActorsByType("hpad")
				rafact = p1.GetActorsByType("rafact")
				tent = p1.GetActorsByType("tent")
				barr = p1.GetActorsByType("barr")
				raweapa = p1.GetActorsByType("raweapa")
				raweap = p1.GetActorsByType("raweap")
				rahpad = p1.GetActorsByType("rahpad")
				raafld = p1.GetActorsByType("raafld")
				hq = p1.GetActorsByType("2100hq")
				fy1 = p1.GetActorsByType("2100fy1")
				fy2 = p1.GetActorsByType("2100fy2")
				sccommandcenter = p1.GetActorsByType("sccommandcenter")
				scbarracks = p1.GetActorsByType("scbarracks")
				scfactory = p1.GetActorsByType("scfactory")
				scstarport = p1.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p2 and p13.IsAlliedWith(p2) then
				fact = p2.GetActorsByType("fact")
				pyle = p2.GetActorsByType("pyle")
				hand = p2.GetActorsByType("hand")
				afld = p2.GetActorsByType("afld")
				weap = p2.GetActorsByType("weap")
				hpad = p2.GetActorsByType("hpad")
				rafact = p2.GetActorsByType("rafact")
				tent = p2.GetActorsByType("tent")
				barr = p2.GetActorsByType("barr")
				raweapa = p2.GetActorsByType("raweapa")
				raweap = p2.GetActorsByType("raweap")
				rahpad = p2.GetActorsByType("rahpad")
				raafld = p2.GetActorsByType("raafld")
				hq = p2.GetActorsByType("2100hq")
				fy1 = p2.GetActorsByType("2100fy1")
				fy2 = p2.GetActorsByType("2100fy2")
				sccommandcenter = p2.GetActorsByType("sccommandcenter")
				scbarracks = p2.GetActorsByType("scbarracks")
				scfactory = p2.GetActorsByType("scfactory")
				scstarport = p2.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p3 and p13.IsAlliedWith(p3) then
				fact = p3.GetActorsByType("fact")
				pyle = p3.GetActorsByType("pyle")
				hand = p3.GetActorsByType("hand")
				afld = p3.GetActorsByType("afld")
				weap = p3.GetActorsByType("weap")
				hpad = p3.GetActorsByType("hpad")
				rafact = p3.GetActorsByType("rafact")
				tent = p3.GetActorsByType("tent")
				barr = p3.GetActorsByType("barr")
				raweapa = p3.GetActorsByType("raweapa")
				raweap = p3.GetActorsByType("raweap")
				rahpad = p3.GetActorsByType("rahpad")
				raafld = p3.GetActorsByType("raafld")
				hq = p3.GetActorsByType("2100hq")
				fy1 = p3.GetActorsByType("2100fy1")
				fy2 = p3.GetActorsByType("2100fy2")
				sccommandcenter = p3.GetActorsByType("sccommandcenter")
				scbarracks = p3.GetActorsByType("scbarracks")
				scfactory = p3.GetActorsByType("scfactory")
				scstarport = p3.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p4 and p13.IsAlliedWith(p4) then
				fact = p4.GetActorsByType("fact")
				pyle = p4.GetActorsByType("pyle")
				hand = p4.GetActorsByType("hand")
				afld = p4.GetActorsByType("afld")
				weap = p4.GetActorsByType("weap")
				hpad = p4.GetActorsByType("hpad")
				rafact = p4.GetActorsByType("rafact")
				tent = p4.GetActorsByType("tent")
				barr = p4.GetActorsByType("barr")
				raweapa = p4.GetActorsByType("raweapa")
				raweap = p4.GetActorsByType("raweap")
				rahpad = p4.GetActorsByType("rahpad")
				raafld = p4.GetActorsByType("raafld")
				hq = p4.GetActorsByType("2100hq")
				fy1 = p4.GetActorsByType("2100fy1")
				fy2 = p4.GetActorsByType("2100fy2")
				sccommandcenter = p4.GetActorsByType("sccommandcenter")
				scbarracks = p4.GetActorsByType("scbarracks")
				scfactory = p4.GetActorsByType("scfactory")
				scstarport = p4.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p5 and p13.IsAlliedWith(p5) then
				fact = p5.GetActorsByType("fact")
				pyle = p5.GetActorsByType("pyle")
				hand = p5.GetActorsByType("hand")
				afld = p5.GetActorsByType("afld")
				weap = p5.GetActorsByType("weap")
				hpad = p5.GetActorsByType("hpad")
				rafact = p5.GetActorsByType("rafact")
				tent = p5.GetActorsByType("tent")
				barr = p5.GetActorsByType("barr")
				raweapa = p5.GetActorsByType("raweapa")
				raweap = p5.GetActorsByType("raweap")
				rahpad = p5.GetActorsByType("rahpad")
				raafld = p5.GetActorsByType("raafld")
				hq = p5.GetActorsByType("2100hq")
				fy1 = p5.GetActorsByType("2100fy1")
				fy2 = p5.GetActorsByType("2100fy2")
				sccommandcenter = p5.GetActorsByType("sccommandcenter")
				scbarracks = p5.GetActorsByType("scbarracks")
				scfactory = p5.GetActorsByType("scfactory")
				scstarport = p5.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p6 and p13.IsAlliedWith(p6) then
				fact = p6.GetActorsByType("fact")
				pyle = p6.GetActorsByType("pyle")
				hand = p6.GetActorsByType("hand")
				afld = p6.GetActorsByType("afld")
				weap = p6.GetActorsByType("weap")
				hpad = p6.GetActorsByType("hpad")
				rafact = p6.GetActorsByType("rafact")
				tent = p6.GetActorsByType("tent")
				barr = p6.GetActorsByType("barr")
				raweapa = p6.GetActorsByType("raweapa")
				raweap = p6.GetActorsByType("raweap")
				rahpad = p6.GetActorsByType("rahpad")
				raafld = p6.GetActorsByType("raafld")
				hq = p6.GetActorsByType("2100hq")
				fy1 = p6.GetActorsByType("2100fy1")
				fy2 = p6.GetActorsByType("2100fy2")
				sccommandcenter = p6.GetActorsByType("sccommandcenter")
				scbarracks = p6.GetActorsByType("scbarracks")
				scfactory = p6.GetActorsByType("scfactory")
				scstarport = p6.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p7 and p13.IsAlliedWith(p7) then
				fact = p7.GetActorsByType("fact")
				pyle = p7.GetActorsByType("pyle")
				hand = p7.GetActorsByType("hand")
				afld = p7.GetActorsByType("afld")
				weap = p7.GetActorsByType("weap")
				hpad = p7.GetActorsByType("hpad")
				rafact = p7.GetActorsByType("rafact")
				tent = p7.GetActorsByType("tent")
				barr = p7.GetActorsByType("barr")
				raweapa = p7.GetActorsByType("raweapa")
				raweap = p7.GetActorsByType("raweap")
				rahpad = p7.GetActorsByType("rahpad")
				raafld = p7.GetActorsByType("raafld")
				hq = p7.GetActorsByType("2100hq")
				fy1 = p7.GetActorsByType("2100fy1")
				fy2 = p7.GetActorsByType("2100fy2")
				sccommandcenter = p7.GetActorsByType("sccommandcenter")
				scbarracks = p7.GetActorsByType("scbarracks")
				scfactory = p7.GetActorsByType("scfactory")
				scstarport = p7.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p8 and p13.IsAlliedWith(p8) then
				fact = p8.GetActorsByType("fact")
				pyle = p8.GetActorsByType("pyle")
				hand = p8.GetActorsByType("hand")
				afld = p8.GetActorsByType("afld")
				weap = p8.GetActorsByType("weap")
				hpad = p8.GetActorsByType("hpad")
				rafact = p8.GetActorsByType("rafact")
				tent = p8.GetActorsByType("tent")
				barr = p8.GetActorsByType("barr")
				raweapa = p8.GetActorsByType("raweapa")
				raweap = p8.GetActorsByType("raweap")
				rahpad = p8.GetActorsByType("rahpad")
				raafld = p8.GetActorsByType("raafld")
				hq = p8.GetActorsByType("2100hq")
				fy1 = p8.GetActorsByType("2100fy1")
				fy2 = p8.GetActorsByType("2100fy2")
				sccommandcenter = p8.GetActorsByType("sccommandcenter")
				scbarracks = p8.GetActorsByType("scbarracks")
				scfactory = p8.GetActorsByType("scfactory")
				scstarport = p8.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p9 and p13.IsAlliedWith(p9) then
				fact = p9.GetActorsByType("fact")
				pyle = p9.GetActorsByType("pyle")
				hand = p9.GetActorsByType("hand")
				afld = p9.GetActorsByType("afld")
				weap = p9.GetActorsByType("weap")
				hpad = p9.GetActorsByType("hpad")
				rafact = p9.GetActorsByType("rafact")
				tent = p9.GetActorsByType("tent")
				barr = p9.GetActorsByType("barr")
				raweapa = p9.GetActorsByType("raweapa")
				raweap = p9.GetActorsByType("raweap")
				rahpad = p9.GetActorsByType("rahpad")
				raafld = p9.GetActorsByType("raafld")
				hq = p9.GetActorsByType("2100hq")
				fy1 = p9.GetActorsByType("2100fy1")
				fy2 = p9.GetActorsByType("2100fy2")
				sccommandcenter = p9.GetActorsByType("sccommandcenter")
				scbarracks = p9.GetActorsByType("scbarracks")
				scfactory = p9.GetActorsByType("scfactory")
				scstarport = p9.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p10 and p13.IsAlliedWith(p10) then
				fact = p10.GetActorsByType("fact")
				pyle = p10.GetActorsByType("pyle")
				hand = p10.GetActorsByType("hand")
				afld = p10.GetActorsByType("afld")
				weap = p10.GetActorsByType("weap")
				hpad = p10.GetActorsByType("hpad")
				rafact = p10.GetActorsByType("rafact")
				tent = p10.GetActorsByType("tent")
				barr = p10.GetActorsByType("barr")
				raweapa = p10.GetActorsByType("raweapa")
				raweap = p10.GetActorsByType("raweap")
				rahpad = p10.GetActorsByType("rahpad")
				raafld = p10.GetActorsByType("raafld")
				hq = p10.GetActorsByType("2100hq")
				fy1 = p10.GetActorsByType("2100fy1")
				fy2 = p10.GetActorsByType("2100fy2")
				sccommandcenter = p10.GetActorsByType("sccommandcenter")
				scbarracks = p10.GetActorsByType("scbarracks")
				scfactory = p10.GetActorsByType("scfactory")
				scstarport = p10.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p11 and p13.IsAlliedWith(p11) then
				fact = p11.GetActorsByType("fact")
				pyle = p11.GetActorsByType("pyle")
				hand = p11.GetActorsByType("hand")
				afld = p11.GetActorsByType("afld")
				weap = p11.GetActorsByType("weap")
				hpad = p11.GetActorsByType("hpad")
				rafact = p11.GetActorsByType("rafact")
				tent = p11.GetActorsByType("tent")
				barr = p11.GetActorsByType("barr")
				raweapa = p11.GetActorsByType("raweapa")
				raweap = p11.GetActorsByType("raweap")
				rahpad = p11.GetActorsByType("rahpad")
				raafld = p11.GetActorsByType("raafld")
				hq = p11.GetActorsByType("2100hq")
				fy1 = p11.GetActorsByType("2100fy1")
				fy2 = p11.GetActorsByType("2100fy2")
				sccommandcenter = p11.GetActorsByType("sccommandcenter")
				scbarracks = p11.GetActorsByType("scbarracks")
				scfactory = p11.GetActorsByType("scfactory")
				scstarport = p11.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p12 and p13.IsAlliedWith(p12) then
				fact = p12.GetActorsByType("fact")
				pyle = p12.GetActorsByType("pyle")
				hand = p12.GetActorsByType("hand")
				afld = p12.GetActorsByType("afld")
				weap = p12.GetActorsByType("weap")
				hpad = p12.GetActorsByType("hpad")
				rafact = p12.GetActorsByType("rafact")
				tent = p12.GetActorsByType("tent")
				barr = p12.GetActorsByType("barr")
				raweapa = p12.GetActorsByType("raweapa")
				raweap = p12.GetActorsByType("raweap")
				rahpad = p12.GetActorsByType("rahpad")
				raafld = p12.GetActorsByType("raafld")
				hq = p12.GetActorsByType("2100hq")
				fy1 = p12.GetActorsByType("2100fy1")
				fy2 = p12.GetActorsByType("2100fy2")
				sccommandcenter = p12.GetActorsByType("sccommandcenter")
				scbarracks = p12.GetActorsByType("scbarracks")
				scfactory = p12.GetActorsByType("scfactory")
				scstarport = p12.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p13cash()
	end)
end
p14cash = function()
	if p14 and p14.IsBot then
		citybottruck = p14.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p15 and p14.IsAlliedWith(p15) then
				fact = p15.GetActorsByType("fact")
				pyle = p15.GetActorsByType("pyle")
				hand = p15.GetActorsByType("hand")
				afld = p15.GetActorsByType("afld")
				weap = p15.GetActorsByType("weap")
				hpad = p15.GetActorsByType("hpad")
				rafact = p15.GetActorsByType("rafact")
				tent = p15.GetActorsByType("tent")
				barr = p15.GetActorsByType("barr")
				raweapa = p15.GetActorsByType("raweapa")
				raweap = p15.GetActorsByType("raweap")
				rahpad = p15.GetActorsByType("rahpad")
				raafld = p15.GetActorsByType("raafld")
				hq = p15.GetActorsByType("2100hq")
				fy1 = p15.GetActorsByType("2100fy1")
				fy2 = p15.GetActorsByType("2100fy2")
				sccommandcenter = p15.GetActorsByType("sccommandcenter")
				scbarracks = p15.GetActorsByType("scbarracks")
				scfactory = p15.GetActorsByType("scfactory")
				scstarport = p15.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p16 and p14.IsAlliedWith(p16) then
				fact = p16.GetActorsByType("fact")
				pyle = p16.GetActorsByType("pyle")
				hand = p16.GetActorsByType("hand")
				afld = p16.GetActorsByType("afld")
				weap = p16.GetActorsByType("weap")
				hpad = p16.GetActorsByType("hpad")
				rafact = p16.GetActorsByType("rafact")
				tent = p16.GetActorsByType("tent")
				barr = p16.GetActorsByType("barr")
				raweapa = p16.GetActorsByType("raweapa")
				raweap = p16.GetActorsByType("raweap")
				rahpad = p16.GetActorsByType("rahpad")
				raafld = p16.GetActorsByType("raafld")
				hq = p16.GetActorsByType("2100hq")
				fy1 = p16.GetActorsByType("2100fy1")
				fy2 = p16.GetActorsByType("2100fy2")
				sccommandcenter = p16.GetActorsByType("sccommandcenter")
				scbarracks = p16.GetActorsByType("scbarracks")
				scfactory = p16.GetActorsByType("scfactory")
				scstarport = p16.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p1 and p14.IsAlliedWith(p1) then
				fact = p1.GetActorsByType("fact")
				pyle = p1.GetActorsByType("pyle")
				hand = p1.GetActorsByType("hand")
				afld = p1.GetActorsByType("afld")
				weap = p1.GetActorsByType("weap")
				hpad = p1.GetActorsByType("hpad")
				rafact = p1.GetActorsByType("rafact")
				tent = p1.GetActorsByType("tent")
				barr = p1.GetActorsByType("barr")
				raweapa = p1.GetActorsByType("raweapa")
				raweap = p1.GetActorsByType("raweap")
				rahpad = p1.GetActorsByType("rahpad")
				raafld = p1.GetActorsByType("raafld")
				hq = p1.GetActorsByType("2100hq")
				fy1 = p1.GetActorsByType("2100fy1")
				fy2 = p1.GetActorsByType("2100fy2")
				sccommandcenter = p1.GetActorsByType("sccommandcenter")
				scbarracks = p1.GetActorsByType("scbarracks")
				scfactory = p1.GetActorsByType("scfactory")
				scstarport = p1.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p2 and p14.IsAlliedWith(p2) then
				fact = p2.GetActorsByType("fact")
				pyle = p2.GetActorsByType("pyle")
				hand = p2.GetActorsByType("hand")
				afld = p2.GetActorsByType("afld")
				weap = p2.GetActorsByType("weap")
				hpad = p2.GetActorsByType("hpad")
				rafact = p2.GetActorsByType("rafact")
				tent = p2.GetActorsByType("tent")
				barr = p2.GetActorsByType("barr")
				raweapa = p2.GetActorsByType("raweapa")
				raweap = p2.GetActorsByType("raweap")
				rahpad = p2.GetActorsByType("rahpad")
				raafld = p2.GetActorsByType("raafld")
				hq = p2.GetActorsByType("2100hq")
				fy1 = p2.GetActorsByType("2100fy1")
				fy2 = p2.GetActorsByType("2100fy2")
				sccommandcenter = p2.GetActorsByType("sccommandcenter")
				scbarracks = p2.GetActorsByType("scbarracks")
				scfactory = p2.GetActorsByType("scfactory")
				scstarport = p2.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p3 and p14.IsAlliedWith(p3) then
				fact = p3.GetActorsByType("fact")
				pyle = p3.GetActorsByType("pyle")
				hand = p3.GetActorsByType("hand")
				afld = p3.GetActorsByType("afld")
				weap = p3.GetActorsByType("weap")
				hpad = p3.GetActorsByType("hpad")
				rafact = p3.GetActorsByType("rafact")
				tent = p3.GetActorsByType("tent")
				barr = p3.GetActorsByType("barr")
				raweapa = p3.GetActorsByType("raweapa")
				raweap = p3.GetActorsByType("raweap")
				rahpad = p3.GetActorsByType("rahpad")
				raafld = p3.GetActorsByType("raafld")
				hq = p3.GetActorsByType("2100hq")
				fy1 = p3.GetActorsByType("2100fy1")
				fy2 = p3.GetActorsByType("2100fy2")
				sccommandcenter = p3.GetActorsByType("sccommandcenter")
				scbarracks = p3.GetActorsByType("scbarracks")
				scfactory = p3.GetActorsByType("scfactory")
				scstarport = p3.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p4 and p14.IsAlliedWith(p4) then
				fact = p4.GetActorsByType("fact")
				pyle = p4.GetActorsByType("pyle")
				hand = p4.GetActorsByType("hand")
				afld = p4.GetActorsByType("afld")
				weap = p4.GetActorsByType("weap")
				hpad = p4.GetActorsByType("hpad")
				rafact = p4.GetActorsByType("rafact")
				tent = p4.GetActorsByType("tent")
				barr = p4.GetActorsByType("barr")
				raweapa = p4.GetActorsByType("raweapa")
				raweap = p4.GetActorsByType("raweap")
				rahpad = p4.GetActorsByType("rahpad")
				raafld = p4.GetActorsByType("raafld")
				hq = p4.GetActorsByType("2100hq")
				fy1 = p4.GetActorsByType("2100fy1")
				fy2 = p4.GetActorsByType("2100fy2")
				sccommandcenter = p4.GetActorsByType("sccommandcenter")
				scbarracks = p4.GetActorsByType("scbarracks")
				scfactory = p4.GetActorsByType("scfactory")
				scstarport = p4.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p5 and p14.IsAlliedWith(p5) then
				fact = p5.GetActorsByType("fact")
				pyle = p5.GetActorsByType("pyle")
				hand = p5.GetActorsByType("hand")
				afld = p5.GetActorsByType("afld")
				weap = p5.GetActorsByType("weap")
				hpad = p5.GetActorsByType("hpad")
				rafact = p5.GetActorsByType("rafact")
				tent = p5.GetActorsByType("tent")
				barr = p5.GetActorsByType("barr")
				raweapa = p5.GetActorsByType("raweapa")
				raweap = p5.GetActorsByType("raweap")
				rahpad = p5.GetActorsByType("rahpad")
				raafld = p5.GetActorsByType("raafld")
				hq = p5.GetActorsByType("2100hq")
				fy1 = p5.GetActorsByType("2100fy1")
				fy2 = p5.GetActorsByType("2100fy2")
				sccommandcenter = p5.GetActorsByType("sccommandcenter")
				scbarracks = p5.GetActorsByType("scbarracks")
				scfactory = p5.GetActorsByType("scfactory")
				scstarport = p5.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p6 and p14.IsAlliedWith(p6) then
				fact = p6.GetActorsByType("fact")
				pyle = p6.GetActorsByType("pyle")
				hand = p6.GetActorsByType("hand")
				afld = p6.GetActorsByType("afld")
				weap = p6.GetActorsByType("weap")
				hpad = p6.GetActorsByType("hpad")
				rafact = p6.GetActorsByType("rafact")
				tent = p6.GetActorsByType("tent")
				barr = p6.GetActorsByType("barr")
				raweapa = p6.GetActorsByType("raweapa")
				raweap = p6.GetActorsByType("raweap")
				rahpad = p6.GetActorsByType("rahpad")
				raafld = p6.GetActorsByType("raafld")
				hq = p6.GetActorsByType("2100hq")
				fy1 = p6.GetActorsByType("2100fy1")
				fy2 = p6.GetActorsByType("2100fy2")
				sccommandcenter = p6.GetActorsByType("sccommandcenter")
				scbarracks = p6.GetActorsByType("scbarracks")
				scfactory = p6.GetActorsByType("scfactory")
				scstarport = p6.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p7 and p14.IsAlliedWith(p7) then
				fact = p7.GetActorsByType("fact")
				pyle = p7.GetActorsByType("pyle")
				hand = p7.GetActorsByType("hand")
				afld = p7.GetActorsByType("afld")
				weap = p7.GetActorsByType("weap")
				hpad = p7.GetActorsByType("hpad")
				rafact = p7.GetActorsByType("rafact")
				tent = p7.GetActorsByType("tent")
				barr = p7.GetActorsByType("barr")
				raweapa = p7.GetActorsByType("raweapa")
				raweap = p7.GetActorsByType("raweap")
				rahpad = p7.GetActorsByType("rahpad")
				raafld = p7.GetActorsByType("raafld")
				hq = p7.GetActorsByType("2100hq")
				fy1 = p7.GetActorsByType("2100fy1")
				fy2 = p7.GetActorsByType("2100fy2")
				sccommandcenter = p7.GetActorsByType("sccommandcenter")
				scbarracks = p7.GetActorsByType("scbarracks")
				scfactory = p7.GetActorsByType("scfactory")
				scstarport = p7.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p8 and p14.IsAlliedWith(p8) then
				fact = p8.GetActorsByType("fact")
				pyle = p8.GetActorsByType("pyle")
				hand = p8.GetActorsByType("hand")
				afld = p8.GetActorsByType("afld")
				weap = p8.GetActorsByType("weap")
				hpad = p8.GetActorsByType("hpad")
				rafact = p8.GetActorsByType("rafact")
				tent = p8.GetActorsByType("tent")
				barr = p8.GetActorsByType("barr")
				raweapa = p8.GetActorsByType("raweapa")
				raweap = p8.GetActorsByType("raweap")
				rahpad = p8.GetActorsByType("rahpad")
				raafld = p8.GetActorsByType("raafld")
				hq = p8.GetActorsByType("2100hq")
				fy1 = p8.GetActorsByType("2100fy1")
				fy2 = p8.GetActorsByType("2100fy2")
				sccommandcenter = p8.GetActorsByType("sccommandcenter")
				scbarracks = p8.GetActorsByType("scbarracks")
				scfactory = p8.GetActorsByType("scfactory")
				scstarport = p8.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p9 and p14.IsAlliedWith(p9) then
				fact = p9.GetActorsByType("fact")
				pyle = p9.GetActorsByType("pyle")
				hand = p9.GetActorsByType("hand")
				afld = p9.GetActorsByType("afld")
				weap = p9.GetActorsByType("weap")
				hpad = p9.GetActorsByType("hpad")
				rafact = p9.GetActorsByType("rafact")
				tent = p9.GetActorsByType("tent")
				barr = p9.GetActorsByType("barr")
				raweapa = p9.GetActorsByType("raweapa")
				raweap = p9.GetActorsByType("raweap")
				rahpad = p9.GetActorsByType("rahpad")
				raafld = p9.GetActorsByType("raafld")
				hq = p9.GetActorsByType("2100hq")
				fy1 = p9.GetActorsByType("2100fy1")
				fy2 = p9.GetActorsByType("2100fy2")
				sccommandcenter = p9.GetActorsByType("sccommandcenter")
				scbarracks = p9.GetActorsByType("scbarracks")
				scfactory = p9.GetActorsByType("scfactory")
				scstarport = p9.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p10 and p14.IsAlliedWith(p10) then
				fact = p10.GetActorsByType("fact")
				pyle = p10.GetActorsByType("pyle")
				hand = p10.GetActorsByType("hand")
				afld = p10.GetActorsByType("afld")
				weap = p10.GetActorsByType("weap")
				hpad = p10.GetActorsByType("hpad")
				rafact = p10.GetActorsByType("rafact")
				tent = p10.GetActorsByType("tent")
				barr = p10.GetActorsByType("barr")
				raweapa = p10.GetActorsByType("raweapa")
				raweap = p10.GetActorsByType("raweap")
				rahpad = p10.GetActorsByType("rahpad")
				raafld = p10.GetActorsByType("raafld")
				hq = p10.GetActorsByType("2100hq")
				fy1 = p10.GetActorsByType("2100fy1")
				fy2 = p10.GetActorsByType("2100fy2")
				sccommandcenter = p10.GetActorsByType("sccommandcenter")
				scbarracks = p10.GetActorsByType("scbarracks")
				scfactory = p10.GetActorsByType("scfactory")
				scstarport = p10.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p11 and p14.IsAlliedWith(p11) then
				fact = p11.GetActorsByType("fact")
				pyle = p11.GetActorsByType("pyle")
				hand = p11.GetActorsByType("hand")
				afld = p11.GetActorsByType("afld")
				weap = p11.GetActorsByType("weap")
				hpad = p11.GetActorsByType("hpad")
				rafact = p11.GetActorsByType("rafact")
				tent = p11.GetActorsByType("tent")
				barr = p11.GetActorsByType("barr")
				raweapa = p11.GetActorsByType("raweapa")
				raweap = p11.GetActorsByType("raweap")
				rahpad = p11.GetActorsByType("rahpad")
				raafld = p11.GetActorsByType("raafld")
				hq = p11.GetActorsByType("2100hq")
				fy1 = p11.GetActorsByType("2100fy1")
				fy2 = p11.GetActorsByType("2100fy2")
				sccommandcenter = p11.GetActorsByType("sccommandcenter")
				scbarracks = p11.GetActorsByType("scbarracks")
				scfactory = p11.GetActorsByType("scfactory")
				scstarport = p11.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p12 and p14.IsAlliedWith(p12) then
				fact = p12.GetActorsByType("fact")
				pyle = p12.GetActorsByType("pyle")
				hand = p12.GetActorsByType("hand")
				afld = p12.GetActorsByType("afld")
				weap = p12.GetActorsByType("weap")
				hpad = p12.GetActorsByType("hpad")
				rafact = p12.GetActorsByType("rafact")
				tent = p12.GetActorsByType("tent")
				barr = p12.GetActorsByType("barr")
				raweapa = p12.GetActorsByType("raweapa")
				raweap = p12.GetActorsByType("raweap")
				rahpad = p12.GetActorsByType("rahpad")
				raafld = p12.GetActorsByType("raafld")
				hq = p12.GetActorsByType("2100hq")
				fy1 = p12.GetActorsByType("2100fy1")
				fy2 = p12.GetActorsByType("2100fy2")
				sccommandcenter = p12.GetActorsByType("sccommandcenter")
				scbarracks = p12.GetActorsByType("scbarracks")
				scfactory = p12.GetActorsByType("scfactory")
				scstarport = p12.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p13 and p14.IsAlliedWith(p13) then
				fact = p13.GetActorsByType("fact")
				pyle = p13.GetActorsByType("pyle")
				hand = p13.GetActorsByType("hand")
				afld = p13.GetActorsByType("afld")
				weap = p13.GetActorsByType("weap")
				hpad = p13.GetActorsByType("hpad")
				rafact = p13.GetActorsByType("rafact")
				tent = p13.GetActorsByType("tent")
				barr = p13.GetActorsByType("barr")
				raweapa = p13.GetActorsByType("raweapa")
				raweap = p13.GetActorsByType("raweap")
				rahpad = p13.GetActorsByType("rahpad")
				raafld = p13.GetActorsByType("raafld")
				hq = p13.GetActorsByType("2100hq")
				fy1 = p13.GetActorsByType("2100fy1")
				fy2 = p13.GetActorsByType("2100fy2")
				sccommandcenter = p13.GetActorsByType("sccommandcenter")
				scbarracks = p13.GetActorsByType("scbarracks")
				scfactory = p13.GetActorsByType("scfactory")
				scstarport = p13.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p14cash()
	end)
end
p15cash = function()
	if p15 and p15.IsBot then
		citybottruck = p15.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p16 and p15.IsAlliedWith(p16) then
				fact = p16.GetActorsByType("fact")
				pyle = p16.GetActorsByType("pyle")
				hand = p16.GetActorsByType("hand")
				afld = p16.GetActorsByType("afld")
				weap = p16.GetActorsByType("weap")
				hpad = p16.GetActorsByType("hpad")
				rafact = p16.GetActorsByType("rafact")
				tent = p16.GetActorsByType("tent")
				barr = p16.GetActorsByType("barr")
				raweapa = p16.GetActorsByType("raweapa")
				raweap = p16.GetActorsByType("raweap")
				rahpad = p16.GetActorsByType("rahpad")
				raafld = p16.GetActorsByType("raafld")
				hq = p16.GetActorsByType("2100hq")
				fy1 = p16.GetActorsByType("2100fy1")
				fy2 = p16.GetActorsByType("2100fy2")
				sccommandcenter = p16.GetActorsByType("sccommandcenter")
				scbarracks = p16.GetActorsByType("scbarracks")
				scfactory = p16.GetActorsByType("scfactory")
				scstarport = p16.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p1 and p15.IsAlliedWith(p1) then
				fact = p1.GetActorsByType("fact")
				pyle = p1.GetActorsByType("pyle")
				hand = p1.GetActorsByType("hand")
				afld = p1.GetActorsByType("afld")
				weap = p1.GetActorsByType("weap")
				hpad = p1.GetActorsByType("hpad")
				rafact = p1.GetActorsByType("rafact")
				tent = p1.GetActorsByType("tent")
				barr = p1.GetActorsByType("barr")
				raweapa = p1.GetActorsByType("raweapa")
				raweap = p1.GetActorsByType("raweap")
				rahpad = p1.GetActorsByType("rahpad")
				raafld = p1.GetActorsByType("raafld")
				hq = p1.GetActorsByType("2100hq")
				fy1 = p1.GetActorsByType("2100fy1")
				fy2 = p1.GetActorsByType("2100fy2")
				sccommandcenter = p1.GetActorsByType("sccommandcenter")
				scbarracks = p1.GetActorsByType("scbarracks")
				scfactory = p1.GetActorsByType("scfactory")
				scstarport = p1.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p2 and p15.IsAlliedWith(p2) then
				fact = p2.GetActorsByType("fact")
				pyle = p2.GetActorsByType("pyle")
				hand = p2.GetActorsByType("hand")
				afld = p2.GetActorsByType("afld")
				weap = p2.GetActorsByType("weap")
				hpad = p2.GetActorsByType("hpad")
				rafact = p2.GetActorsByType("rafact")
				tent = p2.GetActorsByType("tent")
				barr = p2.GetActorsByType("barr")
				raweapa = p2.GetActorsByType("raweapa")
				raweap = p2.GetActorsByType("raweap")
				rahpad = p2.GetActorsByType("rahpad")
				raafld = p2.GetActorsByType("raafld")
				hq = p2.GetActorsByType("2100hq")
				fy1 = p2.GetActorsByType("2100fy1")
				fy2 = p2.GetActorsByType("2100fy2")
				sccommandcenter = p2.GetActorsByType("sccommandcenter")
				scbarracks = p2.GetActorsByType("scbarracks")
				scfactory = p2.GetActorsByType("scfactory")
				scstarport = p2.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p3 and p15.IsAlliedWith(p3) then
				fact = p3.GetActorsByType("fact")
				pyle = p3.GetActorsByType("pyle")
				hand = p3.GetActorsByType("hand")
				afld = p3.GetActorsByType("afld")
				weap = p3.GetActorsByType("weap")
				hpad = p3.GetActorsByType("hpad")
				rafact = p3.GetActorsByType("rafact")
				tent = p3.GetActorsByType("tent")
				barr = p3.GetActorsByType("barr")
				raweapa = p3.GetActorsByType("raweapa")
				raweap = p3.GetActorsByType("raweap")
				rahpad = p3.GetActorsByType("rahpad")
				raafld = p3.GetActorsByType("raafld")
				hq = p3.GetActorsByType("2100hq")
				fy1 = p3.GetActorsByType("2100fy1")
				fy2 = p3.GetActorsByType("2100fy2")
				sccommandcenter = p3.GetActorsByType("sccommandcenter")
				scbarracks = p3.GetActorsByType("scbarracks")
				scfactory = p3.GetActorsByType("scfactory")
				scstarport = p3.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p4 and p15.IsAlliedWith(p4) then
				fact = p4.GetActorsByType("fact")
				pyle = p4.GetActorsByType("pyle")
				hand = p4.GetActorsByType("hand")
				afld = p4.GetActorsByType("afld")
				weap = p4.GetActorsByType("weap")
				hpad = p4.GetActorsByType("hpad")
				rafact = p4.GetActorsByType("rafact")
				tent = p4.GetActorsByType("tent")
				barr = p4.GetActorsByType("barr")
				raweapa = p4.GetActorsByType("raweapa")
				raweap = p4.GetActorsByType("raweap")
				rahpad = p4.GetActorsByType("rahpad")
				raafld = p4.GetActorsByType("raafld")
				hq = p4.GetActorsByType("2100hq")
				fy1 = p4.GetActorsByType("2100fy1")
				fy2 = p4.GetActorsByType("2100fy2")
				sccommandcenter = p4.GetActorsByType("sccommandcenter")
				scbarracks = p4.GetActorsByType("scbarracks")
				scfactory = p4.GetActorsByType("scfactory")
				scstarport = p4.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p5 and p15.IsAlliedWith(p5) then
				fact = p5.GetActorsByType("fact")
				pyle = p5.GetActorsByType("pyle")
				hand = p5.GetActorsByType("hand")
				afld = p5.GetActorsByType("afld")
				weap = p5.GetActorsByType("weap")
				hpad = p5.GetActorsByType("hpad")
				rafact = p5.GetActorsByType("rafact")
				tent = p5.GetActorsByType("tent")
				barr = p5.GetActorsByType("barr")
				raweapa = p5.GetActorsByType("raweapa")
				raweap = p5.GetActorsByType("raweap")
				rahpad = p5.GetActorsByType("rahpad")
				raafld = p5.GetActorsByType("raafld")
				hq = p5.GetActorsByType("2100hq")
				fy1 = p5.GetActorsByType("2100fy1")
				fy2 = p5.GetActorsByType("2100fy2")
				sccommandcenter = p5.GetActorsByType("sccommandcenter")
				scbarracks = p5.GetActorsByType("scbarracks")
				scfactory = p5.GetActorsByType("scfactory")
				scstarport = p5.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p6 and p15.IsAlliedWith(p6) then
				fact = p6.GetActorsByType("fact")
				pyle = p6.GetActorsByType("pyle")
				hand = p6.GetActorsByType("hand")
				afld = p6.GetActorsByType("afld")
				weap = p6.GetActorsByType("weap")
				hpad = p6.GetActorsByType("hpad")
				rafact = p6.GetActorsByType("rafact")
				tent = p6.GetActorsByType("tent")
				barr = p6.GetActorsByType("barr")
				raweapa = p6.GetActorsByType("raweapa")
				raweap = p6.GetActorsByType("raweap")
				rahpad = p6.GetActorsByType("rahpad")
				raafld = p6.GetActorsByType("raafld")
				hq = p6.GetActorsByType("2100hq")
				fy1 = p6.GetActorsByType("2100fy1")
				fy2 = p6.GetActorsByType("2100fy2")
				sccommandcenter = p6.GetActorsByType("sccommandcenter")
				scbarracks = p6.GetActorsByType("scbarracks")
				scfactory = p6.GetActorsByType("scfactory")
				scstarport = p6.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p7 and p15.IsAlliedWith(p7) then
				fact = p7.GetActorsByType("fact")
				pyle = p7.GetActorsByType("pyle")
				hand = p7.GetActorsByType("hand")
				afld = p7.GetActorsByType("afld")
				weap = p7.GetActorsByType("weap")
				hpad = p7.GetActorsByType("hpad")
				rafact = p7.GetActorsByType("rafact")
				tent = p7.GetActorsByType("tent")
				barr = p7.GetActorsByType("barr")
				raweapa = p7.GetActorsByType("raweapa")
				raweap = p7.GetActorsByType("raweap")
				rahpad = p7.GetActorsByType("rahpad")
				raafld = p7.GetActorsByType("raafld")
				hq = p7.GetActorsByType("2100hq")
				fy1 = p7.GetActorsByType("2100fy1")
				fy2 = p7.GetActorsByType("2100fy2")
				sccommandcenter = p7.GetActorsByType("sccommandcenter")
				scbarracks = p7.GetActorsByType("scbarracks")
				scfactory = p7.GetActorsByType("scfactory")
				scstarport = p7.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p8 and p15.IsAlliedWith(p8) then
				fact = p8.GetActorsByType("fact")
				pyle = p8.GetActorsByType("pyle")
				hand = p8.GetActorsByType("hand")
				afld = p8.GetActorsByType("afld")
				weap = p8.GetActorsByType("weap")
				hpad = p8.GetActorsByType("hpad")
				rafact = p8.GetActorsByType("rafact")
				tent = p8.GetActorsByType("tent")
				barr = p8.GetActorsByType("barr")
				raweapa = p8.GetActorsByType("raweapa")
				raweap = p8.GetActorsByType("raweap")
				rahpad = p8.GetActorsByType("rahpad")
				raafld = p8.GetActorsByType("raafld")
				hq = p8.GetActorsByType("2100hq")
				fy1 = p8.GetActorsByType("2100fy1")
				fy2 = p8.GetActorsByType("2100fy2")
				sccommandcenter = p8.GetActorsByType("sccommandcenter")
				scbarracks = p8.GetActorsByType("scbarracks")
				scfactory = p8.GetActorsByType("scfactory")
				scstarport = p8.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p9 and p15.IsAlliedWith(p9) then
				fact = p9.GetActorsByType("fact")
				pyle = p9.GetActorsByType("pyle")
				hand = p9.GetActorsByType("hand")
				afld = p9.GetActorsByType("afld")
				weap = p9.GetActorsByType("weap")
				hpad = p9.GetActorsByType("hpad")
				rafact = p9.GetActorsByType("rafact")
				tent = p9.GetActorsByType("tent")
				barr = p9.GetActorsByType("barr")
				raweapa = p9.GetActorsByType("raweapa")
				raweap = p9.GetActorsByType("raweap")
				rahpad = p9.GetActorsByType("rahpad")
				raafld = p9.GetActorsByType("raafld")
				hq = p9.GetActorsByType("2100hq")
				fy1 = p9.GetActorsByType("2100fy1")
				fy2 = p9.GetActorsByType("2100fy2")
				sccommandcenter = p9.GetActorsByType("sccommandcenter")
				scbarracks = p9.GetActorsByType("scbarracks")
				scfactory = p9.GetActorsByType("scfactory")
				scstarport = p9.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p10 and p15.IsAlliedWith(p10) then
				fact = p10.GetActorsByType("fact")
				pyle = p10.GetActorsByType("pyle")
				hand = p10.GetActorsByType("hand")
				afld = p10.GetActorsByType("afld")
				weap = p10.GetActorsByType("weap")
				hpad = p10.GetActorsByType("hpad")
				rafact = p10.GetActorsByType("rafact")
				tent = p10.GetActorsByType("tent")
				barr = p10.GetActorsByType("barr")
				raweapa = p10.GetActorsByType("raweapa")
				raweap = p10.GetActorsByType("raweap")
				rahpad = p10.GetActorsByType("rahpad")
				raafld = p10.GetActorsByType("raafld")
				hq = p10.GetActorsByType("2100hq")
				fy1 = p10.GetActorsByType("2100fy1")
				fy2 = p10.GetActorsByType("2100fy2")
				sccommandcenter = p10.GetActorsByType("sccommandcenter")
				scbarracks = p10.GetActorsByType("scbarracks")
				scfactory = p10.GetActorsByType("scfactory")
				scstarport = p10.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p11 and p15.IsAlliedWith(p11) then
				fact = p11.GetActorsByType("fact")
				pyle = p11.GetActorsByType("pyle")
				hand = p11.GetActorsByType("hand")
				afld = p11.GetActorsByType("afld")
				weap = p11.GetActorsByType("weap")
				hpad = p11.GetActorsByType("hpad")
				rafact = p11.GetActorsByType("rafact")
				tent = p11.GetActorsByType("tent")
				barr = p11.GetActorsByType("barr")
				raweapa = p11.GetActorsByType("raweapa")
				raweap = p11.GetActorsByType("raweap")
				rahpad = p11.GetActorsByType("rahpad")
				raafld = p11.GetActorsByType("raafld")
				hq = p11.GetActorsByType("2100hq")
				fy1 = p11.GetActorsByType("2100fy1")
				fy2 = p11.GetActorsByType("2100fy2")
				sccommandcenter = p11.GetActorsByType("sccommandcenter")
				scbarracks = p11.GetActorsByType("scbarracks")
				scfactory = p11.GetActorsByType("scfactory")
				scstarport = p11.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p12 and p15.IsAlliedWith(p12) then
				fact = p12.GetActorsByType("fact")
				pyle = p12.GetActorsByType("pyle")
				hand = p12.GetActorsByType("hand")
				afld = p12.GetActorsByType("afld")
				weap = p12.GetActorsByType("weap")
				hpad = p12.GetActorsByType("hpad")
				rafact = p12.GetActorsByType("rafact")
				tent = p12.GetActorsByType("tent")
				barr = p12.GetActorsByType("barr")
				raweapa = p12.GetActorsByType("raweapa")
				raweap = p12.GetActorsByType("raweap")
				rahpad = p12.GetActorsByType("rahpad")
				raafld = p12.GetActorsByType("raafld")
				hq = p12.GetActorsByType("2100hq")
				fy1 = p12.GetActorsByType("2100fy1")
				fy2 = p12.GetActorsByType("2100fy2")
				sccommandcenter = p12.GetActorsByType("sccommandcenter")
				scbarracks = p12.GetActorsByType("scbarracks")
				scfactory = p12.GetActorsByType("scfactory")
				scstarport = p12.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p13 and p15.IsAlliedWith(p13) then
				fact = p13.GetActorsByType("fact")
				pyle = p13.GetActorsByType("pyle")
				hand = p13.GetActorsByType("hand")
				afld = p13.GetActorsByType("afld")
				weap = p13.GetActorsByType("weap")
				hpad = p13.GetActorsByType("hpad")
				rafact = p13.GetActorsByType("rafact")
				tent = p13.GetActorsByType("tent")
				barr = p13.GetActorsByType("barr")
				raweapa = p13.GetActorsByType("raweapa")
				raweap = p13.GetActorsByType("raweap")
				rahpad = p13.GetActorsByType("rahpad")
				raafld = p13.GetActorsByType("raafld")
				hq = p13.GetActorsByType("2100hq")
				fy1 = p13.GetActorsByType("2100fy1")
				fy2 = p13.GetActorsByType("2100fy2")
				sccommandcenter = p13.GetActorsByType("sccommandcenter")
				scbarracks = p13.GetActorsByType("scbarracks")
				scfactory = p13.GetActorsByType("scfactory")
				scstarport = p13.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p14 and p15.IsAlliedWith(p14) then
				fact = p14.GetActorsByType("fact")
				pyle = p14.GetActorsByType("pyle")
				hand = p14.GetActorsByType("hand")
				afld = p14.GetActorsByType("afld")
				weap = p14.GetActorsByType("weap")
				hpad = p14.GetActorsByType("hpad")
				rafact = p14.GetActorsByType("rafact")
				tent = p14.GetActorsByType("tent")
				barr = p14.GetActorsByType("barr")
				raweapa = p14.GetActorsByType("raweapa")
				raweap = p14.GetActorsByType("raweap")
				rahpad = p14.GetActorsByType("rahpad")
				raafld = p14.GetActorsByType("raafld")
				hq = p14.GetActorsByType("2100hq")
				fy1 = p14.GetActorsByType("2100fy1")
				fy2 = p14.GetActorsByType("2100fy2")
				sccommandcenter = p14.GetActorsByType("sccommandcenter")
				scbarracks = p14.GetActorsByType("scbarracks")
				scfactory = p14.GetActorsByType("scfactory")
				scstarport = p14.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p15cash()
	end)
end
p16cash = function()
	if p16 and p16.IsBot then
		citybottruck = p16.GetActorsByType("citybottruck")
		for _,trucks in pairs(citybottruck) do
			if p1 and p16.IsAlliedWith(p1) then
				fact = p1.GetActorsByType("fact")
				pyle = p1.GetActorsByType("pyle")
				hand = p1.GetActorsByType("hand")
				afld = p1.GetActorsByType("afld")
				weap = p1.GetActorsByType("weap")
				hpad = p1.GetActorsByType("hpad")
				rafact = p1.GetActorsByType("rafact")
				tent = p1.GetActorsByType("tent")
				barr = p1.GetActorsByType("barr")
				raweapa = p1.GetActorsByType("raweapa")
				raweap = p1.GetActorsByType("raweap")
				rahpad = p1.GetActorsByType("rahpad")
				raafld = p1.GetActorsByType("raafld")
				hq = p1.GetActorsByType("2100hq")
				fy1 = p1.GetActorsByType("2100fy1")
				fy2 = p1.GetActorsByType("2100fy2")
				sccommandcenter = p1.GetActorsByType("sccommandcenter")
				scbarracks = p1.GetActorsByType("scbarracks")
				scfactory = p1.GetActorsByType("scfactory")
				scstarport = p1.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p2 and p16.IsAlliedWith(p2) then
				fact = p2.GetActorsByType("fact")
				pyle = p2.GetActorsByType("pyle")
				hand = p2.GetActorsByType("hand")
				afld = p2.GetActorsByType("afld")
				weap = p2.GetActorsByType("weap")
				hpad = p2.GetActorsByType("hpad")
				rafact = p2.GetActorsByType("rafact")
				tent = p2.GetActorsByType("tent")
				barr = p2.GetActorsByType("barr")
				raweapa = p2.GetActorsByType("raweapa")
				raweap = p2.GetActorsByType("raweap")
				rahpad = p2.GetActorsByType("rahpad")
				raafld = p2.GetActorsByType("raafld")
				hq = p2.GetActorsByType("2100hq")
				fy1 = p2.GetActorsByType("2100fy1")
				fy2 = p2.GetActorsByType("2100fy2")
				sccommandcenter = p2.GetActorsByType("sccommandcenter")
				scbarracks = p2.GetActorsByType("scbarracks")
				scfactory = p2.GetActorsByType("scfactory")
				scstarport = p2.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p3 and p16.IsAlliedWith(p3) then
				fact = p3.GetActorsByType("fact")
				pyle = p3.GetActorsByType("pyle")
				hand = p3.GetActorsByType("hand")
				afld = p3.GetActorsByType("afld")
				weap = p3.GetActorsByType("weap")
				hpad = p3.GetActorsByType("hpad")
				rafact = p3.GetActorsByType("rafact")
				tent = p3.GetActorsByType("tent")
				barr = p3.GetActorsByType("barr")
				raweapa = p3.GetActorsByType("raweapa")
				raweap = p3.GetActorsByType("raweap")
				rahpad = p3.GetActorsByType("rahpad")
				raafld = p3.GetActorsByType("raafld")
				hq = p3.GetActorsByType("2100hq")
				fy1 = p3.GetActorsByType("2100fy1")
				fy2 = p3.GetActorsByType("2100fy2")
				sccommandcenter = p3.GetActorsByType("sccommandcenter")
				scbarracks = p3.GetActorsByType("scbarracks")
				scfactory = p3.GetActorsByType("scfactory")
				scstarport = p3.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p4 and p16.IsAlliedWith(p4) then
				fact = p4.GetActorsByType("fact")
				pyle = p4.GetActorsByType("pyle")
				hand = p4.GetActorsByType("hand")
				afld = p4.GetActorsByType("afld")
				weap = p4.GetActorsByType("weap")
				hpad = p4.GetActorsByType("hpad")
				rafact = p4.GetActorsByType("rafact")
				tent = p4.GetActorsByType("tent")
				barr = p4.GetActorsByType("barr")
				raweapa = p4.GetActorsByType("raweapa")
				raweap = p4.GetActorsByType("raweap")
				rahpad = p4.GetActorsByType("rahpad")
				raafld = p4.GetActorsByType("raafld")
				hq = p4.GetActorsByType("2100hq")
				fy1 = p4.GetActorsByType("2100fy1")
				fy2 = p4.GetActorsByType("2100fy2")
				sccommandcenter = p4.GetActorsByType("sccommandcenter")
				scbarracks = p4.GetActorsByType("scbarracks")
				scfactory = p4.GetActorsByType("scfactory")
				scstarport = p4.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p5 and p16.IsAlliedWith(p5) then
				fact = p5.GetActorsByType("fact")
				pyle = p5.GetActorsByType("pyle")
				hand = p5.GetActorsByType("hand")
				afld = p5.GetActorsByType("afld")
				weap = p5.GetActorsByType("weap")
				hpad = p5.GetActorsByType("hpad")
				rafact = p5.GetActorsByType("rafact")
				tent = p5.GetActorsByType("tent")
				barr = p5.GetActorsByType("barr")
				raweapa = p5.GetActorsByType("raweapa")
				raweap = p5.GetActorsByType("raweap")
				rahpad = p5.GetActorsByType("rahpad")
				raafld = p5.GetActorsByType("raafld")
				hq = p5.GetActorsByType("2100hq")
				fy1 = p5.GetActorsByType("2100fy1")
				fy2 = p5.GetActorsByType("2100fy2")
				sccommandcenter = p5.GetActorsByType("sccommandcenter")
				scbarracks = p5.GetActorsByType("scbarracks")
				scfactory = p5.GetActorsByType("scfactory")
				scstarport = p5.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p6 and p16.IsAlliedWith(p6) then
				fact = p6.GetActorsByType("fact")
				pyle = p6.GetActorsByType("pyle")
				hand = p6.GetActorsByType("hand")
				afld = p6.GetActorsByType("afld")
				weap = p6.GetActorsByType("weap")
				hpad = p6.GetActorsByType("hpad")
				rafact = p6.GetActorsByType("rafact")
				tent = p6.GetActorsByType("tent")
				barr = p6.GetActorsByType("barr")
				raweapa = p6.GetActorsByType("raweapa")
				raweap = p6.GetActorsByType("raweap")
				rahpad = p6.GetActorsByType("rahpad")
				raafld = p6.GetActorsByType("raafld")
				hq = p6.GetActorsByType("2100hq")
				fy1 = p6.GetActorsByType("2100fy1")
				fy2 = p6.GetActorsByType("2100fy2")
				sccommandcenter = p6.GetActorsByType("sccommandcenter")
				scbarracks = p6.GetActorsByType("scbarracks")
				scfactory = p6.GetActorsByType("scfactory")
				scstarport = p6.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p7 and p16.IsAlliedWith(p7) then
				fact = p7.GetActorsByType("fact")
				pyle = p7.GetActorsByType("pyle")
				hand = p7.GetActorsByType("hand")
				afld = p7.GetActorsByType("afld")
				weap = p7.GetActorsByType("weap")
				hpad = p7.GetActorsByType("hpad")
				rafact = p7.GetActorsByType("rafact")
				tent = p7.GetActorsByType("tent")
				barr = p7.GetActorsByType("barr")
				raweapa = p7.GetActorsByType("raweapa")
				raweap = p7.GetActorsByType("raweap")
				rahpad = p7.GetActorsByType("rahpad")
				raafld = p7.GetActorsByType("raafld")
				hq = p7.GetActorsByType("2100hq")
				fy1 = p7.GetActorsByType("2100fy1")
				fy2 = p7.GetActorsByType("2100fy2")
				sccommandcenter = p7.GetActorsByType("sccommandcenter")
				scbarracks = p7.GetActorsByType("scbarracks")
				scfactory = p7.GetActorsByType("scfactory")
				scstarport = p7.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p8 and p16.IsAlliedWith(p8) then
				fact = p8.GetActorsByType("fact")
				pyle = p8.GetActorsByType("pyle")
				hand = p8.GetActorsByType("hand")
				afld = p8.GetActorsByType("afld")
				weap = p8.GetActorsByType("weap")
				hpad = p8.GetActorsByType("hpad")
				rafact = p8.GetActorsByType("rafact")
				tent = p8.GetActorsByType("tent")
				barr = p8.GetActorsByType("barr")
				raweapa = p8.GetActorsByType("raweapa")
				raweap = p8.GetActorsByType("raweap")
				rahpad = p8.GetActorsByType("rahpad")
				raafld = p8.GetActorsByType("raafld")
				hq = p8.GetActorsByType("2100hq")
				fy1 = p8.GetActorsByType("2100fy1")
				fy2 = p8.GetActorsByType("2100fy2")
				sccommandcenter = p8.GetActorsByType("sccommandcenter")
				scbarracks = p8.GetActorsByType("scbarracks")
				scfactory = p8.GetActorsByType("scfactory")
				scstarport = p8.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p9 and p16.IsAlliedWith(p9) then
				fact = p9.GetActorsByType("fact")
				pyle = p9.GetActorsByType("pyle")
				hand = p9.GetActorsByType("hand")
				afld = p9.GetActorsByType("afld")
				weap = p9.GetActorsByType("weap")
				hpad = p9.GetActorsByType("hpad")
				rafact = p9.GetActorsByType("rafact")
				tent = p9.GetActorsByType("tent")
				barr = p9.GetActorsByType("barr")
				raweapa = p9.GetActorsByType("raweapa")
				raweap = p9.GetActorsByType("raweap")
				rahpad = p9.GetActorsByType("rahpad")
				raafld = p9.GetActorsByType("raafld")
				hq = p9.GetActorsByType("2100hq")
				fy1 = p9.GetActorsByType("2100fy1")
				fy2 = p9.GetActorsByType("2100fy2")
				sccommandcenter = p9.GetActorsByType("sccommandcenter")
				scbarracks = p9.GetActorsByType("scbarracks")
				scfactory = p9.GetActorsByType("scfactory")
				scstarport = p9.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p10 and p16.IsAlliedWith(p10) then
				fact = p10.GetActorsByType("fact")
				pyle = p10.GetActorsByType("pyle")
				hand = p10.GetActorsByType("hand")
				afld = p10.GetActorsByType("afld")
				weap = p10.GetActorsByType("weap")
				hpad = p10.GetActorsByType("hpad")
				rafact = p10.GetActorsByType("rafact")
				tent = p10.GetActorsByType("tent")
				barr = p10.GetActorsByType("barr")
				raweapa = p10.GetActorsByType("raweapa")
				raweap = p10.GetActorsByType("raweap")
				rahpad = p10.GetActorsByType("rahpad")
				raafld = p10.GetActorsByType("raafld")
				hq = p10.GetActorsByType("2100hq")
				fy1 = p10.GetActorsByType("2100fy1")
				fy2 = p10.GetActorsByType("2100fy2")
				sccommandcenter = p10.GetActorsByType("sccommandcenter")
				scbarracks = p10.GetActorsByType("scbarracks")
				scfactory = p10.GetActorsByType("scfactory")
				scstarport = p10.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p11 and p16.IsAlliedWith(p11) then
				fact = p11.GetActorsByType("fact")
				pyle = p11.GetActorsByType("pyle")
				hand = p11.GetActorsByType("hand")
				afld = p11.GetActorsByType("afld")
				weap = p11.GetActorsByType("weap")
				hpad = p11.GetActorsByType("hpad")
				rafact = p11.GetActorsByType("rafact")
				tent = p11.GetActorsByType("tent")
				barr = p11.GetActorsByType("barr")
				raweapa = p11.GetActorsByType("raweapa")
				raweap = p11.GetActorsByType("raweap")
				rahpad = p11.GetActorsByType("rahpad")
				raafld = p11.GetActorsByType("raafld")
				hq = p11.GetActorsByType("2100hq")
				fy1 = p11.GetActorsByType("2100fy1")
				fy2 = p11.GetActorsByType("2100fy2")
				sccommandcenter = p11.GetActorsByType("sccommandcenter")
				scbarracks = p11.GetActorsByType("scbarracks")
				scfactory = p11.GetActorsByType("scfactory")
				scstarport = p11.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p12 and p16.IsAlliedWith(p12) then
				fact = p12.GetActorsByType("fact")
				pyle = p12.GetActorsByType("pyle")
				hand = p12.GetActorsByType("hand")
				afld = p12.GetActorsByType("afld")
				weap = p12.GetActorsByType("weap")
				hpad = p12.GetActorsByType("hpad")
				rafact = p12.GetActorsByType("rafact")
				tent = p12.GetActorsByType("tent")
				barr = p12.GetActorsByType("barr")
				raweapa = p12.GetActorsByType("raweapa")
				raweap = p12.GetActorsByType("raweap")
				rahpad = p12.GetActorsByType("rahpad")
				raafld = p12.GetActorsByType("raafld")
				hq = p12.GetActorsByType("2100hq")
				fy1 = p12.GetActorsByType("2100fy1")
				fy2 = p12.GetActorsByType("2100fy2")
				sccommandcenter = p12.GetActorsByType("sccommandcenter")
				scbarracks = p12.GetActorsByType("scbarracks")
				scfactory = p12.GetActorsByType("scfactory")
				scstarport = p12.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p13 and p16.IsAlliedWith(p13) then
				fact = p13.GetActorsByType("fact")
				pyle = p13.GetActorsByType("pyle")
				hand = p13.GetActorsByType("hand")
				afld = p13.GetActorsByType("afld")
				weap = p13.GetActorsByType("weap")
				hpad = p13.GetActorsByType("hpad")
				rafact = p13.GetActorsByType("rafact")
				tent = p13.GetActorsByType("tent")
				barr = p13.GetActorsByType("barr")
				raweapa = p13.GetActorsByType("raweapa")
				raweap = p13.GetActorsByType("raweap")
				rahpad = p13.GetActorsByType("rahpad")
				raafld = p13.GetActorsByType("raafld")
				hq = p13.GetActorsByType("2100hq")
				fy1 = p13.GetActorsByType("2100fy1")
				fy2 = p13.GetActorsByType("2100fy2")
				sccommandcenter = p13.GetActorsByType("sccommandcenter")
				scbarracks = p13.GetActorsByType("scbarracks")
				scfactory = p13.GetActorsByType("scfactory")
				scstarport = p13.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p14 and p16.IsAlliedWith(p14) then
				fact = p14.GetActorsByType("fact")
				pyle = p14.GetActorsByType("pyle")
				hand = p14.GetActorsByType("hand")
				afld = p14.GetActorsByType("afld")
				weap = p14.GetActorsByType("weap")
				hpad = p14.GetActorsByType("hpad")
				rafact = p14.GetActorsByType("rafact")
				tent = p14.GetActorsByType("tent")
				barr = p14.GetActorsByType("barr")
				raweapa = p14.GetActorsByType("raweapa")
				raweap = p14.GetActorsByType("raweap")
				rahpad = p14.GetActorsByType("rahpad")
				raafld = p14.GetActorsByType("raafld")
				hq = p14.GetActorsByType("2100hq")
				fy1 = p14.GetActorsByType("2100fy1")
				fy2 = p14.GetActorsByType("2100fy2")
				sccommandcenter = p14.GetActorsByType("sccommandcenter")
				scbarracks = p14.GetActorsByType("scbarracks")
				scfactory = p14.GetActorsByType("scfactory")
				scstarport = p14.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
			if p15 and p16.IsAlliedWith(p15) then
				fact = p15.GetActorsByType("fact")
				pyle = p15.GetActorsByType("pyle")
				hand = p15.GetActorsByType("hand")
				afld = p15.GetActorsByType("afld")
				weap = p15.GetActorsByType("weap")
				hpad = p15.GetActorsByType("hpad")
				rafact = p15.GetActorsByType("rafact")
				tent = p15.GetActorsByType("tent")
				barr = p15.GetActorsByType("barr")
				raweapa = p15.GetActorsByType("raweapa")
				raweap = p15.GetActorsByType("raweap")
				rahpad = p15.GetActorsByType("rahpad")
				raafld = p15.GetActorsByType("raafld")
				hq = p15.GetActorsByType("2100hq")
				fy1 = p15.GetActorsByType("2100fy1")
				fy2 = p15.GetActorsByType("2100fy2")
				sccommandcenter = p15.GetActorsByType("sccommandcenter")
				scbarracks = p15.GetActorsByType("scbarracks")
				scfactory = p15.GetActorsByType("scfactory")
				scstarport = p15.GetActorsByType("scstarport")
				for _,fact in pairs(fact) do trucks.DeliverCash(fact) end
				for _,pyle in pairs(pyle) do trucks.DeliverCash(pyle) end
				for _,hand in pairs(hand) do trucks.DeliverCash(hand) end
				for _,afld in pairs(afld) do trucks.DeliverCash(afld) end
				for _,weap in pairs(weap) do trucks.DeliverCash(weap) end
				for _,hpad in pairs(hpad) do trucks.DeliverCash(hpad) end
				for _,rafact in pairs(rafact) do trucks.DeliverCash(rafact) end
				for _,tent in pairs(tent) do trucks.DeliverCash(tent) end
				for _,barr in pairs(barr) do trucks.DeliverCash(barr) end
				for _,raweapa in pairs(raweapa) do trucks.DeliverCash(raweapa) end
				for _,raweap in pairs(raweap) do trucks.DeliverCash(raweap) end
				for _,rahpad in pairs(rahpad) do trucks.DeliverCash(rahpad) end
				for _,raafld in pairs(raafld) do trucks.DeliverCash(raafld) end
				for _,hq in pairs(hq) do trucks.DeliverCash(hq) end
				for _,fy1 in pairs(fy1) do trucks.DeliverCash(fy1) end
				for _,fy2 in pairs(fy2) do trucks.DeliverCash(fy2) end
				for _,sccommandcenter in pairs(sccommandcenter) do trucks.DeliverCash(sccommandcenter) end
				for _,scbarracks in pairs(scbarracks) do trucks.DeliverCash(scbarracks) end
				for _,scfactory in pairs(scfactory) do trucks.DeliverCash(scfactory) end
				for _,scstarport in pairs(scstarport) do trucks.DeliverCash(scstarport) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(5), function()
		p16cash()
	end)
end

p1building = function()
	if p1 and p1.IsBot then
		citybotelitearchitect = p1.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p1.GetActorsByType("cityboti")
				citycown = p1.GetActorsByType("cityc")
				citybotrown = p1.GetActorsByType("citybotr")
				citybotnukeown = p1.GetActorsByType("citybotnuke")
				citybotcoalown = p1.GetActorsByType("citybotcoal")
				citymayorown = p1.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p2 and p1.IsAlliedWith(p2) then
				nuk2 = p2.GetActorsByType("nuk2")
				nuke = p2.GetActorsByType("nuke")
				apwr = p2.GetActorsByType("apwr")
				powr = p2.GetActorsByType("powr")
				power2100 = p2.GetActorsByType("2100power")
				scsupplydepot = p2.GetActorsByType("scsupplydepot")
				cityboti = p2.GetActorsByType("cityboti")
				cityc = p2.GetActorsByType("cityc")
				citybotr = p2.GetActorsByType("citybotr")
				citybotnuke = p2.GetActorsByType("citybotnuke")
				citybotcoal = p2.GetActorsByType("citybotcoal")
				citymayor = p2.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p3 and p1.IsAlliedWith(p3) then
				nuk2 = p3.GetActorsByType("nuk2")
				nuke = p3.GetActorsByType("nuke")
				apwr = p3.GetActorsByType("apwr")
				powr = p3.GetActorsByType("powr")
				power2100 = p3.GetActorsByType("2100power")
				scsupplydepot = p3.GetActorsByType("scsupplydepot")
				cityboti = p3.GetActorsByType("cityboti")
				cityc = p3.GetActorsByType("cityc")
				citybotr = p3.GetActorsByType("citybotr")
				citybotnuke = p3.GetActorsByType("citybotnuke")
				citybotcoal = p3.GetActorsByType("citybotcoal")
				citymayor = p3.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p4 and p1.IsAlliedWith(p4) then
				nuk2 = p4.GetActorsByType("nuk2")
				nuke = p4.GetActorsByType("nuke")
				apwr = p4.GetActorsByType("apwr")
				powr = p4.GetActorsByType("powr")
				power2100 = p4.GetActorsByType("2100power")
				scsupplydepot = p4.GetActorsByType("scsupplydepot")
				cityboti = p4.GetActorsByType("cityboti")
				cityc = p4.GetActorsByType("cityc")
				citybotr = p4.GetActorsByType("citybotr")
				citybotnuke = p4.GetActorsByType("citybotnuke")
				citybotcoal = p4.GetActorsByType("citybotcoal")
				citymayor = p4.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p5 and p1.IsAlliedWith(p5) then
				nuk2 = p5.GetActorsByType("nuk2")
				nuke = p5.GetActorsByType("nuke")
				apwr = p5.GetActorsByType("apwr")
				powr = p5.GetActorsByType("powr")
				power2100 = p5.GetActorsByType("2100power")
				scsupplydepot = p5.GetActorsByType("scsupplydepot")
				cityboti = p5.GetActorsByType("cityboti")
				cityc = p5.GetActorsByType("cityc")
				citybotr = p5.GetActorsByType("citybotr")
				citybotnuke = p5.GetActorsByType("citybotnuke")
				citybotcoal = p5.GetActorsByType("citybotcoal")
				citymayor = p5.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p6 and p1.IsAlliedWith(p6) then
				nuk2 = p6.GetActorsByType("nuk2")
				nuke = p6.GetActorsByType("nuke")
				apwr = p6.GetActorsByType("apwr")
				powr = p6.GetActorsByType("powr")
				power2100 = p6.GetActorsByType("2100power")
				scsupplydepot = p6.GetActorsByType("scsupplydepot")
				cityboti = p6.GetActorsByType("cityboti")
				cityc = p6.GetActorsByType("cityc")
				citybotr = p6.GetActorsByType("citybotr")
				citybotnuke = p6.GetActorsByType("citybotnuke")
				citybotcoal = p6.GetActorsByType("citybotcoal")
				citymayor = p6.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p7 and p1.IsAlliedWith(p7) then
				nuk2 = p7.GetActorsByType("nuk2")
				nuke = p7.GetActorsByType("nuke")
				apwr = p7.GetActorsByType("apwr")
				powr = p7.GetActorsByType("powr")
				power2100 = p7.GetActorsByType("2100power")
				scsupplydepot = p7.GetActorsByType("scsupplydepot")
				cityboti = p7.GetActorsByType("cityboti")
				cityc = p7.GetActorsByType("cityc")
				citybotr = p7.GetActorsByType("citybotr")
				citybotnuke = p7.GetActorsByType("citybotnuke")
				citybotcoal = p7.GetActorsByType("citybotcoal")
				citymayor = p7.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p8 and p1.IsAlliedWith(p8) then
				nuk2 = p8.GetActorsByType("nuk2")
				nuke = p8.GetActorsByType("nuke")
				apwr = p8.GetActorsByType("apwr")
				powr = p8.GetActorsByType("powr")
				power2100 = p8.GetActorsByType("2100power")
				scsupplydepot = p8.GetActorsByType("scsupplydepot")
				cityboti = p8.GetActorsByType("cityboti")
				cityc = p8.GetActorsByType("cityc")
				citybotr = p8.GetActorsByType("citybotr")
				citybotnuke = p8.GetActorsByType("citybotnuke")
				citybotcoal = p8.GetActorsByType("citybotcoal")
				citymayor = p8.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p9 and p1.IsAlliedWith(p9) then
				nuk2 = p9.GetActorsByType("nuk2")
				nuke = p9.GetActorsByType("nuke")
				apwr = p9.GetActorsByType("apwr")
				powr = p9.GetActorsByType("powr")
				power2100 = p9.GetActorsByType("2100power")
				scsupplydepot = p9.GetActorsByType("scsupplydepot")
				cityboti = p9.GetActorsByType("cityboti")
				cityc = p9.GetActorsByType("cityc")
				citybotr = p9.GetActorsByType("citybotr")
				citybotnuke = p9.GetActorsByType("citybotnuke")
				citybotcoal = p9.GetActorsByType("citybotcoal")
				citymayor = p9.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p10 and p1.IsAlliedWith(p10) then
				nuk2 = p10.GetActorsByType("nuk2")
				nuke = p10.GetActorsByType("nuke")
				apwr = p10.GetActorsByType("apwr")
				powr = p10.GetActorsByType("powr")
				power2100 = p10.GetActorsByType("2100power")
				scsupplydepot = p10.GetActorsByType("scsupplydepot")
				cityboti = p10.GetActorsByType("cityboti")
				cityc = p10.GetActorsByType("cityc")
				citybotr = p10.GetActorsByType("citybotr")
				citybotnuke = p10.GetActorsByType("citybotnuke")
				citybotcoal = p10.GetActorsByType("citybotcoal")
				citymayor = p10.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p11 and p1.IsAlliedWith(p11) then
				nuk2 = p11.GetActorsByType("nuk2")
				nuke = p11.GetActorsByType("nuke")
				apwr = p11.GetActorsByType("apwr")
				powr = p11.GetActorsByType("powr")
				power2100 = p11.GetActorsByType("2100power")
				scsupplydepot = p11.GetActorsByType("scsupplydepot")
				cityboti = p11.GetActorsByType("cityboti")
				cityc = p11.GetActorsByType("cityc")
				citybotr = p11.GetActorsByType("citybotr")
				citybotnuke = p11.GetActorsByType("citybotnuke")
				citybotcoal = p11.GetActorsByType("citybotcoal")
				citymayor = p11.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p12 and p1.IsAlliedWith(p12) then
				nuk2 = p12.GetActorsByType("nuk2")
				nuke = p12.GetActorsByType("nuke")
				apwr = p12.GetActorsByType("apwr")
				powr = p12.GetActorsByType("powr")
				power2100 = p12.GetActorsByType("2100power")
				scsupplydepot = p12.GetActorsByType("scsupplydepot")
				cityboti = p12.GetActorsByType("cityboti")
				cityc = p12.GetActorsByType("cityc")
				citybotr = p12.GetActorsByType("citybotr")
				citybotnuke = p12.GetActorsByType("citybotnuke")
				citybotcoal = p12.GetActorsByType("citybotcoal")
				citymayor = p12.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p13 and p1.IsAlliedWith(p13) then
				nuk2 = p13.GetActorsByType("nuk2")
				nuke = p13.GetActorsByType("nuke")
				apwr = p13.GetActorsByType("apwr")
				powr = p13.GetActorsByType("powr")
				power2100 = p13.GetActorsByType("2100power")
				scsupplydepot = p13.GetActorsByType("scsupplydepot")
				cityboti = p13.GetActorsByType("cityboti")
				cityc = p13.GetActorsByType("cityc")
				citybotr = p13.GetActorsByType("citybotr")
				citybotnuke = p13.GetActorsByType("citybotnuke")
				citybotcoal = p13.GetActorsByType("citybotcoal")
				citymayor = p13.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p14 and p1.IsAlliedWith(p14) then
				nuk2 = p14.GetActorsByType("nuk2")
				nuke = p14.GetActorsByType("nuke")
				apwr = p14.GetActorsByType("apwr")
				powr = p14.GetActorsByType("powr")
				power2100 = p14.GetActorsByType("2100power")
				scsupplydepot = p14.GetActorsByType("scsupplydepot")
				cityboti = p14.GetActorsByType("cityboti")
				cityc = p14.GetActorsByType("cityc")
				citybotr = p14.GetActorsByType("citybotr")
				citybotnuke = p14.GetActorsByType("citybotnuke")
				citybotcoal = p14.GetActorsByType("citybotcoal")
				citymayor = p14.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p15 and p1.IsAlliedWith(p15) then
				nuk2 = p15.GetActorsByType("nuk2")
				nuke = p15.GetActorsByType("nuke")
				apwr = p15.GetActorsByType("apwr")
				powr = p15.GetActorsByType("powr")
				power2100 = p15.GetActorsByType("2100power")
				scsupplydepot = p15.GetActorsByType("scsupplydepot")
				cityboti = p15.GetActorsByType("cityboti")
				cityc = p15.GetActorsByType("cityc")
				citybotr = p15.GetActorsByType("citybotr")
				citybotnuke = p15.GetActorsByType("citybotnuke")
				citybotcoal = p15.GetActorsByType("citybotcoal")
				citymayor = p15.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p16 and p1.IsAlliedWith(p16) then
				nuk2 = p16.GetActorsByType("nuk2")
				nuke = p16.GetActorsByType("nuke")
				apwr = p16.GetActorsByType("apwr")
				powr = p16.GetActorsByType("powr")
				power2100 = p16.GetActorsByType("2100power")
				scsupplydepot = p16.GetActorsByType("scsupplydepot")
				cityboti = p16.GetActorsByType("cityboti")
				cityc = p16.GetActorsByType("cityc")
				citybotr = p16.GetActorsByType("citybotr")
				citybotnuke = p16.GetActorsByType("citybotnuke")
				citybotcoal = p16.GetActorsByType("citybotcoal")
				citymayor = p16.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p1building()
	end)
end
p2building = function()
	if p2 and p2.IsBot then
		citybotelitearchitect = p2.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p2.GetActorsByType("cityboti")
				citycown = p2.GetActorsByType("cityc")
				citybotrown = p2.GetActorsByType("citybotr")
				citybotnukeown = p2.GetActorsByType("citybotnuke")
				citybotcoalown = p2.GetActorsByType("citybotcoal")
				citymayorown = p2.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p3 and p2.IsAlliedWith(p3) then
				nuk2 = p3.GetActorsByType("nuk2")
				nuke = p3.GetActorsByType("nuke")
				apwr = p3.GetActorsByType("apwr")
				powr = p3.GetActorsByType("powr")
				power2100 = p3.GetActorsByType("2100power")
				scsupplydepot = p3.GetActorsByType("scsupplydepot")
				cityboti = p3.GetActorsByType("cityboti")
				cityc = p3.GetActorsByType("cityc")
				citybotr = p3.GetActorsByType("citybotr")
				citybotnuke = p3.GetActorsByType("citybotnuke")
				citybotcoal = p3.GetActorsByType("citybotcoal")
				citymayor = p3.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p4 and p2.IsAlliedWith(p4) then
				nuk2 = p4.GetActorsByType("nuk2")
				nuke = p4.GetActorsByType("nuke")
				apwr = p4.GetActorsByType("apwr")
				powr = p4.GetActorsByType("powr")
				power2100 = p4.GetActorsByType("2100power")
				scsupplydepot = p4.GetActorsByType("scsupplydepot")
				cityboti = p4.GetActorsByType("cityboti")
				cityc = p4.GetActorsByType("cityc")
				citybotr = p4.GetActorsByType("citybotr")
				citybotnuke = p4.GetActorsByType("citybotnuke")
				citybotcoal = p4.GetActorsByType("citybotcoal")
				citymayor = p4.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p5 and p2.IsAlliedWith(p5) then
				nuk2 = p5.GetActorsByType("nuk2")
				nuke = p5.GetActorsByType("nuke")
				apwr = p5.GetActorsByType("apwr")
				powr = p5.GetActorsByType("powr")
				power2100 = p5.GetActorsByType("2100power")
				scsupplydepot = p5.GetActorsByType("scsupplydepot")
				cityboti = p5.GetActorsByType("cityboti")
				cityc = p5.GetActorsByType("cityc")
				citybotr = p5.GetActorsByType("citybotr")
				citybotnuke = p5.GetActorsByType("citybotnuke")
				citybotcoal = p5.GetActorsByType("citybotcoal")
				citymayor = p5.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p6 and p2.IsAlliedWith(p6) then
				nuk2 = p6.GetActorsByType("nuk2")
				nuke = p6.GetActorsByType("nuke")
				apwr = p6.GetActorsByType("apwr")
				powr = p6.GetActorsByType("powr")
				power2100 = p6.GetActorsByType("2100power")
				scsupplydepot = p6.GetActorsByType("scsupplydepot")
				cityboti = p6.GetActorsByType("cityboti")
				cityc = p6.GetActorsByType("cityc")
				citybotr = p6.GetActorsByType("citybotr")
				citybotnuke = p6.GetActorsByType("citybotnuke")
				citybotcoal = p6.GetActorsByType("citybotcoal")
				citymayor = p6.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p7 and p2.IsAlliedWith(p7) then
				nuk2 = p7.GetActorsByType("nuk2")
				nuke = p7.GetActorsByType("nuke")
				apwr = p7.GetActorsByType("apwr")
				powr = p7.GetActorsByType("powr")
				power2100 = p7.GetActorsByType("2100power")
				scsupplydepot = p7.GetActorsByType("scsupplydepot")
				cityboti = p7.GetActorsByType("cityboti")
				cityc = p7.GetActorsByType("cityc")
				citybotr = p7.GetActorsByType("citybotr")
				citybotnuke = p7.GetActorsByType("citybotnuke")
				citybotcoal = p7.GetActorsByType("citybotcoal")
				citymayor = p7.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p8 and p2.IsAlliedWith(p8) then
				nuk2 = p8.GetActorsByType("nuk2")
				nuke = p8.GetActorsByType("nuke")
				apwr = p8.GetActorsByType("apwr")
				powr = p8.GetActorsByType("powr")
				power2100 = p8.GetActorsByType("2100power")
				scsupplydepot = p8.GetActorsByType("scsupplydepot")
				cityboti = p8.GetActorsByType("cityboti")
				cityc = p8.GetActorsByType("cityc")
				citybotr = p8.GetActorsByType("citybotr")
				citybotnuke = p8.GetActorsByType("citybotnuke")
				citybotcoal = p8.GetActorsByType("citybotcoal")
				citymayor = p8.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p9 and p2.IsAlliedWith(p9) then
				nuk2 = p9.GetActorsByType("nuk2")
				nuke = p9.GetActorsByType("nuke")
				apwr = p9.GetActorsByType("apwr")
				powr = p9.GetActorsByType("powr")
				power2100 = p9.GetActorsByType("2100power")
				scsupplydepot = p9.GetActorsByType("scsupplydepot")
				cityboti = p9.GetActorsByType("cityboti")
				cityc = p9.GetActorsByType("cityc")
				citybotr = p9.GetActorsByType("citybotr")
				citybotnuke = p9.GetActorsByType("citybotnuke")
				citybotcoal = p9.GetActorsByType("citybotcoal")
				citymayor = p9.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p10 and p2.IsAlliedWith(p10) then
				nuk2 = p10.GetActorsByType("nuk2")
				nuke = p10.GetActorsByType("nuke")
				apwr = p10.GetActorsByType("apwr")
				powr = p10.GetActorsByType("powr")
				power2100 = p10.GetActorsByType("2100power")
				scsupplydepot = p10.GetActorsByType("scsupplydepot")
				cityboti = p10.GetActorsByType("cityboti")
				cityc = p10.GetActorsByType("cityc")
				citybotr = p10.GetActorsByType("citybotr")
				citybotnuke = p10.GetActorsByType("citybotnuke")
				citybotcoal = p10.GetActorsByType("citybotcoal")
				citymayor = p10.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p11 and p2.IsAlliedWith(p11) then
				nuk2 = p11.GetActorsByType("nuk2")
				nuke = p11.GetActorsByType("nuke")
				apwr = p11.GetActorsByType("apwr")
				powr = p11.GetActorsByType("powr")
				power2100 = p11.GetActorsByType("2100power")
				scsupplydepot = p11.GetActorsByType("scsupplydepot")
				cityboti = p11.GetActorsByType("cityboti")
				cityc = p11.GetActorsByType("cityc")
				citybotr = p11.GetActorsByType("citybotr")
				citybotnuke = p11.GetActorsByType("citybotnuke")
				citybotcoal = p11.GetActorsByType("citybotcoal")
				citymayor = p11.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p12 and p2.IsAlliedWith(p12) then
				nuk2 = p12.GetActorsByType("nuk2")
				nuke = p12.GetActorsByType("nuke")
				apwr = p12.GetActorsByType("apwr")
				powr = p12.GetActorsByType("powr")
				power2100 = p12.GetActorsByType("2100power")
				scsupplydepot = p12.GetActorsByType("scsupplydepot")
				cityboti = p12.GetActorsByType("cityboti")
				cityc = p12.GetActorsByType("cityc")
				citybotr = p12.GetActorsByType("citybotr")
				citybotnuke = p12.GetActorsByType("citybotnuke")
				citybotcoal = p12.GetActorsByType("citybotcoal")
				citymayor = p12.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p13 and p2.IsAlliedWith(p13) then
				nuk2 = p13.GetActorsByType("nuk2")
				nuke = p13.GetActorsByType("nuke")
				apwr = p13.GetActorsByType("apwr")
				powr = p13.GetActorsByType("powr")
				power2100 = p13.GetActorsByType("2100power")
				scsupplydepot = p13.GetActorsByType("scsupplydepot")
				cityboti = p13.GetActorsByType("cityboti")
				cityc = p13.GetActorsByType("cityc")
				citybotr = p13.GetActorsByType("citybotr")
				citybotnuke = p13.GetActorsByType("citybotnuke")
				citybotcoal = p13.GetActorsByType("citybotcoal")
				citymayor = p13.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p14 and p2.IsAlliedWith(p14) then
				nuk2 = p14.GetActorsByType("nuk2")
				nuke = p14.GetActorsByType("nuke")
				apwr = p14.GetActorsByType("apwr")
				powr = p14.GetActorsByType("powr")
				power2100 = p14.GetActorsByType("2100power")
				scsupplydepot = p14.GetActorsByType("scsupplydepot")
				cityboti = p14.GetActorsByType("cityboti")
				cityc = p14.GetActorsByType("cityc")
				citybotr = p14.GetActorsByType("citybotr")
				citybotnuke = p14.GetActorsByType("citybotnuke")
				citybotcoal = p14.GetActorsByType("citybotcoal")
				citymayor = p14.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p15 and p2.IsAlliedWith(p15) then
				nuk2 = p15.GetActorsByType("nuk2")
				nuke = p15.GetActorsByType("nuke")
				apwr = p15.GetActorsByType("apwr")
				powr = p15.GetActorsByType("powr")
				power2100 = p15.GetActorsByType("2100power")
				scsupplydepot = p15.GetActorsByType("scsupplydepot")
				cityboti = p15.GetActorsByType("cityboti")
				cityc = p15.GetActorsByType("cityc")
				citybotr = p15.GetActorsByType("citybotr")
				citybotnuke = p15.GetActorsByType("citybotnuke")
				citybotcoal = p15.GetActorsByType("citybotcoal")
				citymayor = p15.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p16 and p2.IsAlliedWith(p16) then
				nuk2 = p16.GetActorsByType("nuk2")
				nuke = p16.GetActorsByType("nuke")
				apwr = p16.GetActorsByType("apwr")
				powr = p16.GetActorsByType("powr")
				power2100 = p16.GetActorsByType("2100power")
				scsupplydepot = p16.GetActorsByType("scsupplydepot")
				cityboti = p16.GetActorsByType("cityboti")
				cityc = p16.GetActorsByType("cityc")
				citybotr = p16.GetActorsByType("citybotr")
				citybotnuke = p16.GetActorsByType("citybotnuke")
				citybotcoal = p16.GetActorsByType("citybotcoal")
				citymayor = p16.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p1 and p2.IsAlliedWith(p1) then
				nuk2 = p1.GetActorsByType("nuk2")
				nuke = p1.GetActorsByType("nuke")
				apwr = p1.GetActorsByType("apwr")
				powr = p1.GetActorsByType("powr")
				power2100 = p1.GetActorsByType("2100power")
				scsupplydepot = p1.GetActorsByType("scsupplydepot")
				cityboti = p1.GetActorsByType("cityboti")
				cityc = p1.GetActorsByType("cityc")
				citybotr = p1.GetActorsByType("citybotr")
				citybotnuke = p1.GetActorsByType("citybotnuke")
				citybotcoal = p1.GetActorsByType("citybotcoal")
				citymayor = p1.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p2building()
	end)
end
p3building = function()
	if p3 and p3.IsBot then
		citybotelitearchitect = p3.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p3.GetActorsByType("cityboti")
				citycown = p3.GetActorsByType("cityc")
				citybotrown = p3.GetActorsByType("citybotr")
				citybotnukeown = p3.GetActorsByType("citybotnuke")
				citybotcoalown = p3.GetActorsByType("citybotcoal")
				citymayorown = p3.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p4 and p3.IsAlliedWith(p4) then
				nuk2 = p4.GetActorsByType("nuk2")
				nuke = p4.GetActorsByType("nuke")
				apwr = p4.GetActorsByType("apwr")
				powr = p4.GetActorsByType("powr")
				power2100 = p4.GetActorsByType("2100power")
				scsupplydepot = p4.GetActorsByType("scsupplydepot")
				cityboti = p4.GetActorsByType("cityboti")
				cityc = p4.GetActorsByType("cityc")
				citybotr = p4.GetActorsByType("citybotr")
				citybotnuke = p4.GetActorsByType("citybotnuke")
				citybotcoal = p4.GetActorsByType("citybotcoal")
				citymayor = p4.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p5 and p3.IsAlliedWith(p5) then
				nuk2 = p5.GetActorsByType("nuk2")
				nuke = p5.GetActorsByType("nuke")
				apwr = p5.GetActorsByType("apwr")
				powr = p5.GetActorsByType("powr")
				power2100 = p5.GetActorsByType("2100power")
				scsupplydepot = p5.GetActorsByType("scsupplydepot")
				cityboti = p5.GetActorsByType("cityboti")
				cityc = p5.GetActorsByType("cityc")
				citybotr = p5.GetActorsByType("citybotr")
				citybotnuke = p5.GetActorsByType("citybotnuke")
				citybotcoal = p5.GetActorsByType("citybotcoal")
				citymayor = p5.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p6 and p3.IsAlliedWith(p6) then
				nuk2 = p6.GetActorsByType("nuk2")
				nuke = p6.GetActorsByType("nuke")
				apwr = p6.GetActorsByType("apwr")
				powr = p6.GetActorsByType("powr")
				power2100 = p6.GetActorsByType("2100power")
				scsupplydepot = p6.GetActorsByType("scsupplydepot")
				cityboti = p6.GetActorsByType("cityboti")
				cityc = p6.GetActorsByType("cityc")
				citybotr = p6.GetActorsByType("citybotr")
				citybotnuke = p6.GetActorsByType("citybotnuke")
				citybotcoal = p6.GetActorsByType("citybotcoal")
				citymayor = p6.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p7 and p3.IsAlliedWith(p7) then
				nuk2 = p7.GetActorsByType("nuk2")
				nuke = p7.GetActorsByType("nuke")
				apwr = p7.GetActorsByType("apwr")
				powr = p7.GetActorsByType("powr")
				power2100 = p7.GetActorsByType("2100power")
				scsupplydepot = p7.GetActorsByType("scsupplydepot")
				cityboti = p7.GetActorsByType("cityboti")
				cityc = p7.GetActorsByType("cityc")
				citybotr = p7.GetActorsByType("citybotr")
				citybotnuke = p7.GetActorsByType("citybotnuke")
				citybotcoal = p7.GetActorsByType("citybotcoal")
				citymayor = p7.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p8 and p3.IsAlliedWith(p8) then
				nuk2 = p8.GetActorsByType("nuk2")
				nuke = p8.GetActorsByType("nuke")
				apwr = p8.GetActorsByType("apwr")
				powr = p8.GetActorsByType("powr")
				power2100 = p8.GetActorsByType("2100power")
				scsupplydepot = p8.GetActorsByType("scsupplydepot")
				cityboti = p8.GetActorsByType("cityboti")
				cityc = p8.GetActorsByType("cityc")
				citybotr = p8.GetActorsByType("citybotr")
				citybotnuke = p8.GetActorsByType("citybotnuke")
				citybotcoal = p8.GetActorsByType("citybotcoal")
				citymayor = p8.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p9 and p3.IsAlliedWith(p9) then
				nuk2 = p9.GetActorsByType("nuk2")
				nuke = p9.GetActorsByType("nuke")
				apwr = p9.GetActorsByType("apwr")
				powr = p9.GetActorsByType("powr")
				power2100 = p9.GetActorsByType("2100power")
				scsupplydepot = p9.GetActorsByType("scsupplydepot")
				cityboti = p9.GetActorsByType("cityboti")
				cityc = p9.GetActorsByType("cityc")
				citybotr = p9.GetActorsByType("citybotr")
				citybotnuke = p9.GetActorsByType("citybotnuke")
				citybotcoal = p9.GetActorsByType("citybotcoal")
				citymayor = p9.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p10 and p3.IsAlliedWith(p10) then
				nuk2 = p10.GetActorsByType("nuk2")
				nuke = p10.GetActorsByType("nuke")
				apwr = p10.GetActorsByType("apwr")
				powr = p10.GetActorsByType("powr")
				power2100 = p10.GetActorsByType("2100power")
				scsupplydepot = p10.GetActorsByType("scsupplydepot")
				cityboti = p10.GetActorsByType("cityboti")
				cityc = p10.GetActorsByType("cityc")
				citybotr = p10.GetActorsByType("citybotr")
				citybotnuke = p10.GetActorsByType("citybotnuke")
				citybotcoal = p10.GetActorsByType("citybotcoal")
				citymayor = p10.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p11 and p3.IsAlliedWith(p11) then
				nuk2 = p11.GetActorsByType("nuk2")
				nuke = p11.GetActorsByType("nuke")
				apwr = p11.GetActorsByType("apwr")
				powr = p11.GetActorsByType("powr")
				power2100 = p11.GetActorsByType("2100power")
				scsupplydepot = p11.GetActorsByType("scsupplydepot")
				cityboti = p11.GetActorsByType("cityboti")
				cityc = p11.GetActorsByType("cityc")
				citybotr = p11.GetActorsByType("citybotr")
				citybotnuke = p11.GetActorsByType("citybotnuke")
				citybotcoal = p11.GetActorsByType("citybotcoal")
				citymayor = p11.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p12 and p3.IsAlliedWith(p12) then
				nuk2 = p12.GetActorsByType("nuk2")
				nuke = p12.GetActorsByType("nuke")
				apwr = p12.GetActorsByType("apwr")
				powr = p12.GetActorsByType("powr")
				power2100 = p12.GetActorsByType("2100power")
				scsupplydepot = p12.GetActorsByType("scsupplydepot")
				cityboti = p12.GetActorsByType("cityboti")
				cityc = p12.GetActorsByType("cityc")
				citybotr = p12.GetActorsByType("citybotr")
				citybotnuke = p12.GetActorsByType("citybotnuke")
				citybotcoal = p12.GetActorsByType("citybotcoal")
				citymayor = p12.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p13 and p3.IsAlliedWith(p13) then
				nuk2 = p13.GetActorsByType("nuk2")
				nuke = p13.GetActorsByType("nuke")
				apwr = p13.GetActorsByType("apwr")
				powr = p13.GetActorsByType("powr")
				power2100 = p13.GetActorsByType("2100power")
				scsupplydepot = p13.GetActorsByType("scsupplydepot")
				cityboti = p13.GetActorsByType("cityboti")
				cityc = p13.GetActorsByType("cityc")
				citybotr = p13.GetActorsByType("citybotr")
				citybotnuke = p13.GetActorsByType("citybotnuke")
				citybotcoal = p13.GetActorsByType("citybotcoal")
				citymayor = p13.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p14 and p3.IsAlliedWith(p14) then
				nuk2 = p14.GetActorsByType("nuk2")
				nuke = p14.GetActorsByType("nuke")
				apwr = p14.GetActorsByType("apwr")
				powr = p14.GetActorsByType("powr")
				power2100 = p14.GetActorsByType("2100power")
				scsupplydepot = p14.GetActorsByType("scsupplydepot")
				cityboti = p14.GetActorsByType("cityboti")
				cityc = p14.GetActorsByType("cityc")
				citybotr = p14.GetActorsByType("citybotr")
				citybotnuke = p14.GetActorsByType("citybotnuke")
				citybotcoal = p14.GetActorsByType("citybotcoal")
				citymayor = p14.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p15 and p3.IsAlliedWith(p15) then
				nuk2 = p15.GetActorsByType("nuk2")
				nuke = p15.GetActorsByType("nuke")
				apwr = p15.GetActorsByType("apwr")
				powr = p15.GetActorsByType("powr")
				power2100 = p15.GetActorsByType("2100power")
				scsupplydepot = p15.GetActorsByType("scsupplydepot")
				cityboti = p15.GetActorsByType("cityboti")
				cityc = p15.GetActorsByType("cityc")
				citybotr = p15.GetActorsByType("citybotr")
				citybotnuke = p15.GetActorsByType("citybotnuke")
				citybotcoal = p15.GetActorsByType("citybotcoal")
				citymayor = p15.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p16 and p3.IsAlliedWith(p16) then
				nuk2 = p16.GetActorsByType("nuk2")
				nuke = p16.GetActorsByType("nuke")
				apwr = p16.GetActorsByType("apwr")
				powr = p16.GetActorsByType("powr")
				power2100 = p16.GetActorsByType("2100power")
				scsupplydepot = p16.GetActorsByType("scsupplydepot")
				cityboti = p16.GetActorsByType("cityboti")
				cityc = p16.GetActorsByType("cityc")
				citybotr = p16.GetActorsByType("citybotr")
				citybotnuke = p16.GetActorsByType("citybotnuke")
				citybotcoal = p16.GetActorsByType("citybotcoal")
				citymayor = p16.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p1 and p3.IsAlliedWith(p1) then
				nuk2 = p1.GetActorsByType("nuk2")
				nuke = p1.GetActorsByType("nuke")
				apwr = p1.GetActorsByType("apwr")
				powr = p1.GetActorsByType("powr")
				power2100 = p1.GetActorsByType("2100power")
				scsupplydepot = p1.GetActorsByType("scsupplydepot")
				cityboti = p1.GetActorsByType("cityboti")
				cityc = p1.GetActorsByType("cityc")
				citybotr = p1.GetActorsByType("citybotr")
				citybotnuke = p1.GetActorsByType("citybotnuke")
				citybotcoal = p1.GetActorsByType("citybotcoal")
				citymayor = p1.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p2 and p3.IsAlliedWith(p2) then
				nuk2 = p2.GetActorsByType("nuk2")
				nuke = p2.GetActorsByType("nuke")
				apwr = p2.GetActorsByType("apwr")
				powr = p2.GetActorsByType("powr")
				power2100 = p2.GetActorsByType("2100power")
				scsupplydepot = p2.GetActorsByType("scsupplydepot")
				cityboti = p2.GetActorsByType("cityboti")
				cityc = p2.GetActorsByType("cityc")
				citybotr = p2.GetActorsByType("citybotr")
				citybotnuke = p2.GetActorsByType("citybotnuke")
				citybotcoal = p2.GetActorsByType("citybotcoal")
				citymayor = p2.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p3building()
	end)
end
p4building = function()
	if p4 and p4.IsBot then
		citybotelitearchitect = p4.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p4.GetActorsByType("cityboti")
				citycown = p4.GetActorsByType("cityc")
				citybotrown = p4.GetActorsByType("citybotr")
				citybotnukeown = p4.GetActorsByType("citybotnuke")
				citybotcoalown = p4.GetActorsByType("citybotcoal")
				citymayorown = p4.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p5 and p4.IsAlliedWith(p5) then
				nuk2 = p5.GetActorsByType("nuk2")
				nuke = p5.GetActorsByType("nuke")
				apwr = p5.GetActorsByType("apwr")
				powr = p5.GetActorsByType("powr")
				power2100 = p5.GetActorsByType("2100power")
				scsupplydepot = p5.GetActorsByType("scsupplydepot")
				cityboti = p5.GetActorsByType("cityboti")
				cityc = p5.GetActorsByType("cityc")
				citybotr = p5.GetActorsByType("citybotr")
				citybotnuke = p5.GetActorsByType("citybotnuke")
				citybotcoal = p5.GetActorsByType("citybotcoal")
				citymayor = p5.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p6 and p4.IsAlliedWith(p6) then
				nuk2 = p6.GetActorsByType("nuk2")
				nuke = p6.GetActorsByType("nuke")
				apwr = p6.GetActorsByType("apwr")
				powr = p6.GetActorsByType("powr")
				power2100 = p6.GetActorsByType("2100power")
				scsupplydepot = p6.GetActorsByType("scsupplydepot")
				cityboti = p6.GetActorsByType("cityboti")
				cityc = p6.GetActorsByType("cityc")
				citybotr = p6.GetActorsByType("citybotr")
				citybotnuke = p6.GetActorsByType("citybotnuke")
				citybotcoal = p6.GetActorsByType("citybotcoal")
				citymayor = p6.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p7 and p4.IsAlliedWith(p7) then
				nuk2 = p7.GetActorsByType("nuk2")
				nuke = p7.GetActorsByType("nuke")
				apwr = p7.GetActorsByType("apwr")
				powr = p7.GetActorsByType("powr")
				power2100 = p7.GetActorsByType("2100power")
				scsupplydepot = p7.GetActorsByType("scsupplydepot")
				cityboti = p7.GetActorsByType("cityboti")
				cityc = p7.GetActorsByType("cityc")
				citybotr = p7.GetActorsByType("citybotr")
				citybotnuke = p7.GetActorsByType("citybotnuke")
				citybotcoal = p7.GetActorsByType("citybotcoal")
				citymayor = p7.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p8 and p4.IsAlliedWith(p8) then
				nuk2 = p8.GetActorsByType("nuk2")
				nuke = p8.GetActorsByType("nuke")
				apwr = p8.GetActorsByType("apwr")
				powr = p8.GetActorsByType("powr")
				power2100 = p8.GetActorsByType("2100power")
				scsupplydepot = p8.GetActorsByType("scsupplydepot")
				cityboti = p8.GetActorsByType("cityboti")
				cityc = p8.GetActorsByType("cityc")
				citybotr = p8.GetActorsByType("citybotr")
				citybotnuke = p8.GetActorsByType("citybotnuke")
				citybotcoal = p8.GetActorsByType("citybotcoal")
				citymayor = p8.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p9 and p4.IsAlliedWith(p9) then
				nuk2 = p9.GetActorsByType("nuk2")
				nuke = p9.GetActorsByType("nuke")
				apwr = p9.GetActorsByType("apwr")
				powr = p9.GetActorsByType("powr")
				power2100 = p9.GetActorsByType("2100power")
				scsupplydepot = p9.GetActorsByType("scsupplydepot")
				cityboti = p9.GetActorsByType("cityboti")
				cityc = p9.GetActorsByType("cityc")
				citybotr = p9.GetActorsByType("citybotr")
				citybotnuke = p9.GetActorsByType("citybotnuke")
				citybotcoal = p9.GetActorsByType("citybotcoal")
				citymayor = p9.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p10 and p4.IsAlliedWith(p10) then
				nuk2 = p10.GetActorsByType("nuk2")
				nuke = p10.GetActorsByType("nuke")
				apwr = p10.GetActorsByType("apwr")
				powr = p10.GetActorsByType("powr")
				power2100 = p10.GetActorsByType("2100power")
				scsupplydepot = p10.GetActorsByType("scsupplydepot")
				cityboti = p10.GetActorsByType("cityboti")
				cityc = p10.GetActorsByType("cityc")
				citybotr = p10.GetActorsByType("citybotr")
				citybotnuke = p10.GetActorsByType("citybotnuke")
				citybotcoal = p10.GetActorsByType("citybotcoal")
				citymayor = p10.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p11 and p4.IsAlliedWith(p11) then
				nuk2 = p11.GetActorsByType("nuk2")
				nuke = p11.GetActorsByType("nuke")
				apwr = p11.GetActorsByType("apwr")
				powr = p11.GetActorsByType("powr")
				power2100 = p11.GetActorsByType("2100power")
				scsupplydepot = p11.GetActorsByType("scsupplydepot")
				cityboti = p11.GetActorsByType("cityboti")
				cityc = p11.GetActorsByType("cityc")
				citybotr = p11.GetActorsByType("citybotr")
				citybotnuke = p11.GetActorsByType("citybotnuke")
				citybotcoal = p11.GetActorsByType("citybotcoal")
				citymayor = p11.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p12 and p4.IsAlliedWith(p12) then
				nuk2 = p12.GetActorsByType("nuk2")
				nuke = p12.GetActorsByType("nuke")
				apwr = p12.GetActorsByType("apwr")
				powr = p12.GetActorsByType("powr")
				power2100 = p12.GetActorsByType("2100power")
				scsupplydepot = p12.GetActorsByType("scsupplydepot")
				cityboti = p12.GetActorsByType("cityboti")
				cityc = p12.GetActorsByType("cityc")
				citybotr = p12.GetActorsByType("citybotr")
				citybotnuke = p12.GetActorsByType("citybotnuke")
				citybotcoal = p12.GetActorsByType("citybotcoal")
				citymayor = p12.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p13 and p4.IsAlliedWith(p13) then
				nuk2 = p13.GetActorsByType("nuk2")
				nuke = p13.GetActorsByType("nuke")
				apwr = p13.GetActorsByType("apwr")
				powr = p13.GetActorsByType("powr")
				power2100 = p13.GetActorsByType("2100power")
				scsupplydepot = p13.GetActorsByType("scsupplydepot")
				cityboti = p13.GetActorsByType("cityboti")
				cityc = p13.GetActorsByType("cityc")
				citybotr = p13.GetActorsByType("citybotr")
				citybotnuke = p13.GetActorsByType("citybotnuke")
				citybotcoal = p13.GetActorsByType("citybotcoal")
				citymayor = p13.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p14 and p4.IsAlliedWith(p14) then
				nuk2 = p14.GetActorsByType("nuk2")
				nuke = p14.GetActorsByType("nuke")
				apwr = p14.GetActorsByType("apwr")
				powr = p14.GetActorsByType("powr")
				power2100 = p14.GetActorsByType("2100power")
				scsupplydepot = p14.GetActorsByType("scsupplydepot")
				cityboti = p14.GetActorsByType("cityboti")
				cityc = p14.GetActorsByType("cityc")
				citybotr = p14.GetActorsByType("citybotr")
				citybotnuke = p14.GetActorsByType("citybotnuke")
				citybotcoal = p14.GetActorsByType("citybotcoal")
				citymayor = p14.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p15 and p4.IsAlliedWith(p15) then
				nuk2 = p15.GetActorsByType("nuk2")
				nuke = p15.GetActorsByType("nuke")
				apwr = p15.GetActorsByType("apwr")
				powr = p15.GetActorsByType("powr")
				power2100 = p15.GetActorsByType("2100power")
				scsupplydepot = p15.GetActorsByType("scsupplydepot")
				cityboti = p15.GetActorsByType("cityboti")
				cityc = p15.GetActorsByType("cityc")
				citybotr = p15.GetActorsByType("citybotr")
				citybotnuke = p15.GetActorsByType("citybotnuke")
				citybotcoal = p15.GetActorsByType("citybotcoal")
				citymayor = p15.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p16 and p4.IsAlliedWith(p16) then
				nuk2 = p16.GetActorsByType("nuk2")
				nuke = p16.GetActorsByType("nuke")
				apwr = p16.GetActorsByType("apwr")
				powr = p16.GetActorsByType("powr")
				power2100 = p16.GetActorsByType("2100power")
				scsupplydepot = p16.GetActorsByType("scsupplydepot")
				cityboti = p16.GetActorsByType("cityboti")
				cityc = p16.GetActorsByType("cityc")
				citybotr = p16.GetActorsByType("citybotr")
				citybotnuke = p16.GetActorsByType("citybotnuke")
				citybotcoal = p16.GetActorsByType("citybotcoal")
				citymayor = p16.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p1 and p4.IsAlliedWith(p1) then
				nuk2 = p1.GetActorsByType("nuk2")
				nuke = p1.GetActorsByType("nuke")
				apwr = p1.GetActorsByType("apwr")
				powr = p1.GetActorsByType("powr")
				power2100 = p1.GetActorsByType("2100power")
				scsupplydepot = p1.GetActorsByType("scsupplydepot")
				cityboti = p1.GetActorsByType("cityboti")
				cityc = p1.GetActorsByType("cityc")
				citybotr = p1.GetActorsByType("citybotr")
				citybotnuke = p1.GetActorsByType("citybotnuke")
				citybotcoal = p1.GetActorsByType("citybotcoal")
				citymayor = p1.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p2 and p4.IsAlliedWith(p2) then
				nuk2 = p2.GetActorsByType("nuk2")
				nuke = p2.GetActorsByType("nuke")
				apwr = p2.GetActorsByType("apwr")
				powr = p2.GetActorsByType("powr")
				power2100 = p2.GetActorsByType("2100power")
				scsupplydepot = p2.GetActorsByType("scsupplydepot")
				cityboti = p2.GetActorsByType("cityboti")
				cityc = p2.GetActorsByType("cityc")
				citybotr = p2.GetActorsByType("citybotr")
				citybotnuke = p2.GetActorsByType("citybotnuke")
				citybotcoal = p2.GetActorsByType("citybotcoal")
				citymayor = p2.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p3 and p4.IsAlliedWith(p3) then
				nuk2 = p3.GetActorsByType("nuk2")
				nuke = p3.GetActorsByType("nuke")
				apwr = p3.GetActorsByType("apwr")
				powr = p3.GetActorsByType("powr")
				power2100 = p3.GetActorsByType("2100power")
				scsupplydepot = p3.GetActorsByType("scsupplydepot")
				cityboti = p3.GetActorsByType("cityboti")
				cityc = p3.GetActorsByType("cityc")
				citybotr = p3.GetActorsByType("citybotr")
				citybotnuke = p3.GetActorsByType("citybotnuke")
				citybotcoal = p3.GetActorsByType("citybotcoal")
				citymayor = p3.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p4building()
	end)
end
p5building = function()
	if p5 and p5.IsBot then
		citybotelitearchitect = p5.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p5.GetActorsByType("cityboti")
				citycown = p5.GetActorsByType("cityc")
				citybotrown = p5.GetActorsByType("citybotr")
				citybotnukeown = p5.GetActorsByType("citybotnuke")
				citybotcoalown = p5.GetActorsByType("citybotcoal")
				citymayorown = p5.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p6 and p5.IsAlliedWith(p6) then
				nuk2 = p6.GetActorsByType("nuk2")
				nuke = p6.GetActorsByType("nuke")
				apwr = p6.GetActorsByType("apwr")
				powr = p6.GetActorsByType("powr")
				power2100 = p6.GetActorsByType("2100power")
				scsupplydepot = p6.GetActorsByType("scsupplydepot")
				cityboti = p6.GetActorsByType("cityboti")
				cityc = p6.GetActorsByType("cityc")
				citybotr = p6.GetActorsByType("citybotr")
				citybotnuke = p6.GetActorsByType("citybotnuke")
				citybotcoal = p6.GetActorsByType("citybotcoal")
				citymayor = p6.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p7 and p5.IsAlliedWith(p7) then
				nuk2 = p7.GetActorsByType("nuk2")
				nuke = p7.GetActorsByType("nuke")
				apwr = p7.GetActorsByType("apwr")
				powr = p7.GetActorsByType("powr")
				power2100 = p7.GetActorsByType("2100power")
				scsupplydepot = p7.GetActorsByType("scsupplydepot")
				cityboti = p7.GetActorsByType("cityboti")
				cityc = p7.GetActorsByType("cityc")
				citybotr = p7.GetActorsByType("citybotr")
				citybotnuke = p7.GetActorsByType("citybotnuke")
				citybotcoal = p7.GetActorsByType("citybotcoal")
				citymayor = p7.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p8 and p5.IsAlliedWith(p8) then
				nuk2 = p8.GetActorsByType("nuk2")
				nuke = p8.GetActorsByType("nuke")
				apwr = p8.GetActorsByType("apwr")
				powr = p8.GetActorsByType("powr")
				power2100 = p8.GetActorsByType("2100power")
				scsupplydepot = p8.GetActorsByType("scsupplydepot")
				cityboti = p8.GetActorsByType("cityboti")
				cityc = p8.GetActorsByType("cityc")
				citybotr = p8.GetActorsByType("citybotr")
				citybotnuke = p8.GetActorsByType("citybotnuke")
				citybotcoal = p8.GetActorsByType("citybotcoal")
				citymayor = p8.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p9 and p5.IsAlliedWith(p9) then
				nuk2 = p9.GetActorsByType("nuk2")
				nuke = p9.GetActorsByType("nuke")
				apwr = p9.GetActorsByType("apwr")
				powr = p9.GetActorsByType("powr")
				power2100 = p9.GetActorsByType("2100power")
				scsupplydepot = p9.GetActorsByType("scsupplydepot")
				cityboti = p9.GetActorsByType("cityboti")
				cityc = p9.GetActorsByType("cityc")
				citybotr = p9.GetActorsByType("citybotr")
				citybotnuke = p9.GetActorsByType("citybotnuke")
				citybotcoal = p9.GetActorsByType("citybotcoal")
				citymayor = p9.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p10 and p5.IsAlliedWith(p10) then
				nuk2 = p10.GetActorsByType("nuk2")
				nuke = p10.GetActorsByType("nuke")
				apwr = p10.GetActorsByType("apwr")
				powr = p10.GetActorsByType("powr")
				power2100 = p10.GetActorsByType("2100power")
				scsupplydepot = p10.GetActorsByType("scsupplydepot")
				cityboti = p10.GetActorsByType("cityboti")
				cityc = p10.GetActorsByType("cityc")
				citybotr = p10.GetActorsByType("citybotr")
				citybotnuke = p10.GetActorsByType("citybotnuke")
				citybotcoal = p10.GetActorsByType("citybotcoal")
				citymayor = p10.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p11 and p5.IsAlliedWith(p11) then
				nuk2 = p11.GetActorsByType("nuk2")
				nuke = p11.GetActorsByType("nuke")
				apwr = p11.GetActorsByType("apwr")
				powr = p11.GetActorsByType("powr")
				power2100 = p11.GetActorsByType("2100power")
				scsupplydepot = p11.GetActorsByType("scsupplydepot")
				cityboti = p11.GetActorsByType("cityboti")
				cityc = p11.GetActorsByType("cityc")
				citybotr = p11.GetActorsByType("citybotr")
				citybotnuke = p11.GetActorsByType("citybotnuke")
				citybotcoal = p11.GetActorsByType("citybotcoal")
				citymayor = p11.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p12 and p5.IsAlliedWith(p12) then
				nuk2 = p12.GetActorsByType("nuk2")
				nuke = p12.GetActorsByType("nuke")
				apwr = p12.GetActorsByType("apwr")
				powr = p12.GetActorsByType("powr")
				power2100 = p12.GetActorsByType("2100power")
				scsupplydepot = p12.GetActorsByType("scsupplydepot")
				cityboti = p12.GetActorsByType("cityboti")
				cityc = p12.GetActorsByType("cityc")
				citybotr = p12.GetActorsByType("citybotr")
				citybotnuke = p12.GetActorsByType("citybotnuke")
				citybotcoal = p12.GetActorsByType("citybotcoal")
				citymayor = p12.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p13 and p5.IsAlliedWith(p13) then
				nuk2 = p13.GetActorsByType("nuk2")
				nuke = p13.GetActorsByType("nuke")
				apwr = p13.GetActorsByType("apwr")
				powr = p13.GetActorsByType("powr")
				power2100 = p13.GetActorsByType("2100power")
				scsupplydepot = p13.GetActorsByType("scsupplydepot")
				cityboti = p13.GetActorsByType("cityboti")
				cityc = p13.GetActorsByType("cityc")
				citybotr = p13.GetActorsByType("citybotr")
				citybotnuke = p13.GetActorsByType("citybotnuke")
				citybotcoal = p13.GetActorsByType("citybotcoal")
				citymayor = p13.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p14 and p5.IsAlliedWith(p14) then
				nuk2 = p14.GetActorsByType("nuk2")
				nuke = p14.GetActorsByType("nuke")
				apwr = p14.GetActorsByType("apwr")
				powr = p14.GetActorsByType("powr")
				power2100 = p14.GetActorsByType("2100power")
				scsupplydepot = p14.GetActorsByType("scsupplydepot")
				cityboti = p14.GetActorsByType("cityboti")
				cityc = p14.GetActorsByType("cityc")
				citybotr = p14.GetActorsByType("citybotr")
				citybotnuke = p14.GetActorsByType("citybotnuke")
				citybotcoal = p14.GetActorsByType("citybotcoal")
				citymayor = p14.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p15 and p5.IsAlliedWith(p15) then
				nuk2 = p15.GetActorsByType("nuk2")
				nuke = p15.GetActorsByType("nuke")
				apwr = p15.GetActorsByType("apwr")
				powr = p15.GetActorsByType("powr")
				power2100 = p15.GetActorsByType("2100power")
				scsupplydepot = p15.GetActorsByType("scsupplydepot")
				cityboti = p15.GetActorsByType("cityboti")
				cityc = p15.GetActorsByType("cityc")
				citybotr = p15.GetActorsByType("citybotr")
				citybotnuke = p15.GetActorsByType("citybotnuke")
				citybotcoal = p15.GetActorsByType("citybotcoal")
				citymayor = p15.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p16 and p5.IsAlliedWith(p16) then
				nuk2 = p16.GetActorsByType("nuk2")
				nuke = p16.GetActorsByType("nuke")
				apwr = p16.GetActorsByType("apwr")
				powr = p16.GetActorsByType("powr")
				power2100 = p16.GetActorsByType("2100power")
				scsupplydepot = p16.GetActorsByType("scsupplydepot")
				cityboti = p16.GetActorsByType("cityboti")
				cityc = p16.GetActorsByType("cityc")
				citybotr = p16.GetActorsByType("citybotr")
				citybotnuke = p16.GetActorsByType("citybotnuke")
				citybotcoal = p16.GetActorsByType("citybotcoal")
				citymayor = p16.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p1 and p5.IsAlliedWith(p1) then
				nuk2 = p1.GetActorsByType("nuk2")
				nuke = p1.GetActorsByType("nuke")
				apwr = p1.GetActorsByType("apwr")
				powr = p1.GetActorsByType("powr")
				power2100 = p1.GetActorsByType("2100power")
				scsupplydepot = p1.GetActorsByType("scsupplydepot")
				cityboti = p1.GetActorsByType("cityboti")
				cityc = p1.GetActorsByType("cityc")
				citybotr = p1.GetActorsByType("citybotr")
				citybotnuke = p1.GetActorsByType("citybotnuke")
				citybotcoal = p1.GetActorsByType("citybotcoal")
				citymayor = p1.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p2 and p5.IsAlliedWith(p2) then
				nuk2 = p2.GetActorsByType("nuk2")
				nuke = p2.GetActorsByType("nuke")
				apwr = p2.GetActorsByType("apwr")
				powr = p2.GetActorsByType("powr")
				power2100 = p2.GetActorsByType("2100power")
				scsupplydepot = p2.GetActorsByType("scsupplydepot")
				cityboti = p2.GetActorsByType("cityboti")
				cityc = p2.GetActorsByType("cityc")
				citybotr = p2.GetActorsByType("citybotr")
				citybotnuke = p2.GetActorsByType("citybotnuke")
				citybotcoal = p2.GetActorsByType("citybotcoal")
				citymayor = p2.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p3 and p5.IsAlliedWith(p3) then
				nuk2 = p3.GetActorsByType("nuk2")
				nuke = p3.GetActorsByType("nuke")
				apwr = p3.GetActorsByType("apwr")
				powr = p3.GetActorsByType("powr")
				power2100 = p3.GetActorsByType("2100power")
				scsupplydepot = p3.GetActorsByType("scsupplydepot")
				cityboti = p3.GetActorsByType("cityboti")
				cityc = p3.GetActorsByType("cityc")
				citybotr = p3.GetActorsByType("citybotr")
				citybotnuke = p3.GetActorsByType("citybotnuke")
				citybotcoal = p3.GetActorsByType("citybotcoal")
				citymayor = p3.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p4 and p5.IsAlliedWith(p4) then
				nuk2 = p4.GetActorsByType("nuk2")
				nuke = p4.GetActorsByType("nuke")
				apwr = p4.GetActorsByType("apwr")
				powr = p4.GetActorsByType("powr")
				power2100 = p4.GetActorsByType("2100power")
				scsupplydepot = p4.GetActorsByType("scsupplydepot")
				cityboti = p4.GetActorsByType("cityboti")
				cityc = p4.GetActorsByType("cityc")
				citybotr = p4.GetActorsByType("citybotr")
				citybotnuke = p4.GetActorsByType("citybotnuke")
				citybotcoal = p4.GetActorsByType("citybotcoal")
				citymayor = p4.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p5building()
	end)
end
p6building = function()
	if p6 and p6.IsBot then
		citybotelitearchitect = p6.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p6.GetActorsByType("cityboti")
				citycown = p6.GetActorsByType("cityc")
				citybotrown = p6.GetActorsByType("citybotr")
				citybotnukeown = p6.GetActorsByType("citybotnuke")
				citybotcoalown = p6.GetActorsByType("citybotcoal")
				citymayorown = p6.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p7 and p6.IsAlliedWith(p7) then
				nuk2 = p7.GetActorsByType("nuk2")
				nuke = p7.GetActorsByType("nuke")
				apwr = p7.GetActorsByType("apwr")
				powr = p7.GetActorsByType("powr")
				power2100 = p7.GetActorsByType("2100power")
				scsupplydepot = p7.GetActorsByType("scsupplydepot")
				cityboti = p7.GetActorsByType("cityboti")
				cityc = p7.GetActorsByType("cityc")
				citybotr = p7.GetActorsByType("citybotr")
				citybotnuke = p7.GetActorsByType("citybotnuke")
				citybotcoal = p7.GetActorsByType("citybotcoal")
				citymayor = p7.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p8 and p6.IsAlliedWith(p8) then
				nuk2 = p8.GetActorsByType("nuk2")
				nuke = p8.GetActorsByType("nuke")
				apwr = p8.GetActorsByType("apwr")
				powr = p8.GetActorsByType("powr")
				power2100 = p8.GetActorsByType("2100power")
				scsupplydepot = p8.GetActorsByType("scsupplydepot")
				cityboti = p8.GetActorsByType("cityboti")
				cityc = p8.GetActorsByType("cityc")
				citybotr = p8.GetActorsByType("citybotr")
				citybotnuke = p8.GetActorsByType("citybotnuke")
				citybotcoal = p8.GetActorsByType("citybotcoal")
				citymayor = p8.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p9 and p6.IsAlliedWith(p9) then
				nuk2 = p9.GetActorsByType("nuk2")
				nuke = p9.GetActorsByType("nuke")
				apwr = p9.GetActorsByType("apwr")
				powr = p9.GetActorsByType("powr")
				power2100 = p9.GetActorsByType("2100power")
				scsupplydepot = p9.GetActorsByType("scsupplydepot")
				cityboti = p9.GetActorsByType("cityboti")
				cityc = p9.GetActorsByType("cityc")
				citybotr = p9.GetActorsByType("citybotr")
				citybotnuke = p9.GetActorsByType("citybotnuke")
				citybotcoal = p9.GetActorsByType("citybotcoal")
				citymayor = p9.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p10 and p6.IsAlliedWith(p10) then
				nuk2 = p10.GetActorsByType("nuk2")
				nuke = p10.GetActorsByType("nuke")
				apwr = p10.GetActorsByType("apwr")
				powr = p10.GetActorsByType("powr")
				power2100 = p10.GetActorsByType("2100power")
				scsupplydepot = p10.GetActorsByType("scsupplydepot")
				cityboti = p10.GetActorsByType("cityboti")
				cityc = p10.GetActorsByType("cityc")
				citybotr = p10.GetActorsByType("citybotr")
				citybotnuke = p10.GetActorsByType("citybotnuke")
				citybotcoal = p10.GetActorsByType("citybotcoal")
				citymayor = p10.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p11 and p6.IsAlliedWith(p11) then
				nuk2 = p11.GetActorsByType("nuk2")
				nuke = p11.GetActorsByType("nuke")
				apwr = p11.GetActorsByType("apwr")
				powr = p11.GetActorsByType("powr")
				power2100 = p11.GetActorsByType("2100power")
				scsupplydepot = p11.GetActorsByType("scsupplydepot")
				cityboti = p11.GetActorsByType("cityboti")
				cityc = p11.GetActorsByType("cityc")
				citybotr = p11.GetActorsByType("citybotr")
				citybotnuke = p11.GetActorsByType("citybotnuke")
				citybotcoal = p11.GetActorsByType("citybotcoal")
				citymayor = p11.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p12 and p6.IsAlliedWith(p12) then
				nuk2 = p12.GetActorsByType("nuk2")
				nuke = p12.GetActorsByType("nuke")
				apwr = p12.GetActorsByType("apwr")
				powr = p12.GetActorsByType("powr")
				power2100 = p12.GetActorsByType("2100power")
				scsupplydepot = p12.GetActorsByType("scsupplydepot")
				cityboti = p12.GetActorsByType("cityboti")
				cityc = p12.GetActorsByType("cityc")
				citybotr = p12.GetActorsByType("citybotr")
				citybotnuke = p12.GetActorsByType("citybotnuke")
				citybotcoal = p12.GetActorsByType("citybotcoal")
				citymayor = p12.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p13 and p6.IsAlliedWith(p13) then
				nuk2 = p13.GetActorsByType("nuk2")
				nuke = p13.GetActorsByType("nuke")
				apwr = p13.GetActorsByType("apwr")
				powr = p13.GetActorsByType("powr")
				power2100 = p13.GetActorsByType("2100power")
				scsupplydepot = p13.GetActorsByType("scsupplydepot")
				cityboti = p13.GetActorsByType("cityboti")
				cityc = p13.GetActorsByType("cityc")
				citybotr = p13.GetActorsByType("citybotr")
				citybotnuke = p13.GetActorsByType("citybotnuke")
				citybotcoal = p13.GetActorsByType("citybotcoal")
				citymayor = p13.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p14 and p6.IsAlliedWith(p14) then
				nuk2 = p14.GetActorsByType("nuk2")
				nuke = p14.GetActorsByType("nuke")
				apwr = p14.GetActorsByType("apwr")
				powr = p14.GetActorsByType("powr")
				power2100 = p14.GetActorsByType("2100power")
				scsupplydepot = p14.GetActorsByType("scsupplydepot")
				cityboti = p14.GetActorsByType("cityboti")
				cityc = p14.GetActorsByType("cityc")
				citybotr = p14.GetActorsByType("citybotr")
				citybotnuke = p14.GetActorsByType("citybotnuke")
				citybotcoal = p14.GetActorsByType("citybotcoal")
				citymayor = p14.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p15 and p6.IsAlliedWith(p15) then
				nuk2 = p15.GetActorsByType("nuk2")
				nuke = p15.GetActorsByType("nuke")
				apwr = p15.GetActorsByType("apwr")
				powr = p15.GetActorsByType("powr")
				power2100 = p15.GetActorsByType("2100power")
				scsupplydepot = p15.GetActorsByType("scsupplydepot")
				cityboti = p15.GetActorsByType("cityboti")
				cityc = p15.GetActorsByType("cityc")
				citybotr = p15.GetActorsByType("citybotr")
				citybotnuke = p15.GetActorsByType("citybotnuke")
				citybotcoal = p15.GetActorsByType("citybotcoal")
				citymayor = p15.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p16 and p6.IsAlliedWith(p16) then
				nuk2 = p16.GetActorsByType("nuk2")
				nuke = p16.GetActorsByType("nuke")
				apwr = p16.GetActorsByType("apwr")
				powr = p16.GetActorsByType("powr")
				power2100 = p16.GetActorsByType("2100power")
				scsupplydepot = p16.GetActorsByType("scsupplydepot")
				cityboti = p16.GetActorsByType("cityboti")
				cityc = p16.GetActorsByType("cityc")
				citybotr = p16.GetActorsByType("citybotr")
				citybotnuke = p16.GetActorsByType("citybotnuke")
				citybotcoal = p16.GetActorsByType("citybotcoal")
				citymayor = p16.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p1 and p6.IsAlliedWith(p1) then
				nuk2 = p1.GetActorsByType("nuk2")
				nuke = p1.GetActorsByType("nuke")
				apwr = p1.GetActorsByType("apwr")
				powr = p1.GetActorsByType("powr")
				power2100 = p1.GetActorsByType("2100power")
				scsupplydepot = p1.GetActorsByType("scsupplydepot")
				cityboti = p1.GetActorsByType("cityboti")
				cityc = p1.GetActorsByType("cityc")
				citybotr = p1.GetActorsByType("citybotr")
				citybotnuke = p1.GetActorsByType("citybotnuke")
				citybotcoal = p1.GetActorsByType("citybotcoal")
				citymayor = p1.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p2 and p6.IsAlliedWith(p2) then
				nuk2 = p2.GetActorsByType("nuk2")
				nuke = p2.GetActorsByType("nuke")
				apwr = p2.GetActorsByType("apwr")
				powr = p2.GetActorsByType("powr")
				power2100 = p2.GetActorsByType("2100power")
				scsupplydepot = p2.GetActorsByType("scsupplydepot")
				cityboti = p2.GetActorsByType("cityboti")
				cityc = p2.GetActorsByType("cityc")
				citybotr = p2.GetActorsByType("citybotr")
				citybotnuke = p2.GetActorsByType("citybotnuke")
				citybotcoal = p2.GetActorsByType("citybotcoal")
				citymayor = p2.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p3 and p6.IsAlliedWith(p3) then
				nuk2 = p3.GetActorsByType("nuk2")
				nuke = p3.GetActorsByType("nuke")
				apwr = p3.GetActorsByType("apwr")
				powr = p3.GetActorsByType("powr")
				power2100 = p3.GetActorsByType("2100power")
				scsupplydepot = p3.GetActorsByType("scsupplydepot")
				cityboti = p3.GetActorsByType("cityboti")
				cityc = p3.GetActorsByType("cityc")
				citybotr = p3.GetActorsByType("citybotr")
				citybotnuke = p3.GetActorsByType("citybotnuke")
				citybotcoal = p3.GetActorsByType("citybotcoal")
				citymayor = p3.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p4 and p6.IsAlliedWith(p4) then
				nuk2 = p4.GetActorsByType("nuk2")
				nuke = p4.GetActorsByType("nuke")
				apwr = p4.GetActorsByType("apwr")
				powr = p4.GetActorsByType("powr")
				power2100 = p4.GetActorsByType("2100power")
				scsupplydepot = p4.GetActorsByType("scsupplydepot")
				cityboti = p4.GetActorsByType("cityboti")
				cityc = p4.GetActorsByType("cityc")
				citybotr = p4.GetActorsByType("citybotr")
				citybotnuke = p4.GetActorsByType("citybotnuke")
				citybotcoal = p4.GetActorsByType("citybotcoal")
				citymayor = p4.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p5 and p6.IsAlliedWith(p5) then
				nuk2 = p5.GetActorsByType("nuk2")
				nuke = p5.GetActorsByType("nuke")
				apwr = p5.GetActorsByType("apwr")
				powr = p5.GetActorsByType("powr")
				power2100 = p5.GetActorsByType("2100power")
				scsupplydepot = p5.GetActorsByType("scsupplydepot")
				cityboti = p5.GetActorsByType("cityboti")
				cityc = p5.GetActorsByType("cityc")
				citybotr = p5.GetActorsByType("citybotr")
				citybotnuke = p5.GetActorsByType("citybotnuke")
				citybotcoal = p5.GetActorsByType("citybotcoal")
				citymayor = p5.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p6building()
	end)
end
p7building = function()
	if p7 and p7.IsBot then
		citybotelitearchitect = p7.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p7.GetActorsByType("cityboti")
				citycown = p7.GetActorsByType("cityc")
				citybotrown = p7.GetActorsByType("citybotr")
				citybotnukeown = p7.GetActorsByType("citybotnuke")
				citybotcoalown = p7.GetActorsByType("citybotcoal")
				citymayorown = p7.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p8 and p7.IsAlliedWith(p8) then
				nuk2 = p8.GetActorsByType("nuk2")
				nuke = p8.GetActorsByType("nuke")
				apwr = p8.GetActorsByType("apwr")
				powr = p8.GetActorsByType("powr")
				power2100 = p8.GetActorsByType("2100power")
				scsupplydepot = p8.GetActorsByType("scsupplydepot")
				cityboti = p8.GetActorsByType("cityboti")
				cityc = p8.GetActorsByType("cityc")
				citybotr = p8.GetActorsByType("citybotr")
				citybotnuke = p8.GetActorsByType("citybotnuke")
				citybotcoal = p8.GetActorsByType("citybotcoal")
				citymayor = p8.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p9 and p7.IsAlliedWith(p9) then
				nuk2 = p9.GetActorsByType("nuk2")
				nuke = p9.GetActorsByType("nuke")
				apwr = p9.GetActorsByType("apwr")
				powr = p9.GetActorsByType("powr")
				power2100 = p9.GetActorsByType("2100power")
				scsupplydepot = p9.GetActorsByType("scsupplydepot")
				cityboti = p9.GetActorsByType("cityboti")
				cityc = p9.GetActorsByType("cityc")
				citybotr = p9.GetActorsByType("citybotr")
				citybotnuke = p9.GetActorsByType("citybotnuke")
				citybotcoal = p9.GetActorsByType("citybotcoal")
				citymayor = p9.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p10 and p7.IsAlliedWith(p10) then
				nuk2 = p10.GetActorsByType("nuk2")
				nuke = p10.GetActorsByType("nuke")
				apwr = p10.GetActorsByType("apwr")
				powr = p10.GetActorsByType("powr")
				power2100 = p10.GetActorsByType("2100power")
				scsupplydepot = p10.GetActorsByType("scsupplydepot")
				cityboti = p10.GetActorsByType("cityboti")
				cityc = p10.GetActorsByType("cityc")
				citybotr = p10.GetActorsByType("citybotr")
				citybotnuke = p10.GetActorsByType("citybotnuke")
				citybotcoal = p10.GetActorsByType("citybotcoal")
				citymayor = p10.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p11 and p7.IsAlliedWith(p11) then
				nuk2 = p11.GetActorsByType("nuk2")
				nuke = p11.GetActorsByType("nuke")
				apwr = p11.GetActorsByType("apwr")
				powr = p11.GetActorsByType("powr")
				power2100 = p11.GetActorsByType("2100power")
				scsupplydepot = p11.GetActorsByType("scsupplydepot")
				cityboti = p11.GetActorsByType("cityboti")
				cityc = p11.GetActorsByType("cityc")
				citybotr = p11.GetActorsByType("citybotr")
				citybotnuke = p11.GetActorsByType("citybotnuke")
				citybotcoal = p11.GetActorsByType("citybotcoal")
				citymayor = p11.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p12 and p7.IsAlliedWith(p12) then
				nuk2 = p12.GetActorsByType("nuk2")
				nuke = p12.GetActorsByType("nuke")
				apwr = p12.GetActorsByType("apwr")
				powr = p12.GetActorsByType("powr")
				power2100 = p12.GetActorsByType("2100power")
				scsupplydepot = p12.GetActorsByType("scsupplydepot")
				cityboti = p12.GetActorsByType("cityboti")
				cityc = p12.GetActorsByType("cityc")
				citybotr = p12.GetActorsByType("citybotr")
				citybotnuke = p12.GetActorsByType("citybotnuke")
				citybotcoal = p12.GetActorsByType("citybotcoal")
				citymayor = p12.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p13 and p7.IsAlliedWith(p13) then
				nuk2 = p13.GetActorsByType("nuk2")
				nuke = p13.GetActorsByType("nuke")
				apwr = p13.GetActorsByType("apwr")
				powr = p13.GetActorsByType("powr")
				power2100 = p13.GetActorsByType("2100power")
				scsupplydepot = p13.GetActorsByType("scsupplydepot")
				cityboti = p13.GetActorsByType("cityboti")
				cityc = p13.GetActorsByType("cityc")
				citybotr = p13.GetActorsByType("citybotr")
				citybotnuke = p13.GetActorsByType("citybotnuke")
				citybotcoal = p13.GetActorsByType("citybotcoal")
				citymayor = p13.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p14 and p7.IsAlliedWith(p14) then
				nuk2 = p14.GetActorsByType("nuk2")
				nuke = p14.GetActorsByType("nuke")
				apwr = p14.GetActorsByType("apwr")
				powr = p14.GetActorsByType("powr")
				power2100 = p14.GetActorsByType("2100power")
				scsupplydepot = p14.GetActorsByType("scsupplydepot")
				cityboti = p14.GetActorsByType("cityboti")
				cityc = p14.GetActorsByType("cityc")
				citybotr = p14.GetActorsByType("citybotr")
				citybotnuke = p14.GetActorsByType("citybotnuke")
				citybotcoal = p14.GetActorsByType("citybotcoal")
				citymayor = p14.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p15 and p7.IsAlliedWith(p15) then
				nuk2 = p15.GetActorsByType("nuk2")
				nuke = p15.GetActorsByType("nuke")
				apwr = p15.GetActorsByType("apwr")
				powr = p15.GetActorsByType("powr")
				power2100 = p15.GetActorsByType("2100power")
				scsupplydepot = p15.GetActorsByType("scsupplydepot")
				cityboti = p15.GetActorsByType("cityboti")
				cityc = p15.GetActorsByType("cityc")
				citybotr = p15.GetActorsByType("citybotr")
				citybotnuke = p15.GetActorsByType("citybotnuke")
				citybotcoal = p15.GetActorsByType("citybotcoal")
				citymayor = p15.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p16 and p7.IsAlliedWith(p16) then
				nuk2 = p16.GetActorsByType("nuk2")
				nuke = p16.GetActorsByType("nuke")
				apwr = p16.GetActorsByType("apwr")
				powr = p16.GetActorsByType("powr")
				power2100 = p16.GetActorsByType("2100power")
				scsupplydepot = p16.GetActorsByType("scsupplydepot")
				cityboti = p16.GetActorsByType("cityboti")
				cityc = p16.GetActorsByType("cityc")
				citybotr = p16.GetActorsByType("citybotr")
				citybotnuke = p16.GetActorsByType("citybotnuke")
				citybotcoal = p16.GetActorsByType("citybotcoal")
				citymayor = p16.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p1 and p7.IsAlliedWith(p1) then
				nuk2 = p1.GetActorsByType("nuk2")
				nuke = p1.GetActorsByType("nuke")
				apwr = p1.GetActorsByType("apwr")
				powr = p1.GetActorsByType("powr")
				power2100 = p1.GetActorsByType("2100power")
				scsupplydepot = p1.GetActorsByType("scsupplydepot")
				cityboti = p1.GetActorsByType("cityboti")
				cityc = p1.GetActorsByType("cityc")
				citybotr = p1.GetActorsByType("citybotr")
				citybotnuke = p1.GetActorsByType("citybotnuke")
				citybotcoal = p1.GetActorsByType("citybotcoal")
				citymayor = p1.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p2 and p7.IsAlliedWith(p2) then
				nuk2 = p2.GetActorsByType("nuk2")
				nuke = p2.GetActorsByType("nuke")
				apwr = p2.GetActorsByType("apwr")
				powr = p2.GetActorsByType("powr")
				power2100 = p2.GetActorsByType("2100power")
				scsupplydepot = p2.GetActorsByType("scsupplydepot")
				cityboti = p2.GetActorsByType("cityboti")
				cityc = p2.GetActorsByType("cityc")
				citybotr = p2.GetActorsByType("citybotr")
				citybotnuke = p2.GetActorsByType("citybotnuke")
				citybotcoal = p2.GetActorsByType("citybotcoal")
				citymayor = p2.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p3 and p7.IsAlliedWith(p3) then
				nuk2 = p3.GetActorsByType("nuk2")
				nuke = p3.GetActorsByType("nuke")
				apwr = p3.GetActorsByType("apwr")
				powr = p3.GetActorsByType("powr")
				power2100 = p3.GetActorsByType("2100power")
				scsupplydepot = p3.GetActorsByType("scsupplydepot")
				cityboti = p3.GetActorsByType("cityboti")
				cityc = p3.GetActorsByType("cityc")
				citybotr = p3.GetActorsByType("citybotr")
				citybotnuke = p3.GetActorsByType("citybotnuke")
				citybotcoal = p3.GetActorsByType("citybotcoal")
				citymayor = p3.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p4 and p7.IsAlliedWith(p4) then
				nuk2 = p4.GetActorsByType("nuk2")
				nuke = p4.GetActorsByType("nuke")
				apwr = p4.GetActorsByType("apwr")
				powr = p4.GetActorsByType("powr")
				power2100 = p4.GetActorsByType("2100power")
				scsupplydepot = p4.GetActorsByType("scsupplydepot")
				cityboti = p4.GetActorsByType("cityboti")
				cityc = p4.GetActorsByType("cityc")
				citybotr = p4.GetActorsByType("citybotr")
				citybotnuke = p4.GetActorsByType("citybotnuke")
				citybotcoal = p4.GetActorsByType("citybotcoal")
				citymayor = p4.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p5 and p7.IsAlliedWith(p5) then
				nuk2 = p5.GetActorsByType("nuk2")
				nuke = p5.GetActorsByType("nuke")
				apwr = p5.GetActorsByType("apwr")
				powr = p5.GetActorsByType("powr")
				power2100 = p5.GetActorsByType("2100power")
				scsupplydepot = p5.GetActorsByType("scsupplydepot")
				cityboti = p5.GetActorsByType("cityboti")
				cityc = p5.GetActorsByType("cityc")
				citybotr = p5.GetActorsByType("citybotr")
				citybotnuke = p5.GetActorsByType("citybotnuke")
				citybotcoal = p5.GetActorsByType("citybotcoal")
				citymayor = p5.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p6 and p7.IsAlliedWith(p6) then
				nuk2 = p6.GetActorsByType("nuk2")
				nuke = p6.GetActorsByType("nuke")
				apwr = p6.GetActorsByType("apwr")
				powr = p6.GetActorsByType("powr")
				power2100 = p6.GetActorsByType("2100power")
				scsupplydepot = p6.GetActorsByType("scsupplydepot")
				cityboti = p6.GetActorsByType("cityboti")
				cityc = p6.GetActorsByType("cityc")
				citybotr = p6.GetActorsByType("citybotr")
				citybotnuke = p6.GetActorsByType("citybotnuke")
				citybotcoal = p6.GetActorsByType("citybotcoal")
				citymayor = p6.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p7building()
	end)
end
p8building = function()
	if p8 and p8.IsBot then
		citybotelitearchitect = p8.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p8.GetActorsByType("cityboti")
				citycown = p8.GetActorsByType("cityc")
				citybotrown = p8.GetActorsByType("citybotr")
				citybotnukeown = p8.GetActorsByType("citybotnuke")
				citybotcoalown = p8.GetActorsByType("citybotcoal")
				citymayorown = p8.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p9 and p8.IsAlliedWith(p9) then
				nuk2 = p9.GetActorsByType("nuk2")
				nuke = p9.GetActorsByType("nuke")
				apwr = p9.GetActorsByType("apwr")
				powr = p9.GetActorsByType("powr")
				power2100 = p9.GetActorsByType("2100power")
				scsupplydepot = p9.GetActorsByType("scsupplydepot")
				cityboti = p9.GetActorsByType("cityboti")
				cityc = p9.GetActorsByType("cityc")
				citybotr = p9.GetActorsByType("citybotr")
				citybotnuke = p9.GetActorsByType("citybotnuke")
				citybotcoal = p9.GetActorsByType("citybotcoal")
				citymayor = p9.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p10 and p8.IsAlliedWith(p10) then
				nuk2 = p10.GetActorsByType("nuk2")
				nuke = p10.GetActorsByType("nuke")
				apwr = p10.GetActorsByType("apwr")
				powr = p10.GetActorsByType("powr")
				power2100 = p10.GetActorsByType("2100power")
				scsupplydepot = p10.GetActorsByType("scsupplydepot")
				cityboti = p10.GetActorsByType("cityboti")
				cityc = p10.GetActorsByType("cityc")
				citybotr = p10.GetActorsByType("citybotr")
				citybotnuke = p10.GetActorsByType("citybotnuke")
				citybotcoal = p10.GetActorsByType("citybotcoal")
				citymayor = p10.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p11 and p8.IsAlliedWith(p11) then
				nuk2 = p11.GetActorsByType("nuk2")
				nuke = p11.GetActorsByType("nuke")
				apwr = p11.GetActorsByType("apwr")
				powr = p11.GetActorsByType("powr")
				power2100 = p11.GetActorsByType("2100power")
				scsupplydepot = p11.GetActorsByType("scsupplydepot")
				cityboti = p11.GetActorsByType("cityboti")
				cityc = p11.GetActorsByType("cityc")
				citybotr = p11.GetActorsByType("citybotr")
				citybotnuke = p11.GetActorsByType("citybotnuke")
				citybotcoal = p11.GetActorsByType("citybotcoal")
				citymayor = p11.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p12 and p8.IsAlliedWith(p12) then
				nuk2 = p12.GetActorsByType("nuk2")
				nuke = p12.GetActorsByType("nuke")
				apwr = p12.GetActorsByType("apwr")
				powr = p12.GetActorsByType("powr")
				power2100 = p12.GetActorsByType("2100power")
				scsupplydepot = p12.GetActorsByType("scsupplydepot")
				cityboti = p12.GetActorsByType("cityboti")
				cityc = p12.GetActorsByType("cityc")
				citybotr = p12.GetActorsByType("citybotr")
				citybotnuke = p12.GetActorsByType("citybotnuke")
				citybotcoal = p12.GetActorsByType("citybotcoal")
				citymayor = p12.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p13 and p8.IsAlliedWith(p13) then
				nuk2 = p13.GetActorsByType("nuk2")
				nuke = p13.GetActorsByType("nuke")
				apwr = p13.GetActorsByType("apwr")
				powr = p13.GetActorsByType("powr")
				power2100 = p13.GetActorsByType("2100power")
				scsupplydepot = p13.GetActorsByType("scsupplydepot")
				cityboti = p13.GetActorsByType("cityboti")
				cityc = p13.GetActorsByType("cityc")
				citybotr = p13.GetActorsByType("citybotr")
				citybotnuke = p13.GetActorsByType("citybotnuke")
				citybotcoal = p13.GetActorsByType("citybotcoal")
				citymayor = p13.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p14 and p8.IsAlliedWith(p14) then
				nuk2 = p14.GetActorsByType("nuk2")
				nuke = p14.GetActorsByType("nuke")
				apwr = p14.GetActorsByType("apwr")
				powr = p14.GetActorsByType("powr")
				power2100 = p14.GetActorsByType("2100power")
				scsupplydepot = p14.GetActorsByType("scsupplydepot")
				cityboti = p14.GetActorsByType("cityboti")
				cityc = p14.GetActorsByType("cityc")
				citybotr = p14.GetActorsByType("citybotr")
				citybotnuke = p14.GetActorsByType("citybotnuke")
				citybotcoal = p14.GetActorsByType("citybotcoal")
				citymayor = p14.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p15 and p8.IsAlliedWith(p15) then
				nuk2 = p15.GetActorsByType("nuk2")
				nuke = p15.GetActorsByType("nuke")
				apwr = p15.GetActorsByType("apwr")
				powr = p15.GetActorsByType("powr")
				power2100 = p15.GetActorsByType("2100power")
				scsupplydepot = p15.GetActorsByType("scsupplydepot")
				cityboti = p15.GetActorsByType("cityboti")
				cityc = p15.GetActorsByType("cityc")
				citybotr = p15.GetActorsByType("citybotr")
				citybotnuke = p15.GetActorsByType("citybotnuke")
				citybotcoal = p15.GetActorsByType("citybotcoal")
				citymayor = p15.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p16 and p8.IsAlliedWith(p16) then
				nuk2 = p16.GetActorsByType("nuk2")
				nuke = p16.GetActorsByType("nuke")
				apwr = p16.GetActorsByType("apwr")
				powr = p16.GetActorsByType("powr")
				power2100 = p16.GetActorsByType("2100power")
				scsupplydepot = p16.GetActorsByType("scsupplydepot")
				cityboti = p16.GetActorsByType("cityboti")
				cityc = p16.GetActorsByType("cityc")
				citybotr = p16.GetActorsByType("citybotr")
				citybotnuke = p16.GetActorsByType("citybotnuke")
				citybotcoal = p16.GetActorsByType("citybotcoal")
				citymayor = p16.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p1 and p8.IsAlliedWith(p1) then
				nuk2 = p1.GetActorsByType("nuk2")
				nuke = p1.GetActorsByType("nuke")
				apwr = p1.GetActorsByType("apwr")
				powr = p1.GetActorsByType("powr")
				power2100 = p1.GetActorsByType("2100power")
				scsupplydepot = p1.GetActorsByType("scsupplydepot")
				cityboti = p1.GetActorsByType("cityboti")
				cityc = p1.GetActorsByType("cityc")
				citybotr = p1.GetActorsByType("citybotr")
				citybotnuke = p1.GetActorsByType("citybotnuke")
				citybotcoal = p1.GetActorsByType("citybotcoal")
				citymayor = p1.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p2 and p8.IsAlliedWith(p2) then
				nuk2 = p2.GetActorsByType("nuk2")
				nuke = p2.GetActorsByType("nuke")
				apwr = p2.GetActorsByType("apwr")
				powr = p2.GetActorsByType("powr")
				power2100 = p2.GetActorsByType("2100power")
				scsupplydepot = p2.GetActorsByType("scsupplydepot")
				cityboti = p2.GetActorsByType("cityboti")
				cityc = p2.GetActorsByType("cityc")
				citybotr = p2.GetActorsByType("citybotr")
				citybotnuke = p2.GetActorsByType("citybotnuke")
				citybotcoal = p2.GetActorsByType("citybotcoal")
				citymayor = p2.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p3 and p8.IsAlliedWith(p3) then
				nuk2 = p3.GetActorsByType("nuk2")
				nuke = p3.GetActorsByType("nuke")
				apwr = p3.GetActorsByType("apwr")
				powr = p3.GetActorsByType("powr")
				power2100 = p3.GetActorsByType("2100power")
				scsupplydepot = p3.GetActorsByType("scsupplydepot")
				cityboti = p3.GetActorsByType("cityboti")
				cityc = p3.GetActorsByType("cityc")
				citybotr = p3.GetActorsByType("citybotr")
				citybotnuke = p3.GetActorsByType("citybotnuke")
				citybotcoal = p3.GetActorsByType("citybotcoal")
				citymayor = p3.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p4 and p8.IsAlliedWith(p4) then
				nuk2 = p4.GetActorsByType("nuk2")
				nuke = p4.GetActorsByType("nuke")
				apwr = p4.GetActorsByType("apwr")
				powr = p4.GetActorsByType("powr")
				power2100 = p4.GetActorsByType("2100power")
				scsupplydepot = p4.GetActorsByType("scsupplydepot")
				cityboti = p4.GetActorsByType("cityboti")
				cityc = p4.GetActorsByType("cityc")
				citybotr = p4.GetActorsByType("citybotr")
				citybotnuke = p4.GetActorsByType("citybotnuke")
				citybotcoal = p4.GetActorsByType("citybotcoal")
				citymayor = p4.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p5 and p8.IsAlliedWith(p5) then
				nuk2 = p5.GetActorsByType("nuk2")
				nuke = p5.GetActorsByType("nuke")
				apwr = p5.GetActorsByType("apwr")
				powr = p5.GetActorsByType("powr")
				power2100 = p5.GetActorsByType("2100power")
				scsupplydepot = p5.GetActorsByType("scsupplydepot")
				cityboti = p5.GetActorsByType("cityboti")
				cityc = p5.GetActorsByType("cityc")
				citybotr = p5.GetActorsByType("citybotr")
				citybotnuke = p5.GetActorsByType("citybotnuke")
				citybotcoal = p5.GetActorsByType("citybotcoal")
				citymayor = p5.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p6 and p8.IsAlliedWith(p6) then
				nuk2 = p6.GetActorsByType("nuk2")
				nuke = p6.GetActorsByType("nuke")
				apwr = p6.GetActorsByType("apwr")
				powr = p6.GetActorsByType("powr")
				power2100 = p6.GetActorsByType("2100power")
				scsupplydepot = p6.GetActorsByType("scsupplydepot")
				cityboti = p6.GetActorsByType("cityboti")
				cityc = p6.GetActorsByType("cityc")
				citybotr = p6.GetActorsByType("citybotr")
				citybotnuke = p6.GetActorsByType("citybotnuke")
				citybotcoal = p6.GetActorsByType("citybotcoal")
				citymayor = p6.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p7 and p8.IsAlliedWith(p7) then
				nuk2 = p7.GetActorsByType("nuk2")
				nuke = p7.GetActorsByType("nuke")
				apwr = p7.GetActorsByType("apwr")
				powr = p7.GetActorsByType("powr")
				power2100 = p7.GetActorsByType("2100power")
				scsupplydepot = p7.GetActorsByType("scsupplydepot")
				cityboti = p7.GetActorsByType("cityboti")
				cityc = p7.GetActorsByType("cityc")
				citybotr = p7.GetActorsByType("citybotr")
				citybotnuke = p7.GetActorsByType("citybotnuke")
				citybotcoal = p7.GetActorsByType("citybotcoal")
				citymayor = p7.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p8building()
	end)
end
p9building = function()
	if p9 and p9.IsBot then
		citybotelitearchitect = p9.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p9.GetActorsByType("cityboti")
				citycown = p9.GetActorsByType("cityc")
				citybotrown = p9.GetActorsByType("citybotr")
				citybotnukeown = p9.GetActorsByType("citybotnuke")
				citybotcoalown = p9.GetActorsByType("citybotcoal")
				citymayorown = p9.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p10 and p9.IsAlliedWith(p10) then
				nuk2 = p10.GetActorsByType("nuk2")
				nuke = p10.GetActorsByType("nuke")
				apwr = p10.GetActorsByType("apwr")
				powr = p10.GetActorsByType("powr")
				power2100 = p10.GetActorsByType("2100power")
				scsupplydepot = p10.GetActorsByType("scsupplydepot")
				cityboti = p10.GetActorsByType("cityboti")
				cityc = p10.GetActorsByType("cityc")
				citybotr = p10.GetActorsByType("citybotr")
				citybotnuke = p10.GetActorsByType("citybotnuke")
				citybotcoal = p10.GetActorsByType("citybotcoal")
				citymayor = p10.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p11 and p9.IsAlliedWith(p11) then
				nuk2 = p11.GetActorsByType("nuk2")
				nuke = p11.GetActorsByType("nuke")
				apwr = p11.GetActorsByType("apwr")
				powr = p11.GetActorsByType("powr")
				power2100 = p11.GetActorsByType("2100power")
				scsupplydepot = p11.GetActorsByType("scsupplydepot")
				cityboti = p11.GetActorsByType("cityboti")
				cityc = p11.GetActorsByType("cityc")
				citybotr = p11.GetActorsByType("citybotr")
				citybotnuke = p11.GetActorsByType("citybotnuke")
				citybotcoal = p11.GetActorsByType("citybotcoal")
				citymayor = p11.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p12 and p9.IsAlliedWith(p12) then
				nuk2 = p12.GetActorsByType("nuk2")
				nuke = p12.GetActorsByType("nuke")
				apwr = p12.GetActorsByType("apwr")
				powr = p12.GetActorsByType("powr")
				power2100 = p12.GetActorsByType("2100power")
				scsupplydepot = p12.GetActorsByType("scsupplydepot")
				cityboti = p12.GetActorsByType("cityboti")
				cityc = p12.GetActorsByType("cityc")
				citybotr = p12.GetActorsByType("citybotr")
				citybotnuke = p12.GetActorsByType("citybotnuke")
				citybotcoal = p12.GetActorsByType("citybotcoal")
				citymayor = p12.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p13 and p9.IsAlliedWith(p13) then
				nuk2 = p13.GetActorsByType("nuk2")
				nuke = p13.GetActorsByType("nuke")
				apwr = p13.GetActorsByType("apwr")
				powr = p13.GetActorsByType("powr")
				power2100 = p13.GetActorsByType("2100power")
				scsupplydepot = p13.GetActorsByType("scsupplydepot")
				cityboti = p13.GetActorsByType("cityboti")
				cityc = p13.GetActorsByType("cityc")
				citybotr = p13.GetActorsByType("citybotr")
				citybotnuke = p13.GetActorsByType("citybotnuke")
				citybotcoal = p13.GetActorsByType("citybotcoal")
				citymayor = p13.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p14 and p9.IsAlliedWith(p14) then
				nuk2 = p14.GetActorsByType("nuk2")
				nuke = p14.GetActorsByType("nuke")
				apwr = p14.GetActorsByType("apwr")
				powr = p14.GetActorsByType("powr")
				power2100 = p14.GetActorsByType("2100power")
				scsupplydepot = p14.GetActorsByType("scsupplydepot")
				cityboti = p14.GetActorsByType("cityboti")
				cityc = p14.GetActorsByType("cityc")
				citybotr = p14.GetActorsByType("citybotr")
				citybotnuke = p14.GetActorsByType("citybotnuke")
				citybotcoal = p14.GetActorsByType("citybotcoal")
				citymayor = p14.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p15 and p9.IsAlliedWith(p15) then
				nuk2 = p15.GetActorsByType("nuk2")
				nuke = p15.GetActorsByType("nuke")
				apwr = p15.GetActorsByType("apwr")
				powr = p15.GetActorsByType("powr")
				power2100 = p15.GetActorsByType("2100power")
				scsupplydepot = p15.GetActorsByType("scsupplydepot")
				cityboti = p15.GetActorsByType("cityboti")
				cityc = p15.GetActorsByType("cityc")
				citybotr = p15.GetActorsByType("citybotr")
				citybotnuke = p15.GetActorsByType("citybotnuke")
				citybotcoal = p15.GetActorsByType("citybotcoal")
				citymayor = p15.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p16 and p9.IsAlliedWith(p16) then
				nuk2 = p16.GetActorsByType("nuk2")
				nuke = p16.GetActorsByType("nuke")
				apwr = p16.GetActorsByType("apwr")
				powr = p16.GetActorsByType("powr")
				power2100 = p16.GetActorsByType("2100power")
				scsupplydepot = p16.GetActorsByType("scsupplydepot")
				cityboti = p16.GetActorsByType("cityboti")
				cityc = p16.GetActorsByType("cityc")
				citybotr = p16.GetActorsByType("citybotr")
				citybotnuke = p16.GetActorsByType("citybotnuke")
				citybotcoal = p16.GetActorsByType("citybotcoal")
				citymayor = p16.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p1 and p9.IsAlliedWith(p1) then
				nuk2 = p1.GetActorsByType("nuk2")
				nuke = p1.GetActorsByType("nuke")
				apwr = p1.GetActorsByType("apwr")
				powr = p1.GetActorsByType("powr")
				power2100 = p1.GetActorsByType("2100power")
				scsupplydepot = p1.GetActorsByType("scsupplydepot")
				cityboti = p1.GetActorsByType("cityboti")
				cityc = p1.GetActorsByType("cityc")
				citybotr = p1.GetActorsByType("citybotr")
				citybotnuke = p1.GetActorsByType("citybotnuke")
				citybotcoal = p1.GetActorsByType("citybotcoal")
				citymayor = p1.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p2 and p9.IsAlliedWith(p2) then
				nuk2 = p2.GetActorsByType("nuk2")
				nuke = p2.GetActorsByType("nuke")
				apwr = p2.GetActorsByType("apwr")
				powr = p2.GetActorsByType("powr")
				power2100 = p2.GetActorsByType("2100power")
				scsupplydepot = p2.GetActorsByType("scsupplydepot")
				cityboti = p2.GetActorsByType("cityboti")
				cityc = p2.GetActorsByType("cityc")
				citybotr = p2.GetActorsByType("citybotr")
				citybotnuke = p2.GetActorsByType("citybotnuke")
				citybotcoal = p2.GetActorsByType("citybotcoal")
				citymayor = p2.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p3 and p9.IsAlliedWith(p3) then
				nuk2 = p3.GetActorsByType("nuk2")
				nuke = p3.GetActorsByType("nuke")
				apwr = p3.GetActorsByType("apwr")
				powr = p3.GetActorsByType("powr")
				power2100 = p3.GetActorsByType("2100power")
				scsupplydepot = p3.GetActorsByType("scsupplydepot")
				cityboti = p3.GetActorsByType("cityboti")
				cityc = p3.GetActorsByType("cityc")
				citybotr = p3.GetActorsByType("citybotr")
				citybotnuke = p3.GetActorsByType("citybotnuke")
				citybotcoal = p3.GetActorsByType("citybotcoal")
				citymayor = p3.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p4 and p9.IsAlliedWith(p4) then
				nuk2 = p4.GetActorsByType("nuk2")
				nuke = p4.GetActorsByType("nuke")
				apwr = p4.GetActorsByType("apwr")
				powr = p4.GetActorsByType("powr")
				power2100 = p4.GetActorsByType("2100power")
				scsupplydepot = p4.GetActorsByType("scsupplydepot")
				cityboti = p4.GetActorsByType("cityboti")
				cityc = p4.GetActorsByType("cityc")
				citybotr = p4.GetActorsByType("citybotr")
				citybotnuke = p4.GetActorsByType("citybotnuke")
				citybotcoal = p4.GetActorsByType("citybotcoal")
				citymayor = p4.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p5 and p9.IsAlliedWith(p5) then
				nuk2 = p5.GetActorsByType("nuk2")
				nuke = p5.GetActorsByType("nuke")
				apwr = p5.GetActorsByType("apwr")
				powr = p5.GetActorsByType("powr")
				power2100 = p5.GetActorsByType("2100power")
				scsupplydepot = p5.GetActorsByType("scsupplydepot")
				cityboti = p5.GetActorsByType("cityboti")
				cityc = p5.GetActorsByType("cityc")
				citybotr = p5.GetActorsByType("citybotr")
				citybotnuke = p5.GetActorsByType("citybotnuke")
				citybotcoal = p5.GetActorsByType("citybotcoal")
				citymayor = p5.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p6 and p9.IsAlliedWith(p6) then
				nuk2 = p6.GetActorsByType("nuk2")
				nuke = p6.GetActorsByType("nuke")
				apwr = p6.GetActorsByType("apwr")
				powr = p6.GetActorsByType("powr")
				power2100 = p6.GetActorsByType("2100power")
				scsupplydepot = p6.GetActorsByType("scsupplydepot")
				cityboti = p6.GetActorsByType("cityboti")
				cityc = p6.GetActorsByType("cityc")
				citybotr = p6.GetActorsByType("citybotr")
				citybotnuke = p6.GetActorsByType("citybotnuke")
				citybotcoal = p6.GetActorsByType("citybotcoal")
				citymayor = p6.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p7 and p9.IsAlliedWith(p7) then
				nuk2 = p7.GetActorsByType("nuk2")
				nuke = p7.GetActorsByType("nuke")
				apwr = p7.GetActorsByType("apwr")
				powr = p7.GetActorsByType("powr")
				power2100 = p7.GetActorsByType("2100power")
				scsupplydepot = p7.GetActorsByType("scsupplydepot")
				cityboti = p7.GetActorsByType("cityboti")
				cityc = p7.GetActorsByType("cityc")
				citybotr = p7.GetActorsByType("citybotr")
				citybotnuke = p7.GetActorsByType("citybotnuke")
				citybotcoal = p7.GetActorsByType("citybotcoal")
				citymayor = p7.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p8 and p9.IsAlliedWith(p8) then
				nuk2 = p8.GetActorsByType("nuk2")
				nuke = p8.GetActorsByType("nuke")
				apwr = p8.GetActorsByType("apwr")
				powr = p8.GetActorsByType("powr")
				power2100 = p8.GetActorsByType("2100power")
				scsupplydepot = p8.GetActorsByType("scsupplydepot")
				cityboti = p8.GetActorsByType("cityboti")
				cityc = p8.GetActorsByType("cityc")
				citybotr = p8.GetActorsByType("citybotr")
				citybotnuke = p8.GetActorsByType("citybotnuke")
				citybotcoal = p8.GetActorsByType("citybotcoal")
				citymayor = p8.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p9building()
	end)
end
p10building = function()
	if p10 and p10.IsBot then
		citybotelitearchitect = p10.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p10.GetActorsByType("cityboti")
				citycown = p10.GetActorsByType("cityc")
				citybotrown = p10.GetActorsByType("citybotr")
				citybotnukeown = p10.GetActorsByType("citybotnuke")
				citybotcoalown = p10.GetActorsByType("citybotcoal")
				citymayorown = p10.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p11 and p10.IsAlliedWith(p11) then
				nuk2 = p11.GetActorsByType("nuk2")
				nuke = p11.GetActorsByType("nuke")
				apwr = p11.GetActorsByType("apwr")
				powr = p11.GetActorsByType("powr")
				power2100 = p11.GetActorsByType("2100power")
				scsupplydepot = p11.GetActorsByType("scsupplydepot")
				cityboti = p11.GetActorsByType("cityboti")
				cityc = p11.GetActorsByType("cityc")
				citybotr = p11.GetActorsByType("citybotr")
				citybotnuke = p11.GetActorsByType("citybotnuke")
				citybotcoal = p11.GetActorsByType("citybotcoal")
				citymayor = p11.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p12 and p10.IsAlliedWith(p12) then
				nuk2 = p12.GetActorsByType("nuk2")
				nuke = p12.GetActorsByType("nuke")
				apwr = p12.GetActorsByType("apwr")
				powr = p12.GetActorsByType("powr")
				power2100 = p12.GetActorsByType("2100power")
				scsupplydepot = p12.GetActorsByType("scsupplydepot")
				cityboti = p12.GetActorsByType("cityboti")
				cityc = p12.GetActorsByType("cityc")
				citybotr = p12.GetActorsByType("citybotr")
				citybotnuke = p12.GetActorsByType("citybotnuke")
				citybotcoal = p12.GetActorsByType("citybotcoal")
				citymayor = p12.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p13 and p10.IsAlliedWith(p13) then
				nuk2 = p13.GetActorsByType("nuk2")
				nuke = p13.GetActorsByType("nuke")
				apwr = p13.GetActorsByType("apwr")
				powr = p13.GetActorsByType("powr")
				power2100 = p13.GetActorsByType("2100power")
				scsupplydepot = p13.GetActorsByType("scsupplydepot")
				cityboti = p13.GetActorsByType("cityboti")
				cityc = p13.GetActorsByType("cityc")
				citybotr = p13.GetActorsByType("citybotr")
				citybotnuke = p13.GetActorsByType("citybotnuke")
				citybotcoal = p13.GetActorsByType("citybotcoal")
				citymayor = p13.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p14 and p10.IsAlliedWith(p14) then
				nuk2 = p14.GetActorsByType("nuk2")
				nuke = p14.GetActorsByType("nuke")
				apwr = p14.GetActorsByType("apwr")
				powr = p14.GetActorsByType("powr")
				power2100 = p14.GetActorsByType("2100power")
				scsupplydepot = p14.GetActorsByType("scsupplydepot")
				cityboti = p14.GetActorsByType("cityboti")
				cityc = p14.GetActorsByType("cityc")
				citybotr = p14.GetActorsByType("citybotr")
				citybotnuke = p14.GetActorsByType("citybotnuke")
				citybotcoal = p14.GetActorsByType("citybotcoal")
				citymayor = p14.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p15 and p10.IsAlliedWith(p15) then
				nuk2 = p15.GetActorsByType("nuk2")
				nuke = p15.GetActorsByType("nuke")
				apwr = p15.GetActorsByType("apwr")
				powr = p15.GetActorsByType("powr")
				power2100 = p15.GetActorsByType("2100power")
				scsupplydepot = p15.GetActorsByType("scsupplydepot")
				cityboti = p15.GetActorsByType("cityboti")
				cityc = p15.GetActorsByType("cityc")
				citybotr = p15.GetActorsByType("citybotr")
				citybotnuke = p15.GetActorsByType("citybotnuke")
				citybotcoal = p15.GetActorsByType("citybotcoal")
				citymayor = p15.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p16 and p10.IsAlliedWith(p16) then
				nuk2 = p16.GetActorsByType("nuk2")
				nuke = p16.GetActorsByType("nuke")
				apwr = p16.GetActorsByType("apwr")
				powr = p16.GetActorsByType("powr")
				power2100 = p16.GetActorsByType("2100power")
				scsupplydepot = p16.GetActorsByType("scsupplydepot")
				cityboti = p16.GetActorsByType("cityboti")
				cityc = p16.GetActorsByType("cityc")
				citybotr = p16.GetActorsByType("citybotr")
				citybotnuke = p16.GetActorsByType("citybotnuke")
				citybotcoal = p16.GetActorsByType("citybotcoal")
				citymayor = p16.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p1 and p10.IsAlliedWith(p1) then
				nuk2 = p1.GetActorsByType("nuk2")
				nuke = p1.GetActorsByType("nuke")
				apwr = p1.GetActorsByType("apwr")
				powr = p1.GetActorsByType("powr")
				power2100 = p1.GetActorsByType("2100power")
				scsupplydepot = p1.GetActorsByType("scsupplydepot")
				cityboti = p1.GetActorsByType("cityboti")
				cityc = p1.GetActorsByType("cityc")
				citybotr = p1.GetActorsByType("citybotr")
				citybotnuke = p1.GetActorsByType("citybotnuke")
				citybotcoal = p1.GetActorsByType("citybotcoal")
				citymayor = p1.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p2 and p10.IsAlliedWith(p2) then
				nuk2 = p2.GetActorsByType("nuk2")
				nuke = p2.GetActorsByType("nuke")
				apwr = p2.GetActorsByType("apwr")
				powr = p2.GetActorsByType("powr")
				power2100 = p2.GetActorsByType("2100power")
				scsupplydepot = p2.GetActorsByType("scsupplydepot")
				cityboti = p2.GetActorsByType("cityboti")
				cityc = p2.GetActorsByType("cityc")
				citybotr = p2.GetActorsByType("citybotr")
				citybotnuke = p2.GetActorsByType("citybotnuke")
				citybotcoal = p2.GetActorsByType("citybotcoal")
				citymayor = p2.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p3 and p10.IsAlliedWith(p3) then
				nuk2 = p3.GetActorsByType("nuk2")
				nuke = p3.GetActorsByType("nuke")
				apwr = p3.GetActorsByType("apwr")
				powr = p3.GetActorsByType("powr")
				power2100 = p3.GetActorsByType("2100power")
				scsupplydepot = p3.GetActorsByType("scsupplydepot")
				cityboti = p3.GetActorsByType("cityboti")
				cityc = p3.GetActorsByType("cityc")
				citybotr = p3.GetActorsByType("citybotr")
				citybotnuke = p3.GetActorsByType("citybotnuke")
				citybotcoal = p3.GetActorsByType("citybotcoal")
				citymayor = p3.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p4 and p10.IsAlliedWith(p4) then
				nuk2 = p4.GetActorsByType("nuk2")
				nuke = p4.GetActorsByType("nuke")
				apwr = p4.GetActorsByType("apwr")
				powr = p4.GetActorsByType("powr")
				power2100 = p4.GetActorsByType("2100power")
				scsupplydepot = p4.GetActorsByType("scsupplydepot")
				cityboti = p4.GetActorsByType("cityboti")
				cityc = p4.GetActorsByType("cityc")
				citybotr = p4.GetActorsByType("citybotr")
				citybotnuke = p4.GetActorsByType("citybotnuke")
				citybotcoal = p4.GetActorsByType("citybotcoal")
				citymayor = p4.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p5 and p10.IsAlliedWith(p5) then
				nuk2 = p5.GetActorsByType("nuk2")
				nuke = p5.GetActorsByType("nuke")
				apwr = p5.GetActorsByType("apwr")
				powr = p5.GetActorsByType("powr")
				power2100 = p5.GetActorsByType("2100power")
				scsupplydepot = p5.GetActorsByType("scsupplydepot")
				cityboti = p5.GetActorsByType("cityboti")
				cityc = p5.GetActorsByType("cityc")
				citybotr = p5.GetActorsByType("citybotr")
				citybotnuke = p5.GetActorsByType("citybotnuke")
				citybotcoal = p5.GetActorsByType("citybotcoal")
				citymayor = p5.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p6 and p10.IsAlliedWith(p6) then
				nuk2 = p6.GetActorsByType("nuk2")
				nuke = p6.GetActorsByType("nuke")
				apwr = p6.GetActorsByType("apwr")
				powr = p6.GetActorsByType("powr")
				power2100 = p6.GetActorsByType("2100power")
				scsupplydepot = p6.GetActorsByType("scsupplydepot")
				cityboti = p6.GetActorsByType("cityboti")
				cityc = p6.GetActorsByType("cityc")
				citybotr = p6.GetActorsByType("citybotr")
				citybotnuke = p6.GetActorsByType("citybotnuke")
				citybotcoal = p6.GetActorsByType("citybotcoal")
				citymayor = p6.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p7 and p10.IsAlliedWith(p7) then
				nuk2 = p7.GetActorsByType("nuk2")
				nuke = p7.GetActorsByType("nuke")
				apwr = p7.GetActorsByType("apwr")
				powr = p7.GetActorsByType("powr")
				power2100 = p7.GetActorsByType("2100power")
				scsupplydepot = p7.GetActorsByType("scsupplydepot")
				cityboti = p7.GetActorsByType("cityboti")
				cityc = p7.GetActorsByType("cityc")
				citybotr = p7.GetActorsByType("citybotr")
				citybotnuke = p7.GetActorsByType("citybotnuke")
				citybotcoal = p7.GetActorsByType("citybotcoal")
				citymayor = p7.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p8 and p10.IsAlliedWith(p8) then
				nuk2 = p8.GetActorsByType("nuk2")
				nuke = p8.GetActorsByType("nuke")
				apwr = p8.GetActorsByType("apwr")
				powr = p8.GetActorsByType("powr")
				power2100 = p8.GetActorsByType("2100power")
				scsupplydepot = p8.GetActorsByType("scsupplydepot")
				cityboti = p8.GetActorsByType("cityboti")
				cityc = p8.GetActorsByType("cityc")
				citybotr = p8.GetActorsByType("citybotr")
				citybotnuke = p8.GetActorsByType("citybotnuke")
				citybotcoal = p8.GetActorsByType("citybotcoal")
				citymayor = p8.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p9 and p10.IsAlliedWith(p9) then
				nuk2 = p9.GetActorsByType("nuk2")
				nuke = p9.GetActorsByType("nuke")
				apwr = p9.GetActorsByType("apwr")
				powr = p9.GetActorsByType("powr")
				power2100 = p9.GetActorsByType("2100power")
				scsupplydepot = p9.GetActorsByType("scsupplydepot")
				cityboti = p9.GetActorsByType("cityboti")
				cityc = p9.GetActorsByType("cityc")
				citybotr = p9.GetActorsByType("citybotr")
				citybotnuke = p9.GetActorsByType("citybotnuke")
				citybotcoal = p9.GetActorsByType("citybotcoal")
				citymayor = p9.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p10building()
	end)
end
p11building = function()
	if p11 and p11.IsBot then
		citybotelitearchitect = p11.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p11.GetActorsByType("cityboti")
				citycown = p11.GetActorsByType("cityc")
				citybotrown = p11.GetActorsByType("citybotr")
				citybotnukeown = p11.GetActorsByType("citybotnuke")
				citybotcoalown = p11.GetActorsByType("citybotcoal")
				citymayorown = p11.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p12 and p11.IsAlliedWith(p12) then
				nuk2 = p12.GetActorsByType("nuk2")
				nuke = p12.GetActorsByType("nuke")
				apwr = p12.GetActorsByType("apwr")
				powr = p12.GetActorsByType("powr")
				power2100 = p12.GetActorsByType("2100power")
				scsupplydepot = p12.GetActorsByType("scsupplydepot")
				cityboti = p12.GetActorsByType("cityboti")
				cityc = p12.GetActorsByType("cityc")
				citybotr = p12.GetActorsByType("citybotr")
				citybotnuke = p12.GetActorsByType("citybotnuke")
				citybotcoal = p12.GetActorsByType("citybotcoal")
				citymayor = p12.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p13 and p11.IsAlliedWith(p13) then
				nuk2 = p13.GetActorsByType("nuk2")
				nuke = p13.GetActorsByType("nuke")
				apwr = p13.GetActorsByType("apwr")
				powr = p13.GetActorsByType("powr")
				power2100 = p13.GetActorsByType("2100power")
				scsupplydepot = p13.GetActorsByType("scsupplydepot")
				cityboti = p13.GetActorsByType("cityboti")
				cityc = p13.GetActorsByType("cityc")
				citybotr = p13.GetActorsByType("citybotr")
				citybotnuke = p13.GetActorsByType("citybotnuke")
				citybotcoal = p13.GetActorsByType("citybotcoal")
				citymayor = p13.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p14 and p11.IsAlliedWith(p14) then
				nuk2 = p14.GetActorsByType("nuk2")
				nuke = p14.GetActorsByType("nuke")
				apwr = p14.GetActorsByType("apwr")
				powr = p14.GetActorsByType("powr")
				power2100 = p14.GetActorsByType("2100power")
				scsupplydepot = p14.GetActorsByType("scsupplydepot")
				cityboti = p14.GetActorsByType("cityboti")
				cityc = p14.GetActorsByType("cityc")
				citybotr = p14.GetActorsByType("citybotr")
				citybotnuke = p14.GetActorsByType("citybotnuke")
				citybotcoal = p14.GetActorsByType("citybotcoal")
				citymayor = p14.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p15 and p11.IsAlliedWith(p15) then
				nuk2 = p15.GetActorsByType("nuk2")
				nuke = p15.GetActorsByType("nuke")
				apwr = p15.GetActorsByType("apwr")
				powr = p15.GetActorsByType("powr")
				power2100 = p15.GetActorsByType("2100power")
				scsupplydepot = p15.GetActorsByType("scsupplydepot")
				cityboti = p15.GetActorsByType("cityboti")
				cityc = p15.GetActorsByType("cityc")
				citybotr = p15.GetActorsByType("citybotr")
				citybotnuke = p15.GetActorsByType("citybotnuke")
				citybotcoal = p15.GetActorsByType("citybotcoal")
				citymayor = p15.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p16 and p11.IsAlliedWith(p16) then
				nuk2 = p16.GetActorsByType("nuk2")
				nuke = p16.GetActorsByType("nuke")
				apwr = p16.GetActorsByType("apwr")
				powr = p16.GetActorsByType("powr")
				power2100 = p16.GetActorsByType("2100power")
				scsupplydepot = p16.GetActorsByType("scsupplydepot")
				cityboti = p16.GetActorsByType("cityboti")
				cityc = p16.GetActorsByType("cityc")
				citybotr = p16.GetActorsByType("citybotr")
				citybotnuke = p16.GetActorsByType("citybotnuke")
				citybotcoal = p16.GetActorsByType("citybotcoal")
				citymayor = p16.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p1 and p11.IsAlliedWith(p1) then
				nuk2 = p1.GetActorsByType("nuk2")
				nuke = p1.GetActorsByType("nuke")
				apwr = p1.GetActorsByType("apwr")
				powr = p1.GetActorsByType("powr")
				power2100 = p1.GetActorsByType("2100power")
				scsupplydepot = p1.GetActorsByType("scsupplydepot")
				cityboti = p1.GetActorsByType("cityboti")
				cityc = p1.GetActorsByType("cityc")
				citybotr = p1.GetActorsByType("citybotr")
				citybotnuke = p1.GetActorsByType("citybotnuke")
				citybotcoal = p1.GetActorsByType("citybotcoal")
				citymayor = p1.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p2 and p11.IsAlliedWith(p2) then
				nuk2 = p2.GetActorsByType("nuk2")
				nuke = p2.GetActorsByType("nuke")
				apwr = p2.GetActorsByType("apwr")
				powr = p2.GetActorsByType("powr")
				power2100 = p2.GetActorsByType("2100power")
				scsupplydepot = p2.GetActorsByType("scsupplydepot")
				cityboti = p2.GetActorsByType("cityboti")
				cityc = p2.GetActorsByType("cityc")
				citybotr = p2.GetActorsByType("citybotr")
				citybotnuke = p2.GetActorsByType("citybotnuke")
				citybotcoal = p2.GetActorsByType("citybotcoal")
				citymayor = p2.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p3 and p11.IsAlliedWith(p3) then
				nuk2 = p3.GetActorsByType("nuk2")
				nuke = p3.GetActorsByType("nuke")
				apwr = p3.GetActorsByType("apwr")
				powr = p3.GetActorsByType("powr")
				power2100 = p3.GetActorsByType("2100power")
				scsupplydepot = p3.GetActorsByType("scsupplydepot")
				cityboti = p3.GetActorsByType("cityboti")
				cityc = p3.GetActorsByType("cityc")
				citybotr = p3.GetActorsByType("citybotr")
				citybotnuke = p3.GetActorsByType("citybotnuke")
				citybotcoal = p3.GetActorsByType("citybotcoal")
				citymayor = p3.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p4 and p11.IsAlliedWith(p4) then
				nuk2 = p4.GetActorsByType("nuk2")
				nuke = p4.GetActorsByType("nuke")
				apwr = p4.GetActorsByType("apwr")
				powr = p4.GetActorsByType("powr")
				power2100 = p4.GetActorsByType("2100power")
				scsupplydepot = p4.GetActorsByType("scsupplydepot")
				cityboti = p4.GetActorsByType("cityboti")
				cityc = p4.GetActorsByType("cityc")
				citybotr = p4.GetActorsByType("citybotr")
				citybotnuke = p4.GetActorsByType("citybotnuke")
				citybotcoal = p4.GetActorsByType("citybotcoal")
				citymayor = p4.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p5 and p11.IsAlliedWith(p5) then
				nuk2 = p5.GetActorsByType("nuk2")
				nuke = p5.GetActorsByType("nuke")
				apwr = p5.GetActorsByType("apwr")
				powr = p5.GetActorsByType("powr")
				power2100 = p5.GetActorsByType("2100power")
				scsupplydepot = p5.GetActorsByType("scsupplydepot")
				cityboti = p5.GetActorsByType("cityboti")
				cityc = p5.GetActorsByType("cityc")
				citybotr = p5.GetActorsByType("citybotr")
				citybotnuke = p5.GetActorsByType("citybotnuke")
				citybotcoal = p5.GetActorsByType("citybotcoal")
				citymayor = p5.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p6 and p11.IsAlliedWith(p6) then
				nuk2 = p6.GetActorsByType("nuk2")
				nuke = p6.GetActorsByType("nuke")
				apwr = p6.GetActorsByType("apwr")
				powr = p6.GetActorsByType("powr")
				power2100 = p6.GetActorsByType("2100power")
				scsupplydepot = p6.GetActorsByType("scsupplydepot")
				cityboti = p6.GetActorsByType("cityboti")
				cityc = p6.GetActorsByType("cityc")
				citybotr = p6.GetActorsByType("citybotr")
				citybotnuke = p6.GetActorsByType("citybotnuke")
				citybotcoal = p6.GetActorsByType("citybotcoal")
				citymayor = p6.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p7 and p11.IsAlliedWith(p7) then
				nuk2 = p7.GetActorsByType("nuk2")
				nuke = p7.GetActorsByType("nuke")
				apwr = p7.GetActorsByType("apwr")
				powr = p7.GetActorsByType("powr")
				power2100 = p7.GetActorsByType("2100power")
				scsupplydepot = p7.GetActorsByType("scsupplydepot")
				cityboti = p7.GetActorsByType("cityboti")
				cityc = p7.GetActorsByType("cityc")
				citybotr = p7.GetActorsByType("citybotr")
				citybotnuke = p7.GetActorsByType("citybotnuke")
				citybotcoal = p7.GetActorsByType("citybotcoal")
				citymayor = p7.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p8 and p11.IsAlliedWith(p8) then
				nuk2 = p8.GetActorsByType("nuk2")
				nuke = p8.GetActorsByType("nuke")
				apwr = p8.GetActorsByType("apwr")
				powr = p8.GetActorsByType("powr")
				power2100 = p8.GetActorsByType("2100power")
				scsupplydepot = p8.GetActorsByType("scsupplydepot")
				cityboti = p8.GetActorsByType("cityboti")
				cityc = p8.GetActorsByType("cityc")
				citybotr = p8.GetActorsByType("citybotr")
				citybotnuke = p8.GetActorsByType("citybotnuke")
				citybotcoal = p8.GetActorsByType("citybotcoal")
				citymayor = p8.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p9 and p11.IsAlliedWith(p9) then
				nuk2 = p9.GetActorsByType("nuk2")
				nuke = p9.GetActorsByType("nuke")
				apwr = p9.GetActorsByType("apwr")
				powr = p9.GetActorsByType("powr")
				power2100 = p9.GetActorsByType("2100power")
				scsupplydepot = p9.GetActorsByType("scsupplydepot")
				cityboti = p9.GetActorsByType("cityboti")
				cityc = p9.GetActorsByType("cityc")
				citybotr = p9.GetActorsByType("citybotr")
				citybotnuke = p9.GetActorsByType("citybotnuke")
				citybotcoal = p9.GetActorsByType("citybotcoal")
				citymayor = p9.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p10 and p11.IsAlliedWith(p10) then
				nuk2 = p10.GetActorsByType("nuk2")
				nuke = p10.GetActorsByType("nuke")
				apwr = p10.GetActorsByType("apwr")
				powr = p10.GetActorsByType("powr")
				power2100 = p10.GetActorsByType("2100power")
				scsupplydepot = p10.GetActorsByType("scsupplydepot")
				cityboti = p10.GetActorsByType("cityboti")
				cityc = p10.GetActorsByType("cityc")
				citybotr = p10.GetActorsByType("citybotr")
				citybotnuke = p10.GetActorsByType("citybotnuke")
				citybotcoal = p10.GetActorsByType("citybotcoal")
				citymayor = p10.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p11building()
	end)
end
p12building = function()
	if p12 and p12.IsBot then
		citybotelitearchitect = p12.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p12.GetActorsByType("cityboti")
				citycown = p12.GetActorsByType("cityc")
				citybotrown = p12.GetActorsByType("citybotr")
				citybotnukeown = p12.GetActorsByType("citybotnuke")
				citybotcoalown = p12.GetActorsByType("citybotcoal")
				citymayorown = p12.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p13 and p12.IsAlliedWith(p13) then
				nuk2 = p13.GetActorsByType("nuk2")
				nuke = p13.GetActorsByType("nuke")
				apwr = p13.GetActorsByType("apwr")
				powr = p13.GetActorsByType("powr")
				power2100 = p13.GetActorsByType("2100power")
				scsupplydepot = p13.GetActorsByType("scsupplydepot")
				cityboti = p13.GetActorsByType("cityboti")
				cityc = p13.GetActorsByType("cityc")
				citybotr = p13.GetActorsByType("citybotr")
				citybotnuke = p13.GetActorsByType("citybotnuke")
				citybotcoal = p13.GetActorsByType("citybotcoal")
				citymayor = p13.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p14 and p12.IsAlliedWith(p14) then
				nuk2 = p14.GetActorsByType("nuk2")
				nuke = p14.GetActorsByType("nuke")
				apwr = p14.GetActorsByType("apwr")
				powr = p14.GetActorsByType("powr")
				power2100 = p14.GetActorsByType("2100power")
				scsupplydepot = p14.GetActorsByType("scsupplydepot")
				cityboti = p14.GetActorsByType("cityboti")
				cityc = p14.GetActorsByType("cityc")
				citybotr = p14.GetActorsByType("citybotr")
				citybotnuke = p14.GetActorsByType("citybotnuke")
				citybotcoal = p14.GetActorsByType("citybotcoal")
				citymayor = p14.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p15 and p12.IsAlliedWith(p15) then
				nuk2 = p15.GetActorsByType("nuk2")
				nuke = p15.GetActorsByType("nuke")
				apwr = p15.GetActorsByType("apwr")
				powr = p15.GetActorsByType("powr")
				power2100 = p15.GetActorsByType("2100power")
				scsupplydepot = p15.GetActorsByType("scsupplydepot")
				cityboti = p15.GetActorsByType("cityboti")
				cityc = p15.GetActorsByType("cityc")
				citybotr = p15.GetActorsByType("citybotr")
				citybotnuke = p15.GetActorsByType("citybotnuke")
				citybotcoal = p15.GetActorsByType("citybotcoal")
				citymayor = p15.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p16 and p12.IsAlliedWith(p16) then
				nuk2 = p16.GetActorsByType("nuk2")
				nuke = p16.GetActorsByType("nuke")
				apwr = p16.GetActorsByType("apwr")
				powr = p16.GetActorsByType("powr")
				power2100 = p16.GetActorsByType("2100power")
				scsupplydepot = p16.GetActorsByType("scsupplydepot")
				cityboti = p16.GetActorsByType("cityboti")
				cityc = p16.GetActorsByType("cityc")
				citybotr = p16.GetActorsByType("citybotr")
				citybotnuke = p16.GetActorsByType("citybotnuke")
				citybotcoal = p16.GetActorsByType("citybotcoal")
				citymayor = p16.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p1 and p12.IsAlliedWith(p1) then
				nuk2 = p1.GetActorsByType("nuk2")
				nuke = p1.GetActorsByType("nuke")
				apwr = p1.GetActorsByType("apwr")
				powr = p1.GetActorsByType("powr")
				power2100 = p1.GetActorsByType("2100power")
				scsupplydepot = p1.GetActorsByType("scsupplydepot")
				cityboti = p1.GetActorsByType("cityboti")
				cityc = p1.GetActorsByType("cityc")
				citybotr = p1.GetActorsByType("citybotr")
				citybotnuke = p1.GetActorsByType("citybotnuke")
				citybotcoal = p1.GetActorsByType("citybotcoal")
				citymayor = p1.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p2 and p12.IsAlliedWith(p2) then
				nuk2 = p2.GetActorsByType("nuk2")
				nuke = p2.GetActorsByType("nuke")
				apwr = p2.GetActorsByType("apwr")
				powr = p2.GetActorsByType("powr")
				power2100 = p2.GetActorsByType("2100power")
				scsupplydepot = p2.GetActorsByType("scsupplydepot")
				cityboti = p2.GetActorsByType("cityboti")
				cityc = p2.GetActorsByType("cityc")
				citybotr = p2.GetActorsByType("citybotr")
				citybotnuke = p2.GetActorsByType("citybotnuke")
				citybotcoal = p2.GetActorsByType("citybotcoal")
				citymayor = p2.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p3 and p12.IsAlliedWith(p3) then
				nuk2 = p3.GetActorsByType("nuk2")
				nuke = p3.GetActorsByType("nuke")
				apwr = p3.GetActorsByType("apwr")
				powr = p3.GetActorsByType("powr")
				power2100 = p3.GetActorsByType("2100power")
				scsupplydepot = p3.GetActorsByType("scsupplydepot")
				cityboti = p3.GetActorsByType("cityboti")
				cityc = p3.GetActorsByType("cityc")
				citybotr = p3.GetActorsByType("citybotr")
				citybotnuke = p3.GetActorsByType("citybotnuke")
				citybotcoal = p3.GetActorsByType("citybotcoal")
				citymayor = p3.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p4 and p12.IsAlliedWith(p4) then
				nuk2 = p4.GetActorsByType("nuk2")
				nuke = p4.GetActorsByType("nuke")
				apwr = p4.GetActorsByType("apwr")
				powr = p4.GetActorsByType("powr")
				power2100 = p4.GetActorsByType("2100power")
				scsupplydepot = p4.GetActorsByType("scsupplydepot")
				cityboti = p4.GetActorsByType("cityboti")
				cityc = p4.GetActorsByType("cityc")
				citybotr = p4.GetActorsByType("citybotr")
				citybotnuke = p4.GetActorsByType("citybotnuke")
				citybotcoal = p4.GetActorsByType("citybotcoal")
				citymayor = p4.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p5 and p12.IsAlliedWith(p5) then
				nuk2 = p5.GetActorsByType("nuk2")
				nuke = p5.GetActorsByType("nuke")
				apwr = p5.GetActorsByType("apwr")
				powr = p5.GetActorsByType("powr")
				power2100 = p5.GetActorsByType("2100power")
				scsupplydepot = p5.GetActorsByType("scsupplydepot")
				cityboti = p5.GetActorsByType("cityboti")
				cityc = p5.GetActorsByType("cityc")
				citybotr = p5.GetActorsByType("citybotr")
				citybotnuke = p5.GetActorsByType("citybotnuke")
				citybotcoal = p5.GetActorsByType("citybotcoal")
				citymayor = p5.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p6 and p12.IsAlliedWith(p6) then
				nuk2 = p6.GetActorsByType("nuk2")
				nuke = p6.GetActorsByType("nuke")
				apwr = p6.GetActorsByType("apwr")
				powr = p6.GetActorsByType("powr")
				power2100 = p6.GetActorsByType("2100power")
				scsupplydepot = p6.GetActorsByType("scsupplydepot")
				cityboti = p6.GetActorsByType("cityboti")
				cityc = p6.GetActorsByType("cityc")
				citybotr = p6.GetActorsByType("citybotr")
				citybotnuke = p6.GetActorsByType("citybotnuke")
				citybotcoal = p6.GetActorsByType("citybotcoal")
				citymayor = p6.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p7 and p12.IsAlliedWith(p7) then
				nuk2 = p7.GetActorsByType("nuk2")
				nuke = p7.GetActorsByType("nuke")
				apwr = p7.GetActorsByType("apwr")
				powr = p7.GetActorsByType("powr")
				power2100 = p7.GetActorsByType("2100power")
				scsupplydepot = p7.GetActorsByType("scsupplydepot")
				cityboti = p7.GetActorsByType("cityboti")
				cityc = p7.GetActorsByType("cityc")
				citybotr = p7.GetActorsByType("citybotr")
				citybotnuke = p7.GetActorsByType("citybotnuke")
				citybotcoal = p7.GetActorsByType("citybotcoal")
				citymayor = p7.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p8 and p12.IsAlliedWith(p8) then
				nuk2 = p8.GetActorsByType("nuk2")
				nuke = p8.GetActorsByType("nuke")
				apwr = p8.GetActorsByType("apwr")
				powr = p8.GetActorsByType("powr")
				power2100 = p8.GetActorsByType("2100power")
				scsupplydepot = p8.GetActorsByType("scsupplydepot")
				cityboti = p8.GetActorsByType("cityboti")
				cityc = p8.GetActorsByType("cityc")
				citybotr = p8.GetActorsByType("citybotr")
				citybotnuke = p8.GetActorsByType("citybotnuke")
				citybotcoal = p8.GetActorsByType("citybotcoal")
				citymayor = p8.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p9 and p12.IsAlliedWith(p9) then
				nuk2 = p9.GetActorsByType("nuk2")
				nuke = p9.GetActorsByType("nuke")
				apwr = p9.GetActorsByType("apwr")
				powr = p9.GetActorsByType("powr")
				power2100 = p9.GetActorsByType("2100power")
				scsupplydepot = p9.GetActorsByType("scsupplydepot")
				cityboti = p9.GetActorsByType("cityboti")
				cityc = p9.GetActorsByType("cityc")
				citybotr = p9.GetActorsByType("citybotr")
				citybotnuke = p9.GetActorsByType("citybotnuke")
				citybotcoal = p9.GetActorsByType("citybotcoal")
				citymayor = p9.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p10 and p12.IsAlliedWith(p10) then
				nuk2 = p10.GetActorsByType("nuk2")
				nuke = p10.GetActorsByType("nuke")
				apwr = p10.GetActorsByType("apwr")
				powr = p10.GetActorsByType("powr")
				power2100 = p10.GetActorsByType("2100power")
				scsupplydepot = p10.GetActorsByType("scsupplydepot")
				cityboti = p10.GetActorsByType("cityboti")
				cityc = p10.GetActorsByType("cityc")
				citybotr = p10.GetActorsByType("citybotr")
				citybotnuke = p10.GetActorsByType("citybotnuke")
				citybotcoal = p10.GetActorsByType("citybotcoal")
				citymayor = p10.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p11 and p12.IsAlliedWith(p11) then
				nuk2 = p11.GetActorsByType("nuk2")
				nuke = p11.GetActorsByType("nuke")
				apwr = p11.GetActorsByType("apwr")
				powr = p11.GetActorsByType("powr")
				power2100 = p11.GetActorsByType("2100power")
				scsupplydepot = p11.GetActorsByType("scsupplydepot")
				cityboti = p11.GetActorsByType("cityboti")
				cityc = p11.GetActorsByType("cityc")
				citybotr = p11.GetActorsByType("citybotr")
				citybotnuke = p11.GetActorsByType("citybotnuke")
				citybotcoal = p11.GetActorsByType("citybotcoal")
				citymayor = p11.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p12building()
	end)
end
p13building = function()
	if p13 and p13.IsBot then
		citybotelitearchitect = p13.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p13.GetActorsByType("cityboti")
				citycown = p13.GetActorsByType("cityc")
				citybotrown = p13.GetActorsByType("citybotr")
				citybotnukeown = p13.GetActorsByType("citybotnuke")
				citybotcoalown = p13.GetActorsByType("citybotcoal")
				citymayorown = p13.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p14 and p13.IsAlliedWith(p14) then
				nuk2 = p14.GetActorsByType("nuk2")
				nuke = p14.GetActorsByType("nuke")
				apwr = p14.GetActorsByType("apwr")
				powr = p14.GetActorsByType("powr")
				power2100 = p14.GetActorsByType("2100power")
				scsupplydepot = p14.GetActorsByType("scsupplydepot")
				cityboti = p14.GetActorsByType("cityboti")
				cityc = p14.GetActorsByType("cityc")
				citybotr = p14.GetActorsByType("citybotr")
				citybotnuke = p14.GetActorsByType("citybotnuke")
				citybotcoal = p14.GetActorsByType("citybotcoal")
				citymayor = p14.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p15 and p13.IsAlliedWith(p15) then
				nuk2 = p15.GetActorsByType("nuk2")
				nuke = p15.GetActorsByType("nuke")
				apwr = p15.GetActorsByType("apwr")
				powr = p15.GetActorsByType("powr")
				power2100 = p15.GetActorsByType("2100power")
				scsupplydepot = p15.GetActorsByType("scsupplydepot")
				cityboti = p15.GetActorsByType("cityboti")
				cityc = p15.GetActorsByType("cityc")
				citybotr = p15.GetActorsByType("citybotr")
				citybotnuke = p15.GetActorsByType("citybotnuke")
				citybotcoal = p15.GetActorsByType("citybotcoal")
				citymayor = p15.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p16 and p13.IsAlliedWith(p16) then
				nuk2 = p16.GetActorsByType("nuk2")
				nuke = p16.GetActorsByType("nuke")
				apwr = p16.GetActorsByType("apwr")
				powr = p16.GetActorsByType("powr")
				power2100 = p16.GetActorsByType("2100power")
				scsupplydepot = p16.GetActorsByType("scsupplydepot")
				cityboti = p16.GetActorsByType("cityboti")
				cityc = p16.GetActorsByType("cityc")
				citybotr = p16.GetActorsByType("citybotr")
				citybotnuke = p16.GetActorsByType("citybotnuke")
				citybotcoal = p16.GetActorsByType("citybotcoal")
				citymayor = p16.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p1 and p13.IsAlliedWith(p1) then
				nuk2 = p1.GetActorsByType("nuk2")
				nuke = p1.GetActorsByType("nuke")
				apwr = p1.GetActorsByType("apwr")
				powr = p1.GetActorsByType("powr")
				power2100 = p1.GetActorsByType("2100power")
				scsupplydepot = p1.GetActorsByType("scsupplydepot")
				cityboti = p1.GetActorsByType("cityboti")
				cityc = p1.GetActorsByType("cityc")
				citybotr = p1.GetActorsByType("citybotr")
				citybotnuke = p1.GetActorsByType("citybotnuke")
				citybotcoal = p1.GetActorsByType("citybotcoal")
				citymayor = p1.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p2 and p13.IsAlliedWith(p2) then
				nuk2 = p2.GetActorsByType("nuk2")
				nuke = p2.GetActorsByType("nuke")
				apwr = p2.GetActorsByType("apwr")
				powr = p2.GetActorsByType("powr")
				power2100 = p2.GetActorsByType("2100power")
				scsupplydepot = p2.GetActorsByType("scsupplydepot")
				cityboti = p2.GetActorsByType("cityboti")
				cityc = p2.GetActorsByType("cityc")
				citybotr = p2.GetActorsByType("citybotr")
				citybotnuke = p2.GetActorsByType("citybotnuke")
				citybotcoal = p2.GetActorsByType("citybotcoal")
				citymayor = p2.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p3 and p13.IsAlliedWith(p3) then
				nuk2 = p3.GetActorsByType("nuk2")
				nuke = p3.GetActorsByType("nuke")
				apwr = p3.GetActorsByType("apwr")
				powr = p3.GetActorsByType("powr")
				power2100 = p3.GetActorsByType("2100power")
				scsupplydepot = p3.GetActorsByType("scsupplydepot")
				cityboti = p3.GetActorsByType("cityboti")
				cityc = p3.GetActorsByType("cityc")
				citybotr = p3.GetActorsByType("citybotr")
				citybotnuke = p3.GetActorsByType("citybotnuke")
				citybotcoal = p3.GetActorsByType("citybotcoal")
				citymayor = p3.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p4 and p13.IsAlliedWith(p4) then
				nuk2 = p4.GetActorsByType("nuk2")
				nuke = p4.GetActorsByType("nuke")
				apwr = p4.GetActorsByType("apwr")
				powr = p4.GetActorsByType("powr")
				power2100 = p4.GetActorsByType("2100power")
				scsupplydepot = p4.GetActorsByType("scsupplydepot")
				cityboti = p4.GetActorsByType("cityboti")
				cityc = p4.GetActorsByType("cityc")
				citybotr = p4.GetActorsByType("citybotr")
				citybotnuke = p4.GetActorsByType("citybotnuke")
				citybotcoal = p4.GetActorsByType("citybotcoal")
				citymayor = p4.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p5 and p13.IsAlliedWith(p5) then
				nuk2 = p5.GetActorsByType("nuk2")
				nuke = p5.GetActorsByType("nuke")
				apwr = p5.GetActorsByType("apwr")
				powr = p5.GetActorsByType("powr")
				power2100 = p5.GetActorsByType("2100power")
				scsupplydepot = p5.GetActorsByType("scsupplydepot")
				cityboti = p5.GetActorsByType("cityboti")
				cityc = p5.GetActorsByType("cityc")
				citybotr = p5.GetActorsByType("citybotr")
				citybotnuke = p5.GetActorsByType("citybotnuke")
				citybotcoal = p5.GetActorsByType("citybotcoal")
				citymayor = p5.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p6 and p13.IsAlliedWith(p6) then
				nuk2 = p6.GetActorsByType("nuk2")
				nuke = p6.GetActorsByType("nuke")
				apwr = p6.GetActorsByType("apwr")
				powr = p6.GetActorsByType("powr")
				power2100 = p6.GetActorsByType("2100power")
				scsupplydepot = p6.GetActorsByType("scsupplydepot")
				cityboti = p6.GetActorsByType("cityboti")
				cityc = p6.GetActorsByType("cityc")
				citybotr = p6.GetActorsByType("citybotr")
				citybotnuke = p6.GetActorsByType("citybotnuke")
				citybotcoal = p6.GetActorsByType("citybotcoal")
				citymayor = p6.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p7 and p13.IsAlliedWith(p7) then
				nuk2 = p7.GetActorsByType("nuk2")
				nuke = p7.GetActorsByType("nuke")
				apwr = p7.GetActorsByType("apwr")
				powr = p7.GetActorsByType("powr")
				power2100 = p7.GetActorsByType("2100power")
				scsupplydepot = p7.GetActorsByType("scsupplydepot")
				cityboti = p7.GetActorsByType("cityboti")
				cityc = p7.GetActorsByType("cityc")
				citybotr = p7.GetActorsByType("citybotr")
				citybotnuke = p7.GetActorsByType("citybotnuke")
				citybotcoal = p7.GetActorsByType("citybotcoal")
				citymayor = p7.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p8 and p13.IsAlliedWith(p8) then
				nuk2 = p8.GetActorsByType("nuk2")
				nuke = p8.GetActorsByType("nuke")
				apwr = p8.GetActorsByType("apwr")
				powr = p8.GetActorsByType("powr")
				power2100 = p8.GetActorsByType("2100power")
				scsupplydepot = p8.GetActorsByType("scsupplydepot")
				cityboti = p8.GetActorsByType("cityboti")
				cityc = p8.GetActorsByType("cityc")
				citybotr = p8.GetActorsByType("citybotr")
				citybotnuke = p8.GetActorsByType("citybotnuke")
				citybotcoal = p8.GetActorsByType("citybotcoal")
				citymayor = p8.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p9 and p13.IsAlliedWith(p9) then
				nuk2 = p9.GetActorsByType("nuk2")
				nuke = p9.GetActorsByType("nuke")
				apwr = p9.GetActorsByType("apwr")
				powr = p9.GetActorsByType("powr")
				power2100 = p9.GetActorsByType("2100power")
				scsupplydepot = p9.GetActorsByType("scsupplydepot")
				cityboti = p9.GetActorsByType("cityboti")
				cityc = p9.GetActorsByType("cityc")
				citybotr = p9.GetActorsByType("citybotr")
				citybotnuke = p9.GetActorsByType("citybotnuke")
				citybotcoal = p9.GetActorsByType("citybotcoal")
				citymayor = p9.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p10 and p13.IsAlliedWith(p10) then
				nuk2 = p10.GetActorsByType("nuk2")
				nuke = p10.GetActorsByType("nuke")
				apwr = p10.GetActorsByType("apwr")
				powr = p10.GetActorsByType("powr")
				power2100 = p10.GetActorsByType("2100power")
				scsupplydepot = p10.GetActorsByType("scsupplydepot")
				cityboti = p10.GetActorsByType("cityboti")
				cityc = p10.GetActorsByType("cityc")
				citybotr = p10.GetActorsByType("citybotr")
				citybotnuke = p10.GetActorsByType("citybotnuke")
				citybotcoal = p10.GetActorsByType("citybotcoal")
				citymayor = p10.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p11 and p13.IsAlliedWith(p11) then
				nuk2 = p11.GetActorsByType("nuk2")
				nuke = p11.GetActorsByType("nuke")
				apwr = p11.GetActorsByType("apwr")
				powr = p11.GetActorsByType("powr")
				power2100 = p11.GetActorsByType("2100power")
				scsupplydepot = p11.GetActorsByType("scsupplydepot")
				cityboti = p11.GetActorsByType("cityboti")
				cityc = p11.GetActorsByType("cityc")
				citybotr = p11.GetActorsByType("citybotr")
				citybotnuke = p11.GetActorsByType("citybotnuke")
				citybotcoal = p11.GetActorsByType("citybotcoal")
				citymayor = p11.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p12 and p13.IsAlliedWith(p12) then
				nuk2 = p12.GetActorsByType("nuk2")
				nuke = p12.GetActorsByType("nuke")
				apwr = p12.GetActorsByType("apwr")
				powr = p12.GetActorsByType("powr")
				power2100 = p12.GetActorsByType("2100power")
				scsupplydepot = p12.GetActorsByType("scsupplydepot")
				cityboti = p12.GetActorsByType("cityboti")
				cityc = p12.GetActorsByType("cityc")
				citybotr = p12.GetActorsByType("citybotr")
				citybotnuke = p12.GetActorsByType("citybotnuke")
				citybotcoal = p12.GetActorsByType("citybotcoal")
				citymayor = p12.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p13building()
	end)
end
p14building = function()
	if p14 and p14.IsBot then
		citybotelitearchitect = p14.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p14.GetActorsByType("cityboti")
				citycown = p14.GetActorsByType("cityc")
				citybotrown = p14.GetActorsByType("citybotr")
				citybotnukeown = p14.GetActorsByType("citybotnuke")
				citybotcoalown = p14.GetActorsByType("citybotcoal")
				citymayorown = p14.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p15 and p14.IsAlliedWith(p15) then
				nuk2 = p15.GetActorsByType("nuk2")
				nuke = p15.GetActorsByType("nuke")
				apwr = p15.GetActorsByType("apwr")
				powr = p15.GetActorsByType("powr")
				power2100 = p15.GetActorsByType("2100power")
				scsupplydepot = p15.GetActorsByType("scsupplydepot")
				cityboti = p15.GetActorsByType("cityboti")
				cityc = p15.GetActorsByType("cityc")
				citybotr = p15.GetActorsByType("citybotr")
				citybotnuke = p15.GetActorsByType("citybotnuke")
				citybotcoal = p15.GetActorsByType("citybotcoal")
				citymayor = p15.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p16 and p14.IsAlliedWith(p16) then
				nuk2 = p16.GetActorsByType("nuk2")
				nuke = p16.GetActorsByType("nuke")
				apwr = p16.GetActorsByType("apwr")
				powr = p16.GetActorsByType("powr")
				power2100 = p16.GetActorsByType("2100power")
				scsupplydepot = p16.GetActorsByType("scsupplydepot")
				cityboti = p16.GetActorsByType("cityboti")
				cityc = p16.GetActorsByType("cityc")
				citybotr = p16.GetActorsByType("citybotr")
				citybotnuke = p16.GetActorsByType("citybotnuke")
				citybotcoal = p16.GetActorsByType("citybotcoal")
				citymayor = p16.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p1 and p14.IsAlliedWith(p1) then
				nuk2 = p1.GetActorsByType("nuk2")
				nuke = p1.GetActorsByType("nuke")
				apwr = p1.GetActorsByType("apwr")
				powr = p1.GetActorsByType("powr")
				power2100 = p1.GetActorsByType("2100power")
				scsupplydepot = p1.GetActorsByType("scsupplydepot")
				cityboti = p1.GetActorsByType("cityboti")
				cityc = p1.GetActorsByType("cityc")
				citybotr = p1.GetActorsByType("citybotr")
				citybotnuke = p1.GetActorsByType("citybotnuke")
				citybotcoal = p1.GetActorsByType("citybotcoal")
				citymayor = p1.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p2 and p14.IsAlliedWith(p2) then
				nuk2 = p2.GetActorsByType("nuk2")
				nuke = p2.GetActorsByType("nuke")
				apwr = p2.GetActorsByType("apwr")
				powr = p2.GetActorsByType("powr")
				power2100 = p2.GetActorsByType("2100power")
				scsupplydepot = p2.GetActorsByType("scsupplydepot")
				cityboti = p2.GetActorsByType("cityboti")
				cityc = p2.GetActorsByType("cityc")
				citybotr = p2.GetActorsByType("citybotr")
				citybotnuke = p2.GetActorsByType("citybotnuke")
				citybotcoal = p2.GetActorsByType("citybotcoal")
				citymayor = p2.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p3 and p14.IsAlliedWith(p3) then
				nuk2 = p3.GetActorsByType("nuk2")
				nuke = p3.GetActorsByType("nuke")
				apwr = p3.GetActorsByType("apwr")
				powr = p3.GetActorsByType("powr")
				power2100 = p3.GetActorsByType("2100power")
				scsupplydepot = p3.GetActorsByType("scsupplydepot")
				cityboti = p3.GetActorsByType("cityboti")
				cityc = p3.GetActorsByType("cityc")
				citybotr = p3.GetActorsByType("citybotr")
				citybotnuke = p3.GetActorsByType("citybotnuke")
				citybotcoal = p3.GetActorsByType("citybotcoal")
				citymayor = p3.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p4 and p14.IsAlliedWith(p4) then
				nuk2 = p4.GetActorsByType("nuk2")
				nuke = p4.GetActorsByType("nuke")
				apwr = p4.GetActorsByType("apwr")
				powr = p4.GetActorsByType("powr")
				power2100 = p4.GetActorsByType("2100power")
				scsupplydepot = p4.GetActorsByType("scsupplydepot")
				cityboti = p4.GetActorsByType("cityboti")
				cityc = p4.GetActorsByType("cityc")
				citybotr = p4.GetActorsByType("citybotr")
				citybotnuke = p4.GetActorsByType("citybotnuke")
				citybotcoal = p4.GetActorsByType("citybotcoal")
				citymayor = p4.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p5 and p14.IsAlliedWith(p5) then
				nuk2 = p5.GetActorsByType("nuk2")
				nuke = p5.GetActorsByType("nuke")
				apwr = p5.GetActorsByType("apwr")
				powr = p5.GetActorsByType("powr")
				power2100 = p5.GetActorsByType("2100power")
				scsupplydepot = p5.GetActorsByType("scsupplydepot")
				cityboti = p5.GetActorsByType("cityboti")
				cityc = p5.GetActorsByType("cityc")
				citybotr = p5.GetActorsByType("citybotr")
				citybotnuke = p5.GetActorsByType("citybotnuke")
				citybotcoal = p5.GetActorsByType("citybotcoal")
				citymayor = p5.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p6 and p14.IsAlliedWith(p6) then
				nuk2 = p6.GetActorsByType("nuk2")
				nuke = p6.GetActorsByType("nuke")
				apwr = p6.GetActorsByType("apwr")
				powr = p6.GetActorsByType("powr")
				power2100 = p6.GetActorsByType("2100power")
				scsupplydepot = p6.GetActorsByType("scsupplydepot")
				cityboti = p6.GetActorsByType("cityboti")
				cityc = p6.GetActorsByType("cityc")
				citybotr = p6.GetActorsByType("citybotr")
				citybotnuke = p6.GetActorsByType("citybotnuke")
				citybotcoal = p6.GetActorsByType("citybotcoal")
				citymayor = p6.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p7 and p14.IsAlliedWith(p7) then
				nuk2 = p7.GetActorsByType("nuk2")
				nuke = p7.GetActorsByType("nuke")
				apwr = p7.GetActorsByType("apwr")
				powr = p7.GetActorsByType("powr")
				power2100 = p7.GetActorsByType("2100power")
				scsupplydepot = p7.GetActorsByType("scsupplydepot")
				cityboti = p7.GetActorsByType("cityboti")
				cityc = p7.GetActorsByType("cityc")
				citybotr = p7.GetActorsByType("citybotr")
				citybotnuke = p7.GetActorsByType("citybotnuke")
				citybotcoal = p7.GetActorsByType("citybotcoal")
				citymayor = p7.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p8 and p14.IsAlliedWith(p8) then
				nuk2 = p8.GetActorsByType("nuk2")
				nuke = p8.GetActorsByType("nuke")
				apwr = p8.GetActorsByType("apwr")
				powr = p8.GetActorsByType("powr")
				power2100 = p8.GetActorsByType("2100power")
				scsupplydepot = p8.GetActorsByType("scsupplydepot")
				cityboti = p8.GetActorsByType("cityboti")
				cityc = p8.GetActorsByType("cityc")
				citybotr = p8.GetActorsByType("citybotr")
				citybotnuke = p8.GetActorsByType("citybotnuke")
				citybotcoal = p8.GetActorsByType("citybotcoal")
				citymayor = p8.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p9 and p14.IsAlliedWith(p9) then
				nuk2 = p9.GetActorsByType("nuk2")
				nuke = p9.GetActorsByType("nuke")
				apwr = p9.GetActorsByType("apwr")
				powr = p9.GetActorsByType("powr")
				power2100 = p9.GetActorsByType("2100power")
				scsupplydepot = p9.GetActorsByType("scsupplydepot")
				cityboti = p9.GetActorsByType("cityboti")
				cityc = p9.GetActorsByType("cityc")
				citybotr = p9.GetActorsByType("citybotr")
				citybotnuke = p9.GetActorsByType("citybotnuke")
				citybotcoal = p9.GetActorsByType("citybotcoal")
				citymayor = p9.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p10 and p14.IsAlliedWith(p10) then
				nuk2 = p10.GetActorsByType("nuk2")
				nuke = p10.GetActorsByType("nuke")
				apwr = p10.GetActorsByType("apwr")
				powr = p10.GetActorsByType("powr")
				power2100 = p10.GetActorsByType("2100power")
				scsupplydepot = p10.GetActorsByType("scsupplydepot")
				cityboti = p10.GetActorsByType("cityboti")
				cityc = p10.GetActorsByType("cityc")
				citybotr = p10.GetActorsByType("citybotr")
				citybotnuke = p10.GetActorsByType("citybotnuke")
				citybotcoal = p10.GetActorsByType("citybotcoal")
				citymayor = p10.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p11 and p14.IsAlliedWith(p11) then
				nuk2 = p11.GetActorsByType("nuk2")
				nuke = p11.GetActorsByType("nuke")
				apwr = p11.GetActorsByType("apwr")
				powr = p11.GetActorsByType("powr")
				power2100 = p11.GetActorsByType("2100power")
				scsupplydepot = p11.GetActorsByType("scsupplydepot")
				cityboti = p11.GetActorsByType("cityboti")
				cityc = p11.GetActorsByType("cityc")
				citybotr = p11.GetActorsByType("citybotr")
				citybotnuke = p11.GetActorsByType("citybotnuke")
				citybotcoal = p11.GetActorsByType("citybotcoal")
				citymayor = p11.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p12 and p14.IsAlliedWith(p12) then
				nuk2 = p12.GetActorsByType("nuk2")
				nuke = p12.GetActorsByType("nuke")
				apwr = p12.GetActorsByType("apwr")
				powr = p12.GetActorsByType("powr")
				power2100 = p12.GetActorsByType("2100power")
				scsupplydepot = p12.GetActorsByType("scsupplydepot")
				cityboti = p12.GetActorsByType("cityboti")
				cityc = p12.GetActorsByType("cityc")
				citybotr = p12.GetActorsByType("citybotr")
				citybotnuke = p12.GetActorsByType("citybotnuke")
				citybotcoal = p12.GetActorsByType("citybotcoal")
				citymayor = p12.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p13 and p14.IsAlliedWith(p13) then
				nuk2 = p13.GetActorsByType("nuk2")
				nuke = p13.GetActorsByType("nuke")
				apwr = p13.GetActorsByType("apwr")
				powr = p13.GetActorsByType("powr")
				power2100 = p13.GetActorsByType("2100power")
				scsupplydepot = p13.GetActorsByType("scsupplydepot")
				cityboti = p13.GetActorsByType("cityboti")
				cityc = p13.GetActorsByType("cityc")
				citybotr = p13.GetActorsByType("citybotr")
				citybotnuke = p13.GetActorsByType("citybotnuke")
				citybotcoal = p13.GetActorsByType("citybotcoal")
				citymayor = p13.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p14building()
	end)
end
p15building = function()
	if p15 and p15.IsBot then
		citybotelitearchitect = p15.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p15.GetActorsByType("cityboti")
				citycown = p15.GetActorsByType("cityc")
				citybotrown = p15.GetActorsByType("citybotr")
				citybotnukeown = p15.GetActorsByType("citybotnuke")
				citybotcoalown = p15.GetActorsByType("citybotcoal")
				citymayorown = p15.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p16 and p15.IsAlliedWith(p16) then
				nuk2 = p16.GetActorsByType("nuk2")
				nuke = p16.GetActorsByType("nuke")
				apwr = p16.GetActorsByType("apwr")
				powr = p16.GetActorsByType("powr")
				power2100 = p16.GetActorsByType("2100power")
				scsupplydepot = p16.GetActorsByType("scsupplydepot")
				cityboti = p16.GetActorsByType("cityboti")
				cityc = p16.GetActorsByType("cityc")
				citybotr = p16.GetActorsByType("citybotr")
				citybotnuke = p16.GetActorsByType("citybotnuke")
				citybotcoal = p16.GetActorsByType("citybotcoal")
				citymayor = p16.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p1 and p15.IsAlliedWith(p1) then
				nuk2 = p1.GetActorsByType("nuk2")
				nuke = p1.GetActorsByType("nuke")
				apwr = p1.GetActorsByType("apwr")
				powr = p1.GetActorsByType("powr")
				power2100 = p1.GetActorsByType("2100power")
				scsupplydepot = p1.GetActorsByType("scsupplydepot")
				cityboti = p1.GetActorsByType("cityboti")
				cityc = p1.GetActorsByType("cityc")
				citybotr = p1.GetActorsByType("citybotr")
				citybotnuke = p1.GetActorsByType("citybotnuke")
				citybotcoal = p1.GetActorsByType("citybotcoal")
				citymayor = p1.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p2 and p15.IsAlliedWith(p2) then
				nuk2 = p2.GetActorsByType("nuk2")
				nuke = p2.GetActorsByType("nuke")
				apwr = p2.GetActorsByType("apwr")
				powr = p2.GetActorsByType("powr")
				power2100 = p2.GetActorsByType("2100power")
				scsupplydepot = p2.GetActorsByType("scsupplydepot")
				cityboti = p2.GetActorsByType("cityboti")
				cityc = p2.GetActorsByType("cityc")
				citybotr = p2.GetActorsByType("citybotr")
				citybotnuke = p2.GetActorsByType("citybotnuke")
				citybotcoal = p2.GetActorsByType("citybotcoal")
				citymayor = p2.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p3 and p15.IsAlliedWith(p3) then
				nuk2 = p3.GetActorsByType("nuk2")
				nuke = p3.GetActorsByType("nuke")
				apwr = p3.GetActorsByType("apwr")
				powr = p3.GetActorsByType("powr")
				power2100 = p3.GetActorsByType("2100power")
				scsupplydepot = p3.GetActorsByType("scsupplydepot")
				cityboti = p3.GetActorsByType("cityboti")
				cityc = p3.GetActorsByType("cityc")
				citybotr = p3.GetActorsByType("citybotr")
				citybotnuke = p3.GetActorsByType("citybotnuke")
				citybotcoal = p3.GetActorsByType("citybotcoal")
				citymayor = p3.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p4 and p15.IsAlliedWith(p4) then
				nuk2 = p4.GetActorsByType("nuk2")
				nuke = p4.GetActorsByType("nuke")
				apwr = p4.GetActorsByType("apwr")
				powr = p4.GetActorsByType("powr")
				power2100 = p4.GetActorsByType("2100power")
				scsupplydepot = p4.GetActorsByType("scsupplydepot")
				cityboti = p4.GetActorsByType("cityboti")
				cityc = p4.GetActorsByType("cityc")
				citybotr = p4.GetActorsByType("citybotr")
				citybotnuke = p4.GetActorsByType("citybotnuke")
				citybotcoal = p4.GetActorsByType("citybotcoal")
				citymayor = p4.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p5 and p15.IsAlliedWith(p5) then
				nuk2 = p5.GetActorsByType("nuk2")
				nuke = p5.GetActorsByType("nuke")
				apwr = p5.GetActorsByType("apwr")
				powr = p5.GetActorsByType("powr")
				power2100 = p5.GetActorsByType("2100power")
				scsupplydepot = p5.GetActorsByType("scsupplydepot")
				cityboti = p5.GetActorsByType("cityboti")
				cityc = p5.GetActorsByType("cityc")
				citybotr = p5.GetActorsByType("citybotr")
				citybotnuke = p5.GetActorsByType("citybotnuke")
				citybotcoal = p5.GetActorsByType("citybotcoal")
				citymayor = p5.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p6 and p15.IsAlliedWith(p6) then
				nuk2 = p6.GetActorsByType("nuk2")
				nuke = p6.GetActorsByType("nuke")
				apwr = p6.GetActorsByType("apwr")
				powr = p6.GetActorsByType("powr")
				power2100 = p6.GetActorsByType("2100power")
				scsupplydepot = p6.GetActorsByType("scsupplydepot")
				cityboti = p6.GetActorsByType("cityboti")
				cityc = p6.GetActorsByType("cityc")
				citybotr = p6.GetActorsByType("citybotr")
				citybotnuke = p6.GetActorsByType("citybotnuke")
				citybotcoal = p6.GetActorsByType("citybotcoal")
				citymayor = p6.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p7 and p15.IsAlliedWith(p7) then
				nuk2 = p7.GetActorsByType("nuk2")
				nuke = p7.GetActorsByType("nuke")
				apwr = p7.GetActorsByType("apwr")
				powr = p7.GetActorsByType("powr")
				power2100 = p7.GetActorsByType("2100power")
				scsupplydepot = p7.GetActorsByType("scsupplydepot")
				cityboti = p7.GetActorsByType("cityboti")
				cityc = p7.GetActorsByType("cityc")
				citybotr = p7.GetActorsByType("citybotr")
				citybotnuke = p7.GetActorsByType("citybotnuke")
				citybotcoal = p7.GetActorsByType("citybotcoal")
				citymayor = p7.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p8 and p15.IsAlliedWith(p8) then
				nuk2 = p8.GetActorsByType("nuk2")
				nuke = p8.GetActorsByType("nuke")
				apwr = p8.GetActorsByType("apwr")
				powr = p8.GetActorsByType("powr")
				power2100 = p8.GetActorsByType("2100power")
				scsupplydepot = p8.GetActorsByType("scsupplydepot")
				cityboti = p8.GetActorsByType("cityboti")
				cityc = p8.GetActorsByType("cityc")
				citybotr = p8.GetActorsByType("citybotr")
				citybotnuke = p8.GetActorsByType("citybotnuke")
				citybotcoal = p8.GetActorsByType("citybotcoal")
				citymayor = p8.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p9 and p15.IsAlliedWith(p9) then
				nuk2 = p9.GetActorsByType("nuk2")
				nuke = p9.GetActorsByType("nuke")
				apwr = p9.GetActorsByType("apwr")
				powr = p9.GetActorsByType("powr")
				power2100 = p9.GetActorsByType("2100power")
				scsupplydepot = p9.GetActorsByType("scsupplydepot")
				cityboti = p9.GetActorsByType("cityboti")
				cityc = p9.GetActorsByType("cityc")
				citybotr = p9.GetActorsByType("citybotr")
				citybotnuke = p9.GetActorsByType("citybotnuke")
				citybotcoal = p9.GetActorsByType("citybotcoal")
				citymayor = p9.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p10 and p15.IsAlliedWith(p10) then
				nuk2 = p10.GetActorsByType("nuk2")
				nuke = p10.GetActorsByType("nuke")
				apwr = p10.GetActorsByType("apwr")
				powr = p10.GetActorsByType("powr")
				power2100 = p10.GetActorsByType("2100power")
				scsupplydepot = p10.GetActorsByType("scsupplydepot")
				cityboti = p10.GetActorsByType("cityboti")
				cityc = p10.GetActorsByType("cityc")
				citybotr = p10.GetActorsByType("citybotr")
				citybotnuke = p10.GetActorsByType("citybotnuke")
				citybotcoal = p10.GetActorsByType("citybotcoal")
				citymayor = p10.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p11 and p15.IsAlliedWith(p11) then
				nuk2 = p11.GetActorsByType("nuk2")
				nuke = p11.GetActorsByType("nuke")
				apwr = p11.GetActorsByType("apwr")
				powr = p11.GetActorsByType("powr")
				power2100 = p11.GetActorsByType("2100power")
				scsupplydepot = p11.GetActorsByType("scsupplydepot")
				cityboti = p11.GetActorsByType("cityboti")
				cityc = p11.GetActorsByType("cityc")
				citybotr = p11.GetActorsByType("citybotr")
				citybotnuke = p11.GetActorsByType("citybotnuke")
				citybotcoal = p11.GetActorsByType("citybotcoal")
				citymayor = p11.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p12 and p15.IsAlliedWith(p12) then
				nuk2 = p12.GetActorsByType("nuk2")
				nuke = p12.GetActorsByType("nuke")
				apwr = p12.GetActorsByType("apwr")
				powr = p12.GetActorsByType("powr")
				power2100 = p12.GetActorsByType("2100power")
				scsupplydepot = p12.GetActorsByType("scsupplydepot")
				cityboti = p12.GetActorsByType("cityboti")
				cityc = p12.GetActorsByType("cityc")
				citybotr = p12.GetActorsByType("citybotr")
				citybotnuke = p12.GetActorsByType("citybotnuke")
				citybotcoal = p12.GetActorsByType("citybotcoal")
				citymayor = p12.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p13 and p15.IsAlliedWith(p13) then
				nuk2 = p13.GetActorsByType("nuk2")
				nuke = p13.GetActorsByType("nuke")
				apwr = p13.GetActorsByType("apwr")
				powr = p13.GetActorsByType("powr")
				power2100 = p13.GetActorsByType("2100power")
				scsupplydepot = p13.GetActorsByType("scsupplydepot")
				cityboti = p13.GetActorsByType("cityboti")
				cityc = p13.GetActorsByType("cityc")
				citybotr = p13.GetActorsByType("citybotr")
				citybotnuke = p13.GetActorsByType("citybotnuke")
				citybotcoal = p13.GetActorsByType("citybotcoal")
				citymayor = p13.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p14 and p15.IsAlliedWith(p14) then
				nuk2 = p14.GetActorsByType("nuk2")
				nuke = p14.GetActorsByType("nuke")
				apwr = p14.GetActorsByType("apwr")
				powr = p14.GetActorsByType("powr")
				power2100 = p14.GetActorsByType("2100power")
				scsupplydepot = p14.GetActorsByType("scsupplydepot")
				cityboti = p14.GetActorsByType("cityboti")
				cityc = p14.GetActorsByType("cityc")
				citybotr = p14.GetActorsByType("citybotr")
				citybotnuke = p14.GetActorsByType("citybotnuke")
				citybotcoal = p14.GetActorsByType("citybotcoal")
				citymayor = p14.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p15building()
	end)
end
p16building = function()
	if p16 and p16.IsBot then
		citybotelitearchitect = p16.GetActorsByType("citybotelitearchitect")
		for _,expert in pairs(citybotelitearchitect) do
				-- Player own Buildings
				citybotiown = p16.GetActorsByType("cityboti")
				citycown = p16.GetActorsByType("cityc")
				citybotrown = p16.GetActorsByType("citybotr")
				citybotnukeown = p16.GetActorsByType("citybotnuke")
				citybotcoalown = p16.GetActorsByType("citybotcoal")
				citymayorown = p16.GetActorsByType("citymayor")
				for _,citybotiown in pairs(citybotiown) do expert.DeliverExperience(citybotiown) end
				for _,citycown in pairs(citycown) do expert.DeliverExperience(citycown) end
				for _,citybotrown in pairs(citybotrown) do expert.DeliverExperience(citybotrown) end
				for _,citybotnukeown in pairs(citybotnukeown) do expert.DeliverExperience(citybotnukeown) end
				for _,citybotcoalown in pairs(citybotcoalown) do expert.DeliverExperience(citybotcoalown) end
				for _,citymayorown in pairs(citymayorown) do expert.DeliverExperience(citymayorown) end
				-- Other Players Buildings
			if p1 and p16.IsAlliedWith(p1) then
				nuk2 = p1.GetActorsByType("nuk2")
				nuke = p1.GetActorsByType("nuke")
				apwr = p1.GetActorsByType("apwr")
				powr = p1.GetActorsByType("powr")
				power2100 = p1.GetActorsByType("2100power")
				scsupplydepot = p1.GetActorsByType("scsupplydepot")
				cityboti = p1.GetActorsByType("cityboti")
				cityc = p1.GetActorsByType("cityc")
				citybotr = p1.GetActorsByType("citybotr")
				citybotnuke = p1.GetActorsByType("citybotnuke")
				citybotcoal = p1.GetActorsByType("citybotcoal")
				citymayor = p1.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p2 and p16.IsAlliedWith(p2) then
				nuk2 = p2.GetActorsByType("nuk2")
				nuke = p2.GetActorsByType("nuke")
				apwr = p2.GetActorsByType("apwr")
				powr = p2.GetActorsByType("powr")
				power2100 = p2.GetActorsByType("2100power")
				scsupplydepot = p2.GetActorsByType("scsupplydepot")
				cityboti = p2.GetActorsByType("cityboti")
				cityc = p2.GetActorsByType("cityc")
				citybotr = p2.GetActorsByType("citybotr")
				citybotnuke = p2.GetActorsByType("citybotnuke")
				citybotcoal = p2.GetActorsByType("citybotcoal")
				citymayor = p2.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p3 and p16.IsAlliedWith(p3) then
				nuk2 = p3.GetActorsByType("nuk2")
				nuke = p3.GetActorsByType("nuke")
				apwr = p3.GetActorsByType("apwr")
				powr = p3.GetActorsByType("powr")
				power2100 = p3.GetActorsByType("2100power")
				scsupplydepot = p3.GetActorsByType("scsupplydepot")
				cityboti = p3.GetActorsByType("cityboti")
				cityc = p3.GetActorsByType("cityc")
				citybotr = p3.GetActorsByType("citybotr")
				citybotnuke = p3.GetActorsByType("citybotnuke")
				citybotcoal = p3.GetActorsByType("citybotcoal")
				citymayor = p3.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p4 and p16.IsAlliedWith(p4) then
				nuk2 = p4.GetActorsByType("nuk2")
				nuke = p4.GetActorsByType("nuke")
				apwr = p4.GetActorsByType("apwr")
				powr = p4.GetActorsByType("powr")
				power2100 = p4.GetActorsByType("2100power")
				scsupplydepot = p4.GetActorsByType("scsupplydepot")
				cityboti = p4.GetActorsByType("cityboti")
				cityc = p4.GetActorsByType("cityc")
				citybotr = p4.GetActorsByType("citybotr")
				citybotnuke = p4.GetActorsByType("citybotnuke")
				citybotcoal = p4.GetActorsByType("citybotcoal")
				citymayor = p4.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p5 and p16.IsAlliedWith(p5) then
				nuk2 = p5.GetActorsByType("nuk2")
				nuke = p5.GetActorsByType("nuke")
				apwr = p5.GetActorsByType("apwr")
				powr = p5.GetActorsByType("powr")
				power2100 = p5.GetActorsByType("2100power")
				scsupplydepot = p5.GetActorsByType("scsupplydepot")
				cityboti = p5.GetActorsByType("cityboti")
				cityc = p5.GetActorsByType("cityc")
				citybotr = p5.GetActorsByType("citybotr")
				citybotnuke = p5.GetActorsByType("citybotnuke")
				citybotcoal = p5.GetActorsByType("citybotcoal")
				citymayor = p5.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p6 and p16.IsAlliedWith(p6) then
				nuk2 = p6.GetActorsByType("nuk2")
				nuke = p6.GetActorsByType("nuke")
				apwr = p6.GetActorsByType("apwr")
				powr = p6.GetActorsByType("powr")
				power2100 = p6.GetActorsByType("2100power")
				scsupplydepot = p6.GetActorsByType("scsupplydepot")
				cityboti = p6.GetActorsByType("cityboti")
				cityc = p6.GetActorsByType("cityc")
				citybotr = p6.GetActorsByType("citybotr")
				citybotnuke = p6.GetActorsByType("citybotnuke")
				citybotcoal = p6.GetActorsByType("citybotcoal")
				citymayor = p6.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p7 and p16.IsAlliedWith(p7) then
				nuk2 = p7.GetActorsByType("nuk2")
				nuke = p7.GetActorsByType("nuke")
				apwr = p7.GetActorsByType("apwr")
				powr = p7.GetActorsByType("powr")
				power2100 = p7.GetActorsByType("2100power")
				scsupplydepot = p7.GetActorsByType("scsupplydepot")
				cityboti = p7.GetActorsByType("cityboti")
				cityc = p7.GetActorsByType("cityc")
				citybotr = p7.GetActorsByType("citybotr")
				citybotnuke = p7.GetActorsByType("citybotnuke")
				citybotcoal = p7.GetActorsByType("citybotcoal")
				citymayor = p7.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p8 and p16.IsAlliedWith(p8) then
				nuk2 = p8.GetActorsByType("nuk2")
				nuke = p8.GetActorsByType("nuke")
				apwr = p8.GetActorsByType("apwr")
				powr = p8.GetActorsByType("powr")
				power2100 = p8.GetActorsByType("2100power")
				scsupplydepot = p8.GetActorsByType("scsupplydepot")
				cityboti = p8.GetActorsByType("cityboti")
				cityc = p8.GetActorsByType("cityc")
				citybotr = p8.GetActorsByType("citybotr")
				citybotnuke = p8.GetActorsByType("citybotnuke")
				citybotcoal = p8.GetActorsByType("citybotcoal")
				citymayor = p8.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p9 and p16.IsAlliedWith(p9) then
				nuk2 = p9.GetActorsByType("nuk2")
				nuke = p9.GetActorsByType("nuke")
				apwr = p9.GetActorsByType("apwr")
				powr = p9.GetActorsByType("powr")
				power2100 = p9.GetActorsByType("2100power")
				scsupplydepot = p9.GetActorsByType("scsupplydepot")
				cityboti = p9.GetActorsByType("cityboti")
				cityc = p9.GetActorsByType("cityc")
				citybotr = p9.GetActorsByType("citybotr")
				citybotnuke = p9.GetActorsByType("citybotnuke")
				citybotcoal = p9.GetActorsByType("citybotcoal")
				citymayor = p9.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p10 and p16.IsAlliedWith(p10) then
				nuk2 = p10.GetActorsByType("nuk2")
				nuke = p10.GetActorsByType("nuke")
				apwr = p10.GetActorsByType("apwr")
				powr = p10.GetActorsByType("powr")
				power2100 = p10.GetActorsByType("2100power")
				scsupplydepot = p10.GetActorsByType("scsupplydepot")
				cityboti = p10.GetActorsByType("cityboti")
				cityc = p10.GetActorsByType("cityc")
				citybotr = p10.GetActorsByType("citybotr")
				citybotnuke = p10.GetActorsByType("citybotnuke")
				citybotcoal = p10.GetActorsByType("citybotcoal")
				citymayor = p10.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p11 and p16.IsAlliedWith(p11) then
				nuk2 = p11.GetActorsByType("nuk2")
				nuke = p11.GetActorsByType("nuke")
				apwr = p11.GetActorsByType("apwr")
				powr = p11.GetActorsByType("powr")
				power2100 = p11.GetActorsByType("2100power")
				scsupplydepot = p11.GetActorsByType("scsupplydepot")
				cityboti = p11.GetActorsByType("cityboti")
				cityc = p11.GetActorsByType("cityc")
				citybotr = p11.GetActorsByType("citybotr")
				citybotnuke = p11.GetActorsByType("citybotnuke")
				citybotcoal = p11.GetActorsByType("citybotcoal")
				citymayor = p11.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p12 and p16.IsAlliedWith(p12) then
				nuk2 = p12.GetActorsByType("nuk2")
				nuke = p12.GetActorsByType("nuke")
				apwr = p12.GetActorsByType("apwr")
				powr = p12.GetActorsByType("powr")
				power2100 = p12.GetActorsByType("2100power")
				scsupplydepot = p12.GetActorsByType("scsupplydepot")
				cityboti = p12.GetActorsByType("cityboti")
				cityc = p12.GetActorsByType("cityc")
				citybotr = p12.GetActorsByType("citybotr")
				citybotnuke = p12.GetActorsByType("citybotnuke")
				citybotcoal = p12.GetActorsByType("citybotcoal")
				citymayor = p12.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p13 and p16.IsAlliedWith(p13) then
				nuk2 = p13.GetActorsByType("nuk2")
				nuke = p13.GetActorsByType("nuke")
				apwr = p13.GetActorsByType("apwr")
				powr = p13.GetActorsByType("powr")
				power2100 = p13.GetActorsByType("2100power")
				scsupplydepot = p13.GetActorsByType("scsupplydepot")
				cityboti = p13.GetActorsByType("cityboti")
				cityc = p13.GetActorsByType("cityc")
				citybotr = p13.GetActorsByType("citybotr")
				citybotnuke = p13.GetActorsByType("citybotnuke")
				citybotcoal = p13.GetActorsByType("citybotcoal")
				citymayor = p13.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p14 and p16.IsAlliedWith(p14) then
				nuk2 = p14.GetActorsByType("nuk2")
				nuke = p14.GetActorsByType("nuke")
				apwr = p14.GetActorsByType("apwr")
				powr = p14.GetActorsByType("powr")
				power2100 = p14.GetActorsByType("2100power")
				scsupplydepot = p14.GetActorsByType("scsupplydepot")
				cityboti = p14.GetActorsByType("cityboti")
				cityc = p14.GetActorsByType("cityc")
				citybotr = p14.GetActorsByType("citybotr")
				citybotnuke = p14.GetActorsByType("citybotnuke")
				citybotcoal = p14.GetActorsByType("citybotcoal")
				citymayor = p14.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
			if p15 and p16.IsAlliedWith(p15) then
				nuk2 = p15.GetActorsByType("nuk2")
				nuke = p15.GetActorsByType("nuke")
				apwr = p15.GetActorsByType("apwr")
				powr = p15.GetActorsByType("powr")
				power2100 = p15.GetActorsByType("2100power")
				scsupplydepot = p15.GetActorsByType("scsupplydepot")
				cityboti = p15.GetActorsByType("cityboti")
				cityc = p15.GetActorsByType("cityc")
				citybotr = p15.GetActorsByType("citybotr")
				citybotnuke = p15.GetActorsByType("citybotnuke")
				citybotcoal = p15.GetActorsByType("citybotcoal")
				citymayor = p15.GetActorsByType("citymayor")
				for _,nuk2 in pairs(nuk2) do expert.DeliverExperience(nuk2) end
				for _,nuke in pairs(nuke) do expert.DeliverExperience(nuke) end
				for _,apwr in pairs(apwr) do expert.DeliverExperience(apwr) end
				for _,powr in pairs(powr) do expert.DeliverExperience(powr) end
				for _,power2100 in pairs(power2100) do expert.DeliverExperience(power2100) end
				for _,scsupplydepot in pairs(scsupplydepot) do expert.DeliverExperience(scsupplydepot) end
				for _,cityboti in pairs(cityboti) do expert.DeliverExperience(cityboti) end
				for _,cityc in pairs(cityc) do expert.DeliverExperience(cityc) end
				for _,citybotr in pairs(citybotr) do expert.DeliverExperience(citybotr) end
				for _,citybotnuke in pairs(citybotnuke) do expert.DeliverExperience(citybotnuke) end
				for _,citybotcoal in pairs(citybotcoal) do expert.DeliverExperience(citybotcoal) end
				for _,citymayor in pairs(citymayor) do expert.DeliverExperience(citymayor) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(6), function()
		p16building()
	end)
end

p1vehicle = function()
	if p1 and p1.IsBot then
		cityboteliteexpert = p1.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p2 and p1.IsAlliedWith(p2) then
				htnk = p2.GetActorsByType("htnk")
				msam = p2.GetActorsByType("msam")
				mtnk = p2.GetActorsByType("mtnk")
				apc = p2.GetActorsByType("apc")
				jeep = p2.GetActorsByType("jeep")
				stnk = p2.GetActorsByType("stnk")
				arty = p2.GetActorsByType("arty")
				ltnk = p2.GetActorsByType("ltnk")
				ftnk = p2.GetActorsByType("ftnk")
				mlrs = p2.GetActorsByType("mlrs")
				bike = p2.GetActorsByType("bike")
				bggy = p2.GetActorsByType("bggy")
				ctnk = p2.GetActorsByType("ctnk")
				tnk2 = p2.GetActorsByType("2tnk")
				raarty = p2.GetActorsByType("raarty")
				tnk1 = p2.GetActorsByType("1tnk")
				rajeep = p2.GetActorsByType("rajeep")
				tnk4 = p2.GetActorsByType("4tnk")
				v2rl = p2.GetActorsByType("v2rl")
				tnk3 = p2.GetActorsByType("3tnk")
				ttnk = p2.GetActorsByType("ttnk")
				ftrk = p2.GetActorsByType("ftrk")
				raapc = p2.GetActorsByType("raapc")
				hpt2100 = p2.GetActorsByType("2100hpt")
				apt2100 = p2.GetActorsByType("2100apt")
				mch2100 = p2.GetActorsByType("2100mch")
				tch2100 = p2.GetActorsByType("2100tch")
				rvw2100 = p2.GetActorsByType("2100rvw")
				mvw2100 = p2.GetActorsByType("2100mvw")
				rvw22100 = p2.GetActorsByType("2100rvw2")
				mvw22100 = p2.GetActorsByType("2100mvw2")
				scgoliath = p2.GetActorsByType("scgoliath")
				scvulture = p2.GetActorsByType("scvulture")
				scsiegetank = p2.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p3 and p1.IsAlliedWith(p3) then
				htnk = p3.GetActorsByType("htnk")
				msam = p3.GetActorsByType("msam")
				mtnk = p3.GetActorsByType("mtnk")
				apc = p3.GetActorsByType("apc")
				jeep = p3.GetActorsByType("jeep")
				stnk = p3.GetActorsByType("stnk")
				arty = p3.GetActorsByType("arty")
				ltnk = p3.GetActorsByType("ltnk")
				ftnk = p3.GetActorsByType("ftnk")
				mlrs = p3.GetActorsByType("mlrs")
				bike = p3.GetActorsByType("bike")
				bggy = p3.GetActorsByType("bggy")
				ctnk = p3.GetActorsByType("ctnk")
				tnk2 = p3.GetActorsByType("2tnk")
				raarty = p3.GetActorsByType("raarty")
				tnk1 = p3.GetActorsByType("1tnk")
				rajeep = p3.GetActorsByType("rajeep")
				tnk4 = p3.GetActorsByType("4tnk")
				v2rl = p3.GetActorsByType("v2rl")
				tnk3 = p3.GetActorsByType("3tnk")
				ttnk = p3.GetActorsByType("ttnk")
				ftrk = p3.GetActorsByType("ftrk")
				raapc = p3.GetActorsByType("raapc")
				hpt2100 = p3.GetActorsByType("2100hpt")
				apt2100 = p3.GetActorsByType("2100apt")
				mch2100 = p3.GetActorsByType("2100mch")
				tch2100 = p3.GetActorsByType("2100tch")
				rvw2100 = p3.GetActorsByType("2100rvw")
				mvw2100 = p3.GetActorsByType("2100mvw")
				rvw22100 = p3.GetActorsByType("2100rvw2")
				mvw22100 = p3.GetActorsByType("2100mvw2")
				scgoliath = p3.GetActorsByType("scgoliath")
				scvulture = p3.GetActorsByType("scvulture")
				scsiegetank = p3.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p4 and p1.IsAlliedWith(p4) then
				htnk = p4.GetActorsByType("htnk")
				msam = p4.GetActorsByType("msam")
				mtnk = p4.GetActorsByType("mtnk")
				apc = p4.GetActorsByType("apc")
				jeep = p4.GetActorsByType("jeep")
				stnk = p4.GetActorsByType("stnk")
				arty = p4.GetActorsByType("arty")
				ltnk = p4.GetActorsByType("ltnk")
				ftnk = p4.GetActorsByType("ftnk")
				mlrs = p4.GetActorsByType("mlrs")
				bike = p4.GetActorsByType("bike")
				bggy = p4.GetActorsByType("bggy")
				ctnk = p4.GetActorsByType("ctnk")
				tnk2 = p4.GetActorsByType("2tnk")
				raarty = p4.GetActorsByType("raarty")
				tnk1 = p4.GetActorsByType("1tnk")
				rajeep = p4.GetActorsByType("rajeep")
				tnk4 = p4.GetActorsByType("4tnk")
				v2rl = p4.GetActorsByType("v2rl")
				tnk3 = p4.GetActorsByType("3tnk")
				ttnk = p4.GetActorsByType("ttnk")
				ftrk = p4.GetActorsByType("ftrk")
				raapc = p4.GetActorsByType("raapc")
				hpt2100 = p4.GetActorsByType("2100hpt")
				apt2100 = p4.GetActorsByType("2100apt")
				mch2100 = p4.GetActorsByType("2100mch")
				tch2100 = p4.GetActorsByType("2100tch")
				rvw2100 = p4.GetActorsByType("2100rvw")
				mvw2100 = p4.GetActorsByType("2100mvw")
				rvw22100 = p4.GetActorsByType("2100rvw2")
				mvw22100 = p4.GetActorsByType("2100mvw2")
				scgoliath = p4.GetActorsByType("scgoliath")
				scvulture = p4.GetActorsByType("scvulture")
				scsiegetank = p4.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p5 and p1.IsAlliedWith(p5) then
				htnk = p5.GetActorsByType("htnk")
				msam = p5.GetActorsByType("msam")
				mtnk = p5.GetActorsByType("mtnk")
				apc = p5.GetActorsByType("apc")
				jeep = p5.GetActorsByType("jeep")
				stnk = p5.GetActorsByType("stnk")
				arty = p5.GetActorsByType("arty")
				ltnk = p5.GetActorsByType("ltnk")
				ftnk = p5.GetActorsByType("ftnk")
				mlrs = p5.GetActorsByType("mlrs")
				bike = p5.GetActorsByType("bike")
				bggy = p5.GetActorsByType("bggy")
				ctnk = p5.GetActorsByType("ctnk")
				tnk2 = p5.GetActorsByType("2tnk")
				raarty = p5.GetActorsByType("raarty")
				tnk1 = p5.GetActorsByType("1tnk")
				rajeep = p5.GetActorsByType("rajeep")
				tnk4 = p5.GetActorsByType("4tnk")
				v2rl = p5.GetActorsByType("v2rl")
				tnk3 = p5.GetActorsByType("3tnk")
				ttnk = p5.GetActorsByType("ttnk")
				ftrk = p5.GetActorsByType("ftrk")
				raapc = p5.GetActorsByType("raapc")
				hpt2100 = p5.GetActorsByType("2100hpt")
				apt2100 = p5.GetActorsByType("2100apt")
				mch2100 = p5.GetActorsByType("2100mch")
				tch2100 = p5.GetActorsByType("2100tch")
				rvw2100 = p5.GetActorsByType("2100rvw")
				mvw2100 = p5.GetActorsByType("2100mvw")
				rvw22100 = p5.GetActorsByType("2100rvw2")
				mvw22100 = p5.GetActorsByType("2100mvw2")
				scgoliath = p5.GetActorsByType("scgoliath")
				scvulture = p5.GetActorsByType("scvulture")
				scsiegetank = p5.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p6 and p1.IsAlliedWith(p6) then
				htnk = p6.GetActorsByType("htnk")
				msam = p6.GetActorsByType("msam")
				mtnk = p6.GetActorsByType("mtnk")
				apc = p6.GetActorsByType("apc")
				jeep = p6.GetActorsByType("jeep")
				stnk = p6.GetActorsByType("stnk")
				arty = p6.GetActorsByType("arty")
				ltnk = p6.GetActorsByType("ltnk")
				ftnk = p6.GetActorsByType("ftnk")
				mlrs = p6.GetActorsByType("mlrs")
				bike = p6.GetActorsByType("bike")
				bggy = p6.GetActorsByType("bggy")
				ctnk = p6.GetActorsByType("ctnk")
				tnk2 = p6.GetActorsByType("2tnk")
				raarty = p6.GetActorsByType("raarty")
				tnk1 = p6.GetActorsByType("1tnk")
				rajeep = p6.GetActorsByType("rajeep")
				tnk4 = p6.GetActorsByType("4tnk")
				v2rl = p6.GetActorsByType("v2rl")
				tnk3 = p6.GetActorsByType("3tnk")
				ttnk = p6.GetActorsByType("ttnk")
				ftrk = p6.GetActorsByType("ftrk")
				raapc = p6.GetActorsByType("raapc")
				hpt2100 = p6.GetActorsByType("2100hpt")
				apt2100 = p6.GetActorsByType("2100apt")
				mch2100 = p6.GetActorsByType("2100mch")
				tch2100 = p6.GetActorsByType("2100tch")
				rvw2100 = p6.GetActorsByType("2100rvw")
				mvw2100 = p6.GetActorsByType("2100mvw")
				rvw22100 = p6.GetActorsByType("2100rvw2")
				mvw22100 = p6.GetActorsByType("2100mvw2")
				scgoliath = p6.GetActorsByType("scgoliath")
				scvulture = p6.GetActorsByType("scvulture")
				scsiegetank = p6.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p7 and p1.IsAlliedWith(p7) then
				htnk = p7.GetActorsByType("htnk")
				msam = p7.GetActorsByType("msam")
				mtnk = p7.GetActorsByType("mtnk")
				apc = p7.GetActorsByType("apc")
				jeep = p7.GetActorsByType("jeep")
				stnk = p7.GetActorsByType("stnk")
				arty = p7.GetActorsByType("arty")
				ltnk = p7.GetActorsByType("ltnk")
				ftnk = p7.GetActorsByType("ftnk")
				mlrs = p7.GetActorsByType("mlrs")
				bike = p7.GetActorsByType("bike")
				bggy = p7.GetActorsByType("bggy")
				ctnk = p7.GetActorsByType("ctnk")
				tnk2 = p7.GetActorsByType("2tnk")
				raarty = p7.GetActorsByType("raarty")
				tnk1 = p7.GetActorsByType("1tnk")
				rajeep = p7.GetActorsByType("rajeep")
				tnk4 = p7.GetActorsByType("4tnk")
				v2rl = p7.GetActorsByType("v2rl")
				tnk3 = p7.GetActorsByType("3tnk")
				ttnk = p7.GetActorsByType("ttnk")
				ftrk = p7.GetActorsByType("ftrk")
				raapc = p7.GetActorsByType("raapc")
				hpt2100 = p7.GetActorsByType("2100hpt")
				apt2100 = p7.GetActorsByType("2100apt")
				mch2100 = p7.GetActorsByType("2100mch")
				tch2100 = p7.GetActorsByType("2100tch")
				rvw2100 = p7.GetActorsByType("2100rvw")
				mvw2100 = p7.GetActorsByType("2100mvw")
				rvw22100 = p7.GetActorsByType("2100rvw2")
				mvw22100 = p7.GetActorsByType("2100mvw2")
				scgoliath = p7.GetActorsByType("scgoliath")
				scvulture = p7.GetActorsByType("scvulture")
				scsiegetank = p7.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p8 and p1.IsAlliedWith(p8) then
				htnk = p8.GetActorsByType("htnk")
				msam = p8.GetActorsByType("msam")
				mtnk = p8.GetActorsByType("mtnk")
				apc = p8.GetActorsByType("apc")
				jeep = p8.GetActorsByType("jeep")
				stnk = p8.GetActorsByType("stnk")
				arty = p8.GetActorsByType("arty")
				ltnk = p8.GetActorsByType("ltnk")
				ftnk = p8.GetActorsByType("ftnk")
				mlrs = p8.GetActorsByType("mlrs")
				bike = p8.GetActorsByType("bike")
				bggy = p8.GetActorsByType("bggy")
				ctnk = p8.GetActorsByType("ctnk")
				tnk2 = p8.GetActorsByType("2tnk")
				raarty = p8.GetActorsByType("raarty")
				tnk1 = p8.GetActorsByType("1tnk")
				rajeep = p8.GetActorsByType("rajeep")
				tnk4 = p8.GetActorsByType("4tnk")
				v2rl = p8.GetActorsByType("v2rl")
				tnk3 = p8.GetActorsByType("3tnk")
				ttnk = p8.GetActorsByType("ttnk")
				ftrk = p8.GetActorsByType("ftrk")
				raapc = p8.GetActorsByType("raapc")
				hpt2100 = p8.GetActorsByType("2100hpt")
				apt2100 = p8.GetActorsByType("2100apt")
				mch2100 = p8.GetActorsByType("2100mch")
				tch2100 = p8.GetActorsByType("2100tch")
				rvw2100 = p8.GetActorsByType("2100rvw")
				mvw2100 = p8.GetActorsByType("2100mvw")
				rvw22100 = p8.GetActorsByType("2100rvw2")
				mvw22100 = p8.GetActorsByType("2100mvw2")
				scgoliath = p8.GetActorsByType("scgoliath")
				scvulture = p8.GetActorsByType("scvulture")
				scsiegetank = p8.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p9 and p1.IsAlliedWith(p9) then
				htnk = p9.GetActorsByType("htnk")
				msam = p9.GetActorsByType("msam")
				mtnk = p9.GetActorsByType("mtnk")
				apc = p9.GetActorsByType("apc")
				jeep = p9.GetActorsByType("jeep")
				stnk = p9.GetActorsByType("stnk")
				arty = p9.GetActorsByType("arty")
				ltnk = p9.GetActorsByType("ltnk")
				ftnk = p9.GetActorsByType("ftnk")
				mlrs = p9.GetActorsByType("mlrs")
				bike = p9.GetActorsByType("bike")
				bggy = p9.GetActorsByType("bggy")
				ctnk = p9.GetActorsByType("ctnk")
				tnk2 = p9.GetActorsByType("2tnk")
				raarty = p9.GetActorsByType("raarty")
				tnk1 = p9.GetActorsByType("1tnk")
				rajeep = p9.GetActorsByType("rajeep")
				tnk4 = p9.GetActorsByType("4tnk")
				v2rl = p9.GetActorsByType("v2rl")
				tnk3 = p9.GetActorsByType("3tnk")
				ttnk = p9.GetActorsByType("ttnk")
				ftrk = p9.GetActorsByType("ftrk")
				raapc = p9.GetActorsByType("raapc")
				hpt2100 = p9.GetActorsByType("2100hpt")
				apt2100 = p9.GetActorsByType("2100apt")
				mch2100 = p9.GetActorsByType("2100mch")
				tch2100 = p9.GetActorsByType("2100tch")
				rvw2100 = p9.GetActorsByType("2100rvw")
				mvw2100 = p9.GetActorsByType("2100mvw")
				rvw22100 = p9.GetActorsByType("2100rvw2")
				mvw22100 = p9.GetActorsByType("2100mvw2")
				scgoliath = p9.GetActorsByType("scgoliath")
				scvulture = p9.GetActorsByType("scvulture")
				scsiegetank = p9.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p10 and p1.IsAlliedWith(p10) then
				htnk = p10.GetActorsByType("htnk")
				msam = p10.GetActorsByType("msam")
				mtnk = p10.GetActorsByType("mtnk")
				apc = p10.GetActorsByType("apc")
				jeep = p10.GetActorsByType("jeep")
				stnk = p10.GetActorsByType("stnk")
				arty = p10.GetActorsByType("arty")
				ltnk = p10.GetActorsByType("ltnk")
				ftnk = p10.GetActorsByType("ftnk")
				mlrs = p10.GetActorsByType("mlrs")
				bike = p10.GetActorsByType("bike")
				bggy = p10.GetActorsByType("bggy")
				ctnk = p10.GetActorsByType("ctnk")
				tnk2 = p10.GetActorsByType("2tnk")
				raarty = p10.GetActorsByType("raarty")
				tnk1 = p10.GetActorsByType("1tnk")
				rajeep = p10.GetActorsByType("rajeep")
				tnk4 = p10.GetActorsByType("4tnk")
				v2rl = p10.GetActorsByType("v2rl")
				tnk3 = p10.GetActorsByType("3tnk")
				ttnk = p10.GetActorsByType("ttnk")
				ftrk = p10.GetActorsByType("ftrk")
				raapc = p10.GetActorsByType("raapc")
				hpt2100 = p10.GetActorsByType("2100hpt")
				apt2100 = p10.GetActorsByType("2100apt")
				mch2100 = p10.GetActorsByType("2100mch")
				tch2100 = p10.GetActorsByType("2100tch")
				rvw2100 = p10.GetActorsByType("2100rvw")
				mvw2100 = p10.GetActorsByType("2100mvw")
				rvw22100 = p10.GetActorsByType("2100rvw2")
				mvw22100 = p10.GetActorsByType("2100mvw2")
				scgoliath = p10.GetActorsByType("scgoliath")
				scvulture = p10.GetActorsByType("scvulture")
				scsiegetank = p10.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p11 and p1.IsAlliedWith(p11) then
				htnk = p11.GetActorsByType("htnk")
				msam = p11.GetActorsByType("msam")
				mtnk = p11.GetActorsByType("mtnk")
				apc = p11.GetActorsByType("apc")
				jeep = p11.GetActorsByType("jeep")
				stnk = p11.GetActorsByType("stnk")
				arty = p11.GetActorsByType("arty")
				ltnk = p11.GetActorsByType("ltnk")
				ftnk = p11.GetActorsByType("ftnk")
				mlrs = p11.GetActorsByType("mlrs")
				bike = p11.GetActorsByType("bike")
				bggy = p11.GetActorsByType("bggy")
				ctnk = p11.GetActorsByType("ctnk")
				tnk2 = p11.GetActorsByType("2tnk")
				raarty = p11.GetActorsByType("raarty")
				tnk1 = p11.GetActorsByType("1tnk")
				rajeep = p11.GetActorsByType("rajeep")
				tnk4 = p11.GetActorsByType("4tnk")
				v2rl = p11.GetActorsByType("v2rl")
				tnk3 = p11.GetActorsByType("3tnk")
				ttnk = p11.GetActorsByType("ttnk")
				ftrk = p11.GetActorsByType("ftrk")
				raapc = p11.GetActorsByType("raapc")
				hpt2100 = p11.GetActorsByType("2100hpt")
				apt2100 = p11.GetActorsByType("2100apt")
				mch2100 = p11.GetActorsByType("2100mch")
				tch2100 = p11.GetActorsByType("2100tch")
				rvw2100 = p11.GetActorsByType("2100rvw")
				mvw2100 = p11.GetActorsByType("2100mvw")
				rvw22100 = p11.GetActorsByType("2100rvw2")
				mvw22100 = p11.GetActorsByType("2100mvw2")
				scgoliath = p11.GetActorsByType("scgoliath")
				scvulture = p11.GetActorsByType("scvulture")
				scsiegetank = p11.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p12 and p1.IsAlliedWith(p12) then
				htnk = p12.GetActorsByType("htnk")
				msam = p12.GetActorsByType("msam")
				mtnk = p12.GetActorsByType("mtnk")
				apc = p12.GetActorsByType("apc")
				jeep = p12.GetActorsByType("jeep")
				stnk = p12.GetActorsByType("stnk")
				arty = p12.GetActorsByType("arty")
				ltnk = p12.GetActorsByType("ltnk")
				ftnk = p12.GetActorsByType("ftnk")
				mlrs = p12.GetActorsByType("mlrs")
				bike = p12.GetActorsByType("bike")
				bggy = p12.GetActorsByType("bggy")
				ctnk = p12.GetActorsByType("ctnk")
				tnk2 = p12.GetActorsByType("2tnk")
				raarty = p12.GetActorsByType("raarty")
				tnk1 = p12.GetActorsByType("1tnk")
				rajeep = p12.GetActorsByType("rajeep")
				tnk4 = p12.GetActorsByType("4tnk")
				v2rl = p12.GetActorsByType("v2rl")
				tnk3 = p12.GetActorsByType("3tnk")
				ttnk = p12.GetActorsByType("ttnk")
				ftrk = p12.GetActorsByType("ftrk")
				raapc = p12.GetActorsByType("raapc")
				hpt2100 = p12.GetActorsByType("2100hpt")
				apt2100 = p12.GetActorsByType("2100apt")
				mch2100 = p12.GetActorsByType("2100mch")
				tch2100 = p12.GetActorsByType("2100tch")
				rvw2100 = p12.GetActorsByType("2100rvw")
				mvw2100 = p12.GetActorsByType("2100mvw")
				rvw22100 = p12.GetActorsByType("2100rvw2")
				mvw22100 = p12.GetActorsByType("2100mvw2")
				scgoliath = p12.GetActorsByType("scgoliath")
				scvulture = p12.GetActorsByType("scvulture")
				scsiegetank = p12.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p13 and p1.IsAlliedWith(p13) then
				htnk = p13.GetActorsByType("htnk")
				msam = p13.GetActorsByType("msam")
				mtnk = p13.GetActorsByType("mtnk")
				apc = p13.GetActorsByType("apc")
				jeep = p13.GetActorsByType("jeep")
				stnk = p13.GetActorsByType("stnk")
				arty = p13.GetActorsByType("arty")
				ltnk = p13.GetActorsByType("ltnk")
				ftnk = p13.GetActorsByType("ftnk")
				mlrs = p13.GetActorsByType("mlrs")
				bike = p13.GetActorsByType("bike")
				bggy = p13.GetActorsByType("bggy")
				ctnk = p13.GetActorsByType("ctnk")
				tnk2 = p13.GetActorsByType("2tnk")
				raarty = p13.GetActorsByType("raarty")
				tnk1 = p13.GetActorsByType("1tnk")
				rajeep = p13.GetActorsByType("rajeep")
				tnk4 = p13.GetActorsByType("4tnk")
				v2rl = p13.GetActorsByType("v2rl")
				tnk3 = p13.GetActorsByType("3tnk")
				ttnk = p13.GetActorsByType("ttnk")
				ftrk = p13.GetActorsByType("ftrk")
				raapc = p13.GetActorsByType("raapc")
				hpt2100 = p13.GetActorsByType("2100hpt")
				apt2100 = p13.GetActorsByType("2100apt")
				mch2100 = p13.GetActorsByType("2100mch")
				tch2100 = p13.GetActorsByType("2100tch")
				rvw2100 = p13.GetActorsByType("2100rvw")
				mvw2100 = p13.GetActorsByType("2100mvw")
				rvw22100 = p13.GetActorsByType("2100rvw2")
				mvw22100 = p13.GetActorsByType("2100mvw2")
				scgoliath = p13.GetActorsByType("scgoliath")
				scvulture = p13.GetActorsByType("scvulture")
				scsiegetank = p13.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p14 and p1.IsAlliedWith(p14) then
				htnk = p14.GetActorsByType("htnk")
				msam = p14.GetActorsByType("msam")
				mtnk = p14.GetActorsByType("mtnk")
				apc = p14.GetActorsByType("apc")
				jeep = p14.GetActorsByType("jeep")
				stnk = p14.GetActorsByType("stnk")
				arty = p14.GetActorsByType("arty")
				ltnk = p14.GetActorsByType("ltnk")
				ftnk = p14.GetActorsByType("ftnk")
				mlrs = p14.GetActorsByType("mlrs")
				bike = p14.GetActorsByType("bike")
				bggy = p14.GetActorsByType("bggy")
				ctnk = p14.GetActorsByType("ctnk")
				tnk2 = p14.GetActorsByType("2tnk")
				raarty = p14.GetActorsByType("raarty")
				tnk1 = p14.GetActorsByType("1tnk")
				rajeep = p14.GetActorsByType("rajeep")
				tnk4 = p14.GetActorsByType("4tnk")
				v2rl = p14.GetActorsByType("v2rl")
				tnk3 = p14.GetActorsByType("3tnk")
				ttnk = p14.GetActorsByType("ttnk")
				ftrk = p14.GetActorsByType("ftrk")
				raapc = p14.GetActorsByType("raapc")
				hpt2100 = p14.GetActorsByType("2100hpt")
				apt2100 = p14.GetActorsByType("2100apt")
				mch2100 = p14.GetActorsByType("2100mch")
				tch2100 = p14.GetActorsByType("2100tch")
				rvw2100 = p14.GetActorsByType("2100rvw")
				mvw2100 = p14.GetActorsByType("2100mvw")
				rvw22100 = p14.GetActorsByType("2100rvw2")
				mvw22100 = p14.GetActorsByType("2100mvw2")
				scgoliath = p14.GetActorsByType("scgoliath")
				scvulture = p14.GetActorsByType("scvulture")
				scsiegetank = p14.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p15 and p1.IsAlliedWith(p15) then
				htnk = p15.GetActorsByType("htnk")
				msam = p15.GetActorsByType("msam")
				mtnk = p15.GetActorsByType("mtnk")
				apc = p15.GetActorsByType("apc")
				jeep = p15.GetActorsByType("jeep")
				stnk = p15.GetActorsByType("stnk")
				arty = p15.GetActorsByType("arty")
				ltnk = p15.GetActorsByType("ltnk")
				ftnk = p15.GetActorsByType("ftnk")
				mlrs = p15.GetActorsByType("mlrs")
				bike = p15.GetActorsByType("bike")
				bggy = p15.GetActorsByType("bggy")
				ctnk = p15.GetActorsByType("ctnk")
				tnk2 = p15.GetActorsByType("2tnk")
				raarty = p15.GetActorsByType("raarty")
				tnk1 = p15.GetActorsByType("1tnk")
				rajeep = p15.GetActorsByType("rajeep")
				tnk4 = p15.GetActorsByType("4tnk")
				v2rl = p15.GetActorsByType("v2rl")
				tnk3 = p15.GetActorsByType("3tnk")
				ttnk = p15.GetActorsByType("ttnk")
				ftrk = p15.GetActorsByType("ftrk")
				raapc = p15.GetActorsByType("raapc")
				hpt2100 = p15.GetActorsByType("2100hpt")
				apt2100 = p15.GetActorsByType("2100apt")
				mch2100 = p15.GetActorsByType("2100mch")
				tch2100 = p15.GetActorsByType("2100tch")
				rvw2100 = p15.GetActorsByType("2100rvw")
				mvw2100 = p15.GetActorsByType("2100mvw")
				rvw22100 = p15.GetActorsByType("2100rvw2")
				mvw22100 = p15.GetActorsByType("2100mvw2")
				scgoliath = p15.GetActorsByType("scgoliath")
				scvulture = p15.GetActorsByType("scvulture")
				scsiegetank = p15.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p16 and p1.IsAlliedWith(p16) then
				htnk = p16.GetActorsByType("htnk")
				msam = p16.GetActorsByType("msam")
				mtnk = p16.GetActorsByType("mtnk")
				apc = p16.GetActorsByType("apc")
				jeep = p16.GetActorsByType("jeep")
				stnk = p16.GetActorsByType("stnk")
				arty = p16.GetActorsByType("arty")
				ltnk = p16.GetActorsByType("ltnk")
				ftnk = p16.GetActorsByType("ftnk")
				mlrs = p16.GetActorsByType("mlrs")
				bike = p16.GetActorsByType("bike")
				bggy = p16.GetActorsByType("bggy")
				ctnk = p16.GetActorsByType("ctnk")
				tnk2 = p16.GetActorsByType("2tnk")
				raarty = p16.GetActorsByType("raarty")
				tnk1 = p16.GetActorsByType("1tnk")
				rajeep = p16.GetActorsByType("rajeep")
				tnk4 = p16.GetActorsByType("4tnk")
				v2rl = p16.GetActorsByType("v2rl")
				tnk3 = p16.GetActorsByType("3tnk")
				ttnk = p16.GetActorsByType("ttnk")
				ftrk = p16.GetActorsByType("ftrk")
				raapc = p16.GetActorsByType("raapc")
				hpt2100 = p16.GetActorsByType("2100hpt")
				apt2100 = p16.GetActorsByType("2100apt")
				mch2100 = p16.GetActorsByType("2100mch")
				tch2100 = p16.GetActorsByType("2100tch")
				rvw2100 = p16.GetActorsByType("2100rvw")
				mvw2100 = p16.GetActorsByType("2100mvw")
				rvw22100 = p16.GetActorsByType("2100rvw2")
				mvw22100 = p16.GetActorsByType("2100mvw2")
				scgoliath = p16.GetActorsByType("scgoliath")
				scvulture = p16.GetActorsByType("scvulture")
				scsiegetank = p16.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p1.GetActorsByType("citypolicecar")
				cityfiretruck = p1.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p1vehicle()
	end)
end
p2vehicle = function()
	if p2 and p2.IsBot then
		cityboteliteexpert = p2.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p3 and p2.IsAlliedWith(p3) then
				htnk = p3.GetActorsByType("htnk")
				msam = p3.GetActorsByType("msam")
				mtnk = p3.GetActorsByType("mtnk")
				apc = p3.GetActorsByType("apc")
				jeep = p3.GetActorsByType("jeep")
				stnk = p3.GetActorsByType("stnk")
				arty = p3.GetActorsByType("arty")
				ltnk = p3.GetActorsByType("ltnk")
				ftnk = p3.GetActorsByType("ftnk")
				mlrs = p3.GetActorsByType("mlrs")
				bike = p3.GetActorsByType("bike")
				bggy = p3.GetActorsByType("bggy")
				ctnk = p3.GetActorsByType("ctnk")
				tnk2 = p3.GetActorsByType("2tnk")
				raarty = p3.GetActorsByType("raarty")
				tnk1 = p3.GetActorsByType("1tnk")
				rajeep = p3.GetActorsByType("rajeep")
				tnk4 = p3.GetActorsByType("4tnk")
				v2rl = p3.GetActorsByType("v2rl")
				tnk3 = p3.GetActorsByType("3tnk")
				ttnk = p3.GetActorsByType("ttnk")
				ftrk = p3.GetActorsByType("ftrk")
				raapc = p3.GetActorsByType("raapc")
				hpt2100 = p3.GetActorsByType("2100hpt")
				apt2100 = p3.GetActorsByType("2100apt")
				mch2100 = p3.GetActorsByType("2100mch")
				tch2100 = p3.GetActorsByType("2100tch")
				rvw2100 = p3.GetActorsByType("2100rvw")
				mvw2100 = p3.GetActorsByType("2100mvw")
				rvw22100 = p3.GetActorsByType("2100rvw2")
				mvw22100 = p3.GetActorsByType("2100mvw2")
				scgoliath = p3.GetActorsByType("scgoliath")
				scvulture = p3.GetActorsByType("scvulture")
				scsiegetank = p3.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p4 and p2.IsAlliedWith(p4) then
				htnk = p4.GetActorsByType("htnk")
				msam = p4.GetActorsByType("msam")
				mtnk = p4.GetActorsByType("mtnk")
				apc = p4.GetActorsByType("apc")
				jeep = p4.GetActorsByType("jeep")
				stnk = p4.GetActorsByType("stnk")
				arty = p4.GetActorsByType("arty")
				ltnk = p4.GetActorsByType("ltnk")
				ftnk = p4.GetActorsByType("ftnk")
				mlrs = p4.GetActorsByType("mlrs")
				bike = p4.GetActorsByType("bike")
				bggy = p4.GetActorsByType("bggy")
				ctnk = p4.GetActorsByType("ctnk")
				tnk2 = p4.GetActorsByType("2tnk")
				raarty = p4.GetActorsByType("raarty")
				tnk1 = p4.GetActorsByType("1tnk")
				rajeep = p4.GetActorsByType("rajeep")
				tnk4 = p4.GetActorsByType("4tnk")
				v2rl = p4.GetActorsByType("v2rl")
				tnk3 = p4.GetActorsByType("3tnk")
				ttnk = p4.GetActorsByType("ttnk")
				ftrk = p4.GetActorsByType("ftrk")
				raapc = p4.GetActorsByType("raapc")
				hpt2100 = p4.GetActorsByType("2100hpt")
				apt2100 = p4.GetActorsByType("2100apt")
				mch2100 = p4.GetActorsByType("2100mch")
				tch2100 = p4.GetActorsByType("2100tch")
				rvw2100 = p4.GetActorsByType("2100rvw")
				mvw2100 = p4.GetActorsByType("2100mvw")
				rvw22100 = p4.GetActorsByType("2100rvw2")
				mvw22100 = p4.GetActorsByType("2100mvw2")
				scgoliath = p4.GetActorsByType("scgoliath")
				scvulture = p4.GetActorsByType("scvulture")
				scsiegetank = p4.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p5 and p2.IsAlliedWith(p5) then
				htnk = p5.GetActorsByType("htnk")
				msam = p5.GetActorsByType("msam")
				mtnk = p5.GetActorsByType("mtnk")
				apc = p5.GetActorsByType("apc")
				jeep = p5.GetActorsByType("jeep")
				stnk = p5.GetActorsByType("stnk")
				arty = p5.GetActorsByType("arty")
				ltnk = p5.GetActorsByType("ltnk")
				ftnk = p5.GetActorsByType("ftnk")
				mlrs = p5.GetActorsByType("mlrs")
				bike = p5.GetActorsByType("bike")
				bggy = p5.GetActorsByType("bggy")
				ctnk = p5.GetActorsByType("ctnk")
				tnk2 = p5.GetActorsByType("2tnk")
				raarty = p5.GetActorsByType("raarty")
				tnk1 = p5.GetActorsByType("1tnk")
				rajeep = p5.GetActorsByType("rajeep")
				tnk4 = p5.GetActorsByType("4tnk")
				v2rl = p5.GetActorsByType("v2rl")
				tnk3 = p5.GetActorsByType("3tnk")
				ttnk = p5.GetActorsByType("ttnk")
				ftrk = p5.GetActorsByType("ftrk")
				raapc = p5.GetActorsByType("raapc")
				hpt2100 = p5.GetActorsByType("2100hpt")
				apt2100 = p5.GetActorsByType("2100apt")
				mch2100 = p5.GetActorsByType("2100mch")
				tch2100 = p5.GetActorsByType("2100tch")
				rvw2100 = p5.GetActorsByType("2100rvw")
				mvw2100 = p5.GetActorsByType("2100mvw")
				rvw22100 = p5.GetActorsByType("2100rvw2")
				mvw22100 = p5.GetActorsByType("2100mvw2")
				scgoliath = p5.GetActorsByType("scgoliath")
				scvulture = p5.GetActorsByType("scvulture")
				scsiegetank = p5.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p6 and p2.IsAlliedWith(p6) then
				htnk = p6.GetActorsByType("htnk")
				msam = p6.GetActorsByType("msam")
				mtnk = p6.GetActorsByType("mtnk")
				apc = p6.GetActorsByType("apc")
				jeep = p6.GetActorsByType("jeep")
				stnk = p6.GetActorsByType("stnk")
				arty = p6.GetActorsByType("arty")
				ltnk = p6.GetActorsByType("ltnk")
				ftnk = p6.GetActorsByType("ftnk")
				mlrs = p6.GetActorsByType("mlrs")
				bike = p6.GetActorsByType("bike")
				bggy = p6.GetActorsByType("bggy")
				ctnk = p6.GetActorsByType("ctnk")
				tnk2 = p6.GetActorsByType("2tnk")
				raarty = p6.GetActorsByType("raarty")
				tnk1 = p6.GetActorsByType("1tnk")
				rajeep = p6.GetActorsByType("rajeep")
				tnk4 = p6.GetActorsByType("4tnk")
				v2rl = p6.GetActorsByType("v2rl")
				tnk3 = p6.GetActorsByType("3tnk")
				ttnk = p6.GetActorsByType("ttnk")
				ftrk = p6.GetActorsByType("ftrk")
				raapc = p6.GetActorsByType("raapc")
				hpt2100 = p6.GetActorsByType("2100hpt")
				apt2100 = p6.GetActorsByType("2100apt")
				mch2100 = p6.GetActorsByType("2100mch")
				tch2100 = p6.GetActorsByType("2100tch")
				rvw2100 = p6.GetActorsByType("2100rvw")
				mvw2100 = p6.GetActorsByType("2100mvw")
				rvw22100 = p6.GetActorsByType("2100rvw2")
				mvw22100 = p6.GetActorsByType("2100mvw2")
				scgoliath = p6.GetActorsByType("scgoliath")
				scvulture = p6.GetActorsByType("scvulture")
				scsiegetank = p6.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p7 and p2.IsAlliedWith(p7) then
				htnk = p7.GetActorsByType("htnk")
				msam = p7.GetActorsByType("msam")
				mtnk = p7.GetActorsByType("mtnk")
				apc = p7.GetActorsByType("apc")
				jeep = p7.GetActorsByType("jeep")
				stnk = p7.GetActorsByType("stnk")
				arty = p7.GetActorsByType("arty")
				ltnk = p7.GetActorsByType("ltnk")
				ftnk = p7.GetActorsByType("ftnk")
				mlrs = p7.GetActorsByType("mlrs")
				bike = p7.GetActorsByType("bike")
				bggy = p7.GetActorsByType("bggy")
				ctnk = p7.GetActorsByType("ctnk")
				tnk2 = p7.GetActorsByType("2tnk")
				raarty = p7.GetActorsByType("raarty")
				tnk1 = p7.GetActorsByType("1tnk")
				rajeep = p7.GetActorsByType("rajeep")
				tnk4 = p7.GetActorsByType("4tnk")
				v2rl = p7.GetActorsByType("v2rl")
				tnk3 = p7.GetActorsByType("3tnk")
				ttnk = p7.GetActorsByType("ttnk")
				ftrk = p7.GetActorsByType("ftrk")
				raapc = p7.GetActorsByType("raapc")
				hpt2100 = p7.GetActorsByType("2100hpt")
				apt2100 = p7.GetActorsByType("2100apt")
				mch2100 = p7.GetActorsByType("2100mch")
				tch2100 = p7.GetActorsByType("2100tch")
				rvw2100 = p7.GetActorsByType("2100rvw")
				mvw2100 = p7.GetActorsByType("2100mvw")
				rvw22100 = p7.GetActorsByType("2100rvw2")
				mvw22100 = p7.GetActorsByType("2100mvw2")
				scgoliath = p7.GetActorsByType("scgoliath")
				scvulture = p7.GetActorsByType("scvulture")
				scsiegetank = p7.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p8 and p2.IsAlliedWith(p8) then
				htnk = p8.GetActorsByType("htnk")
				msam = p8.GetActorsByType("msam")
				mtnk = p8.GetActorsByType("mtnk")
				apc = p8.GetActorsByType("apc")
				jeep = p8.GetActorsByType("jeep")
				stnk = p8.GetActorsByType("stnk")
				arty = p8.GetActorsByType("arty")
				ltnk = p8.GetActorsByType("ltnk")
				ftnk = p8.GetActorsByType("ftnk")
				mlrs = p8.GetActorsByType("mlrs")
				bike = p8.GetActorsByType("bike")
				bggy = p8.GetActorsByType("bggy")
				ctnk = p8.GetActorsByType("ctnk")
				tnk2 = p8.GetActorsByType("2tnk")
				raarty = p8.GetActorsByType("raarty")
				tnk1 = p8.GetActorsByType("1tnk")
				rajeep = p8.GetActorsByType("rajeep")
				tnk4 = p8.GetActorsByType("4tnk")
				v2rl = p8.GetActorsByType("v2rl")
				tnk3 = p8.GetActorsByType("3tnk")
				ttnk = p8.GetActorsByType("ttnk")
				ftrk = p8.GetActorsByType("ftrk")
				raapc = p8.GetActorsByType("raapc")
				hpt2100 = p8.GetActorsByType("2100hpt")
				apt2100 = p8.GetActorsByType("2100apt")
				mch2100 = p8.GetActorsByType("2100mch")
				tch2100 = p8.GetActorsByType("2100tch")
				rvw2100 = p8.GetActorsByType("2100rvw")
				mvw2100 = p8.GetActorsByType("2100mvw")
				rvw22100 = p8.GetActorsByType("2100rvw2")
				mvw22100 = p8.GetActorsByType("2100mvw2")
				scgoliath = p8.GetActorsByType("scgoliath")
				scvulture = p8.GetActorsByType("scvulture")
				scsiegetank = p8.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p9 and p2.IsAlliedWith(p9) then
				htnk = p9.GetActorsByType("htnk")
				msam = p9.GetActorsByType("msam")
				mtnk = p9.GetActorsByType("mtnk")
				apc = p9.GetActorsByType("apc")
				jeep = p9.GetActorsByType("jeep")
				stnk = p9.GetActorsByType("stnk")
				arty = p9.GetActorsByType("arty")
				ltnk = p9.GetActorsByType("ltnk")
				ftnk = p9.GetActorsByType("ftnk")
				mlrs = p9.GetActorsByType("mlrs")
				bike = p9.GetActorsByType("bike")
				bggy = p9.GetActorsByType("bggy")
				ctnk = p9.GetActorsByType("ctnk")
				tnk2 = p9.GetActorsByType("2tnk")
				raarty = p9.GetActorsByType("raarty")
				tnk1 = p9.GetActorsByType("1tnk")
				rajeep = p9.GetActorsByType("rajeep")
				tnk4 = p9.GetActorsByType("4tnk")
				v2rl = p9.GetActorsByType("v2rl")
				tnk3 = p9.GetActorsByType("3tnk")
				ttnk = p9.GetActorsByType("ttnk")
				ftrk = p9.GetActorsByType("ftrk")
				raapc = p9.GetActorsByType("raapc")
				hpt2100 = p9.GetActorsByType("2100hpt")
				apt2100 = p9.GetActorsByType("2100apt")
				mch2100 = p9.GetActorsByType("2100mch")
				tch2100 = p9.GetActorsByType("2100tch")
				rvw2100 = p9.GetActorsByType("2100rvw")
				mvw2100 = p9.GetActorsByType("2100mvw")
				rvw22100 = p9.GetActorsByType("2100rvw2")
				mvw22100 = p9.GetActorsByType("2100mvw2")
				scgoliath = p9.GetActorsByType("scgoliath")
				scvulture = p9.GetActorsByType("scvulture")
				scsiegetank = p9.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p10 and p2.IsAlliedWith(p10) then
				htnk = p10.GetActorsByType("htnk")
				msam = p10.GetActorsByType("msam")
				mtnk = p10.GetActorsByType("mtnk")
				apc = p10.GetActorsByType("apc")
				jeep = p10.GetActorsByType("jeep")
				stnk = p10.GetActorsByType("stnk")
				arty = p10.GetActorsByType("arty")
				ltnk = p10.GetActorsByType("ltnk")
				ftnk = p10.GetActorsByType("ftnk")
				mlrs = p10.GetActorsByType("mlrs")
				bike = p10.GetActorsByType("bike")
				bggy = p10.GetActorsByType("bggy")
				ctnk = p10.GetActorsByType("ctnk")
				tnk2 = p10.GetActorsByType("2tnk")
				raarty = p10.GetActorsByType("raarty")
				tnk1 = p10.GetActorsByType("1tnk")
				rajeep = p10.GetActorsByType("rajeep")
				tnk4 = p10.GetActorsByType("4tnk")
				v2rl = p10.GetActorsByType("v2rl")
				tnk3 = p10.GetActorsByType("3tnk")
				ttnk = p10.GetActorsByType("ttnk")
				ftrk = p10.GetActorsByType("ftrk")
				raapc = p10.GetActorsByType("raapc")
				hpt2100 = p10.GetActorsByType("2100hpt")
				apt2100 = p10.GetActorsByType("2100apt")
				mch2100 = p10.GetActorsByType("2100mch")
				tch2100 = p10.GetActorsByType("2100tch")
				rvw2100 = p10.GetActorsByType("2100rvw")
				mvw2100 = p10.GetActorsByType("2100mvw")
				rvw22100 = p10.GetActorsByType("2100rvw2")
				mvw22100 = p10.GetActorsByType("2100mvw2")
				scgoliath = p10.GetActorsByType("scgoliath")
				scvulture = p10.GetActorsByType("scvulture")
				scsiegetank = p10.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p11 and p2.IsAlliedWith(p11) then
				htnk = p11.GetActorsByType("htnk")
				msam = p11.GetActorsByType("msam")
				mtnk = p11.GetActorsByType("mtnk")
				apc = p11.GetActorsByType("apc")
				jeep = p11.GetActorsByType("jeep")
				stnk = p11.GetActorsByType("stnk")
				arty = p11.GetActorsByType("arty")
				ltnk = p11.GetActorsByType("ltnk")
				ftnk = p11.GetActorsByType("ftnk")
				mlrs = p11.GetActorsByType("mlrs")
				bike = p11.GetActorsByType("bike")
				bggy = p11.GetActorsByType("bggy")
				ctnk = p11.GetActorsByType("ctnk")
				tnk2 = p11.GetActorsByType("2tnk")
				raarty = p11.GetActorsByType("raarty")
				tnk1 = p11.GetActorsByType("1tnk")
				rajeep = p11.GetActorsByType("rajeep")
				tnk4 = p11.GetActorsByType("4tnk")
				v2rl = p11.GetActorsByType("v2rl")
				tnk3 = p11.GetActorsByType("3tnk")
				ttnk = p11.GetActorsByType("ttnk")
				ftrk = p11.GetActorsByType("ftrk")
				raapc = p11.GetActorsByType("raapc")
				hpt2100 = p11.GetActorsByType("2100hpt")
				apt2100 = p11.GetActorsByType("2100apt")
				mch2100 = p11.GetActorsByType("2100mch")
				tch2100 = p11.GetActorsByType("2100tch")
				rvw2100 = p11.GetActorsByType("2100rvw")
				mvw2100 = p11.GetActorsByType("2100mvw")
				rvw22100 = p11.GetActorsByType("2100rvw2")
				mvw22100 = p11.GetActorsByType("2100mvw2")
				scgoliath = p11.GetActorsByType("scgoliath")
				scvulture = p11.GetActorsByType("scvulture")
				scsiegetank = p11.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p12 and p2.IsAlliedWith(p12) then
				htnk = p12.GetActorsByType("htnk")
				msam = p12.GetActorsByType("msam")
				mtnk = p12.GetActorsByType("mtnk")
				apc = p12.GetActorsByType("apc")
				jeep = p12.GetActorsByType("jeep")
				stnk = p12.GetActorsByType("stnk")
				arty = p12.GetActorsByType("arty")
				ltnk = p12.GetActorsByType("ltnk")
				ftnk = p12.GetActorsByType("ftnk")
				mlrs = p12.GetActorsByType("mlrs")
				bike = p12.GetActorsByType("bike")
				bggy = p12.GetActorsByType("bggy")
				ctnk = p12.GetActorsByType("ctnk")
				tnk2 = p12.GetActorsByType("2tnk")
				raarty = p12.GetActorsByType("raarty")
				tnk1 = p12.GetActorsByType("1tnk")
				rajeep = p12.GetActorsByType("rajeep")
				tnk4 = p12.GetActorsByType("4tnk")
				v2rl = p12.GetActorsByType("v2rl")
				tnk3 = p12.GetActorsByType("3tnk")
				ttnk = p12.GetActorsByType("ttnk")
				ftrk = p12.GetActorsByType("ftrk")
				raapc = p12.GetActorsByType("raapc")
				hpt2100 = p12.GetActorsByType("2100hpt")
				apt2100 = p12.GetActorsByType("2100apt")
				mch2100 = p12.GetActorsByType("2100mch")
				tch2100 = p12.GetActorsByType("2100tch")
				rvw2100 = p12.GetActorsByType("2100rvw")
				mvw2100 = p12.GetActorsByType("2100mvw")
				rvw22100 = p12.GetActorsByType("2100rvw2")
				mvw22100 = p12.GetActorsByType("2100mvw2")
				scgoliath = p12.GetActorsByType("scgoliath")
				scvulture = p12.GetActorsByType("scvulture")
				scsiegetank = p12.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p13 and p2.IsAlliedWith(p13) then
				htnk = p13.GetActorsByType("htnk")
				msam = p13.GetActorsByType("msam")
				mtnk = p13.GetActorsByType("mtnk")
				apc = p13.GetActorsByType("apc")
				jeep = p13.GetActorsByType("jeep")
				stnk = p13.GetActorsByType("stnk")
				arty = p13.GetActorsByType("arty")
				ltnk = p13.GetActorsByType("ltnk")
				ftnk = p13.GetActorsByType("ftnk")
				mlrs = p13.GetActorsByType("mlrs")
				bike = p13.GetActorsByType("bike")
				bggy = p13.GetActorsByType("bggy")
				ctnk = p13.GetActorsByType("ctnk")
				tnk2 = p13.GetActorsByType("2tnk")
				raarty = p13.GetActorsByType("raarty")
				tnk1 = p13.GetActorsByType("1tnk")
				rajeep = p13.GetActorsByType("rajeep")
				tnk4 = p13.GetActorsByType("4tnk")
				v2rl = p13.GetActorsByType("v2rl")
				tnk3 = p13.GetActorsByType("3tnk")
				ttnk = p13.GetActorsByType("ttnk")
				ftrk = p13.GetActorsByType("ftrk")
				raapc = p13.GetActorsByType("raapc")
				hpt2100 = p13.GetActorsByType("2100hpt")
				apt2100 = p13.GetActorsByType("2100apt")
				mch2100 = p13.GetActorsByType("2100mch")
				tch2100 = p13.GetActorsByType("2100tch")
				rvw2100 = p13.GetActorsByType("2100rvw")
				mvw2100 = p13.GetActorsByType("2100mvw")
				rvw22100 = p13.GetActorsByType("2100rvw2")
				mvw22100 = p13.GetActorsByType("2100mvw2")
				scgoliath = p13.GetActorsByType("scgoliath")
				scvulture = p13.GetActorsByType("scvulture")
				scsiegetank = p13.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p14 and p2.IsAlliedWith(p14) then
				htnk = p14.GetActorsByType("htnk")
				msam = p14.GetActorsByType("msam")
				mtnk = p14.GetActorsByType("mtnk")
				apc = p14.GetActorsByType("apc")
				jeep = p14.GetActorsByType("jeep")
				stnk = p14.GetActorsByType("stnk")
				arty = p14.GetActorsByType("arty")
				ltnk = p14.GetActorsByType("ltnk")
				ftnk = p14.GetActorsByType("ftnk")
				mlrs = p14.GetActorsByType("mlrs")
				bike = p14.GetActorsByType("bike")
				bggy = p14.GetActorsByType("bggy")
				ctnk = p14.GetActorsByType("ctnk")
				tnk2 = p14.GetActorsByType("2tnk")
				raarty = p14.GetActorsByType("raarty")
				tnk1 = p14.GetActorsByType("1tnk")
				rajeep = p14.GetActorsByType("rajeep")
				tnk4 = p14.GetActorsByType("4tnk")
				v2rl = p14.GetActorsByType("v2rl")
				tnk3 = p14.GetActorsByType("3tnk")
				ttnk = p14.GetActorsByType("ttnk")
				ftrk = p14.GetActorsByType("ftrk")
				raapc = p14.GetActorsByType("raapc")
				hpt2100 = p14.GetActorsByType("2100hpt")
				apt2100 = p14.GetActorsByType("2100apt")
				mch2100 = p14.GetActorsByType("2100mch")
				tch2100 = p14.GetActorsByType("2100tch")
				rvw2100 = p14.GetActorsByType("2100rvw")
				mvw2100 = p14.GetActorsByType("2100mvw")
				rvw22100 = p14.GetActorsByType("2100rvw2")
				mvw22100 = p14.GetActorsByType("2100mvw2")
				scgoliath = p14.GetActorsByType("scgoliath")
				scvulture = p14.GetActorsByType("scvulture")
				scsiegetank = p14.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p15 and p2.IsAlliedWith(p15) then
				htnk = p15.GetActorsByType("htnk")
				msam = p15.GetActorsByType("msam")
				mtnk = p15.GetActorsByType("mtnk")
				apc = p15.GetActorsByType("apc")
				jeep = p15.GetActorsByType("jeep")
				stnk = p15.GetActorsByType("stnk")
				arty = p15.GetActorsByType("arty")
				ltnk = p15.GetActorsByType("ltnk")
				ftnk = p15.GetActorsByType("ftnk")
				mlrs = p15.GetActorsByType("mlrs")
				bike = p15.GetActorsByType("bike")
				bggy = p15.GetActorsByType("bggy")
				ctnk = p15.GetActorsByType("ctnk")
				tnk2 = p15.GetActorsByType("2tnk")
				raarty = p15.GetActorsByType("raarty")
				tnk1 = p15.GetActorsByType("1tnk")
				rajeep = p15.GetActorsByType("rajeep")
				tnk4 = p15.GetActorsByType("4tnk")
				v2rl = p15.GetActorsByType("v2rl")
				tnk3 = p15.GetActorsByType("3tnk")
				ttnk = p15.GetActorsByType("ttnk")
				ftrk = p15.GetActorsByType("ftrk")
				raapc = p15.GetActorsByType("raapc")
				hpt2100 = p15.GetActorsByType("2100hpt")
				apt2100 = p15.GetActorsByType("2100apt")
				mch2100 = p15.GetActorsByType("2100mch")
				tch2100 = p15.GetActorsByType("2100tch")
				rvw2100 = p15.GetActorsByType("2100rvw")
				mvw2100 = p15.GetActorsByType("2100mvw")
				rvw22100 = p15.GetActorsByType("2100rvw2")
				mvw22100 = p15.GetActorsByType("2100mvw2")
				scgoliath = p15.GetActorsByType("scgoliath")
				scvulture = p15.GetActorsByType("scvulture")
				scsiegetank = p15.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p16 and p2.IsAlliedWith(p16) then
				htnk = p16.GetActorsByType("htnk")
				msam = p16.GetActorsByType("msam")
				mtnk = p16.GetActorsByType("mtnk")
				apc = p16.GetActorsByType("apc")
				jeep = p16.GetActorsByType("jeep")
				stnk = p16.GetActorsByType("stnk")
				arty = p16.GetActorsByType("arty")
				ltnk = p16.GetActorsByType("ltnk")
				ftnk = p16.GetActorsByType("ftnk")
				mlrs = p16.GetActorsByType("mlrs")
				bike = p16.GetActorsByType("bike")
				bggy = p16.GetActorsByType("bggy")
				ctnk = p16.GetActorsByType("ctnk")
				tnk2 = p16.GetActorsByType("2tnk")
				raarty = p16.GetActorsByType("raarty")
				tnk1 = p16.GetActorsByType("1tnk")
				rajeep = p16.GetActorsByType("rajeep")
				tnk4 = p16.GetActorsByType("4tnk")
				v2rl = p16.GetActorsByType("v2rl")
				tnk3 = p16.GetActorsByType("3tnk")
				ttnk = p16.GetActorsByType("ttnk")
				ftrk = p16.GetActorsByType("ftrk")
				raapc = p16.GetActorsByType("raapc")
				hpt2100 = p16.GetActorsByType("2100hpt")
				apt2100 = p16.GetActorsByType("2100apt")
				mch2100 = p16.GetActorsByType("2100mch")
				tch2100 = p16.GetActorsByType("2100tch")
				rvw2100 = p16.GetActorsByType("2100rvw")
				mvw2100 = p16.GetActorsByType("2100mvw")
				rvw22100 = p16.GetActorsByType("2100rvw2")
				mvw22100 = p16.GetActorsByType("2100mvw2")
				scgoliath = p16.GetActorsByType("scgoliath")
				scvulture = p16.GetActorsByType("scvulture")
				scsiegetank = p16.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p1 and p2.IsAlliedWith(p1) then
				htnk = p1.GetActorsByType("htnk")
				msam = p1.GetActorsByType("msam")
				mtnk = p1.GetActorsByType("mtnk")
				apc = p1.GetActorsByType("apc")
				jeep = p1.GetActorsByType("jeep")
				stnk = p1.GetActorsByType("stnk")
				arty = p1.GetActorsByType("arty")
				ltnk = p1.GetActorsByType("ltnk")
				ftnk = p1.GetActorsByType("ftnk")
				mlrs = p1.GetActorsByType("mlrs")
				bike = p1.GetActorsByType("bike")
				bggy = p1.GetActorsByType("bggy")
				ctnk = p1.GetActorsByType("ctnk")
				tnk2 = p1.GetActorsByType("2tnk")
				raarty = p1.GetActorsByType("raarty")
				tnk1 = p1.GetActorsByType("1tnk")
				rajeep = p1.GetActorsByType("rajeep")
				tnk4 = p1.GetActorsByType("4tnk")
				v2rl = p1.GetActorsByType("v2rl")
				tnk3 = p1.GetActorsByType("3tnk")
				ttnk = p1.GetActorsByType("ttnk")
				ftrk = p1.GetActorsByType("ftrk")
				raapc = p1.GetActorsByType("raapc")
				hpt2100 = p1.GetActorsByType("2100hpt")
				apt2100 = p1.GetActorsByType("2100apt")
				mch2100 = p1.GetActorsByType("2100mch")
				tch2100 = p1.GetActorsByType("2100tch")
				rvw2100 = p1.GetActorsByType("2100rvw")
				mvw2100 = p1.GetActorsByType("2100mvw")
				rvw22100 = p1.GetActorsByType("2100rvw2")
				mvw22100 = p1.GetActorsByType("2100mvw2")
				scgoliath = p1.GetActorsByType("scgoliath")
				scvulture = p1.GetActorsByType("scvulture")
				scsiegetank = p1.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p2.GetActorsByType("citypolicecar")
				cityfiretruck = p2.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p2vehicle()
	end)
end
p3vehicle = function()
	if p3 and p3.IsBot then
		cityboteliteexpert = p3.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p4 and p3.IsAlliedWith(p4) then
				htnk = p4.GetActorsByType("htnk")
				msam = p4.GetActorsByType("msam")
				mtnk = p4.GetActorsByType("mtnk")
				apc = p4.GetActorsByType("apc")
				jeep = p4.GetActorsByType("jeep")
				stnk = p4.GetActorsByType("stnk")
				arty = p4.GetActorsByType("arty")
				ltnk = p4.GetActorsByType("ltnk")
				ftnk = p4.GetActorsByType("ftnk")
				mlrs = p4.GetActorsByType("mlrs")
				bike = p4.GetActorsByType("bike")
				bggy = p4.GetActorsByType("bggy")
				ctnk = p4.GetActorsByType("ctnk")
				tnk2 = p4.GetActorsByType("2tnk")
				raarty = p4.GetActorsByType("raarty")
				tnk1 = p4.GetActorsByType("1tnk")
				rajeep = p4.GetActorsByType("rajeep")
				tnk4 = p4.GetActorsByType("4tnk")
				v2rl = p4.GetActorsByType("v2rl")
				tnk3 = p4.GetActorsByType("3tnk")
				ttnk = p4.GetActorsByType("ttnk")
				ftrk = p4.GetActorsByType("ftrk")
				raapc = p4.GetActorsByType("raapc")
				hpt2100 = p4.GetActorsByType("2100hpt")
				apt2100 = p4.GetActorsByType("2100apt")
				mch2100 = p4.GetActorsByType("2100mch")
				tch2100 = p4.GetActorsByType("2100tch")
				rvw2100 = p4.GetActorsByType("2100rvw")
				mvw2100 = p4.GetActorsByType("2100mvw")
				rvw22100 = p4.GetActorsByType("2100rvw2")
				mvw22100 = p4.GetActorsByType("2100mvw2")
				scgoliath = p4.GetActorsByType("scgoliath")
				scvulture = p4.GetActorsByType("scvulture")
				scsiegetank = p4.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p5 and p3.IsAlliedWith(p5) then
				htnk = p5.GetActorsByType("htnk")
				msam = p5.GetActorsByType("msam")
				mtnk = p5.GetActorsByType("mtnk")
				apc = p5.GetActorsByType("apc")
				jeep = p5.GetActorsByType("jeep")
				stnk = p5.GetActorsByType("stnk")
				arty = p5.GetActorsByType("arty")
				ltnk = p5.GetActorsByType("ltnk")
				ftnk = p5.GetActorsByType("ftnk")
				mlrs = p5.GetActorsByType("mlrs")
				bike = p5.GetActorsByType("bike")
				bggy = p5.GetActorsByType("bggy")
				ctnk = p5.GetActorsByType("ctnk")
				tnk2 = p5.GetActorsByType("2tnk")
				raarty = p5.GetActorsByType("raarty")
				tnk1 = p5.GetActorsByType("1tnk")
				rajeep = p5.GetActorsByType("rajeep")
				tnk4 = p5.GetActorsByType("4tnk")
				v2rl = p5.GetActorsByType("v2rl")
				tnk3 = p5.GetActorsByType("3tnk")
				ttnk = p5.GetActorsByType("ttnk")
				ftrk = p5.GetActorsByType("ftrk")
				raapc = p5.GetActorsByType("raapc")
				hpt2100 = p5.GetActorsByType("2100hpt")
				apt2100 = p5.GetActorsByType("2100apt")
				mch2100 = p5.GetActorsByType("2100mch")
				tch2100 = p5.GetActorsByType("2100tch")
				rvw2100 = p5.GetActorsByType("2100rvw")
				mvw2100 = p5.GetActorsByType("2100mvw")
				rvw22100 = p5.GetActorsByType("2100rvw2")
				mvw22100 = p5.GetActorsByType("2100mvw2")
				scgoliath = p5.GetActorsByType("scgoliath")
				scvulture = p5.GetActorsByType("scvulture")
				scsiegetank = p5.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p6 and p3.IsAlliedWith(p6) then
				htnk = p6.GetActorsByType("htnk")
				msam = p6.GetActorsByType("msam")
				mtnk = p6.GetActorsByType("mtnk")
				apc = p6.GetActorsByType("apc")
				jeep = p6.GetActorsByType("jeep")
				stnk = p6.GetActorsByType("stnk")
				arty = p6.GetActorsByType("arty")
				ltnk = p6.GetActorsByType("ltnk")
				ftnk = p6.GetActorsByType("ftnk")
				mlrs = p6.GetActorsByType("mlrs")
				bike = p6.GetActorsByType("bike")
				bggy = p6.GetActorsByType("bggy")
				ctnk = p6.GetActorsByType("ctnk")
				tnk2 = p6.GetActorsByType("2tnk")
				raarty = p6.GetActorsByType("raarty")
				tnk1 = p6.GetActorsByType("1tnk")
				rajeep = p6.GetActorsByType("rajeep")
				tnk4 = p6.GetActorsByType("4tnk")
				v2rl = p6.GetActorsByType("v2rl")
				tnk3 = p6.GetActorsByType("3tnk")
				ttnk = p6.GetActorsByType("ttnk")
				ftrk = p6.GetActorsByType("ftrk")
				raapc = p6.GetActorsByType("raapc")
				hpt2100 = p6.GetActorsByType("2100hpt")
				apt2100 = p6.GetActorsByType("2100apt")
				mch2100 = p6.GetActorsByType("2100mch")
				tch2100 = p6.GetActorsByType("2100tch")
				rvw2100 = p6.GetActorsByType("2100rvw")
				mvw2100 = p6.GetActorsByType("2100mvw")
				rvw22100 = p6.GetActorsByType("2100rvw2")
				mvw22100 = p6.GetActorsByType("2100mvw2")
				scgoliath = p6.GetActorsByType("scgoliath")
				scvulture = p6.GetActorsByType("scvulture")
				scsiegetank = p6.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p7 and p3.IsAlliedWith(p7) then
				htnk = p7.GetActorsByType("htnk")
				msam = p7.GetActorsByType("msam")
				mtnk = p7.GetActorsByType("mtnk")
				apc = p7.GetActorsByType("apc")
				jeep = p7.GetActorsByType("jeep")
				stnk = p7.GetActorsByType("stnk")
				arty = p7.GetActorsByType("arty")
				ltnk = p7.GetActorsByType("ltnk")
				ftnk = p7.GetActorsByType("ftnk")
				mlrs = p7.GetActorsByType("mlrs")
				bike = p7.GetActorsByType("bike")
				bggy = p7.GetActorsByType("bggy")
				ctnk = p7.GetActorsByType("ctnk")
				tnk2 = p7.GetActorsByType("2tnk")
				raarty = p7.GetActorsByType("raarty")
				tnk1 = p7.GetActorsByType("1tnk")
				rajeep = p7.GetActorsByType("rajeep")
				tnk4 = p7.GetActorsByType("4tnk")
				v2rl = p7.GetActorsByType("v2rl")
				tnk3 = p7.GetActorsByType("3tnk")
				ttnk = p7.GetActorsByType("ttnk")
				ftrk = p7.GetActorsByType("ftrk")
				raapc = p7.GetActorsByType("raapc")
				hpt2100 = p7.GetActorsByType("2100hpt")
				apt2100 = p7.GetActorsByType("2100apt")
				mch2100 = p7.GetActorsByType("2100mch")
				tch2100 = p7.GetActorsByType("2100tch")
				rvw2100 = p7.GetActorsByType("2100rvw")
				mvw2100 = p7.GetActorsByType("2100mvw")
				rvw22100 = p7.GetActorsByType("2100rvw2")
				mvw22100 = p7.GetActorsByType("2100mvw2")
				scgoliath = p7.GetActorsByType("scgoliath")
				scvulture = p7.GetActorsByType("scvulture")
				scsiegetank = p7.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p8 and p3.IsAlliedWith(p8) then
				htnk = p8.GetActorsByType("htnk")
				msam = p8.GetActorsByType("msam")
				mtnk = p8.GetActorsByType("mtnk")
				apc = p8.GetActorsByType("apc")
				jeep = p8.GetActorsByType("jeep")
				stnk = p8.GetActorsByType("stnk")
				arty = p8.GetActorsByType("arty")
				ltnk = p8.GetActorsByType("ltnk")
				ftnk = p8.GetActorsByType("ftnk")
				mlrs = p8.GetActorsByType("mlrs")
				bike = p8.GetActorsByType("bike")
				bggy = p8.GetActorsByType("bggy")
				ctnk = p8.GetActorsByType("ctnk")
				tnk2 = p8.GetActorsByType("2tnk")
				raarty = p8.GetActorsByType("raarty")
				tnk1 = p8.GetActorsByType("1tnk")
				rajeep = p8.GetActorsByType("rajeep")
				tnk4 = p8.GetActorsByType("4tnk")
				v2rl = p8.GetActorsByType("v2rl")
				tnk3 = p8.GetActorsByType("3tnk")
				ttnk = p8.GetActorsByType("ttnk")
				ftrk = p8.GetActorsByType("ftrk")
				raapc = p8.GetActorsByType("raapc")
				hpt2100 = p8.GetActorsByType("2100hpt")
				apt2100 = p8.GetActorsByType("2100apt")
				mch2100 = p8.GetActorsByType("2100mch")
				tch2100 = p8.GetActorsByType("2100tch")
				rvw2100 = p8.GetActorsByType("2100rvw")
				mvw2100 = p8.GetActorsByType("2100mvw")
				rvw22100 = p8.GetActorsByType("2100rvw2")
				mvw22100 = p8.GetActorsByType("2100mvw2")
				scgoliath = p8.GetActorsByType("scgoliath")
				scvulture = p8.GetActorsByType("scvulture")
				scsiegetank = p8.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p9 and p3.IsAlliedWith(p9) then
				htnk = p9.GetActorsByType("htnk")
				msam = p9.GetActorsByType("msam")
				mtnk = p9.GetActorsByType("mtnk")
				apc = p9.GetActorsByType("apc")
				jeep = p9.GetActorsByType("jeep")
				stnk = p9.GetActorsByType("stnk")
				arty = p9.GetActorsByType("arty")
				ltnk = p9.GetActorsByType("ltnk")
				ftnk = p9.GetActorsByType("ftnk")
				mlrs = p9.GetActorsByType("mlrs")
				bike = p9.GetActorsByType("bike")
				bggy = p9.GetActorsByType("bggy")
				ctnk = p9.GetActorsByType("ctnk")
				tnk2 = p9.GetActorsByType("2tnk")
				raarty = p9.GetActorsByType("raarty")
				tnk1 = p9.GetActorsByType("1tnk")
				rajeep = p9.GetActorsByType("rajeep")
				tnk4 = p9.GetActorsByType("4tnk")
				v2rl = p9.GetActorsByType("v2rl")
				tnk3 = p9.GetActorsByType("3tnk")
				ttnk = p9.GetActorsByType("ttnk")
				ftrk = p9.GetActorsByType("ftrk")
				raapc = p9.GetActorsByType("raapc")
				hpt2100 = p9.GetActorsByType("2100hpt")
				apt2100 = p9.GetActorsByType("2100apt")
				mch2100 = p9.GetActorsByType("2100mch")
				tch2100 = p9.GetActorsByType("2100tch")
				rvw2100 = p9.GetActorsByType("2100rvw")
				mvw2100 = p9.GetActorsByType("2100mvw")
				rvw22100 = p9.GetActorsByType("2100rvw2")
				mvw22100 = p9.GetActorsByType("2100mvw2")
				scgoliath = p9.GetActorsByType("scgoliath")
				scvulture = p9.GetActorsByType("scvulture")
				scsiegetank = p9.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p10 and p3.IsAlliedWith(p10) then
				htnk = p10.GetActorsByType("htnk")
				msam = p10.GetActorsByType("msam")
				mtnk = p10.GetActorsByType("mtnk")
				apc = p10.GetActorsByType("apc")
				jeep = p10.GetActorsByType("jeep")
				stnk = p10.GetActorsByType("stnk")
				arty = p10.GetActorsByType("arty")
				ltnk = p10.GetActorsByType("ltnk")
				ftnk = p10.GetActorsByType("ftnk")
				mlrs = p10.GetActorsByType("mlrs")
				bike = p10.GetActorsByType("bike")
				bggy = p10.GetActorsByType("bggy")
				ctnk = p10.GetActorsByType("ctnk")
				tnk2 = p10.GetActorsByType("2tnk")
				raarty = p10.GetActorsByType("raarty")
				tnk1 = p10.GetActorsByType("1tnk")
				rajeep = p10.GetActorsByType("rajeep")
				tnk4 = p10.GetActorsByType("4tnk")
				v2rl = p10.GetActorsByType("v2rl")
				tnk3 = p10.GetActorsByType("3tnk")
				ttnk = p10.GetActorsByType("ttnk")
				ftrk = p10.GetActorsByType("ftrk")
				raapc = p10.GetActorsByType("raapc")
				hpt2100 = p10.GetActorsByType("2100hpt")
				apt2100 = p10.GetActorsByType("2100apt")
				mch2100 = p10.GetActorsByType("2100mch")
				tch2100 = p10.GetActorsByType("2100tch")
				rvw2100 = p10.GetActorsByType("2100rvw")
				mvw2100 = p10.GetActorsByType("2100mvw")
				rvw22100 = p10.GetActorsByType("2100rvw2")
				mvw22100 = p10.GetActorsByType("2100mvw2")
				scgoliath = p10.GetActorsByType("scgoliath")
				scvulture = p10.GetActorsByType("scvulture")
				scsiegetank = p10.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p11 and p3.IsAlliedWith(p11) then
				htnk = p11.GetActorsByType("htnk")
				msam = p11.GetActorsByType("msam")
				mtnk = p11.GetActorsByType("mtnk")
				apc = p11.GetActorsByType("apc")
				jeep = p11.GetActorsByType("jeep")
				stnk = p11.GetActorsByType("stnk")
				arty = p11.GetActorsByType("arty")
				ltnk = p11.GetActorsByType("ltnk")
				ftnk = p11.GetActorsByType("ftnk")
				mlrs = p11.GetActorsByType("mlrs")
				bike = p11.GetActorsByType("bike")
				bggy = p11.GetActorsByType("bggy")
				ctnk = p11.GetActorsByType("ctnk")
				tnk2 = p11.GetActorsByType("2tnk")
				raarty = p11.GetActorsByType("raarty")
				tnk1 = p11.GetActorsByType("1tnk")
				rajeep = p11.GetActorsByType("rajeep")
				tnk4 = p11.GetActorsByType("4tnk")
				v2rl = p11.GetActorsByType("v2rl")
				tnk3 = p11.GetActorsByType("3tnk")
				ttnk = p11.GetActorsByType("ttnk")
				ftrk = p11.GetActorsByType("ftrk")
				raapc = p11.GetActorsByType("raapc")
				hpt2100 = p11.GetActorsByType("2100hpt")
				apt2100 = p11.GetActorsByType("2100apt")
				mch2100 = p11.GetActorsByType("2100mch")
				tch2100 = p11.GetActorsByType("2100tch")
				rvw2100 = p11.GetActorsByType("2100rvw")
				mvw2100 = p11.GetActorsByType("2100mvw")
				rvw22100 = p11.GetActorsByType("2100rvw2")
				mvw22100 = p11.GetActorsByType("2100mvw2")
				scgoliath = p11.GetActorsByType("scgoliath")
				scvulture = p11.GetActorsByType("scvulture")
				scsiegetank = p11.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p12 and p3.IsAlliedWith(p12) then
				htnk = p12.GetActorsByType("htnk")
				msam = p12.GetActorsByType("msam")
				mtnk = p12.GetActorsByType("mtnk")
				apc = p12.GetActorsByType("apc")
				jeep = p12.GetActorsByType("jeep")
				stnk = p12.GetActorsByType("stnk")
				arty = p12.GetActorsByType("arty")
				ltnk = p12.GetActorsByType("ltnk")
				ftnk = p12.GetActorsByType("ftnk")
				mlrs = p12.GetActorsByType("mlrs")
				bike = p12.GetActorsByType("bike")
				bggy = p12.GetActorsByType("bggy")
				ctnk = p12.GetActorsByType("ctnk")
				tnk2 = p12.GetActorsByType("2tnk")
				raarty = p12.GetActorsByType("raarty")
				tnk1 = p12.GetActorsByType("1tnk")
				rajeep = p12.GetActorsByType("rajeep")
				tnk4 = p12.GetActorsByType("4tnk")
				v2rl = p12.GetActorsByType("v2rl")
				tnk3 = p12.GetActorsByType("3tnk")
				ttnk = p12.GetActorsByType("ttnk")
				ftrk = p12.GetActorsByType("ftrk")
				raapc = p12.GetActorsByType("raapc")
				hpt2100 = p12.GetActorsByType("2100hpt")
				apt2100 = p12.GetActorsByType("2100apt")
				mch2100 = p12.GetActorsByType("2100mch")
				tch2100 = p12.GetActorsByType("2100tch")
				rvw2100 = p12.GetActorsByType("2100rvw")
				mvw2100 = p12.GetActorsByType("2100mvw")
				rvw22100 = p12.GetActorsByType("2100rvw2")
				mvw22100 = p12.GetActorsByType("2100mvw2")
				scgoliath = p12.GetActorsByType("scgoliath")
				scvulture = p12.GetActorsByType("scvulture")
				scsiegetank = p12.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p13 and p3.IsAlliedWith(p13) then
				htnk = p13.GetActorsByType("htnk")
				msam = p13.GetActorsByType("msam")
				mtnk = p13.GetActorsByType("mtnk")
				apc = p13.GetActorsByType("apc")
				jeep = p13.GetActorsByType("jeep")
				stnk = p13.GetActorsByType("stnk")
				arty = p13.GetActorsByType("arty")
				ltnk = p13.GetActorsByType("ltnk")
				ftnk = p13.GetActorsByType("ftnk")
				mlrs = p13.GetActorsByType("mlrs")
				bike = p13.GetActorsByType("bike")
				bggy = p13.GetActorsByType("bggy")
				ctnk = p13.GetActorsByType("ctnk")
				tnk2 = p13.GetActorsByType("2tnk")
				raarty = p13.GetActorsByType("raarty")
				tnk1 = p13.GetActorsByType("1tnk")
				rajeep = p13.GetActorsByType("rajeep")
				tnk4 = p13.GetActorsByType("4tnk")
				v2rl = p13.GetActorsByType("v2rl")
				tnk3 = p13.GetActorsByType("3tnk")
				ttnk = p13.GetActorsByType("ttnk")
				ftrk = p13.GetActorsByType("ftrk")
				raapc = p13.GetActorsByType("raapc")
				hpt2100 = p13.GetActorsByType("2100hpt")
				apt2100 = p13.GetActorsByType("2100apt")
				mch2100 = p13.GetActorsByType("2100mch")
				tch2100 = p13.GetActorsByType("2100tch")
				rvw2100 = p13.GetActorsByType("2100rvw")
				mvw2100 = p13.GetActorsByType("2100mvw")
				rvw22100 = p13.GetActorsByType("2100rvw2")
				mvw22100 = p13.GetActorsByType("2100mvw2")
				scgoliath = p13.GetActorsByType("scgoliath")
				scvulture = p13.GetActorsByType("scvulture")
				scsiegetank = p13.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p14 and p3.IsAlliedWith(p14) then
				htnk = p14.GetActorsByType("htnk")
				msam = p14.GetActorsByType("msam")
				mtnk = p14.GetActorsByType("mtnk")
				apc = p14.GetActorsByType("apc")
				jeep = p14.GetActorsByType("jeep")
				stnk = p14.GetActorsByType("stnk")
				arty = p14.GetActorsByType("arty")
				ltnk = p14.GetActorsByType("ltnk")
				ftnk = p14.GetActorsByType("ftnk")
				mlrs = p14.GetActorsByType("mlrs")
				bike = p14.GetActorsByType("bike")
				bggy = p14.GetActorsByType("bggy")
				ctnk = p14.GetActorsByType("ctnk")
				tnk2 = p14.GetActorsByType("2tnk")
				raarty = p14.GetActorsByType("raarty")
				tnk1 = p14.GetActorsByType("1tnk")
				rajeep = p14.GetActorsByType("rajeep")
				tnk4 = p14.GetActorsByType("4tnk")
				v2rl = p14.GetActorsByType("v2rl")
				tnk3 = p14.GetActorsByType("3tnk")
				ttnk = p14.GetActorsByType("ttnk")
				ftrk = p14.GetActorsByType("ftrk")
				raapc = p14.GetActorsByType("raapc")
				hpt2100 = p14.GetActorsByType("2100hpt")
				apt2100 = p14.GetActorsByType("2100apt")
				mch2100 = p14.GetActorsByType("2100mch")
				tch2100 = p14.GetActorsByType("2100tch")
				rvw2100 = p14.GetActorsByType("2100rvw")
				mvw2100 = p14.GetActorsByType("2100mvw")
				rvw22100 = p14.GetActorsByType("2100rvw2")
				mvw22100 = p14.GetActorsByType("2100mvw2")
				scgoliath = p14.GetActorsByType("scgoliath")
				scvulture = p14.GetActorsByType("scvulture")
				scsiegetank = p14.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p15 and p3.IsAlliedWith(p15) then
				htnk = p15.GetActorsByType("htnk")
				msam = p15.GetActorsByType("msam")
				mtnk = p15.GetActorsByType("mtnk")
				apc = p15.GetActorsByType("apc")
				jeep = p15.GetActorsByType("jeep")
				stnk = p15.GetActorsByType("stnk")
				arty = p15.GetActorsByType("arty")
				ltnk = p15.GetActorsByType("ltnk")
				ftnk = p15.GetActorsByType("ftnk")
				mlrs = p15.GetActorsByType("mlrs")
				bike = p15.GetActorsByType("bike")
				bggy = p15.GetActorsByType("bggy")
				ctnk = p15.GetActorsByType("ctnk")
				tnk2 = p15.GetActorsByType("2tnk")
				raarty = p15.GetActorsByType("raarty")
				tnk1 = p15.GetActorsByType("1tnk")
				rajeep = p15.GetActorsByType("rajeep")
				tnk4 = p15.GetActorsByType("4tnk")
				v2rl = p15.GetActorsByType("v2rl")
				tnk3 = p15.GetActorsByType("3tnk")
				ttnk = p15.GetActorsByType("ttnk")
				ftrk = p15.GetActorsByType("ftrk")
				raapc = p15.GetActorsByType("raapc")
				hpt2100 = p15.GetActorsByType("2100hpt")
				apt2100 = p15.GetActorsByType("2100apt")
				mch2100 = p15.GetActorsByType("2100mch")
				tch2100 = p15.GetActorsByType("2100tch")
				rvw2100 = p15.GetActorsByType("2100rvw")
				mvw2100 = p15.GetActorsByType("2100mvw")
				rvw22100 = p15.GetActorsByType("2100rvw2")
				mvw22100 = p15.GetActorsByType("2100mvw2")
				scgoliath = p15.GetActorsByType("scgoliath")
				scvulture = p15.GetActorsByType("scvulture")
				scsiegetank = p15.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p16 and p3.IsAlliedWith(p16) then
				htnk = p16.GetActorsByType("htnk")
				msam = p16.GetActorsByType("msam")
				mtnk = p16.GetActorsByType("mtnk")
				apc = p16.GetActorsByType("apc")
				jeep = p16.GetActorsByType("jeep")
				stnk = p16.GetActorsByType("stnk")
				arty = p16.GetActorsByType("arty")
				ltnk = p16.GetActorsByType("ltnk")
				ftnk = p16.GetActorsByType("ftnk")
				mlrs = p16.GetActorsByType("mlrs")
				bike = p16.GetActorsByType("bike")
				bggy = p16.GetActorsByType("bggy")
				ctnk = p16.GetActorsByType("ctnk")
				tnk2 = p16.GetActorsByType("2tnk")
				raarty = p16.GetActorsByType("raarty")
				tnk1 = p16.GetActorsByType("1tnk")
				rajeep = p16.GetActorsByType("rajeep")
				tnk4 = p16.GetActorsByType("4tnk")
				v2rl = p16.GetActorsByType("v2rl")
				tnk3 = p16.GetActorsByType("3tnk")
				ttnk = p16.GetActorsByType("ttnk")
				ftrk = p16.GetActorsByType("ftrk")
				raapc = p16.GetActorsByType("raapc")
				hpt2100 = p16.GetActorsByType("2100hpt")
				apt2100 = p16.GetActorsByType("2100apt")
				mch2100 = p16.GetActorsByType("2100mch")
				tch2100 = p16.GetActorsByType("2100tch")
				rvw2100 = p16.GetActorsByType("2100rvw")
				mvw2100 = p16.GetActorsByType("2100mvw")
				rvw22100 = p16.GetActorsByType("2100rvw2")
				mvw22100 = p16.GetActorsByType("2100mvw2")
				scgoliath = p16.GetActorsByType("scgoliath")
				scvulture = p16.GetActorsByType("scvulture")
				scsiegetank = p16.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p1 and p3.IsAlliedWith(p1) then
				htnk = p1.GetActorsByType("htnk")
				msam = p1.GetActorsByType("msam")
				mtnk = p1.GetActorsByType("mtnk")
				apc = p1.GetActorsByType("apc")
				jeep = p1.GetActorsByType("jeep")
				stnk = p1.GetActorsByType("stnk")
				arty = p1.GetActorsByType("arty")
				ltnk = p1.GetActorsByType("ltnk")
				ftnk = p1.GetActorsByType("ftnk")
				mlrs = p1.GetActorsByType("mlrs")
				bike = p1.GetActorsByType("bike")
				bggy = p1.GetActorsByType("bggy")
				ctnk = p1.GetActorsByType("ctnk")
				tnk2 = p1.GetActorsByType("2tnk")
				raarty = p1.GetActorsByType("raarty")
				tnk1 = p1.GetActorsByType("1tnk")
				rajeep = p1.GetActorsByType("rajeep")
				tnk4 = p1.GetActorsByType("4tnk")
				v2rl = p1.GetActorsByType("v2rl")
				tnk3 = p1.GetActorsByType("3tnk")
				ttnk = p1.GetActorsByType("ttnk")
				ftrk = p1.GetActorsByType("ftrk")
				raapc = p1.GetActorsByType("raapc")
				hpt2100 = p1.GetActorsByType("2100hpt")
				apt2100 = p1.GetActorsByType("2100apt")
				mch2100 = p1.GetActorsByType("2100mch")
				tch2100 = p1.GetActorsByType("2100tch")
				rvw2100 = p1.GetActorsByType("2100rvw")
				mvw2100 = p1.GetActorsByType("2100mvw")
				rvw22100 = p1.GetActorsByType("2100rvw2")
				mvw22100 = p1.GetActorsByType("2100mvw2")
				scgoliath = p1.GetActorsByType("scgoliath")
				scvulture = p1.GetActorsByType("scvulture")
				scsiegetank = p1.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p2 and p3.IsAlliedWith(p2) then
				htnk = p2.GetActorsByType("htnk")
				msam = p2.GetActorsByType("msam")
				mtnk = p2.GetActorsByType("mtnk")
				apc = p2.GetActorsByType("apc")
				jeep = p2.GetActorsByType("jeep")
				stnk = p2.GetActorsByType("stnk")
				arty = p2.GetActorsByType("arty")
				ltnk = p2.GetActorsByType("ltnk")
				ftnk = p2.GetActorsByType("ftnk")
				mlrs = p2.GetActorsByType("mlrs")
				bike = p2.GetActorsByType("bike")
				bggy = p2.GetActorsByType("bggy")
				ctnk = p2.GetActorsByType("ctnk")
				tnk2 = p2.GetActorsByType("2tnk")
				raarty = p2.GetActorsByType("raarty")
				tnk1 = p2.GetActorsByType("1tnk")
				rajeep = p2.GetActorsByType("rajeep")
				tnk4 = p2.GetActorsByType("4tnk")
				v2rl = p2.GetActorsByType("v2rl")
				tnk3 = p2.GetActorsByType("3tnk")
				ttnk = p2.GetActorsByType("ttnk")
				ftrk = p2.GetActorsByType("ftrk")
				raapc = p2.GetActorsByType("raapc")
				hpt2100 = p2.GetActorsByType("2100hpt")
				apt2100 = p2.GetActorsByType("2100apt")
				mch2100 = p2.GetActorsByType("2100mch")
				tch2100 = p2.GetActorsByType("2100tch")
				rvw2100 = p2.GetActorsByType("2100rvw")
				mvw2100 = p2.GetActorsByType("2100mvw")
				rvw22100 = p2.GetActorsByType("2100rvw2")
				mvw22100 = p2.GetActorsByType("2100mvw2")
				scgoliath = p2.GetActorsByType("scgoliath")
				scvulture = p2.GetActorsByType("scvulture")
				scsiegetank = p2.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p3.GetActorsByType("citypolicecar")
				cityfiretruck = p3.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p3vehicle()
	end)
end
p4vehicle = function()
	if p4 and p4.IsBot then
		cityboteliteexpert = p4.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p5 and p4.IsAlliedWith(p5) then
				htnk = p5.GetActorsByType("htnk")
				msam = p5.GetActorsByType("msam")
				mtnk = p5.GetActorsByType("mtnk")
				apc = p5.GetActorsByType("apc")
				jeep = p5.GetActorsByType("jeep")
				stnk = p5.GetActorsByType("stnk")
				arty = p5.GetActorsByType("arty")
				ltnk = p5.GetActorsByType("ltnk")
				ftnk = p5.GetActorsByType("ftnk")
				mlrs = p5.GetActorsByType("mlrs")
				bike = p5.GetActorsByType("bike")
				bggy = p5.GetActorsByType("bggy")
				ctnk = p5.GetActorsByType("ctnk")
				tnk2 = p5.GetActorsByType("2tnk")
				raarty = p5.GetActorsByType("raarty")
				tnk1 = p5.GetActorsByType("1tnk")
				rajeep = p5.GetActorsByType("rajeep")
				tnk4 = p5.GetActorsByType("4tnk")
				v2rl = p5.GetActorsByType("v2rl")
				tnk3 = p5.GetActorsByType("3tnk")
				ttnk = p5.GetActorsByType("ttnk")
				ftrk = p5.GetActorsByType("ftrk")
				raapc = p5.GetActorsByType("raapc")
				hpt2100 = p5.GetActorsByType("2100hpt")
				apt2100 = p5.GetActorsByType("2100apt")
				mch2100 = p5.GetActorsByType("2100mch")
				tch2100 = p5.GetActorsByType("2100tch")
				rvw2100 = p5.GetActorsByType("2100rvw")
				mvw2100 = p5.GetActorsByType("2100mvw")
				rvw22100 = p5.GetActorsByType("2100rvw2")
				mvw22100 = p5.GetActorsByType("2100mvw2")
				scgoliath = p5.GetActorsByType("scgoliath")
				scvulture = p5.GetActorsByType("scvulture")
				scsiegetank = p5.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p6 and p4.IsAlliedWith(p6) then
				htnk = p6.GetActorsByType("htnk")
				msam = p6.GetActorsByType("msam")
				mtnk = p6.GetActorsByType("mtnk")
				apc = p6.GetActorsByType("apc")
				jeep = p6.GetActorsByType("jeep")
				stnk = p6.GetActorsByType("stnk")
				arty = p6.GetActorsByType("arty")
				ltnk = p6.GetActorsByType("ltnk")
				ftnk = p6.GetActorsByType("ftnk")
				mlrs = p6.GetActorsByType("mlrs")
				bike = p6.GetActorsByType("bike")
				bggy = p6.GetActorsByType("bggy")
				ctnk = p6.GetActorsByType("ctnk")
				tnk2 = p6.GetActorsByType("2tnk")
				raarty = p6.GetActorsByType("raarty")
				tnk1 = p6.GetActorsByType("1tnk")
				rajeep = p6.GetActorsByType("rajeep")
				tnk4 = p6.GetActorsByType("4tnk")
				v2rl = p6.GetActorsByType("v2rl")
				tnk3 = p6.GetActorsByType("3tnk")
				ttnk = p6.GetActorsByType("ttnk")
				ftrk = p6.GetActorsByType("ftrk")
				raapc = p6.GetActorsByType("raapc")
				hpt2100 = p6.GetActorsByType("2100hpt")
				apt2100 = p6.GetActorsByType("2100apt")
				mch2100 = p6.GetActorsByType("2100mch")
				tch2100 = p6.GetActorsByType("2100tch")
				rvw2100 = p6.GetActorsByType("2100rvw")
				mvw2100 = p6.GetActorsByType("2100mvw")
				rvw22100 = p6.GetActorsByType("2100rvw2")
				mvw22100 = p6.GetActorsByType("2100mvw2")
				scgoliath = p6.GetActorsByType("scgoliath")
				scvulture = p6.GetActorsByType("scvulture")
				scsiegetank = p6.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p7 and p4.IsAlliedWith(p7) then
				htnk = p7.GetActorsByType("htnk")
				msam = p7.GetActorsByType("msam")
				mtnk = p7.GetActorsByType("mtnk")
				apc = p7.GetActorsByType("apc")
				jeep = p7.GetActorsByType("jeep")
				stnk = p7.GetActorsByType("stnk")
				arty = p7.GetActorsByType("arty")
				ltnk = p7.GetActorsByType("ltnk")
				ftnk = p7.GetActorsByType("ftnk")
				mlrs = p7.GetActorsByType("mlrs")
				bike = p7.GetActorsByType("bike")
				bggy = p7.GetActorsByType("bggy")
				ctnk = p7.GetActorsByType("ctnk")
				tnk2 = p7.GetActorsByType("2tnk")
				raarty = p7.GetActorsByType("raarty")
				tnk1 = p7.GetActorsByType("1tnk")
				rajeep = p7.GetActorsByType("rajeep")
				tnk4 = p7.GetActorsByType("4tnk")
				v2rl = p7.GetActorsByType("v2rl")
				tnk3 = p7.GetActorsByType("3tnk")
				ttnk = p7.GetActorsByType("ttnk")
				ftrk = p7.GetActorsByType("ftrk")
				raapc = p7.GetActorsByType("raapc")
				hpt2100 = p7.GetActorsByType("2100hpt")
				apt2100 = p7.GetActorsByType("2100apt")
				mch2100 = p7.GetActorsByType("2100mch")
				tch2100 = p7.GetActorsByType("2100tch")
				rvw2100 = p7.GetActorsByType("2100rvw")
				mvw2100 = p7.GetActorsByType("2100mvw")
				rvw22100 = p7.GetActorsByType("2100rvw2")
				mvw22100 = p7.GetActorsByType("2100mvw2")
				scgoliath = p7.GetActorsByType("scgoliath")
				scvulture = p7.GetActorsByType("scvulture")
				scsiegetank = p7.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p8 and p4.IsAlliedWith(p8) then
				htnk = p8.GetActorsByType("htnk")
				msam = p8.GetActorsByType("msam")
				mtnk = p8.GetActorsByType("mtnk")
				apc = p8.GetActorsByType("apc")
				jeep = p8.GetActorsByType("jeep")
				stnk = p8.GetActorsByType("stnk")
				arty = p8.GetActorsByType("arty")
				ltnk = p8.GetActorsByType("ltnk")
				ftnk = p8.GetActorsByType("ftnk")
				mlrs = p8.GetActorsByType("mlrs")
				bike = p8.GetActorsByType("bike")
				bggy = p8.GetActorsByType("bggy")
				ctnk = p8.GetActorsByType("ctnk")
				tnk2 = p8.GetActorsByType("2tnk")
				raarty = p8.GetActorsByType("raarty")
				tnk1 = p8.GetActorsByType("1tnk")
				rajeep = p8.GetActorsByType("rajeep")
				tnk4 = p8.GetActorsByType("4tnk")
				v2rl = p8.GetActorsByType("v2rl")
				tnk3 = p8.GetActorsByType("3tnk")
				ttnk = p8.GetActorsByType("ttnk")
				ftrk = p8.GetActorsByType("ftrk")
				raapc = p8.GetActorsByType("raapc")
				hpt2100 = p8.GetActorsByType("2100hpt")
				apt2100 = p8.GetActorsByType("2100apt")
				mch2100 = p8.GetActorsByType("2100mch")
				tch2100 = p8.GetActorsByType("2100tch")
				rvw2100 = p8.GetActorsByType("2100rvw")
				mvw2100 = p8.GetActorsByType("2100mvw")
				rvw22100 = p8.GetActorsByType("2100rvw2")
				mvw22100 = p8.GetActorsByType("2100mvw2")
				scgoliath = p8.GetActorsByType("scgoliath")
				scvulture = p8.GetActorsByType("scvulture")
				scsiegetank = p8.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p9 and p4.IsAlliedWith(p9) then
				htnk = p9.GetActorsByType("htnk")
				msam = p9.GetActorsByType("msam")
				mtnk = p9.GetActorsByType("mtnk")
				apc = p9.GetActorsByType("apc")
				jeep = p9.GetActorsByType("jeep")
				stnk = p9.GetActorsByType("stnk")
				arty = p9.GetActorsByType("arty")
				ltnk = p9.GetActorsByType("ltnk")
				ftnk = p9.GetActorsByType("ftnk")
				mlrs = p9.GetActorsByType("mlrs")
				bike = p9.GetActorsByType("bike")
				bggy = p9.GetActorsByType("bggy")
				ctnk = p9.GetActorsByType("ctnk")
				tnk2 = p9.GetActorsByType("2tnk")
				raarty = p9.GetActorsByType("raarty")
				tnk1 = p9.GetActorsByType("1tnk")
				rajeep = p9.GetActorsByType("rajeep")
				tnk4 = p9.GetActorsByType("4tnk")
				v2rl = p9.GetActorsByType("v2rl")
				tnk3 = p9.GetActorsByType("3tnk")
				ttnk = p9.GetActorsByType("ttnk")
				ftrk = p9.GetActorsByType("ftrk")
				raapc = p9.GetActorsByType("raapc")
				hpt2100 = p9.GetActorsByType("2100hpt")
				apt2100 = p9.GetActorsByType("2100apt")
				mch2100 = p9.GetActorsByType("2100mch")
				tch2100 = p9.GetActorsByType("2100tch")
				rvw2100 = p9.GetActorsByType("2100rvw")
				mvw2100 = p9.GetActorsByType("2100mvw")
				rvw22100 = p9.GetActorsByType("2100rvw2")
				mvw22100 = p9.GetActorsByType("2100mvw2")
				scgoliath = p9.GetActorsByType("scgoliath")
				scvulture = p9.GetActorsByType("scvulture")
				scsiegetank = p9.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p10 and p4.IsAlliedWith(p10) then
				htnk = p10.GetActorsByType("htnk")
				msam = p10.GetActorsByType("msam")
				mtnk = p10.GetActorsByType("mtnk")
				apc = p10.GetActorsByType("apc")
				jeep = p10.GetActorsByType("jeep")
				stnk = p10.GetActorsByType("stnk")
				arty = p10.GetActorsByType("arty")
				ltnk = p10.GetActorsByType("ltnk")
				ftnk = p10.GetActorsByType("ftnk")
				mlrs = p10.GetActorsByType("mlrs")
				bike = p10.GetActorsByType("bike")
				bggy = p10.GetActorsByType("bggy")
				ctnk = p10.GetActorsByType("ctnk")
				tnk2 = p10.GetActorsByType("2tnk")
				raarty = p10.GetActorsByType("raarty")
				tnk1 = p10.GetActorsByType("1tnk")
				rajeep = p10.GetActorsByType("rajeep")
				tnk4 = p10.GetActorsByType("4tnk")
				v2rl = p10.GetActorsByType("v2rl")
				tnk3 = p10.GetActorsByType("3tnk")
				ttnk = p10.GetActorsByType("ttnk")
				ftrk = p10.GetActorsByType("ftrk")
				raapc = p10.GetActorsByType("raapc")
				hpt2100 = p10.GetActorsByType("2100hpt")
				apt2100 = p10.GetActorsByType("2100apt")
				mch2100 = p10.GetActorsByType("2100mch")
				tch2100 = p10.GetActorsByType("2100tch")
				rvw2100 = p10.GetActorsByType("2100rvw")
				mvw2100 = p10.GetActorsByType("2100mvw")
				rvw22100 = p10.GetActorsByType("2100rvw2")
				mvw22100 = p10.GetActorsByType("2100mvw2")
				scgoliath = p10.GetActorsByType("scgoliath")
				scvulture = p10.GetActorsByType("scvulture")
				scsiegetank = p10.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p11 and p4.IsAlliedWith(p11) then
				htnk = p11.GetActorsByType("htnk")
				msam = p11.GetActorsByType("msam")
				mtnk = p11.GetActorsByType("mtnk")
				apc = p11.GetActorsByType("apc")
				jeep = p11.GetActorsByType("jeep")
				stnk = p11.GetActorsByType("stnk")
				arty = p11.GetActorsByType("arty")
				ltnk = p11.GetActorsByType("ltnk")
				ftnk = p11.GetActorsByType("ftnk")
				mlrs = p11.GetActorsByType("mlrs")
				bike = p11.GetActorsByType("bike")
				bggy = p11.GetActorsByType("bggy")
				ctnk = p11.GetActorsByType("ctnk")
				tnk2 = p11.GetActorsByType("2tnk")
				raarty = p11.GetActorsByType("raarty")
				tnk1 = p11.GetActorsByType("1tnk")
				rajeep = p11.GetActorsByType("rajeep")
				tnk4 = p11.GetActorsByType("4tnk")
				v2rl = p11.GetActorsByType("v2rl")
				tnk3 = p11.GetActorsByType("3tnk")
				ttnk = p11.GetActorsByType("ttnk")
				ftrk = p11.GetActorsByType("ftrk")
				raapc = p11.GetActorsByType("raapc")
				hpt2100 = p11.GetActorsByType("2100hpt")
				apt2100 = p11.GetActorsByType("2100apt")
				mch2100 = p11.GetActorsByType("2100mch")
				tch2100 = p11.GetActorsByType("2100tch")
				rvw2100 = p11.GetActorsByType("2100rvw")
				mvw2100 = p11.GetActorsByType("2100mvw")
				rvw22100 = p11.GetActorsByType("2100rvw2")
				mvw22100 = p11.GetActorsByType("2100mvw2")
				scgoliath = p11.GetActorsByType("scgoliath")
				scvulture = p11.GetActorsByType("scvulture")
				scsiegetank = p11.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p12 and p4.IsAlliedWith(p12) then
				htnk = p12.GetActorsByType("htnk")
				msam = p12.GetActorsByType("msam")
				mtnk = p12.GetActorsByType("mtnk")
				apc = p12.GetActorsByType("apc")
				jeep = p12.GetActorsByType("jeep")
				stnk = p12.GetActorsByType("stnk")
				arty = p12.GetActorsByType("arty")
				ltnk = p12.GetActorsByType("ltnk")
				ftnk = p12.GetActorsByType("ftnk")
				mlrs = p12.GetActorsByType("mlrs")
				bike = p12.GetActorsByType("bike")
				bggy = p12.GetActorsByType("bggy")
				ctnk = p12.GetActorsByType("ctnk")
				tnk2 = p12.GetActorsByType("2tnk")
				raarty = p12.GetActorsByType("raarty")
				tnk1 = p12.GetActorsByType("1tnk")
				rajeep = p12.GetActorsByType("rajeep")
				tnk4 = p12.GetActorsByType("4tnk")
				v2rl = p12.GetActorsByType("v2rl")
				tnk3 = p12.GetActorsByType("3tnk")
				ttnk = p12.GetActorsByType("ttnk")
				ftrk = p12.GetActorsByType("ftrk")
				raapc = p12.GetActorsByType("raapc")
				hpt2100 = p12.GetActorsByType("2100hpt")
				apt2100 = p12.GetActorsByType("2100apt")
				mch2100 = p12.GetActorsByType("2100mch")
				tch2100 = p12.GetActorsByType("2100tch")
				rvw2100 = p12.GetActorsByType("2100rvw")
				mvw2100 = p12.GetActorsByType("2100mvw")
				rvw22100 = p12.GetActorsByType("2100rvw2")
				mvw22100 = p12.GetActorsByType("2100mvw2")
				scgoliath = p12.GetActorsByType("scgoliath")
				scvulture = p12.GetActorsByType("scvulture")
				scsiegetank = p12.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p13 and p4.IsAlliedWith(p13) then
				htnk = p13.GetActorsByType("htnk")
				msam = p13.GetActorsByType("msam")
				mtnk = p13.GetActorsByType("mtnk")
				apc = p13.GetActorsByType("apc")
				jeep = p13.GetActorsByType("jeep")
				stnk = p13.GetActorsByType("stnk")
				arty = p13.GetActorsByType("arty")
				ltnk = p13.GetActorsByType("ltnk")
				ftnk = p13.GetActorsByType("ftnk")
				mlrs = p13.GetActorsByType("mlrs")
				bike = p13.GetActorsByType("bike")
				bggy = p13.GetActorsByType("bggy")
				ctnk = p13.GetActorsByType("ctnk")
				tnk2 = p13.GetActorsByType("2tnk")
				raarty = p13.GetActorsByType("raarty")
				tnk1 = p13.GetActorsByType("1tnk")
				rajeep = p13.GetActorsByType("rajeep")
				tnk4 = p13.GetActorsByType("4tnk")
				v2rl = p13.GetActorsByType("v2rl")
				tnk3 = p13.GetActorsByType("3tnk")
				ttnk = p13.GetActorsByType("ttnk")
				ftrk = p13.GetActorsByType("ftrk")
				raapc = p13.GetActorsByType("raapc")
				hpt2100 = p13.GetActorsByType("2100hpt")
				apt2100 = p13.GetActorsByType("2100apt")
				mch2100 = p13.GetActorsByType("2100mch")
				tch2100 = p13.GetActorsByType("2100tch")
				rvw2100 = p13.GetActorsByType("2100rvw")
				mvw2100 = p13.GetActorsByType("2100mvw")
				rvw22100 = p13.GetActorsByType("2100rvw2")
				mvw22100 = p13.GetActorsByType("2100mvw2")
				scgoliath = p13.GetActorsByType("scgoliath")
				scvulture = p13.GetActorsByType("scvulture")
				scsiegetank = p13.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p14 and p4.IsAlliedWith(p14) then
				htnk = p14.GetActorsByType("htnk")
				msam = p14.GetActorsByType("msam")
				mtnk = p14.GetActorsByType("mtnk")
				apc = p14.GetActorsByType("apc")
				jeep = p14.GetActorsByType("jeep")
				stnk = p14.GetActorsByType("stnk")
				arty = p14.GetActorsByType("arty")
				ltnk = p14.GetActorsByType("ltnk")
				ftnk = p14.GetActorsByType("ftnk")
				mlrs = p14.GetActorsByType("mlrs")
				bike = p14.GetActorsByType("bike")
				bggy = p14.GetActorsByType("bggy")
				ctnk = p14.GetActorsByType("ctnk")
				tnk2 = p14.GetActorsByType("2tnk")
				raarty = p14.GetActorsByType("raarty")
				tnk1 = p14.GetActorsByType("1tnk")
				rajeep = p14.GetActorsByType("rajeep")
				tnk4 = p14.GetActorsByType("4tnk")
				v2rl = p14.GetActorsByType("v2rl")
				tnk3 = p14.GetActorsByType("3tnk")
				ttnk = p14.GetActorsByType("ttnk")
				ftrk = p14.GetActorsByType("ftrk")
				raapc = p14.GetActorsByType("raapc")
				hpt2100 = p14.GetActorsByType("2100hpt")
				apt2100 = p14.GetActorsByType("2100apt")
				mch2100 = p14.GetActorsByType("2100mch")
				tch2100 = p14.GetActorsByType("2100tch")
				rvw2100 = p14.GetActorsByType("2100rvw")
				mvw2100 = p14.GetActorsByType("2100mvw")
				rvw22100 = p14.GetActorsByType("2100rvw2")
				mvw22100 = p14.GetActorsByType("2100mvw2")
				scgoliath = p14.GetActorsByType("scgoliath")
				scvulture = p14.GetActorsByType("scvulture")
				scsiegetank = p14.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p15 and p4.IsAlliedWith(p15) then
				htnk = p15.GetActorsByType("htnk")
				msam = p15.GetActorsByType("msam")
				mtnk = p15.GetActorsByType("mtnk")
				apc = p15.GetActorsByType("apc")
				jeep = p15.GetActorsByType("jeep")
				stnk = p15.GetActorsByType("stnk")
				arty = p15.GetActorsByType("arty")
				ltnk = p15.GetActorsByType("ltnk")
				ftnk = p15.GetActorsByType("ftnk")
				mlrs = p15.GetActorsByType("mlrs")
				bike = p15.GetActorsByType("bike")
				bggy = p15.GetActorsByType("bggy")
				ctnk = p15.GetActorsByType("ctnk")
				tnk2 = p15.GetActorsByType("2tnk")
				raarty = p15.GetActorsByType("raarty")
				tnk1 = p15.GetActorsByType("1tnk")
				rajeep = p15.GetActorsByType("rajeep")
				tnk4 = p15.GetActorsByType("4tnk")
				v2rl = p15.GetActorsByType("v2rl")
				tnk3 = p15.GetActorsByType("3tnk")
				ttnk = p15.GetActorsByType("ttnk")
				ftrk = p15.GetActorsByType("ftrk")
				raapc = p15.GetActorsByType("raapc")
				hpt2100 = p15.GetActorsByType("2100hpt")
				apt2100 = p15.GetActorsByType("2100apt")
				mch2100 = p15.GetActorsByType("2100mch")
				tch2100 = p15.GetActorsByType("2100tch")
				rvw2100 = p15.GetActorsByType("2100rvw")
				mvw2100 = p15.GetActorsByType("2100mvw")
				rvw22100 = p15.GetActorsByType("2100rvw2")
				mvw22100 = p15.GetActorsByType("2100mvw2")
				scgoliath = p15.GetActorsByType("scgoliath")
				scvulture = p15.GetActorsByType("scvulture")
				scsiegetank = p15.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p16 and p4.IsAlliedWith(p16) then
				htnk = p16.GetActorsByType("htnk")
				msam = p16.GetActorsByType("msam")
				mtnk = p16.GetActorsByType("mtnk")
				apc = p16.GetActorsByType("apc")
				jeep = p16.GetActorsByType("jeep")
				stnk = p16.GetActorsByType("stnk")
				arty = p16.GetActorsByType("arty")
				ltnk = p16.GetActorsByType("ltnk")
				ftnk = p16.GetActorsByType("ftnk")
				mlrs = p16.GetActorsByType("mlrs")
				bike = p16.GetActorsByType("bike")
				bggy = p16.GetActorsByType("bggy")
				ctnk = p16.GetActorsByType("ctnk")
				tnk2 = p16.GetActorsByType("2tnk")
				raarty = p16.GetActorsByType("raarty")
				tnk1 = p16.GetActorsByType("1tnk")
				rajeep = p16.GetActorsByType("rajeep")
				tnk4 = p16.GetActorsByType("4tnk")
				v2rl = p16.GetActorsByType("v2rl")
				tnk3 = p16.GetActorsByType("3tnk")
				ttnk = p16.GetActorsByType("ttnk")
				ftrk = p16.GetActorsByType("ftrk")
				raapc = p16.GetActorsByType("raapc")
				hpt2100 = p16.GetActorsByType("2100hpt")
				apt2100 = p16.GetActorsByType("2100apt")
				mch2100 = p16.GetActorsByType("2100mch")
				tch2100 = p16.GetActorsByType("2100tch")
				rvw2100 = p16.GetActorsByType("2100rvw")
				mvw2100 = p16.GetActorsByType("2100mvw")
				rvw22100 = p16.GetActorsByType("2100rvw2")
				mvw22100 = p16.GetActorsByType("2100mvw2")
				scgoliath = p16.GetActorsByType("scgoliath")
				scvulture = p16.GetActorsByType("scvulture")
				scsiegetank = p16.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p1 and p4.IsAlliedWith(p1) then
				htnk = p1.GetActorsByType("htnk")
				msam = p1.GetActorsByType("msam")
				mtnk = p1.GetActorsByType("mtnk")
				apc = p1.GetActorsByType("apc")
				jeep = p1.GetActorsByType("jeep")
				stnk = p1.GetActorsByType("stnk")
				arty = p1.GetActorsByType("arty")
				ltnk = p1.GetActorsByType("ltnk")
				ftnk = p1.GetActorsByType("ftnk")
				mlrs = p1.GetActorsByType("mlrs")
				bike = p1.GetActorsByType("bike")
				bggy = p1.GetActorsByType("bggy")
				ctnk = p1.GetActorsByType("ctnk")
				tnk2 = p1.GetActorsByType("2tnk")
				raarty = p1.GetActorsByType("raarty")
				tnk1 = p1.GetActorsByType("1tnk")
				rajeep = p1.GetActorsByType("rajeep")
				tnk4 = p1.GetActorsByType("4tnk")
				v2rl = p1.GetActorsByType("v2rl")
				tnk3 = p1.GetActorsByType("3tnk")
				ttnk = p1.GetActorsByType("ttnk")
				ftrk = p1.GetActorsByType("ftrk")
				raapc = p1.GetActorsByType("raapc")
				hpt2100 = p1.GetActorsByType("2100hpt")
				apt2100 = p1.GetActorsByType("2100apt")
				mch2100 = p1.GetActorsByType("2100mch")
				tch2100 = p1.GetActorsByType("2100tch")
				rvw2100 = p1.GetActorsByType("2100rvw")
				mvw2100 = p1.GetActorsByType("2100mvw")
				rvw22100 = p1.GetActorsByType("2100rvw2")
				mvw22100 = p1.GetActorsByType("2100mvw2")
				scgoliath = p1.GetActorsByType("scgoliath")
				scvulture = p1.GetActorsByType("scvulture")
				scsiegetank = p1.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p2 and p4.IsAlliedWith(p2) then
				htnk = p2.GetActorsByType("htnk")
				msam = p2.GetActorsByType("msam")
				mtnk = p2.GetActorsByType("mtnk")
				apc = p2.GetActorsByType("apc")
				jeep = p2.GetActorsByType("jeep")
				stnk = p2.GetActorsByType("stnk")
				arty = p2.GetActorsByType("arty")
				ltnk = p2.GetActorsByType("ltnk")
				ftnk = p2.GetActorsByType("ftnk")
				mlrs = p2.GetActorsByType("mlrs")
				bike = p2.GetActorsByType("bike")
				bggy = p2.GetActorsByType("bggy")
				ctnk = p2.GetActorsByType("ctnk")
				tnk2 = p2.GetActorsByType("2tnk")
				raarty = p2.GetActorsByType("raarty")
				tnk1 = p2.GetActorsByType("1tnk")
				rajeep = p2.GetActorsByType("rajeep")
				tnk4 = p2.GetActorsByType("4tnk")
				v2rl = p2.GetActorsByType("v2rl")
				tnk3 = p2.GetActorsByType("3tnk")
				ttnk = p2.GetActorsByType("ttnk")
				ftrk = p2.GetActorsByType("ftrk")
				raapc = p2.GetActorsByType("raapc")
				hpt2100 = p2.GetActorsByType("2100hpt")
				apt2100 = p2.GetActorsByType("2100apt")
				mch2100 = p2.GetActorsByType("2100mch")
				tch2100 = p2.GetActorsByType("2100tch")
				rvw2100 = p2.GetActorsByType("2100rvw")
				mvw2100 = p2.GetActorsByType("2100mvw")
				rvw22100 = p2.GetActorsByType("2100rvw2")
				mvw22100 = p2.GetActorsByType("2100mvw2")
				scgoliath = p2.GetActorsByType("scgoliath")
				scvulture = p2.GetActorsByType("scvulture")
				scsiegetank = p2.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p3 and p4.IsAlliedWith(p3) then
				htnk = p3.GetActorsByType("htnk")
				msam = p3.GetActorsByType("msam")
				mtnk = p3.GetActorsByType("mtnk")
				apc = p3.GetActorsByType("apc")
				jeep = p3.GetActorsByType("jeep")
				stnk = p3.GetActorsByType("stnk")
				arty = p3.GetActorsByType("arty")
				ltnk = p3.GetActorsByType("ltnk")
				ftnk = p3.GetActorsByType("ftnk")
				mlrs = p3.GetActorsByType("mlrs")
				bike = p3.GetActorsByType("bike")
				bggy = p3.GetActorsByType("bggy")
				ctnk = p3.GetActorsByType("ctnk")
				tnk2 = p3.GetActorsByType("2tnk")
				raarty = p3.GetActorsByType("raarty")
				tnk1 = p3.GetActorsByType("1tnk")
				rajeep = p3.GetActorsByType("rajeep")
				tnk4 = p3.GetActorsByType("4tnk")
				v2rl = p3.GetActorsByType("v2rl")
				tnk3 = p3.GetActorsByType("3tnk")
				ttnk = p3.GetActorsByType("ttnk")
				ftrk = p3.GetActorsByType("ftrk")
				raapc = p3.GetActorsByType("raapc")
				hpt2100 = p3.GetActorsByType("2100hpt")
				apt2100 = p3.GetActorsByType("2100apt")
				mch2100 = p3.GetActorsByType("2100mch")
				tch2100 = p3.GetActorsByType("2100tch")
				rvw2100 = p3.GetActorsByType("2100rvw")
				mvw2100 = p3.GetActorsByType("2100mvw")
				rvw22100 = p3.GetActorsByType("2100rvw2")
				mvw22100 = p3.GetActorsByType("2100mvw2")
				scgoliath = p3.GetActorsByType("scgoliath")
				scvulture = p3.GetActorsByType("scvulture")
				scsiegetank = p3.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p4.GetActorsByType("citypolicecar")
				cityfiretruck = p4.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p4vehicle()
	end)
end
p5vehicle = function()
	if p5 and p5.IsBot then
		cityboteliteexpert = p5.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p6 and p5.IsAlliedWith(p6) then
				htnk = p6.GetActorsByType("htnk")
				msam = p6.GetActorsByType("msam")
				mtnk = p6.GetActorsByType("mtnk")
				apc = p6.GetActorsByType("apc")
				jeep = p6.GetActorsByType("jeep")
				stnk = p6.GetActorsByType("stnk")
				arty = p6.GetActorsByType("arty")
				ltnk = p6.GetActorsByType("ltnk")
				ftnk = p6.GetActorsByType("ftnk")
				mlrs = p6.GetActorsByType("mlrs")
				bike = p6.GetActorsByType("bike")
				bggy = p6.GetActorsByType("bggy")
				ctnk = p6.GetActorsByType("ctnk")
				tnk2 = p6.GetActorsByType("2tnk")
				raarty = p6.GetActorsByType("raarty")
				tnk1 = p6.GetActorsByType("1tnk")
				rajeep = p6.GetActorsByType("rajeep")
				tnk4 = p6.GetActorsByType("4tnk")
				v2rl = p6.GetActorsByType("v2rl")
				tnk3 = p6.GetActorsByType("3tnk")
				ttnk = p6.GetActorsByType("ttnk")
				ftrk = p6.GetActorsByType("ftrk")
				raapc = p6.GetActorsByType("raapc")
				hpt2100 = p6.GetActorsByType("2100hpt")
				apt2100 = p6.GetActorsByType("2100apt")
				mch2100 = p6.GetActorsByType("2100mch")
				tch2100 = p6.GetActorsByType("2100tch")
				rvw2100 = p6.GetActorsByType("2100rvw")
				mvw2100 = p6.GetActorsByType("2100mvw")
				rvw22100 = p6.GetActorsByType("2100rvw2")
				mvw22100 = p6.GetActorsByType("2100mvw2")
				scgoliath = p6.GetActorsByType("scgoliath")
				scvulture = p6.GetActorsByType("scvulture")
				scsiegetank = p6.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p7 and p5.IsAlliedWith(p7) then
				htnk = p7.GetActorsByType("htnk")
				msam = p7.GetActorsByType("msam")
				mtnk = p7.GetActorsByType("mtnk")
				apc = p7.GetActorsByType("apc")
				jeep = p7.GetActorsByType("jeep")
				stnk = p7.GetActorsByType("stnk")
				arty = p7.GetActorsByType("arty")
				ltnk = p7.GetActorsByType("ltnk")
				ftnk = p7.GetActorsByType("ftnk")
				mlrs = p7.GetActorsByType("mlrs")
				bike = p7.GetActorsByType("bike")
				bggy = p7.GetActorsByType("bggy")
				ctnk = p7.GetActorsByType("ctnk")
				tnk2 = p7.GetActorsByType("2tnk")
				raarty = p7.GetActorsByType("raarty")
				tnk1 = p7.GetActorsByType("1tnk")
				rajeep = p7.GetActorsByType("rajeep")
				tnk4 = p7.GetActorsByType("4tnk")
				v2rl = p7.GetActorsByType("v2rl")
				tnk3 = p7.GetActorsByType("3tnk")
				ttnk = p7.GetActorsByType("ttnk")
				ftrk = p7.GetActorsByType("ftrk")
				raapc = p7.GetActorsByType("raapc")
				hpt2100 = p7.GetActorsByType("2100hpt")
				apt2100 = p7.GetActorsByType("2100apt")
				mch2100 = p7.GetActorsByType("2100mch")
				tch2100 = p7.GetActorsByType("2100tch")
				rvw2100 = p7.GetActorsByType("2100rvw")
				mvw2100 = p7.GetActorsByType("2100mvw")
				rvw22100 = p7.GetActorsByType("2100rvw2")
				mvw22100 = p7.GetActorsByType("2100mvw2")
				scgoliath = p7.GetActorsByType("scgoliath")
				scvulture = p7.GetActorsByType("scvulture")
				scsiegetank = p7.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p8 and p5.IsAlliedWith(p8) then
				htnk = p8.GetActorsByType("htnk")
				msam = p8.GetActorsByType("msam")
				mtnk = p8.GetActorsByType("mtnk")
				apc = p8.GetActorsByType("apc")
				jeep = p8.GetActorsByType("jeep")
				stnk = p8.GetActorsByType("stnk")
				arty = p8.GetActorsByType("arty")
				ltnk = p8.GetActorsByType("ltnk")
				ftnk = p8.GetActorsByType("ftnk")
				mlrs = p8.GetActorsByType("mlrs")
				bike = p8.GetActorsByType("bike")
				bggy = p8.GetActorsByType("bggy")
				ctnk = p8.GetActorsByType("ctnk")
				tnk2 = p8.GetActorsByType("2tnk")
				raarty = p8.GetActorsByType("raarty")
				tnk1 = p8.GetActorsByType("1tnk")
				rajeep = p8.GetActorsByType("rajeep")
				tnk4 = p8.GetActorsByType("4tnk")
				v2rl = p8.GetActorsByType("v2rl")
				tnk3 = p8.GetActorsByType("3tnk")
				ttnk = p8.GetActorsByType("ttnk")
				ftrk = p8.GetActorsByType("ftrk")
				raapc = p8.GetActorsByType("raapc")
				hpt2100 = p8.GetActorsByType("2100hpt")
				apt2100 = p8.GetActorsByType("2100apt")
				mch2100 = p8.GetActorsByType("2100mch")
				tch2100 = p8.GetActorsByType("2100tch")
				rvw2100 = p8.GetActorsByType("2100rvw")
				mvw2100 = p8.GetActorsByType("2100mvw")
				rvw22100 = p8.GetActorsByType("2100rvw2")
				mvw22100 = p8.GetActorsByType("2100mvw2")
				scgoliath = p8.GetActorsByType("scgoliath")
				scvulture = p8.GetActorsByType("scvulture")
				scsiegetank = p8.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p9 and p5.IsAlliedWith(p9) then
				htnk = p9.GetActorsByType("htnk")
				msam = p9.GetActorsByType("msam")
				mtnk = p9.GetActorsByType("mtnk")
				apc = p9.GetActorsByType("apc")
				jeep = p9.GetActorsByType("jeep")
				stnk = p9.GetActorsByType("stnk")
				arty = p9.GetActorsByType("arty")
				ltnk = p9.GetActorsByType("ltnk")
				ftnk = p9.GetActorsByType("ftnk")
				mlrs = p9.GetActorsByType("mlrs")
				bike = p9.GetActorsByType("bike")
				bggy = p9.GetActorsByType("bggy")
				ctnk = p9.GetActorsByType("ctnk")
				tnk2 = p9.GetActorsByType("2tnk")
				raarty = p9.GetActorsByType("raarty")
				tnk1 = p9.GetActorsByType("1tnk")
				rajeep = p9.GetActorsByType("rajeep")
				tnk4 = p9.GetActorsByType("4tnk")
				v2rl = p9.GetActorsByType("v2rl")
				tnk3 = p9.GetActorsByType("3tnk")
				ttnk = p9.GetActorsByType("ttnk")
				ftrk = p9.GetActorsByType("ftrk")
				raapc = p9.GetActorsByType("raapc")
				hpt2100 = p9.GetActorsByType("2100hpt")
				apt2100 = p9.GetActorsByType("2100apt")
				mch2100 = p9.GetActorsByType("2100mch")
				tch2100 = p9.GetActorsByType("2100tch")
				rvw2100 = p9.GetActorsByType("2100rvw")
				mvw2100 = p9.GetActorsByType("2100mvw")
				rvw22100 = p9.GetActorsByType("2100rvw2")
				mvw22100 = p9.GetActorsByType("2100mvw2")
				scgoliath = p9.GetActorsByType("scgoliath")
				scvulture = p9.GetActorsByType("scvulture")
				scsiegetank = p9.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p10 and p5.IsAlliedWith(p10) then
				htnk = p10.GetActorsByType("htnk")
				msam = p10.GetActorsByType("msam")
				mtnk = p10.GetActorsByType("mtnk")
				apc = p10.GetActorsByType("apc")
				jeep = p10.GetActorsByType("jeep")
				stnk = p10.GetActorsByType("stnk")
				arty = p10.GetActorsByType("arty")
				ltnk = p10.GetActorsByType("ltnk")
				ftnk = p10.GetActorsByType("ftnk")
				mlrs = p10.GetActorsByType("mlrs")
				bike = p10.GetActorsByType("bike")
				bggy = p10.GetActorsByType("bggy")
				ctnk = p10.GetActorsByType("ctnk")
				tnk2 = p10.GetActorsByType("2tnk")
				raarty = p10.GetActorsByType("raarty")
				tnk1 = p10.GetActorsByType("1tnk")
				rajeep = p10.GetActorsByType("rajeep")
				tnk4 = p10.GetActorsByType("4tnk")
				v2rl = p10.GetActorsByType("v2rl")
				tnk3 = p10.GetActorsByType("3tnk")
				ttnk = p10.GetActorsByType("ttnk")
				ftrk = p10.GetActorsByType("ftrk")
				raapc = p10.GetActorsByType("raapc")
				hpt2100 = p10.GetActorsByType("2100hpt")
				apt2100 = p10.GetActorsByType("2100apt")
				mch2100 = p10.GetActorsByType("2100mch")
				tch2100 = p10.GetActorsByType("2100tch")
				rvw2100 = p10.GetActorsByType("2100rvw")
				mvw2100 = p10.GetActorsByType("2100mvw")
				rvw22100 = p10.GetActorsByType("2100rvw2")
				mvw22100 = p10.GetActorsByType("2100mvw2")
				scgoliath = p10.GetActorsByType("scgoliath")
				scvulture = p10.GetActorsByType("scvulture")
				scsiegetank = p10.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p11 and p5.IsAlliedWith(p11) then
				htnk = p11.GetActorsByType("htnk")
				msam = p11.GetActorsByType("msam")
				mtnk = p11.GetActorsByType("mtnk")
				apc = p11.GetActorsByType("apc")
				jeep = p11.GetActorsByType("jeep")
				stnk = p11.GetActorsByType("stnk")
				arty = p11.GetActorsByType("arty")
				ltnk = p11.GetActorsByType("ltnk")
				ftnk = p11.GetActorsByType("ftnk")
				mlrs = p11.GetActorsByType("mlrs")
				bike = p11.GetActorsByType("bike")
				bggy = p11.GetActorsByType("bggy")
				ctnk = p11.GetActorsByType("ctnk")
				tnk2 = p11.GetActorsByType("2tnk")
				raarty = p11.GetActorsByType("raarty")
				tnk1 = p11.GetActorsByType("1tnk")
				rajeep = p11.GetActorsByType("rajeep")
				tnk4 = p11.GetActorsByType("4tnk")
				v2rl = p11.GetActorsByType("v2rl")
				tnk3 = p11.GetActorsByType("3tnk")
				ttnk = p11.GetActorsByType("ttnk")
				ftrk = p11.GetActorsByType("ftrk")
				raapc = p11.GetActorsByType("raapc")
				hpt2100 = p11.GetActorsByType("2100hpt")
				apt2100 = p11.GetActorsByType("2100apt")
				mch2100 = p11.GetActorsByType("2100mch")
				tch2100 = p11.GetActorsByType("2100tch")
				rvw2100 = p11.GetActorsByType("2100rvw")
				mvw2100 = p11.GetActorsByType("2100mvw")
				rvw22100 = p11.GetActorsByType("2100rvw2")
				mvw22100 = p11.GetActorsByType("2100mvw2")
				scgoliath = p11.GetActorsByType("scgoliath")
				scvulture = p11.GetActorsByType("scvulture")
				scsiegetank = p11.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p12 and p5.IsAlliedWith(p12) then
				htnk = p12.GetActorsByType("htnk")
				msam = p12.GetActorsByType("msam")
				mtnk = p12.GetActorsByType("mtnk")
				apc = p12.GetActorsByType("apc")
				jeep = p12.GetActorsByType("jeep")
				stnk = p12.GetActorsByType("stnk")
				arty = p12.GetActorsByType("arty")
				ltnk = p12.GetActorsByType("ltnk")
				ftnk = p12.GetActorsByType("ftnk")
				mlrs = p12.GetActorsByType("mlrs")
				bike = p12.GetActorsByType("bike")
				bggy = p12.GetActorsByType("bggy")
				ctnk = p12.GetActorsByType("ctnk")
				tnk2 = p12.GetActorsByType("2tnk")
				raarty = p12.GetActorsByType("raarty")
				tnk1 = p12.GetActorsByType("1tnk")
				rajeep = p12.GetActorsByType("rajeep")
				tnk4 = p12.GetActorsByType("4tnk")
				v2rl = p12.GetActorsByType("v2rl")
				tnk3 = p12.GetActorsByType("3tnk")
				ttnk = p12.GetActorsByType("ttnk")
				ftrk = p12.GetActorsByType("ftrk")
				raapc = p12.GetActorsByType("raapc")
				hpt2100 = p12.GetActorsByType("2100hpt")
				apt2100 = p12.GetActorsByType("2100apt")
				mch2100 = p12.GetActorsByType("2100mch")
				tch2100 = p12.GetActorsByType("2100tch")
				rvw2100 = p12.GetActorsByType("2100rvw")
				mvw2100 = p12.GetActorsByType("2100mvw")
				rvw22100 = p12.GetActorsByType("2100rvw2")
				mvw22100 = p12.GetActorsByType("2100mvw2")
				scgoliath = p12.GetActorsByType("scgoliath")
				scvulture = p12.GetActorsByType("scvulture")
				scsiegetank = p12.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p13 and p5.IsAlliedWith(p13) then
				htnk = p13.GetActorsByType("htnk")
				msam = p13.GetActorsByType("msam")
				mtnk = p13.GetActorsByType("mtnk")
				apc = p13.GetActorsByType("apc")
				jeep = p13.GetActorsByType("jeep")
				stnk = p13.GetActorsByType("stnk")
				arty = p13.GetActorsByType("arty")
				ltnk = p13.GetActorsByType("ltnk")
				ftnk = p13.GetActorsByType("ftnk")
				mlrs = p13.GetActorsByType("mlrs")
				bike = p13.GetActorsByType("bike")
				bggy = p13.GetActorsByType("bggy")
				ctnk = p13.GetActorsByType("ctnk")
				tnk2 = p13.GetActorsByType("2tnk")
				raarty = p13.GetActorsByType("raarty")
				tnk1 = p13.GetActorsByType("1tnk")
				rajeep = p13.GetActorsByType("rajeep")
				tnk4 = p13.GetActorsByType("4tnk")
				v2rl = p13.GetActorsByType("v2rl")
				tnk3 = p13.GetActorsByType("3tnk")
				ttnk = p13.GetActorsByType("ttnk")
				ftrk = p13.GetActorsByType("ftrk")
				raapc = p13.GetActorsByType("raapc")
				hpt2100 = p13.GetActorsByType("2100hpt")
				apt2100 = p13.GetActorsByType("2100apt")
				mch2100 = p13.GetActorsByType("2100mch")
				tch2100 = p13.GetActorsByType("2100tch")
				rvw2100 = p13.GetActorsByType("2100rvw")
				mvw2100 = p13.GetActorsByType("2100mvw")
				rvw22100 = p13.GetActorsByType("2100rvw2")
				mvw22100 = p13.GetActorsByType("2100mvw2")
				scgoliath = p13.GetActorsByType("scgoliath")
				scvulture = p13.GetActorsByType("scvulture")
				scsiegetank = p13.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p14 and p5.IsAlliedWith(p14) then
				htnk = p14.GetActorsByType("htnk")
				msam = p14.GetActorsByType("msam")
				mtnk = p14.GetActorsByType("mtnk")
				apc = p14.GetActorsByType("apc")
				jeep = p14.GetActorsByType("jeep")
				stnk = p14.GetActorsByType("stnk")
				arty = p14.GetActorsByType("arty")
				ltnk = p14.GetActorsByType("ltnk")
				ftnk = p14.GetActorsByType("ftnk")
				mlrs = p14.GetActorsByType("mlrs")
				bike = p14.GetActorsByType("bike")
				bggy = p14.GetActorsByType("bggy")
				ctnk = p14.GetActorsByType("ctnk")
				tnk2 = p14.GetActorsByType("2tnk")
				raarty = p14.GetActorsByType("raarty")
				tnk1 = p14.GetActorsByType("1tnk")
				rajeep = p14.GetActorsByType("rajeep")
				tnk4 = p14.GetActorsByType("4tnk")
				v2rl = p14.GetActorsByType("v2rl")
				tnk3 = p14.GetActorsByType("3tnk")
				ttnk = p14.GetActorsByType("ttnk")
				ftrk = p14.GetActorsByType("ftrk")
				raapc = p14.GetActorsByType("raapc")
				hpt2100 = p14.GetActorsByType("2100hpt")
				apt2100 = p14.GetActorsByType("2100apt")
				mch2100 = p14.GetActorsByType("2100mch")
				tch2100 = p14.GetActorsByType("2100tch")
				rvw2100 = p14.GetActorsByType("2100rvw")
				mvw2100 = p14.GetActorsByType("2100mvw")
				rvw22100 = p14.GetActorsByType("2100rvw2")
				mvw22100 = p14.GetActorsByType("2100mvw2")
				scgoliath = p14.GetActorsByType("scgoliath")
				scvulture = p14.GetActorsByType("scvulture")
				scsiegetank = p14.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p15 and p5.IsAlliedWith(p15) then
				htnk = p15.GetActorsByType("htnk")
				msam = p15.GetActorsByType("msam")
				mtnk = p15.GetActorsByType("mtnk")
				apc = p15.GetActorsByType("apc")
				jeep = p15.GetActorsByType("jeep")
				stnk = p15.GetActorsByType("stnk")
				arty = p15.GetActorsByType("arty")
				ltnk = p15.GetActorsByType("ltnk")
				ftnk = p15.GetActorsByType("ftnk")
				mlrs = p15.GetActorsByType("mlrs")
				bike = p15.GetActorsByType("bike")
				bggy = p15.GetActorsByType("bggy")
				ctnk = p15.GetActorsByType("ctnk")
				tnk2 = p15.GetActorsByType("2tnk")
				raarty = p15.GetActorsByType("raarty")
				tnk1 = p15.GetActorsByType("1tnk")
				rajeep = p15.GetActorsByType("rajeep")
				tnk4 = p15.GetActorsByType("4tnk")
				v2rl = p15.GetActorsByType("v2rl")
				tnk3 = p15.GetActorsByType("3tnk")
				ttnk = p15.GetActorsByType("ttnk")
				ftrk = p15.GetActorsByType("ftrk")
				raapc = p15.GetActorsByType("raapc")
				hpt2100 = p15.GetActorsByType("2100hpt")
				apt2100 = p15.GetActorsByType("2100apt")
				mch2100 = p15.GetActorsByType("2100mch")
				tch2100 = p15.GetActorsByType("2100tch")
				rvw2100 = p15.GetActorsByType("2100rvw")
				mvw2100 = p15.GetActorsByType("2100mvw")
				rvw22100 = p15.GetActorsByType("2100rvw2")
				mvw22100 = p15.GetActorsByType("2100mvw2")
				scgoliath = p15.GetActorsByType("scgoliath")
				scvulture = p15.GetActorsByType("scvulture")
				scsiegetank = p15.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p16 and p5.IsAlliedWith(p16) then
				htnk = p16.GetActorsByType("htnk")
				msam = p16.GetActorsByType("msam")
				mtnk = p16.GetActorsByType("mtnk")
				apc = p16.GetActorsByType("apc")
				jeep = p16.GetActorsByType("jeep")
				stnk = p16.GetActorsByType("stnk")
				arty = p16.GetActorsByType("arty")
				ltnk = p16.GetActorsByType("ltnk")
				ftnk = p16.GetActorsByType("ftnk")
				mlrs = p16.GetActorsByType("mlrs")
				bike = p16.GetActorsByType("bike")
				bggy = p16.GetActorsByType("bggy")
				ctnk = p16.GetActorsByType("ctnk")
				tnk2 = p16.GetActorsByType("2tnk")
				raarty = p16.GetActorsByType("raarty")
				tnk1 = p16.GetActorsByType("1tnk")
				rajeep = p16.GetActorsByType("rajeep")
				tnk4 = p16.GetActorsByType("4tnk")
				v2rl = p16.GetActorsByType("v2rl")
				tnk3 = p16.GetActorsByType("3tnk")
				ttnk = p16.GetActorsByType("ttnk")
				ftrk = p16.GetActorsByType("ftrk")
				raapc = p16.GetActorsByType("raapc")
				hpt2100 = p16.GetActorsByType("2100hpt")
				apt2100 = p16.GetActorsByType("2100apt")
				mch2100 = p16.GetActorsByType("2100mch")
				tch2100 = p16.GetActorsByType("2100tch")
				rvw2100 = p16.GetActorsByType("2100rvw")
				mvw2100 = p16.GetActorsByType("2100mvw")
				rvw22100 = p16.GetActorsByType("2100rvw2")
				mvw22100 = p16.GetActorsByType("2100mvw2")
				scgoliath = p16.GetActorsByType("scgoliath")
				scvulture = p16.GetActorsByType("scvulture")
				scsiegetank = p16.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p1 and p5.IsAlliedWith(p1) then
				htnk = p1.GetActorsByType("htnk")
				msam = p1.GetActorsByType("msam")
				mtnk = p1.GetActorsByType("mtnk")
				apc = p1.GetActorsByType("apc")
				jeep = p1.GetActorsByType("jeep")
				stnk = p1.GetActorsByType("stnk")
				arty = p1.GetActorsByType("arty")
				ltnk = p1.GetActorsByType("ltnk")
				ftnk = p1.GetActorsByType("ftnk")
				mlrs = p1.GetActorsByType("mlrs")
				bike = p1.GetActorsByType("bike")
				bggy = p1.GetActorsByType("bggy")
				ctnk = p1.GetActorsByType("ctnk")
				tnk2 = p1.GetActorsByType("2tnk")
				raarty = p1.GetActorsByType("raarty")
				tnk1 = p1.GetActorsByType("1tnk")
				rajeep = p1.GetActorsByType("rajeep")
				tnk4 = p1.GetActorsByType("4tnk")
				v2rl = p1.GetActorsByType("v2rl")
				tnk3 = p1.GetActorsByType("3tnk")
				ttnk = p1.GetActorsByType("ttnk")
				ftrk = p1.GetActorsByType("ftrk")
				raapc = p1.GetActorsByType("raapc")
				hpt2100 = p1.GetActorsByType("2100hpt")
				apt2100 = p1.GetActorsByType("2100apt")
				mch2100 = p1.GetActorsByType("2100mch")
				tch2100 = p1.GetActorsByType("2100tch")
				rvw2100 = p1.GetActorsByType("2100rvw")
				mvw2100 = p1.GetActorsByType("2100mvw")
				rvw22100 = p1.GetActorsByType("2100rvw2")
				mvw22100 = p1.GetActorsByType("2100mvw2")
				scgoliath = p1.GetActorsByType("scgoliath")
				scvulture = p1.GetActorsByType("scvulture")
				scsiegetank = p1.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p2 and p5.IsAlliedWith(p2) then
				htnk = p2.GetActorsByType("htnk")
				msam = p2.GetActorsByType("msam")
				mtnk = p2.GetActorsByType("mtnk")
				apc = p2.GetActorsByType("apc")
				jeep = p2.GetActorsByType("jeep")
				stnk = p2.GetActorsByType("stnk")
				arty = p2.GetActorsByType("arty")
				ltnk = p2.GetActorsByType("ltnk")
				ftnk = p2.GetActorsByType("ftnk")
				mlrs = p2.GetActorsByType("mlrs")
				bike = p2.GetActorsByType("bike")
				bggy = p2.GetActorsByType("bggy")
				ctnk = p2.GetActorsByType("ctnk")
				tnk2 = p2.GetActorsByType("2tnk")
				raarty = p2.GetActorsByType("raarty")
				tnk1 = p2.GetActorsByType("1tnk")
				rajeep = p2.GetActorsByType("rajeep")
				tnk4 = p2.GetActorsByType("4tnk")
				v2rl = p2.GetActorsByType("v2rl")
				tnk3 = p2.GetActorsByType("3tnk")
				ttnk = p2.GetActorsByType("ttnk")
				ftrk = p2.GetActorsByType("ftrk")
				raapc = p2.GetActorsByType("raapc")
				hpt2100 = p2.GetActorsByType("2100hpt")
				apt2100 = p2.GetActorsByType("2100apt")
				mch2100 = p2.GetActorsByType("2100mch")
				tch2100 = p2.GetActorsByType("2100tch")
				rvw2100 = p2.GetActorsByType("2100rvw")
				mvw2100 = p2.GetActorsByType("2100mvw")
				rvw22100 = p2.GetActorsByType("2100rvw2")
				mvw22100 = p2.GetActorsByType("2100mvw2")
				scgoliath = p2.GetActorsByType("scgoliath")
				scvulture = p2.GetActorsByType("scvulture")
				scsiegetank = p2.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p3 and p5.IsAlliedWith(p3) then
				htnk = p3.GetActorsByType("htnk")
				msam = p3.GetActorsByType("msam")
				mtnk = p3.GetActorsByType("mtnk")
				apc = p3.GetActorsByType("apc")
				jeep = p3.GetActorsByType("jeep")
				stnk = p3.GetActorsByType("stnk")
				arty = p3.GetActorsByType("arty")
				ltnk = p3.GetActorsByType("ltnk")
				ftnk = p3.GetActorsByType("ftnk")
				mlrs = p3.GetActorsByType("mlrs")
				bike = p3.GetActorsByType("bike")
				bggy = p3.GetActorsByType("bggy")
				ctnk = p3.GetActorsByType("ctnk")
				tnk2 = p3.GetActorsByType("2tnk")
				raarty = p3.GetActorsByType("raarty")
				tnk1 = p3.GetActorsByType("1tnk")
				rajeep = p3.GetActorsByType("rajeep")
				tnk4 = p3.GetActorsByType("4tnk")
				v2rl = p3.GetActorsByType("v2rl")
				tnk3 = p3.GetActorsByType("3tnk")
				ttnk = p3.GetActorsByType("ttnk")
				ftrk = p3.GetActorsByType("ftrk")
				raapc = p3.GetActorsByType("raapc")
				hpt2100 = p3.GetActorsByType("2100hpt")
				apt2100 = p3.GetActorsByType("2100apt")
				mch2100 = p3.GetActorsByType("2100mch")
				tch2100 = p3.GetActorsByType("2100tch")
				rvw2100 = p3.GetActorsByType("2100rvw")
				mvw2100 = p3.GetActorsByType("2100mvw")
				rvw22100 = p3.GetActorsByType("2100rvw2")
				mvw22100 = p3.GetActorsByType("2100mvw2")
				scgoliath = p3.GetActorsByType("scgoliath")
				scvulture = p3.GetActorsByType("scvulture")
				scsiegetank = p3.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p4 and p5.IsAlliedWith(p4) then
				htnk = p4.GetActorsByType("htnk")
				msam = p4.GetActorsByType("msam")
				mtnk = p4.GetActorsByType("mtnk")
				apc = p4.GetActorsByType("apc")
				jeep = p4.GetActorsByType("jeep")
				stnk = p4.GetActorsByType("stnk")
				arty = p4.GetActorsByType("arty")
				ltnk = p4.GetActorsByType("ltnk")
				ftnk = p4.GetActorsByType("ftnk")
				mlrs = p4.GetActorsByType("mlrs")
				bike = p4.GetActorsByType("bike")
				bggy = p4.GetActorsByType("bggy")
				ctnk = p4.GetActorsByType("ctnk")
				tnk2 = p4.GetActorsByType("2tnk")
				raarty = p4.GetActorsByType("raarty")
				tnk1 = p4.GetActorsByType("1tnk")
				rajeep = p4.GetActorsByType("rajeep")
				tnk4 = p4.GetActorsByType("4tnk")
				v2rl = p4.GetActorsByType("v2rl")
				tnk3 = p4.GetActorsByType("3tnk")
				ttnk = p4.GetActorsByType("ttnk")
				ftrk = p4.GetActorsByType("ftrk")
				raapc = p4.GetActorsByType("raapc")
				hpt2100 = p4.GetActorsByType("2100hpt")
				apt2100 = p4.GetActorsByType("2100apt")
				mch2100 = p4.GetActorsByType("2100mch")
				tch2100 = p4.GetActorsByType("2100tch")
				rvw2100 = p4.GetActorsByType("2100rvw")
				mvw2100 = p4.GetActorsByType("2100mvw")
				rvw22100 = p4.GetActorsByType("2100rvw2")
				mvw22100 = p4.GetActorsByType("2100mvw2")
				scgoliath = p4.GetActorsByType("scgoliath")
				scvulture = p4.GetActorsByType("scvulture")
				scsiegetank = p4.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p5.GetActorsByType("citypolicecar")
				cityfiretruck = p5.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p5vehicle()
	end)
end
p6vehicle = function()
	if p6 and p6.IsBot then
		cityboteliteexpert = p6.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p7 and p6.IsAlliedWith(p7) then
				htnk = p7.GetActorsByType("htnk")
				msam = p7.GetActorsByType("msam")
				mtnk = p7.GetActorsByType("mtnk")
				apc = p7.GetActorsByType("apc")
				jeep = p7.GetActorsByType("jeep")
				stnk = p7.GetActorsByType("stnk")
				arty = p7.GetActorsByType("arty")
				ltnk = p7.GetActorsByType("ltnk")
				ftnk = p7.GetActorsByType("ftnk")
				mlrs = p7.GetActorsByType("mlrs")
				bike = p7.GetActorsByType("bike")
				bggy = p7.GetActorsByType("bggy")
				ctnk = p7.GetActorsByType("ctnk")
				tnk2 = p7.GetActorsByType("2tnk")
				raarty = p7.GetActorsByType("raarty")
				tnk1 = p7.GetActorsByType("1tnk")
				rajeep = p7.GetActorsByType("rajeep")
				tnk4 = p7.GetActorsByType("4tnk")
				v2rl = p7.GetActorsByType("v2rl")
				tnk3 = p7.GetActorsByType("3tnk")
				ttnk = p7.GetActorsByType("ttnk")
				ftrk = p7.GetActorsByType("ftrk")
				raapc = p7.GetActorsByType("raapc")
				hpt2100 = p7.GetActorsByType("2100hpt")
				apt2100 = p7.GetActorsByType("2100apt")
				mch2100 = p7.GetActorsByType("2100mch")
				tch2100 = p7.GetActorsByType("2100tch")
				rvw2100 = p7.GetActorsByType("2100rvw")
				mvw2100 = p7.GetActorsByType("2100mvw")
				rvw22100 = p7.GetActorsByType("2100rvw2")
				mvw22100 = p7.GetActorsByType("2100mvw2")
				scgoliath = p7.GetActorsByType("scgoliath")
				scvulture = p7.GetActorsByType("scvulture")
				scsiegetank = p7.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p8 and p6.IsAlliedWith(p8) then
				htnk = p8.GetActorsByType("htnk")
				msam = p8.GetActorsByType("msam")
				mtnk = p8.GetActorsByType("mtnk")
				apc = p8.GetActorsByType("apc")
				jeep = p8.GetActorsByType("jeep")
				stnk = p8.GetActorsByType("stnk")
				arty = p8.GetActorsByType("arty")
				ltnk = p8.GetActorsByType("ltnk")
				ftnk = p8.GetActorsByType("ftnk")
				mlrs = p8.GetActorsByType("mlrs")
				bike = p8.GetActorsByType("bike")
				bggy = p8.GetActorsByType("bggy")
				ctnk = p8.GetActorsByType("ctnk")
				tnk2 = p8.GetActorsByType("2tnk")
				raarty = p8.GetActorsByType("raarty")
				tnk1 = p8.GetActorsByType("1tnk")
				rajeep = p8.GetActorsByType("rajeep")
				tnk4 = p8.GetActorsByType("4tnk")
				v2rl = p8.GetActorsByType("v2rl")
				tnk3 = p8.GetActorsByType("3tnk")
				ttnk = p8.GetActorsByType("ttnk")
				ftrk = p8.GetActorsByType("ftrk")
				raapc = p8.GetActorsByType("raapc")
				hpt2100 = p8.GetActorsByType("2100hpt")
				apt2100 = p8.GetActorsByType("2100apt")
				mch2100 = p8.GetActorsByType("2100mch")
				tch2100 = p8.GetActorsByType("2100tch")
				rvw2100 = p8.GetActorsByType("2100rvw")
				mvw2100 = p8.GetActorsByType("2100mvw")
				rvw22100 = p8.GetActorsByType("2100rvw2")
				mvw22100 = p8.GetActorsByType("2100mvw2")
				scgoliath = p8.GetActorsByType("scgoliath")
				scvulture = p8.GetActorsByType("scvulture")
				scsiegetank = p8.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p9 and p6.IsAlliedWith(p9) then
				htnk = p9.GetActorsByType("htnk")
				msam = p9.GetActorsByType("msam")
				mtnk = p9.GetActorsByType("mtnk")
				apc = p9.GetActorsByType("apc")
				jeep = p9.GetActorsByType("jeep")
				stnk = p9.GetActorsByType("stnk")
				arty = p9.GetActorsByType("arty")
				ltnk = p9.GetActorsByType("ltnk")
				ftnk = p9.GetActorsByType("ftnk")
				mlrs = p9.GetActorsByType("mlrs")
				bike = p9.GetActorsByType("bike")
				bggy = p9.GetActorsByType("bggy")
				ctnk = p9.GetActorsByType("ctnk")
				tnk2 = p9.GetActorsByType("2tnk")
				raarty = p9.GetActorsByType("raarty")
				tnk1 = p9.GetActorsByType("1tnk")
				rajeep = p9.GetActorsByType("rajeep")
				tnk4 = p9.GetActorsByType("4tnk")
				v2rl = p9.GetActorsByType("v2rl")
				tnk3 = p9.GetActorsByType("3tnk")
				ttnk = p9.GetActorsByType("ttnk")
				ftrk = p9.GetActorsByType("ftrk")
				raapc = p9.GetActorsByType("raapc")
				hpt2100 = p9.GetActorsByType("2100hpt")
				apt2100 = p9.GetActorsByType("2100apt")
				mch2100 = p9.GetActorsByType("2100mch")
				tch2100 = p9.GetActorsByType("2100tch")
				rvw2100 = p9.GetActorsByType("2100rvw")
				mvw2100 = p9.GetActorsByType("2100mvw")
				rvw22100 = p9.GetActorsByType("2100rvw2")
				mvw22100 = p9.GetActorsByType("2100mvw2")
				scgoliath = p9.GetActorsByType("scgoliath")
				scvulture = p9.GetActorsByType("scvulture")
				scsiegetank = p9.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p10 and p6.IsAlliedWith(p10) then
				htnk = p10.GetActorsByType("htnk")
				msam = p10.GetActorsByType("msam")
				mtnk = p10.GetActorsByType("mtnk")
				apc = p10.GetActorsByType("apc")
				jeep = p10.GetActorsByType("jeep")
				stnk = p10.GetActorsByType("stnk")
				arty = p10.GetActorsByType("arty")
				ltnk = p10.GetActorsByType("ltnk")
				ftnk = p10.GetActorsByType("ftnk")
				mlrs = p10.GetActorsByType("mlrs")
				bike = p10.GetActorsByType("bike")
				bggy = p10.GetActorsByType("bggy")
				ctnk = p10.GetActorsByType("ctnk")
				tnk2 = p10.GetActorsByType("2tnk")
				raarty = p10.GetActorsByType("raarty")
				tnk1 = p10.GetActorsByType("1tnk")
				rajeep = p10.GetActorsByType("rajeep")
				tnk4 = p10.GetActorsByType("4tnk")
				v2rl = p10.GetActorsByType("v2rl")
				tnk3 = p10.GetActorsByType("3tnk")
				ttnk = p10.GetActorsByType("ttnk")
				ftrk = p10.GetActorsByType("ftrk")
				raapc = p10.GetActorsByType("raapc")
				hpt2100 = p10.GetActorsByType("2100hpt")
				apt2100 = p10.GetActorsByType("2100apt")
				mch2100 = p10.GetActorsByType("2100mch")
				tch2100 = p10.GetActorsByType("2100tch")
				rvw2100 = p10.GetActorsByType("2100rvw")
				mvw2100 = p10.GetActorsByType("2100mvw")
				rvw22100 = p10.GetActorsByType("2100rvw2")
				mvw22100 = p10.GetActorsByType("2100mvw2")
				scgoliath = p10.GetActorsByType("scgoliath")
				scvulture = p10.GetActorsByType("scvulture")
				scsiegetank = p10.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p11 and p6.IsAlliedWith(p11) then
				htnk = p11.GetActorsByType("htnk")
				msam = p11.GetActorsByType("msam")
				mtnk = p11.GetActorsByType("mtnk")
				apc = p11.GetActorsByType("apc")
				jeep = p11.GetActorsByType("jeep")
				stnk = p11.GetActorsByType("stnk")
				arty = p11.GetActorsByType("arty")
				ltnk = p11.GetActorsByType("ltnk")
				ftnk = p11.GetActorsByType("ftnk")
				mlrs = p11.GetActorsByType("mlrs")
				bike = p11.GetActorsByType("bike")
				bggy = p11.GetActorsByType("bggy")
				ctnk = p11.GetActorsByType("ctnk")
				tnk2 = p11.GetActorsByType("2tnk")
				raarty = p11.GetActorsByType("raarty")
				tnk1 = p11.GetActorsByType("1tnk")
				rajeep = p11.GetActorsByType("rajeep")
				tnk4 = p11.GetActorsByType("4tnk")
				v2rl = p11.GetActorsByType("v2rl")
				tnk3 = p11.GetActorsByType("3tnk")
				ttnk = p11.GetActorsByType("ttnk")
				ftrk = p11.GetActorsByType("ftrk")
				raapc = p11.GetActorsByType("raapc")
				hpt2100 = p11.GetActorsByType("2100hpt")
				apt2100 = p11.GetActorsByType("2100apt")
				mch2100 = p11.GetActorsByType("2100mch")
				tch2100 = p11.GetActorsByType("2100tch")
				rvw2100 = p11.GetActorsByType("2100rvw")
				mvw2100 = p11.GetActorsByType("2100mvw")
				rvw22100 = p11.GetActorsByType("2100rvw2")
				mvw22100 = p11.GetActorsByType("2100mvw2")
				scgoliath = p11.GetActorsByType("scgoliath")
				scvulture = p11.GetActorsByType("scvulture")
				scsiegetank = p11.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p12 and p6.IsAlliedWith(p12) then
				htnk = p12.GetActorsByType("htnk")
				msam = p12.GetActorsByType("msam")
				mtnk = p12.GetActorsByType("mtnk")
				apc = p12.GetActorsByType("apc")
				jeep = p12.GetActorsByType("jeep")
				stnk = p12.GetActorsByType("stnk")
				arty = p12.GetActorsByType("arty")
				ltnk = p12.GetActorsByType("ltnk")
				ftnk = p12.GetActorsByType("ftnk")
				mlrs = p12.GetActorsByType("mlrs")
				bike = p12.GetActorsByType("bike")
				bggy = p12.GetActorsByType("bggy")
				ctnk = p12.GetActorsByType("ctnk")
				tnk2 = p12.GetActorsByType("2tnk")
				raarty = p12.GetActorsByType("raarty")
				tnk1 = p12.GetActorsByType("1tnk")
				rajeep = p12.GetActorsByType("rajeep")
				tnk4 = p12.GetActorsByType("4tnk")
				v2rl = p12.GetActorsByType("v2rl")
				tnk3 = p12.GetActorsByType("3tnk")
				ttnk = p12.GetActorsByType("ttnk")
				ftrk = p12.GetActorsByType("ftrk")
				raapc = p12.GetActorsByType("raapc")
				hpt2100 = p12.GetActorsByType("2100hpt")
				apt2100 = p12.GetActorsByType("2100apt")
				mch2100 = p12.GetActorsByType("2100mch")
				tch2100 = p12.GetActorsByType("2100tch")
				rvw2100 = p12.GetActorsByType("2100rvw")
				mvw2100 = p12.GetActorsByType("2100mvw")
				rvw22100 = p12.GetActorsByType("2100rvw2")
				mvw22100 = p12.GetActorsByType("2100mvw2")
				scgoliath = p12.GetActorsByType("scgoliath")
				scvulture = p12.GetActorsByType("scvulture")
				scsiegetank = p12.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p13 and p6.IsAlliedWith(p13) then
				htnk = p13.GetActorsByType("htnk")
				msam = p13.GetActorsByType("msam")
				mtnk = p13.GetActorsByType("mtnk")
				apc = p13.GetActorsByType("apc")
				jeep = p13.GetActorsByType("jeep")
				stnk = p13.GetActorsByType("stnk")
				arty = p13.GetActorsByType("arty")
				ltnk = p13.GetActorsByType("ltnk")
				ftnk = p13.GetActorsByType("ftnk")
				mlrs = p13.GetActorsByType("mlrs")
				bike = p13.GetActorsByType("bike")
				bggy = p13.GetActorsByType("bggy")
				ctnk = p13.GetActorsByType("ctnk")
				tnk2 = p13.GetActorsByType("2tnk")
				raarty = p13.GetActorsByType("raarty")
				tnk1 = p13.GetActorsByType("1tnk")
				rajeep = p13.GetActorsByType("rajeep")
				tnk4 = p13.GetActorsByType("4tnk")
				v2rl = p13.GetActorsByType("v2rl")
				tnk3 = p13.GetActorsByType("3tnk")
				ttnk = p13.GetActorsByType("ttnk")
				ftrk = p13.GetActorsByType("ftrk")
				raapc = p13.GetActorsByType("raapc")
				hpt2100 = p13.GetActorsByType("2100hpt")
				apt2100 = p13.GetActorsByType("2100apt")
				mch2100 = p13.GetActorsByType("2100mch")
				tch2100 = p13.GetActorsByType("2100tch")
				rvw2100 = p13.GetActorsByType("2100rvw")
				mvw2100 = p13.GetActorsByType("2100mvw")
				rvw22100 = p13.GetActorsByType("2100rvw2")
				mvw22100 = p13.GetActorsByType("2100mvw2")
				scgoliath = p13.GetActorsByType("scgoliath")
				scvulture = p13.GetActorsByType("scvulture")
				scsiegetank = p13.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p14 and p6.IsAlliedWith(p14) then
				htnk = p14.GetActorsByType("htnk")
				msam = p14.GetActorsByType("msam")
				mtnk = p14.GetActorsByType("mtnk")
				apc = p14.GetActorsByType("apc")
				jeep = p14.GetActorsByType("jeep")
				stnk = p14.GetActorsByType("stnk")
				arty = p14.GetActorsByType("arty")
				ltnk = p14.GetActorsByType("ltnk")
				ftnk = p14.GetActorsByType("ftnk")
				mlrs = p14.GetActorsByType("mlrs")
				bike = p14.GetActorsByType("bike")
				bggy = p14.GetActorsByType("bggy")
				ctnk = p14.GetActorsByType("ctnk")
				tnk2 = p14.GetActorsByType("2tnk")
				raarty = p14.GetActorsByType("raarty")
				tnk1 = p14.GetActorsByType("1tnk")
				rajeep = p14.GetActorsByType("rajeep")
				tnk4 = p14.GetActorsByType("4tnk")
				v2rl = p14.GetActorsByType("v2rl")
				tnk3 = p14.GetActorsByType("3tnk")
				ttnk = p14.GetActorsByType("ttnk")
				ftrk = p14.GetActorsByType("ftrk")
				raapc = p14.GetActorsByType("raapc")
				hpt2100 = p14.GetActorsByType("2100hpt")
				apt2100 = p14.GetActorsByType("2100apt")
				mch2100 = p14.GetActorsByType("2100mch")
				tch2100 = p14.GetActorsByType("2100tch")
				rvw2100 = p14.GetActorsByType("2100rvw")
				mvw2100 = p14.GetActorsByType("2100mvw")
				rvw22100 = p14.GetActorsByType("2100rvw2")
				mvw22100 = p14.GetActorsByType("2100mvw2")
				scgoliath = p14.GetActorsByType("scgoliath")
				scvulture = p14.GetActorsByType("scvulture")
				scsiegetank = p14.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p15 and p6.IsAlliedWith(p15) then
				htnk = p15.GetActorsByType("htnk")
				msam = p15.GetActorsByType("msam")
				mtnk = p15.GetActorsByType("mtnk")
				apc = p15.GetActorsByType("apc")
				jeep = p15.GetActorsByType("jeep")
				stnk = p15.GetActorsByType("stnk")
				arty = p15.GetActorsByType("arty")
				ltnk = p15.GetActorsByType("ltnk")
				ftnk = p15.GetActorsByType("ftnk")
				mlrs = p15.GetActorsByType("mlrs")
				bike = p15.GetActorsByType("bike")
				bggy = p15.GetActorsByType("bggy")
				ctnk = p15.GetActorsByType("ctnk")
				tnk2 = p15.GetActorsByType("2tnk")
				raarty = p15.GetActorsByType("raarty")
				tnk1 = p15.GetActorsByType("1tnk")
				rajeep = p15.GetActorsByType("rajeep")
				tnk4 = p15.GetActorsByType("4tnk")
				v2rl = p15.GetActorsByType("v2rl")
				tnk3 = p15.GetActorsByType("3tnk")
				ttnk = p15.GetActorsByType("ttnk")
				ftrk = p15.GetActorsByType("ftrk")
				raapc = p15.GetActorsByType("raapc")
				hpt2100 = p15.GetActorsByType("2100hpt")
				apt2100 = p15.GetActorsByType("2100apt")
				mch2100 = p15.GetActorsByType("2100mch")
				tch2100 = p15.GetActorsByType("2100tch")
				rvw2100 = p15.GetActorsByType("2100rvw")
				mvw2100 = p15.GetActorsByType("2100mvw")
				rvw22100 = p15.GetActorsByType("2100rvw2")
				mvw22100 = p15.GetActorsByType("2100mvw2")
				scgoliath = p15.GetActorsByType("scgoliath")
				scvulture = p15.GetActorsByType("scvulture")
				scsiegetank = p15.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p16 and p6.IsAlliedWith(p16) then
				htnk = p16.GetActorsByType("htnk")
				msam = p16.GetActorsByType("msam")
				mtnk = p16.GetActorsByType("mtnk")
				apc = p16.GetActorsByType("apc")
				jeep = p16.GetActorsByType("jeep")
				stnk = p16.GetActorsByType("stnk")
				arty = p16.GetActorsByType("arty")
				ltnk = p16.GetActorsByType("ltnk")
				ftnk = p16.GetActorsByType("ftnk")
				mlrs = p16.GetActorsByType("mlrs")
				bike = p16.GetActorsByType("bike")
				bggy = p16.GetActorsByType("bggy")
				ctnk = p16.GetActorsByType("ctnk")
				tnk2 = p16.GetActorsByType("2tnk")
				raarty = p16.GetActorsByType("raarty")
				tnk1 = p16.GetActorsByType("1tnk")
				rajeep = p16.GetActorsByType("rajeep")
				tnk4 = p16.GetActorsByType("4tnk")
				v2rl = p16.GetActorsByType("v2rl")
				tnk3 = p16.GetActorsByType("3tnk")
				ttnk = p16.GetActorsByType("ttnk")
				ftrk = p16.GetActorsByType("ftrk")
				raapc = p16.GetActorsByType("raapc")
				hpt2100 = p16.GetActorsByType("2100hpt")
				apt2100 = p16.GetActorsByType("2100apt")
				mch2100 = p16.GetActorsByType("2100mch")
				tch2100 = p16.GetActorsByType("2100tch")
				rvw2100 = p16.GetActorsByType("2100rvw")
				mvw2100 = p16.GetActorsByType("2100mvw")
				rvw22100 = p16.GetActorsByType("2100rvw2")
				mvw22100 = p16.GetActorsByType("2100mvw2")
				scgoliath = p16.GetActorsByType("scgoliath")
				scvulture = p16.GetActorsByType("scvulture")
				scsiegetank = p16.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p1 and p6.IsAlliedWith(p1) then
				htnk = p1.GetActorsByType("htnk")
				msam = p1.GetActorsByType("msam")
				mtnk = p1.GetActorsByType("mtnk")
				apc = p1.GetActorsByType("apc")
				jeep = p1.GetActorsByType("jeep")
				stnk = p1.GetActorsByType("stnk")
				arty = p1.GetActorsByType("arty")
				ltnk = p1.GetActorsByType("ltnk")
				ftnk = p1.GetActorsByType("ftnk")
				mlrs = p1.GetActorsByType("mlrs")
				bike = p1.GetActorsByType("bike")
				bggy = p1.GetActorsByType("bggy")
				ctnk = p1.GetActorsByType("ctnk")
				tnk2 = p1.GetActorsByType("2tnk")
				raarty = p1.GetActorsByType("raarty")
				tnk1 = p1.GetActorsByType("1tnk")
				rajeep = p1.GetActorsByType("rajeep")
				tnk4 = p1.GetActorsByType("4tnk")
				v2rl = p1.GetActorsByType("v2rl")
				tnk3 = p1.GetActorsByType("3tnk")
				ttnk = p1.GetActorsByType("ttnk")
				ftrk = p1.GetActorsByType("ftrk")
				raapc = p1.GetActorsByType("raapc")
				hpt2100 = p1.GetActorsByType("2100hpt")
				apt2100 = p1.GetActorsByType("2100apt")
				mch2100 = p1.GetActorsByType("2100mch")
				tch2100 = p1.GetActorsByType("2100tch")
				rvw2100 = p1.GetActorsByType("2100rvw")
				mvw2100 = p1.GetActorsByType("2100mvw")
				rvw22100 = p1.GetActorsByType("2100rvw2")
				mvw22100 = p1.GetActorsByType("2100mvw2")
				scgoliath = p1.GetActorsByType("scgoliath")
				scvulture = p1.GetActorsByType("scvulture")
				scsiegetank = p1.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p2 and p6.IsAlliedWith(p2) then
				htnk = p2.GetActorsByType("htnk")
				msam = p2.GetActorsByType("msam")
				mtnk = p2.GetActorsByType("mtnk")
				apc = p2.GetActorsByType("apc")
				jeep = p2.GetActorsByType("jeep")
				stnk = p2.GetActorsByType("stnk")
				arty = p2.GetActorsByType("arty")
				ltnk = p2.GetActorsByType("ltnk")
				ftnk = p2.GetActorsByType("ftnk")
				mlrs = p2.GetActorsByType("mlrs")
				bike = p2.GetActorsByType("bike")
				bggy = p2.GetActorsByType("bggy")
				ctnk = p2.GetActorsByType("ctnk")
				tnk2 = p2.GetActorsByType("2tnk")
				raarty = p2.GetActorsByType("raarty")
				tnk1 = p2.GetActorsByType("1tnk")
				rajeep = p2.GetActorsByType("rajeep")
				tnk4 = p2.GetActorsByType("4tnk")
				v2rl = p2.GetActorsByType("v2rl")
				tnk3 = p2.GetActorsByType("3tnk")
				ttnk = p2.GetActorsByType("ttnk")
				ftrk = p2.GetActorsByType("ftrk")
				raapc = p2.GetActorsByType("raapc")
				hpt2100 = p2.GetActorsByType("2100hpt")
				apt2100 = p2.GetActorsByType("2100apt")
				mch2100 = p2.GetActorsByType("2100mch")
				tch2100 = p2.GetActorsByType("2100tch")
				rvw2100 = p2.GetActorsByType("2100rvw")
				mvw2100 = p2.GetActorsByType("2100mvw")
				rvw22100 = p2.GetActorsByType("2100rvw2")
				mvw22100 = p2.GetActorsByType("2100mvw2")
				scgoliath = p2.GetActorsByType("scgoliath")
				scvulture = p2.GetActorsByType("scvulture")
				scsiegetank = p2.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p3 and p6.IsAlliedWith(p3) then
				htnk = p3.GetActorsByType("htnk")
				msam = p3.GetActorsByType("msam")
				mtnk = p3.GetActorsByType("mtnk")
				apc = p3.GetActorsByType("apc")
				jeep = p3.GetActorsByType("jeep")
				stnk = p3.GetActorsByType("stnk")
				arty = p3.GetActorsByType("arty")
				ltnk = p3.GetActorsByType("ltnk")
				ftnk = p3.GetActorsByType("ftnk")
				mlrs = p3.GetActorsByType("mlrs")
				bike = p3.GetActorsByType("bike")
				bggy = p3.GetActorsByType("bggy")
				ctnk = p3.GetActorsByType("ctnk")
				tnk2 = p3.GetActorsByType("2tnk")
				raarty = p3.GetActorsByType("raarty")
				tnk1 = p3.GetActorsByType("1tnk")
				rajeep = p3.GetActorsByType("rajeep")
				tnk4 = p3.GetActorsByType("4tnk")
				v2rl = p3.GetActorsByType("v2rl")
				tnk3 = p3.GetActorsByType("3tnk")
				ttnk = p3.GetActorsByType("ttnk")
				ftrk = p3.GetActorsByType("ftrk")
				raapc = p3.GetActorsByType("raapc")
				hpt2100 = p3.GetActorsByType("2100hpt")
				apt2100 = p3.GetActorsByType("2100apt")
				mch2100 = p3.GetActorsByType("2100mch")
				tch2100 = p3.GetActorsByType("2100tch")
				rvw2100 = p3.GetActorsByType("2100rvw")
				mvw2100 = p3.GetActorsByType("2100mvw")
				rvw22100 = p3.GetActorsByType("2100rvw2")
				mvw22100 = p3.GetActorsByType("2100mvw2")
				scgoliath = p3.GetActorsByType("scgoliath")
				scvulture = p3.GetActorsByType("scvulture")
				scsiegetank = p3.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p4 and p6.IsAlliedWith(p4) then
				htnk = p4.GetActorsByType("htnk")
				msam = p4.GetActorsByType("msam")
				mtnk = p4.GetActorsByType("mtnk")
				apc = p4.GetActorsByType("apc")
				jeep = p4.GetActorsByType("jeep")
				stnk = p4.GetActorsByType("stnk")
				arty = p4.GetActorsByType("arty")
				ltnk = p4.GetActorsByType("ltnk")
				ftnk = p4.GetActorsByType("ftnk")
				mlrs = p4.GetActorsByType("mlrs")
				bike = p4.GetActorsByType("bike")
				bggy = p4.GetActorsByType("bggy")
				ctnk = p4.GetActorsByType("ctnk")
				tnk2 = p4.GetActorsByType("2tnk")
				raarty = p4.GetActorsByType("raarty")
				tnk1 = p4.GetActorsByType("1tnk")
				rajeep = p4.GetActorsByType("rajeep")
				tnk4 = p4.GetActorsByType("4tnk")
				v2rl = p4.GetActorsByType("v2rl")
				tnk3 = p4.GetActorsByType("3tnk")
				ttnk = p4.GetActorsByType("ttnk")
				ftrk = p4.GetActorsByType("ftrk")
				raapc = p4.GetActorsByType("raapc")
				hpt2100 = p4.GetActorsByType("2100hpt")
				apt2100 = p4.GetActorsByType("2100apt")
				mch2100 = p4.GetActorsByType("2100mch")
				tch2100 = p4.GetActorsByType("2100tch")
				rvw2100 = p4.GetActorsByType("2100rvw")
				mvw2100 = p4.GetActorsByType("2100mvw")
				rvw22100 = p4.GetActorsByType("2100rvw2")
				mvw22100 = p4.GetActorsByType("2100mvw2")
				scgoliath = p4.GetActorsByType("scgoliath")
				scvulture = p4.GetActorsByType("scvulture")
				scsiegetank = p4.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p5 and p6.IsAlliedWith(p5) then
				htnk = p5.GetActorsByType("htnk")
				msam = p5.GetActorsByType("msam")
				mtnk = p5.GetActorsByType("mtnk")
				apc = p5.GetActorsByType("apc")
				jeep = p5.GetActorsByType("jeep")
				stnk = p5.GetActorsByType("stnk")
				arty = p5.GetActorsByType("arty")
				ltnk = p5.GetActorsByType("ltnk")
				ftnk = p5.GetActorsByType("ftnk")
				mlrs = p5.GetActorsByType("mlrs")
				bike = p5.GetActorsByType("bike")
				bggy = p5.GetActorsByType("bggy")
				ctnk = p5.GetActorsByType("ctnk")
				tnk2 = p5.GetActorsByType("2tnk")
				raarty = p5.GetActorsByType("raarty")
				tnk1 = p5.GetActorsByType("1tnk")
				rajeep = p5.GetActorsByType("rajeep")
				tnk4 = p5.GetActorsByType("4tnk")
				v2rl = p5.GetActorsByType("v2rl")
				tnk3 = p5.GetActorsByType("3tnk")
				ttnk = p5.GetActorsByType("ttnk")
				ftrk = p5.GetActorsByType("ftrk")
				raapc = p5.GetActorsByType("raapc")
				hpt2100 = p5.GetActorsByType("2100hpt")
				apt2100 = p5.GetActorsByType("2100apt")
				mch2100 = p5.GetActorsByType("2100mch")
				tch2100 = p5.GetActorsByType("2100tch")
				rvw2100 = p5.GetActorsByType("2100rvw")
				mvw2100 = p5.GetActorsByType("2100mvw")
				rvw22100 = p5.GetActorsByType("2100rvw2")
				mvw22100 = p5.GetActorsByType("2100mvw2")
				scgoliath = p5.GetActorsByType("scgoliath")
				scvulture = p5.GetActorsByType("scvulture")
				scsiegetank = p5.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p6.GetActorsByType("citypolicecar")
				cityfiretruck = p6.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p6vehicle()
	end)
end
p7vehicle = function()
	if p7 and p7.IsBot then
		cityboteliteexpert = p7.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p8 and p7.IsAlliedWith(p8) then
				htnk = p8.GetActorsByType("htnk")
				msam = p8.GetActorsByType("msam")
				mtnk = p8.GetActorsByType("mtnk")
				apc = p8.GetActorsByType("apc")
				jeep = p8.GetActorsByType("jeep")
				stnk = p8.GetActorsByType("stnk")
				arty = p8.GetActorsByType("arty")
				ltnk = p8.GetActorsByType("ltnk")
				ftnk = p8.GetActorsByType("ftnk")
				mlrs = p8.GetActorsByType("mlrs")
				bike = p8.GetActorsByType("bike")
				bggy = p8.GetActorsByType("bggy")
				ctnk = p8.GetActorsByType("ctnk")
				tnk2 = p8.GetActorsByType("2tnk")
				raarty = p8.GetActorsByType("raarty")
				tnk1 = p8.GetActorsByType("1tnk")
				rajeep = p8.GetActorsByType("rajeep")
				tnk4 = p8.GetActorsByType("4tnk")
				v2rl = p8.GetActorsByType("v2rl")
				tnk3 = p8.GetActorsByType("3tnk")
				ttnk = p8.GetActorsByType("ttnk")
				ftrk = p8.GetActorsByType("ftrk")
				raapc = p8.GetActorsByType("raapc")
				hpt2100 = p8.GetActorsByType("2100hpt")
				apt2100 = p8.GetActorsByType("2100apt")
				mch2100 = p8.GetActorsByType("2100mch")
				tch2100 = p8.GetActorsByType("2100tch")
				rvw2100 = p8.GetActorsByType("2100rvw")
				mvw2100 = p8.GetActorsByType("2100mvw")
				rvw22100 = p8.GetActorsByType("2100rvw2")
				mvw22100 = p8.GetActorsByType("2100mvw2")
				scgoliath = p8.GetActorsByType("scgoliath")
				scvulture = p8.GetActorsByType("scvulture")
				scsiegetank = p8.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p9 and p7.IsAlliedWith(p9) then
				htnk = p9.GetActorsByType("htnk")
				msam = p9.GetActorsByType("msam")
				mtnk = p9.GetActorsByType("mtnk")
				apc = p9.GetActorsByType("apc")
				jeep = p9.GetActorsByType("jeep")
				stnk = p9.GetActorsByType("stnk")
				arty = p9.GetActorsByType("arty")
				ltnk = p9.GetActorsByType("ltnk")
				ftnk = p9.GetActorsByType("ftnk")
				mlrs = p9.GetActorsByType("mlrs")
				bike = p9.GetActorsByType("bike")
				bggy = p9.GetActorsByType("bggy")
				ctnk = p9.GetActorsByType("ctnk")
				tnk2 = p9.GetActorsByType("2tnk")
				raarty = p9.GetActorsByType("raarty")
				tnk1 = p9.GetActorsByType("1tnk")
				rajeep = p9.GetActorsByType("rajeep")
				tnk4 = p9.GetActorsByType("4tnk")
				v2rl = p9.GetActorsByType("v2rl")
				tnk3 = p9.GetActorsByType("3tnk")
				ttnk = p9.GetActorsByType("ttnk")
				ftrk = p9.GetActorsByType("ftrk")
				raapc = p9.GetActorsByType("raapc")
				hpt2100 = p9.GetActorsByType("2100hpt")
				apt2100 = p9.GetActorsByType("2100apt")
				mch2100 = p9.GetActorsByType("2100mch")
				tch2100 = p9.GetActorsByType("2100tch")
				rvw2100 = p9.GetActorsByType("2100rvw")
				mvw2100 = p9.GetActorsByType("2100mvw")
				rvw22100 = p9.GetActorsByType("2100rvw2")
				mvw22100 = p9.GetActorsByType("2100mvw2")
				scgoliath = p9.GetActorsByType("scgoliath")
				scvulture = p9.GetActorsByType("scvulture")
				scsiegetank = p9.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p10 and p7.IsAlliedWith(p10) then
				htnk = p10.GetActorsByType("htnk")
				msam = p10.GetActorsByType("msam")
				mtnk = p10.GetActorsByType("mtnk")
				apc = p10.GetActorsByType("apc")
				jeep = p10.GetActorsByType("jeep")
				stnk = p10.GetActorsByType("stnk")
				arty = p10.GetActorsByType("arty")
				ltnk = p10.GetActorsByType("ltnk")
				ftnk = p10.GetActorsByType("ftnk")
				mlrs = p10.GetActorsByType("mlrs")
				bike = p10.GetActorsByType("bike")
				bggy = p10.GetActorsByType("bggy")
				ctnk = p10.GetActorsByType("ctnk")
				tnk2 = p10.GetActorsByType("2tnk")
				raarty = p10.GetActorsByType("raarty")
				tnk1 = p10.GetActorsByType("1tnk")
				rajeep = p10.GetActorsByType("rajeep")
				tnk4 = p10.GetActorsByType("4tnk")
				v2rl = p10.GetActorsByType("v2rl")
				tnk3 = p10.GetActorsByType("3tnk")
				ttnk = p10.GetActorsByType("ttnk")
				ftrk = p10.GetActorsByType("ftrk")
				raapc = p10.GetActorsByType("raapc")
				hpt2100 = p10.GetActorsByType("2100hpt")
				apt2100 = p10.GetActorsByType("2100apt")
				mch2100 = p10.GetActorsByType("2100mch")
				tch2100 = p10.GetActorsByType("2100tch")
				rvw2100 = p10.GetActorsByType("2100rvw")
				mvw2100 = p10.GetActorsByType("2100mvw")
				rvw22100 = p10.GetActorsByType("2100rvw2")
				mvw22100 = p10.GetActorsByType("2100mvw2")
				scgoliath = p10.GetActorsByType("scgoliath")
				scvulture = p10.GetActorsByType("scvulture")
				scsiegetank = p10.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p11 and p7.IsAlliedWith(p11) then
				htnk = p11.GetActorsByType("htnk")
				msam = p11.GetActorsByType("msam")
				mtnk = p11.GetActorsByType("mtnk")
				apc = p11.GetActorsByType("apc")
				jeep = p11.GetActorsByType("jeep")
				stnk = p11.GetActorsByType("stnk")
				arty = p11.GetActorsByType("arty")
				ltnk = p11.GetActorsByType("ltnk")
				ftnk = p11.GetActorsByType("ftnk")
				mlrs = p11.GetActorsByType("mlrs")
				bike = p11.GetActorsByType("bike")
				bggy = p11.GetActorsByType("bggy")
				ctnk = p11.GetActorsByType("ctnk")
				tnk2 = p11.GetActorsByType("2tnk")
				raarty = p11.GetActorsByType("raarty")
				tnk1 = p11.GetActorsByType("1tnk")
				rajeep = p11.GetActorsByType("rajeep")
				tnk4 = p11.GetActorsByType("4tnk")
				v2rl = p11.GetActorsByType("v2rl")
				tnk3 = p11.GetActorsByType("3tnk")
				ttnk = p11.GetActorsByType("ttnk")
				ftrk = p11.GetActorsByType("ftrk")
				raapc = p11.GetActorsByType("raapc")
				hpt2100 = p11.GetActorsByType("2100hpt")
				apt2100 = p11.GetActorsByType("2100apt")
				mch2100 = p11.GetActorsByType("2100mch")
				tch2100 = p11.GetActorsByType("2100tch")
				rvw2100 = p11.GetActorsByType("2100rvw")
				mvw2100 = p11.GetActorsByType("2100mvw")
				rvw22100 = p11.GetActorsByType("2100rvw2")
				mvw22100 = p11.GetActorsByType("2100mvw2")
				scgoliath = p11.GetActorsByType("scgoliath")
				scvulture = p11.GetActorsByType("scvulture")
				scsiegetank = p11.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p12 and p7.IsAlliedWith(p12) then
				htnk = p12.GetActorsByType("htnk")
				msam = p12.GetActorsByType("msam")
				mtnk = p12.GetActorsByType("mtnk")
				apc = p12.GetActorsByType("apc")
				jeep = p12.GetActorsByType("jeep")
				stnk = p12.GetActorsByType("stnk")
				arty = p12.GetActorsByType("arty")
				ltnk = p12.GetActorsByType("ltnk")
				ftnk = p12.GetActorsByType("ftnk")
				mlrs = p12.GetActorsByType("mlrs")
				bike = p12.GetActorsByType("bike")
				bggy = p12.GetActorsByType("bggy")
				ctnk = p12.GetActorsByType("ctnk")
				tnk2 = p12.GetActorsByType("2tnk")
				raarty = p12.GetActorsByType("raarty")
				tnk1 = p12.GetActorsByType("1tnk")
				rajeep = p12.GetActorsByType("rajeep")
				tnk4 = p12.GetActorsByType("4tnk")
				v2rl = p12.GetActorsByType("v2rl")
				tnk3 = p12.GetActorsByType("3tnk")
				ttnk = p12.GetActorsByType("ttnk")
				ftrk = p12.GetActorsByType("ftrk")
				raapc = p12.GetActorsByType("raapc")
				hpt2100 = p12.GetActorsByType("2100hpt")
				apt2100 = p12.GetActorsByType("2100apt")
				mch2100 = p12.GetActorsByType("2100mch")
				tch2100 = p12.GetActorsByType("2100tch")
				rvw2100 = p12.GetActorsByType("2100rvw")
				mvw2100 = p12.GetActorsByType("2100mvw")
				rvw22100 = p12.GetActorsByType("2100rvw2")
				mvw22100 = p12.GetActorsByType("2100mvw2")
				scgoliath = p12.GetActorsByType("scgoliath")
				scvulture = p12.GetActorsByType("scvulture")
				scsiegetank = p12.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p13 and p7.IsAlliedWith(p13) then
				htnk = p13.GetActorsByType("htnk")
				msam = p13.GetActorsByType("msam")
				mtnk = p13.GetActorsByType("mtnk")
				apc = p13.GetActorsByType("apc")
				jeep = p13.GetActorsByType("jeep")
				stnk = p13.GetActorsByType("stnk")
				arty = p13.GetActorsByType("arty")
				ltnk = p13.GetActorsByType("ltnk")
				ftnk = p13.GetActorsByType("ftnk")
				mlrs = p13.GetActorsByType("mlrs")
				bike = p13.GetActorsByType("bike")
				bggy = p13.GetActorsByType("bggy")
				ctnk = p13.GetActorsByType("ctnk")
				tnk2 = p13.GetActorsByType("2tnk")
				raarty = p13.GetActorsByType("raarty")
				tnk1 = p13.GetActorsByType("1tnk")
				rajeep = p13.GetActorsByType("rajeep")
				tnk4 = p13.GetActorsByType("4tnk")
				v2rl = p13.GetActorsByType("v2rl")
				tnk3 = p13.GetActorsByType("3tnk")
				ttnk = p13.GetActorsByType("ttnk")
				ftrk = p13.GetActorsByType("ftrk")
				raapc = p13.GetActorsByType("raapc")
				hpt2100 = p13.GetActorsByType("2100hpt")
				apt2100 = p13.GetActorsByType("2100apt")
				mch2100 = p13.GetActorsByType("2100mch")
				tch2100 = p13.GetActorsByType("2100tch")
				rvw2100 = p13.GetActorsByType("2100rvw")
				mvw2100 = p13.GetActorsByType("2100mvw")
				rvw22100 = p13.GetActorsByType("2100rvw2")
				mvw22100 = p13.GetActorsByType("2100mvw2")
				scgoliath = p13.GetActorsByType("scgoliath")
				scvulture = p13.GetActorsByType("scvulture")
				scsiegetank = p13.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p14 and p7.IsAlliedWith(p14) then
				htnk = p14.GetActorsByType("htnk")
				msam = p14.GetActorsByType("msam")
				mtnk = p14.GetActorsByType("mtnk")
				apc = p14.GetActorsByType("apc")
				jeep = p14.GetActorsByType("jeep")
				stnk = p14.GetActorsByType("stnk")
				arty = p14.GetActorsByType("arty")
				ltnk = p14.GetActorsByType("ltnk")
				ftnk = p14.GetActorsByType("ftnk")
				mlrs = p14.GetActorsByType("mlrs")
				bike = p14.GetActorsByType("bike")
				bggy = p14.GetActorsByType("bggy")
				ctnk = p14.GetActorsByType("ctnk")
				tnk2 = p14.GetActorsByType("2tnk")
				raarty = p14.GetActorsByType("raarty")
				tnk1 = p14.GetActorsByType("1tnk")
				rajeep = p14.GetActorsByType("rajeep")
				tnk4 = p14.GetActorsByType("4tnk")
				v2rl = p14.GetActorsByType("v2rl")
				tnk3 = p14.GetActorsByType("3tnk")
				ttnk = p14.GetActorsByType("ttnk")
				ftrk = p14.GetActorsByType("ftrk")
				raapc = p14.GetActorsByType("raapc")
				hpt2100 = p14.GetActorsByType("2100hpt")
				apt2100 = p14.GetActorsByType("2100apt")
				mch2100 = p14.GetActorsByType("2100mch")
				tch2100 = p14.GetActorsByType("2100tch")
				rvw2100 = p14.GetActorsByType("2100rvw")
				mvw2100 = p14.GetActorsByType("2100mvw")
				rvw22100 = p14.GetActorsByType("2100rvw2")
				mvw22100 = p14.GetActorsByType("2100mvw2")
				scgoliath = p14.GetActorsByType("scgoliath")
				scvulture = p14.GetActorsByType("scvulture")
				scsiegetank = p14.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p15 and p7.IsAlliedWith(p15) then
				htnk = p15.GetActorsByType("htnk")
				msam = p15.GetActorsByType("msam")
				mtnk = p15.GetActorsByType("mtnk")
				apc = p15.GetActorsByType("apc")
				jeep = p15.GetActorsByType("jeep")
				stnk = p15.GetActorsByType("stnk")
				arty = p15.GetActorsByType("arty")
				ltnk = p15.GetActorsByType("ltnk")
				ftnk = p15.GetActorsByType("ftnk")
				mlrs = p15.GetActorsByType("mlrs")
				bike = p15.GetActorsByType("bike")
				bggy = p15.GetActorsByType("bggy")
				ctnk = p15.GetActorsByType("ctnk")
				tnk2 = p15.GetActorsByType("2tnk")
				raarty = p15.GetActorsByType("raarty")
				tnk1 = p15.GetActorsByType("1tnk")
				rajeep = p15.GetActorsByType("rajeep")
				tnk4 = p15.GetActorsByType("4tnk")
				v2rl = p15.GetActorsByType("v2rl")
				tnk3 = p15.GetActorsByType("3tnk")
				ttnk = p15.GetActorsByType("ttnk")
				ftrk = p15.GetActorsByType("ftrk")
				raapc = p15.GetActorsByType("raapc")
				hpt2100 = p15.GetActorsByType("2100hpt")
				apt2100 = p15.GetActorsByType("2100apt")
				mch2100 = p15.GetActorsByType("2100mch")
				tch2100 = p15.GetActorsByType("2100tch")
				rvw2100 = p15.GetActorsByType("2100rvw")
				mvw2100 = p15.GetActorsByType("2100mvw")
				rvw22100 = p15.GetActorsByType("2100rvw2")
				mvw22100 = p15.GetActorsByType("2100mvw2")
				scgoliath = p15.GetActorsByType("scgoliath")
				scvulture = p15.GetActorsByType("scvulture")
				scsiegetank = p15.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p16 and p7.IsAlliedWith(p16) then
				htnk = p16.GetActorsByType("htnk")
				msam = p16.GetActorsByType("msam")
				mtnk = p16.GetActorsByType("mtnk")
				apc = p16.GetActorsByType("apc")
				jeep = p16.GetActorsByType("jeep")
				stnk = p16.GetActorsByType("stnk")
				arty = p16.GetActorsByType("arty")
				ltnk = p16.GetActorsByType("ltnk")
				ftnk = p16.GetActorsByType("ftnk")
				mlrs = p16.GetActorsByType("mlrs")
				bike = p16.GetActorsByType("bike")
				bggy = p16.GetActorsByType("bggy")
				ctnk = p16.GetActorsByType("ctnk")
				tnk2 = p16.GetActorsByType("2tnk")
				raarty = p16.GetActorsByType("raarty")
				tnk1 = p16.GetActorsByType("1tnk")
				rajeep = p16.GetActorsByType("rajeep")
				tnk4 = p16.GetActorsByType("4tnk")
				v2rl = p16.GetActorsByType("v2rl")
				tnk3 = p16.GetActorsByType("3tnk")
				ttnk = p16.GetActorsByType("ttnk")
				ftrk = p16.GetActorsByType("ftrk")
				raapc = p16.GetActorsByType("raapc")
				hpt2100 = p16.GetActorsByType("2100hpt")
				apt2100 = p16.GetActorsByType("2100apt")
				mch2100 = p16.GetActorsByType("2100mch")
				tch2100 = p16.GetActorsByType("2100tch")
				rvw2100 = p16.GetActorsByType("2100rvw")
				mvw2100 = p16.GetActorsByType("2100mvw")
				rvw22100 = p16.GetActorsByType("2100rvw2")
				mvw22100 = p16.GetActorsByType("2100mvw2")
				scgoliath = p16.GetActorsByType("scgoliath")
				scvulture = p16.GetActorsByType("scvulture")
				scsiegetank = p16.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p1 and p7.IsAlliedWith(p1) then
				htnk = p1.GetActorsByType("htnk")
				msam = p1.GetActorsByType("msam")
				mtnk = p1.GetActorsByType("mtnk")
				apc = p1.GetActorsByType("apc")
				jeep = p1.GetActorsByType("jeep")
				stnk = p1.GetActorsByType("stnk")
				arty = p1.GetActorsByType("arty")
				ltnk = p1.GetActorsByType("ltnk")
				ftnk = p1.GetActorsByType("ftnk")
				mlrs = p1.GetActorsByType("mlrs")
				bike = p1.GetActorsByType("bike")
				bggy = p1.GetActorsByType("bggy")
				ctnk = p1.GetActorsByType("ctnk")
				tnk2 = p1.GetActorsByType("2tnk")
				raarty = p1.GetActorsByType("raarty")
				tnk1 = p1.GetActorsByType("1tnk")
				rajeep = p1.GetActorsByType("rajeep")
				tnk4 = p1.GetActorsByType("4tnk")
				v2rl = p1.GetActorsByType("v2rl")
				tnk3 = p1.GetActorsByType("3tnk")
				ttnk = p1.GetActorsByType("ttnk")
				ftrk = p1.GetActorsByType("ftrk")
				raapc = p1.GetActorsByType("raapc")
				hpt2100 = p1.GetActorsByType("2100hpt")
				apt2100 = p1.GetActorsByType("2100apt")
				mch2100 = p1.GetActorsByType("2100mch")
				tch2100 = p1.GetActorsByType("2100tch")
				rvw2100 = p1.GetActorsByType("2100rvw")
				mvw2100 = p1.GetActorsByType("2100mvw")
				rvw22100 = p1.GetActorsByType("2100rvw2")
				mvw22100 = p1.GetActorsByType("2100mvw2")
				scgoliath = p1.GetActorsByType("scgoliath")
				scvulture = p1.GetActorsByType("scvulture")
				scsiegetank = p1.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p2 and p7.IsAlliedWith(p2) then
				htnk = p2.GetActorsByType("htnk")
				msam = p2.GetActorsByType("msam")
				mtnk = p2.GetActorsByType("mtnk")
				apc = p2.GetActorsByType("apc")
				jeep = p2.GetActorsByType("jeep")
				stnk = p2.GetActorsByType("stnk")
				arty = p2.GetActorsByType("arty")
				ltnk = p2.GetActorsByType("ltnk")
				ftnk = p2.GetActorsByType("ftnk")
				mlrs = p2.GetActorsByType("mlrs")
				bike = p2.GetActorsByType("bike")
				bggy = p2.GetActorsByType("bggy")
				ctnk = p2.GetActorsByType("ctnk")
				tnk2 = p2.GetActorsByType("2tnk")
				raarty = p2.GetActorsByType("raarty")
				tnk1 = p2.GetActorsByType("1tnk")
				rajeep = p2.GetActorsByType("rajeep")
				tnk4 = p2.GetActorsByType("4tnk")
				v2rl = p2.GetActorsByType("v2rl")
				tnk3 = p2.GetActorsByType("3tnk")
				ttnk = p2.GetActorsByType("ttnk")
				ftrk = p2.GetActorsByType("ftrk")
				raapc = p2.GetActorsByType("raapc")
				hpt2100 = p2.GetActorsByType("2100hpt")
				apt2100 = p2.GetActorsByType("2100apt")
				mch2100 = p2.GetActorsByType("2100mch")
				tch2100 = p2.GetActorsByType("2100tch")
				rvw2100 = p2.GetActorsByType("2100rvw")
				mvw2100 = p2.GetActorsByType("2100mvw")
				rvw22100 = p2.GetActorsByType("2100rvw2")
				mvw22100 = p2.GetActorsByType("2100mvw2")
				scgoliath = p2.GetActorsByType("scgoliath")
				scvulture = p2.GetActorsByType("scvulture")
				scsiegetank = p2.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p3 and p7.IsAlliedWith(p3) then
				htnk = p3.GetActorsByType("htnk")
				msam = p3.GetActorsByType("msam")
				mtnk = p3.GetActorsByType("mtnk")
				apc = p3.GetActorsByType("apc")
				jeep = p3.GetActorsByType("jeep")
				stnk = p3.GetActorsByType("stnk")
				arty = p3.GetActorsByType("arty")
				ltnk = p3.GetActorsByType("ltnk")
				ftnk = p3.GetActorsByType("ftnk")
				mlrs = p3.GetActorsByType("mlrs")
				bike = p3.GetActorsByType("bike")
				bggy = p3.GetActorsByType("bggy")
				ctnk = p3.GetActorsByType("ctnk")
				tnk2 = p3.GetActorsByType("2tnk")
				raarty = p3.GetActorsByType("raarty")
				tnk1 = p3.GetActorsByType("1tnk")
				rajeep = p3.GetActorsByType("rajeep")
				tnk4 = p3.GetActorsByType("4tnk")
				v2rl = p3.GetActorsByType("v2rl")
				tnk3 = p3.GetActorsByType("3tnk")
				ttnk = p3.GetActorsByType("ttnk")
				ftrk = p3.GetActorsByType("ftrk")
				raapc = p3.GetActorsByType("raapc")
				hpt2100 = p3.GetActorsByType("2100hpt")
				apt2100 = p3.GetActorsByType("2100apt")
				mch2100 = p3.GetActorsByType("2100mch")
				tch2100 = p3.GetActorsByType("2100tch")
				rvw2100 = p3.GetActorsByType("2100rvw")
				mvw2100 = p3.GetActorsByType("2100mvw")
				rvw22100 = p3.GetActorsByType("2100rvw2")
				mvw22100 = p3.GetActorsByType("2100mvw2")
				scgoliath = p3.GetActorsByType("scgoliath")
				scvulture = p3.GetActorsByType("scvulture")
				scsiegetank = p3.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p4 and p7.IsAlliedWith(p4) then
				htnk = p4.GetActorsByType("htnk")
				msam = p4.GetActorsByType("msam")
				mtnk = p4.GetActorsByType("mtnk")
				apc = p4.GetActorsByType("apc")
				jeep = p4.GetActorsByType("jeep")
				stnk = p4.GetActorsByType("stnk")
				arty = p4.GetActorsByType("arty")
				ltnk = p4.GetActorsByType("ltnk")
				ftnk = p4.GetActorsByType("ftnk")
				mlrs = p4.GetActorsByType("mlrs")
				bike = p4.GetActorsByType("bike")
				bggy = p4.GetActorsByType("bggy")
				ctnk = p4.GetActorsByType("ctnk")
				tnk2 = p4.GetActorsByType("2tnk")
				raarty = p4.GetActorsByType("raarty")
				tnk1 = p4.GetActorsByType("1tnk")
				rajeep = p4.GetActorsByType("rajeep")
				tnk4 = p4.GetActorsByType("4tnk")
				v2rl = p4.GetActorsByType("v2rl")
				tnk3 = p4.GetActorsByType("3tnk")
				ttnk = p4.GetActorsByType("ttnk")
				ftrk = p4.GetActorsByType("ftrk")
				raapc = p4.GetActorsByType("raapc")
				hpt2100 = p4.GetActorsByType("2100hpt")
				apt2100 = p4.GetActorsByType("2100apt")
				mch2100 = p4.GetActorsByType("2100mch")
				tch2100 = p4.GetActorsByType("2100tch")
				rvw2100 = p4.GetActorsByType("2100rvw")
				mvw2100 = p4.GetActorsByType("2100mvw")
				rvw22100 = p4.GetActorsByType("2100rvw2")
				mvw22100 = p4.GetActorsByType("2100mvw2")
				scgoliath = p4.GetActorsByType("scgoliath")
				scvulture = p4.GetActorsByType("scvulture")
				scsiegetank = p4.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p5 and p7.IsAlliedWith(p5) then
				htnk = p5.GetActorsByType("htnk")
				msam = p5.GetActorsByType("msam")
				mtnk = p5.GetActorsByType("mtnk")
				apc = p5.GetActorsByType("apc")
				jeep = p5.GetActorsByType("jeep")
				stnk = p5.GetActorsByType("stnk")
				arty = p5.GetActorsByType("arty")
				ltnk = p5.GetActorsByType("ltnk")
				ftnk = p5.GetActorsByType("ftnk")
				mlrs = p5.GetActorsByType("mlrs")
				bike = p5.GetActorsByType("bike")
				bggy = p5.GetActorsByType("bggy")
				ctnk = p5.GetActorsByType("ctnk")
				tnk2 = p5.GetActorsByType("2tnk")
				raarty = p5.GetActorsByType("raarty")
				tnk1 = p5.GetActorsByType("1tnk")
				rajeep = p5.GetActorsByType("rajeep")
				tnk4 = p5.GetActorsByType("4tnk")
				v2rl = p5.GetActorsByType("v2rl")
				tnk3 = p5.GetActorsByType("3tnk")
				ttnk = p5.GetActorsByType("ttnk")
				ftrk = p5.GetActorsByType("ftrk")
				raapc = p5.GetActorsByType("raapc")
				hpt2100 = p5.GetActorsByType("2100hpt")
				apt2100 = p5.GetActorsByType("2100apt")
				mch2100 = p5.GetActorsByType("2100mch")
				tch2100 = p5.GetActorsByType("2100tch")
				rvw2100 = p5.GetActorsByType("2100rvw")
				mvw2100 = p5.GetActorsByType("2100mvw")
				rvw22100 = p5.GetActorsByType("2100rvw2")
				mvw22100 = p5.GetActorsByType("2100mvw2")
				scgoliath = p5.GetActorsByType("scgoliath")
				scvulture = p5.GetActorsByType("scvulture")
				scsiegetank = p5.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p6 and p7.IsAlliedWith(p6) then
				htnk = p6.GetActorsByType("htnk")
				msam = p6.GetActorsByType("msam")
				mtnk = p6.GetActorsByType("mtnk")
				apc = p6.GetActorsByType("apc")
				jeep = p6.GetActorsByType("jeep")
				stnk = p6.GetActorsByType("stnk")
				arty = p6.GetActorsByType("arty")
				ltnk = p6.GetActorsByType("ltnk")
				ftnk = p6.GetActorsByType("ftnk")
				mlrs = p6.GetActorsByType("mlrs")
				bike = p6.GetActorsByType("bike")
				bggy = p6.GetActorsByType("bggy")
				ctnk = p6.GetActorsByType("ctnk")
				tnk2 = p6.GetActorsByType("2tnk")
				raarty = p6.GetActorsByType("raarty")
				tnk1 = p6.GetActorsByType("1tnk")
				rajeep = p6.GetActorsByType("rajeep")
				tnk4 = p6.GetActorsByType("4tnk")
				v2rl = p6.GetActorsByType("v2rl")
				tnk3 = p6.GetActorsByType("3tnk")
				ttnk = p6.GetActorsByType("ttnk")
				ftrk = p6.GetActorsByType("ftrk")
				raapc = p6.GetActorsByType("raapc")
				hpt2100 = p6.GetActorsByType("2100hpt")
				apt2100 = p6.GetActorsByType("2100apt")
				mch2100 = p6.GetActorsByType("2100mch")
				tch2100 = p6.GetActorsByType("2100tch")
				rvw2100 = p6.GetActorsByType("2100rvw")
				mvw2100 = p6.GetActorsByType("2100mvw")
				rvw22100 = p6.GetActorsByType("2100rvw2")
				mvw22100 = p6.GetActorsByType("2100mvw2")
				scgoliath = p6.GetActorsByType("scgoliath")
				scvulture = p6.GetActorsByType("scvulture")
				scsiegetank = p6.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p7.GetActorsByType("citypolicecar")
				cityfiretruck = p7.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p7vehicle()
	end)
end
p8vehicle = function()
	if p8 and p8.IsBot then
		cityboteliteexpert = p8.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p9 and p8.IsAlliedWith(p9) then
				htnk = p9.GetActorsByType("htnk")
				msam = p9.GetActorsByType("msam")
				mtnk = p9.GetActorsByType("mtnk")
				apc = p9.GetActorsByType("apc")
				jeep = p9.GetActorsByType("jeep")
				stnk = p9.GetActorsByType("stnk")
				arty = p9.GetActorsByType("arty")
				ltnk = p9.GetActorsByType("ltnk")
				ftnk = p9.GetActorsByType("ftnk")
				mlrs = p9.GetActorsByType("mlrs")
				bike = p9.GetActorsByType("bike")
				bggy = p9.GetActorsByType("bggy")
				ctnk = p9.GetActorsByType("ctnk")
				tnk2 = p9.GetActorsByType("2tnk")
				raarty = p9.GetActorsByType("raarty")
				tnk1 = p9.GetActorsByType("1tnk")
				rajeep = p9.GetActorsByType("rajeep")
				tnk4 = p9.GetActorsByType("4tnk")
				v2rl = p9.GetActorsByType("v2rl")
				tnk3 = p9.GetActorsByType("3tnk")
				ttnk = p9.GetActorsByType("ttnk")
				ftrk = p9.GetActorsByType("ftrk")
				raapc = p9.GetActorsByType("raapc")
				hpt2100 = p9.GetActorsByType("2100hpt")
				apt2100 = p9.GetActorsByType("2100apt")
				mch2100 = p9.GetActorsByType("2100mch")
				tch2100 = p9.GetActorsByType("2100tch")
				rvw2100 = p9.GetActorsByType("2100rvw")
				mvw2100 = p9.GetActorsByType("2100mvw")
				rvw22100 = p9.GetActorsByType("2100rvw2")
				mvw22100 = p9.GetActorsByType("2100mvw2")
				scgoliath = p9.GetActorsByType("scgoliath")
				scvulture = p9.GetActorsByType("scvulture")
				scsiegetank = p9.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p10 and p8.IsAlliedWith(p10) then
				htnk = p10.GetActorsByType("htnk")
				msam = p10.GetActorsByType("msam")
				mtnk = p10.GetActorsByType("mtnk")
				apc = p10.GetActorsByType("apc")
				jeep = p10.GetActorsByType("jeep")
				stnk = p10.GetActorsByType("stnk")
				arty = p10.GetActorsByType("arty")
				ltnk = p10.GetActorsByType("ltnk")
				ftnk = p10.GetActorsByType("ftnk")
				mlrs = p10.GetActorsByType("mlrs")
				bike = p10.GetActorsByType("bike")
				bggy = p10.GetActorsByType("bggy")
				ctnk = p10.GetActorsByType("ctnk")
				tnk2 = p10.GetActorsByType("2tnk")
				raarty = p10.GetActorsByType("raarty")
				tnk1 = p10.GetActorsByType("1tnk")
				rajeep = p10.GetActorsByType("rajeep")
				tnk4 = p10.GetActorsByType("4tnk")
				v2rl = p10.GetActorsByType("v2rl")
				tnk3 = p10.GetActorsByType("3tnk")
				ttnk = p10.GetActorsByType("ttnk")
				ftrk = p10.GetActorsByType("ftrk")
				raapc = p10.GetActorsByType("raapc")
				hpt2100 = p10.GetActorsByType("2100hpt")
				apt2100 = p10.GetActorsByType("2100apt")
				mch2100 = p10.GetActorsByType("2100mch")
				tch2100 = p10.GetActorsByType("2100tch")
				rvw2100 = p10.GetActorsByType("2100rvw")
				mvw2100 = p10.GetActorsByType("2100mvw")
				rvw22100 = p10.GetActorsByType("2100rvw2")
				mvw22100 = p10.GetActorsByType("2100mvw2")
				scgoliath = p10.GetActorsByType("scgoliath")
				scvulture = p10.GetActorsByType("scvulture")
				scsiegetank = p10.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p11 and p8.IsAlliedWith(p11) then
				htnk = p11.GetActorsByType("htnk")
				msam = p11.GetActorsByType("msam")
				mtnk = p11.GetActorsByType("mtnk")
				apc = p11.GetActorsByType("apc")
				jeep = p11.GetActorsByType("jeep")
				stnk = p11.GetActorsByType("stnk")
				arty = p11.GetActorsByType("arty")
				ltnk = p11.GetActorsByType("ltnk")
				ftnk = p11.GetActorsByType("ftnk")
				mlrs = p11.GetActorsByType("mlrs")
				bike = p11.GetActorsByType("bike")
				bggy = p11.GetActorsByType("bggy")
				ctnk = p11.GetActorsByType("ctnk")
				tnk2 = p11.GetActorsByType("2tnk")
				raarty = p11.GetActorsByType("raarty")
				tnk1 = p11.GetActorsByType("1tnk")
				rajeep = p11.GetActorsByType("rajeep")
				tnk4 = p11.GetActorsByType("4tnk")
				v2rl = p11.GetActorsByType("v2rl")
				tnk3 = p11.GetActorsByType("3tnk")
				ttnk = p11.GetActorsByType("ttnk")
				ftrk = p11.GetActorsByType("ftrk")
				raapc = p11.GetActorsByType("raapc")
				hpt2100 = p11.GetActorsByType("2100hpt")
				apt2100 = p11.GetActorsByType("2100apt")
				mch2100 = p11.GetActorsByType("2100mch")
				tch2100 = p11.GetActorsByType("2100tch")
				rvw2100 = p11.GetActorsByType("2100rvw")
				mvw2100 = p11.GetActorsByType("2100mvw")
				rvw22100 = p11.GetActorsByType("2100rvw2")
				mvw22100 = p11.GetActorsByType("2100mvw2")
				scgoliath = p11.GetActorsByType("scgoliath")
				scvulture = p11.GetActorsByType("scvulture")
				scsiegetank = p11.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p12 and p8.IsAlliedWith(p12) then
				htnk = p12.GetActorsByType("htnk")
				msam = p12.GetActorsByType("msam")
				mtnk = p12.GetActorsByType("mtnk")
				apc = p12.GetActorsByType("apc")
				jeep = p12.GetActorsByType("jeep")
				stnk = p12.GetActorsByType("stnk")
				arty = p12.GetActorsByType("arty")
				ltnk = p12.GetActorsByType("ltnk")
				ftnk = p12.GetActorsByType("ftnk")
				mlrs = p12.GetActorsByType("mlrs")
				bike = p12.GetActorsByType("bike")
				bggy = p12.GetActorsByType("bggy")
				ctnk = p12.GetActorsByType("ctnk")
				tnk2 = p12.GetActorsByType("2tnk")
				raarty = p12.GetActorsByType("raarty")
				tnk1 = p12.GetActorsByType("1tnk")
				rajeep = p12.GetActorsByType("rajeep")
				tnk4 = p12.GetActorsByType("4tnk")
				v2rl = p12.GetActorsByType("v2rl")
				tnk3 = p12.GetActorsByType("3tnk")
				ttnk = p12.GetActorsByType("ttnk")
				ftrk = p12.GetActorsByType("ftrk")
				raapc = p12.GetActorsByType("raapc")
				hpt2100 = p12.GetActorsByType("2100hpt")
				apt2100 = p12.GetActorsByType("2100apt")
				mch2100 = p12.GetActorsByType("2100mch")
				tch2100 = p12.GetActorsByType("2100tch")
				rvw2100 = p12.GetActorsByType("2100rvw")
				mvw2100 = p12.GetActorsByType("2100mvw")
				rvw22100 = p12.GetActorsByType("2100rvw2")
				mvw22100 = p12.GetActorsByType("2100mvw2")
				scgoliath = p12.GetActorsByType("scgoliath")
				scvulture = p12.GetActorsByType("scvulture")
				scsiegetank = p12.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p13 and p8.IsAlliedWith(p13) then
				htnk = p13.GetActorsByType("htnk")
				msam = p13.GetActorsByType("msam")
				mtnk = p13.GetActorsByType("mtnk")
				apc = p13.GetActorsByType("apc")
				jeep = p13.GetActorsByType("jeep")
				stnk = p13.GetActorsByType("stnk")
				arty = p13.GetActorsByType("arty")
				ltnk = p13.GetActorsByType("ltnk")
				ftnk = p13.GetActorsByType("ftnk")
				mlrs = p13.GetActorsByType("mlrs")
				bike = p13.GetActorsByType("bike")
				bggy = p13.GetActorsByType("bggy")
				ctnk = p13.GetActorsByType("ctnk")
				tnk2 = p13.GetActorsByType("2tnk")
				raarty = p13.GetActorsByType("raarty")
				tnk1 = p13.GetActorsByType("1tnk")
				rajeep = p13.GetActorsByType("rajeep")
				tnk4 = p13.GetActorsByType("4tnk")
				v2rl = p13.GetActorsByType("v2rl")
				tnk3 = p13.GetActorsByType("3tnk")
				ttnk = p13.GetActorsByType("ttnk")
				ftrk = p13.GetActorsByType("ftrk")
				raapc = p13.GetActorsByType("raapc")
				hpt2100 = p13.GetActorsByType("2100hpt")
				apt2100 = p13.GetActorsByType("2100apt")
				mch2100 = p13.GetActorsByType("2100mch")
				tch2100 = p13.GetActorsByType("2100tch")
				rvw2100 = p13.GetActorsByType("2100rvw")
				mvw2100 = p13.GetActorsByType("2100mvw")
				rvw22100 = p13.GetActorsByType("2100rvw2")
				mvw22100 = p13.GetActorsByType("2100mvw2")
				scgoliath = p13.GetActorsByType("scgoliath")
				scvulture = p13.GetActorsByType("scvulture")
				scsiegetank = p13.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p14 and p8.IsAlliedWith(p14) then
				htnk = p14.GetActorsByType("htnk")
				msam = p14.GetActorsByType("msam")
				mtnk = p14.GetActorsByType("mtnk")
				apc = p14.GetActorsByType("apc")
				jeep = p14.GetActorsByType("jeep")
				stnk = p14.GetActorsByType("stnk")
				arty = p14.GetActorsByType("arty")
				ltnk = p14.GetActorsByType("ltnk")
				ftnk = p14.GetActorsByType("ftnk")
				mlrs = p14.GetActorsByType("mlrs")
				bike = p14.GetActorsByType("bike")
				bggy = p14.GetActorsByType("bggy")
				ctnk = p14.GetActorsByType("ctnk")
				tnk2 = p14.GetActorsByType("2tnk")
				raarty = p14.GetActorsByType("raarty")
				tnk1 = p14.GetActorsByType("1tnk")
				rajeep = p14.GetActorsByType("rajeep")
				tnk4 = p14.GetActorsByType("4tnk")
				v2rl = p14.GetActorsByType("v2rl")
				tnk3 = p14.GetActorsByType("3tnk")
				ttnk = p14.GetActorsByType("ttnk")
				ftrk = p14.GetActorsByType("ftrk")
				raapc = p14.GetActorsByType("raapc")
				hpt2100 = p14.GetActorsByType("2100hpt")
				apt2100 = p14.GetActorsByType("2100apt")
				mch2100 = p14.GetActorsByType("2100mch")
				tch2100 = p14.GetActorsByType("2100tch")
				rvw2100 = p14.GetActorsByType("2100rvw")
				mvw2100 = p14.GetActorsByType("2100mvw")
				rvw22100 = p14.GetActorsByType("2100rvw2")
				mvw22100 = p14.GetActorsByType("2100mvw2")
				scgoliath = p14.GetActorsByType("scgoliath")
				scvulture = p14.GetActorsByType("scvulture")
				scsiegetank = p14.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p15 and p8.IsAlliedWith(p15) then
				htnk = p15.GetActorsByType("htnk")
				msam = p15.GetActorsByType("msam")
				mtnk = p15.GetActorsByType("mtnk")
				apc = p15.GetActorsByType("apc")
				jeep = p15.GetActorsByType("jeep")
				stnk = p15.GetActorsByType("stnk")
				arty = p15.GetActorsByType("arty")
				ltnk = p15.GetActorsByType("ltnk")
				ftnk = p15.GetActorsByType("ftnk")
				mlrs = p15.GetActorsByType("mlrs")
				bike = p15.GetActorsByType("bike")
				bggy = p15.GetActorsByType("bggy")
				ctnk = p15.GetActorsByType("ctnk")
				tnk2 = p15.GetActorsByType("2tnk")
				raarty = p15.GetActorsByType("raarty")
				tnk1 = p15.GetActorsByType("1tnk")
				rajeep = p15.GetActorsByType("rajeep")
				tnk4 = p15.GetActorsByType("4tnk")
				v2rl = p15.GetActorsByType("v2rl")
				tnk3 = p15.GetActorsByType("3tnk")
				ttnk = p15.GetActorsByType("ttnk")
				ftrk = p15.GetActorsByType("ftrk")
				raapc = p15.GetActorsByType("raapc")
				hpt2100 = p15.GetActorsByType("2100hpt")
				apt2100 = p15.GetActorsByType("2100apt")
				mch2100 = p15.GetActorsByType("2100mch")
				tch2100 = p15.GetActorsByType("2100tch")
				rvw2100 = p15.GetActorsByType("2100rvw")
				mvw2100 = p15.GetActorsByType("2100mvw")
				rvw22100 = p15.GetActorsByType("2100rvw2")
				mvw22100 = p15.GetActorsByType("2100mvw2")
				scgoliath = p15.GetActorsByType("scgoliath")
				scvulture = p15.GetActorsByType("scvulture")
				scsiegetank = p15.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p16 and p8.IsAlliedWith(p16) then
				htnk = p16.GetActorsByType("htnk")
				msam = p16.GetActorsByType("msam")
				mtnk = p16.GetActorsByType("mtnk")
				apc = p16.GetActorsByType("apc")
				jeep = p16.GetActorsByType("jeep")
				stnk = p16.GetActorsByType("stnk")
				arty = p16.GetActorsByType("arty")
				ltnk = p16.GetActorsByType("ltnk")
				ftnk = p16.GetActorsByType("ftnk")
				mlrs = p16.GetActorsByType("mlrs")
				bike = p16.GetActorsByType("bike")
				bggy = p16.GetActorsByType("bggy")
				ctnk = p16.GetActorsByType("ctnk")
				tnk2 = p16.GetActorsByType("2tnk")
				raarty = p16.GetActorsByType("raarty")
				tnk1 = p16.GetActorsByType("1tnk")
				rajeep = p16.GetActorsByType("rajeep")
				tnk4 = p16.GetActorsByType("4tnk")
				v2rl = p16.GetActorsByType("v2rl")
				tnk3 = p16.GetActorsByType("3tnk")
				ttnk = p16.GetActorsByType("ttnk")
				ftrk = p16.GetActorsByType("ftrk")
				raapc = p16.GetActorsByType("raapc")
				hpt2100 = p16.GetActorsByType("2100hpt")
				apt2100 = p16.GetActorsByType("2100apt")
				mch2100 = p16.GetActorsByType("2100mch")
				tch2100 = p16.GetActorsByType("2100tch")
				rvw2100 = p16.GetActorsByType("2100rvw")
				mvw2100 = p16.GetActorsByType("2100mvw")
				rvw22100 = p16.GetActorsByType("2100rvw2")
				mvw22100 = p16.GetActorsByType("2100mvw2")
				scgoliath = p16.GetActorsByType("scgoliath")
				scvulture = p16.GetActorsByType("scvulture")
				scsiegetank = p16.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p1 and p8.IsAlliedWith(p1) then
				htnk = p1.GetActorsByType("htnk")
				msam = p1.GetActorsByType("msam")
				mtnk = p1.GetActorsByType("mtnk")
				apc = p1.GetActorsByType("apc")
				jeep = p1.GetActorsByType("jeep")
				stnk = p1.GetActorsByType("stnk")
				arty = p1.GetActorsByType("arty")
				ltnk = p1.GetActorsByType("ltnk")
				ftnk = p1.GetActorsByType("ftnk")
				mlrs = p1.GetActorsByType("mlrs")
				bike = p1.GetActorsByType("bike")
				bggy = p1.GetActorsByType("bggy")
				ctnk = p1.GetActorsByType("ctnk")
				tnk2 = p1.GetActorsByType("2tnk")
				raarty = p1.GetActorsByType("raarty")
				tnk1 = p1.GetActorsByType("1tnk")
				rajeep = p1.GetActorsByType("rajeep")
				tnk4 = p1.GetActorsByType("4tnk")
				v2rl = p1.GetActorsByType("v2rl")
				tnk3 = p1.GetActorsByType("3tnk")
				ttnk = p1.GetActorsByType("ttnk")
				ftrk = p1.GetActorsByType("ftrk")
				raapc = p1.GetActorsByType("raapc")
				hpt2100 = p1.GetActorsByType("2100hpt")
				apt2100 = p1.GetActorsByType("2100apt")
				mch2100 = p1.GetActorsByType("2100mch")
				tch2100 = p1.GetActorsByType("2100tch")
				rvw2100 = p1.GetActorsByType("2100rvw")
				mvw2100 = p1.GetActorsByType("2100mvw")
				rvw22100 = p1.GetActorsByType("2100rvw2")
				mvw22100 = p1.GetActorsByType("2100mvw2")
				scgoliath = p1.GetActorsByType("scgoliath")
				scvulture = p1.GetActorsByType("scvulture")
				scsiegetank = p1.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p2 and p8.IsAlliedWith(p2) then
				htnk = p2.GetActorsByType("htnk")
				msam = p2.GetActorsByType("msam")
				mtnk = p2.GetActorsByType("mtnk")
				apc = p2.GetActorsByType("apc")
				jeep = p2.GetActorsByType("jeep")
				stnk = p2.GetActorsByType("stnk")
				arty = p2.GetActorsByType("arty")
				ltnk = p2.GetActorsByType("ltnk")
				ftnk = p2.GetActorsByType("ftnk")
				mlrs = p2.GetActorsByType("mlrs")
				bike = p2.GetActorsByType("bike")
				bggy = p2.GetActorsByType("bggy")
				ctnk = p2.GetActorsByType("ctnk")
				tnk2 = p2.GetActorsByType("2tnk")
				raarty = p2.GetActorsByType("raarty")
				tnk1 = p2.GetActorsByType("1tnk")
				rajeep = p2.GetActorsByType("rajeep")
				tnk4 = p2.GetActorsByType("4tnk")
				v2rl = p2.GetActorsByType("v2rl")
				tnk3 = p2.GetActorsByType("3tnk")
				ttnk = p2.GetActorsByType("ttnk")
				ftrk = p2.GetActorsByType("ftrk")
				raapc = p2.GetActorsByType("raapc")
				hpt2100 = p2.GetActorsByType("2100hpt")
				apt2100 = p2.GetActorsByType("2100apt")
				mch2100 = p2.GetActorsByType("2100mch")
				tch2100 = p2.GetActorsByType("2100tch")
				rvw2100 = p2.GetActorsByType("2100rvw")
				mvw2100 = p2.GetActorsByType("2100mvw")
				rvw22100 = p2.GetActorsByType("2100rvw2")
				mvw22100 = p2.GetActorsByType("2100mvw2")
				scgoliath = p2.GetActorsByType("scgoliath")
				scvulture = p2.GetActorsByType("scvulture")
				scsiegetank = p2.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p3 and p8.IsAlliedWith(p3) then
				htnk = p3.GetActorsByType("htnk")
				msam = p3.GetActorsByType("msam")
				mtnk = p3.GetActorsByType("mtnk")
				apc = p3.GetActorsByType("apc")
				jeep = p3.GetActorsByType("jeep")
				stnk = p3.GetActorsByType("stnk")
				arty = p3.GetActorsByType("arty")
				ltnk = p3.GetActorsByType("ltnk")
				ftnk = p3.GetActorsByType("ftnk")
				mlrs = p3.GetActorsByType("mlrs")
				bike = p3.GetActorsByType("bike")
				bggy = p3.GetActorsByType("bggy")
				ctnk = p3.GetActorsByType("ctnk")
				tnk2 = p3.GetActorsByType("2tnk")
				raarty = p3.GetActorsByType("raarty")
				tnk1 = p3.GetActorsByType("1tnk")
				rajeep = p3.GetActorsByType("rajeep")
				tnk4 = p3.GetActorsByType("4tnk")
				v2rl = p3.GetActorsByType("v2rl")
				tnk3 = p3.GetActorsByType("3tnk")
				ttnk = p3.GetActorsByType("ttnk")
				ftrk = p3.GetActorsByType("ftrk")
				raapc = p3.GetActorsByType("raapc")
				hpt2100 = p3.GetActorsByType("2100hpt")
				apt2100 = p3.GetActorsByType("2100apt")
				mch2100 = p3.GetActorsByType("2100mch")
				tch2100 = p3.GetActorsByType("2100tch")
				rvw2100 = p3.GetActorsByType("2100rvw")
				mvw2100 = p3.GetActorsByType("2100mvw")
				rvw22100 = p3.GetActorsByType("2100rvw2")
				mvw22100 = p3.GetActorsByType("2100mvw2")
				scgoliath = p3.GetActorsByType("scgoliath")
				scvulture = p3.GetActorsByType("scvulture")
				scsiegetank = p3.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p4 and p8.IsAlliedWith(p4) then
				htnk = p4.GetActorsByType("htnk")
				msam = p4.GetActorsByType("msam")
				mtnk = p4.GetActorsByType("mtnk")
				apc = p4.GetActorsByType("apc")
				jeep = p4.GetActorsByType("jeep")
				stnk = p4.GetActorsByType("stnk")
				arty = p4.GetActorsByType("arty")
				ltnk = p4.GetActorsByType("ltnk")
				ftnk = p4.GetActorsByType("ftnk")
				mlrs = p4.GetActorsByType("mlrs")
				bike = p4.GetActorsByType("bike")
				bggy = p4.GetActorsByType("bggy")
				ctnk = p4.GetActorsByType("ctnk")
				tnk2 = p4.GetActorsByType("2tnk")
				raarty = p4.GetActorsByType("raarty")
				tnk1 = p4.GetActorsByType("1tnk")
				rajeep = p4.GetActorsByType("rajeep")
				tnk4 = p4.GetActorsByType("4tnk")
				v2rl = p4.GetActorsByType("v2rl")
				tnk3 = p4.GetActorsByType("3tnk")
				ttnk = p4.GetActorsByType("ttnk")
				ftrk = p4.GetActorsByType("ftrk")
				raapc = p4.GetActorsByType("raapc")
				hpt2100 = p4.GetActorsByType("2100hpt")
				apt2100 = p4.GetActorsByType("2100apt")
				mch2100 = p4.GetActorsByType("2100mch")
				tch2100 = p4.GetActorsByType("2100tch")
				rvw2100 = p4.GetActorsByType("2100rvw")
				mvw2100 = p4.GetActorsByType("2100mvw")
				rvw22100 = p4.GetActorsByType("2100rvw2")
				mvw22100 = p4.GetActorsByType("2100mvw2")
				scgoliath = p4.GetActorsByType("scgoliath")
				scvulture = p4.GetActorsByType("scvulture")
				scsiegetank = p4.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p5 and p8.IsAlliedWith(p5) then
				htnk = p5.GetActorsByType("htnk")
				msam = p5.GetActorsByType("msam")
				mtnk = p5.GetActorsByType("mtnk")
				apc = p5.GetActorsByType("apc")
				jeep = p5.GetActorsByType("jeep")
				stnk = p5.GetActorsByType("stnk")
				arty = p5.GetActorsByType("arty")
				ltnk = p5.GetActorsByType("ltnk")
				ftnk = p5.GetActorsByType("ftnk")
				mlrs = p5.GetActorsByType("mlrs")
				bike = p5.GetActorsByType("bike")
				bggy = p5.GetActorsByType("bggy")
				ctnk = p5.GetActorsByType("ctnk")
				tnk2 = p5.GetActorsByType("2tnk")
				raarty = p5.GetActorsByType("raarty")
				tnk1 = p5.GetActorsByType("1tnk")
				rajeep = p5.GetActorsByType("rajeep")
				tnk4 = p5.GetActorsByType("4tnk")
				v2rl = p5.GetActorsByType("v2rl")
				tnk3 = p5.GetActorsByType("3tnk")
				ttnk = p5.GetActorsByType("ttnk")
				ftrk = p5.GetActorsByType("ftrk")
				raapc = p5.GetActorsByType("raapc")
				hpt2100 = p5.GetActorsByType("2100hpt")
				apt2100 = p5.GetActorsByType("2100apt")
				mch2100 = p5.GetActorsByType("2100mch")
				tch2100 = p5.GetActorsByType("2100tch")
				rvw2100 = p5.GetActorsByType("2100rvw")
				mvw2100 = p5.GetActorsByType("2100mvw")
				rvw22100 = p5.GetActorsByType("2100rvw2")
				mvw22100 = p5.GetActorsByType("2100mvw2")
				scgoliath = p5.GetActorsByType("scgoliath")
				scvulture = p5.GetActorsByType("scvulture")
				scsiegetank = p5.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p6 and p8.IsAlliedWith(p6) then
				htnk = p6.GetActorsByType("htnk")
				msam = p6.GetActorsByType("msam")
				mtnk = p6.GetActorsByType("mtnk")
				apc = p6.GetActorsByType("apc")
				jeep = p6.GetActorsByType("jeep")
				stnk = p6.GetActorsByType("stnk")
				arty = p6.GetActorsByType("arty")
				ltnk = p6.GetActorsByType("ltnk")
				ftnk = p6.GetActorsByType("ftnk")
				mlrs = p6.GetActorsByType("mlrs")
				bike = p6.GetActorsByType("bike")
				bggy = p6.GetActorsByType("bggy")
				ctnk = p6.GetActorsByType("ctnk")
				tnk2 = p6.GetActorsByType("2tnk")
				raarty = p6.GetActorsByType("raarty")
				tnk1 = p6.GetActorsByType("1tnk")
				rajeep = p6.GetActorsByType("rajeep")
				tnk4 = p6.GetActorsByType("4tnk")
				v2rl = p6.GetActorsByType("v2rl")
				tnk3 = p6.GetActorsByType("3tnk")
				ttnk = p6.GetActorsByType("ttnk")
				ftrk = p6.GetActorsByType("ftrk")
				raapc = p6.GetActorsByType("raapc")
				hpt2100 = p6.GetActorsByType("2100hpt")
				apt2100 = p6.GetActorsByType("2100apt")
				mch2100 = p6.GetActorsByType("2100mch")
				tch2100 = p6.GetActorsByType("2100tch")
				rvw2100 = p6.GetActorsByType("2100rvw")
				mvw2100 = p6.GetActorsByType("2100mvw")
				rvw22100 = p6.GetActorsByType("2100rvw2")
				mvw22100 = p6.GetActorsByType("2100mvw2")
				scgoliath = p6.GetActorsByType("scgoliath")
				scvulture = p6.GetActorsByType("scvulture")
				scsiegetank = p6.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p7 and p8.IsAlliedWith(p7) then
				htnk = p7.GetActorsByType("htnk")
				msam = p7.GetActorsByType("msam")
				mtnk = p7.GetActorsByType("mtnk")
				apc = p7.GetActorsByType("apc")
				jeep = p7.GetActorsByType("jeep")
				stnk = p7.GetActorsByType("stnk")
				arty = p7.GetActorsByType("arty")
				ltnk = p7.GetActorsByType("ltnk")
				ftnk = p7.GetActorsByType("ftnk")
				mlrs = p7.GetActorsByType("mlrs")
				bike = p7.GetActorsByType("bike")
				bggy = p7.GetActorsByType("bggy")
				ctnk = p7.GetActorsByType("ctnk")
				tnk2 = p7.GetActorsByType("2tnk")
				raarty = p7.GetActorsByType("raarty")
				tnk1 = p7.GetActorsByType("1tnk")
				rajeep = p7.GetActorsByType("rajeep")
				tnk4 = p7.GetActorsByType("4tnk")
				v2rl = p7.GetActorsByType("v2rl")
				tnk3 = p7.GetActorsByType("3tnk")
				ttnk = p7.GetActorsByType("ttnk")
				ftrk = p7.GetActorsByType("ftrk")
				raapc = p7.GetActorsByType("raapc")
				hpt2100 = p7.GetActorsByType("2100hpt")
				apt2100 = p7.GetActorsByType("2100apt")
				mch2100 = p7.GetActorsByType("2100mch")
				tch2100 = p7.GetActorsByType("2100tch")
				rvw2100 = p7.GetActorsByType("2100rvw")
				mvw2100 = p7.GetActorsByType("2100mvw")
				rvw22100 = p7.GetActorsByType("2100rvw2")
				mvw22100 = p7.GetActorsByType("2100mvw2")
				scgoliath = p7.GetActorsByType("scgoliath")
				scvulture = p7.GetActorsByType("scvulture")
				scsiegetank = p7.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p8.GetActorsByType("citypolicecar")
				cityfiretruck = p8.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p8vehicle()
	end)
end
p9vehicle = function()
	if p9 and p9.IsBot then
		cityboteliteexpert = p9.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p10 and p9.IsAlliedWith(p10) then
				htnk = p10.GetActorsByType("htnk")
				msam = p10.GetActorsByType("msam")
				mtnk = p10.GetActorsByType("mtnk")
				apc = p10.GetActorsByType("apc")
				jeep = p10.GetActorsByType("jeep")
				stnk = p10.GetActorsByType("stnk")
				arty = p10.GetActorsByType("arty")
				ltnk = p10.GetActorsByType("ltnk")
				ftnk = p10.GetActorsByType("ftnk")
				mlrs = p10.GetActorsByType("mlrs")
				bike = p10.GetActorsByType("bike")
				bggy = p10.GetActorsByType("bggy")
				ctnk = p10.GetActorsByType("ctnk")
				tnk2 = p10.GetActorsByType("2tnk")
				raarty = p10.GetActorsByType("raarty")
				tnk1 = p10.GetActorsByType("1tnk")
				rajeep = p10.GetActorsByType("rajeep")
				tnk4 = p10.GetActorsByType("4tnk")
				v2rl = p10.GetActorsByType("v2rl")
				tnk3 = p10.GetActorsByType("3tnk")
				ttnk = p10.GetActorsByType("ttnk")
				ftrk = p10.GetActorsByType("ftrk")
				raapc = p10.GetActorsByType("raapc")
				hpt2100 = p10.GetActorsByType("2100hpt")
				apt2100 = p10.GetActorsByType("2100apt")
				mch2100 = p10.GetActorsByType("2100mch")
				tch2100 = p10.GetActorsByType("2100tch")
				rvw2100 = p10.GetActorsByType("2100rvw")
				mvw2100 = p10.GetActorsByType("2100mvw")
				rvw22100 = p10.GetActorsByType("2100rvw2")
				mvw22100 = p10.GetActorsByType("2100mvw2")
				scgoliath = p10.GetActorsByType("scgoliath")
				scvulture = p10.GetActorsByType("scvulture")
				scsiegetank = p10.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p11 and p9.IsAlliedWith(p11) then
				htnk = p11.GetActorsByType("htnk")
				msam = p11.GetActorsByType("msam")
				mtnk = p11.GetActorsByType("mtnk")
				apc = p11.GetActorsByType("apc")
				jeep = p11.GetActorsByType("jeep")
				stnk = p11.GetActorsByType("stnk")
				arty = p11.GetActorsByType("arty")
				ltnk = p11.GetActorsByType("ltnk")
				ftnk = p11.GetActorsByType("ftnk")
				mlrs = p11.GetActorsByType("mlrs")
				bike = p11.GetActorsByType("bike")
				bggy = p11.GetActorsByType("bggy")
				ctnk = p11.GetActorsByType("ctnk")
				tnk2 = p11.GetActorsByType("2tnk")
				raarty = p11.GetActorsByType("raarty")
				tnk1 = p11.GetActorsByType("1tnk")
				rajeep = p11.GetActorsByType("rajeep")
				tnk4 = p11.GetActorsByType("4tnk")
				v2rl = p11.GetActorsByType("v2rl")
				tnk3 = p11.GetActorsByType("3tnk")
				ttnk = p11.GetActorsByType("ttnk")
				ftrk = p11.GetActorsByType("ftrk")
				raapc = p11.GetActorsByType("raapc")
				hpt2100 = p11.GetActorsByType("2100hpt")
				apt2100 = p11.GetActorsByType("2100apt")
				mch2100 = p11.GetActorsByType("2100mch")
				tch2100 = p11.GetActorsByType("2100tch")
				rvw2100 = p11.GetActorsByType("2100rvw")
				mvw2100 = p11.GetActorsByType("2100mvw")
				rvw22100 = p11.GetActorsByType("2100rvw2")
				mvw22100 = p11.GetActorsByType("2100mvw2")
				scgoliath = p11.GetActorsByType("scgoliath")
				scvulture = p11.GetActorsByType("scvulture")
				scsiegetank = p11.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p12 and p9.IsAlliedWith(p12) then
				htnk = p12.GetActorsByType("htnk")
				msam = p12.GetActorsByType("msam")
				mtnk = p12.GetActorsByType("mtnk")
				apc = p12.GetActorsByType("apc")
				jeep = p12.GetActorsByType("jeep")
				stnk = p12.GetActorsByType("stnk")
				arty = p12.GetActorsByType("arty")
				ltnk = p12.GetActorsByType("ltnk")
				ftnk = p12.GetActorsByType("ftnk")
				mlrs = p12.GetActorsByType("mlrs")
				bike = p12.GetActorsByType("bike")
				bggy = p12.GetActorsByType("bggy")
				ctnk = p12.GetActorsByType("ctnk")
				tnk2 = p12.GetActorsByType("2tnk")
				raarty = p12.GetActorsByType("raarty")
				tnk1 = p12.GetActorsByType("1tnk")
				rajeep = p12.GetActorsByType("rajeep")
				tnk4 = p12.GetActorsByType("4tnk")
				v2rl = p12.GetActorsByType("v2rl")
				tnk3 = p12.GetActorsByType("3tnk")
				ttnk = p12.GetActorsByType("ttnk")
				ftrk = p12.GetActorsByType("ftrk")
				raapc = p12.GetActorsByType("raapc")
				hpt2100 = p12.GetActorsByType("2100hpt")
				apt2100 = p12.GetActorsByType("2100apt")
				mch2100 = p12.GetActorsByType("2100mch")
				tch2100 = p12.GetActorsByType("2100tch")
				rvw2100 = p12.GetActorsByType("2100rvw")
				mvw2100 = p12.GetActorsByType("2100mvw")
				rvw22100 = p12.GetActorsByType("2100rvw2")
				mvw22100 = p12.GetActorsByType("2100mvw2")
				scgoliath = p12.GetActorsByType("scgoliath")
				scvulture = p12.GetActorsByType("scvulture")
				scsiegetank = p12.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p13 and p9.IsAlliedWith(p13) then
				htnk = p13.GetActorsByType("htnk")
				msam = p13.GetActorsByType("msam")
				mtnk = p13.GetActorsByType("mtnk")
				apc = p13.GetActorsByType("apc")
				jeep = p13.GetActorsByType("jeep")
				stnk = p13.GetActorsByType("stnk")
				arty = p13.GetActorsByType("arty")
				ltnk = p13.GetActorsByType("ltnk")
				ftnk = p13.GetActorsByType("ftnk")
				mlrs = p13.GetActorsByType("mlrs")
				bike = p13.GetActorsByType("bike")
				bggy = p13.GetActorsByType("bggy")
				ctnk = p13.GetActorsByType("ctnk")
				tnk2 = p13.GetActorsByType("2tnk")
				raarty = p13.GetActorsByType("raarty")
				tnk1 = p13.GetActorsByType("1tnk")
				rajeep = p13.GetActorsByType("rajeep")
				tnk4 = p13.GetActorsByType("4tnk")
				v2rl = p13.GetActorsByType("v2rl")
				tnk3 = p13.GetActorsByType("3tnk")
				ttnk = p13.GetActorsByType("ttnk")
				ftrk = p13.GetActorsByType("ftrk")
				raapc = p13.GetActorsByType("raapc")
				hpt2100 = p13.GetActorsByType("2100hpt")
				apt2100 = p13.GetActorsByType("2100apt")
				mch2100 = p13.GetActorsByType("2100mch")
				tch2100 = p13.GetActorsByType("2100tch")
				rvw2100 = p13.GetActorsByType("2100rvw")
				mvw2100 = p13.GetActorsByType("2100mvw")
				rvw22100 = p13.GetActorsByType("2100rvw2")
				mvw22100 = p13.GetActorsByType("2100mvw2")
				scgoliath = p13.GetActorsByType("scgoliath")
				scvulture = p13.GetActorsByType("scvulture")
				scsiegetank = p13.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p14 and p9.IsAlliedWith(p14) then
				htnk = p14.GetActorsByType("htnk")
				msam = p14.GetActorsByType("msam")
				mtnk = p14.GetActorsByType("mtnk")
				apc = p14.GetActorsByType("apc")
				jeep = p14.GetActorsByType("jeep")
				stnk = p14.GetActorsByType("stnk")
				arty = p14.GetActorsByType("arty")
				ltnk = p14.GetActorsByType("ltnk")
				ftnk = p14.GetActorsByType("ftnk")
				mlrs = p14.GetActorsByType("mlrs")
				bike = p14.GetActorsByType("bike")
				bggy = p14.GetActorsByType("bggy")
				ctnk = p14.GetActorsByType("ctnk")
				tnk2 = p14.GetActorsByType("2tnk")
				raarty = p14.GetActorsByType("raarty")
				tnk1 = p14.GetActorsByType("1tnk")
				rajeep = p14.GetActorsByType("rajeep")
				tnk4 = p14.GetActorsByType("4tnk")
				v2rl = p14.GetActorsByType("v2rl")
				tnk3 = p14.GetActorsByType("3tnk")
				ttnk = p14.GetActorsByType("ttnk")
				ftrk = p14.GetActorsByType("ftrk")
				raapc = p14.GetActorsByType("raapc")
				hpt2100 = p14.GetActorsByType("2100hpt")
				apt2100 = p14.GetActorsByType("2100apt")
				mch2100 = p14.GetActorsByType("2100mch")
				tch2100 = p14.GetActorsByType("2100tch")
				rvw2100 = p14.GetActorsByType("2100rvw")
				mvw2100 = p14.GetActorsByType("2100mvw")
				rvw22100 = p14.GetActorsByType("2100rvw2")
				mvw22100 = p14.GetActorsByType("2100mvw2")
				scgoliath = p14.GetActorsByType("scgoliath")
				scvulture = p14.GetActorsByType("scvulture")
				scsiegetank = p14.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p15 and p9.IsAlliedWith(p15) then
				htnk = p15.GetActorsByType("htnk")
				msam = p15.GetActorsByType("msam")
				mtnk = p15.GetActorsByType("mtnk")
				apc = p15.GetActorsByType("apc")
				jeep = p15.GetActorsByType("jeep")
				stnk = p15.GetActorsByType("stnk")
				arty = p15.GetActorsByType("arty")
				ltnk = p15.GetActorsByType("ltnk")
				ftnk = p15.GetActorsByType("ftnk")
				mlrs = p15.GetActorsByType("mlrs")
				bike = p15.GetActorsByType("bike")
				bggy = p15.GetActorsByType("bggy")
				ctnk = p15.GetActorsByType("ctnk")
				tnk2 = p15.GetActorsByType("2tnk")
				raarty = p15.GetActorsByType("raarty")
				tnk1 = p15.GetActorsByType("1tnk")
				rajeep = p15.GetActorsByType("rajeep")
				tnk4 = p15.GetActorsByType("4tnk")
				v2rl = p15.GetActorsByType("v2rl")
				tnk3 = p15.GetActorsByType("3tnk")
				ttnk = p15.GetActorsByType("ttnk")
				ftrk = p15.GetActorsByType("ftrk")
				raapc = p15.GetActorsByType("raapc")
				hpt2100 = p15.GetActorsByType("2100hpt")
				apt2100 = p15.GetActorsByType("2100apt")
				mch2100 = p15.GetActorsByType("2100mch")
				tch2100 = p15.GetActorsByType("2100tch")
				rvw2100 = p15.GetActorsByType("2100rvw")
				mvw2100 = p15.GetActorsByType("2100mvw")
				rvw22100 = p15.GetActorsByType("2100rvw2")
				mvw22100 = p15.GetActorsByType("2100mvw2")
				scgoliath = p15.GetActorsByType("scgoliath")
				scvulture = p15.GetActorsByType("scvulture")
				scsiegetank = p15.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p16 and p9.IsAlliedWith(p16) then
				htnk = p16.GetActorsByType("htnk")
				msam = p16.GetActorsByType("msam")
				mtnk = p16.GetActorsByType("mtnk")
				apc = p16.GetActorsByType("apc")
				jeep = p16.GetActorsByType("jeep")
				stnk = p16.GetActorsByType("stnk")
				arty = p16.GetActorsByType("arty")
				ltnk = p16.GetActorsByType("ltnk")
				ftnk = p16.GetActorsByType("ftnk")
				mlrs = p16.GetActorsByType("mlrs")
				bike = p16.GetActorsByType("bike")
				bggy = p16.GetActorsByType("bggy")
				ctnk = p16.GetActorsByType("ctnk")
				tnk2 = p16.GetActorsByType("2tnk")
				raarty = p16.GetActorsByType("raarty")
				tnk1 = p16.GetActorsByType("1tnk")
				rajeep = p16.GetActorsByType("rajeep")
				tnk4 = p16.GetActorsByType("4tnk")
				v2rl = p16.GetActorsByType("v2rl")
				tnk3 = p16.GetActorsByType("3tnk")
				ttnk = p16.GetActorsByType("ttnk")
				ftrk = p16.GetActorsByType("ftrk")
				raapc = p16.GetActorsByType("raapc")
				hpt2100 = p16.GetActorsByType("2100hpt")
				apt2100 = p16.GetActorsByType("2100apt")
				mch2100 = p16.GetActorsByType("2100mch")
				tch2100 = p16.GetActorsByType("2100tch")
				rvw2100 = p16.GetActorsByType("2100rvw")
				mvw2100 = p16.GetActorsByType("2100mvw")
				rvw22100 = p16.GetActorsByType("2100rvw2")
				mvw22100 = p16.GetActorsByType("2100mvw2")
				scgoliath = p16.GetActorsByType("scgoliath")
				scvulture = p16.GetActorsByType("scvulture")
				scsiegetank = p16.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p1 and p9.IsAlliedWith(p1) then
				htnk = p1.GetActorsByType("htnk")
				msam = p1.GetActorsByType("msam")
				mtnk = p1.GetActorsByType("mtnk")
				apc = p1.GetActorsByType("apc")
				jeep = p1.GetActorsByType("jeep")
				stnk = p1.GetActorsByType("stnk")
				arty = p1.GetActorsByType("arty")
				ltnk = p1.GetActorsByType("ltnk")
				ftnk = p1.GetActorsByType("ftnk")
				mlrs = p1.GetActorsByType("mlrs")
				bike = p1.GetActorsByType("bike")
				bggy = p1.GetActorsByType("bggy")
				ctnk = p1.GetActorsByType("ctnk")
				tnk2 = p1.GetActorsByType("2tnk")
				raarty = p1.GetActorsByType("raarty")
				tnk1 = p1.GetActorsByType("1tnk")
				rajeep = p1.GetActorsByType("rajeep")
				tnk4 = p1.GetActorsByType("4tnk")
				v2rl = p1.GetActorsByType("v2rl")
				tnk3 = p1.GetActorsByType("3tnk")
				ttnk = p1.GetActorsByType("ttnk")
				ftrk = p1.GetActorsByType("ftrk")
				raapc = p1.GetActorsByType("raapc")
				hpt2100 = p1.GetActorsByType("2100hpt")
				apt2100 = p1.GetActorsByType("2100apt")
				mch2100 = p1.GetActorsByType("2100mch")
				tch2100 = p1.GetActorsByType("2100tch")
				rvw2100 = p1.GetActorsByType("2100rvw")
				mvw2100 = p1.GetActorsByType("2100mvw")
				rvw22100 = p1.GetActorsByType("2100rvw2")
				mvw22100 = p1.GetActorsByType("2100mvw2")
				scgoliath = p1.GetActorsByType("scgoliath")
				scvulture = p1.GetActorsByType("scvulture")
				scsiegetank = p1.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p2 and p9.IsAlliedWith(p2) then
				htnk = p2.GetActorsByType("htnk")
				msam = p2.GetActorsByType("msam")
				mtnk = p2.GetActorsByType("mtnk")
				apc = p2.GetActorsByType("apc")
				jeep = p2.GetActorsByType("jeep")
				stnk = p2.GetActorsByType("stnk")
				arty = p2.GetActorsByType("arty")
				ltnk = p2.GetActorsByType("ltnk")
				ftnk = p2.GetActorsByType("ftnk")
				mlrs = p2.GetActorsByType("mlrs")
				bike = p2.GetActorsByType("bike")
				bggy = p2.GetActorsByType("bggy")
				ctnk = p2.GetActorsByType("ctnk")
				tnk2 = p2.GetActorsByType("2tnk")
				raarty = p2.GetActorsByType("raarty")
				tnk1 = p2.GetActorsByType("1tnk")
				rajeep = p2.GetActorsByType("rajeep")
				tnk4 = p2.GetActorsByType("4tnk")
				v2rl = p2.GetActorsByType("v2rl")
				tnk3 = p2.GetActorsByType("3tnk")
				ttnk = p2.GetActorsByType("ttnk")
				ftrk = p2.GetActorsByType("ftrk")
				raapc = p2.GetActorsByType("raapc")
				hpt2100 = p2.GetActorsByType("2100hpt")
				apt2100 = p2.GetActorsByType("2100apt")
				mch2100 = p2.GetActorsByType("2100mch")
				tch2100 = p2.GetActorsByType("2100tch")
				rvw2100 = p2.GetActorsByType("2100rvw")
				mvw2100 = p2.GetActorsByType("2100mvw")
				rvw22100 = p2.GetActorsByType("2100rvw2")
				mvw22100 = p2.GetActorsByType("2100mvw2")
				scgoliath = p2.GetActorsByType("scgoliath")
				scvulture = p2.GetActorsByType("scvulture")
				scsiegetank = p2.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p3 and p9.IsAlliedWith(p3) then
				htnk = p3.GetActorsByType("htnk")
				msam = p3.GetActorsByType("msam")
				mtnk = p3.GetActorsByType("mtnk")
				apc = p3.GetActorsByType("apc")
				jeep = p3.GetActorsByType("jeep")
				stnk = p3.GetActorsByType("stnk")
				arty = p3.GetActorsByType("arty")
				ltnk = p3.GetActorsByType("ltnk")
				ftnk = p3.GetActorsByType("ftnk")
				mlrs = p3.GetActorsByType("mlrs")
				bike = p3.GetActorsByType("bike")
				bggy = p3.GetActorsByType("bggy")
				ctnk = p3.GetActorsByType("ctnk")
				tnk2 = p3.GetActorsByType("2tnk")
				raarty = p3.GetActorsByType("raarty")
				tnk1 = p3.GetActorsByType("1tnk")
				rajeep = p3.GetActorsByType("rajeep")
				tnk4 = p3.GetActorsByType("4tnk")
				v2rl = p3.GetActorsByType("v2rl")
				tnk3 = p3.GetActorsByType("3tnk")
				ttnk = p3.GetActorsByType("ttnk")
				ftrk = p3.GetActorsByType("ftrk")
				raapc = p3.GetActorsByType("raapc")
				hpt2100 = p3.GetActorsByType("2100hpt")
				apt2100 = p3.GetActorsByType("2100apt")
				mch2100 = p3.GetActorsByType("2100mch")
				tch2100 = p3.GetActorsByType("2100tch")
				rvw2100 = p3.GetActorsByType("2100rvw")
				mvw2100 = p3.GetActorsByType("2100mvw")
				rvw22100 = p3.GetActorsByType("2100rvw2")
				mvw22100 = p3.GetActorsByType("2100mvw2")
				scgoliath = p3.GetActorsByType("scgoliath")
				scvulture = p3.GetActorsByType("scvulture")
				scsiegetank = p3.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p4 and p9.IsAlliedWith(p4) then
				htnk = p4.GetActorsByType("htnk")
				msam = p4.GetActorsByType("msam")
				mtnk = p4.GetActorsByType("mtnk")
				apc = p4.GetActorsByType("apc")
				jeep = p4.GetActorsByType("jeep")
				stnk = p4.GetActorsByType("stnk")
				arty = p4.GetActorsByType("arty")
				ltnk = p4.GetActorsByType("ltnk")
				ftnk = p4.GetActorsByType("ftnk")
				mlrs = p4.GetActorsByType("mlrs")
				bike = p4.GetActorsByType("bike")
				bggy = p4.GetActorsByType("bggy")
				ctnk = p4.GetActorsByType("ctnk")
				tnk2 = p4.GetActorsByType("2tnk")
				raarty = p4.GetActorsByType("raarty")
				tnk1 = p4.GetActorsByType("1tnk")
				rajeep = p4.GetActorsByType("rajeep")
				tnk4 = p4.GetActorsByType("4tnk")
				v2rl = p4.GetActorsByType("v2rl")
				tnk3 = p4.GetActorsByType("3tnk")
				ttnk = p4.GetActorsByType("ttnk")
				ftrk = p4.GetActorsByType("ftrk")
				raapc = p4.GetActorsByType("raapc")
				hpt2100 = p4.GetActorsByType("2100hpt")
				apt2100 = p4.GetActorsByType("2100apt")
				mch2100 = p4.GetActorsByType("2100mch")
				tch2100 = p4.GetActorsByType("2100tch")
				rvw2100 = p4.GetActorsByType("2100rvw")
				mvw2100 = p4.GetActorsByType("2100mvw")
				rvw22100 = p4.GetActorsByType("2100rvw2")
				mvw22100 = p4.GetActorsByType("2100mvw2")
				scgoliath = p4.GetActorsByType("scgoliath")
				scvulture = p4.GetActorsByType("scvulture")
				scsiegetank = p4.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p5 and p9.IsAlliedWith(p5) then
				htnk = p5.GetActorsByType("htnk")
				msam = p5.GetActorsByType("msam")
				mtnk = p5.GetActorsByType("mtnk")
				apc = p5.GetActorsByType("apc")
				jeep = p5.GetActorsByType("jeep")
				stnk = p5.GetActorsByType("stnk")
				arty = p5.GetActorsByType("arty")
				ltnk = p5.GetActorsByType("ltnk")
				ftnk = p5.GetActorsByType("ftnk")
				mlrs = p5.GetActorsByType("mlrs")
				bike = p5.GetActorsByType("bike")
				bggy = p5.GetActorsByType("bggy")
				ctnk = p5.GetActorsByType("ctnk")
				tnk2 = p5.GetActorsByType("2tnk")
				raarty = p5.GetActorsByType("raarty")
				tnk1 = p5.GetActorsByType("1tnk")
				rajeep = p5.GetActorsByType("rajeep")
				tnk4 = p5.GetActorsByType("4tnk")
				v2rl = p5.GetActorsByType("v2rl")
				tnk3 = p5.GetActorsByType("3tnk")
				ttnk = p5.GetActorsByType("ttnk")
				ftrk = p5.GetActorsByType("ftrk")
				raapc = p5.GetActorsByType("raapc")
				hpt2100 = p5.GetActorsByType("2100hpt")
				apt2100 = p5.GetActorsByType("2100apt")
				mch2100 = p5.GetActorsByType("2100mch")
				tch2100 = p5.GetActorsByType("2100tch")
				rvw2100 = p5.GetActorsByType("2100rvw")
				mvw2100 = p5.GetActorsByType("2100mvw")
				rvw22100 = p5.GetActorsByType("2100rvw2")
				mvw22100 = p5.GetActorsByType("2100mvw2")
				scgoliath = p5.GetActorsByType("scgoliath")
				scvulture = p5.GetActorsByType("scvulture")
				scsiegetank = p5.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p6 and p9.IsAlliedWith(p6) then
				htnk = p6.GetActorsByType("htnk")
				msam = p6.GetActorsByType("msam")
				mtnk = p6.GetActorsByType("mtnk")
				apc = p6.GetActorsByType("apc")
				jeep = p6.GetActorsByType("jeep")
				stnk = p6.GetActorsByType("stnk")
				arty = p6.GetActorsByType("arty")
				ltnk = p6.GetActorsByType("ltnk")
				ftnk = p6.GetActorsByType("ftnk")
				mlrs = p6.GetActorsByType("mlrs")
				bike = p6.GetActorsByType("bike")
				bggy = p6.GetActorsByType("bggy")
				ctnk = p6.GetActorsByType("ctnk")
				tnk2 = p6.GetActorsByType("2tnk")
				raarty = p6.GetActorsByType("raarty")
				tnk1 = p6.GetActorsByType("1tnk")
				rajeep = p6.GetActorsByType("rajeep")
				tnk4 = p6.GetActorsByType("4tnk")
				v2rl = p6.GetActorsByType("v2rl")
				tnk3 = p6.GetActorsByType("3tnk")
				ttnk = p6.GetActorsByType("ttnk")
				ftrk = p6.GetActorsByType("ftrk")
				raapc = p6.GetActorsByType("raapc")
				hpt2100 = p6.GetActorsByType("2100hpt")
				apt2100 = p6.GetActorsByType("2100apt")
				mch2100 = p6.GetActorsByType("2100mch")
				tch2100 = p6.GetActorsByType("2100tch")
				rvw2100 = p6.GetActorsByType("2100rvw")
				mvw2100 = p6.GetActorsByType("2100mvw")
				rvw22100 = p6.GetActorsByType("2100rvw2")
				mvw22100 = p6.GetActorsByType("2100mvw2")
				scgoliath = p6.GetActorsByType("scgoliath")
				scvulture = p6.GetActorsByType("scvulture")
				scsiegetank = p6.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p7 and p9.IsAlliedWith(p7) then
				htnk = p7.GetActorsByType("htnk")
				msam = p7.GetActorsByType("msam")
				mtnk = p7.GetActorsByType("mtnk")
				apc = p7.GetActorsByType("apc")
				jeep = p7.GetActorsByType("jeep")
				stnk = p7.GetActorsByType("stnk")
				arty = p7.GetActorsByType("arty")
				ltnk = p7.GetActorsByType("ltnk")
				ftnk = p7.GetActorsByType("ftnk")
				mlrs = p7.GetActorsByType("mlrs")
				bike = p7.GetActorsByType("bike")
				bggy = p7.GetActorsByType("bggy")
				ctnk = p7.GetActorsByType("ctnk")
				tnk2 = p7.GetActorsByType("2tnk")
				raarty = p7.GetActorsByType("raarty")
				tnk1 = p7.GetActorsByType("1tnk")
				rajeep = p7.GetActorsByType("rajeep")
				tnk4 = p7.GetActorsByType("4tnk")
				v2rl = p7.GetActorsByType("v2rl")
				tnk3 = p7.GetActorsByType("3tnk")
				ttnk = p7.GetActorsByType("ttnk")
				ftrk = p7.GetActorsByType("ftrk")
				raapc = p7.GetActorsByType("raapc")
				hpt2100 = p7.GetActorsByType("2100hpt")
				apt2100 = p7.GetActorsByType("2100apt")
				mch2100 = p7.GetActorsByType("2100mch")
				tch2100 = p7.GetActorsByType("2100tch")
				rvw2100 = p7.GetActorsByType("2100rvw")
				mvw2100 = p7.GetActorsByType("2100mvw")
				rvw22100 = p7.GetActorsByType("2100rvw2")
				mvw22100 = p7.GetActorsByType("2100mvw2")
				scgoliath = p7.GetActorsByType("scgoliath")
				scvulture = p7.GetActorsByType("scvulture")
				scsiegetank = p7.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p8 and p9.IsAlliedWith(p8) then
				htnk = p8.GetActorsByType("htnk")
				msam = p8.GetActorsByType("msam")
				mtnk = p8.GetActorsByType("mtnk")
				apc = p8.GetActorsByType("apc")
				jeep = p8.GetActorsByType("jeep")
				stnk = p8.GetActorsByType("stnk")
				arty = p8.GetActorsByType("arty")
				ltnk = p8.GetActorsByType("ltnk")
				ftnk = p8.GetActorsByType("ftnk")
				mlrs = p8.GetActorsByType("mlrs")
				bike = p8.GetActorsByType("bike")
				bggy = p8.GetActorsByType("bggy")
				ctnk = p8.GetActorsByType("ctnk")
				tnk2 = p8.GetActorsByType("2tnk")
				raarty = p8.GetActorsByType("raarty")
				tnk1 = p8.GetActorsByType("1tnk")
				rajeep = p8.GetActorsByType("rajeep")
				tnk4 = p8.GetActorsByType("4tnk")
				v2rl = p8.GetActorsByType("v2rl")
				tnk3 = p8.GetActorsByType("3tnk")
				ttnk = p8.GetActorsByType("ttnk")
				ftrk = p8.GetActorsByType("ftrk")
				raapc = p8.GetActorsByType("raapc")
				hpt2100 = p8.GetActorsByType("2100hpt")
				apt2100 = p8.GetActorsByType("2100apt")
				mch2100 = p8.GetActorsByType("2100mch")
				tch2100 = p8.GetActorsByType("2100tch")
				rvw2100 = p8.GetActorsByType("2100rvw")
				mvw2100 = p8.GetActorsByType("2100mvw")
				rvw22100 = p8.GetActorsByType("2100rvw2")
				mvw22100 = p8.GetActorsByType("2100mvw2")
				scgoliath = p8.GetActorsByType("scgoliath")
				scvulture = p8.GetActorsByType("scvulture")
				scsiegetank = p8.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p9.GetActorsByType("citypolicecar")
				cityfiretruck = p9.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p9vehicle()
	end)
end
p10vehicle = function()
	if p10 and p10.IsBot then
		cityboteliteexpert = p10.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p11 and p10.IsAlliedWith(p11) then
				htnk = p11.GetActorsByType("htnk")
				msam = p11.GetActorsByType("msam")
				mtnk = p11.GetActorsByType("mtnk")
				apc = p11.GetActorsByType("apc")
				jeep = p11.GetActorsByType("jeep")
				stnk = p11.GetActorsByType("stnk")
				arty = p11.GetActorsByType("arty")
				ltnk = p11.GetActorsByType("ltnk")
				ftnk = p11.GetActorsByType("ftnk")
				mlrs = p11.GetActorsByType("mlrs")
				bike = p11.GetActorsByType("bike")
				bggy = p11.GetActorsByType("bggy")
				ctnk = p11.GetActorsByType("ctnk")
				tnk2 = p11.GetActorsByType("2tnk")
				raarty = p11.GetActorsByType("raarty")
				tnk1 = p11.GetActorsByType("1tnk")
				rajeep = p11.GetActorsByType("rajeep")
				tnk4 = p11.GetActorsByType("4tnk")
				v2rl = p11.GetActorsByType("v2rl")
				tnk3 = p11.GetActorsByType("3tnk")
				ttnk = p11.GetActorsByType("ttnk")
				ftrk = p11.GetActorsByType("ftrk")
				raapc = p11.GetActorsByType("raapc")
				hpt2100 = p11.GetActorsByType("2100hpt")
				apt2100 = p11.GetActorsByType("2100apt")
				mch2100 = p11.GetActorsByType("2100mch")
				tch2100 = p11.GetActorsByType("2100tch")
				rvw2100 = p11.GetActorsByType("2100rvw")
				mvw2100 = p11.GetActorsByType("2100mvw")
				rvw22100 = p11.GetActorsByType("2100rvw2")
				mvw22100 = p11.GetActorsByType("2100mvw2")
				scgoliath = p11.GetActorsByType("scgoliath")
				scvulture = p11.GetActorsByType("scvulture")
				scsiegetank = p11.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p12 and p10.IsAlliedWith(p12) then
				htnk = p12.GetActorsByType("htnk")
				msam = p12.GetActorsByType("msam")
				mtnk = p12.GetActorsByType("mtnk")
				apc = p12.GetActorsByType("apc")
				jeep = p12.GetActorsByType("jeep")
				stnk = p12.GetActorsByType("stnk")
				arty = p12.GetActorsByType("arty")
				ltnk = p12.GetActorsByType("ltnk")
				ftnk = p12.GetActorsByType("ftnk")
				mlrs = p12.GetActorsByType("mlrs")
				bike = p12.GetActorsByType("bike")
				bggy = p12.GetActorsByType("bggy")
				ctnk = p12.GetActorsByType("ctnk")
				tnk2 = p12.GetActorsByType("2tnk")
				raarty = p12.GetActorsByType("raarty")
				tnk1 = p12.GetActorsByType("1tnk")
				rajeep = p12.GetActorsByType("rajeep")
				tnk4 = p12.GetActorsByType("4tnk")
				v2rl = p12.GetActorsByType("v2rl")
				tnk3 = p12.GetActorsByType("3tnk")
				ttnk = p12.GetActorsByType("ttnk")
				ftrk = p12.GetActorsByType("ftrk")
				raapc = p12.GetActorsByType("raapc")
				hpt2100 = p12.GetActorsByType("2100hpt")
				apt2100 = p12.GetActorsByType("2100apt")
				mch2100 = p12.GetActorsByType("2100mch")
				tch2100 = p12.GetActorsByType("2100tch")
				rvw2100 = p12.GetActorsByType("2100rvw")
				mvw2100 = p12.GetActorsByType("2100mvw")
				rvw22100 = p12.GetActorsByType("2100rvw2")
				mvw22100 = p12.GetActorsByType("2100mvw2")
				scgoliath = p12.GetActorsByType("scgoliath")
				scvulture = p12.GetActorsByType("scvulture")
				scsiegetank = p12.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p13 and p10.IsAlliedWith(p13) then
				htnk = p13.GetActorsByType("htnk")
				msam = p13.GetActorsByType("msam")
				mtnk = p13.GetActorsByType("mtnk")
				apc = p13.GetActorsByType("apc")
				jeep = p13.GetActorsByType("jeep")
				stnk = p13.GetActorsByType("stnk")
				arty = p13.GetActorsByType("arty")
				ltnk = p13.GetActorsByType("ltnk")
				ftnk = p13.GetActorsByType("ftnk")
				mlrs = p13.GetActorsByType("mlrs")
				bike = p13.GetActorsByType("bike")
				bggy = p13.GetActorsByType("bggy")
				ctnk = p13.GetActorsByType("ctnk")
				tnk2 = p13.GetActorsByType("2tnk")
				raarty = p13.GetActorsByType("raarty")
				tnk1 = p13.GetActorsByType("1tnk")
				rajeep = p13.GetActorsByType("rajeep")
				tnk4 = p13.GetActorsByType("4tnk")
				v2rl = p13.GetActorsByType("v2rl")
				tnk3 = p13.GetActorsByType("3tnk")
				ttnk = p13.GetActorsByType("ttnk")
				ftrk = p13.GetActorsByType("ftrk")
				raapc = p13.GetActorsByType("raapc")
				hpt2100 = p13.GetActorsByType("2100hpt")
				apt2100 = p13.GetActorsByType("2100apt")
				mch2100 = p13.GetActorsByType("2100mch")
				tch2100 = p13.GetActorsByType("2100tch")
				rvw2100 = p13.GetActorsByType("2100rvw")
				mvw2100 = p13.GetActorsByType("2100mvw")
				rvw22100 = p13.GetActorsByType("2100rvw2")
				mvw22100 = p13.GetActorsByType("2100mvw2")
				scgoliath = p13.GetActorsByType("scgoliath")
				scvulture = p13.GetActorsByType("scvulture")
				scsiegetank = p13.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p14 and p10.IsAlliedWith(p14) then
				htnk = p14.GetActorsByType("htnk")
				msam = p14.GetActorsByType("msam")
				mtnk = p14.GetActorsByType("mtnk")
				apc = p14.GetActorsByType("apc")
				jeep = p14.GetActorsByType("jeep")
				stnk = p14.GetActorsByType("stnk")
				arty = p14.GetActorsByType("arty")
				ltnk = p14.GetActorsByType("ltnk")
				ftnk = p14.GetActorsByType("ftnk")
				mlrs = p14.GetActorsByType("mlrs")
				bike = p14.GetActorsByType("bike")
				bggy = p14.GetActorsByType("bggy")
				ctnk = p14.GetActorsByType("ctnk")
				tnk2 = p14.GetActorsByType("2tnk")
				raarty = p14.GetActorsByType("raarty")
				tnk1 = p14.GetActorsByType("1tnk")
				rajeep = p14.GetActorsByType("rajeep")
				tnk4 = p14.GetActorsByType("4tnk")
				v2rl = p14.GetActorsByType("v2rl")
				tnk3 = p14.GetActorsByType("3tnk")
				ttnk = p14.GetActorsByType("ttnk")
				ftrk = p14.GetActorsByType("ftrk")
				raapc = p14.GetActorsByType("raapc")
				hpt2100 = p14.GetActorsByType("2100hpt")
				apt2100 = p14.GetActorsByType("2100apt")
				mch2100 = p14.GetActorsByType("2100mch")
				tch2100 = p14.GetActorsByType("2100tch")
				rvw2100 = p14.GetActorsByType("2100rvw")
				mvw2100 = p14.GetActorsByType("2100mvw")
				rvw22100 = p14.GetActorsByType("2100rvw2")
				mvw22100 = p14.GetActorsByType("2100mvw2")
				scgoliath = p14.GetActorsByType("scgoliath")
				scvulture = p14.GetActorsByType("scvulture")
				scsiegetank = p14.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p15 and p10.IsAlliedWith(p15) then
				htnk = p15.GetActorsByType("htnk")
				msam = p15.GetActorsByType("msam")
				mtnk = p15.GetActorsByType("mtnk")
				apc = p15.GetActorsByType("apc")
				jeep = p15.GetActorsByType("jeep")
				stnk = p15.GetActorsByType("stnk")
				arty = p15.GetActorsByType("arty")
				ltnk = p15.GetActorsByType("ltnk")
				ftnk = p15.GetActorsByType("ftnk")
				mlrs = p15.GetActorsByType("mlrs")
				bike = p15.GetActorsByType("bike")
				bggy = p15.GetActorsByType("bggy")
				ctnk = p15.GetActorsByType("ctnk")
				tnk2 = p15.GetActorsByType("2tnk")
				raarty = p15.GetActorsByType("raarty")
				tnk1 = p15.GetActorsByType("1tnk")
				rajeep = p15.GetActorsByType("rajeep")
				tnk4 = p15.GetActorsByType("4tnk")
				v2rl = p15.GetActorsByType("v2rl")
				tnk3 = p15.GetActorsByType("3tnk")
				ttnk = p15.GetActorsByType("ttnk")
				ftrk = p15.GetActorsByType("ftrk")
				raapc = p15.GetActorsByType("raapc")
				hpt2100 = p15.GetActorsByType("2100hpt")
				apt2100 = p15.GetActorsByType("2100apt")
				mch2100 = p15.GetActorsByType("2100mch")
				tch2100 = p15.GetActorsByType("2100tch")
				rvw2100 = p15.GetActorsByType("2100rvw")
				mvw2100 = p15.GetActorsByType("2100mvw")
				rvw22100 = p15.GetActorsByType("2100rvw2")
				mvw22100 = p15.GetActorsByType("2100mvw2")
				scgoliath = p15.GetActorsByType("scgoliath")
				scvulture = p15.GetActorsByType("scvulture")
				scsiegetank = p15.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p16 and p10.IsAlliedWith(p16) then
				htnk = p16.GetActorsByType("htnk")
				msam = p16.GetActorsByType("msam")
				mtnk = p16.GetActorsByType("mtnk")
				apc = p16.GetActorsByType("apc")
				jeep = p16.GetActorsByType("jeep")
				stnk = p16.GetActorsByType("stnk")
				arty = p16.GetActorsByType("arty")
				ltnk = p16.GetActorsByType("ltnk")
				ftnk = p16.GetActorsByType("ftnk")
				mlrs = p16.GetActorsByType("mlrs")
				bike = p16.GetActorsByType("bike")
				bggy = p16.GetActorsByType("bggy")
				ctnk = p16.GetActorsByType("ctnk")
				tnk2 = p16.GetActorsByType("2tnk")
				raarty = p16.GetActorsByType("raarty")
				tnk1 = p16.GetActorsByType("1tnk")
				rajeep = p16.GetActorsByType("rajeep")
				tnk4 = p16.GetActorsByType("4tnk")
				v2rl = p16.GetActorsByType("v2rl")
				tnk3 = p16.GetActorsByType("3tnk")
				ttnk = p16.GetActorsByType("ttnk")
				ftrk = p16.GetActorsByType("ftrk")
				raapc = p16.GetActorsByType("raapc")
				hpt2100 = p16.GetActorsByType("2100hpt")
				apt2100 = p16.GetActorsByType("2100apt")
				mch2100 = p16.GetActorsByType("2100mch")
				tch2100 = p16.GetActorsByType("2100tch")
				rvw2100 = p16.GetActorsByType("2100rvw")
				mvw2100 = p16.GetActorsByType("2100mvw")
				rvw22100 = p16.GetActorsByType("2100rvw2")
				mvw22100 = p16.GetActorsByType("2100mvw2")
				scgoliath = p16.GetActorsByType("scgoliath")
				scvulture = p16.GetActorsByType("scvulture")
				scsiegetank = p16.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p1 and p10.IsAlliedWith(p1) then
				htnk = p1.GetActorsByType("htnk")
				msam = p1.GetActorsByType("msam")
				mtnk = p1.GetActorsByType("mtnk")
				apc = p1.GetActorsByType("apc")
				jeep = p1.GetActorsByType("jeep")
				stnk = p1.GetActorsByType("stnk")
				arty = p1.GetActorsByType("arty")
				ltnk = p1.GetActorsByType("ltnk")
				ftnk = p1.GetActorsByType("ftnk")
				mlrs = p1.GetActorsByType("mlrs")
				bike = p1.GetActorsByType("bike")
				bggy = p1.GetActorsByType("bggy")
				ctnk = p1.GetActorsByType("ctnk")
				tnk2 = p1.GetActorsByType("2tnk")
				raarty = p1.GetActorsByType("raarty")
				tnk1 = p1.GetActorsByType("1tnk")
				rajeep = p1.GetActorsByType("rajeep")
				tnk4 = p1.GetActorsByType("4tnk")
				v2rl = p1.GetActorsByType("v2rl")
				tnk3 = p1.GetActorsByType("3tnk")
				ttnk = p1.GetActorsByType("ttnk")
				ftrk = p1.GetActorsByType("ftrk")
				raapc = p1.GetActorsByType("raapc")
				hpt2100 = p1.GetActorsByType("2100hpt")
				apt2100 = p1.GetActorsByType("2100apt")
				mch2100 = p1.GetActorsByType("2100mch")
				tch2100 = p1.GetActorsByType("2100tch")
				rvw2100 = p1.GetActorsByType("2100rvw")
				mvw2100 = p1.GetActorsByType("2100mvw")
				rvw22100 = p1.GetActorsByType("2100rvw2")
				mvw22100 = p1.GetActorsByType("2100mvw2")
				scgoliath = p1.GetActorsByType("scgoliath")
				scvulture = p1.GetActorsByType("scvulture")
				scsiegetank = p1.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p2 and p10.IsAlliedWith(p2) then
				htnk = p2.GetActorsByType("htnk")
				msam = p2.GetActorsByType("msam")
				mtnk = p2.GetActorsByType("mtnk")
				apc = p2.GetActorsByType("apc")
				jeep = p2.GetActorsByType("jeep")
				stnk = p2.GetActorsByType("stnk")
				arty = p2.GetActorsByType("arty")
				ltnk = p2.GetActorsByType("ltnk")
				ftnk = p2.GetActorsByType("ftnk")
				mlrs = p2.GetActorsByType("mlrs")
				bike = p2.GetActorsByType("bike")
				bggy = p2.GetActorsByType("bggy")
				ctnk = p2.GetActorsByType("ctnk")
				tnk2 = p2.GetActorsByType("2tnk")
				raarty = p2.GetActorsByType("raarty")
				tnk1 = p2.GetActorsByType("1tnk")
				rajeep = p2.GetActorsByType("rajeep")
				tnk4 = p2.GetActorsByType("4tnk")
				v2rl = p2.GetActorsByType("v2rl")
				tnk3 = p2.GetActorsByType("3tnk")
				ttnk = p2.GetActorsByType("ttnk")
				ftrk = p2.GetActorsByType("ftrk")
				raapc = p2.GetActorsByType("raapc")
				hpt2100 = p2.GetActorsByType("2100hpt")
				apt2100 = p2.GetActorsByType("2100apt")
				mch2100 = p2.GetActorsByType("2100mch")
				tch2100 = p2.GetActorsByType("2100tch")
				rvw2100 = p2.GetActorsByType("2100rvw")
				mvw2100 = p2.GetActorsByType("2100mvw")
				rvw22100 = p2.GetActorsByType("2100rvw2")
				mvw22100 = p2.GetActorsByType("2100mvw2")
				scgoliath = p2.GetActorsByType("scgoliath")
				scvulture = p2.GetActorsByType("scvulture")
				scsiegetank = p2.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p3 and p10.IsAlliedWith(p3) then
				htnk = p3.GetActorsByType("htnk")
				msam = p3.GetActorsByType("msam")
				mtnk = p3.GetActorsByType("mtnk")
				apc = p3.GetActorsByType("apc")
				jeep = p3.GetActorsByType("jeep")
				stnk = p3.GetActorsByType("stnk")
				arty = p3.GetActorsByType("arty")
				ltnk = p3.GetActorsByType("ltnk")
				ftnk = p3.GetActorsByType("ftnk")
				mlrs = p3.GetActorsByType("mlrs")
				bike = p3.GetActorsByType("bike")
				bggy = p3.GetActorsByType("bggy")
				ctnk = p3.GetActorsByType("ctnk")
				tnk2 = p3.GetActorsByType("2tnk")
				raarty = p3.GetActorsByType("raarty")
				tnk1 = p3.GetActorsByType("1tnk")
				rajeep = p3.GetActorsByType("rajeep")
				tnk4 = p3.GetActorsByType("4tnk")
				v2rl = p3.GetActorsByType("v2rl")
				tnk3 = p3.GetActorsByType("3tnk")
				ttnk = p3.GetActorsByType("ttnk")
				ftrk = p3.GetActorsByType("ftrk")
				raapc = p3.GetActorsByType("raapc")
				hpt2100 = p3.GetActorsByType("2100hpt")
				apt2100 = p3.GetActorsByType("2100apt")
				mch2100 = p3.GetActorsByType("2100mch")
				tch2100 = p3.GetActorsByType("2100tch")
				rvw2100 = p3.GetActorsByType("2100rvw")
				mvw2100 = p3.GetActorsByType("2100mvw")
				rvw22100 = p3.GetActorsByType("2100rvw2")
				mvw22100 = p3.GetActorsByType("2100mvw2")
				scgoliath = p3.GetActorsByType("scgoliath")
				scvulture = p3.GetActorsByType("scvulture")
				scsiegetank = p3.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p4 and p10.IsAlliedWith(p4) then
				htnk = p4.GetActorsByType("htnk")
				msam = p4.GetActorsByType("msam")
				mtnk = p4.GetActorsByType("mtnk")
				apc = p4.GetActorsByType("apc")
				jeep = p4.GetActorsByType("jeep")
				stnk = p4.GetActorsByType("stnk")
				arty = p4.GetActorsByType("arty")
				ltnk = p4.GetActorsByType("ltnk")
				ftnk = p4.GetActorsByType("ftnk")
				mlrs = p4.GetActorsByType("mlrs")
				bike = p4.GetActorsByType("bike")
				bggy = p4.GetActorsByType("bggy")
				ctnk = p4.GetActorsByType("ctnk")
				tnk2 = p4.GetActorsByType("2tnk")
				raarty = p4.GetActorsByType("raarty")
				tnk1 = p4.GetActorsByType("1tnk")
				rajeep = p4.GetActorsByType("rajeep")
				tnk4 = p4.GetActorsByType("4tnk")
				v2rl = p4.GetActorsByType("v2rl")
				tnk3 = p4.GetActorsByType("3tnk")
				ttnk = p4.GetActorsByType("ttnk")
				ftrk = p4.GetActorsByType("ftrk")
				raapc = p4.GetActorsByType("raapc")
				hpt2100 = p4.GetActorsByType("2100hpt")
				apt2100 = p4.GetActorsByType("2100apt")
				mch2100 = p4.GetActorsByType("2100mch")
				tch2100 = p4.GetActorsByType("2100tch")
				rvw2100 = p4.GetActorsByType("2100rvw")
				mvw2100 = p4.GetActorsByType("2100mvw")
				rvw22100 = p4.GetActorsByType("2100rvw2")
				mvw22100 = p4.GetActorsByType("2100mvw2")
				scgoliath = p4.GetActorsByType("scgoliath")
				scvulture = p4.GetActorsByType("scvulture")
				scsiegetank = p4.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p5 and p10.IsAlliedWith(p5) then
				htnk = p5.GetActorsByType("htnk")
				msam = p5.GetActorsByType("msam")
				mtnk = p5.GetActorsByType("mtnk")
				apc = p5.GetActorsByType("apc")
				jeep = p5.GetActorsByType("jeep")
				stnk = p5.GetActorsByType("stnk")
				arty = p5.GetActorsByType("arty")
				ltnk = p5.GetActorsByType("ltnk")
				ftnk = p5.GetActorsByType("ftnk")
				mlrs = p5.GetActorsByType("mlrs")
				bike = p5.GetActorsByType("bike")
				bggy = p5.GetActorsByType("bggy")
				ctnk = p5.GetActorsByType("ctnk")
				tnk2 = p5.GetActorsByType("2tnk")
				raarty = p5.GetActorsByType("raarty")
				tnk1 = p5.GetActorsByType("1tnk")
				rajeep = p5.GetActorsByType("rajeep")
				tnk4 = p5.GetActorsByType("4tnk")
				v2rl = p5.GetActorsByType("v2rl")
				tnk3 = p5.GetActorsByType("3tnk")
				ttnk = p5.GetActorsByType("ttnk")
				ftrk = p5.GetActorsByType("ftrk")
				raapc = p5.GetActorsByType("raapc")
				hpt2100 = p5.GetActorsByType("2100hpt")
				apt2100 = p5.GetActorsByType("2100apt")
				mch2100 = p5.GetActorsByType("2100mch")
				tch2100 = p5.GetActorsByType("2100tch")
				rvw2100 = p5.GetActorsByType("2100rvw")
				mvw2100 = p5.GetActorsByType("2100mvw")
				rvw22100 = p5.GetActorsByType("2100rvw2")
				mvw22100 = p5.GetActorsByType("2100mvw2")
				scgoliath = p5.GetActorsByType("scgoliath")
				scvulture = p5.GetActorsByType("scvulture")
				scsiegetank = p5.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p6 and p10.IsAlliedWith(p6) then
				htnk = p6.GetActorsByType("htnk")
				msam = p6.GetActorsByType("msam")
				mtnk = p6.GetActorsByType("mtnk")
				apc = p6.GetActorsByType("apc")
				jeep = p6.GetActorsByType("jeep")
				stnk = p6.GetActorsByType("stnk")
				arty = p6.GetActorsByType("arty")
				ltnk = p6.GetActorsByType("ltnk")
				ftnk = p6.GetActorsByType("ftnk")
				mlrs = p6.GetActorsByType("mlrs")
				bike = p6.GetActorsByType("bike")
				bggy = p6.GetActorsByType("bggy")
				ctnk = p6.GetActorsByType("ctnk")
				tnk2 = p6.GetActorsByType("2tnk")
				raarty = p6.GetActorsByType("raarty")
				tnk1 = p6.GetActorsByType("1tnk")
				rajeep = p6.GetActorsByType("rajeep")
				tnk4 = p6.GetActorsByType("4tnk")
				v2rl = p6.GetActorsByType("v2rl")
				tnk3 = p6.GetActorsByType("3tnk")
				ttnk = p6.GetActorsByType("ttnk")
				ftrk = p6.GetActorsByType("ftrk")
				raapc = p6.GetActorsByType("raapc")
				hpt2100 = p6.GetActorsByType("2100hpt")
				apt2100 = p6.GetActorsByType("2100apt")
				mch2100 = p6.GetActorsByType("2100mch")
				tch2100 = p6.GetActorsByType("2100tch")
				rvw2100 = p6.GetActorsByType("2100rvw")
				mvw2100 = p6.GetActorsByType("2100mvw")
				rvw22100 = p6.GetActorsByType("2100rvw2")
				mvw22100 = p6.GetActorsByType("2100mvw2")
				scgoliath = p6.GetActorsByType("scgoliath")
				scvulture = p6.GetActorsByType("scvulture")
				scsiegetank = p6.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p7 and p10.IsAlliedWith(p7) then
				htnk = p7.GetActorsByType("htnk")
				msam = p7.GetActorsByType("msam")
				mtnk = p7.GetActorsByType("mtnk")
				apc = p7.GetActorsByType("apc")
				jeep = p7.GetActorsByType("jeep")
				stnk = p7.GetActorsByType("stnk")
				arty = p7.GetActorsByType("arty")
				ltnk = p7.GetActorsByType("ltnk")
				ftnk = p7.GetActorsByType("ftnk")
				mlrs = p7.GetActorsByType("mlrs")
				bike = p7.GetActorsByType("bike")
				bggy = p7.GetActorsByType("bggy")
				ctnk = p7.GetActorsByType("ctnk")
				tnk2 = p7.GetActorsByType("2tnk")
				raarty = p7.GetActorsByType("raarty")
				tnk1 = p7.GetActorsByType("1tnk")
				rajeep = p7.GetActorsByType("rajeep")
				tnk4 = p7.GetActorsByType("4tnk")
				v2rl = p7.GetActorsByType("v2rl")
				tnk3 = p7.GetActorsByType("3tnk")
				ttnk = p7.GetActorsByType("ttnk")
				ftrk = p7.GetActorsByType("ftrk")
				raapc = p7.GetActorsByType("raapc")
				hpt2100 = p7.GetActorsByType("2100hpt")
				apt2100 = p7.GetActorsByType("2100apt")
				mch2100 = p7.GetActorsByType("2100mch")
				tch2100 = p7.GetActorsByType("2100tch")
				rvw2100 = p7.GetActorsByType("2100rvw")
				mvw2100 = p7.GetActorsByType("2100mvw")
				rvw22100 = p7.GetActorsByType("2100rvw2")
				mvw22100 = p7.GetActorsByType("2100mvw2")
				scgoliath = p7.GetActorsByType("scgoliath")
				scvulture = p7.GetActorsByType("scvulture")
				scsiegetank = p7.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p8 and p10.IsAlliedWith(p8) then
				htnk = p8.GetActorsByType("htnk")
				msam = p8.GetActorsByType("msam")
				mtnk = p8.GetActorsByType("mtnk")
				apc = p8.GetActorsByType("apc")
				jeep = p8.GetActorsByType("jeep")
				stnk = p8.GetActorsByType("stnk")
				arty = p8.GetActorsByType("arty")
				ltnk = p8.GetActorsByType("ltnk")
				ftnk = p8.GetActorsByType("ftnk")
				mlrs = p8.GetActorsByType("mlrs")
				bike = p8.GetActorsByType("bike")
				bggy = p8.GetActorsByType("bggy")
				ctnk = p8.GetActorsByType("ctnk")
				tnk2 = p8.GetActorsByType("2tnk")
				raarty = p8.GetActorsByType("raarty")
				tnk1 = p8.GetActorsByType("1tnk")
				rajeep = p8.GetActorsByType("rajeep")
				tnk4 = p8.GetActorsByType("4tnk")
				v2rl = p8.GetActorsByType("v2rl")
				tnk3 = p8.GetActorsByType("3tnk")
				ttnk = p8.GetActorsByType("ttnk")
				ftrk = p8.GetActorsByType("ftrk")
				raapc = p8.GetActorsByType("raapc")
				hpt2100 = p8.GetActorsByType("2100hpt")
				apt2100 = p8.GetActorsByType("2100apt")
				mch2100 = p8.GetActorsByType("2100mch")
				tch2100 = p8.GetActorsByType("2100tch")
				rvw2100 = p8.GetActorsByType("2100rvw")
				mvw2100 = p8.GetActorsByType("2100mvw")
				rvw22100 = p8.GetActorsByType("2100rvw2")
				mvw22100 = p8.GetActorsByType("2100mvw2")
				scgoliath = p8.GetActorsByType("scgoliath")
				scvulture = p8.GetActorsByType("scvulture")
				scsiegetank = p8.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p9 and p10.IsAlliedWith(p9) then
				htnk = p9.GetActorsByType("htnk")
				msam = p9.GetActorsByType("msam")
				mtnk = p9.GetActorsByType("mtnk")
				apc = p9.GetActorsByType("apc")
				jeep = p9.GetActorsByType("jeep")
				stnk = p9.GetActorsByType("stnk")
				arty = p9.GetActorsByType("arty")
				ltnk = p9.GetActorsByType("ltnk")
				ftnk = p9.GetActorsByType("ftnk")
				mlrs = p9.GetActorsByType("mlrs")
				bike = p9.GetActorsByType("bike")
				bggy = p9.GetActorsByType("bggy")
				ctnk = p9.GetActorsByType("ctnk")
				tnk2 = p9.GetActorsByType("2tnk")
				raarty = p9.GetActorsByType("raarty")
				tnk1 = p9.GetActorsByType("1tnk")
				rajeep = p9.GetActorsByType("rajeep")
				tnk4 = p9.GetActorsByType("4tnk")
				v2rl = p9.GetActorsByType("v2rl")
				tnk3 = p9.GetActorsByType("3tnk")
				ttnk = p9.GetActorsByType("ttnk")
				ftrk = p9.GetActorsByType("ftrk")
				raapc = p9.GetActorsByType("raapc")
				hpt2100 = p9.GetActorsByType("2100hpt")
				apt2100 = p9.GetActorsByType("2100apt")
				mch2100 = p9.GetActorsByType("2100mch")
				tch2100 = p9.GetActorsByType("2100tch")
				rvw2100 = p9.GetActorsByType("2100rvw")
				mvw2100 = p9.GetActorsByType("2100mvw")
				rvw22100 = p9.GetActorsByType("2100rvw2")
				mvw22100 = p9.GetActorsByType("2100mvw2")
				scgoliath = p9.GetActorsByType("scgoliath")
				scvulture = p9.GetActorsByType("scvulture")
				scsiegetank = p9.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p10.GetActorsByType("citypolicecar")
				cityfiretruck = p10.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p10vehicle()
	end)
end
p11vehicle = function()
	if p11 and p11.IsBot then
		cityboteliteexpert = p11.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p12 and p11.IsAlliedWith(p12) then
				htnk = p12.GetActorsByType("htnk")
				msam = p12.GetActorsByType("msam")
				mtnk = p12.GetActorsByType("mtnk")
				apc = p12.GetActorsByType("apc")
				jeep = p12.GetActorsByType("jeep")
				stnk = p12.GetActorsByType("stnk")
				arty = p12.GetActorsByType("arty")
				ltnk = p12.GetActorsByType("ltnk")
				ftnk = p12.GetActorsByType("ftnk")
				mlrs = p12.GetActorsByType("mlrs")
				bike = p12.GetActorsByType("bike")
				bggy = p12.GetActorsByType("bggy")
				ctnk = p12.GetActorsByType("ctnk")
				tnk2 = p12.GetActorsByType("2tnk")
				raarty = p12.GetActorsByType("raarty")
				tnk1 = p12.GetActorsByType("1tnk")
				rajeep = p12.GetActorsByType("rajeep")
				tnk4 = p12.GetActorsByType("4tnk")
				v2rl = p12.GetActorsByType("v2rl")
				tnk3 = p12.GetActorsByType("3tnk")
				ttnk = p12.GetActorsByType("ttnk")
				ftrk = p12.GetActorsByType("ftrk")
				raapc = p12.GetActorsByType("raapc")
				hpt2100 = p12.GetActorsByType("2100hpt")
				apt2100 = p12.GetActorsByType("2100apt")
				mch2100 = p12.GetActorsByType("2100mch")
				tch2100 = p12.GetActorsByType("2100tch")
				rvw2100 = p12.GetActorsByType("2100rvw")
				mvw2100 = p12.GetActorsByType("2100mvw")
				rvw22100 = p12.GetActorsByType("2100rvw2")
				mvw22100 = p12.GetActorsByType("2100mvw2")
				scgoliath = p12.GetActorsByType("scgoliath")
				scvulture = p12.GetActorsByType("scvulture")
				scsiegetank = p12.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p13 and p11.IsAlliedWith(p13) then
				htnk = p13.GetActorsByType("htnk")
				msam = p13.GetActorsByType("msam")
				mtnk = p13.GetActorsByType("mtnk")
				apc = p13.GetActorsByType("apc")
				jeep = p13.GetActorsByType("jeep")
				stnk = p13.GetActorsByType("stnk")
				arty = p13.GetActorsByType("arty")
				ltnk = p13.GetActorsByType("ltnk")
				ftnk = p13.GetActorsByType("ftnk")
				mlrs = p13.GetActorsByType("mlrs")
				bike = p13.GetActorsByType("bike")
				bggy = p13.GetActorsByType("bggy")
				ctnk = p13.GetActorsByType("ctnk")
				tnk2 = p13.GetActorsByType("2tnk")
				raarty = p13.GetActorsByType("raarty")
				tnk1 = p13.GetActorsByType("1tnk")
				rajeep = p13.GetActorsByType("rajeep")
				tnk4 = p13.GetActorsByType("4tnk")
				v2rl = p13.GetActorsByType("v2rl")
				tnk3 = p13.GetActorsByType("3tnk")
				ttnk = p13.GetActorsByType("ttnk")
				ftrk = p13.GetActorsByType("ftrk")
				raapc = p13.GetActorsByType("raapc")
				hpt2100 = p13.GetActorsByType("2100hpt")
				apt2100 = p13.GetActorsByType("2100apt")
				mch2100 = p13.GetActorsByType("2100mch")
				tch2100 = p13.GetActorsByType("2100tch")
				rvw2100 = p13.GetActorsByType("2100rvw")
				mvw2100 = p13.GetActorsByType("2100mvw")
				rvw22100 = p13.GetActorsByType("2100rvw2")
				mvw22100 = p13.GetActorsByType("2100mvw2")
				scgoliath = p13.GetActorsByType("scgoliath")
				scvulture = p13.GetActorsByType("scvulture")
				scsiegetank = p13.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p14 and p11.IsAlliedWith(p14) then
				htnk = p14.GetActorsByType("htnk")
				msam = p14.GetActorsByType("msam")
				mtnk = p14.GetActorsByType("mtnk")
				apc = p14.GetActorsByType("apc")
				jeep = p14.GetActorsByType("jeep")
				stnk = p14.GetActorsByType("stnk")
				arty = p14.GetActorsByType("arty")
				ltnk = p14.GetActorsByType("ltnk")
				ftnk = p14.GetActorsByType("ftnk")
				mlrs = p14.GetActorsByType("mlrs")
				bike = p14.GetActorsByType("bike")
				bggy = p14.GetActorsByType("bggy")
				ctnk = p14.GetActorsByType("ctnk")
				tnk2 = p14.GetActorsByType("2tnk")
				raarty = p14.GetActorsByType("raarty")
				tnk1 = p14.GetActorsByType("1tnk")
				rajeep = p14.GetActorsByType("rajeep")
				tnk4 = p14.GetActorsByType("4tnk")
				v2rl = p14.GetActorsByType("v2rl")
				tnk3 = p14.GetActorsByType("3tnk")
				ttnk = p14.GetActorsByType("ttnk")
				ftrk = p14.GetActorsByType("ftrk")
				raapc = p14.GetActorsByType("raapc")
				hpt2100 = p14.GetActorsByType("2100hpt")
				apt2100 = p14.GetActorsByType("2100apt")
				mch2100 = p14.GetActorsByType("2100mch")
				tch2100 = p14.GetActorsByType("2100tch")
				rvw2100 = p14.GetActorsByType("2100rvw")
				mvw2100 = p14.GetActorsByType("2100mvw")
				rvw22100 = p14.GetActorsByType("2100rvw2")
				mvw22100 = p14.GetActorsByType("2100mvw2")
				scgoliath = p14.GetActorsByType("scgoliath")
				scvulture = p14.GetActorsByType("scvulture")
				scsiegetank = p14.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p15 and p11.IsAlliedWith(p15) then
				htnk = p15.GetActorsByType("htnk")
				msam = p15.GetActorsByType("msam")
				mtnk = p15.GetActorsByType("mtnk")
				apc = p15.GetActorsByType("apc")
				jeep = p15.GetActorsByType("jeep")
				stnk = p15.GetActorsByType("stnk")
				arty = p15.GetActorsByType("arty")
				ltnk = p15.GetActorsByType("ltnk")
				ftnk = p15.GetActorsByType("ftnk")
				mlrs = p15.GetActorsByType("mlrs")
				bike = p15.GetActorsByType("bike")
				bggy = p15.GetActorsByType("bggy")
				ctnk = p15.GetActorsByType("ctnk")
				tnk2 = p15.GetActorsByType("2tnk")
				raarty = p15.GetActorsByType("raarty")
				tnk1 = p15.GetActorsByType("1tnk")
				rajeep = p15.GetActorsByType("rajeep")
				tnk4 = p15.GetActorsByType("4tnk")
				v2rl = p15.GetActorsByType("v2rl")
				tnk3 = p15.GetActorsByType("3tnk")
				ttnk = p15.GetActorsByType("ttnk")
				ftrk = p15.GetActorsByType("ftrk")
				raapc = p15.GetActorsByType("raapc")
				hpt2100 = p15.GetActorsByType("2100hpt")
				apt2100 = p15.GetActorsByType("2100apt")
				mch2100 = p15.GetActorsByType("2100mch")
				tch2100 = p15.GetActorsByType("2100tch")
				rvw2100 = p15.GetActorsByType("2100rvw")
				mvw2100 = p15.GetActorsByType("2100mvw")
				rvw22100 = p15.GetActorsByType("2100rvw2")
				mvw22100 = p15.GetActorsByType("2100mvw2")
				scgoliath = p15.GetActorsByType("scgoliath")
				scvulture = p15.GetActorsByType("scvulture")
				scsiegetank = p15.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p16 and p11.IsAlliedWith(p16) then
				htnk = p16.GetActorsByType("htnk")
				msam = p16.GetActorsByType("msam")
				mtnk = p16.GetActorsByType("mtnk")
				apc = p16.GetActorsByType("apc")
				jeep = p16.GetActorsByType("jeep")
				stnk = p16.GetActorsByType("stnk")
				arty = p16.GetActorsByType("arty")
				ltnk = p16.GetActorsByType("ltnk")
				ftnk = p16.GetActorsByType("ftnk")
				mlrs = p16.GetActorsByType("mlrs")
				bike = p16.GetActorsByType("bike")
				bggy = p16.GetActorsByType("bggy")
				ctnk = p16.GetActorsByType("ctnk")
				tnk2 = p16.GetActorsByType("2tnk")
				raarty = p16.GetActorsByType("raarty")
				tnk1 = p16.GetActorsByType("1tnk")
				rajeep = p16.GetActorsByType("rajeep")
				tnk4 = p16.GetActorsByType("4tnk")
				v2rl = p16.GetActorsByType("v2rl")
				tnk3 = p16.GetActorsByType("3tnk")
				ttnk = p16.GetActorsByType("ttnk")
				ftrk = p16.GetActorsByType("ftrk")
				raapc = p16.GetActorsByType("raapc")
				hpt2100 = p16.GetActorsByType("2100hpt")
				apt2100 = p16.GetActorsByType("2100apt")
				mch2100 = p16.GetActorsByType("2100mch")
				tch2100 = p16.GetActorsByType("2100tch")
				rvw2100 = p16.GetActorsByType("2100rvw")
				mvw2100 = p16.GetActorsByType("2100mvw")
				rvw22100 = p16.GetActorsByType("2100rvw2")
				mvw22100 = p16.GetActorsByType("2100mvw2")
				scgoliath = p16.GetActorsByType("scgoliath")
				scvulture = p16.GetActorsByType("scvulture")
				scsiegetank = p16.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p1 and p11.IsAlliedWith(p1) then
				htnk = p1.GetActorsByType("htnk")
				msam = p1.GetActorsByType("msam")
				mtnk = p1.GetActorsByType("mtnk")
				apc = p1.GetActorsByType("apc")
				jeep = p1.GetActorsByType("jeep")
				stnk = p1.GetActorsByType("stnk")
				arty = p1.GetActorsByType("arty")
				ltnk = p1.GetActorsByType("ltnk")
				ftnk = p1.GetActorsByType("ftnk")
				mlrs = p1.GetActorsByType("mlrs")
				bike = p1.GetActorsByType("bike")
				bggy = p1.GetActorsByType("bggy")
				ctnk = p1.GetActorsByType("ctnk")
				tnk2 = p1.GetActorsByType("2tnk")
				raarty = p1.GetActorsByType("raarty")
				tnk1 = p1.GetActorsByType("1tnk")
				rajeep = p1.GetActorsByType("rajeep")
				tnk4 = p1.GetActorsByType("4tnk")
				v2rl = p1.GetActorsByType("v2rl")
				tnk3 = p1.GetActorsByType("3tnk")
				ttnk = p1.GetActorsByType("ttnk")
				ftrk = p1.GetActorsByType("ftrk")
				raapc = p1.GetActorsByType("raapc")
				hpt2100 = p1.GetActorsByType("2100hpt")
				apt2100 = p1.GetActorsByType("2100apt")
				mch2100 = p1.GetActorsByType("2100mch")
				tch2100 = p1.GetActorsByType("2100tch")
				rvw2100 = p1.GetActorsByType("2100rvw")
				mvw2100 = p1.GetActorsByType("2100mvw")
				rvw22100 = p1.GetActorsByType("2100rvw2")
				mvw22100 = p1.GetActorsByType("2100mvw2")
				scgoliath = p1.GetActorsByType("scgoliath")
				scvulture = p1.GetActorsByType("scvulture")
				scsiegetank = p1.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p2 and p11.IsAlliedWith(p2) then
				htnk = p2.GetActorsByType("htnk")
				msam = p2.GetActorsByType("msam")
				mtnk = p2.GetActorsByType("mtnk")
				apc = p2.GetActorsByType("apc")
				jeep = p2.GetActorsByType("jeep")
				stnk = p2.GetActorsByType("stnk")
				arty = p2.GetActorsByType("arty")
				ltnk = p2.GetActorsByType("ltnk")
				ftnk = p2.GetActorsByType("ftnk")
				mlrs = p2.GetActorsByType("mlrs")
				bike = p2.GetActorsByType("bike")
				bggy = p2.GetActorsByType("bggy")
				ctnk = p2.GetActorsByType("ctnk")
				tnk2 = p2.GetActorsByType("2tnk")
				raarty = p2.GetActorsByType("raarty")
				tnk1 = p2.GetActorsByType("1tnk")
				rajeep = p2.GetActorsByType("rajeep")
				tnk4 = p2.GetActorsByType("4tnk")
				v2rl = p2.GetActorsByType("v2rl")
				tnk3 = p2.GetActorsByType("3tnk")
				ttnk = p2.GetActorsByType("ttnk")
				ftrk = p2.GetActorsByType("ftrk")
				raapc = p2.GetActorsByType("raapc")
				hpt2100 = p2.GetActorsByType("2100hpt")
				apt2100 = p2.GetActorsByType("2100apt")
				mch2100 = p2.GetActorsByType("2100mch")
				tch2100 = p2.GetActorsByType("2100tch")
				rvw2100 = p2.GetActorsByType("2100rvw")
				mvw2100 = p2.GetActorsByType("2100mvw")
				rvw22100 = p2.GetActorsByType("2100rvw2")
				mvw22100 = p2.GetActorsByType("2100mvw2")
				scgoliath = p2.GetActorsByType("scgoliath")
				scvulture = p2.GetActorsByType("scvulture")
				scsiegetank = p2.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p3 and p11.IsAlliedWith(p3) then
				htnk = p3.GetActorsByType("htnk")
				msam = p3.GetActorsByType("msam")
				mtnk = p3.GetActorsByType("mtnk")
				apc = p3.GetActorsByType("apc")
				jeep = p3.GetActorsByType("jeep")
				stnk = p3.GetActorsByType("stnk")
				arty = p3.GetActorsByType("arty")
				ltnk = p3.GetActorsByType("ltnk")
				ftnk = p3.GetActorsByType("ftnk")
				mlrs = p3.GetActorsByType("mlrs")
				bike = p3.GetActorsByType("bike")
				bggy = p3.GetActorsByType("bggy")
				ctnk = p3.GetActorsByType("ctnk")
				tnk2 = p3.GetActorsByType("2tnk")
				raarty = p3.GetActorsByType("raarty")
				tnk1 = p3.GetActorsByType("1tnk")
				rajeep = p3.GetActorsByType("rajeep")
				tnk4 = p3.GetActorsByType("4tnk")
				v2rl = p3.GetActorsByType("v2rl")
				tnk3 = p3.GetActorsByType("3tnk")
				ttnk = p3.GetActorsByType("ttnk")
				ftrk = p3.GetActorsByType("ftrk")
				raapc = p3.GetActorsByType("raapc")
				hpt2100 = p3.GetActorsByType("2100hpt")
				apt2100 = p3.GetActorsByType("2100apt")
				mch2100 = p3.GetActorsByType("2100mch")
				tch2100 = p3.GetActorsByType("2100tch")
				rvw2100 = p3.GetActorsByType("2100rvw")
				mvw2100 = p3.GetActorsByType("2100mvw")
				rvw22100 = p3.GetActorsByType("2100rvw2")
				mvw22100 = p3.GetActorsByType("2100mvw2")
				scgoliath = p3.GetActorsByType("scgoliath")
				scvulture = p3.GetActorsByType("scvulture")
				scsiegetank = p3.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p4 and p11.IsAlliedWith(p4) then
				htnk = p4.GetActorsByType("htnk")
				msam = p4.GetActorsByType("msam")
				mtnk = p4.GetActorsByType("mtnk")
				apc = p4.GetActorsByType("apc")
				jeep = p4.GetActorsByType("jeep")
				stnk = p4.GetActorsByType("stnk")
				arty = p4.GetActorsByType("arty")
				ltnk = p4.GetActorsByType("ltnk")
				ftnk = p4.GetActorsByType("ftnk")
				mlrs = p4.GetActorsByType("mlrs")
				bike = p4.GetActorsByType("bike")
				bggy = p4.GetActorsByType("bggy")
				ctnk = p4.GetActorsByType("ctnk")
				tnk2 = p4.GetActorsByType("2tnk")
				raarty = p4.GetActorsByType("raarty")
				tnk1 = p4.GetActorsByType("1tnk")
				rajeep = p4.GetActorsByType("rajeep")
				tnk4 = p4.GetActorsByType("4tnk")
				v2rl = p4.GetActorsByType("v2rl")
				tnk3 = p4.GetActorsByType("3tnk")
				ttnk = p4.GetActorsByType("ttnk")
				ftrk = p4.GetActorsByType("ftrk")
				raapc = p4.GetActorsByType("raapc")
				hpt2100 = p4.GetActorsByType("2100hpt")
				apt2100 = p4.GetActorsByType("2100apt")
				mch2100 = p4.GetActorsByType("2100mch")
				tch2100 = p4.GetActorsByType("2100tch")
				rvw2100 = p4.GetActorsByType("2100rvw")
				mvw2100 = p4.GetActorsByType("2100mvw")
				rvw22100 = p4.GetActorsByType("2100rvw2")
				mvw22100 = p4.GetActorsByType("2100mvw2")
				scgoliath = p4.GetActorsByType("scgoliath")
				scvulture = p4.GetActorsByType("scvulture")
				scsiegetank = p4.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p5 and p11.IsAlliedWith(p5) then
				htnk = p5.GetActorsByType("htnk")
				msam = p5.GetActorsByType("msam")
				mtnk = p5.GetActorsByType("mtnk")
				apc = p5.GetActorsByType("apc")
				jeep = p5.GetActorsByType("jeep")
				stnk = p5.GetActorsByType("stnk")
				arty = p5.GetActorsByType("arty")
				ltnk = p5.GetActorsByType("ltnk")
				ftnk = p5.GetActorsByType("ftnk")
				mlrs = p5.GetActorsByType("mlrs")
				bike = p5.GetActorsByType("bike")
				bggy = p5.GetActorsByType("bggy")
				ctnk = p5.GetActorsByType("ctnk")
				tnk2 = p5.GetActorsByType("2tnk")
				raarty = p5.GetActorsByType("raarty")
				tnk1 = p5.GetActorsByType("1tnk")
				rajeep = p5.GetActorsByType("rajeep")
				tnk4 = p5.GetActorsByType("4tnk")
				v2rl = p5.GetActorsByType("v2rl")
				tnk3 = p5.GetActorsByType("3tnk")
				ttnk = p5.GetActorsByType("ttnk")
				ftrk = p5.GetActorsByType("ftrk")
				raapc = p5.GetActorsByType("raapc")
				hpt2100 = p5.GetActorsByType("2100hpt")
				apt2100 = p5.GetActorsByType("2100apt")
				mch2100 = p5.GetActorsByType("2100mch")
				tch2100 = p5.GetActorsByType("2100tch")
				rvw2100 = p5.GetActorsByType("2100rvw")
				mvw2100 = p5.GetActorsByType("2100mvw")
				rvw22100 = p5.GetActorsByType("2100rvw2")
				mvw22100 = p5.GetActorsByType("2100mvw2")
				scgoliath = p5.GetActorsByType("scgoliath")
				scvulture = p5.GetActorsByType("scvulture")
				scsiegetank = p5.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p6 and p11.IsAlliedWith(p6) then
				htnk = p6.GetActorsByType("htnk")
				msam = p6.GetActorsByType("msam")
				mtnk = p6.GetActorsByType("mtnk")
				apc = p6.GetActorsByType("apc")
				jeep = p6.GetActorsByType("jeep")
				stnk = p6.GetActorsByType("stnk")
				arty = p6.GetActorsByType("arty")
				ltnk = p6.GetActorsByType("ltnk")
				ftnk = p6.GetActorsByType("ftnk")
				mlrs = p6.GetActorsByType("mlrs")
				bike = p6.GetActorsByType("bike")
				bggy = p6.GetActorsByType("bggy")
				ctnk = p6.GetActorsByType("ctnk")
				tnk2 = p6.GetActorsByType("2tnk")
				raarty = p6.GetActorsByType("raarty")
				tnk1 = p6.GetActorsByType("1tnk")
				rajeep = p6.GetActorsByType("rajeep")
				tnk4 = p6.GetActorsByType("4tnk")
				v2rl = p6.GetActorsByType("v2rl")
				tnk3 = p6.GetActorsByType("3tnk")
				ttnk = p6.GetActorsByType("ttnk")
				ftrk = p6.GetActorsByType("ftrk")
				raapc = p6.GetActorsByType("raapc")
				hpt2100 = p6.GetActorsByType("2100hpt")
				apt2100 = p6.GetActorsByType("2100apt")
				mch2100 = p6.GetActorsByType("2100mch")
				tch2100 = p6.GetActorsByType("2100tch")
				rvw2100 = p6.GetActorsByType("2100rvw")
				mvw2100 = p6.GetActorsByType("2100mvw")
				rvw22100 = p6.GetActorsByType("2100rvw2")
				mvw22100 = p6.GetActorsByType("2100mvw2")
				scgoliath = p6.GetActorsByType("scgoliath")
				scvulture = p6.GetActorsByType("scvulture")
				scsiegetank = p6.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p7 and p11.IsAlliedWith(p7) then
				htnk = p7.GetActorsByType("htnk")
				msam = p7.GetActorsByType("msam")
				mtnk = p7.GetActorsByType("mtnk")
				apc = p7.GetActorsByType("apc")
				jeep = p7.GetActorsByType("jeep")
				stnk = p7.GetActorsByType("stnk")
				arty = p7.GetActorsByType("arty")
				ltnk = p7.GetActorsByType("ltnk")
				ftnk = p7.GetActorsByType("ftnk")
				mlrs = p7.GetActorsByType("mlrs")
				bike = p7.GetActorsByType("bike")
				bggy = p7.GetActorsByType("bggy")
				ctnk = p7.GetActorsByType("ctnk")
				tnk2 = p7.GetActorsByType("2tnk")
				raarty = p7.GetActorsByType("raarty")
				tnk1 = p7.GetActorsByType("1tnk")
				rajeep = p7.GetActorsByType("rajeep")
				tnk4 = p7.GetActorsByType("4tnk")
				v2rl = p7.GetActorsByType("v2rl")
				tnk3 = p7.GetActorsByType("3tnk")
				ttnk = p7.GetActorsByType("ttnk")
				ftrk = p7.GetActorsByType("ftrk")
				raapc = p7.GetActorsByType("raapc")
				hpt2100 = p7.GetActorsByType("2100hpt")
				apt2100 = p7.GetActorsByType("2100apt")
				mch2100 = p7.GetActorsByType("2100mch")
				tch2100 = p7.GetActorsByType("2100tch")
				rvw2100 = p7.GetActorsByType("2100rvw")
				mvw2100 = p7.GetActorsByType("2100mvw")
				rvw22100 = p7.GetActorsByType("2100rvw2")
				mvw22100 = p7.GetActorsByType("2100mvw2")
				scgoliath = p7.GetActorsByType("scgoliath")
				scvulture = p7.GetActorsByType("scvulture")
				scsiegetank = p7.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p8 and p11.IsAlliedWith(p8) then
				htnk = p8.GetActorsByType("htnk")
				msam = p8.GetActorsByType("msam")
				mtnk = p8.GetActorsByType("mtnk")
				apc = p8.GetActorsByType("apc")
				jeep = p8.GetActorsByType("jeep")
				stnk = p8.GetActorsByType("stnk")
				arty = p8.GetActorsByType("arty")
				ltnk = p8.GetActorsByType("ltnk")
				ftnk = p8.GetActorsByType("ftnk")
				mlrs = p8.GetActorsByType("mlrs")
				bike = p8.GetActorsByType("bike")
				bggy = p8.GetActorsByType("bggy")
				ctnk = p8.GetActorsByType("ctnk")
				tnk2 = p8.GetActorsByType("2tnk")
				raarty = p8.GetActorsByType("raarty")
				tnk1 = p8.GetActorsByType("1tnk")
				rajeep = p8.GetActorsByType("rajeep")
				tnk4 = p8.GetActorsByType("4tnk")
				v2rl = p8.GetActorsByType("v2rl")
				tnk3 = p8.GetActorsByType("3tnk")
				ttnk = p8.GetActorsByType("ttnk")
				ftrk = p8.GetActorsByType("ftrk")
				raapc = p8.GetActorsByType("raapc")
				hpt2100 = p8.GetActorsByType("2100hpt")
				apt2100 = p8.GetActorsByType("2100apt")
				mch2100 = p8.GetActorsByType("2100mch")
				tch2100 = p8.GetActorsByType("2100tch")
				rvw2100 = p8.GetActorsByType("2100rvw")
				mvw2100 = p8.GetActorsByType("2100mvw")
				rvw22100 = p8.GetActorsByType("2100rvw2")
				mvw22100 = p8.GetActorsByType("2100mvw2")
				scgoliath = p8.GetActorsByType("scgoliath")
				scvulture = p8.GetActorsByType("scvulture")
				scsiegetank = p8.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p9 and p11.IsAlliedWith(p9) then
				htnk = p9.GetActorsByType("htnk")
				msam = p9.GetActorsByType("msam")
				mtnk = p9.GetActorsByType("mtnk")
				apc = p9.GetActorsByType("apc")
				jeep = p9.GetActorsByType("jeep")
				stnk = p9.GetActorsByType("stnk")
				arty = p9.GetActorsByType("arty")
				ltnk = p9.GetActorsByType("ltnk")
				ftnk = p9.GetActorsByType("ftnk")
				mlrs = p9.GetActorsByType("mlrs")
				bike = p9.GetActorsByType("bike")
				bggy = p9.GetActorsByType("bggy")
				ctnk = p9.GetActorsByType("ctnk")
				tnk2 = p9.GetActorsByType("2tnk")
				raarty = p9.GetActorsByType("raarty")
				tnk1 = p9.GetActorsByType("1tnk")
				rajeep = p9.GetActorsByType("rajeep")
				tnk4 = p9.GetActorsByType("4tnk")
				v2rl = p9.GetActorsByType("v2rl")
				tnk3 = p9.GetActorsByType("3tnk")
				ttnk = p9.GetActorsByType("ttnk")
				ftrk = p9.GetActorsByType("ftrk")
				raapc = p9.GetActorsByType("raapc")
				hpt2100 = p9.GetActorsByType("2100hpt")
				apt2100 = p9.GetActorsByType("2100apt")
				mch2100 = p9.GetActorsByType("2100mch")
				tch2100 = p9.GetActorsByType("2100tch")
				rvw2100 = p9.GetActorsByType("2100rvw")
				mvw2100 = p9.GetActorsByType("2100mvw")
				rvw22100 = p9.GetActorsByType("2100rvw2")
				mvw22100 = p9.GetActorsByType("2100mvw2")
				scgoliath = p9.GetActorsByType("scgoliath")
				scvulture = p9.GetActorsByType("scvulture")
				scsiegetank = p9.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p10 and p11.IsAlliedWith(p10) then
				htnk = p10.GetActorsByType("htnk")
				msam = p10.GetActorsByType("msam")
				mtnk = p10.GetActorsByType("mtnk")
				apc = p10.GetActorsByType("apc")
				jeep = p10.GetActorsByType("jeep")
				stnk = p10.GetActorsByType("stnk")
				arty = p10.GetActorsByType("arty")
				ltnk = p10.GetActorsByType("ltnk")
				ftnk = p10.GetActorsByType("ftnk")
				mlrs = p10.GetActorsByType("mlrs")
				bike = p10.GetActorsByType("bike")
				bggy = p10.GetActorsByType("bggy")
				ctnk = p10.GetActorsByType("ctnk")
				tnk2 = p10.GetActorsByType("2tnk")
				raarty = p10.GetActorsByType("raarty")
				tnk1 = p10.GetActorsByType("1tnk")
				rajeep = p10.GetActorsByType("rajeep")
				tnk4 = p10.GetActorsByType("4tnk")
				v2rl = p10.GetActorsByType("v2rl")
				tnk3 = p10.GetActorsByType("3tnk")
				ttnk = p10.GetActorsByType("ttnk")
				ftrk = p10.GetActorsByType("ftrk")
				raapc = p10.GetActorsByType("raapc")
				hpt2100 = p10.GetActorsByType("2100hpt")
				apt2100 = p10.GetActorsByType("2100apt")
				mch2100 = p10.GetActorsByType("2100mch")
				tch2100 = p10.GetActorsByType("2100tch")
				rvw2100 = p10.GetActorsByType("2100rvw")
				mvw2100 = p10.GetActorsByType("2100mvw")
				rvw22100 = p10.GetActorsByType("2100rvw2")
				mvw22100 = p10.GetActorsByType("2100mvw2")
				scgoliath = p10.GetActorsByType("scgoliath")
				scvulture = p10.GetActorsByType("scvulture")
				scsiegetank = p10.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p11.GetActorsByType("citypolicecar")
				cityfiretruck = p11.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p11vehicle()
	end)
end
p12vehicle = function()
	if p12 and p12.IsBot then
		cityboteliteexpert = p12.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p13 and p12.IsAlliedWith(p13) then
				htnk = p13.GetActorsByType("htnk")
				msam = p13.GetActorsByType("msam")
				mtnk = p13.GetActorsByType("mtnk")
				apc = p13.GetActorsByType("apc")
				jeep = p13.GetActorsByType("jeep")
				stnk = p13.GetActorsByType("stnk")
				arty = p13.GetActorsByType("arty")
				ltnk = p13.GetActorsByType("ltnk")
				ftnk = p13.GetActorsByType("ftnk")
				mlrs = p13.GetActorsByType("mlrs")
				bike = p13.GetActorsByType("bike")
				bggy = p13.GetActorsByType("bggy")
				ctnk = p13.GetActorsByType("ctnk")
				tnk2 = p13.GetActorsByType("2tnk")
				raarty = p13.GetActorsByType("raarty")
				tnk1 = p13.GetActorsByType("1tnk")
				rajeep = p13.GetActorsByType("rajeep")
				tnk4 = p13.GetActorsByType("4tnk")
				v2rl = p13.GetActorsByType("v2rl")
				tnk3 = p13.GetActorsByType("3tnk")
				ttnk = p13.GetActorsByType("ttnk")
				ftrk = p13.GetActorsByType("ftrk")
				raapc = p13.GetActorsByType("raapc")
				hpt2100 = p13.GetActorsByType("2100hpt")
				apt2100 = p13.GetActorsByType("2100apt")
				mch2100 = p13.GetActorsByType("2100mch")
				tch2100 = p13.GetActorsByType("2100tch")
				rvw2100 = p13.GetActorsByType("2100rvw")
				mvw2100 = p13.GetActorsByType("2100mvw")
				rvw22100 = p13.GetActorsByType("2100rvw2")
				mvw22100 = p13.GetActorsByType("2100mvw2")
				scgoliath = p13.GetActorsByType("scgoliath")
				scvulture = p13.GetActorsByType("scvulture")
				scsiegetank = p13.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p14 and p12.IsAlliedWith(p14) then
				htnk = p14.GetActorsByType("htnk")
				msam = p14.GetActorsByType("msam")
				mtnk = p14.GetActorsByType("mtnk")
				apc = p14.GetActorsByType("apc")
				jeep = p14.GetActorsByType("jeep")
				stnk = p14.GetActorsByType("stnk")
				arty = p14.GetActorsByType("arty")
				ltnk = p14.GetActorsByType("ltnk")
				ftnk = p14.GetActorsByType("ftnk")
				mlrs = p14.GetActorsByType("mlrs")
				bike = p14.GetActorsByType("bike")
				bggy = p14.GetActorsByType("bggy")
				ctnk = p14.GetActorsByType("ctnk")
				tnk2 = p14.GetActorsByType("2tnk")
				raarty = p14.GetActorsByType("raarty")
				tnk1 = p14.GetActorsByType("1tnk")
				rajeep = p14.GetActorsByType("rajeep")
				tnk4 = p14.GetActorsByType("4tnk")
				v2rl = p14.GetActorsByType("v2rl")
				tnk3 = p14.GetActorsByType("3tnk")
				ttnk = p14.GetActorsByType("ttnk")
				ftrk = p14.GetActorsByType("ftrk")
				raapc = p14.GetActorsByType("raapc")
				hpt2100 = p14.GetActorsByType("2100hpt")
				apt2100 = p14.GetActorsByType("2100apt")
				mch2100 = p14.GetActorsByType("2100mch")
				tch2100 = p14.GetActorsByType("2100tch")
				rvw2100 = p14.GetActorsByType("2100rvw")
				mvw2100 = p14.GetActorsByType("2100mvw")
				rvw22100 = p14.GetActorsByType("2100rvw2")
				mvw22100 = p14.GetActorsByType("2100mvw2")
				scgoliath = p14.GetActorsByType("scgoliath")
				scvulture = p14.GetActorsByType("scvulture")
				scsiegetank = p14.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p15 and p12.IsAlliedWith(p15) then
				htnk = p15.GetActorsByType("htnk")
				msam = p15.GetActorsByType("msam")
				mtnk = p15.GetActorsByType("mtnk")
				apc = p15.GetActorsByType("apc")
				jeep = p15.GetActorsByType("jeep")
				stnk = p15.GetActorsByType("stnk")
				arty = p15.GetActorsByType("arty")
				ltnk = p15.GetActorsByType("ltnk")
				ftnk = p15.GetActorsByType("ftnk")
				mlrs = p15.GetActorsByType("mlrs")
				bike = p15.GetActorsByType("bike")
				bggy = p15.GetActorsByType("bggy")
				ctnk = p15.GetActorsByType("ctnk")
				tnk2 = p15.GetActorsByType("2tnk")
				raarty = p15.GetActorsByType("raarty")
				tnk1 = p15.GetActorsByType("1tnk")
				rajeep = p15.GetActorsByType("rajeep")
				tnk4 = p15.GetActorsByType("4tnk")
				v2rl = p15.GetActorsByType("v2rl")
				tnk3 = p15.GetActorsByType("3tnk")
				ttnk = p15.GetActorsByType("ttnk")
				ftrk = p15.GetActorsByType("ftrk")
				raapc = p15.GetActorsByType("raapc")
				hpt2100 = p15.GetActorsByType("2100hpt")
				apt2100 = p15.GetActorsByType("2100apt")
				mch2100 = p15.GetActorsByType("2100mch")
				tch2100 = p15.GetActorsByType("2100tch")
				rvw2100 = p15.GetActorsByType("2100rvw")
				mvw2100 = p15.GetActorsByType("2100mvw")
				rvw22100 = p15.GetActorsByType("2100rvw2")
				mvw22100 = p15.GetActorsByType("2100mvw2")
				scgoliath = p15.GetActorsByType("scgoliath")
				scvulture = p15.GetActorsByType("scvulture")
				scsiegetank = p15.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p16 and p12.IsAlliedWith(p16) then
				htnk = p16.GetActorsByType("htnk")
				msam = p16.GetActorsByType("msam")
				mtnk = p16.GetActorsByType("mtnk")
				apc = p16.GetActorsByType("apc")
				jeep = p16.GetActorsByType("jeep")
				stnk = p16.GetActorsByType("stnk")
				arty = p16.GetActorsByType("arty")
				ltnk = p16.GetActorsByType("ltnk")
				ftnk = p16.GetActorsByType("ftnk")
				mlrs = p16.GetActorsByType("mlrs")
				bike = p16.GetActorsByType("bike")
				bggy = p16.GetActorsByType("bggy")
				ctnk = p16.GetActorsByType("ctnk")
				tnk2 = p16.GetActorsByType("2tnk")
				raarty = p16.GetActorsByType("raarty")
				tnk1 = p16.GetActorsByType("1tnk")
				rajeep = p16.GetActorsByType("rajeep")
				tnk4 = p16.GetActorsByType("4tnk")
				v2rl = p16.GetActorsByType("v2rl")
				tnk3 = p16.GetActorsByType("3tnk")
				ttnk = p16.GetActorsByType("ttnk")
				ftrk = p16.GetActorsByType("ftrk")
				raapc = p16.GetActorsByType("raapc")
				hpt2100 = p16.GetActorsByType("2100hpt")
				apt2100 = p16.GetActorsByType("2100apt")
				mch2100 = p16.GetActorsByType("2100mch")
				tch2100 = p16.GetActorsByType("2100tch")
				rvw2100 = p16.GetActorsByType("2100rvw")
				mvw2100 = p16.GetActorsByType("2100mvw")
				rvw22100 = p16.GetActorsByType("2100rvw2")
				mvw22100 = p16.GetActorsByType("2100mvw2")
				scgoliath = p16.GetActorsByType("scgoliath")
				scvulture = p16.GetActorsByType("scvulture")
				scsiegetank = p16.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p1 and p12.IsAlliedWith(p1) then
				htnk = p1.GetActorsByType("htnk")
				msam = p1.GetActorsByType("msam")
				mtnk = p1.GetActorsByType("mtnk")
				apc = p1.GetActorsByType("apc")
				jeep = p1.GetActorsByType("jeep")
				stnk = p1.GetActorsByType("stnk")
				arty = p1.GetActorsByType("arty")
				ltnk = p1.GetActorsByType("ltnk")
				ftnk = p1.GetActorsByType("ftnk")
				mlrs = p1.GetActorsByType("mlrs")
				bike = p1.GetActorsByType("bike")
				bggy = p1.GetActorsByType("bggy")
				ctnk = p1.GetActorsByType("ctnk")
				tnk2 = p1.GetActorsByType("2tnk")
				raarty = p1.GetActorsByType("raarty")
				tnk1 = p1.GetActorsByType("1tnk")
				rajeep = p1.GetActorsByType("rajeep")
				tnk4 = p1.GetActorsByType("4tnk")
				v2rl = p1.GetActorsByType("v2rl")
				tnk3 = p1.GetActorsByType("3tnk")
				ttnk = p1.GetActorsByType("ttnk")
				ftrk = p1.GetActorsByType("ftrk")
				raapc = p1.GetActorsByType("raapc")
				hpt2100 = p1.GetActorsByType("2100hpt")
				apt2100 = p1.GetActorsByType("2100apt")
				mch2100 = p1.GetActorsByType("2100mch")
				tch2100 = p1.GetActorsByType("2100tch")
				rvw2100 = p1.GetActorsByType("2100rvw")
				mvw2100 = p1.GetActorsByType("2100mvw")
				rvw22100 = p1.GetActorsByType("2100rvw2")
				mvw22100 = p1.GetActorsByType("2100mvw2")
				scgoliath = p1.GetActorsByType("scgoliath")
				scvulture = p1.GetActorsByType("scvulture")
				scsiegetank = p1.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p2 and p12.IsAlliedWith(p2) then
				htnk = p2.GetActorsByType("htnk")
				msam = p2.GetActorsByType("msam")
				mtnk = p2.GetActorsByType("mtnk")
				apc = p2.GetActorsByType("apc")
				jeep = p2.GetActorsByType("jeep")
				stnk = p2.GetActorsByType("stnk")
				arty = p2.GetActorsByType("arty")
				ltnk = p2.GetActorsByType("ltnk")
				ftnk = p2.GetActorsByType("ftnk")
				mlrs = p2.GetActorsByType("mlrs")
				bike = p2.GetActorsByType("bike")
				bggy = p2.GetActorsByType("bggy")
				ctnk = p2.GetActorsByType("ctnk")
				tnk2 = p2.GetActorsByType("2tnk")
				raarty = p2.GetActorsByType("raarty")
				tnk1 = p2.GetActorsByType("1tnk")
				rajeep = p2.GetActorsByType("rajeep")
				tnk4 = p2.GetActorsByType("4tnk")
				v2rl = p2.GetActorsByType("v2rl")
				tnk3 = p2.GetActorsByType("3tnk")
				ttnk = p2.GetActorsByType("ttnk")
				ftrk = p2.GetActorsByType("ftrk")
				raapc = p2.GetActorsByType("raapc")
				hpt2100 = p2.GetActorsByType("2100hpt")
				apt2100 = p2.GetActorsByType("2100apt")
				mch2100 = p2.GetActorsByType("2100mch")
				tch2100 = p2.GetActorsByType("2100tch")
				rvw2100 = p2.GetActorsByType("2100rvw")
				mvw2100 = p2.GetActorsByType("2100mvw")
				rvw22100 = p2.GetActorsByType("2100rvw2")
				mvw22100 = p2.GetActorsByType("2100mvw2")
				scgoliath = p2.GetActorsByType("scgoliath")
				scvulture = p2.GetActorsByType("scvulture")
				scsiegetank = p2.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p3 and p12.IsAlliedWith(p3) then
				htnk = p3.GetActorsByType("htnk")
				msam = p3.GetActorsByType("msam")
				mtnk = p3.GetActorsByType("mtnk")
				apc = p3.GetActorsByType("apc")
				jeep = p3.GetActorsByType("jeep")
				stnk = p3.GetActorsByType("stnk")
				arty = p3.GetActorsByType("arty")
				ltnk = p3.GetActorsByType("ltnk")
				ftnk = p3.GetActorsByType("ftnk")
				mlrs = p3.GetActorsByType("mlrs")
				bike = p3.GetActorsByType("bike")
				bggy = p3.GetActorsByType("bggy")
				ctnk = p3.GetActorsByType("ctnk")
				tnk2 = p3.GetActorsByType("2tnk")
				raarty = p3.GetActorsByType("raarty")
				tnk1 = p3.GetActorsByType("1tnk")
				rajeep = p3.GetActorsByType("rajeep")
				tnk4 = p3.GetActorsByType("4tnk")
				v2rl = p3.GetActorsByType("v2rl")
				tnk3 = p3.GetActorsByType("3tnk")
				ttnk = p3.GetActorsByType("ttnk")
				ftrk = p3.GetActorsByType("ftrk")
				raapc = p3.GetActorsByType("raapc")
				hpt2100 = p3.GetActorsByType("2100hpt")
				apt2100 = p3.GetActorsByType("2100apt")
				mch2100 = p3.GetActorsByType("2100mch")
				tch2100 = p3.GetActorsByType("2100tch")
				rvw2100 = p3.GetActorsByType("2100rvw")
				mvw2100 = p3.GetActorsByType("2100mvw")
				rvw22100 = p3.GetActorsByType("2100rvw2")
				mvw22100 = p3.GetActorsByType("2100mvw2")
				scgoliath = p3.GetActorsByType("scgoliath")
				scvulture = p3.GetActorsByType("scvulture")
				scsiegetank = p3.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p4 and p12.IsAlliedWith(p4) then
				htnk = p4.GetActorsByType("htnk")
				msam = p4.GetActorsByType("msam")
				mtnk = p4.GetActorsByType("mtnk")
				apc = p4.GetActorsByType("apc")
				jeep = p4.GetActorsByType("jeep")
				stnk = p4.GetActorsByType("stnk")
				arty = p4.GetActorsByType("arty")
				ltnk = p4.GetActorsByType("ltnk")
				ftnk = p4.GetActorsByType("ftnk")
				mlrs = p4.GetActorsByType("mlrs")
				bike = p4.GetActorsByType("bike")
				bggy = p4.GetActorsByType("bggy")
				ctnk = p4.GetActorsByType("ctnk")
				tnk2 = p4.GetActorsByType("2tnk")
				raarty = p4.GetActorsByType("raarty")
				tnk1 = p4.GetActorsByType("1tnk")
				rajeep = p4.GetActorsByType("rajeep")
				tnk4 = p4.GetActorsByType("4tnk")
				v2rl = p4.GetActorsByType("v2rl")
				tnk3 = p4.GetActorsByType("3tnk")
				ttnk = p4.GetActorsByType("ttnk")
				ftrk = p4.GetActorsByType("ftrk")
				raapc = p4.GetActorsByType("raapc")
				hpt2100 = p4.GetActorsByType("2100hpt")
				apt2100 = p4.GetActorsByType("2100apt")
				mch2100 = p4.GetActorsByType("2100mch")
				tch2100 = p4.GetActorsByType("2100tch")
				rvw2100 = p4.GetActorsByType("2100rvw")
				mvw2100 = p4.GetActorsByType("2100mvw")
				rvw22100 = p4.GetActorsByType("2100rvw2")
				mvw22100 = p4.GetActorsByType("2100mvw2")
				scgoliath = p4.GetActorsByType("scgoliath")
				scvulture = p4.GetActorsByType("scvulture")
				scsiegetank = p4.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p5 and p12.IsAlliedWith(p5) then
				htnk = p5.GetActorsByType("htnk")
				msam = p5.GetActorsByType("msam")
				mtnk = p5.GetActorsByType("mtnk")
				apc = p5.GetActorsByType("apc")
				jeep = p5.GetActorsByType("jeep")
				stnk = p5.GetActorsByType("stnk")
				arty = p5.GetActorsByType("arty")
				ltnk = p5.GetActorsByType("ltnk")
				ftnk = p5.GetActorsByType("ftnk")
				mlrs = p5.GetActorsByType("mlrs")
				bike = p5.GetActorsByType("bike")
				bggy = p5.GetActorsByType("bggy")
				ctnk = p5.GetActorsByType("ctnk")
				tnk2 = p5.GetActorsByType("2tnk")
				raarty = p5.GetActorsByType("raarty")
				tnk1 = p5.GetActorsByType("1tnk")
				rajeep = p5.GetActorsByType("rajeep")
				tnk4 = p5.GetActorsByType("4tnk")
				v2rl = p5.GetActorsByType("v2rl")
				tnk3 = p5.GetActorsByType("3tnk")
				ttnk = p5.GetActorsByType("ttnk")
				ftrk = p5.GetActorsByType("ftrk")
				raapc = p5.GetActorsByType("raapc")
				hpt2100 = p5.GetActorsByType("2100hpt")
				apt2100 = p5.GetActorsByType("2100apt")
				mch2100 = p5.GetActorsByType("2100mch")
				tch2100 = p5.GetActorsByType("2100tch")
				rvw2100 = p5.GetActorsByType("2100rvw")
				mvw2100 = p5.GetActorsByType("2100mvw")
				rvw22100 = p5.GetActorsByType("2100rvw2")
				mvw22100 = p5.GetActorsByType("2100mvw2")
				scgoliath = p5.GetActorsByType("scgoliath")
				scvulture = p5.GetActorsByType("scvulture")
				scsiegetank = p5.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p6 and p12.IsAlliedWith(p6) then
				htnk = p6.GetActorsByType("htnk")
				msam = p6.GetActorsByType("msam")
				mtnk = p6.GetActorsByType("mtnk")
				apc = p6.GetActorsByType("apc")
				jeep = p6.GetActorsByType("jeep")
				stnk = p6.GetActorsByType("stnk")
				arty = p6.GetActorsByType("arty")
				ltnk = p6.GetActorsByType("ltnk")
				ftnk = p6.GetActorsByType("ftnk")
				mlrs = p6.GetActorsByType("mlrs")
				bike = p6.GetActorsByType("bike")
				bggy = p6.GetActorsByType("bggy")
				ctnk = p6.GetActorsByType("ctnk")
				tnk2 = p6.GetActorsByType("2tnk")
				raarty = p6.GetActorsByType("raarty")
				tnk1 = p6.GetActorsByType("1tnk")
				rajeep = p6.GetActorsByType("rajeep")
				tnk4 = p6.GetActorsByType("4tnk")
				v2rl = p6.GetActorsByType("v2rl")
				tnk3 = p6.GetActorsByType("3tnk")
				ttnk = p6.GetActorsByType("ttnk")
				ftrk = p6.GetActorsByType("ftrk")
				raapc = p6.GetActorsByType("raapc")
				hpt2100 = p6.GetActorsByType("2100hpt")
				apt2100 = p6.GetActorsByType("2100apt")
				mch2100 = p6.GetActorsByType("2100mch")
				tch2100 = p6.GetActorsByType("2100tch")
				rvw2100 = p6.GetActorsByType("2100rvw")
				mvw2100 = p6.GetActorsByType("2100mvw")
				rvw22100 = p6.GetActorsByType("2100rvw2")
				mvw22100 = p6.GetActorsByType("2100mvw2")
				scgoliath = p6.GetActorsByType("scgoliath")
				scvulture = p6.GetActorsByType("scvulture")
				scsiegetank = p6.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p7 and p12.IsAlliedWith(p7) then
				htnk = p7.GetActorsByType("htnk")
				msam = p7.GetActorsByType("msam")
				mtnk = p7.GetActorsByType("mtnk")
				apc = p7.GetActorsByType("apc")
				jeep = p7.GetActorsByType("jeep")
				stnk = p7.GetActorsByType("stnk")
				arty = p7.GetActorsByType("arty")
				ltnk = p7.GetActorsByType("ltnk")
				ftnk = p7.GetActorsByType("ftnk")
				mlrs = p7.GetActorsByType("mlrs")
				bike = p7.GetActorsByType("bike")
				bggy = p7.GetActorsByType("bggy")
				ctnk = p7.GetActorsByType("ctnk")
				tnk2 = p7.GetActorsByType("2tnk")
				raarty = p7.GetActorsByType("raarty")
				tnk1 = p7.GetActorsByType("1tnk")
				rajeep = p7.GetActorsByType("rajeep")
				tnk4 = p7.GetActorsByType("4tnk")
				v2rl = p7.GetActorsByType("v2rl")
				tnk3 = p7.GetActorsByType("3tnk")
				ttnk = p7.GetActorsByType("ttnk")
				ftrk = p7.GetActorsByType("ftrk")
				raapc = p7.GetActorsByType("raapc")
				hpt2100 = p7.GetActorsByType("2100hpt")
				apt2100 = p7.GetActorsByType("2100apt")
				mch2100 = p7.GetActorsByType("2100mch")
				tch2100 = p7.GetActorsByType("2100tch")
				rvw2100 = p7.GetActorsByType("2100rvw")
				mvw2100 = p7.GetActorsByType("2100mvw")
				rvw22100 = p7.GetActorsByType("2100rvw2")
				mvw22100 = p7.GetActorsByType("2100mvw2")
				scgoliath = p7.GetActorsByType("scgoliath")
				scvulture = p7.GetActorsByType("scvulture")
				scsiegetank = p7.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p8 and p12.IsAlliedWith(p8) then
				htnk = p8.GetActorsByType("htnk")
				msam = p8.GetActorsByType("msam")
				mtnk = p8.GetActorsByType("mtnk")
				apc = p8.GetActorsByType("apc")
				jeep = p8.GetActorsByType("jeep")
				stnk = p8.GetActorsByType("stnk")
				arty = p8.GetActorsByType("arty")
				ltnk = p8.GetActorsByType("ltnk")
				ftnk = p8.GetActorsByType("ftnk")
				mlrs = p8.GetActorsByType("mlrs")
				bike = p8.GetActorsByType("bike")
				bggy = p8.GetActorsByType("bggy")
				ctnk = p8.GetActorsByType("ctnk")
				tnk2 = p8.GetActorsByType("2tnk")
				raarty = p8.GetActorsByType("raarty")
				tnk1 = p8.GetActorsByType("1tnk")
				rajeep = p8.GetActorsByType("rajeep")
				tnk4 = p8.GetActorsByType("4tnk")
				v2rl = p8.GetActorsByType("v2rl")
				tnk3 = p8.GetActorsByType("3tnk")
				ttnk = p8.GetActorsByType("ttnk")
				ftrk = p8.GetActorsByType("ftrk")
				raapc = p8.GetActorsByType("raapc")
				hpt2100 = p8.GetActorsByType("2100hpt")
				apt2100 = p8.GetActorsByType("2100apt")
				mch2100 = p8.GetActorsByType("2100mch")
				tch2100 = p8.GetActorsByType("2100tch")
				rvw2100 = p8.GetActorsByType("2100rvw")
				mvw2100 = p8.GetActorsByType("2100mvw")
				rvw22100 = p8.GetActorsByType("2100rvw2")
				mvw22100 = p8.GetActorsByType("2100mvw2")
				scgoliath = p8.GetActorsByType("scgoliath")
				scvulture = p8.GetActorsByType("scvulture")
				scsiegetank = p8.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p9 and p12.IsAlliedWith(p9) then
				htnk = p9.GetActorsByType("htnk")
				msam = p9.GetActorsByType("msam")
				mtnk = p9.GetActorsByType("mtnk")
				apc = p9.GetActorsByType("apc")
				jeep = p9.GetActorsByType("jeep")
				stnk = p9.GetActorsByType("stnk")
				arty = p9.GetActorsByType("arty")
				ltnk = p9.GetActorsByType("ltnk")
				ftnk = p9.GetActorsByType("ftnk")
				mlrs = p9.GetActorsByType("mlrs")
				bike = p9.GetActorsByType("bike")
				bggy = p9.GetActorsByType("bggy")
				ctnk = p9.GetActorsByType("ctnk")
				tnk2 = p9.GetActorsByType("2tnk")
				raarty = p9.GetActorsByType("raarty")
				tnk1 = p9.GetActorsByType("1tnk")
				rajeep = p9.GetActorsByType("rajeep")
				tnk4 = p9.GetActorsByType("4tnk")
				v2rl = p9.GetActorsByType("v2rl")
				tnk3 = p9.GetActorsByType("3tnk")
				ttnk = p9.GetActorsByType("ttnk")
				ftrk = p9.GetActorsByType("ftrk")
				raapc = p9.GetActorsByType("raapc")
				hpt2100 = p9.GetActorsByType("2100hpt")
				apt2100 = p9.GetActorsByType("2100apt")
				mch2100 = p9.GetActorsByType("2100mch")
				tch2100 = p9.GetActorsByType("2100tch")
				rvw2100 = p9.GetActorsByType("2100rvw")
				mvw2100 = p9.GetActorsByType("2100mvw")
				rvw22100 = p9.GetActorsByType("2100rvw2")
				mvw22100 = p9.GetActorsByType("2100mvw2")
				scgoliath = p9.GetActorsByType("scgoliath")
				scvulture = p9.GetActorsByType("scvulture")
				scsiegetank = p9.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p10 and p12.IsAlliedWith(p10) then
				htnk = p10.GetActorsByType("htnk")
				msam = p10.GetActorsByType("msam")
				mtnk = p10.GetActorsByType("mtnk")
				apc = p10.GetActorsByType("apc")
				jeep = p10.GetActorsByType("jeep")
				stnk = p10.GetActorsByType("stnk")
				arty = p10.GetActorsByType("arty")
				ltnk = p10.GetActorsByType("ltnk")
				ftnk = p10.GetActorsByType("ftnk")
				mlrs = p10.GetActorsByType("mlrs")
				bike = p10.GetActorsByType("bike")
				bggy = p10.GetActorsByType("bggy")
				ctnk = p10.GetActorsByType("ctnk")
				tnk2 = p10.GetActorsByType("2tnk")
				raarty = p10.GetActorsByType("raarty")
				tnk1 = p10.GetActorsByType("1tnk")
				rajeep = p10.GetActorsByType("rajeep")
				tnk4 = p10.GetActorsByType("4tnk")
				v2rl = p10.GetActorsByType("v2rl")
				tnk3 = p10.GetActorsByType("3tnk")
				ttnk = p10.GetActorsByType("ttnk")
				ftrk = p10.GetActorsByType("ftrk")
				raapc = p10.GetActorsByType("raapc")
				hpt2100 = p10.GetActorsByType("2100hpt")
				apt2100 = p10.GetActorsByType("2100apt")
				mch2100 = p10.GetActorsByType("2100mch")
				tch2100 = p10.GetActorsByType("2100tch")
				rvw2100 = p10.GetActorsByType("2100rvw")
				mvw2100 = p10.GetActorsByType("2100mvw")
				rvw22100 = p10.GetActorsByType("2100rvw2")
				mvw22100 = p10.GetActorsByType("2100mvw2")
				scgoliath = p10.GetActorsByType("scgoliath")
				scvulture = p10.GetActorsByType("scvulture")
				scsiegetank = p10.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p11 and p12.IsAlliedWith(p11) then
				htnk = p11.GetActorsByType("htnk")
				msam = p11.GetActorsByType("msam")
				mtnk = p11.GetActorsByType("mtnk")
				apc = p11.GetActorsByType("apc")
				jeep = p11.GetActorsByType("jeep")
				stnk = p11.GetActorsByType("stnk")
				arty = p11.GetActorsByType("arty")
				ltnk = p11.GetActorsByType("ltnk")
				ftnk = p11.GetActorsByType("ftnk")
				mlrs = p11.GetActorsByType("mlrs")
				bike = p11.GetActorsByType("bike")
				bggy = p11.GetActorsByType("bggy")
				ctnk = p11.GetActorsByType("ctnk")
				tnk2 = p11.GetActorsByType("2tnk")
				raarty = p11.GetActorsByType("raarty")
				tnk1 = p11.GetActorsByType("1tnk")
				rajeep = p11.GetActorsByType("rajeep")
				tnk4 = p11.GetActorsByType("4tnk")
				v2rl = p11.GetActorsByType("v2rl")
				tnk3 = p11.GetActorsByType("3tnk")
				ttnk = p11.GetActorsByType("ttnk")
				ftrk = p11.GetActorsByType("ftrk")
				raapc = p11.GetActorsByType("raapc")
				hpt2100 = p11.GetActorsByType("2100hpt")
				apt2100 = p11.GetActorsByType("2100apt")
				mch2100 = p11.GetActorsByType("2100mch")
				tch2100 = p11.GetActorsByType("2100tch")
				rvw2100 = p11.GetActorsByType("2100rvw")
				mvw2100 = p11.GetActorsByType("2100mvw")
				rvw22100 = p11.GetActorsByType("2100rvw2")
				mvw22100 = p11.GetActorsByType("2100mvw2")
				scgoliath = p11.GetActorsByType("scgoliath")
				scvulture = p11.GetActorsByType("scvulture")
				scsiegetank = p11.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p12.GetActorsByType("citypolicecar")
				cityfiretruck = p12.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p12vehicle()
	end)
end
p13vehicle = function()
	if p13 and p13.IsBot then
		cityboteliteexpert = p13.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p14 and p13.IsAlliedWith(p14) then
				htnk = p14.GetActorsByType("htnk")
				msam = p14.GetActorsByType("msam")
				mtnk = p14.GetActorsByType("mtnk")
				apc = p14.GetActorsByType("apc")
				jeep = p14.GetActorsByType("jeep")
				stnk = p14.GetActorsByType("stnk")
				arty = p14.GetActorsByType("arty")
				ltnk = p14.GetActorsByType("ltnk")
				ftnk = p14.GetActorsByType("ftnk")
				mlrs = p14.GetActorsByType("mlrs")
				bike = p14.GetActorsByType("bike")
				bggy = p14.GetActorsByType("bggy")
				ctnk = p14.GetActorsByType("ctnk")
				tnk2 = p14.GetActorsByType("2tnk")
				raarty = p14.GetActorsByType("raarty")
				tnk1 = p14.GetActorsByType("1tnk")
				rajeep = p14.GetActorsByType("rajeep")
				tnk4 = p14.GetActorsByType("4tnk")
				v2rl = p14.GetActorsByType("v2rl")
				tnk3 = p14.GetActorsByType("3tnk")
				ttnk = p14.GetActorsByType("ttnk")
				ftrk = p14.GetActorsByType("ftrk")
				raapc = p14.GetActorsByType("raapc")
				hpt2100 = p14.GetActorsByType("2100hpt")
				apt2100 = p14.GetActorsByType("2100apt")
				mch2100 = p14.GetActorsByType("2100mch")
				tch2100 = p14.GetActorsByType("2100tch")
				rvw2100 = p14.GetActorsByType("2100rvw")
				mvw2100 = p14.GetActorsByType("2100mvw")
				rvw22100 = p14.GetActorsByType("2100rvw2")
				mvw22100 = p14.GetActorsByType("2100mvw2")
				scgoliath = p14.GetActorsByType("scgoliath")
				scvulture = p14.GetActorsByType("scvulture")
				scsiegetank = p14.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p15 and p13.IsAlliedWith(p15) then
				htnk = p15.GetActorsByType("htnk")
				msam = p15.GetActorsByType("msam")
				mtnk = p15.GetActorsByType("mtnk")
				apc = p15.GetActorsByType("apc")
				jeep = p15.GetActorsByType("jeep")
				stnk = p15.GetActorsByType("stnk")
				arty = p15.GetActorsByType("arty")
				ltnk = p15.GetActorsByType("ltnk")
				ftnk = p15.GetActorsByType("ftnk")
				mlrs = p15.GetActorsByType("mlrs")
				bike = p15.GetActorsByType("bike")
				bggy = p15.GetActorsByType("bggy")
				ctnk = p15.GetActorsByType("ctnk")
				tnk2 = p15.GetActorsByType("2tnk")
				raarty = p15.GetActorsByType("raarty")
				tnk1 = p15.GetActorsByType("1tnk")
				rajeep = p15.GetActorsByType("rajeep")
				tnk4 = p15.GetActorsByType("4tnk")
				v2rl = p15.GetActorsByType("v2rl")
				tnk3 = p15.GetActorsByType("3tnk")
				ttnk = p15.GetActorsByType("ttnk")
				ftrk = p15.GetActorsByType("ftrk")
				raapc = p15.GetActorsByType("raapc")
				hpt2100 = p15.GetActorsByType("2100hpt")
				apt2100 = p15.GetActorsByType("2100apt")
				mch2100 = p15.GetActorsByType("2100mch")
				tch2100 = p15.GetActorsByType("2100tch")
				rvw2100 = p15.GetActorsByType("2100rvw")
				mvw2100 = p15.GetActorsByType("2100mvw")
				rvw22100 = p15.GetActorsByType("2100rvw2")
				mvw22100 = p15.GetActorsByType("2100mvw2")
				scgoliath = p15.GetActorsByType("scgoliath")
				scvulture = p15.GetActorsByType("scvulture")
				scsiegetank = p15.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p16 and p13.IsAlliedWith(p16) then
				htnk = p16.GetActorsByType("htnk")
				msam = p16.GetActorsByType("msam")
				mtnk = p16.GetActorsByType("mtnk")
				apc = p16.GetActorsByType("apc")
				jeep = p16.GetActorsByType("jeep")
				stnk = p16.GetActorsByType("stnk")
				arty = p16.GetActorsByType("arty")
				ltnk = p16.GetActorsByType("ltnk")
				ftnk = p16.GetActorsByType("ftnk")
				mlrs = p16.GetActorsByType("mlrs")
				bike = p16.GetActorsByType("bike")
				bggy = p16.GetActorsByType("bggy")
				ctnk = p16.GetActorsByType("ctnk")
				tnk2 = p16.GetActorsByType("2tnk")
				raarty = p16.GetActorsByType("raarty")
				tnk1 = p16.GetActorsByType("1tnk")
				rajeep = p16.GetActorsByType("rajeep")
				tnk4 = p16.GetActorsByType("4tnk")
				v2rl = p16.GetActorsByType("v2rl")
				tnk3 = p16.GetActorsByType("3tnk")
				ttnk = p16.GetActorsByType("ttnk")
				ftrk = p16.GetActorsByType("ftrk")
				raapc = p16.GetActorsByType("raapc")
				hpt2100 = p16.GetActorsByType("2100hpt")
				apt2100 = p16.GetActorsByType("2100apt")
				mch2100 = p16.GetActorsByType("2100mch")
				tch2100 = p16.GetActorsByType("2100tch")
				rvw2100 = p16.GetActorsByType("2100rvw")
				mvw2100 = p16.GetActorsByType("2100mvw")
				rvw22100 = p16.GetActorsByType("2100rvw2")
				mvw22100 = p16.GetActorsByType("2100mvw2")
				scgoliath = p16.GetActorsByType("scgoliath")
				scvulture = p16.GetActorsByType("scvulture")
				scsiegetank = p16.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p1 and p13.IsAlliedWith(p1) then
				htnk = p1.GetActorsByType("htnk")
				msam = p1.GetActorsByType("msam")
				mtnk = p1.GetActorsByType("mtnk")
				apc = p1.GetActorsByType("apc")
				jeep = p1.GetActorsByType("jeep")
				stnk = p1.GetActorsByType("stnk")
				arty = p1.GetActorsByType("arty")
				ltnk = p1.GetActorsByType("ltnk")
				ftnk = p1.GetActorsByType("ftnk")
				mlrs = p1.GetActorsByType("mlrs")
				bike = p1.GetActorsByType("bike")
				bggy = p1.GetActorsByType("bggy")
				ctnk = p1.GetActorsByType("ctnk")
				tnk2 = p1.GetActorsByType("2tnk")
				raarty = p1.GetActorsByType("raarty")
				tnk1 = p1.GetActorsByType("1tnk")
				rajeep = p1.GetActorsByType("rajeep")
				tnk4 = p1.GetActorsByType("4tnk")
				v2rl = p1.GetActorsByType("v2rl")
				tnk3 = p1.GetActorsByType("3tnk")
				ttnk = p1.GetActorsByType("ttnk")
				ftrk = p1.GetActorsByType("ftrk")
				raapc = p1.GetActorsByType("raapc")
				hpt2100 = p1.GetActorsByType("2100hpt")
				apt2100 = p1.GetActorsByType("2100apt")
				mch2100 = p1.GetActorsByType("2100mch")
				tch2100 = p1.GetActorsByType("2100tch")
				rvw2100 = p1.GetActorsByType("2100rvw")
				mvw2100 = p1.GetActorsByType("2100mvw")
				rvw22100 = p1.GetActorsByType("2100rvw2")
				mvw22100 = p1.GetActorsByType("2100mvw2")
				scgoliath = p1.GetActorsByType("scgoliath")
				scvulture = p1.GetActorsByType("scvulture")
				scsiegetank = p1.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p2 and p13.IsAlliedWith(p2) then
				htnk = p2.GetActorsByType("htnk")
				msam = p2.GetActorsByType("msam")
				mtnk = p2.GetActorsByType("mtnk")
				apc = p2.GetActorsByType("apc")
				jeep = p2.GetActorsByType("jeep")
				stnk = p2.GetActorsByType("stnk")
				arty = p2.GetActorsByType("arty")
				ltnk = p2.GetActorsByType("ltnk")
				ftnk = p2.GetActorsByType("ftnk")
				mlrs = p2.GetActorsByType("mlrs")
				bike = p2.GetActorsByType("bike")
				bggy = p2.GetActorsByType("bggy")
				ctnk = p2.GetActorsByType("ctnk")
				tnk2 = p2.GetActorsByType("2tnk")
				raarty = p2.GetActorsByType("raarty")
				tnk1 = p2.GetActorsByType("1tnk")
				rajeep = p2.GetActorsByType("rajeep")
				tnk4 = p2.GetActorsByType("4tnk")
				v2rl = p2.GetActorsByType("v2rl")
				tnk3 = p2.GetActorsByType("3tnk")
				ttnk = p2.GetActorsByType("ttnk")
				ftrk = p2.GetActorsByType("ftrk")
				raapc = p2.GetActorsByType("raapc")
				hpt2100 = p2.GetActorsByType("2100hpt")
				apt2100 = p2.GetActorsByType("2100apt")
				mch2100 = p2.GetActorsByType("2100mch")
				tch2100 = p2.GetActorsByType("2100tch")
				rvw2100 = p2.GetActorsByType("2100rvw")
				mvw2100 = p2.GetActorsByType("2100mvw")
				rvw22100 = p2.GetActorsByType("2100rvw2")
				mvw22100 = p2.GetActorsByType("2100mvw2")
				scgoliath = p2.GetActorsByType("scgoliath")
				scvulture = p2.GetActorsByType("scvulture")
				scsiegetank = p2.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p3 and p13.IsAlliedWith(p3) then
				htnk = p3.GetActorsByType("htnk")
				msam = p3.GetActorsByType("msam")
				mtnk = p3.GetActorsByType("mtnk")
				apc = p3.GetActorsByType("apc")
				jeep = p3.GetActorsByType("jeep")
				stnk = p3.GetActorsByType("stnk")
				arty = p3.GetActorsByType("arty")
				ltnk = p3.GetActorsByType("ltnk")
				ftnk = p3.GetActorsByType("ftnk")
				mlrs = p3.GetActorsByType("mlrs")
				bike = p3.GetActorsByType("bike")
				bggy = p3.GetActorsByType("bggy")
				ctnk = p3.GetActorsByType("ctnk")
				tnk2 = p3.GetActorsByType("2tnk")
				raarty = p3.GetActorsByType("raarty")
				tnk1 = p3.GetActorsByType("1tnk")
				rajeep = p3.GetActorsByType("rajeep")
				tnk4 = p3.GetActorsByType("4tnk")
				v2rl = p3.GetActorsByType("v2rl")
				tnk3 = p3.GetActorsByType("3tnk")
				ttnk = p3.GetActorsByType("ttnk")
				ftrk = p3.GetActorsByType("ftrk")
				raapc = p3.GetActorsByType("raapc")
				hpt2100 = p3.GetActorsByType("2100hpt")
				apt2100 = p3.GetActorsByType("2100apt")
				mch2100 = p3.GetActorsByType("2100mch")
				tch2100 = p3.GetActorsByType("2100tch")
				rvw2100 = p3.GetActorsByType("2100rvw")
				mvw2100 = p3.GetActorsByType("2100mvw")
				rvw22100 = p3.GetActorsByType("2100rvw2")
				mvw22100 = p3.GetActorsByType("2100mvw2")
				scgoliath = p3.GetActorsByType("scgoliath")
				scvulture = p3.GetActorsByType("scvulture")
				scsiegetank = p3.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p4 and p13.IsAlliedWith(p4) then
				htnk = p4.GetActorsByType("htnk")
				msam = p4.GetActorsByType("msam")
				mtnk = p4.GetActorsByType("mtnk")
				apc = p4.GetActorsByType("apc")
				jeep = p4.GetActorsByType("jeep")
				stnk = p4.GetActorsByType("stnk")
				arty = p4.GetActorsByType("arty")
				ltnk = p4.GetActorsByType("ltnk")
				ftnk = p4.GetActorsByType("ftnk")
				mlrs = p4.GetActorsByType("mlrs")
				bike = p4.GetActorsByType("bike")
				bggy = p4.GetActorsByType("bggy")
				ctnk = p4.GetActorsByType("ctnk")
				tnk2 = p4.GetActorsByType("2tnk")
				raarty = p4.GetActorsByType("raarty")
				tnk1 = p4.GetActorsByType("1tnk")
				rajeep = p4.GetActorsByType("rajeep")
				tnk4 = p4.GetActorsByType("4tnk")
				v2rl = p4.GetActorsByType("v2rl")
				tnk3 = p4.GetActorsByType("3tnk")
				ttnk = p4.GetActorsByType("ttnk")
				ftrk = p4.GetActorsByType("ftrk")
				raapc = p4.GetActorsByType("raapc")
				hpt2100 = p4.GetActorsByType("2100hpt")
				apt2100 = p4.GetActorsByType("2100apt")
				mch2100 = p4.GetActorsByType("2100mch")
				tch2100 = p4.GetActorsByType("2100tch")
				rvw2100 = p4.GetActorsByType("2100rvw")
				mvw2100 = p4.GetActorsByType("2100mvw")
				rvw22100 = p4.GetActorsByType("2100rvw2")
				mvw22100 = p4.GetActorsByType("2100mvw2")
				scgoliath = p4.GetActorsByType("scgoliath")
				scvulture = p4.GetActorsByType("scvulture")
				scsiegetank = p4.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p5 and p13.IsAlliedWith(p5) then
				htnk = p5.GetActorsByType("htnk")
				msam = p5.GetActorsByType("msam")
				mtnk = p5.GetActorsByType("mtnk")
				apc = p5.GetActorsByType("apc")
				jeep = p5.GetActorsByType("jeep")
				stnk = p5.GetActorsByType("stnk")
				arty = p5.GetActorsByType("arty")
				ltnk = p5.GetActorsByType("ltnk")
				ftnk = p5.GetActorsByType("ftnk")
				mlrs = p5.GetActorsByType("mlrs")
				bike = p5.GetActorsByType("bike")
				bggy = p5.GetActorsByType("bggy")
				ctnk = p5.GetActorsByType("ctnk")
				tnk2 = p5.GetActorsByType("2tnk")
				raarty = p5.GetActorsByType("raarty")
				tnk1 = p5.GetActorsByType("1tnk")
				rajeep = p5.GetActorsByType("rajeep")
				tnk4 = p5.GetActorsByType("4tnk")
				v2rl = p5.GetActorsByType("v2rl")
				tnk3 = p5.GetActorsByType("3tnk")
				ttnk = p5.GetActorsByType("ttnk")
				ftrk = p5.GetActorsByType("ftrk")
				raapc = p5.GetActorsByType("raapc")
				hpt2100 = p5.GetActorsByType("2100hpt")
				apt2100 = p5.GetActorsByType("2100apt")
				mch2100 = p5.GetActorsByType("2100mch")
				tch2100 = p5.GetActorsByType("2100tch")
				rvw2100 = p5.GetActorsByType("2100rvw")
				mvw2100 = p5.GetActorsByType("2100mvw")
				rvw22100 = p5.GetActorsByType("2100rvw2")
				mvw22100 = p5.GetActorsByType("2100mvw2")
				scgoliath = p5.GetActorsByType("scgoliath")
				scvulture = p5.GetActorsByType("scvulture")
				scsiegetank = p5.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p6 and p13.IsAlliedWith(p6) then
				htnk = p6.GetActorsByType("htnk")
				msam = p6.GetActorsByType("msam")
				mtnk = p6.GetActorsByType("mtnk")
				apc = p6.GetActorsByType("apc")
				jeep = p6.GetActorsByType("jeep")
				stnk = p6.GetActorsByType("stnk")
				arty = p6.GetActorsByType("arty")
				ltnk = p6.GetActorsByType("ltnk")
				ftnk = p6.GetActorsByType("ftnk")
				mlrs = p6.GetActorsByType("mlrs")
				bike = p6.GetActorsByType("bike")
				bggy = p6.GetActorsByType("bggy")
				ctnk = p6.GetActorsByType("ctnk")
				tnk2 = p6.GetActorsByType("2tnk")
				raarty = p6.GetActorsByType("raarty")
				tnk1 = p6.GetActorsByType("1tnk")
				rajeep = p6.GetActorsByType("rajeep")
				tnk4 = p6.GetActorsByType("4tnk")
				v2rl = p6.GetActorsByType("v2rl")
				tnk3 = p6.GetActorsByType("3tnk")
				ttnk = p6.GetActorsByType("ttnk")
				ftrk = p6.GetActorsByType("ftrk")
				raapc = p6.GetActorsByType("raapc")
				hpt2100 = p6.GetActorsByType("2100hpt")
				apt2100 = p6.GetActorsByType("2100apt")
				mch2100 = p6.GetActorsByType("2100mch")
				tch2100 = p6.GetActorsByType("2100tch")
				rvw2100 = p6.GetActorsByType("2100rvw")
				mvw2100 = p6.GetActorsByType("2100mvw")
				rvw22100 = p6.GetActorsByType("2100rvw2")
				mvw22100 = p6.GetActorsByType("2100mvw2")
				scgoliath = p6.GetActorsByType("scgoliath")
				scvulture = p6.GetActorsByType("scvulture")
				scsiegetank = p6.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p7 and p13.IsAlliedWith(p7) then
				htnk = p7.GetActorsByType("htnk")
				msam = p7.GetActorsByType("msam")
				mtnk = p7.GetActorsByType("mtnk")
				apc = p7.GetActorsByType("apc")
				jeep = p7.GetActorsByType("jeep")
				stnk = p7.GetActorsByType("stnk")
				arty = p7.GetActorsByType("arty")
				ltnk = p7.GetActorsByType("ltnk")
				ftnk = p7.GetActorsByType("ftnk")
				mlrs = p7.GetActorsByType("mlrs")
				bike = p7.GetActorsByType("bike")
				bggy = p7.GetActorsByType("bggy")
				ctnk = p7.GetActorsByType("ctnk")
				tnk2 = p7.GetActorsByType("2tnk")
				raarty = p7.GetActorsByType("raarty")
				tnk1 = p7.GetActorsByType("1tnk")
				rajeep = p7.GetActorsByType("rajeep")
				tnk4 = p7.GetActorsByType("4tnk")
				v2rl = p7.GetActorsByType("v2rl")
				tnk3 = p7.GetActorsByType("3tnk")
				ttnk = p7.GetActorsByType("ttnk")
				ftrk = p7.GetActorsByType("ftrk")
				raapc = p7.GetActorsByType("raapc")
				hpt2100 = p7.GetActorsByType("2100hpt")
				apt2100 = p7.GetActorsByType("2100apt")
				mch2100 = p7.GetActorsByType("2100mch")
				tch2100 = p7.GetActorsByType("2100tch")
				rvw2100 = p7.GetActorsByType("2100rvw")
				mvw2100 = p7.GetActorsByType("2100mvw")
				rvw22100 = p7.GetActorsByType("2100rvw2")
				mvw22100 = p7.GetActorsByType("2100mvw2")
				scgoliath = p7.GetActorsByType("scgoliath")
				scvulture = p7.GetActorsByType("scvulture")
				scsiegetank = p7.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p8 and p13.IsAlliedWith(p8) then
				htnk = p8.GetActorsByType("htnk")
				msam = p8.GetActorsByType("msam")
				mtnk = p8.GetActorsByType("mtnk")
				apc = p8.GetActorsByType("apc")
				jeep = p8.GetActorsByType("jeep")
				stnk = p8.GetActorsByType("stnk")
				arty = p8.GetActorsByType("arty")
				ltnk = p8.GetActorsByType("ltnk")
				ftnk = p8.GetActorsByType("ftnk")
				mlrs = p8.GetActorsByType("mlrs")
				bike = p8.GetActorsByType("bike")
				bggy = p8.GetActorsByType("bggy")
				ctnk = p8.GetActorsByType("ctnk")
				tnk2 = p8.GetActorsByType("2tnk")
				raarty = p8.GetActorsByType("raarty")
				tnk1 = p8.GetActorsByType("1tnk")
				rajeep = p8.GetActorsByType("rajeep")
				tnk4 = p8.GetActorsByType("4tnk")
				v2rl = p8.GetActorsByType("v2rl")
				tnk3 = p8.GetActorsByType("3tnk")
				ttnk = p8.GetActorsByType("ttnk")
				ftrk = p8.GetActorsByType("ftrk")
				raapc = p8.GetActorsByType("raapc")
				hpt2100 = p8.GetActorsByType("2100hpt")
				apt2100 = p8.GetActorsByType("2100apt")
				mch2100 = p8.GetActorsByType("2100mch")
				tch2100 = p8.GetActorsByType("2100tch")
				rvw2100 = p8.GetActorsByType("2100rvw")
				mvw2100 = p8.GetActorsByType("2100mvw")
				rvw22100 = p8.GetActorsByType("2100rvw2")
				mvw22100 = p8.GetActorsByType("2100mvw2")
				scgoliath = p8.GetActorsByType("scgoliath")
				scvulture = p8.GetActorsByType("scvulture")
				scsiegetank = p8.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p9 and p13.IsAlliedWith(p9) then
				htnk = p9.GetActorsByType("htnk")
				msam = p9.GetActorsByType("msam")
				mtnk = p9.GetActorsByType("mtnk")
				apc = p9.GetActorsByType("apc")
				jeep = p9.GetActorsByType("jeep")
				stnk = p9.GetActorsByType("stnk")
				arty = p9.GetActorsByType("arty")
				ltnk = p9.GetActorsByType("ltnk")
				ftnk = p9.GetActorsByType("ftnk")
				mlrs = p9.GetActorsByType("mlrs")
				bike = p9.GetActorsByType("bike")
				bggy = p9.GetActorsByType("bggy")
				ctnk = p9.GetActorsByType("ctnk")
				tnk2 = p9.GetActorsByType("2tnk")
				raarty = p9.GetActorsByType("raarty")
				tnk1 = p9.GetActorsByType("1tnk")
				rajeep = p9.GetActorsByType("rajeep")
				tnk4 = p9.GetActorsByType("4tnk")
				v2rl = p9.GetActorsByType("v2rl")
				tnk3 = p9.GetActorsByType("3tnk")
				ttnk = p9.GetActorsByType("ttnk")
				ftrk = p9.GetActorsByType("ftrk")
				raapc = p9.GetActorsByType("raapc")
				hpt2100 = p9.GetActorsByType("2100hpt")
				apt2100 = p9.GetActorsByType("2100apt")
				mch2100 = p9.GetActorsByType("2100mch")
				tch2100 = p9.GetActorsByType("2100tch")
				rvw2100 = p9.GetActorsByType("2100rvw")
				mvw2100 = p9.GetActorsByType("2100mvw")
				rvw22100 = p9.GetActorsByType("2100rvw2")
				mvw22100 = p9.GetActorsByType("2100mvw2")
				scgoliath = p9.GetActorsByType("scgoliath")
				scvulture = p9.GetActorsByType("scvulture")
				scsiegetank = p9.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p10 and p13.IsAlliedWith(p10) then
				htnk = p10.GetActorsByType("htnk")
				msam = p10.GetActorsByType("msam")
				mtnk = p10.GetActorsByType("mtnk")
				apc = p10.GetActorsByType("apc")
				jeep = p10.GetActorsByType("jeep")
				stnk = p10.GetActorsByType("stnk")
				arty = p10.GetActorsByType("arty")
				ltnk = p10.GetActorsByType("ltnk")
				ftnk = p10.GetActorsByType("ftnk")
				mlrs = p10.GetActorsByType("mlrs")
				bike = p10.GetActorsByType("bike")
				bggy = p10.GetActorsByType("bggy")
				ctnk = p10.GetActorsByType("ctnk")
				tnk2 = p10.GetActorsByType("2tnk")
				raarty = p10.GetActorsByType("raarty")
				tnk1 = p10.GetActorsByType("1tnk")
				rajeep = p10.GetActorsByType("rajeep")
				tnk4 = p10.GetActorsByType("4tnk")
				v2rl = p10.GetActorsByType("v2rl")
				tnk3 = p10.GetActorsByType("3tnk")
				ttnk = p10.GetActorsByType("ttnk")
				ftrk = p10.GetActorsByType("ftrk")
				raapc = p10.GetActorsByType("raapc")
				hpt2100 = p10.GetActorsByType("2100hpt")
				apt2100 = p10.GetActorsByType("2100apt")
				mch2100 = p10.GetActorsByType("2100mch")
				tch2100 = p10.GetActorsByType("2100tch")
				rvw2100 = p10.GetActorsByType("2100rvw")
				mvw2100 = p10.GetActorsByType("2100mvw")
				rvw22100 = p10.GetActorsByType("2100rvw2")
				mvw22100 = p10.GetActorsByType("2100mvw2")
				scgoliath = p10.GetActorsByType("scgoliath")
				scvulture = p10.GetActorsByType("scvulture")
				scsiegetank = p10.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p11 and p13.IsAlliedWith(p11) then
				htnk = p11.GetActorsByType("htnk")
				msam = p11.GetActorsByType("msam")
				mtnk = p11.GetActorsByType("mtnk")
				apc = p11.GetActorsByType("apc")
				jeep = p11.GetActorsByType("jeep")
				stnk = p11.GetActorsByType("stnk")
				arty = p11.GetActorsByType("arty")
				ltnk = p11.GetActorsByType("ltnk")
				ftnk = p11.GetActorsByType("ftnk")
				mlrs = p11.GetActorsByType("mlrs")
				bike = p11.GetActorsByType("bike")
				bggy = p11.GetActorsByType("bggy")
				ctnk = p11.GetActorsByType("ctnk")
				tnk2 = p11.GetActorsByType("2tnk")
				raarty = p11.GetActorsByType("raarty")
				tnk1 = p11.GetActorsByType("1tnk")
				rajeep = p11.GetActorsByType("rajeep")
				tnk4 = p11.GetActorsByType("4tnk")
				v2rl = p11.GetActorsByType("v2rl")
				tnk3 = p11.GetActorsByType("3tnk")
				ttnk = p11.GetActorsByType("ttnk")
				ftrk = p11.GetActorsByType("ftrk")
				raapc = p11.GetActorsByType("raapc")
				hpt2100 = p11.GetActorsByType("2100hpt")
				apt2100 = p11.GetActorsByType("2100apt")
				mch2100 = p11.GetActorsByType("2100mch")
				tch2100 = p11.GetActorsByType("2100tch")
				rvw2100 = p11.GetActorsByType("2100rvw")
				mvw2100 = p11.GetActorsByType("2100mvw")
				rvw22100 = p11.GetActorsByType("2100rvw2")
				mvw22100 = p11.GetActorsByType("2100mvw2")
				scgoliath = p11.GetActorsByType("scgoliath")
				scvulture = p11.GetActorsByType("scvulture")
				scsiegetank = p11.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p12 and p13.IsAlliedWith(p12) then
				htnk = p12.GetActorsByType("htnk")
				msam = p12.GetActorsByType("msam")
				mtnk = p12.GetActorsByType("mtnk")
				apc = p12.GetActorsByType("apc")
				jeep = p12.GetActorsByType("jeep")
				stnk = p12.GetActorsByType("stnk")
				arty = p12.GetActorsByType("arty")
				ltnk = p12.GetActorsByType("ltnk")
				ftnk = p12.GetActorsByType("ftnk")
				mlrs = p12.GetActorsByType("mlrs")
				bike = p12.GetActorsByType("bike")
				bggy = p12.GetActorsByType("bggy")
				ctnk = p12.GetActorsByType("ctnk")
				tnk2 = p12.GetActorsByType("2tnk")
				raarty = p12.GetActorsByType("raarty")
				tnk1 = p12.GetActorsByType("1tnk")
				rajeep = p12.GetActorsByType("rajeep")
				tnk4 = p12.GetActorsByType("4tnk")
				v2rl = p12.GetActorsByType("v2rl")
				tnk3 = p12.GetActorsByType("3tnk")
				ttnk = p12.GetActorsByType("ttnk")
				ftrk = p12.GetActorsByType("ftrk")
				raapc = p12.GetActorsByType("raapc")
				hpt2100 = p12.GetActorsByType("2100hpt")
				apt2100 = p12.GetActorsByType("2100apt")
				mch2100 = p12.GetActorsByType("2100mch")
				tch2100 = p12.GetActorsByType("2100tch")
				rvw2100 = p12.GetActorsByType("2100rvw")
				mvw2100 = p12.GetActorsByType("2100mvw")
				rvw22100 = p12.GetActorsByType("2100rvw2")
				mvw22100 = p12.GetActorsByType("2100mvw2")
				scgoliath = p12.GetActorsByType("scgoliath")
				scvulture = p12.GetActorsByType("scvulture")
				scsiegetank = p12.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p13.GetActorsByType("citypolicecar")
				cityfiretruck = p13.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p13vehicle()
	end)
end
p14vehicle = function()
	if p14 and p14.IsBot then
		cityboteliteexpert = p14.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p15 and p14.IsAlliedWith(p15) then
				htnk = p15.GetActorsByType("htnk")
				msam = p15.GetActorsByType("msam")
				mtnk = p15.GetActorsByType("mtnk")
				apc = p15.GetActorsByType("apc")
				jeep = p15.GetActorsByType("jeep")
				stnk = p15.GetActorsByType("stnk")
				arty = p15.GetActorsByType("arty")
				ltnk = p15.GetActorsByType("ltnk")
				ftnk = p15.GetActorsByType("ftnk")
				mlrs = p15.GetActorsByType("mlrs")
				bike = p15.GetActorsByType("bike")
				bggy = p15.GetActorsByType("bggy")
				ctnk = p15.GetActorsByType("ctnk")
				tnk2 = p15.GetActorsByType("2tnk")
				raarty = p15.GetActorsByType("raarty")
				tnk1 = p15.GetActorsByType("1tnk")
				rajeep = p15.GetActorsByType("rajeep")
				tnk4 = p15.GetActorsByType("4tnk")
				v2rl = p15.GetActorsByType("v2rl")
				tnk3 = p15.GetActorsByType("3tnk")
				ttnk = p15.GetActorsByType("ttnk")
				ftrk = p15.GetActorsByType("ftrk")
				raapc = p15.GetActorsByType("raapc")
				hpt2100 = p15.GetActorsByType("2100hpt")
				apt2100 = p15.GetActorsByType("2100apt")
				mch2100 = p15.GetActorsByType("2100mch")
				tch2100 = p15.GetActorsByType("2100tch")
				rvw2100 = p15.GetActorsByType("2100rvw")
				mvw2100 = p15.GetActorsByType("2100mvw")
				rvw22100 = p15.GetActorsByType("2100rvw2")
				mvw22100 = p15.GetActorsByType("2100mvw2")
				scgoliath = p15.GetActorsByType("scgoliath")
				scvulture = p15.GetActorsByType("scvulture")
				scsiegetank = p15.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p16 and p14.IsAlliedWith(p16) then
				htnk = p16.GetActorsByType("htnk")
				msam = p16.GetActorsByType("msam")
				mtnk = p16.GetActorsByType("mtnk")
				apc = p16.GetActorsByType("apc")
				jeep = p16.GetActorsByType("jeep")
				stnk = p16.GetActorsByType("stnk")
				arty = p16.GetActorsByType("arty")
				ltnk = p16.GetActorsByType("ltnk")
				ftnk = p16.GetActorsByType("ftnk")
				mlrs = p16.GetActorsByType("mlrs")
				bike = p16.GetActorsByType("bike")
				bggy = p16.GetActorsByType("bggy")
				ctnk = p16.GetActorsByType("ctnk")
				tnk2 = p16.GetActorsByType("2tnk")
				raarty = p16.GetActorsByType("raarty")
				tnk1 = p16.GetActorsByType("1tnk")
				rajeep = p16.GetActorsByType("rajeep")
				tnk4 = p16.GetActorsByType("4tnk")
				v2rl = p16.GetActorsByType("v2rl")
				tnk3 = p16.GetActorsByType("3tnk")
				ttnk = p16.GetActorsByType("ttnk")
				ftrk = p16.GetActorsByType("ftrk")
				raapc = p16.GetActorsByType("raapc")
				hpt2100 = p16.GetActorsByType("2100hpt")
				apt2100 = p16.GetActorsByType("2100apt")
				mch2100 = p16.GetActorsByType("2100mch")
				tch2100 = p16.GetActorsByType("2100tch")
				rvw2100 = p16.GetActorsByType("2100rvw")
				mvw2100 = p16.GetActorsByType("2100mvw")
				rvw22100 = p16.GetActorsByType("2100rvw2")
				mvw22100 = p16.GetActorsByType("2100mvw2")
				scgoliath = p16.GetActorsByType("scgoliath")
				scvulture = p16.GetActorsByType("scvulture")
				scsiegetank = p16.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p1 and p14.IsAlliedWith(p1) then
				htnk = p1.GetActorsByType("htnk")
				msam = p1.GetActorsByType("msam")
				mtnk = p1.GetActorsByType("mtnk")
				apc = p1.GetActorsByType("apc")
				jeep = p1.GetActorsByType("jeep")
				stnk = p1.GetActorsByType("stnk")
				arty = p1.GetActorsByType("arty")
				ltnk = p1.GetActorsByType("ltnk")
				ftnk = p1.GetActorsByType("ftnk")
				mlrs = p1.GetActorsByType("mlrs")
				bike = p1.GetActorsByType("bike")
				bggy = p1.GetActorsByType("bggy")
				ctnk = p1.GetActorsByType("ctnk")
				tnk2 = p1.GetActorsByType("2tnk")
				raarty = p1.GetActorsByType("raarty")
				tnk1 = p1.GetActorsByType("1tnk")
				rajeep = p1.GetActorsByType("rajeep")
				tnk4 = p1.GetActorsByType("4tnk")
				v2rl = p1.GetActorsByType("v2rl")
				tnk3 = p1.GetActorsByType("3tnk")
				ttnk = p1.GetActorsByType("ttnk")
				ftrk = p1.GetActorsByType("ftrk")
				raapc = p1.GetActorsByType("raapc")
				hpt2100 = p1.GetActorsByType("2100hpt")
				apt2100 = p1.GetActorsByType("2100apt")
				mch2100 = p1.GetActorsByType("2100mch")
				tch2100 = p1.GetActorsByType("2100tch")
				rvw2100 = p1.GetActorsByType("2100rvw")
				mvw2100 = p1.GetActorsByType("2100mvw")
				rvw22100 = p1.GetActorsByType("2100rvw2")
				mvw22100 = p1.GetActorsByType("2100mvw2")
				scgoliath = p1.GetActorsByType("scgoliath")
				scvulture = p1.GetActorsByType("scvulture")
				scsiegetank = p1.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p2 and p14.IsAlliedWith(p2) then
				htnk = p2.GetActorsByType("htnk")
				msam = p2.GetActorsByType("msam")
				mtnk = p2.GetActorsByType("mtnk")
				apc = p2.GetActorsByType("apc")
				jeep = p2.GetActorsByType("jeep")
				stnk = p2.GetActorsByType("stnk")
				arty = p2.GetActorsByType("arty")
				ltnk = p2.GetActorsByType("ltnk")
				ftnk = p2.GetActorsByType("ftnk")
				mlrs = p2.GetActorsByType("mlrs")
				bike = p2.GetActorsByType("bike")
				bggy = p2.GetActorsByType("bggy")
				ctnk = p2.GetActorsByType("ctnk")
				tnk2 = p2.GetActorsByType("2tnk")
				raarty = p2.GetActorsByType("raarty")
				tnk1 = p2.GetActorsByType("1tnk")
				rajeep = p2.GetActorsByType("rajeep")
				tnk4 = p2.GetActorsByType("4tnk")
				v2rl = p2.GetActorsByType("v2rl")
				tnk3 = p2.GetActorsByType("3tnk")
				ttnk = p2.GetActorsByType("ttnk")
				ftrk = p2.GetActorsByType("ftrk")
				raapc = p2.GetActorsByType("raapc")
				hpt2100 = p2.GetActorsByType("2100hpt")
				apt2100 = p2.GetActorsByType("2100apt")
				mch2100 = p2.GetActorsByType("2100mch")
				tch2100 = p2.GetActorsByType("2100tch")
				rvw2100 = p2.GetActorsByType("2100rvw")
				mvw2100 = p2.GetActorsByType("2100mvw")
				rvw22100 = p2.GetActorsByType("2100rvw2")
				mvw22100 = p2.GetActorsByType("2100mvw2")
				scgoliath = p2.GetActorsByType("scgoliath")
				scvulture = p2.GetActorsByType("scvulture")
				scsiegetank = p2.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p3 and p14.IsAlliedWith(p3) then
				htnk = p3.GetActorsByType("htnk")
				msam = p3.GetActorsByType("msam")
				mtnk = p3.GetActorsByType("mtnk")
				apc = p3.GetActorsByType("apc")
				jeep = p3.GetActorsByType("jeep")
				stnk = p3.GetActorsByType("stnk")
				arty = p3.GetActorsByType("arty")
				ltnk = p3.GetActorsByType("ltnk")
				ftnk = p3.GetActorsByType("ftnk")
				mlrs = p3.GetActorsByType("mlrs")
				bike = p3.GetActorsByType("bike")
				bggy = p3.GetActorsByType("bggy")
				ctnk = p3.GetActorsByType("ctnk")
				tnk2 = p3.GetActorsByType("2tnk")
				raarty = p3.GetActorsByType("raarty")
				tnk1 = p3.GetActorsByType("1tnk")
				rajeep = p3.GetActorsByType("rajeep")
				tnk4 = p3.GetActorsByType("4tnk")
				v2rl = p3.GetActorsByType("v2rl")
				tnk3 = p3.GetActorsByType("3tnk")
				ttnk = p3.GetActorsByType("ttnk")
				ftrk = p3.GetActorsByType("ftrk")
				raapc = p3.GetActorsByType("raapc")
				hpt2100 = p3.GetActorsByType("2100hpt")
				apt2100 = p3.GetActorsByType("2100apt")
				mch2100 = p3.GetActorsByType("2100mch")
				tch2100 = p3.GetActorsByType("2100tch")
				rvw2100 = p3.GetActorsByType("2100rvw")
				mvw2100 = p3.GetActorsByType("2100mvw")
				rvw22100 = p3.GetActorsByType("2100rvw2")
				mvw22100 = p3.GetActorsByType("2100mvw2")
				scgoliath = p3.GetActorsByType("scgoliath")
				scvulture = p3.GetActorsByType("scvulture")
				scsiegetank = p3.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p4 and p14.IsAlliedWith(p4) then
				htnk = p4.GetActorsByType("htnk")
				msam = p4.GetActorsByType("msam")
				mtnk = p4.GetActorsByType("mtnk")
				apc = p4.GetActorsByType("apc")
				jeep = p4.GetActorsByType("jeep")
				stnk = p4.GetActorsByType("stnk")
				arty = p4.GetActorsByType("arty")
				ltnk = p4.GetActorsByType("ltnk")
				ftnk = p4.GetActorsByType("ftnk")
				mlrs = p4.GetActorsByType("mlrs")
				bike = p4.GetActorsByType("bike")
				bggy = p4.GetActorsByType("bggy")
				ctnk = p4.GetActorsByType("ctnk")
				tnk2 = p4.GetActorsByType("2tnk")
				raarty = p4.GetActorsByType("raarty")
				tnk1 = p4.GetActorsByType("1tnk")
				rajeep = p4.GetActorsByType("rajeep")
				tnk4 = p4.GetActorsByType("4tnk")
				v2rl = p4.GetActorsByType("v2rl")
				tnk3 = p4.GetActorsByType("3tnk")
				ttnk = p4.GetActorsByType("ttnk")
				ftrk = p4.GetActorsByType("ftrk")
				raapc = p4.GetActorsByType("raapc")
				hpt2100 = p4.GetActorsByType("2100hpt")
				apt2100 = p4.GetActorsByType("2100apt")
				mch2100 = p4.GetActorsByType("2100mch")
				tch2100 = p4.GetActorsByType("2100tch")
				rvw2100 = p4.GetActorsByType("2100rvw")
				mvw2100 = p4.GetActorsByType("2100mvw")
				rvw22100 = p4.GetActorsByType("2100rvw2")
				mvw22100 = p4.GetActorsByType("2100mvw2")
				scgoliath = p4.GetActorsByType("scgoliath")
				scvulture = p4.GetActorsByType("scvulture")
				scsiegetank = p4.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p5 and p14.IsAlliedWith(p5) then
				htnk = p5.GetActorsByType("htnk")
				msam = p5.GetActorsByType("msam")
				mtnk = p5.GetActorsByType("mtnk")
				apc = p5.GetActorsByType("apc")
				jeep = p5.GetActorsByType("jeep")
				stnk = p5.GetActorsByType("stnk")
				arty = p5.GetActorsByType("arty")
				ltnk = p5.GetActorsByType("ltnk")
				ftnk = p5.GetActorsByType("ftnk")
				mlrs = p5.GetActorsByType("mlrs")
				bike = p5.GetActorsByType("bike")
				bggy = p5.GetActorsByType("bggy")
				ctnk = p5.GetActorsByType("ctnk")
				tnk2 = p5.GetActorsByType("2tnk")
				raarty = p5.GetActorsByType("raarty")
				tnk1 = p5.GetActorsByType("1tnk")
				rajeep = p5.GetActorsByType("rajeep")
				tnk4 = p5.GetActorsByType("4tnk")
				v2rl = p5.GetActorsByType("v2rl")
				tnk3 = p5.GetActorsByType("3tnk")
				ttnk = p5.GetActorsByType("ttnk")
				ftrk = p5.GetActorsByType("ftrk")
				raapc = p5.GetActorsByType("raapc")
				hpt2100 = p5.GetActorsByType("2100hpt")
				apt2100 = p5.GetActorsByType("2100apt")
				mch2100 = p5.GetActorsByType("2100mch")
				tch2100 = p5.GetActorsByType("2100tch")
				rvw2100 = p5.GetActorsByType("2100rvw")
				mvw2100 = p5.GetActorsByType("2100mvw")
				rvw22100 = p5.GetActorsByType("2100rvw2")
				mvw22100 = p5.GetActorsByType("2100mvw2")
				scgoliath = p5.GetActorsByType("scgoliath")
				scvulture = p5.GetActorsByType("scvulture")
				scsiegetank = p5.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p6 and p14.IsAlliedWith(p6) then
				htnk = p6.GetActorsByType("htnk")
				msam = p6.GetActorsByType("msam")
				mtnk = p6.GetActorsByType("mtnk")
				apc = p6.GetActorsByType("apc")
				jeep = p6.GetActorsByType("jeep")
				stnk = p6.GetActorsByType("stnk")
				arty = p6.GetActorsByType("arty")
				ltnk = p6.GetActorsByType("ltnk")
				ftnk = p6.GetActorsByType("ftnk")
				mlrs = p6.GetActorsByType("mlrs")
				bike = p6.GetActorsByType("bike")
				bggy = p6.GetActorsByType("bggy")
				ctnk = p6.GetActorsByType("ctnk")
				tnk2 = p6.GetActorsByType("2tnk")
				raarty = p6.GetActorsByType("raarty")
				tnk1 = p6.GetActorsByType("1tnk")
				rajeep = p6.GetActorsByType("rajeep")
				tnk4 = p6.GetActorsByType("4tnk")
				v2rl = p6.GetActorsByType("v2rl")
				tnk3 = p6.GetActorsByType("3tnk")
				ttnk = p6.GetActorsByType("ttnk")
				ftrk = p6.GetActorsByType("ftrk")
				raapc = p6.GetActorsByType("raapc")
				hpt2100 = p6.GetActorsByType("2100hpt")
				apt2100 = p6.GetActorsByType("2100apt")
				mch2100 = p6.GetActorsByType("2100mch")
				tch2100 = p6.GetActorsByType("2100tch")
				rvw2100 = p6.GetActorsByType("2100rvw")
				mvw2100 = p6.GetActorsByType("2100mvw")
				rvw22100 = p6.GetActorsByType("2100rvw2")
				mvw22100 = p6.GetActorsByType("2100mvw2")
				scgoliath = p6.GetActorsByType("scgoliath")
				scvulture = p6.GetActorsByType("scvulture")
				scsiegetank = p6.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p7 and p14.IsAlliedWith(p7) then
				htnk = p7.GetActorsByType("htnk")
				msam = p7.GetActorsByType("msam")
				mtnk = p7.GetActorsByType("mtnk")
				apc = p7.GetActorsByType("apc")
				jeep = p7.GetActorsByType("jeep")
				stnk = p7.GetActorsByType("stnk")
				arty = p7.GetActorsByType("arty")
				ltnk = p7.GetActorsByType("ltnk")
				ftnk = p7.GetActorsByType("ftnk")
				mlrs = p7.GetActorsByType("mlrs")
				bike = p7.GetActorsByType("bike")
				bggy = p7.GetActorsByType("bggy")
				ctnk = p7.GetActorsByType("ctnk")
				tnk2 = p7.GetActorsByType("2tnk")
				raarty = p7.GetActorsByType("raarty")
				tnk1 = p7.GetActorsByType("1tnk")
				rajeep = p7.GetActorsByType("rajeep")
				tnk4 = p7.GetActorsByType("4tnk")
				v2rl = p7.GetActorsByType("v2rl")
				tnk3 = p7.GetActorsByType("3tnk")
				ttnk = p7.GetActorsByType("ttnk")
				ftrk = p7.GetActorsByType("ftrk")
				raapc = p7.GetActorsByType("raapc")
				hpt2100 = p7.GetActorsByType("2100hpt")
				apt2100 = p7.GetActorsByType("2100apt")
				mch2100 = p7.GetActorsByType("2100mch")
				tch2100 = p7.GetActorsByType("2100tch")
				rvw2100 = p7.GetActorsByType("2100rvw")
				mvw2100 = p7.GetActorsByType("2100mvw")
				rvw22100 = p7.GetActorsByType("2100rvw2")
				mvw22100 = p7.GetActorsByType("2100mvw2")
				scgoliath = p7.GetActorsByType("scgoliath")
				scvulture = p7.GetActorsByType("scvulture")
				scsiegetank = p7.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p8 and p14.IsAlliedWith(p8) then
				htnk = p8.GetActorsByType("htnk")
				msam = p8.GetActorsByType("msam")
				mtnk = p8.GetActorsByType("mtnk")
				apc = p8.GetActorsByType("apc")
				jeep = p8.GetActorsByType("jeep")
				stnk = p8.GetActorsByType("stnk")
				arty = p8.GetActorsByType("arty")
				ltnk = p8.GetActorsByType("ltnk")
				ftnk = p8.GetActorsByType("ftnk")
				mlrs = p8.GetActorsByType("mlrs")
				bike = p8.GetActorsByType("bike")
				bggy = p8.GetActorsByType("bggy")
				ctnk = p8.GetActorsByType("ctnk")
				tnk2 = p8.GetActorsByType("2tnk")
				raarty = p8.GetActorsByType("raarty")
				tnk1 = p8.GetActorsByType("1tnk")
				rajeep = p8.GetActorsByType("rajeep")
				tnk4 = p8.GetActorsByType("4tnk")
				v2rl = p8.GetActorsByType("v2rl")
				tnk3 = p8.GetActorsByType("3tnk")
				ttnk = p8.GetActorsByType("ttnk")
				ftrk = p8.GetActorsByType("ftrk")
				raapc = p8.GetActorsByType("raapc")
				hpt2100 = p8.GetActorsByType("2100hpt")
				apt2100 = p8.GetActorsByType("2100apt")
				mch2100 = p8.GetActorsByType("2100mch")
				tch2100 = p8.GetActorsByType("2100tch")
				rvw2100 = p8.GetActorsByType("2100rvw")
				mvw2100 = p8.GetActorsByType("2100mvw")
				rvw22100 = p8.GetActorsByType("2100rvw2")
				mvw22100 = p8.GetActorsByType("2100mvw2")
				scgoliath = p8.GetActorsByType("scgoliath")
				scvulture = p8.GetActorsByType("scvulture")
				scsiegetank = p8.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p9 and p14.IsAlliedWith(p9) then
				htnk = p9.GetActorsByType("htnk")
				msam = p9.GetActorsByType("msam")
				mtnk = p9.GetActorsByType("mtnk")
				apc = p9.GetActorsByType("apc")
				jeep = p9.GetActorsByType("jeep")
				stnk = p9.GetActorsByType("stnk")
				arty = p9.GetActorsByType("arty")
				ltnk = p9.GetActorsByType("ltnk")
				ftnk = p9.GetActorsByType("ftnk")
				mlrs = p9.GetActorsByType("mlrs")
				bike = p9.GetActorsByType("bike")
				bggy = p9.GetActorsByType("bggy")
				ctnk = p9.GetActorsByType("ctnk")
				tnk2 = p9.GetActorsByType("2tnk")
				raarty = p9.GetActorsByType("raarty")
				tnk1 = p9.GetActorsByType("1tnk")
				rajeep = p9.GetActorsByType("rajeep")
				tnk4 = p9.GetActorsByType("4tnk")
				v2rl = p9.GetActorsByType("v2rl")
				tnk3 = p9.GetActorsByType("3tnk")
				ttnk = p9.GetActorsByType("ttnk")
				ftrk = p9.GetActorsByType("ftrk")
				raapc = p9.GetActorsByType("raapc")
				hpt2100 = p9.GetActorsByType("2100hpt")
				apt2100 = p9.GetActorsByType("2100apt")
				mch2100 = p9.GetActorsByType("2100mch")
				tch2100 = p9.GetActorsByType("2100tch")
				rvw2100 = p9.GetActorsByType("2100rvw")
				mvw2100 = p9.GetActorsByType("2100mvw")
				rvw22100 = p9.GetActorsByType("2100rvw2")
				mvw22100 = p9.GetActorsByType("2100mvw2")
				scgoliath = p9.GetActorsByType("scgoliath")
				scvulture = p9.GetActorsByType("scvulture")
				scsiegetank = p9.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p10 and p14.IsAlliedWith(p10) then
				htnk = p10.GetActorsByType("htnk")
				msam = p10.GetActorsByType("msam")
				mtnk = p10.GetActorsByType("mtnk")
				apc = p10.GetActorsByType("apc")
				jeep = p10.GetActorsByType("jeep")
				stnk = p10.GetActorsByType("stnk")
				arty = p10.GetActorsByType("arty")
				ltnk = p10.GetActorsByType("ltnk")
				ftnk = p10.GetActorsByType("ftnk")
				mlrs = p10.GetActorsByType("mlrs")
				bike = p10.GetActorsByType("bike")
				bggy = p10.GetActorsByType("bggy")
				ctnk = p10.GetActorsByType("ctnk")
				tnk2 = p10.GetActorsByType("2tnk")
				raarty = p10.GetActorsByType("raarty")
				tnk1 = p10.GetActorsByType("1tnk")
				rajeep = p10.GetActorsByType("rajeep")
				tnk4 = p10.GetActorsByType("4tnk")
				v2rl = p10.GetActorsByType("v2rl")
				tnk3 = p10.GetActorsByType("3tnk")
				ttnk = p10.GetActorsByType("ttnk")
				ftrk = p10.GetActorsByType("ftrk")
				raapc = p10.GetActorsByType("raapc")
				hpt2100 = p10.GetActorsByType("2100hpt")
				apt2100 = p10.GetActorsByType("2100apt")
				mch2100 = p10.GetActorsByType("2100mch")
				tch2100 = p10.GetActorsByType("2100tch")
				rvw2100 = p10.GetActorsByType("2100rvw")
				mvw2100 = p10.GetActorsByType("2100mvw")
				rvw22100 = p10.GetActorsByType("2100rvw2")
				mvw22100 = p10.GetActorsByType("2100mvw2")
				scgoliath = p10.GetActorsByType("scgoliath")
				scvulture = p10.GetActorsByType("scvulture")
				scsiegetank = p10.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p11 and p14.IsAlliedWith(p11) then
				htnk = p11.GetActorsByType("htnk")
				msam = p11.GetActorsByType("msam")
				mtnk = p11.GetActorsByType("mtnk")
				apc = p11.GetActorsByType("apc")
				jeep = p11.GetActorsByType("jeep")
				stnk = p11.GetActorsByType("stnk")
				arty = p11.GetActorsByType("arty")
				ltnk = p11.GetActorsByType("ltnk")
				ftnk = p11.GetActorsByType("ftnk")
				mlrs = p11.GetActorsByType("mlrs")
				bike = p11.GetActorsByType("bike")
				bggy = p11.GetActorsByType("bggy")
				ctnk = p11.GetActorsByType("ctnk")
				tnk2 = p11.GetActorsByType("2tnk")
				raarty = p11.GetActorsByType("raarty")
				tnk1 = p11.GetActorsByType("1tnk")
				rajeep = p11.GetActorsByType("rajeep")
				tnk4 = p11.GetActorsByType("4tnk")
				v2rl = p11.GetActorsByType("v2rl")
				tnk3 = p11.GetActorsByType("3tnk")
				ttnk = p11.GetActorsByType("ttnk")
				ftrk = p11.GetActorsByType("ftrk")
				raapc = p11.GetActorsByType("raapc")
				hpt2100 = p11.GetActorsByType("2100hpt")
				apt2100 = p11.GetActorsByType("2100apt")
				mch2100 = p11.GetActorsByType("2100mch")
				tch2100 = p11.GetActorsByType("2100tch")
				rvw2100 = p11.GetActorsByType("2100rvw")
				mvw2100 = p11.GetActorsByType("2100mvw")
				rvw22100 = p11.GetActorsByType("2100rvw2")
				mvw22100 = p11.GetActorsByType("2100mvw2")
				scgoliath = p11.GetActorsByType("scgoliath")
				scvulture = p11.GetActorsByType("scvulture")
				scsiegetank = p11.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p12 and p14.IsAlliedWith(p12) then
				htnk = p12.GetActorsByType("htnk")
				msam = p12.GetActorsByType("msam")
				mtnk = p12.GetActorsByType("mtnk")
				apc = p12.GetActorsByType("apc")
				jeep = p12.GetActorsByType("jeep")
				stnk = p12.GetActorsByType("stnk")
				arty = p12.GetActorsByType("arty")
				ltnk = p12.GetActorsByType("ltnk")
				ftnk = p12.GetActorsByType("ftnk")
				mlrs = p12.GetActorsByType("mlrs")
				bike = p12.GetActorsByType("bike")
				bggy = p12.GetActorsByType("bggy")
				ctnk = p12.GetActorsByType("ctnk")
				tnk2 = p12.GetActorsByType("2tnk")
				raarty = p12.GetActorsByType("raarty")
				tnk1 = p12.GetActorsByType("1tnk")
				rajeep = p12.GetActorsByType("rajeep")
				tnk4 = p12.GetActorsByType("4tnk")
				v2rl = p12.GetActorsByType("v2rl")
				tnk3 = p12.GetActorsByType("3tnk")
				ttnk = p12.GetActorsByType("ttnk")
				ftrk = p12.GetActorsByType("ftrk")
				raapc = p12.GetActorsByType("raapc")
				hpt2100 = p12.GetActorsByType("2100hpt")
				apt2100 = p12.GetActorsByType("2100apt")
				mch2100 = p12.GetActorsByType("2100mch")
				tch2100 = p12.GetActorsByType("2100tch")
				rvw2100 = p12.GetActorsByType("2100rvw")
				mvw2100 = p12.GetActorsByType("2100mvw")
				rvw22100 = p12.GetActorsByType("2100rvw2")
				mvw22100 = p12.GetActorsByType("2100mvw2")
				scgoliath = p12.GetActorsByType("scgoliath")
				scvulture = p12.GetActorsByType("scvulture")
				scsiegetank = p12.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p13 and p14.IsAlliedWith(p13) then
				htnk = p13.GetActorsByType("htnk")
				msam = p13.GetActorsByType("msam")
				mtnk = p13.GetActorsByType("mtnk")
				apc = p13.GetActorsByType("apc")
				jeep = p13.GetActorsByType("jeep")
				stnk = p13.GetActorsByType("stnk")
				arty = p13.GetActorsByType("arty")
				ltnk = p13.GetActorsByType("ltnk")
				ftnk = p13.GetActorsByType("ftnk")
				mlrs = p13.GetActorsByType("mlrs")
				bike = p13.GetActorsByType("bike")
				bggy = p13.GetActorsByType("bggy")
				ctnk = p13.GetActorsByType("ctnk")
				tnk2 = p13.GetActorsByType("2tnk")
				raarty = p13.GetActorsByType("raarty")
				tnk1 = p13.GetActorsByType("1tnk")
				rajeep = p13.GetActorsByType("rajeep")
				tnk4 = p13.GetActorsByType("4tnk")
				v2rl = p13.GetActorsByType("v2rl")
				tnk3 = p13.GetActorsByType("3tnk")
				ttnk = p13.GetActorsByType("ttnk")
				ftrk = p13.GetActorsByType("ftrk")
				raapc = p13.GetActorsByType("raapc")
				hpt2100 = p13.GetActorsByType("2100hpt")
				apt2100 = p13.GetActorsByType("2100apt")
				mch2100 = p13.GetActorsByType("2100mch")
				tch2100 = p13.GetActorsByType("2100tch")
				rvw2100 = p13.GetActorsByType("2100rvw")
				mvw2100 = p13.GetActorsByType("2100mvw")
				rvw22100 = p13.GetActorsByType("2100rvw2")
				mvw22100 = p13.GetActorsByType("2100mvw2")
				scgoliath = p13.GetActorsByType("scgoliath")
				scvulture = p13.GetActorsByType("scvulture")
				scsiegetank = p13.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p14.GetActorsByType("citypolicecar")
				cityfiretruck = p14.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p14vehicle()
	end)
end
p15vehicle = function()
	if p15 and p15.IsBot then
		cityboteliteexpert = p15.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p16 and p15.IsAlliedWith(p16) then
				htnk = p16.GetActorsByType("htnk")
				msam = p16.GetActorsByType("msam")
				mtnk = p16.GetActorsByType("mtnk")
				apc = p16.GetActorsByType("apc")
				jeep = p16.GetActorsByType("jeep")
				stnk = p16.GetActorsByType("stnk")
				arty = p16.GetActorsByType("arty")
				ltnk = p16.GetActorsByType("ltnk")
				ftnk = p16.GetActorsByType("ftnk")
				mlrs = p16.GetActorsByType("mlrs")
				bike = p16.GetActorsByType("bike")
				bggy = p16.GetActorsByType("bggy")
				ctnk = p16.GetActorsByType("ctnk")
				tnk2 = p16.GetActorsByType("2tnk")
				raarty = p16.GetActorsByType("raarty")
				tnk1 = p16.GetActorsByType("1tnk")
				rajeep = p16.GetActorsByType("rajeep")
				tnk4 = p16.GetActorsByType("4tnk")
				v2rl = p16.GetActorsByType("v2rl")
				tnk3 = p16.GetActorsByType("3tnk")
				ttnk = p16.GetActorsByType("ttnk")
				ftrk = p16.GetActorsByType("ftrk")
				raapc = p16.GetActorsByType("raapc")
				hpt2100 = p16.GetActorsByType("2100hpt")
				apt2100 = p16.GetActorsByType("2100apt")
				mch2100 = p16.GetActorsByType("2100mch")
				tch2100 = p16.GetActorsByType("2100tch")
				rvw2100 = p16.GetActorsByType("2100rvw")
				mvw2100 = p16.GetActorsByType("2100mvw")
				rvw22100 = p16.GetActorsByType("2100rvw2")
				mvw22100 = p16.GetActorsByType("2100mvw2")
				scgoliath = p16.GetActorsByType("scgoliath")
				scvulture = p16.GetActorsByType("scvulture")
				scsiegetank = p16.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p1 and p15.IsAlliedWith(p1) then
				htnk = p1.GetActorsByType("htnk")
				msam = p1.GetActorsByType("msam")
				mtnk = p1.GetActorsByType("mtnk")
				apc = p1.GetActorsByType("apc")
				jeep = p1.GetActorsByType("jeep")
				stnk = p1.GetActorsByType("stnk")
				arty = p1.GetActorsByType("arty")
				ltnk = p1.GetActorsByType("ltnk")
				ftnk = p1.GetActorsByType("ftnk")
				mlrs = p1.GetActorsByType("mlrs")
				bike = p1.GetActorsByType("bike")
				bggy = p1.GetActorsByType("bggy")
				ctnk = p1.GetActorsByType("ctnk")
				tnk2 = p1.GetActorsByType("2tnk")
				raarty = p1.GetActorsByType("raarty")
				tnk1 = p1.GetActorsByType("1tnk")
				rajeep = p1.GetActorsByType("rajeep")
				tnk4 = p1.GetActorsByType("4tnk")
				v2rl = p1.GetActorsByType("v2rl")
				tnk3 = p1.GetActorsByType("3tnk")
				ttnk = p1.GetActorsByType("ttnk")
				ftrk = p1.GetActorsByType("ftrk")
				raapc = p1.GetActorsByType("raapc")
				hpt2100 = p1.GetActorsByType("2100hpt")
				apt2100 = p1.GetActorsByType("2100apt")
				mch2100 = p1.GetActorsByType("2100mch")
				tch2100 = p1.GetActorsByType("2100tch")
				rvw2100 = p1.GetActorsByType("2100rvw")
				mvw2100 = p1.GetActorsByType("2100mvw")
				rvw22100 = p1.GetActorsByType("2100rvw2")
				mvw22100 = p1.GetActorsByType("2100mvw2")
				scgoliath = p1.GetActorsByType("scgoliath")
				scvulture = p1.GetActorsByType("scvulture")
				scsiegetank = p1.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p2 and p15.IsAlliedWith(p2) then
				htnk = p2.GetActorsByType("htnk")
				msam = p2.GetActorsByType("msam")
				mtnk = p2.GetActorsByType("mtnk")
				apc = p2.GetActorsByType("apc")
				jeep = p2.GetActorsByType("jeep")
				stnk = p2.GetActorsByType("stnk")
				arty = p2.GetActorsByType("arty")
				ltnk = p2.GetActorsByType("ltnk")
				ftnk = p2.GetActorsByType("ftnk")
				mlrs = p2.GetActorsByType("mlrs")
				bike = p2.GetActorsByType("bike")
				bggy = p2.GetActorsByType("bggy")
				ctnk = p2.GetActorsByType("ctnk")
				tnk2 = p2.GetActorsByType("2tnk")
				raarty = p2.GetActorsByType("raarty")
				tnk1 = p2.GetActorsByType("1tnk")
				rajeep = p2.GetActorsByType("rajeep")
				tnk4 = p2.GetActorsByType("4tnk")
				v2rl = p2.GetActorsByType("v2rl")
				tnk3 = p2.GetActorsByType("3tnk")
				ttnk = p2.GetActorsByType("ttnk")
				ftrk = p2.GetActorsByType("ftrk")
				raapc = p2.GetActorsByType("raapc")
				hpt2100 = p2.GetActorsByType("2100hpt")
				apt2100 = p2.GetActorsByType("2100apt")
				mch2100 = p2.GetActorsByType("2100mch")
				tch2100 = p2.GetActorsByType("2100tch")
				rvw2100 = p2.GetActorsByType("2100rvw")
				mvw2100 = p2.GetActorsByType("2100mvw")
				rvw22100 = p2.GetActorsByType("2100rvw2")
				mvw22100 = p2.GetActorsByType("2100mvw2")
				scgoliath = p2.GetActorsByType("scgoliath")
				scvulture = p2.GetActorsByType("scvulture")
				scsiegetank = p2.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p3 and p15.IsAlliedWith(p3) then
				htnk = p3.GetActorsByType("htnk")
				msam = p3.GetActorsByType("msam")
				mtnk = p3.GetActorsByType("mtnk")
				apc = p3.GetActorsByType("apc")
				jeep = p3.GetActorsByType("jeep")
				stnk = p3.GetActorsByType("stnk")
				arty = p3.GetActorsByType("arty")
				ltnk = p3.GetActorsByType("ltnk")
				ftnk = p3.GetActorsByType("ftnk")
				mlrs = p3.GetActorsByType("mlrs")
				bike = p3.GetActorsByType("bike")
				bggy = p3.GetActorsByType("bggy")
				ctnk = p3.GetActorsByType("ctnk")
				tnk2 = p3.GetActorsByType("2tnk")
				raarty = p3.GetActorsByType("raarty")
				tnk1 = p3.GetActorsByType("1tnk")
				rajeep = p3.GetActorsByType("rajeep")
				tnk4 = p3.GetActorsByType("4tnk")
				v2rl = p3.GetActorsByType("v2rl")
				tnk3 = p3.GetActorsByType("3tnk")
				ttnk = p3.GetActorsByType("ttnk")
				ftrk = p3.GetActorsByType("ftrk")
				raapc = p3.GetActorsByType("raapc")
				hpt2100 = p3.GetActorsByType("2100hpt")
				apt2100 = p3.GetActorsByType("2100apt")
				mch2100 = p3.GetActorsByType("2100mch")
				tch2100 = p3.GetActorsByType("2100tch")
				rvw2100 = p3.GetActorsByType("2100rvw")
				mvw2100 = p3.GetActorsByType("2100mvw")
				rvw22100 = p3.GetActorsByType("2100rvw2")
				mvw22100 = p3.GetActorsByType("2100mvw2")
				scgoliath = p3.GetActorsByType("scgoliath")
				scvulture = p3.GetActorsByType("scvulture")
				scsiegetank = p3.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p4 and p15.IsAlliedWith(p4) then
				htnk = p4.GetActorsByType("htnk")
				msam = p4.GetActorsByType("msam")
				mtnk = p4.GetActorsByType("mtnk")
				apc = p4.GetActorsByType("apc")
				jeep = p4.GetActorsByType("jeep")
				stnk = p4.GetActorsByType("stnk")
				arty = p4.GetActorsByType("arty")
				ltnk = p4.GetActorsByType("ltnk")
				ftnk = p4.GetActorsByType("ftnk")
				mlrs = p4.GetActorsByType("mlrs")
				bike = p4.GetActorsByType("bike")
				bggy = p4.GetActorsByType("bggy")
				ctnk = p4.GetActorsByType("ctnk")
				tnk2 = p4.GetActorsByType("2tnk")
				raarty = p4.GetActorsByType("raarty")
				tnk1 = p4.GetActorsByType("1tnk")
				rajeep = p4.GetActorsByType("rajeep")
				tnk4 = p4.GetActorsByType("4tnk")
				v2rl = p4.GetActorsByType("v2rl")
				tnk3 = p4.GetActorsByType("3tnk")
				ttnk = p4.GetActorsByType("ttnk")
				ftrk = p4.GetActorsByType("ftrk")
				raapc = p4.GetActorsByType("raapc")
				hpt2100 = p4.GetActorsByType("2100hpt")
				apt2100 = p4.GetActorsByType("2100apt")
				mch2100 = p4.GetActorsByType("2100mch")
				tch2100 = p4.GetActorsByType("2100tch")
				rvw2100 = p4.GetActorsByType("2100rvw")
				mvw2100 = p4.GetActorsByType("2100mvw")
				rvw22100 = p4.GetActorsByType("2100rvw2")
				mvw22100 = p4.GetActorsByType("2100mvw2")
				scgoliath = p4.GetActorsByType("scgoliath")
				scvulture = p4.GetActorsByType("scvulture")
				scsiegetank = p4.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p5 and p15.IsAlliedWith(p5) then
				htnk = p5.GetActorsByType("htnk")
				msam = p5.GetActorsByType("msam")
				mtnk = p5.GetActorsByType("mtnk")
				apc = p5.GetActorsByType("apc")
				jeep = p5.GetActorsByType("jeep")
				stnk = p5.GetActorsByType("stnk")
				arty = p5.GetActorsByType("arty")
				ltnk = p5.GetActorsByType("ltnk")
				ftnk = p5.GetActorsByType("ftnk")
				mlrs = p5.GetActorsByType("mlrs")
				bike = p5.GetActorsByType("bike")
				bggy = p5.GetActorsByType("bggy")
				ctnk = p5.GetActorsByType("ctnk")
				tnk2 = p5.GetActorsByType("2tnk")
				raarty = p5.GetActorsByType("raarty")
				tnk1 = p5.GetActorsByType("1tnk")
				rajeep = p5.GetActorsByType("rajeep")
				tnk4 = p5.GetActorsByType("4tnk")
				v2rl = p5.GetActorsByType("v2rl")
				tnk3 = p5.GetActorsByType("3tnk")
				ttnk = p5.GetActorsByType("ttnk")
				ftrk = p5.GetActorsByType("ftrk")
				raapc = p5.GetActorsByType("raapc")
				hpt2100 = p5.GetActorsByType("2100hpt")
				apt2100 = p5.GetActorsByType("2100apt")
				mch2100 = p5.GetActorsByType("2100mch")
				tch2100 = p5.GetActorsByType("2100tch")
				rvw2100 = p5.GetActorsByType("2100rvw")
				mvw2100 = p5.GetActorsByType("2100mvw")
				rvw22100 = p5.GetActorsByType("2100rvw2")
				mvw22100 = p5.GetActorsByType("2100mvw2")
				scgoliath = p5.GetActorsByType("scgoliath")
				scvulture = p5.GetActorsByType("scvulture")
				scsiegetank = p5.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p6 and p15.IsAlliedWith(p6) then
				htnk = p6.GetActorsByType("htnk")
				msam = p6.GetActorsByType("msam")
				mtnk = p6.GetActorsByType("mtnk")
				apc = p6.GetActorsByType("apc")
				jeep = p6.GetActorsByType("jeep")
				stnk = p6.GetActorsByType("stnk")
				arty = p6.GetActorsByType("arty")
				ltnk = p6.GetActorsByType("ltnk")
				ftnk = p6.GetActorsByType("ftnk")
				mlrs = p6.GetActorsByType("mlrs")
				bike = p6.GetActorsByType("bike")
				bggy = p6.GetActorsByType("bggy")
				ctnk = p6.GetActorsByType("ctnk")
				tnk2 = p6.GetActorsByType("2tnk")
				raarty = p6.GetActorsByType("raarty")
				tnk1 = p6.GetActorsByType("1tnk")
				rajeep = p6.GetActorsByType("rajeep")
				tnk4 = p6.GetActorsByType("4tnk")
				v2rl = p6.GetActorsByType("v2rl")
				tnk3 = p6.GetActorsByType("3tnk")
				ttnk = p6.GetActorsByType("ttnk")
				ftrk = p6.GetActorsByType("ftrk")
				raapc = p6.GetActorsByType("raapc")
				hpt2100 = p6.GetActorsByType("2100hpt")
				apt2100 = p6.GetActorsByType("2100apt")
				mch2100 = p6.GetActorsByType("2100mch")
				tch2100 = p6.GetActorsByType("2100tch")
				rvw2100 = p6.GetActorsByType("2100rvw")
				mvw2100 = p6.GetActorsByType("2100mvw")
				rvw22100 = p6.GetActorsByType("2100rvw2")
				mvw22100 = p6.GetActorsByType("2100mvw2")
				scgoliath = p6.GetActorsByType("scgoliath")
				scvulture = p6.GetActorsByType("scvulture")
				scsiegetank = p6.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p7 and p15.IsAlliedWith(p7) then
				htnk = p7.GetActorsByType("htnk")
				msam = p7.GetActorsByType("msam")
				mtnk = p7.GetActorsByType("mtnk")
				apc = p7.GetActorsByType("apc")
				jeep = p7.GetActorsByType("jeep")
				stnk = p7.GetActorsByType("stnk")
				arty = p7.GetActorsByType("arty")
				ltnk = p7.GetActorsByType("ltnk")
				ftnk = p7.GetActorsByType("ftnk")
				mlrs = p7.GetActorsByType("mlrs")
				bike = p7.GetActorsByType("bike")
				bggy = p7.GetActorsByType("bggy")
				ctnk = p7.GetActorsByType("ctnk")
				tnk2 = p7.GetActorsByType("2tnk")
				raarty = p7.GetActorsByType("raarty")
				tnk1 = p7.GetActorsByType("1tnk")
				rajeep = p7.GetActorsByType("rajeep")
				tnk4 = p7.GetActorsByType("4tnk")
				v2rl = p7.GetActorsByType("v2rl")
				tnk3 = p7.GetActorsByType("3tnk")
				ttnk = p7.GetActorsByType("ttnk")
				ftrk = p7.GetActorsByType("ftrk")
				raapc = p7.GetActorsByType("raapc")
				hpt2100 = p7.GetActorsByType("2100hpt")
				apt2100 = p7.GetActorsByType("2100apt")
				mch2100 = p7.GetActorsByType("2100mch")
				tch2100 = p7.GetActorsByType("2100tch")
				rvw2100 = p7.GetActorsByType("2100rvw")
				mvw2100 = p7.GetActorsByType("2100mvw")
				rvw22100 = p7.GetActorsByType("2100rvw2")
				mvw22100 = p7.GetActorsByType("2100mvw2")
				scgoliath = p7.GetActorsByType("scgoliath")
				scvulture = p7.GetActorsByType("scvulture")
				scsiegetank = p7.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p8 and p15.IsAlliedWith(p8) then
				htnk = p8.GetActorsByType("htnk")
				msam = p8.GetActorsByType("msam")
				mtnk = p8.GetActorsByType("mtnk")
				apc = p8.GetActorsByType("apc")
				jeep = p8.GetActorsByType("jeep")
				stnk = p8.GetActorsByType("stnk")
				arty = p8.GetActorsByType("arty")
				ltnk = p8.GetActorsByType("ltnk")
				ftnk = p8.GetActorsByType("ftnk")
				mlrs = p8.GetActorsByType("mlrs")
				bike = p8.GetActorsByType("bike")
				bggy = p8.GetActorsByType("bggy")
				ctnk = p8.GetActorsByType("ctnk")
				tnk2 = p8.GetActorsByType("2tnk")
				raarty = p8.GetActorsByType("raarty")
				tnk1 = p8.GetActorsByType("1tnk")
				rajeep = p8.GetActorsByType("rajeep")
				tnk4 = p8.GetActorsByType("4tnk")
				v2rl = p8.GetActorsByType("v2rl")
				tnk3 = p8.GetActorsByType("3tnk")
				ttnk = p8.GetActorsByType("ttnk")
				ftrk = p8.GetActorsByType("ftrk")
				raapc = p8.GetActorsByType("raapc")
				hpt2100 = p8.GetActorsByType("2100hpt")
				apt2100 = p8.GetActorsByType("2100apt")
				mch2100 = p8.GetActorsByType("2100mch")
				tch2100 = p8.GetActorsByType("2100tch")
				rvw2100 = p8.GetActorsByType("2100rvw")
				mvw2100 = p8.GetActorsByType("2100mvw")
				rvw22100 = p8.GetActorsByType("2100rvw2")
				mvw22100 = p8.GetActorsByType("2100mvw2")
				scgoliath = p8.GetActorsByType("scgoliath")
				scvulture = p8.GetActorsByType("scvulture")
				scsiegetank = p8.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p9 and p15.IsAlliedWith(p9) then
				htnk = p9.GetActorsByType("htnk")
				msam = p9.GetActorsByType("msam")
				mtnk = p9.GetActorsByType("mtnk")
				apc = p9.GetActorsByType("apc")
				jeep = p9.GetActorsByType("jeep")
				stnk = p9.GetActorsByType("stnk")
				arty = p9.GetActorsByType("arty")
				ltnk = p9.GetActorsByType("ltnk")
				ftnk = p9.GetActorsByType("ftnk")
				mlrs = p9.GetActorsByType("mlrs")
				bike = p9.GetActorsByType("bike")
				bggy = p9.GetActorsByType("bggy")
				ctnk = p9.GetActorsByType("ctnk")
				tnk2 = p9.GetActorsByType("2tnk")
				raarty = p9.GetActorsByType("raarty")
				tnk1 = p9.GetActorsByType("1tnk")
				rajeep = p9.GetActorsByType("rajeep")
				tnk4 = p9.GetActorsByType("4tnk")
				v2rl = p9.GetActorsByType("v2rl")
				tnk3 = p9.GetActorsByType("3tnk")
				ttnk = p9.GetActorsByType("ttnk")
				ftrk = p9.GetActorsByType("ftrk")
				raapc = p9.GetActorsByType("raapc")
				hpt2100 = p9.GetActorsByType("2100hpt")
				apt2100 = p9.GetActorsByType("2100apt")
				mch2100 = p9.GetActorsByType("2100mch")
				tch2100 = p9.GetActorsByType("2100tch")
				rvw2100 = p9.GetActorsByType("2100rvw")
				mvw2100 = p9.GetActorsByType("2100mvw")
				rvw22100 = p9.GetActorsByType("2100rvw2")
				mvw22100 = p9.GetActorsByType("2100mvw2")
				scgoliath = p9.GetActorsByType("scgoliath")
				scvulture = p9.GetActorsByType("scvulture")
				scsiegetank = p9.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p10 and p15.IsAlliedWith(p10) then
				htnk = p10.GetActorsByType("htnk")
				msam = p10.GetActorsByType("msam")
				mtnk = p10.GetActorsByType("mtnk")
				apc = p10.GetActorsByType("apc")
				jeep = p10.GetActorsByType("jeep")
				stnk = p10.GetActorsByType("stnk")
				arty = p10.GetActorsByType("arty")
				ltnk = p10.GetActorsByType("ltnk")
				ftnk = p10.GetActorsByType("ftnk")
				mlrs = p10.GetActorsByType("mlrs")
				bike = p10.GetActorsByType("bike")
				bggy = p10.GetActorsByType("bggy")
				ctnk = p10.GetActorsByType("ctnk")
				tnk2 = p10.GetActorsByType("2tnk")
				raarty = p10.GetActorsByType("raarty")
				tnk1 = p10.GetActorsByType("1tnk")
				rajeep = p10.GetActorsByType("rajeep")
				tnk4 = p10.GetActorsByType("4tnk")
				v2rl = p10.GetActorsByType("v2rl")
				tnk3 = p10.GetActorsByType("3tnk")
				ttnk = p10.GetActorsByType("ttnk")
				ftrk = p10.GetActorsByType("ftrk")
				raapc = p10.GetActorsByType("raapc")
				hpt2100 = p10.GetActorsByType("2100hpt")
				apt2100 = p10.GetActorsByType("2100apt")
				mch2100 = p10.GetActorsByType("2100mch")
				tch2100 = p10.GetActorsByType("2100tch")
				rvw2100 = p10.GetActorsByType("2100rvw")
				mvw2100 = p10.GetActorsByType("2100mvw")
				rvw22100 = p10.GetActorsByType("2100rvw2")
				mvw22100 = p10.GetActorsByType("2100mvw2")
				scgoliath = p10.GetActorsByType("scgoliath")
				scvulture = p10.GetActorsByType("scvulture")
				scsiegetank = p10.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p11 and p15.IsAlliedWith(p11) then
				htnk = p11.GetActorsByType("htnk")
				msam = p11.GetActorsByType("msam")
				mtnk = p11.GetActorsByType("mtnk")
				apc = p11.GetActorsByType("apc")
				jeep = p11.GetActorsByType("jeep")
				stnk = p11.GetActorsByType("stnk")
				arty = p11.GetActorsByType("arty")
				ltnk = p11.GetActorsByType("ltnk")
				ftnk = p11.GetActorsByType("ftnk")
				mlrs = p11.GetActorsByType("mlrs")
				bike = p11.GetActorsByType("bike")
				bggy = p11.GetActorsByType("bggy")
				ctnk = p11.GetActorsByType("ctnk")
				tnk2 = p11.GetActorsByType("2tnk")
				raarty = p11.GetActorsByType("raarty")
				tnk1 = p11.GetActorsByType("1tnk")
				rajeep = p11.GetActorsByType("rajeep")
				tnk4 = p11.GetActorsByType("4tnk")
				v2rl = p11.GetActorsByType("v2rl")
				tnk3 = p11.GetActorsByType("3tnk")
				ttnk = p11.GetActorsByType("ttnk")
				ftrk = p11.GetActorsByType("ftrk")
				raapc = p11.GetActorsByType("raapc")
				hpt2100 = p11.GetActorsByType("2100hpt")
				apt2100 = p11.GetActorsByType("2100apt")
				mch2100 = p11.GetActorsByType("2100mch")
				tch2100 = p11.GetActorsByType("2100tch")
				rvw2100 = p11.GetActorsByType("2100rvw")
				mvw2100 = p11.GetActorsByType("2100mvw")
				rvw22100 = p11.GetActorsByType("2100rvw2")
				mvw22100 = p11.GetActorsByType("2100mvw2")
				scgoliath = p11.GetActorsByType("scgoliath")
				scvulture = p11.GetActorsByType("scvulture")
				scsiegetank = p11.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p12 and p15.IsAlliedWith(p12) then
				htnk = p12.GetActorsByType("htnk")
				msam = p12.GetActorsByType("msam")
				mtnk = p12.GetActorsByType("mtnk")
				apc = p12.GetActorsByType("apc")
				jeep = p12.GetActorsByType("jeep")
				stnk = p12.GetActorsByType("stnk")
				arty = p12.GetActorsByType("arty")
				ltnk = p12.GetActorsByType("ltnk")
				ftnk = p12.GetActorsByType("ftnk")
				mlrs = p12.GetActorsByType("mlrs")
				bike = p12.GetActorsByType("bike")
				bggy = p12.GetActorsByType("bggy")
				ctnk = p12.GetActorsByType("ctnk")
				tnk2 = p12.GetActorsByType("2tnk")
				raarty = p12.GetActorsByType("raarty")
				tnk1 = p12.GetActorsByType("1tnk")
				rajeep = p12.GetActorsByType("rajeep")
				tnk4 = p12.GetActorsByType("4tnk")
				v2rl = p12.GetActorsByType("v2rl")
				tnk3 = p12.GetActorsByType("3tnk")
				ttnk = p12.GetActorsByType("ttnk")
				ftrk = p12.GetActorsByType("ftrk")
				raapc = p12.GetActorsByType("raapc")
				hpt2100 = p12.GetActorsByType("2100hpt")
				apt2100 = p12.GetActorsByType("2100apt")
				mch2100 = p12.GetActorsByType("2100mch")
				tch2100 = p12.GetActorsByType("2100tch")
				rvw2100 = p12.GetActorsByType("2100rvw")
				mvw2100 = p12.GetActorsByType("2100mvw")
				rvw22100 = p12.GetActorsByType("2100rvw2")
				mvw22100 = p12.GetActorsByType("2100mvw2")
				scgoliath = p12.GetActorsByType("scgoliath")
				scvulture = p12.GetActorsByType("scvulture")
				scsiegetank = p12.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p13 and p15.IsAlliedWith(p13) then
				htnk = p13.GetActorsByType("htnk")
				msam = p13.GetActorsByType("msam")
				mtnk = p13.GetActorsByType("mtnk")
				apc = p13.GetActorsByType("apc")
				jeep = p13.GetActorsByType("jeep")
				stnk = p13.GetActorsByType("stnk")
				arty = p13.GetActorsByType("arty")
				ltnk = p13.GetActorsByType("ltnk")
				ftnk = p13.GetActorsByType("ftnk")
				mlrs = p13.GetActorsByType("mlrs")
				bike = p13.GetActorsByType("bike")
				bggy = p13.GetActorsByType("bggy")
				ctnk = p13.GetActorsByType("ctnk")
				tnk2 = p13.GetActorsByType("2tnk")
				raarty = p13.GetActorsByType("raarty")
				tnk1 = p13.GetActorsByType("1tnk")
				rajeep = p13.GetActorsByType("rajeep")
				tnk4 = p13.GetActorsByType("4tnk")
				v2rl = p13.GetActorsByType("v2rl")
				tnk3 = p13.GetActorsByType("3tnk")
				ttnk = p13.GetActorsByType("ttnk")
				ftrk = p13.GetActorsByType("ftrk")
				raapc = p13.GetActorsByType("raapc")
				hpt2100 = p13.GetActorsByType("2100hpt")
				apt2100 = p13.GetActorsByType("2100apt")
				mch2100 = p13.GetActorsByType("2100mch")
				tch2100 = p13.GetActorsByType("2100tch")
				rvw2100 = p13.GetActorsByType("2100rvw")
				mvw2100 = p13.GetActorsByType("2100mvw")
				rvw22100 = p13.GetActorsByType("2100rvw2")
				mvw22100 = p13.GetActorsByType("2100mvw2")
				scgoliath = p13.GetActorsByType("scgoliath")
				scvulture = p13.GetActorsByType("scvulture")
				scsiegetank = p13.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p14 and p15.IsAlliedWith(p14) then
				htnk = p14.GetActorsByType("htnk")
				msam = p14.GetActorsByType("msam")
				mtnk = p14.GetActorsByType("mtnk")
				apc = p14.GetActorsByType("apc")
				jeep = p14.GetActorsByType("jeep")
				stnk = p14.GetActorsByType("stnk")
				arty = p14.GetActorsByType("arty")
				ltnk = p14.GetActorsByType("ltnk")
				ftnk = p14.GetActorsByType("ftnk")
				mlrs = p14.GetActorsByType("mlrs")
				bike = p14.GetActorsByType("bike")
				bggy = p14.GetActorsByType("bggy")
				ctnk = p14.GetActorsByType("ctnk")
				tnk2 = p14.GetActorsByType("2tnk")
				raarty = p14.GetActorsByType("raarty")
				tnk1 = p14.GetActorsByType("1tnk")
				rajeep = p14.GetActorsByType("rajeep")
				tnk4 = p14.GetActorsByType("4tnk")
				v2rl = p14.GetActorsByType("v2rl")
				tnk3 = p14.GetActorsByType("3tnk")
				ttnk = p14.GetActorsByType("ttnk")
				ftrk = p14.GetActorsByType("ftrk")
				raapc = p14.GetActorsByType("raapc")
				hpt2100 = p14.GetActorsByType("2100hpt")
				apt2100 = p14.GetActorsByType("2100apt")
				mch2100 = p14.GetActorsByType("2100mch")
				tch2100 = p14.GetActorsByType("2100tch")
				rvw2100 = p14.GetActorsByType("2100rvw")
				mvw2100 = p14.GetActorsByType("2100mvw")
				rvw22100 = p14.GetActorsByType("2100rvw2")
				mvw22100 = p14.GetActorsByType("2100mvw2")
				scgoliath = p14.GetActorsByType("scgoliath")
				scvulture = p14.GetActorsByType("scvulture")
				scsiegetank = p14.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p15.GetActorsByType("citypolicecar")
				cityfiretruck = p15.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p15vehicle()
	end)
end
p16vehicle = function()
	if p16 and p16.IsBot then
		cityboteliteexpert = p16.GetActorsByType("cityboteliteexpert")
		for _,expert in pairs(cityboteliteexpert) do
				-- Other Players Vehicles
			if p1 and p16.IsAlliedWith(p1) then
				htnk = p1.GetActorsByType("htnk")
				msam = p1.GetActorsByType("msam")
				mtnk = p1.GetActorsByType("mtnk")
				apc = p1.GetActorsByType("apc")
				jeep = p1.GetActorsByType("jeep")
				stnk = p1.GetActorsByType("stnk")
				arty = p1.GetActorsByType("arty")
				ltnk = p1.GetActorsByType("ltnk")
				ftnk = p1.GetActorsByType("ftnk")
				mlrs = p1.GetActorsByType("mlrs")
				bike = p1.GetActorsByType("bike")
				bggy = p1.GetActorsByType("bggy")
				ctnk = p1.GetActorsByType("ctnk")
				tnk2 = p1.GetActorsByType("2tnk")
				raarty = p1.GetActorsByType("raarty")
				tnk1 = p1.GetActorsByType("1tnk")
				rajeep = p1.GetActorsByType("rajeep")
				tnk4 = p1.GetActorsByType("4tnk")
				v2rl = p1.GetActorsByType("v2rl")
				tnk3 = p1.GetActorsByType("3tnk")
				ttnk = p1.GetActorsByType("ttnk")
				ftrk = p1.GetActorsByType("ftrk")
				raapc = p1.GetActorsByType("raapc")
				hpt2100 = p1.GetActorsByType("2100hpt")
				apt2100 = p1.GetActorsByType("2100apt")
				mch2100 = p1.GetActorsByType("2100mch")
				tch2100 = p1.GetActorsByType("2100tch")
				rvw2100 = p1.GetActorsByType("2100rvw")
				mvw2100 = p1.GetActorsByType("2100mvw")
				rvw22100 = p1.GetActorsByType("2100rvw2")
				mvw22100 = p1.GetActorsByType("2100mvw2")
				scgoliath = p1.GetActorsByType("scgoliath")
				scvulture = p1.GetActorsByType("scvulture")
				scsiegetank = p1.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p2 and p16.IsAlliedWith(p2) then
				htnk = p2.GetActorsByType("htnk")
				msam = p2.GetActorsByType("msam")
				mtnk = p2.GetActorsByType("mtnk")
				apc = p2.GetActorsByType("apc")
				jeep = p2.GetActorsByType("jeep")
				stnk = p2.GetActorsByType("stnk")
				arty = p2.GetActorsByType("arty")
				ltnk = p2.GetActorsByType("ltnk")
				ftnk = p2.GetActorsByType("ftnk")
				mlrs = p2.GetActorsByType("mlrs")
				bike = p2.GetActorsByType("bike")
				bggy = p2.GetActorsByType("bggy")
				ctnk = p2.GetActorsByType("ctnk")
				tnk2 = p2.GetActorsByType("2tnk")
				raarty = p2.GetActorsByType("raarty")
				tnk1 = p2.GetActorsByType("1tnk")
				rajeep = p2.GetActorsByType("rajeep")
				tnk4 = p2.GetActorsByType("4tnk")
				v2rl = p2.GetActorsByType("v2rl")
				tnk3 = p2.GetActorsByType("3tnk")
				ttnk = p2.GetActorsByType("ttnk")
				ftrk = p2.GetActorsByType("ftrk")
				raapc = p2.GetActorsByType("raapc")
				hpt2100 = p2.GetActorsByType("2100hpt")
				apt2100 = p2.GetActorsByType("2100apt")
				mch2100 = p2.GetActorsByType("2100mch")
				tch2100 = p2.GetActorsByType("2100tch")
				rvw2100 = p2.GetActorsByType("2100rvw")
				mvw2100 = p2.GetActorsByType("2100mvw")
				rvw22100 = p2.GetActorsByType("2100rvw2")
				mvw22100 = p2.GetActorsByType("2100mvw2")
				scgoliath = p2.GetActorsByType("scgoliath")
				scvulture = p2.GetActorsByType("scvulture")
				scsiegetank = p2.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p3 and p16.IsAlliedWith(p3) then
				htnk = p3.GetActorsByType("htnk")
				msam = p3.GetActorsByType("msam")
				mtnk = p3.GetActorsByType("mtnk")
				apc = p3.GetActorsByType("apc")
				jeep = p3.GetActorsByType("jeep")
				stnk = p3.GetActorsByType("stnk")
				arty = p3.GetActorsByType("arty")
				ltnk = p3.GetActorsByType("ltnk")
				ftnk = p3.GetActorsByType("ftnk")
				mlrs = p3.GetActorsByType("mlrs")
				bike = p3.GetActorsByType("bike")
				bggy = p3.GetActorsByType("bggy")
				ctnk = p3.GetActorsByType("ctnk")
				tnk2 = p3.GetActorsByType("2tnk")
				raarty = p3.GetActorsByType("raarty")
				tnk1 = p3.GetActorsByType("1tnk")
				rajeep = p3.GetActorsByType("rajeep")
				tnk4 = p3.GetActorsByType("4tnk")
				v2rl = p3.GetActorsByType("v2rl")
				tnk3 = p3.GetActorsByType("3tnk")
				ttnk = p3.GetActorsByType("ttnk")
				ftrk = p3.GetActorsByType("ftrk")
				raapc = p3.GetActorsByType("raapc")
				hpt2100 = p3.GetActorsByType("2100hpt")
				apt2100 = p3.GetActorsByType("2100apt")
				mch2100 = p3.GetActorsByType("2100mch")
				tch2100 = p3.GetActorsByType("2100tch")
				rvw2100 = p3.GetActorsByType("2100rvw")
				mvw2100 = p3.GetActorsByType("2100mvw")
				rvw22100 = p3.GetActorsByType("2100rvw2")
				mvw22100 = p3.GetActorsByType("2100mvw2")
				scgoliath = p3.GetActorsByType("scgoliath")
				scvulture = p3.GetActorsByType("scvulture")
				scsiegetank = p3.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p4 and p16.IsAlliedWith(p4) then
				htnk = p4.GetActorsByType("htnk")
				msam = p4.GetActorsByType("msam")
				mtnk = p4.GetActorsByType("mtnk")
				apc = p4.GetActorsByType("apc")
				jeep = p4.GetActorsByType("jeep")
				stnk = p4.GetActorsByType("stnk")
				arty = p4.GetActorsByType("arty")
				ltnk = p4.GetActorsByType("ltnk")
				ftnk = p4.GetActorsByType("ftnk")
				mlrs = p4.GetActorsByType("mlrs")
				bike = p4.GetActorsByType("bike")
				bggy = p4.GetActorsByType("bggy")
				ctnk = p4.GetActorsByType("ctnk")
				tnk2 = p4.GetActorsByType("2tnk")
				raarty = p4.GetActorsByType("raarty")
				tnk1 = p4.GetActorsByType("1tnk")
				rajeep = p4.GetActorsByType("rajeep")
				tnk4 = p4.GetActorsByType("4tnk")
				v2rl = p4.GetActorsByType("v2rl")
				tnk3 = p4.GetActorsByType("3tnk")
				ttnk = p4.GetActorsByType("ttnk")
				ftrk = p4.GetActorsByType("ftrk")
				raapc = p4.GetActorsByType("raapc")
				hpt2100 = p4.GetActorsByType("2100hpt")
				apt2100 = p4.GetActorsByType("2100apt")
				mch2100 = p4.GetActorsByType("2100mch")
				tch2100 = p4.GetActorsByType("2100tch")
				rvw2100 = p4.GetActorsByType("2100rvw")
				mvw2100 = p4.GetActorsByType("2100mvw")
				rvw22100 = p4.GetActorsByType("2100rvw2")
				mvw22100 = p4.GetActorsByType("2100mvw2")
				scgoliath = p4.GetActorsByType("scgoliath")
				scvulture = p4.GetActorsByType("scvulture")
				scsiegetank = p4.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p5 and p16.IsAlliedWith(p5) then
				htnk = p5.GetActorsByType("htnk")
				msam = p5.GetActorsByType("msam")
				mtnk = p5.GetActorsByType("mtnk")
				apc = p5.GetActorsByType("apc")
				jeep = p5.GetActorsByType("jeep")
				stnk = p5.GetActorsByType("stnk")
				arty = p5.GetActorsByType("arty")
				ltnk = p5.GetActorsByType("ltnk")
				ftnk = p5.GetActorsByType("ftnk")
				mlrs = p5.GetActorsByType("mlrs")
				bike = p5.GetActorsByType("bike")
				bggy = p5.GetActorsByType("bggy")
				ctnk = p5.GetActorsByType("ctnk")
				tnk2 = p5.GetActorsByType("2tnk")
				raarty = p5.GetActorsByType("raarty")
				tnk1 = p5.GetActorsByType("1tnk")
				rajeep = p5.GetActorsByType("rajeep")
				tnk4 = p5.GetActorsByType("4tnk")
				v2rl = p5.GetActorsByType("v2rl")
				tnk3 = p5.GetActorsByType("3tnk")
				ttnk = p5.GetActorsByType("ttnk")
				ftrk = p5.GetActorsByType("ftrk")
				raapc = p5.GetActorsByType("raapc")
				hpt2100 = p5.GetActorsByType("2100hpt")
				apt2100 = p5.GetActorsByType("2100apt")
				mch2100 = p5.GetActorsByType("2100mch")
				tch2100 = p5.GetActorsByType("2100tch")
				rvw2100 = p5.GetActorsByType("2100rvw")
				mvw2100 = p5.GetActorsByType("2100mvw")
				rvw22100 = p5.GetActorsByType("2100rvw2")
				mvw22100 = p5.GetActorsByType("2100mvw2")
				scgoliath = p5.GetActorsByType("scgoliath")
				scvulture = p5.GetActorsByType("scvulture")
				scsiegetank = p5.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p6 and p16.IsAlliedWith(p6) then
				htnk = p6.GetActorsByType("htnk")
				msam = p6.GetActorsByType("msam")
				mtnk = p6.GetActorsByType("mtnk")
				apc = p6.GetActorsByType("apc")
				jeep = p6.GetActorsByType("jeep")
				stnk = p6.GetActorsByType("stnk")
				arty = p6.GetActorsByType("arty")
				ltnk = p6.GetActorsByType("ltnk")
				ftnk = p6.GetActorsByType("ftnk")
				mlrs = p6.GetActorsByType("mlrs")
				bike = p6.GetActorsByType("bike")
				bggy = p6.GetActorsByType("bggy")
				ctnk = p6.GetActorsByType("ctnk")
				tnk2 = p6.GetActorsByType("2tnk")
				raarty = p6.GetActorsByType("raarty")
				tnk1 = p6.GetActorsByType("1tnk")
				rajeep = p6.GetActorsByType("rajeep")
				tnk4 = p6.GetActorsByType("4tnk")
				v2rl = p6.GetActorsByType("v2rl")
				tnk3 = p6.GetActorsByType("3tnk")
				ttnk = p6.GetActorsByType("ttnk")
				ftrk = p6.GetActorsByType("ftrk")
				raapc = p6.GetActorsByType("raapc")
				hpt2100 = p6.GetActorsByType("2100hpt")
				apt2100 = p6.GetActorsByType("2100apt")
				mch2100 = p6.GetActorsByType("2100mch")
				tch2100 = p6.GetActorsByType("2100tch")
				rvw2100 = p6.GetActorsByType("2100rvw")
				mvw2100 = p6.GetActorsByType("2100mvw")
				rvw22100 = p6.GetActorsByType("2100rvw2")
				mvw22100 = p6.GetActorsByType("2100mvw2")
				scgoliath = p6.GetActorsByType("scgoliath")
				scvulture = p6.GetActorsByType("scvulture")
				scsiegetank = p6.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p7 and p16.IsAlliedWith(p7) then
				htnk = p7.GetActorsByType("htnk")
				msam = p7.GetActorsByType("msam")
				mtnk = p7.GetActorsByType("mtnk")
				apc = p7.GetActorsByType("apc")
				jeep = p7.GetActorsByType("jeep")
				stnk = p7.GetActorsByType("stnk")
				arty = p7.GetActorsByType("arty")
				ltnk = p7.GetActorsByType("ltnk")
				ftnk = p7.GetActorsByType("ftnk")
				mlrs = p7.GetActorsByType("mlrs")
				bike = p7.GetActorsByType("bike")
				bggy = p7.GetActorsByType("bggy")
				ctnk = p7.GetActorsByType("ctnk")
				tnk2 = p7.GetActorsByType("2tnk")
				raarty = p7.GetActorsByType("raarty")
				tnk1 = p7.GetActorsByType("1tnk")
				rajeep = p7.GetActorsByType("rajeep")
				tnk4 = p7.GetActorsByType("4tnk")
				v2rl = p7.GetActorsByType("v2rl")
				tnk3 = p7.GetActorsByType("3tnk")
				ttnk = p7.GetActorsByType("ttnk")
				ftrk = p7.GetActorsByType("ftrk")
				raapc = p7.GetActorsByType("raapc")
				hpt2100 = p7.GetActorsByType("2100hpt")
				apt2100 = p7.GetActorsByType("2100apt")
				mch2100 = p7.GetActorsByType("2100mch")
				tch2100 = p7.GetActorsByType("2100tch")
				rvw2100 = p7.GetActorsByType("2100rvw")
				mvw2100 = p7.GetActorsByType("2100mvw")
				rvw22100 = p7.GetActorsByType("2100rvw2")
				mvw22100 = p7.GetActorsByType("2100mvw2")
				scgoliath = p7.GetActorsByType("scgoliath")
				scvulture = p7.GetActorsByType("scvulture")
				scsiegetank = p7.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p8 and p16.IsAlliedWith(p8) then
				htnk = p8.GetActorsByType("htnk")
				msam = p8.GetActorsByType("msam")
				mtnk = p8.GetActorsByType("mtnk")
				apc = p8.GetActorsByType("apc")
				jeep = p8.GetActorsByType("jeep")
				stnk = p8.GetActorsByType("stnk")
				arty = p8.GetActorsByType("arty")
				ltnk = p8.GetActorsByType("ltnk")
				ftnk = p8.GetActorsByType("ftnk")
				mlrs = p8.GetActorsByType("mlrs")
				bike = p8.GetActorsByType("bike")
				bggy = p8.GetActorsByType("bggy")
				ctnk = p8.GetActorsByType("ctnk")
				tnk2 = p8.GetActorsByType("2tnk")
				raarty = p8.GetActorsByType("raarty")
				tnk1 = p8.GetActorsByType("1tnk")
				rajeep = p8.GetActorsByType("rajeep")
				tnk4 = p8.GetActorsByType("4tnk")
				v2rl = p8.GetActorsByType("v2rl")
				tnk3 = p8.GetActorsByType("3tnk")
				ttnk = p8.GetActorsByType("ttnk")
				ftrk = p8.GetActorsByType("ftrk")
				raapc = p8.GetActorsByType("raapc")
				hpt2100 = p8.GetActorsByType("2100hpt")
				apt2100 = p8.GetActorsByType("2100apt")
				mch2100 = p8.GetActorsByType("2100mch")
				tch2100 = p8.GetActorsByType("2100tch")
				rvw2100 = p8.GetActorsByType("2100rvw")
				mvw2100 = p8.GetActorsByType("2100mvw")
				rvw22100 = p8.GetActorsByType("2100rvw2")
				mvw22100 = p8.GetActorsByType("2100mvw2")
				scgoliath = p8.GetActorsByType("scgoliath")
				scvulture = p8.GetActorsByType("scvulture")
				scsiegetank = p8.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p9 and p16.IsAlliedWith(p9) then
				htnk = p9.GetActorsByType("htnk")
				msam = p9.GetActorsByType("msam")
				mtnk = p9.GetActorsByType("mtnk")
				apc = p9.GetActorsByType("apc")
				jeep = p9.GetActorsByType("jeep")
				stnk = p9.GetActorsByType("stnk")
				arty = p9.GetActorsByType("arty")
				ltnk = p9.GetActorsByType("ltnk")
				ftnk = p9.GetActorsByType("ftnk")
				mlrs = p9.GetActorsByType("mlrs")
				bike = p9.GetActorsByType("bike")
				bggy = p9.GetActorsByType("bggy")
				ctnk = p9.GetActorsByType("ctnk")
				tnk2 = p9.GetActorsByType("2tnk")
				raarty = p9.GetActorsByType("raarty")
				tnk1 = p9.GetActorsByType("1tnk")
				rajeep = p9.GetActorsByType("rajeep")
				tnk4 = p9.GetActorsByType("4tnk")
				v2rl = p9.GetActorsByType("v2rl")
				tnk3 = p9.GetActorsByType("3tnk")
				ttnk = p9.GetActorsByType("ttnk")
				ftrk = p9.GetActorsByType("ftrk")
				raapc = p9.GetActorsByType("raapc")
				hpt2100 = p9.GetActorsByType("2100hpt")
				apt2100 = p9.GetActorsByType("2100apt")
				mch2100 = p9.GetActorsByType("2100mch")
				tch2100 = p9.GetActorsByType("2100tch")
				rvw2100 = p9.GetActorsByType("2100rvw")
				mvw2100 = p9.GetActorsByType("2100mvw")
				rvw22100 = p9.GetActorsByType("2100rvw2")
				mvw22100 = p9.GetActorsByType("2100mvw2")
				scgoliath = p9.GetActorsByType("scgoliath")
				scvulture = p9.GetActorsByType("scvulture")
				scsiegetank = p9.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p10 and p16.IsAlliedWith(p10) then
				htnk = p10.GetActorsByType("htnk")
				msam = p10.GetActorsByType("msam")
				mtnk = p10.GetActorsByType("mtnk")
				apc = p10.GetActorsByType("apc")
				jeep = p10.GetActorsByType("jeep")
				stnk = p10.GetActorsByType("stnk")
				arty = p10.GetActorsByType("arty")
				ltnk = p10.GetActorsByType("ltnk")
				ftnk = p10.GetActorsByType("ftnk")
				mlrs = p10.GetActorsByType("mlrs")
				bike = p10.GetActorsByType("bike")
				bggy = p10.GetActorsByType("bggy")
				ctnk = p10.GetActorsByType("ctnk")
				tnk2 = p10.GetActorsByType("2tnk")
				raarty = p10.GetActorsByType("raarty")
				tnk1 = p10.GetActorsByType("1tnk")
				rajeep = p10.GetActorsByType("rajeep")
				tnk4 = p10.GetActorsByType("4tnk")
				v2rl = p10.GetActorsByType("v2rl")
				tnk3 = p10.GetActorsByType("3tnk")
				ttnk = p10.GetActorsByType("ttnk")
				ftrk = p10.GetActorsByType("ftrk")
				raapc = p10.GetActorsByType("raapc")
				hpt2100 = p10.GetActorsByType("2100hpt")
				apt2100 = p10.GetActorsByType("2100apt")
				mch2100 = p10.GetActorsByType("2100mch")
				tch2100 = p10.GetActorsByType("2100tch")
				rvw2100 = p10.GetActorsByType("2100rvw")
				mvw2100 = p10.GetActorsByType("2100mvw")
				rvw22100 = p10.GetActorsByType("2100rvw2")
				mvw22100 = p10.GetActorsByType("2100mvw2")
				scgoliath = p10.GetActorsByType("scgoliath")
				scvulture = p10.GetActorsByType("scvulture")
				scsiegetank = p10.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p11 and p16.IsAlliedWith(p11) then
				htnk = p11.GetActorsByType("htnk")
				msam = p11.GetActorsByType("msam")
				mtnk = p11.GetActorsByType("mtnk")
				apc = p11.GetActorsByType("apc")
				jeep = p11.GetActorsByType("jeep")
				stnk = p11.GetActorsByType("stnk")
				arty = p11.GetActorsByType("arty")
				ltnk = p11.GetActorsByType("ltnk")
				ftnk = p11.GetActorsByType("ftnk")
				mlrs = p11.GetActorsByType("mlrs")
				bike = p11.GetActorsByType("bike")
				bggy = p11.GetActorsByType("bggy")
				ctnk = p11.GetActorsByType("ctnk")
				tnk2 = p11.GetActorsByType("2tnk")
				raarty = p11.GetActorsByType("raarty")
				tnk1 = p11.GetActorsByType("1tnk")
				rajeep = p11.GetActorsByType("rajeep")
				tnk4 = p11.GetActorsByType("4tnk")
				v2rl = p11.GetActorsByType("v2rl")
				tnk3 = p11.GetActorsByType("3tnk")
				ttnk = p11.GetActorsByType("ttnk")
				ftrk = p11.GetActorsByType("ftrk")
				raapc = p11.GetActorsByType("raapc")
				hpt2100 = p11.GetActorsByType("2100hpt")
				apt2100 = p11.GetActorsByType("2100apt")
				mch2100 = p11.GetActorsByType("2100mch")
				tch2100 = p11.GetActorsByType("2100tch")
				rvw2100 = p11.GetActorsByType("2100rvw")
				mvw2100 = p11.GetActorsByType("2100mvw")
				rvw22100 = p11.GetActorsByType("2100rvw2")
				mvw22100 = p11.GetActorsByType("2100mvw2")
				scgoliath = p11.GetActorsByType("scgoliath")
				scvulture = p11.GetActorsByType("scvulture")
				scsiegetank = p11.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p12 and p16.IsAlliedWith(p12) then
				htnk = p12.GetActorsByType("htnk")
				msam = p12.GetActorsByType("msam")
				mtnk = p12.GetActorsByType("mtnk")
				apc = p12.GetActorsByType("apc")
				jeep = p12.GetActorsByType("jeep")
				stnk = p12.GetActorsByType("stnk")
				arty = p12.GetActorsByType("arty")
				ltnk = p12.GetActorsByType("ltnk")
				ftnk = p12.GetActorsByType("ftnk")
				mlrs = p12.GetActorsByType("mlrs")
				bike = p12.GetActorsByType("bike")
				bggy = p12.GetActorsByType("bggy")
				ctnk = p12.GetActorsByType("ctnk")
				tnk2 = p12.GetActorsByType("2tnk")
				raarty = p12.GetActorsByType("raarty")
				tnk1 = p12.GetActorsByType("1tnk")
				rajeep = p12.GetActorsByType("rajeep")
				tnk4 = p12.GetActorsByType("4tnk")
				v2rl = p12.GetActorsByType("v2rl")
				tnk3 = p12.GetActorsByType("3tnk")
				ttnk = p12.GetActorsByType("ttnk")
				ftrk = p12.GetActorsByType("ftrk")
				raapc = p12.GetActorsByType("raapc")
				hpt2100 = p12.GetActorsByType("2100hpt")
				apt2100 = p12.GetActorsByType("2100apt")
				mch2100 = p12.GetActorsByType("2100mch")
				tch2100 = p12.GetActorsByType("2100tch")
				rvw2100 = p12.GetActorsByType("2100rvw")
				mvw2100 = p12.GetActorsByType("2100mvw")
				rvw22100 = p12.GetActorsByType("2100rvw2")
				mvw22100 = p12.GetActorsByType("2100mvw2")
				scgoliath = p12.GetActorsByType("scgoliath")
				scvulture = p12.GetActorsByType("scvulture")
				scsiegetank = p12.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p13 and p16.IsAlliedWith(p13) then
				htnk = p13.GetActorsByType("htnk")
				msam = p13.GetActorsByType("msam")
				mtnk = p13.GetActorsByType("mtnk")
				apc = p13.GetActorsByType("apc")
				jeep = p13.GetActorsByType("jeep")
				stnk = p13.GetActorsByType("stnk")
				arty = p13.GetActorsByType("arty")
				ltnk = p13.GetActorsByType("ltnk")
				ftnk = p13.GetActorsByType("ftnk")
				mlrs = p13.GetActorsByType("mlrs")
				bike = p13.GetActorsByType("bike")
				bggy = p13.GetActorsByType("bggy")
				ctnk = p13.GetActorsByType("ctnk")
				tnk2 = p13.GetActorsByType("2tnk")
				raarty = p13.GetActorsByType("raarty")
				tnk1 = p13.GetActorsByType("1tnk")
				rajeep = p13.GetActorsByType("rajeep")
				tnk4 = p13.GetActorsByType("4tnk")
				v2rl = p13.GetActorsByType("v2rl")
				tnk3 = p13.GetActorsByType("3tnk")
				ttnk = p13.GetActorsByType("ttnk")
				ftrk = p13.GetActorsByType("ftrk")
				raapc = p13.GetActorsByType("raapc")
				hpt2100 = p13.GetActorsByType("2100hpt")
				apt2100 = p13.GetActorsByType("2100apt")
				mch2100 = p13.GetActorsByType("2100mch")
				tch2100 = p13.GetActorsByType("2100tch")
				rvw2100 = p13.GetActorsByType("2100rvw")
				mvw2100 = p13.GetActorsByType("2100mvw")
				rvw22100 = p13.GetActorsByType("2100rvw2")
				mvw22100 = p13.GetActorsByType("2100mvw2")
				scgoliath = p13.GetActorsByType("scgoliath")
				scvulture = p13.GetActorsByType("scvulture")
				scsiegetank = p13.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p14 and p16.IsAlliedWith(p14) then
				htnk = p14.GetActorsByType("htnk")
				msam = p14.GetActorsByType("msam")
				mtnk = p14.GetActorsByType("mtnk")
				apc = p14.GetActorsByType("apc")
				jeep = p14.GetActorsByType("jeep")
				stnk = p14.GetActorsByType("stnk")
				arty = p14.GetActorsByType("arty")
				ltnk = p14.GetActorsByType("ltnk")
				ftnk = p14.GetActorsByType("ftnk")
				mlrs = p14.GetActorsByType("mlrs")
				bike = p14.GetActorsByType("bike")
				bggy = p14.GetActorsByType("bggy")
				ctnk = p14.GetActorsByType("ctnk")
				tnk2 = p14.GetActorsByType("2tnk")
				raarty = p14.GetActorsByType("raarty")
				tnk1 = p14.GetActorsByType("1tnk")
				rajeep = p14.GetActorsByType("rajeep")
				tnk4 = p14.GetActorsByType("4tnk")
				v2rl = p14.GetActorsByType("v2rl")
				tnk3 = p14.GetActorsByType("3tnk")
				ttnk = p14.GetActorsByType("ttnk")
				ftrk = p14.GetActorsByType("ftrk")
				raapc = p14.GetActorsByType("raapc")
				hpt2100 = p14.GetActorsByType("2100hpt")
				apt2100 = p14.GetActorsByType("2100apt")
				mch2100 = p14.GetActorsByType("2100mch")
				tch2100 = p14.GetActorsByType("2100tch")
				rvw2100 = p14.GetActorsByType("2100rvw")
				mvw2100 = p14.GetActorsByType("2100mvw")
				rvw22100 = p14.GetActorsByType("2100rvw2")
				mvw22100 = p14.GetActorsByType("2100mvw2")
				scgoliath = p14.GetActorsByType("scgoliath")
				scvulture = p14.GetActorsByType("scvulture")
				scsiegetank = p14.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
			if p15 and p16.IsAlliedWith(p15) then
				htnk = p15.GetActorsByType("htnk")
				msam = p15.GetActorsByType("msam")
				mtnk = p15.GetActorsByType("mtnk")
				apc = p15.GetActorsByType("apc")
				jeep = p15.GetActorsByType("jeep")
				stnk = p15.GetActorsByType("stnk")
				arty = p15.GetActorsByType("arty")
				ltnk = p15.GetActorsByType("ltnk")
				ftnk = p15.GetActorsByType("ftnk")
				mlrs = p15.GetActorsByType("mlrs")
				bike = p15.GetActorsByType("bike")
				bggy = p15.GetActorsByType("bggy")
				ctnk = p15.GetActorsByType("ctnk")
				tnk2 = p15.GetActorsByType("2tnk")
				raarty = p15.GetActorsByType("raarty")
				tnk1 = p15.GetActorsByType("1tnk")
				rajeep = p15.GetActorsByType("rajeep")
				tnk4 = p15.GetActorsByType("4tnk")
				v2rl = p15.GetActorsByType("v2rl")
				tnk3 = p15.GetActorsByType("3tnk")
				ttnk = p15.GetActorsByType("ttnk")
				ftrk = p15.GetActorsByType("ftrk")
				raapc = p15.GetActorsByType("raapc")
				hpt2100 = p15.GetActorsByType("2100hpt")
				apt2100 = p15.GetActorsByType("2100apt")
				mch2100 = p15.GetActorsByType("2100mch")
				tch2100 = p15.GetActorsByType("2100tch")
				rvw2100 = p15.GetActorsByType("2100rvw")
				mvw2100 = p15.GetActorsByType("2100mvw")
				rvw22100 = p15.GetActorsByType("2100rvw2")
				mvw22100 = p15.GetActorsByType("2100mvw2")
				scgoliath = p15.GetActorsByType("scgoliath")
				scvulture = p15.GetActorsByType("scvulture")
				scsiegetank = p15.GetActorsByType("scsiegetank")
				for _,htnk in pairs(htnk) do expert.DeliverExperience(htnk) end
				for _,msam in pairs(msam) do expert.DeliverExperience(msam) end
				for _,mtnk in pairs(mtnk) do expert.DeliverExperience(mtnk) end
				for _,apc in pairs(apc) do expert.DeliverExperience(apc) end
				for _,jeep in pairs(jeep) do expert.DeliverExperience(jeep) end
				for _,stnk in pairs(stnk) do expert.DeliverExperience(stnk) end
				for _,arty in pairs(arty) do expert.DeliverExperience(arty) end
				for _,ltnk in pairs(ltnk) do expert.DeliverExperience(ltnk) end
				for _,ftnk in pairs(ftnk) do expert.DeliverExperience(ftnk) end
				for _,mlrs in pairs(mlrs) do expert.DeliverExperience(mlrs) end
				for _,bike in pairs(bike) do expert.DeliverExperience(bike) end
				for _,bggy in pairs(bggy) do expert.DeliverExperience(bggy) end
				for _,ctnk in pairs(ctnk) do expert.DeliverExperience(ctnk) end
				for _,tnk2 in pairs(tnk2) do expert.DeliverExperience(tnk2) end
				for _,raarty in pairs(raarty) do expert.DeliverExperience(raarty) end
				for _,tnk1 in pairs(tnk1) do expert.DeliverExperience(tnk1) end
				for _,rajeep in pairs(rajeep) do expert.DeliverExperience(rajeep) end
				for _,tnk4 in pairs(tnk4) do expert.DeliverExperience(tnk4) end
				for _,v2rl in pairs(v2rl) do expert.DeliverExperience(v2rl) end
				for _,tnk3 in pairs(tnk3) do expert.DeliverExperience(tnk3) end
				for _,ttnk in pairs(ttnk) do expert.DeliverExperience(ttnk) end
				for _,ftrk in pairs(ftrk) do expert.DeliverExperience(ftrk) end
				for _,raapc in pairs(raapc) do expert.DeliverExperience(raapc) end
				for _,hpt2100 in pairs(hpt2100) do expert.DeliverExperience(hpt2100) end
				for _,apt2100 in pairs(apt2100) do expert.DeliverExperience(apt2100) end
				for _,mch2100 in pairs(mch2100) do expert.DeliverExperience(mch2100) end
				for _,tch2100 in pairs(tch2100) do expert.DeliverExperience(tch2100) end
				for _,rvw2100 in pairs(rvw2100) do expert.DeliverExperience(rvw2100) end
				for _,mvw2100 in pairs(mvw2100) do expert.DeliverExperience(mvw2100) end
				for _,rvw22100 in pairs(rvw22100) do expert.DeliverExperience(rvw22100) end
				for _,mvw22100 in pairs(mvw22100) do expert.DeliverExperience(mvw22100) end
				for _,scgoliath in pairs(scgoliath) do expert.DeliverExperience(scgoliath) end
				for _,scvulture in pairs(scvulture) do expert.DeliverExperience(scvulture) end
				for _,scsiegetank in pairs(scsiegetank) do expert.DeliverExperience(scsiegetank) end
			end
				-- Player own Vehicles
				citypolicecar = p16.GetActorsByType("citypolicecar")
				cityfiretruck = p16.GetActorsByType("cityfiretruck")
				for _,citypolicecar in pairs(citypolicecar) do expert.DeliverExperience(citypolicecar) end
				for _,cityfiretruck in pairs(cityfiretruck) do expert.DeliverExperience(cityfiretruck) end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(7), function()
		p16vehicle()
	end)
end

p1infantry = function()
	if p1 and p1.IsBot then
		cityboteliteinstructor = p1.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p2 and p1.IsAlliedWith(p2) then
				rmbo = p2.GetActorsByType("rmbo")
				e3 = p2.GetActorsByType("e3")
				e2 = p2.GetActorsByType("e2")
				e4 = p2.GetActorsByType("e4")
				e5 = p2.GetActorsByType("e5")
				e1 = p2.GetActorsByType("e1")
				e7 = p2.GetActorsByType("e7")
				shok = p2.GetActorsByType("shok")
				rae4 = p2.GetActorsByType("rae4")
				rae3 = p2.GetActorsByType("rae3")
				rae2 = p2.GetActorsByType("rae2")
				rae1 = p2.GetActorsByType("rae1")
				dog = p2.GetActorsByType("dog")
				scghost = p2.GetActorsByType("scghost")
				scfirebat = p2.GetActorsByType("scfirebat")
				scmarine = p2.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p3 and p1.IsAlliedWith(p3) then
				rmbo = p3.GetActorsByType("rmbo")
				e3 = p3.GetActorsByType("e3")
				e2 = p3.GetActorsByType("e2")
				e4 = p3.GetActorsByType("e4")
				e5 = p3.GetActorsByType("e5")
				e1 = p3.GetActorsByType("e1")
				e7 = p3.GetActorsByType("e7")
				shok = p3.GetActorsByType("shok")
				rae4 = p3.GetActorsByType("rae4")
				rae3 = p3.GetActorsByType("rae3")
				rae2 = p3.GetActorsByType("rae2")
				rae1 = p3.GetActorsByType("rae1")
				dog = p3.GetActorsByType("dog")
				scghost = p3.GetActorsByType("scghost")
				scfirebat = p3.GetActorsByType("scfirebat")
				scmarine = p3.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p4 and p1.IsAlliedWith(p4) then
				rmbo = p4.GetActorsByType("rmbo")
				e3 = p4.GetActorsByType("e3")
				e2 = p4.GetActorsByType("e2")
				e4 = p4.GetActorsByType("e4")
				e5 = p4.GetActorsByType("e5")
				e1 = p4.GetActorsByType("e1")
				e7 = p4.GetActorsByType("e7")
				shok = p4.GetActorsByType("shok")
				rae4 = p4.GetActorsByType("rae4")
				rae3 = p4.GetActorsByType("rae3")
				rae2 = p4.GetActorsByType("rae2")
				rae1 = p4.GetActorsByType("rae1")
				dog = p4.GetActorsByType("dog")
				scghost = p4.GetActorsByType("scghost")
				scfirebat = p4.GetActorsByType("scfirebat")
				scmarine = p4.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p5 and p1.IsAlliedWith(p5) then
				rmbo = p5.GetActorsByType("rmbo")
				e3 = p5.GetActorsByType("e3")
				e2 = p5.GetActorsByType("e2")
				e4 = p5.GetActorsByType("e4")
				e5 = p5.GetActorsByType("e5")
				e1 = p5.GetActorsByType("e1")
				e7 = p5.GetActorsByType("e7")
				shok = p5.GetActorsByType("shok")
				rae4 = p5.GetActorsByType("rae4")
				rae3 = p5.GetActorsByType("rae3")
				rae2 = p5.GetActorsByType("rae2")
				rae1 = p5.GetActorsByType("rae1")
				dog = p5.GetActorsByType("dog")
				scghost = p5.GetActorsByType("scghost")
				scfirebat = p5.GetActorsByType("scfirebat")
				scmarine = p5.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p6 and p1.IsAlliedWith(p6) then
				rmbo = p6.GetActorsByType("rmbo")
				e3 = p6.GetActorsByType("e3")
				e2 = p6.GetActorsByType("e2")
				e4 = p6.GetActorsByType("e4")
				e5 = p6.GetActorsByType("e5")
				e1 = p6.GetActorsByType("e1")
				e7 = p6.GetActorsByType("e7")
				shok = p6.GetActorsByType("shok")
				rae4 = p6.GetActorsByType("rae4")
				rae3 = p6.GetActorsByType("rae3")
				rae2 = p6.GetActorsByType("rae2")
				rae1 = p6.GetActorsByType("rae1")
				dog = p6.GetActorsByType("dog")
				scghost = p6.GetActorsByType("scghost")
				scfirebat = p6.GetActorsByType("scfirebat")
				scmarine = p6.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p7 and p1.IsAlliedWith(p7) then
				rmbo = p7.GetActorsByType("rmbo")
				e3 = p7.GetActorsByType("e3")
				e2 = p7.GetActorsByType("e2")
				e4 = p7.GetActorsByType("e4")
				e5 = p7.GetActorsByType("e5")
				e1 = p7.GetActorsByType("e1")
				e7 = p7.GetActorsByType("e7")
				shok = p7.GetActorsByType("shok")
				rae4 = p7.GetActorsByType("rae4")
				rae3 = p7.GetActorsByType("rae3")
				rae2 = p7.GetActorsByType("rae2")
				rae1 = p7.GetActorsByType("rae1")
				dog = p7.GetActorsByType("dog")
				scghost = p7.GetActorsByType("scghost")
				scfirebat = p7.GetActorsByType("scfirebat")
				scmarine = p7.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p8 and p1.IsAlliedWith(p8) then
				rmbo = p8.GetActorsByType("rmbo")
				e3 = p8.GetActorsByType("e3")
				e2 = p8.GetActorsByType("e2")
				e4 = p8.GetActorsByType("e4")
				e5 = p8.GetActorsByType("e5")
				e1 = p8.GetActorsByType("e1")
				e7 = p8.GetActorsByType("e7")
				shok = p8.GetActorsByType("shok")
				rae4 = p8.GetActorsByType("rae4")
				rae3 = p8.GetActorsByType("rae3")
				rae2 = p8.GetActorsByType("rae2")
				rae1 = p8.GetActorsByType("rae1")
				dog = p8.GetActorsByType("dog")
				scghost = p8.GetActorsByType("scghost")
				scfirebat = p8.GetActorsByType("scfirebat")
				scmarine = p8.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p9 and p1.IsAlliedWith(p9) then
				rmbo = p9.GetActorsByType("rmbo")
				e3 = p9.GetActorsByType("e3")
				e2 = p9.GetActorsByType("e2")
				e4 = p9.GetActorsByType("e4")
				e5 = p9.GetActorsByType("e5")
				e1 = p9.GetActorsByType("e1")
				e7 = p9.GetActorsByType("e7")
				shok = p9.GetActorsByType("shok")
				rae4 = p9.GetActorsByType("rae4")
				rae3 = p9.GetActorsByType("rae3")
				rae2 = p9.GetActorsByType("rae2")
				rae1 = p9.GetActorsByType("rae1")
				dog = p9.GetActorsByType("dog")
				scghost = p9.GetActorsByType("scghost")
				scfirebat = p9.GetActorsByType("scfirebat")
				scmarine = p9.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p10 and p1.IsAlliedWith(p10) then
				rmbo = p10.GetActorsByType("rmbo")
				e3 = p10.GetActorsByType("e3")
				e2 = p10.GetActorsByType("e2")
				e4 = p10.GetActorsByType("e4")
				e5 = p10.GetActorsByType("e5")
				e1 = p10.GetActorsByType("e1")
				e7 = p10.GetActorsByType("e7")
				shok = p10.GetActorsByType("shok")
				rae4 = p10.GetActorsByType("rae4")
				rae3 = p10.GetActorsByType("rae3")
				rae2 = p10.GetActorsByType("rae2")
				rae1 = p10.GetActorsByType("rae1")
				dog = p10.GetActorsByType("dog")
				scghost = p10.GetActorsByType("scghost")
				scfirebat = p10.GetActorsByType("scfirebat")
				scmarine = p10.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p11 and p1.IsAlliedWith(p11) then
				rmbo = p11.GetActorsByType("rmbo")
				e3 = p11.GetActorsByType("e3")
				e2 = p11.GetActorsByType("e2")
				e4 = p11.GetActorsByType("e4")
				e5 = p11.GetActorsByType("e5")
				e1 = p11.GetActorsByType("e1")
				e7 = p11.GetActorsByType("e7")
				shok = p11.GetActorsByType("shok")
				rae4 = p11.GetActorsByType("rae4")
				rae3 = p11.GetActorsByType("rae3")
				rae2 = p11.GetActorsByType("rae2")
				rae1 = p11.GetActorsByType("rae1")
				dog = p11.GetActorsByType("dog")
				scghost = p11.GetActorsByType("scghost")
				scfirebat = p11.GetActorsByType("scfirebat")
				scmarine = p11.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p12 and p1.IsAlliedWith(p12) then
				rmbo = p12.GetActorsByType("rmbo")
				e3 = p12.GetActorsByType("e3")
				e2 = p12.GetActorsByType("e2")
				e4 = p12.GetActorsByType("e4")
				e5 = p12.GetActorsByType("e5")
				e1 = p12.GetActorsByType("e1")
				e7 = p12.GetActorsByType("e7")
				shok = p12.GetActorsByType("shok")
				rae4 = p12.GetActorsByType("rae4")
				rae3 = p12.GetActorsByType("rae3")
				rae2 = p12.GetActorsByType("rae2")
				rae1 = p12.GetActorsByType("rae1")
				dog = p12.GetActorsByType("dog")
				scghost = p12.GetActorsByType("scghost")
				scfirebat = p12.GetActorsByType("scfirebat")
				scmarine = p12.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p13 and p1.IsAlliedWith(p13) then
				rmbo = p13.GetActorsByType("rmbo")
				e3 = p13.GetActorsByType("e3")
				e2 = p13.GetActorsByType("e2")
				e4 = p13.GetActorsByType("e4")
				e5 = p13.GetActorsByType("e5")
				e1 = p13.GetActorsByType("e1")
				e7 = p13.GetActorsByType("e7")
				shok = p13.GetActorsByType("shok")
				rae4 = p13.GetActorsByType("rae4")
				rae3 = p13.GetActorsByType("rae3")
				rae2 = p13.GetActorsByType("rae2")
				rae1 = p13.GetActorsByType("rae1")
				dog = p13.GetActorsByType("dog")
				scghost = p13.GetActorsByType("scghost")
				scfirebat = p13.GetActorsByType("scfirebat")
				scmarine = p13.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p14 and p1.IsAlliedWith(p14) then
				rmbo = p14.GetActorsByType("rmbo")
				e3 = p14.GetActorsByType("e3")
				e2 = p14.GetActorsByType("e2")
				e4 = p14.GetActorsByType("e4")
				e5 = p14.GetActorsByType("e5")
				e1 = p14.GetActorsByType("e1")
				e7 = p14.GetActorsByType("e7")
				shok = p14.GetActorsByType("shok")
				rae4 = p14.GetActorsByType("rae4")
				rae3 = p14.GetActorsByType("rae3")
				rae2 = p14.GetActorsByType("rae2")
				rae1 = p14.GetActorsByType("rae1")
				dog = p14.GetActorsByType("dog")
				scghost = p14.GetActorsByType("scghost")
				scfirebat = p14.GetActorsByType("scfirebat")
				scmarine = p14.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p15 and p1.IsAlliedWith(p15) then
				rmbo = p15.GetActorsByType("rmbo")
				e3 = p15.GetActorsByType("e3")
				e2 = p15.GetActorsByType("e2")
				e4 = p15.GetActorsByType("e4")
				e5 = p15.GetActorsByType("e5")
				e1 = p15.GetActorsByType("e1")
				e7 = p15.GetActorsByType("e7")
				shok = p15.GetActorsByType("shok")
				rae4 = p15.GetActorsByType("rae4")
				rae3 = p15.GetActorsByType("rae3")
				rae2 = p15.GetActorsByType("rae2")
				rae1 = p15.GetActorsByType("rae1")
				dog = p15.GetActorsByType("dog")
				scghost = p15.GetActorsByType("scghost")
				scfirebat = p15.GetActorsByType("scfirebat")
				scmarine = p15.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p16 and p1.IsAlliedWith(p16) then
				rmbo = p16.GetActorsByType("rmbo")
				e3 = p16.GetActorsByType("e3")
				e2 = p16.GetActorsByType("e2")
				e4 = p16.GetActorsByType("e4")
				e5 = p16.GetActorsByType("e5")
				e1 = p16.GetActorsByType("e1")
				e7 = p16.GetActorsByType("e7")
				shok = p16.GetActorsByType("shok")
				rae4 = p16.GetActorsByType("rae4")
				rae3 = p16.GetActorsByType("rae3")
				rae2 = p16.GetActorsByType("rae2")
				rae1 = p16.GetActorsByType("rae1")
				dog = p16.GetActorsByType("dog")
				scghost = p16.GetActorsByType("scghost")
				scfirebat = p16.GetActorsByType("scfirebat")
				scmarine = p16.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p1infantry()
	end)
end
p2infantry = function()
	if p2 and p2.IsBot then
		cityboteliteinstructor = p2.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p3 and p2.IsAlliedWith(p3) then
				rmbo = p3.GetActorsByType("rmbo")
				e3 = p3.GetActorsByType("e3")
				e2 = p3.GetActorsByType("e2")
				e4 = p3.GetActorsByType("e4")
				e5 = p3.GetActorsByType("e5")
				e1 = p3.GetActorsByType("e1")
				e7 = p3.GetActorsByType("e7")
				shok = p3.GetActorsByType("shok")
				rae4 = p3.GetActorsByType("rae4")
				rae3 = p3.GetActorsByType("rae3")
				rae2 = p3.GetActorsByType("rae2")
				rae1 = p3.GetActorsByType("rae1")
				dog = p3.GetActorsByType("dog")
				scghost = p3.GetActorsByType("scghost")
				scfirebat = p3.GetActorsByType("scfirebat")
				scmarine = p3.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p4 and p2.IsAlliedWith(p4) then
				rmbo = p4.GetActorsByType("rmbo")
				e3 = p4.GetActorsByType("e3")
				e2 = p4.GetActorsByType("e2")
				e4 = p4.GetActorsByType("e4")
				e5 = p4.GetActorsByType("e5")
				e1 = p4.GetActorsByType("e1")
				e7 = p4.GetActorsByType("e7")
				shok = p4.GetActorsByType("shok")
				rae4 = p4.GetActorsByType("rae4")
				rae3 = p4.GetActorsByType("rae3")
				rae2 = p4.GetActorsByType("rae2")
				rae1 = p4.GetActorsByType("rae1")
				dog = p4.GetActorsByType("dog")
				scghost = p4.GetActorsByType("scghost")
				scfirebat = p4.GetActorsByType("scfirebat")
				scmarine = p4.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p5 and p2.IsAlliedWith(p5) then
				rmbo = p5.GetActorsByType("rmbo")
				e3 = p5.GetActorsByType("e3")
				e2 = p5.GetActorsByType("e2")
				e4 = p5.GetActorsByType("e4")
				e5 = p5.GetActorsByType("e5")
				e1 = p5.GetActorsByType("e1")
				e7 = p5.GetActorsByType("e7")
				shok = p5.GetActorsByType("shok")
				rae4 = p5.GetActorsByType("rae4")
				rae3 = p5.GetActorsByType("rae3")
				rae2 = p5.GetActorsByType("rae2")
				rae1 = p5.GetActorsByType("rae1")
				dog = p5.GetActorsByType("dog")
				scghost = p5.GetActorsByType("scghost")
				scfirebat = p5.GetActorsByType("scfirebat")
				scmarine = p5.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p6 and p2.IsAlliedWith(p6) then
				rmbo = p6.GetActorsByType("rmbo")
				e3 = p6.GetActorsByType("e3")
				e2 = p6.GetActorsByType("e2")
				e4 = p6.GetActorsByType("e4")
				e5 = p6.GetActorsByType("e5")
				e1 = p6.GetActorsByType("e1")
				e7 = p6.GetActorsByType("e7")
				shok = p6.GetActorsByType("shok")
				rae4 = p6.GetActorsByType("rae4")
				rae3 = p6.GetActorsByType("rae3")
				rae2 = p6.GetActorsByType("rae2")
				rae1 = p6.GetActorsByType("rae1")
				dog = p6.GetActorsByType("dog")
				scghost = p6.GetActorsByType("scghost")
				scfirebat = p6.GetActorsByType("scfirebat")
				scmarine = p6.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p7 and p2.IsAlliedWith(p7) then
				rmbo = p7.GetActorsByType("rmbo")
				e3 = p7.GetActorsByType("e3")
				e2 = p7.GetActorsByType("e2")
				e4 = p7.GetActorsByType("e4")
				e5 = p7.GetActorsByType("e5")
				e1 = p7.GetActorsByType("e1")
				e7 = p7.GetActorsByType("e7")
				shok = p7.GetActorsByType("shok")
				rae4 = p7.GetActorsByType("rae4")
				rae3 = p7.GetActorsByType("rae3")
				rae2 = p7.GetActorsByType("rae2")
				rae1 = p7.GetActorsByType("rae1")
				dog = p7.GetActorsByType("dog")
				scghost = p7.GetActorsByType("scghost")
				scfirebat = p7.GetActorsByType("scfirebat")
				scmarine = p7.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p8 and p2.IsAlliedWith(p8) then
				rmbo = p8.GetActorsByType("rmbo")
				e3 = p8.GetActorsByType("e3")
				e2 = p8.GetActorsByType("e2")
				e4 = p8.GetActorsByType("e4")
				e5 = p8.GetActorsByType("e5")
				e1 = p8.GetActorsByType("e1")
				e7 = p8.GetActorsByType("e7")
				shok = p8.GetActorsByType("shok")
				rae4 = p8.GetActorsByType("rae4")
				rae3 = p8.GetActorsByType("rae3")
				rae2 = p8.GetActorsByType("rae2")
				rae1 = p8.GetActorsByType("rae1")
				dog = p8.GetActorsByType("dog")
				scghost = p8.GetActorsByType("scghost")
				scfirebat = p8.GetActorsByType("scfirebat")
				scmarine = p8.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p9 and p2.IsAlliedWith(p9) then
				rmbo = p9.GetActorsByType("rmbo")
				e3 = p9.GetActorsByType("e3")
				e2 = p9.GetActorsByType("e2")
				e4 = p9.GetActorsByType("e4")
				e5 = p9.GetActorsByType("e5")
				e1 = p9.GetActorsByType("e1")
				e7 = p9.GetActorsByType("e7")
				shok = p9.GetActorsByType("shok")
				rae4 = p9.GetActorsByType("rae4")
				rae3 = p9.GetActorsByType("rae3")
				rae2 = p9.GetActorsByType("rae2")
				rae1 = p9.GetActorsByType("rae1")
				dog = p9.GetActorsByType("dog")
				scghost = p9.GetActorsByType("scghost")
				scfirebat = p9.GetActorsByType("scfirebat")
				scmarine = p9.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p10 and p2.IsAlliedWith(p10) then
				rmbo = p10.GetActorsByType("rmbo")
				e3 = p10.GetActorsByType("e3")
				e2 = p10.GetActorsByType("e2")
				e4 = p10.GetActorsByType("e4")
				e5 = p10.GetActorsByType("e5")
				e1 = p10.GetActorsByType("e1")
				e7 = p10.GetActorsByType("e7")
				shok = p10.GetActorsByType("shok")
				rae4 = p10.GetActorsByType("rae4")
				rae3 = p10.GetActorsByType("rae3")
				rae2 = p10.GetActorsByType("rae2")
				rae1 = p10.GetActorsByType("rae1")
				dog = p10.GetActorsByType("dog")
				scghost = p10.GetActorsByType("scghost")
				scfirebat = p10.GetActorsByType("scfirebat")
				scmarine = p10.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p11 and p2.IsAlliedWith(p11) then
				rmbo = p11.GetActorsByType("rmbo")
				e3 = p11.GetActorsByType("e3")
				e2 = p11.GetActorsByType("e2")
				e4 = p11.GetActorsByType("e4")
				e5 = p11.GetActorsByType("e5")
				e1 = p11.GetActorsByType("e1")
				e7 = p11.GetActorsByType("e7")
				shok = p11.GetActorsByType("shok")
				rae4 = p11.GetActorsByType("rae4")
				rae3 = p11.GetActorsByType("rae3")
				rae2 = p11.GetActorsByType("rae2")
				rae1 = p11.GetActorsByType("rae1")
				dog = p11.GetActorsByType("dog")
				scghost = p11.GetActorsByType("scghost")
				scfirebat = p11.GetActorsByType("scfirebat")
				scmarine = p11.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p12 and p2.IsAlliedWith(p12) then
				rmbo = p12.GetActorsByType("rmbo")
				e3 = p12.GetActorsByType("e3")
				e2 = p12.GetActorsByType("e2")
				e4 = p12.GetActorsByType("e4")
				e5 = p12.GetActorsByType("e5")
				e1 = p12.GetActorsByType("e1")
				e7 = p12.GetActorsByType("e7")
				shok = p12.GetActorsByType("shok")
				rae4 = p12.GetActorsByType("rae4")
				rae3 = p12.GetActorsByType("rae3")
				rae2 = p12.GetActorsByType("rae2")
				rae1 = p12.GetActorsByType("rae1")
				dog = p12.GetActorsByType("dog")
				scghost = p12.GetActorsByType("scghost")
				scfirebat = p12.GetActorsByType("scfirebat")
				scmarine = p12.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p13 and p2.IsAlliedWith(p13) then
				rmbo = p13.GetActorsByType("rmbo")
				e3 = p13.GetActorsByType("e3")
				e2 = p13.GetActorsByType("e2")
				e4 = p13.GetActorsByType("e4")
				e5 = p13.GetActorsByType("e5")
				e1 = p13.GetActorsByType("e1")
				e7 = p13.GetActorsByType("e7")
				shok = p13.GetActorsByType("shok")
				rae4 = p13.GetActorsByType("rae4")
				rae3 = p13.GetActorsByType("rae3")
				rae2 = p13.GetActorsByType("rae2")
				rae1 = p13.GetActorsByType("rae1")
				dog = p13.GetActorsByType("dog")
				scghost = p13.GetActorsByType("scghost")
				scfirebat = p13.GetActorsByType("scfirebat")
				scmarine = p13.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p14 and p2.IsAlliedWith(p14) then
				rmbo = p14.GetActorsByType("rmbo")
				e3 = p14.GetActorsByType("e3")
				e2 = p14.GetActorsByType("e2")
				e4 = p14.GetActorsByType("e4")
				e5 = p14.GetActorsByType("e5")
				e1 = p14.GetActorsByType("e1")
				e7 = p14.GetActorsByType("e7")
				shok = p14.GetActorsByType("shok")
				rae4 = p14.GetActorsByType("rae4")
				rae3 = p14.GetActorsByType("rae3")
				rae2 = p14.GetActorsByType("rae2")
				rae1 = p14.GetActorsByType("rae1")
				dog = p14.GetActorsByType("dog")
				scghost = p14.GetActorsByType("scghost")
				scfirebat = p14.GetActorsByType("scfirebat")
				scmarine = p14.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p15 and p2.IsAlliedWith(p15) then
				rmbo = p15.GetActorsByType("rmbo")
				e3 = p15.GetActorsByType("e3")
				e2 = p15.GetActorsByType("e2")
				e4 = p15.GetActorsByType("e4")
				e5 = p15.GetActorsByType("e5")
				e1 = p15.GetActorsByType("e1")
				e7 = p15.GetActorsByType("e7")
				shok = p15.GetActorsByType("shok")
				rae4 = p15.GetActorsByType("rae4")
				rae3 = p15.GetActorsByType("rae3")
				rae2 = p15.GetActorsByType("rae2")
				rae1 = p15.GetActorsByType("rae1")
				dog = p15.GetActorsByType("dog")
				scghost = p15.GetActorsByType("scghost")
				scfirebat = p15.GetActorsByType("scfirebat")
				scmarine = p15.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p16 and p2.IsAlliedWith(p16) then
				rmbo = p16.GetActorsByType("rmbo")
				e3 = p16.GetActorsByType("e3")
				e2 = p16.GetActorsByType("e2")
				e4 = p16.GetActorsByType("e4")
				e5 = p16.GetActorsByType("e5")
				e1 = p16.GetActorsByType("e1")
				e7 = p16.GetActorsByType("e7")
				shok = p16.GetActorsByType("shok")
				rae4 = p16.GetActorsByType("rae4")
				rae3 = p16.GetActorsByType("rae3")
				rae2 = p16.GetActorsByType("rae2")
				rae1 = p16.GetActorsByType("rae1")
				dog = p16.GetActorsByType("dog")
				scghost = p16.GetActorsByType("scghost")
				scfirebat = p16.GetActorsByType("scfirebat")
				scmarine = p16.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p1 and p2.IsAlliedWith(p1) then
				rmbo = p1.GetActorsByType("rmbo")
				e3 = p1.GetActorsByType("e3")
				e2 = p1.GetActorsByType("e2")
				e4 = p1.GetActorsByType("e4")
				e5 = p1.GetActorsByType("e5")
				e1 = p1.GetActorsByType("e1")
				e7 = p1.GetActorsByType("e7")
				shok = p1.GetActorsByType("shok")
				rae4 = p1.GetActorsByType("rae4")
				rae3 = p1.GetActorsByType("rae3")
				rae2 = p1.GetActorsByType("rae2")
				rae1 = p1.GetActorsByType("rae1")
				dog = p1.GetActorsByType("dog")
				scghost = p1.GetActorsByType("scghost")
				scfirebat = p1.GetActorsByType("scfirebat")
				scmarine = p1.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p2infantry()
	end)
end
p3infantry = function()
	if p3 and p3.IsBot then
		cityboteliteinstructor = p3.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p4 and p3.IsAlliedWith(p4) then
				rmbo = p4.GetActorsByType("rmbo")
				e3 = p4.GetActorsByType("e3")
				e2 = p4.GetActorsByType("e2")
				e4 = p4.GetActorsByType("e4")
				e5 = p4.GetActorsByType("e5")
				e1 = p4.GetActorsByType("e1")
				e7 = p4.GetActorsByType("e7")
				shok = p4.GetActorsByType("shok")
				rae4 = p4.GetActorsByType("rae4")
				rae3 = p4.GetActorsByType("rae3")
				rae2 = p4.GetActorsByType("rae2")
				rae1 = p4.GetActorsByType("rae1")
				dog = p4.GetActorsByType("dog")
				scghost = p4.GetActorsByType("scghost")
				scfirebat = p4.GetActorsByType("scfirebat")
				scmarine = p4.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p5 and p3.IsAlliedWith(p5) then
				rmbo = p5.GetActorsByType("rmbo")
				e3 = p5.GetActorsByType("e3")
				e2 = p5.GetActorsByType("e2")
				e4 = p5.GetActorsByType("e4")
				e5 = p5.GetActorsByType("e5")
				e1 = p5.GetActorsByType("e1")
				e7 = p5.GetActorsByType("e7")
				shok = p5.GetActorsByType("shok")
				rae4 = p5.GetActorsByType("rae4")
				rae3 = p5.GetActorsByType("rae3")
				rae2 = p5.GetActorsByType("rae2")
				rae1 = p5.GetActorsByType("rae1")
				dog = p5.GetActorsByType("dog")
				scghost = p5.GetActorsByType("scghost")
				scfirebat = p5.GetActorsByType("scfirebat")
				scmarine = p5.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p6 and p3.IsAlliedWith(p6) then
				rmbo = p6.GetActorsByType("rmbo")
				e3 = p6.GetActorsByType("e3")
				e2 = p6.GetActorsByType("e2")
				e4 = p6.GetActorsByType("e4")
				e5 = p6.GetActorsByType("e5")
				e1 = p6.GetActorsByType("e1")
				e7 = p6.GetActorsByType("e7")
				shok = p6.GetActorsByType("shok")
				rae4 = p6.GetActorsByType("rae4")
				rae3 = p6.GetActorsByType("rae3")
				rae2 = p6.GetActorsByType("rae2")
				rae1 = p6.GetActorsByType("rae1")
				dog = p6.GetActorsByType("dog")
				scghost = p6.GetActorsByType("scghost")
				scfirebat = p6.GetActorsByType("scfirebat")
				scmarine = p6.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p7 and p3.IsAlliedWith(p7) then
				rmbo = p7.GetActorsByType("rmbo")
				e3 = p7.GetActorsByType("e3")
				e2 = p7.GetActorsByType("e2")
				e4 = p7.GetActorsByType("e4")
				e5 = p7.GetActorsByType("e5")
				e1 = p7.GetActorsByType("e1")
				e7 = p7.GetActorsByType("e7")
				shok = p7.GetActorsByType("shok")
				rae4 = p7.GetActorsByType("rae4")
				rae3 = p7.GetActorsByType("rae3")
				rae2 = p7.GetActorsByType("rae2")
				rae1 = p7.GetActorsByType("rae1")
				dog = p7.GetActorsByType("dog")
				scghost = p7.GetActorsByType("scghost")
				scfirebat = p7.GetActorsByType("scfirebat")
				scmarine = p7.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p8 and p3.IsAlliedWith(p8) then
				rmbo = p8.GetActorsByType("rmbo")
				e3 = p8.GetActorsByType("e3")
				e2 = p8.GetActorsByType("e2")
				e4 = p8.GetActorsByType("e4")
				e5 = p8.GetActorsByType("e5")
				e1 = p8.GetActorsByType("e1")
				e7 = p8.GetActorsByType("e7")
				shok = p8.GetActorsByType("shok")
				rae4 = p8.GetActorsByType("rae4")
				rae3 = p8.GetActorsByType("rae3")
				rae2 = p8.GetActorsByType("rae2")
				rae1 = p8.GetActorsByType("rae1")
				dog = p8.GetActorsByType("dog")
				scghost = p8.GetActorsByType("scghost")
				scfirebat = p8.GetActorsByType("scfirebat")
				scmarine = p8.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p9 and p3.IsAlliedWith(p9) then
				rmbo = p9.GetActorsByType("rmbo")
				e3 = p9.GetActorsByType("e3")
				e2 = p9.GetActorsByType("e2")
				e4 = p9.GetActorsByType("e4")
				e5 = p9.GetActorsByType("e5")
				e1 = p9.GetActorsByType("e1")
				e7 = p9.GetActorsByType("e7")
				shok = p9.GetActorsByType("shok")
				rae4 = p9.GetActorsByType("rae4")
				rae3 = p9.GetActorsByType("rae3")
				rae2 = p9.GetActorsByType("rae2")
				rae1 = p9.GetActorsByType("rae1")
				dog = p9.GetActorsByType("dog")
				scghost = p9.GetActorsByType("scghost")
				scfirebat = p9.GetActorsByType("scfirebat")
				scmarine = p9.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p10 and p3.IsAlliedWith(p10) then
				rmbo = p10.GetActorsByType("rmbo")
				e3 = p10.GetActorsByType("e3")
				e2 = p10.GetActorsByType("e2")
				e4 = p10.GetActorsByType("e4")
				e5 = p10.GetActorsByType("e5")
				e1 = p10.GetActorsByType("e1")
				e7 = p10.GetActorsByType("e7")
				shok = p10.GetActorsByType("shok")
				rae4 = p10.GetActorsByType("rae4")
				rae3 = p10.GetActorsByType("rae3")
				rae2 = p10.GetActorsByType("rae2")
				rae1 = p10.GetActorsByType("rae1")
				dog = p10.GetActorsByType("dog")
				scghost = p10.GetActorsByType("scghost")
				scfirebat = p10.GetActorsByType("scfirebat")
				scmarine = p10.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p11 and p3.IsAlliedWith(p11) then
				rmbo = p11.GetActorsByType("rmbo")
				e3 = p11.GetActorsByType("e3")
				e2 = p11.GetActorsByType("e2")
				e4 = p11.GetActorsByType("e4")
				e5 = p11.GetActorsByType("e5")
				e1 = p11.GetActorsByType("e1")
				e7 = p11.GetActorsByType("e7")
				shok = p11.GetActorsByType("shok")
				rae4 = p11.GetActorsByType("rae4")
				rae3 = p11.GetActorsByType("rae3")
				rae2 = p11.GetActorsByType("rae2")
				rae1 = p11.GetActorsByType("rae1")
				dog = p11.GetActorsByType("dog")
				scghost = p11.GetActorsByType("scghost")
				scfirebat = p11.GetActorsByType("scfirebat")
				scmarine = p11.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p12 and p3.IsAlliedWith(p12) then
				rmbo = p12.GetActorsByType("rmbo")
				e3 = p12.GetActorsByType("e3")
				e2 = p12.GetActorsByType("e2")
				e4 = p12.GetActorsByType("e4")
				e5 = p12.GetActorsByType("e5")
				e1 = p12.GetActorsByType("e1")
				e7 = p12.GetActorsByType("e7")
				shok = p12.GetActorsByType("shok")
				rae4 = p12.GetActorsByType("rae4")
				rae3 = p12.GetActorsByType("rae3")
				rae2 = p12.GetActorsByType("rae2")
				rae1 = p12.GetActorsByType("rae1")
				dog = p12.GetActorsByType("dog")
				scghost = p12.GetActorsByType("scghost")
				scfirebat = p12.GetActorsByType("scfirebat")
				scmarine = p12.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p13 and p3.IsAlliedWith(p13) then
				rmbo = p13.GetActorsByType("rmbo")
				e3 = p13.GetActorsByType("e3")
				e2 = p13.GetActorsByType("e2")
				e4 = p13.GetActorsByType("e4")
				e5 = p13.GetActorsByType("e5")
				e1 = p13.GetActorsByType("e1")
				e7 = p13.GetActorsByType("e7")
				shok = p13.GetActorsByType("shok")
				rae4 = p13.GetActorsByType("rae4")
				rae3 = p13.GetActorsByType("rae3")
				rae2 = p13.GetActorsByType("rae2")
				rae1 = p13.GetActorsByType("rae1")
				dog = p13.GetActorsByType("dog")
				scghost = p13.GetActorsByType("scghost")
				scfirebat = p13.GetActorsByType("scfirebat")
				scmarine = p13.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p14 and p3.IsAlliedWith(p14) then
				rmbo = p14.GetActorsByType("rmbo")
				e3 = p14.GetActorsByType("e3")
				e2 = p14.GetActorsByType("e2")
				e4 = p14.GetActorsByType("e4")
				e5 = p14.GetActorsByType("e5")
				e1 = p14.GetActorsByType("e1")
				e7 = p14.GetActorsByType("e7")
				shok = p14.GetActorsByType("shok")
				rae4 = p14.GetActorsByType("rae4")
				rae3 = p14.GetActorsByType("rae3")
				rae2 = p14.GetActorsByType("rae2")
				rae1 = p14.GetActorsByType("rae1")
				dog = p14.GetActorsByType("dog")
				scghost = p14.GetActorsByType("scghost")
				scfirebat = p14.GetActorsByType("scfirebat")
				scmarine = p14.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p15 and p3.IsAlliedWith(p15) then
				rmbo = p15.GetActorsByType("rmbo")
				e3 = p15.GetActorsByType("e3")
				e2 = p15.GetActorsByType("e2")
				e4 = p15.GetActorsByType("e4")
				e5 = p15.GetActorsByType("e5")
				e1 = p15.GetActorsByType("e1")
				e7 = p15.GetActorsByType("e7")
				shok = p15.GetActorsByType("shok")
				rae4 = p15.GetActorsByType("rae4")
				rae3 = p15.GetActorsByType("rae3")
				rae2 = p15.GetActorsByType("rae2")
				rae1 = p15.GetActorsByType("rae1")
				dog = p15.GetActorsByType("dog")
				scghost = p15.GetActorsByType("scghost")
				scfirebat = p15.GetActorsByType("scfirebat")
				scmarine = p15.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p16 and p3.IsAlliedWith(p16) then
				rmbo = p16.GetActorsByType("rmbo")
				e3 = p16.GetActorsByType("e3")
				e2 = p16.GetActorsByType("e2")
				e4 = p16.GetActorsByType("e4")
				e5 = p16.GetActorsByType("e5")
				e1 = p16.GetActorsByType("e1")
				e7 = p16.GetActorsByType("e7")
				shok = p16.GetActorsByType("shok")
				rae4 = p16.GetActorsByType("rae4")
				rae3 = p16.GetActorsByType("rae3")
				rae2 = p16.GetActorsByType("rae2")
				rae1 = p16.GetActorsByType("rae1")
				dog = p16.GetActorsByType("dog")
				scghost = p16.GetActorsByType("scghost")
				scfirebat = p16.GetActorsByType("scfirebat")
				scmarine = p16.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p1 and p3.IsAlliedWith(p1) then
				rmbo = p1.GetActorsByType("rmbo")
				e3 = p1.GetActorsByType("e3")
				e2 = p1.GetActorsByType("e2")
				e4 = p1.GetActorsByType("e4")
				e5 = p1.GetActorsByType("e5")
				e1 = p1.GetActorsByType("e1")
				e7 = p1.GetActorsByType("e7")
				shok = p1.GetActorsByType("shok")
				rae4 = p1.GetActorsByType("rae4")
				rae3 = p1.GetActorsByType("rae3")
				rae2 = p1.GetActorsByType("rae2")
				rae1 = p1.GetActorsByType("rae1")
				dog = p1.GetActorsByType("dog")
				scghost = p1.GetActorsByType("scghost")
				scfirebat = p1.GetActorsByType("scfirebat")
				scmarine = p1.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p2 and p3.IsAlliedWith(p2) then
				rmbo = p2.GetActorsByType("rmbo")
				e3 = p2.GetActorsByType("e3")
				e2 = p2.GetActorsByType("e2")
				e4 = p2.GetActorsByType("e4")
				e5 = p2.GetActorsByType("e5")
				e1 = p2.GetActorsByType("e1")
				e7 = p2.GetActorsByType("e7")
				shok = p2.GetActorsByType("shok")
				rae4 = p2.GetActorsByType("rae4")
				rae3 = p2.GetActorsByType("rae3")
				rae2 = p2.GetActorsByType("rae2")
				rae1 = p2.GetActorsByType("rae1")
				dog = p2.GetActorsByType("dog")
				scghost = p2.GetActorsByType("scghost")
				scfirebat = p2.GetActorsByType("scfirebat")
				scmarine = p2.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p3infantry()
	end)
end
p4infantry = function()
	if p4 and p4.IsBot then
		cityboteliteinstructor = p4.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p5 and p4.IsAlliedWith(p5) then
				rmbo = p5.GetActorsByType("rmbo")
				e3 = p5.GetActorsByType("e3")
				e2 = p5.GetActorsByType("e2")
				e4 = p5.GetActorsByType("e4")
				e5 = p5.GetActorsByType("e5")
				e1 = p5.GetActorsByType("e1")
				e7 = p5.GetActorsByType("e7")
				shok = p5.GetActorsByType("shok")
				rae4 = p5.GetActorsByType("rae4")
				rae3 = p5.GetActorsByType("rae3")
				rae2 = p5.GetActorsByType("rae2")
				rae1 = p5.GetActorsByType("rae1")
				dog = p5.GetActorsByType("dog")
				scghost = p5.GetActorsByType("scghost")
				scfirebat = p5.GetActorsByType("scfirebat")
				scmarine = p5.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p6 and p4.IsAlliedWith(p6) then
				rmbo = p6.GetActorsByType("rmbo")
				e3 = p6.GetActorsByType("e3")
				e2 = p6.GetActorsByType("e2")
				e4 = p6.GetActorsByType("e4")
				e5 = p6.GetActorsByType("e5")
				e1 = p6.GetActorsByType("e1")
				e7 = p6.GetActorsByType("e7")
				shok = p6.GetActorsByType("shok")
				rae4 = p6.GetActorsByType("rae4")
				rae3 = p6.GetActorsByType("rae3")
				rae2 = p6.GetActorsByType("rae2")
				rae1 = p6.GetActorsByType("rae1")
				dog = p6.GetActorsByType("dog")
				scghost = p6.GetActorsByType("scghost")
				scfirebat = p6.GetActorsByType("scfirebat")
				scmarine = p6.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p7 and p4.IsAlliedWith(p7) then
				rmbo = p7.GetActorsByType("rmbo")
				e3 = p7.GetActorsByType("e3")
				e2 = p7.GetActorsByType("e2")
				e4 = p7.GetActorsByType("e4")
				e5 = p7.GetActorsByType("e5")
				e1 = p7.GetActorsByType("e1")
				e7 = p7.GetActorsByType("e7")
				shok = p7.GetActorsByType("shok")
				rae4 = p7.GetActorsByType("rae4")
				rae3 = p7.GetActorsByType("rae3")
				rae2 = p7.GetActorsByType("rae2")
				rae1 = p7.GetActorsByType("rae1")
				dog = p7.GetActorsByType("dog")
				scghost = p7.GetActorsByType("scghost")
				scfirebat = p7.GetActorsByType("scfirebat")
				scmarine = p7.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p8 and p4.IsAlliedWith(p8) then
				rmbo = p8.GetActorsByType("rmbo")
				e3 = p8.GetActorsByType("e3")
				e2 = p8.GetActorsByType("e2")
				e4 = p8.GetActorsByType("e4")
				e5 = p8.GetActorsByType("e5")
				e1 = p8.GetActorsByType("e1")
				e7 = p8.GetActorsByType("e7")
				shok = p8.GetActorsByType("shok")
				rae4 = p8.GetActorsByType("rae4")
				rae3 = p8.GetActorsByType("rae3")
				rae2 = p8.GetActorsByType("rae2")
				rae1 = p8.GetActorsByType("rae1")
				dog = p8.GetActorsByType("dog")
				scghost = p8.GetActorsByType("scghost")
				scfirebat = p8.GetActorsByType("scfirebat")
				scmarine = p8.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p9 and p4.IsAlliedWith(p9) then
				rmbo = p9.GetActorsByType("rmbo")
				e3 = p9.GetActorsByType("e3")
				e2 = p9.GetActorsByType("e2")
				e4 = p9.GetActorsByType("e4")
				e5 = p9.GetActorsByType("e5")
				e1 = p9.GetActorsByType("e1")
				e7 = p9.GetActorsByType("e7")
				shok = p9.GetActorsByType("shok")
				rae4 = p9.GetActorsByType("rae4")
				rae3 = p9.GetActorsByType("rae3")
				rae2 = p9.GetActorsByType("rae2")
				rae1 = p9.GetActorsByType("rae1")
				dog = p9.GetActorsByType("dog")
				scghost = p9.GetActorsByType("scghost")
				scfirebat = p9.GetActorsByType("scfirebat")
				scmarine = p9.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p10 and p4.IsAlliedWith(p10) then
				rmbo = p10.GetActorsByType("rmbo")
				e3 = p10.GetActorsByType("e3")
				e2 = p10.GetActorsByType("e2")
				e4 = p10.GetActorsByType("e4")
				e5 = p10.GetActorsByType("e5")
				e1 = p10.GetActorsByType("e1")
				e7 = p10.GetActorsByType("e7")
				shok = p10.GetActorsByType("shok")
				rae4 = p10.GetActorsByType("rae4")
				rae3 = p10.GetActorsByType("rae3")
				rae2 = p10.GetActorsByType("rae2")
				rae1 = p10.GetActorsByType("rae1")
				dog = p10.GetActorsByType("dog")
				scghost = p10.GetActorsByType("scghost")
				scfirebat = p10.GetActorsByType("scfirebat")
				scmarine = p10.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p11 and p4.IsAlliedWith(p11) then
				rmbo = p11.GetActorsByType("rmbo")
				e3 = p11.GetActorsByType("e3")
				e2 = p11.GetActorsByType("e2")
				e4 = p11.GetActorsByType("e4")
				e5 = p11.GetActorsByType("e5")
				e1 = p11.GetActorsByType("e1")
				e7 = p11.GetActorsByType("e7")
				shok = p11.GetActorsByType("shok")
				rae4 = p11.GetActorsByType("rae4")
				rae3 = p11.GetActorsByType("rae3")
				rae2 = p11.GetActorsByType("rae2")
				rae1 = p11.GetActorsByType("rae1")
				dog = p11.GetActorsByType("dog")
				scghost = p11.GetActorsByType("scghost")
				scfirebat = p11.GetActorsByType("scfirebat")
				scmarine = p11.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p12 and p4.IsAlliedWith(p12) then
				rmbo = p12.GetActorsByType("rmbo")
				e3 = p12.GetActorsByType("e3")
				e2 = p12.GetActorsByType("e2")
				e4 = p12.GetActorsByType("e4")
				e5 = p12.GetActorsByType("e5")
				e1 = p12.GetActorsByType("e1")
				e7 = p12.GetActorsByType("e7")
				shok = p12.GetActorsByType("shok")
				rae4 = p12.GetActorsByType("rae4")
				rae3 = p12.GetActorsByType("rae3")
				rae2 = p12.GetActorsByType("rae2")
				rae1 = p12.GetActorsByType("rae1")
				dog = p12.GetActorsByType("dog")
				scghost = p12.GetActorsByType("scghost")
				scfirebat = p12.GetActorsByType("scfirebat")
				scmarine = p12.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p13 and p4.IsAlliedWith(p13) then
				rmbo = p13.GetActorsByType("rmbo")
				e3 = p13.GetActorsByType("e3")
				e2 = p13.GetActorsByType("e2")
				e4 = p13.GetActorsByType("e4")
				e5 = p13.GetActorsByType("e5")
				e1 = p13.GetActorsByType("e1")
				e7 = p13.GetActorsByType("e7")
				shok = p13.GetActorsByType("shok")
				rae4 = p13.GetActorsByType("rae4")
				rae3 = p13.GetActorsByType("rae3")
				rae2 = p13.GetActorsByType("rae2")
				rae1 = p13.GetActorsByType("rae1")
				dog = p13.GetActorsByType("dog")
				scghost = p13.GetActorsByType("scghost")
				scfirebat = p13.GetActorsByType("scfirebat")
				scmarine = p13.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p14 and p4.IsAlliedWith(p14) then
				rmbo = p14.GetActorsByType("rmbo")
				e3 = p14.GetActorsByType("e3")
				e2 = p14.GetActorsByType("e2")
				e4 = p14.GetActorsByType("e4")
				e5 = p14.GetActorsByType("e5")
				e1 = p14.GetActorsByType("e1")
				e7 = p14.GetActorsByType("e7")
				shok = p14.GetActorsByType("shok")
				rae4 = p14.GetActorsByType("rae4")
				rae3 = p14.GetActorsByType("rae3")
				rae2 = p14.GetActorsByType("rae2")
				rae1 = p14.GetActorsByType("rae1")
				dog = p14.GetActorsByType("dog")
				scghost = p14.GetActorsByType("scghost")
				scfirebat = p14.GetActorsByType("scfirebat")
				scmarine = p14.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p15 and p4.IsAlliedWith(p15) then
				rmbo = p15.GetActorsByType("rmbo")
				e3 = p15.GetActorsByType("e3")
				e2 = p15.GetActorsByType("e2")
				e4 = p15.GetActorsByType("e4")
				e5 = p15.GetActorsByType("e5")
				e1 = p15.GetActorsByType("e1")
				e7 = p15.GetActorsByType("e7")
				shok = p15.GetActorsByType("shok")
				rae4 = p15.GetActorsByType("rae4")
				rae3 = p15.GetActorsByType("rae3")
				rae2 = p15.GetActorsByType("rae2")
				rae1 = p15.GetActorsByType("rae1")
				dog = p15.GetActorsByType("dog")
				scghost = p15.GetActorsByType("scghost")
				scfirebat = p15.GetActorsByType("scfirebat")
				scmarine = p15.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p16 and p4.IsAlliedWith(p16) then
				rmbo = p16.GetActorsByType("rmbo")
				e3 = p16.GetActorsByType("e3")
				e2 = p16.GetActorsByType("e2")
				e4 = p16.GetActorsByType("e4")
				e5 = p16.GetActorsByType("e5")
				e1 = p16.GetActorsByType("e1")
				e7 = p16.GetActorsByType("e7")
				shok = p16.GetActorsByType("shok")
				rae4 = p16.GetActorsByType("rae4")
				rae3 = p16.GetActorsByType("rae3")
				rae2 = p16.GetActorsByType("rae2")
				rae1 = p16.GetActorsByType("rae1")
				dog = p16.GetActorsByType("dog")
				scghost = p16.GetActorsByType("scghost")
				scfirebat = p16.GetActorsByType("scfirebat")
				scmarine = p16.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p1 and p4.IsAlliedWith(p1) then
				rmbo = p1.GetActorsByType("rmbo")
				e3 = p1.GetActorsByType("e3")
				e2 = p1.GetActorsByType("e2")
				e4 = p1.GetActorsByType("e4")
				e5 = p1.GetActorsByType("e5")
				e1 = p1.GetActorsByType("e1")
				e7 = p1.GetActorsByType("e7")
				shok = p1.GetActorsByType("shok")
				rae4 = p1.GetActorsByType("rae4")
				rae3 = p1.GetActorsByType("rae3")
				rae2 = p1.GetActorsByType("rae2")
				rae1 = p1.GetActorsByType("rae1")
				dog = p1.GetActorsByType("dog")
				scghost = p1.GetActorsByType("scghost")
				scfirebat = p1.GetActorsByType("scfirebat")
				scmarine = p1.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p2 and p4.IsAlliedWith(p2) then
				rmbo = p2.GetActorsByType("rmbo")
				e3 = p2.GetActorsByType("e3")
				e2 = p2.GetActorsByType("e2")
				e4 = p2.GetActorsByType("e4")
				e5 = p2.GetActorsByType("e5")
				e1 = p2.GetActorsByType("e1")
				e7 = p2.GetActorsByType("e7")
				shok = p2.GetActorsByType("shok")
				rae4 = p2.GetActorsByType("rae4")
				rae3 = p2.GetActorsByType("rae3")
				rae2 = p2.GetActorsByType("rae2")
				rae1 = p2.GetActorsByType("rae1")
				dog = p2.GetActorsByType("dog")
				scghost = p2.GetActorsByType("scghost")
				scfirebat = p2.GetActorsByType("scfirebat")
				scmarine = p2.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p3 and p4.IsAlliedWith(p3) then
				rmbo = p3.GetActorsByType("rmbo")
				e3 = p3.GetActorsByType("e3")
				e2 = p3.GetActorsByType("e2")
				e4 = p3.GetActorsByType("e4")
				e5 = p3.GetActorsByType("e5")
				e1 = p3.GetActorsByType("e1")
				e7 = p3.GetActorsByType("e7")
				shok = p3.GetActorsByType("shok")
				rae4 = p3.GetActorsByType("rae4")
				rae3 = p3.GetActorsByType("rae3")
				rae2 = p3.GetActorsByType("rae2")
				rae1 = p3.GetActorsByType("rae1")
				dog = p3.GetActorsByType("dog")
				scghost = p3.GetActorsByType("scghost")
				scfirebat = p3.GetActorsByType("scfirebat")
				scmarine = p3.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p4infantry()
	end)
end
p5infantry = function()
	if p5 and p5.IsBot then
		cityboteliteinstructor = p5.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p6 and p5.IsAlliedWith(p6) then
				rmbo = p6.GetActorsByType("rmbo")
				e3 = p6.GetActorsByType("e3")
				e2 = p6.GetActorsByType("e2")
				e4 = p6.GetActorsByType("e4")
				e5 = p6.GetActorsByType("e5")
				e1 = p6.GetActorsByType("e1")
				e7 = p6.GetActorsByType("e7")
				shok = p6.GetActorsByType("shok")
				rae4 = p6.GetActorsByType("rae4")
				rae3 = p6.GetActorsByType("rae3")
				rae2 = p6.GetActorsByType("rae2")
				rae1 = p6.GetActorsByType("rae1")
				dog = p6.GetActorsByType("dog")
				scghost = p6.GetActorsByType("scghost")
				scfirebat = p6.GetActorsByType("scfirebat")
				scmarine = p6.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p7 and p5.IsAlliedWith(p7) then
				rmbo = p7.GetActorsByType("rmbo")
				e3 = p7.GetActorsByType("e3")
				e2 = p7.GetActorsByType("e2")
				e4 = p7.GetActorsByType("e4")
				e5 = p7.GetActorsByType("e5")
				e1 = p7.GetActorsByType("e1")
				e7 = p7.GetActorsByType("e7")
				shok = p7.GetActorsByType("shok")
				rae4 = p7.GetActorsByType("rae4")
				rae3 = p7.GetActorsByType("rae3")
				rae2 = p7.GetActorsByType("rae2")
				rae1 = p7.GetActorsByType("rae1")
				dog = p7.GetActorsByType("dog")
				scghost = p7.GetActorsByType("scghost")
				scfirebat = p7.GetActorsByType("scfirebat")
				scmarine = p7.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p8 and p5.IsAlliedWith(p8) then
				rmbo = p8.GetActorsByType("rmbo")
				e3 = p8.GetActorsByType("e3")
				e2 = p8.GetActorsByType("e2")
				e4 = p8.GetActorsByType("e4")
				e5 = p8.GetActorsByType("e5")
				e1 = p8.GetActorsByType("e1")
				e7 = p8.GetActorsByType("e7")
				shok = p8.GetActorsByType("shok")
				rae4 = p8.GetActorsByType("rae4")
				rae3 = p8.GetActorsByType("rae3")
				rae2 = p8.GetActorsByType("rae2")
				rae1 = p8.GetActorsByType("rae1")
				dog = p8.GetActorsByType("dog")
				scghost = p8.GetActorsByType("scghost")
				scfirebat = p8.GetActorsByType("scfirebat")
				scmarine = p8.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p9 and p5.IsAlliedWith(p9) then
				rmbo = p9.GetActorsByType("rmbo")
				e3 = p9.GetActorsByType("e3")
				e2 = p9.GetActorsByType("e2")
				e4 = p9.GetActorsByType("e4")
				e5 = p9.GetActorsByType("e5")
				e1 = p9.GetActorsByType("e1")
				e7 = p9.GetActorsByType("e7")
				shok = p9.GetActorsByType("shok")
				rae4 = p9.GetActorsByType("rae4")
				rae3 = p9.GetActorsByType("rae3")
				rae2 = p9.GetActorsByType("rae2")
				rae1 = p9.GetActorsByType("rae1")
				dog = p9.GetActorsByType("dog")
				scghost = p9.GetActorsByType("scghost")
				scfirebat = p9.GetActorsByType("scfirebat")
				scmarine = p9.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p10 and p5.IsAlliedWith(p10) then
				rmbo = p10.GetActorsByType("rmbo")
				e3 = p10.GetActorsByType("e3")
				e2 = p10.GetActorsByType("e2")
				e4 = p10.GetActorsByType("e4")
				e5 = p10.GetActorsByType("e5")
				e1 = p10.GetActorsByType("e1")
				e7 = p10.GetActorsByType("e7")
				shok = p10.GetActorsByType("shok")
				rae4 = p10.GetActorsByType("rae4")
				rae3 = p10.GetActorsByType("rae3")
				rae2 = p10.GetActorsByType("rae2")
				rae1 = p10.GetActorsByType("rae1")
				dog = p10.GetActorsByType("dog")
				scghost = p10.GetActorsByType("scghost")
				scfirebat = p10.GetActorsByType("scfirebat")
				scmarine = p10.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p11 and p5.IsAlliedWith(p11) then
				rmbo = p11.GetActorsByType("rmbo")
				e3 = p11.GetActorsByType("e3")
				e2 = p11.GetActorsByType("e2")
				e4 = p11.GetActorsByType("e4")
				e5 = p11.GetActorsByType("e5")
				e1 = p11.GetActorsByType("e1")
				e7 = p11.GetActorsByType("e7")
				shok = p11.GetActorsByType("shok")
				rae4 = p11.GetActorsByType("rae4")
				rae3 = p11.GetActorsByType("rae3")
				rae2 = p11.GetActorsByType("rae2")
				rae1 = p11.GetActorsByType("rae1")
				dog = p11.GetActorsByType("dog")
				scghost = p11.GetActorsByType("scghost")
				scfirebat = p11.GetActorsByType("scfirebat")
				scmarine = p11.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p12 and p5.IsAlliedWith(p12) then
				rmbo = p12.GetActorsByType("rmbo")
				e3 = p12.GetActorsByType("e3")
				e2 = p12.GetActorsByType("e2")
				e4 = p12.GetActorsByType("e4")
				e5 = p12.GetActorsByType("e5")
				e1 = p12.GetActorsByType("e1")
				e7 = p12.GetActorsByType("e7")
				shok = p12.GetActorsByType("shok")
				rae4 = p12.GetActorsByType("rae4")
				rae3 = p12.GetActorsByType("rae3")
				rae2 = p12.GetActorsByType("rae2")
				rae1 = p12.GetActorsByType("rae1")
				dog = p12.GetActorsByType("dog")
				scghost = p12.GetActorsByType("scghost")
				scfirebat = p12.GetActorsByType("scfirebat")
				scmarine = p12.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p13 and p5.IsAlliedWith(p13) then
				rmbo = p13.GetActorsByType("rmbo")
				e3 = p13.GetActorsByType("e3")
				e2 = p13.GetActorsByType("e2")
				e4 = p13.GetActorsByType("e4")
				e5 = p13.GetActorsByType("e5")
				e1 = p13.GetActorsByType("e1")
				e7 = p13.GetActorsByType("e7")
				shok = p13.GetActorsByType("shok")
				rae4 = p13.GetActorsByType("rae4")
				rae3 = p13.GetActorsByType("rae3")
				rae2 = p13.GetActorsByType("rae2")
				rae1 = p13.GetActorsByType("rae1")
				dog = p13.GetActorsByType("dog")
				scghost = p13.GetActorsByType("scghost")
				scfirebat = p13.GetActorsByType("scfirebat")
				scmarine = p13.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p14 and p5.IsAlliedWith(p14) then
				rmbo = p14.GetActorsByType("rmbo")
				e3 = p14.GetActorsByType("e3")
				e2 = p14.GetActorsByType("e2")
				e4 = p14.GetActorsByType("e4")
				e5 = p14.GetActorsByType("e5")
				e1 = p14.GetActorsByType("e1")
				e7 = p14.GetActorsByType("e7")
				shok = p14.GetActorsByType("shok")
				rae4 = p14.GetActorsByType("rae4")
				rae3 = p14.GetActorsByType("rae3")
				rae2 = p14.GetActorsByType("rae2")
				rae1 = p14.GetActorsByType("rae1")
				dog = p14.GetActorsByType("dog")
				scghost = p14.GetActorsByType("scghost")
				scfirebat = p14.GetActorsByType("scfirebat")
				scmarine = p14.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p15 and p5.IsAlliedWith(p15) then
				rmbo = p15.GetActorsByType("rmbo")
				e3 = p15.GetActorsByType("e3")
				e2 = p15.GetActorsByType("e2")
				e4 = p15.GetActorsByType("e4")
				e5 = p15.GetActorsByType("e5")
				e1 = p15.GetActorsByType("e1")
				e7 = p15.GetActorsByType("e7")
				shok = p15.GetActorsByType("shok")
				rae4 = p15.GetActorsByType("rae4")
				rae3 = p15.GetActorsByType("rae3")
				rae2 = p15.GetActorsByType("rae2")
				rae1 = p15.GetActorsByType("rae1")
				dog = p15.GetActorsByType("dog")
				scghost = p15.GetActorsByType("scghost")
				scfirebat = p15.GetActorsByType("scfirebat")
				scmarine = p15.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p16 and p5.IsAlliedWith(p16) then
				rmbo = p16.GetActorsByType("rmbo")
				e3 = p16.GetActorsByType("e3")
				e2 = p16.GetActorsByType("e2")
				e4 = p16.GetActorsByType("e4")
				e5 = p16.GetActorsByType("e5")
				e1 = p16.GetActorsByType("e1")
				e7 = p16.GetActorsByType("e7")
				shok = p16.GetActorsByType("shok")
				rae4 = p16.GetActorsByType("rae4")
				rae3 = p16.GetActorsByType("rae3")
				rae2 = p16.GetActorsByType("rae2")
				rae1 = p16.GetActorsByType("rae1")
				dog = p16.GetActorsByType("dog")
				scghost = p16.GetActorsByType("scghost")
				scfirebat = p16.GetActorsByType("scfirebat")
				scmarine = p16.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p1 and p5.IsAlliedWith(p1) then
				rmbo = p1.GetActorsByType("rmbo")
				e3 = p1.GetActorsByType("e3")
				e2 = p1.GetActorsByType("e2")
				e4 = p1.GetActorsByType("e4")
				e5 = p1.GetActorsByType("e5")
				e1 = p1.GetActorsByType("e1")
				e7 = p1.GetActorsByType("e7")
				shok = p1.GetActorsByType("shok")
				rae4 = p1.GetActorsByType("rae4")
				rae3 = p1.GetActorsByType("rae3")
				rae2 = p1.GetActorsByType("rae2")
				rae1 = p1.GetActorsByType("rae1")
				dog = p1.GetActorsByType("dog")
				scghost = p1.GetActorsByType("scghost")
				scfirebat = p1.GetActorsByType("scfirebat")
				scmarine = p1.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p2 and p5.IsAlliedWith(p2) then
				rmbo = p2.GetActorsByType("rmbo")
				e3 = p2.GetActorsByType("e3")
				e2 = p2.GetActorsByType("e2")
				e4 = p2.GetActorsByType("e4")
				e5 = p2.GetActorsByType("e5")
				e1 = p2.GetActorsByType("e1")
				e7 = p2.GetActorsByType("e7")
				shok = p2.GetActorsByType("shok")
				rae4 = p2.GetActorsByType("rae4")
				rae3 = p2.GetActorsByType("rae3")
				rae2 = p2.GetActorsByType("rae2")
				rae1 = p2.GetActorsByType("rae1")
				dog = p2.GetActorsByType("dog")
				scghost = p2.GetActorsByType("scghost")
				scfirebat = p2.GetActorsByType("scfirebat")
				scmarine = p2.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p3 and p5.IsAlliedWith(p3) then
				rmbo = p3.GetActorsByType("rmbo")
				e3 = p3.GetActorsByType("e3")
				e2 = p3.GetActorsByType("e2")
				e4 = p3.GetActorsByType("e4")
				e5 = p3.GetActorsByType("e5")
				e1 = p3.GetActorsByType("e1")
				e7 = p3.GetActorsByType("e7")
				shok = p3.GetActorsByType("shok")
				rae4 = p3.GetActorsByType("rae4")
				rae3 = p3.GetActorsByType("rae3")
				rae2 = p3.GetActorsByType("rae2")
				rae1 = p3.GetActorsByType("rae1")
				dog = p3.GetActorsByType("dog")
				scghost = p3.GetActorsByType("scghost")
				scfirebat = p3.GetActorsByType("scfirebat")
				scmarine = p3.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p4 and p5.IsAlliedWith(p4) then
				rmbo = p4.GetActorsByType("rmbo")
				e3 = p4.GetActorsByType("e3")
				e2 = p4.GetActorsByType("e2")
				e4 = p4.GetActorsByType("e4")
				e5 = p4.GetActorsByType("e5")
				e1 = p4.GetActorsByType("e1")
				e7 = p4.GetActorsByType("e7")
				shok = p4.GetActorsByType("shok")
				rae4 = p4.GetActorsByType("rae4")
				rae3 = p4.GetActorsByType("rae3")
				rae2 = p4.GetActorsByType("rae2")
				rae1 = p4.GetActorsByType("rae1")
				dog = p4.GetActorsByType("dog")
				scghost = p4.GetActorsByType("scghost")
				scfirebat = p4.GetActorsByType("scfirebat")
				scmarine = p4.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p5infantry()
	end)
end
p6infantry = function()
	if p6 and p6.IsBot then
		cityboteliteinstructor = p6.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p7 and p6.IsAlliedWith(p7) then
				rmbo = p7.GetActorsByType("rmbo")
				e3 = p7.GetActorsByType("e3")
				e2 = p7.GetActorsByType("e2")
				e4 = p7.GetActorsByType("e4")
				e5 = p7.GetActorsByType("e5")
				e1 = p7.GetActorsByType("e1")
				e7 = p7.GetActorsByType("e7")
				shok = p7.GetActorsByType("shok")
				rae4 = p7.GetActorsByType("rae4")
				rae3 = p7.GetActorsByType("rae3")
				rae2 = p7.GetActorsByType("rae2")
				rae1 = p7.GetActorsByType("rae1")
				dog = p7.GetActorsByType("dog")
				scghost = p7.GetActorsByType("scghost")
				scfirebat = p7.GetActorsByType("scfirebat")
				scmarine = p7.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p8 and p6.IsAlliedWith(p8) then
				rmbo = p8.GetActorsByType("rmbo")
				e3 = p8.GetActorsByType("e3")
				e2 = p8.GetActorsByType("e2")
				e4 = p8.GetActorsByType("e4")
				e5 = p8.GetActorsByType("e5")
				e1 = p8.GetActorsByType("e1")
				e7 = p8.GetActorsByType("e7")
				shok = p8.GetActorsByType("shok")
				rae4 = p8.GetActorsByType("rae4")
				rae3 = p8.GetActorsByType("rae3")
				rae2 = p8.GetActorsByType("rae2")
				rae1 = p8.GetActorsByType("rae1")
				dog = p8.GetActorsByType("dog")
				scghost = p8.GetActorsByType("scghost")
				scfirebat = p8.GetActorsByType("scfirebat")
				scmarine = p8.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p9 and p6.IsAlliedWith(p9) then
				rmbo = p9.GetActorsByType("rmbo")
				e3 = p9.GetActorsByType("e3")
				e2 = p9.GetActorsByType("e2")
				e4 = p9.GetActorsByType("e4")
				e5 = p9.GetActorsByType("e5")
				e1 = p9.GetActorsByType("e1")
				e7 = p9.GetActorsByType("e7")
				shok = p9.GetActorsByType("shok")
				rae4 = p9.GetActorsByType("rae4")
				rae3 = p9.GetActorsByType("rae3")
				rae2 = p9.GetActorsByType("rae2")
				rae1 = p9.GetActorsByType("rae1")
				dog = p9.GetActorsByType("dog")
				scghost = p9.GetActorsByType("scghost")
				scfirebat = p9.GetActorsByType("scfirebat")
				scmarine = p9.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p10 and p6.IsAlliedWith(p10) then
				rmbo = p10.GetActorsByType("rmbo")
				e3 = p10.GetActorsByType("e3")
				e2 = p10.GetActorsByType("e2")
				e4 = p10.GetActorsByType("e4")
				e5 = p10.GetActorsByType("e5")
				e1 = p10.GetActorsByType("e1")
				e7 = p10.GetActorsByType("e7")
				shok = p10.GetActorsByType("shok")
				rae4 = p10.GetActorsByType("rae4")
				rae3 = p10.GetActorsByType("rae3")
				rae2 = p10.GetActorsByType("rae2")
				rae1 = p10.GetActorsByType("rae1")
				dog = p10.GetActorsByType("dog")
				scghost = p10.GetActorsByType("scghost")
				scfirebat = p10.GetActorsByType("scfirebat")
				scmarine = p10.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p11 and p6.IsAlliedWith(p11) then
				rmbo = p11.GetActorsByType("rmbo")
				e3 = p11.GetActorsByType("e3")
				e2 = p11.GetActorsByType("e2")
				e4 = p11.GetActorsByType("e4")
				e5 = p11.GetActorsByType("e5")
				e1 = p11.GetActorsByType("e1")
				e7 = p11.GetActorsByType("e7")
				shok = p11.GetActorsByType("shok")
				rae4 = p11.GetActorsByType("rae4")
				rae3 = p11.GetActorsByType("rae3")
				rae2 = p11.GetActorsByType("rae2")
				rae1 = p11.GetActorsByType("rae1")
				dog = p11.GetActorsByType("dog")
				scghost = p11.GetActorsByType("scghost")
				scfirebat = p11.GetActorsByType("scfirebat")
				scmarine = p11.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p12 and p6.IsAlliedWith(p12) then
				rmbo = p12.GetActorsByType("rmbo")
				e3 = p12.GetActorsByType("e3")
				e2 = p12.GetActorsByType("e2")
				e4 = p12.GetActorsByType("e4")
				e5 = p12.GetActorsByType("e5")
				e1 = p12.GetActorsByType("e1")
				e7 = p12.GetActorsByType("e7")
				shok = p12.GetActorsByType("shok")
				rae4 = p12.GetActorsByType("rae4")
				rae3 = p12.GetActorsByType("rae3")
				rae2 = p12.GetActorsByType("rae2")
				rae1 = p12.GetActorsByType("rae1")
				dog = p12.GetActorsByType("dog")
				scghost = p12.GetActorsByType("scghost")
				scfirebat = p12.GetActorsByType("scfirebat")
				scmarine = p12.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p13 and p6.IsAlliedWith(p13) then
				rmbo = p13.GetActorsByType("rmbo")
				e3 = p13.GetActorsByType("e3")
				e2 = p13.GetActorsByType("e2")
				e4 = p13.GetActorsByType("e4")
				e5 = p13.GetActorsByType("e5")
				e1 = p13.GetActorsByType("e1")
				e7 = p13.GetActorsByType("e7")
				shok = p13.GetActorsByType("shok")
				rae4 = p13.GetActorsByType("rae4")
				rae3 = p13.GetActorsByType("rae3")
				rae2 = p13.GetActorsByType("rae2")
				rae1 = p13.GetActorsByType("rae1")
				dog = p13.GetActorsByType("dog")
				scghost = p13.GetActorsByType("scghost")
				scfirebat = p13.GetActorsByType("scfirebat")
				scmarine = p13.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p14 and p6.IsAlliedWith(p14) then
				rmbo = p14.GetActorsByType("rmbo")
				e3 = p14.GetActorsByType("e3")
				e2 = p14.GetActorsByType("e2")
				e4 = p14.GetActorsByType("e4")
				e5 = p14.GetActorsByType("e5")
				e1 = p14.GetActorsByType("e1")
				e7 = p14.GetActorsByType("e7")
				shok = p14.GetActorsByType("shok")
				rae4 = p14.GetActorsByType("rae4")
				rae3 = p14.GetActorsByType("rae3")
				rae2 = p14.GetActorsByType("rae2")
				rae1 = p14.GetActorsByType("rae1")
				dog = p14.GetActorsByType("dog")
				scghost = p14.GetActorsByType("scghost")
				scfirebat = p14.GetActorsByType("scfirebat")
				scmarine = p14.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p15 and p6.IsAlliedWith(p15) then
				rmbo = p15.GetActorsByType("rmbo")
				e3 = p15.GetActorsByType("e3")
				e2 = p15.GetActorsByType("e2")
				e4 = p15.GetActorsByType("e4")
				e5 = p15.GetActorsByType("e5")
				e1 = p15.GetActorsByType("e1")
				e7 = p15.GetActorsByType("e7")
				shok = p15.GetActorsByType("shok")
				rae4 = p15.GetActorsByType("rae4")
				rae3 = p15.GetActorsByType("rae3")
				rae2 = p15.GetActorsByType("rae2")
				rae1 = p15.GetActorsByType("rae1")
				dog = p15.GetActorsByType("dog")
				scghost = p15.GetActorsByType("scghost")
				scfirebat = p15.GetActorsByType("scfirebat")
				scmarine = p15.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p16 and p6.IsAlliedWith(p16) then
				rmbo = p16.GetActorsByType("rmbo")
				e3 = p16.GetActorsByType("e3")
				e2 = p16.GetActorsByType("e2")
				e4 = p16.GetActorsByType("e4")
				e5 = p16.GetActorsByType("e5")
				e1 = p16.GetActorsByType("e1")
				e7 = p16.GetActorsByType("e7")
				shok = p16.GetActorsByType("shok")
				rae4 = p16.GetActorsByType("rae4")
				rae3 = p16.GetActorsByType("rae3")
				rae2 = p16.GetActorsByType("rae2")
				rae1 = p16.GetActorsByType("rae1")
				dog = p16.GetActorsByType("dog")
				scghost = p16.GetActorsByType("scghost")
				scfirebat = p16.GetActorsByType("scfirebat")
				scmarine = p16.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p1 and p6.IsAlliedWith(p1) then
				rmbo = p1.GetActorsByType("rmbo")
				e3 = p1.GetActorsByType("e3")
				e2 = p1.GetActorsByType("e2")
				e4 = p1.GetActorsByType("e4")
				e5 = p1.GetActorsByType("e5")
				e1 = p1.GetActorsByType("e1")
				e7 = p1.GetActorsByType("e7")
				shok = p1.GetActorsByType("shok")
				rae4 = p1.GetActorsByType("rae4")
				rae3 = p1.GetActorsByType("rae3")
				rae2 = p1.GetActorsByType("rae2")
				rae1 = p1.GetActorsByType("rae1")
				dog = p1.GetActorsByType("dog")
				scghost = p1.GetActorsByType("scghost")
				scfirebat = p1.GetActorsByType("scfirebat")
				scmarine = p1.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p2 and p6.IsAlliedWith(p2) then
				rmbo = p2.GetActorsByType("rmbo")
				e3 = p2.GetActorsByType("e3")
				e2 = p2.GetActorsByType("e2")
				e4 = p2.GetActorsByType("e4")
				e5 = p2.GetActorsByType("e5")
				e1 = p2.GetActorsByType("e1")
				e7 = p2.GetActorsByType("e7")
				shok = p2.GetActorsByType("shok")
				rae4 = p2.GetActorsByType("rae4")
				rae3 = p2.GetActorsByType("rae3")
				rae2 = p2.GetActorsByType("rae2")
				rae1 = p2.GetActorsByType("rae1")
				dog = p2.GetActorsByType("dog")
				scghost = p2.GetActorsByType("scghost")
				scfirebat = p2.GetActorsByType("scfirebat")
				scmarine = p2.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p3 and p6.IsAlliedWith(p3) then
				rmbo = p3.GetActorsByType("rmbo")
				e3 = p3.GetActorsByType("e3")
				e2 = p3.GetActorsByType("e2")
				e4 = p3.GetActorsByType("e4")
				e5 = p3.GetActorsByType("e5")
				e1 = p3.GetActorsByType("e1")
				e7 = p3.GetActorsByType("e7")
				shok = p3.GetActorsByType("shok")
				rae4 = p3.GetActorsByType("rae4")
				rae3 = p3.GetActorsByType("rae3")
				rae2 = p3.GetActorsByType("rae2")
				rae1 = p3.GetActorsByType("rae1")
				dog = p3.GetActorsByType("dog")
				scghost = p3.GetActorsByType("scghost")
				scfirebat = p3.GetActorsByType("scfirebat")
				scmarine = p3.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p4 and p6.IsAlliedWith(p4) then
				rmbo = p4.GetActorsByType("rmbo")
				e3 = p4.GetActorsByType("e3")
				e2 = p4.GetActorsByType("e2")
				e4 = p4.GetActorsByType("e4")
				e5 = p4.GetActorsByType("e5")
				e1 = p4.GetActorsByType("e1")
				e7 = p4.GetActorsByType("e7")
				shok = p4.GetActorsByType("shok")
				rae4 = p4.GetActorsByType("rae4")
				rae3 = p4.GetActorsByType("rae3")
				rae2 = p4.GetActorsByType("rae2")
				rae1 = p4.GetActorsByType("rae1")
				dog = p4.GetActorsByType("dog")
				scghost = p4.GetActorsByType("scghost")
				scfirebat = p4.GetActorsByType("scfirebat")
				scmarine = p4.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p5 and p6.IsAlliedWith(p5) then
				rmbo = p5.GetActorsByType("rmbo")
				e3 = p5.GetActorsByType("e3")
				e2 = p5.GetActorsByType("e2")
				e4 = p5.GetActorsByType("e4")
				e5 = p5.GetActorsByType("e5")
				e1 = p5.GetActorsByType("e1")
				e7 = p5.GetActorsByType("e7")
				shok = p5.GetActorsByType("shok")
				rae4 = p5.GetActorsByType("rae4")
				rae3 = p5.GetActorsByType("rae3")
				rae2 = p5.GetActorsByType("rae2")
				rae1 = p5.GetActorsByType("rae1")
				dog = p5.GetActorsByType("dog")
				scghost = p5.GetActorsByType("scghost")
				scfirebat = p5.GetActorsByType("scfirebat")
				scmarine = p5.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p6infantry()
	end)
end
p7infantry = function()
	if p7 and p7.IsBot then
		cityboteliteinstructor = p7.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p8 and p7.IsAlliedWith(p8) then
				rmbo = p8.GetActorsByType("rmbo")
				e3 = p8.GetActorsByType("e3")
				e2 = p8.GetActorsByType("e2")
				e4 = p8.GetActorsByType("e4")
				e5 = p8.GetActorsByType("e5")
				e1 = p8.GetActorsByType("e1")
				e7 = p8.GetActorsByType("e7")
				shok = p8.GetActorsByType("shok")
				rae4 = p8.GetActorsByType("rae4")
				rae3 = p8.GetActorsByType("rae3")
				rae2 = p8.GetActorsByType("rae2")
				rae1 = p8.GetActorsByType("rae1")
				dog = p8.GetActorsByType("dog")
				scghost = p8.GetActorsByType("scghost")
				scfirebat = p8.GetActorsByType("scfirebat")
				scmarine = p8.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p9 and p7.IsAlliedWith(p9) then
				rmbo = p9.GetActorsByType("rmbo")
				e3 = p9.GetActorsByType("e3")
				e2 = p9.GetActorsByType("e2")
				e4 = p9.GetActorsByType("e4")
				e5 = p9.GetActorsByType("e5")
				e1 = p9.GetActorsByType("e1")
				e7 = p9.GetActorsByType("e7")
				shok = p9.GetActorsByType("shok")
				rae4 = p9.GetActorsByType("rae4")
				rae3 = p9.GetActorsByType("rae3")
				rae2 = p9.GetActorsByType("rae2")
				rae1 = p9.GetActorsByType("rae1")
				dog = p9.GetActorsByType("dog")
				scghost = p9.GetActorsByType("scghost")
				scfirebat = p9.GetActorsByType("scfirebat")
				scmarine = p9.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p10 and p7.IsAlliedWith(p10) then
				rmbo = p10.GetActorsByType("rmbo")
				e3 = p10.GetActorsByType("e3")
				e2 = p10.GetActorsByType("e2")
				e4 = p10.GetActorsByType("e4")
				e5 = p10.GetActorsByType("e5")
				e1 = p10.GetActorsByType("e1")
				e7 = p10.GetActorsByType("e7")
				shok = p10.GetActorsByType("shok")
				rae4 = p10.GetActorsByType("rae4")
				rae3 = p10.GetActorsByType("rae3")
				rae2 = p10.GetActorsByType("rae2")
				rae1 = p10.GetActorsByType("rae1")
				dog = p10.GetActorsByType("dog")
				scghost = p10.GetActorsByType("scghost")
				scfirebat = p10.GetActorsByType("scfirebat")
				scmarine = p10.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p11 and p7.IsAlliedWith(p11) then
				rmbo = p11.GetActorsByType("rmbo")
				e3 = p11.GetActorsByType("e3")
				e2 = p11.GetActorsByType("e2")
				e4 = p11.GetActorsByType("e4")
				e5 = p11.GetActorsByType("e5")
				e1 = p11.GetActorsByType("e1")
				e7 = p11.GetActorsByType("e7")
				shok = p11.GetActorsByType("shok")
				rae4 = p11.GetActorsByType("rae4")
				rae3 = p11.GetActorsByType("rae3")
				rae2 = p11.GetActorsByType("rae2")
				rae1 = p11.GetActorsByType("rae1")
				dog = p11.GetActorsByType("dog")
				scghost = p11.GetActorsByType("scghost")
				scfirebat = p11.GetActorsByType("scfirebat")
				scmarine = p11.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p12 and p7.IsAlliedWith(p12) then
				rmbo = p12.GetActorsByType("rmbo")
				e3 = p12.GetActorsByType("e3")
				e2 = p12.GetActorsByType("e2")
				e4 = p12.GetActorsByType("e4")
				e5 = p12.GetActorsByType("e5")
				e1 = p12.GetActorsByType("e1")
				e7 = p12.GetActorsByType("e7")
				shok = p12.GetActorsByType("shok")
				rae4 = p12.GetActorsByType("rae4")
				rae3 = p12.GetActorsByType("rae3")
				rae2 = p12.GetActorsByType("rae2")
				rae1 = p12.GetActorsByType("rae1")
				dog = p12.GetActorsByType("dog")
				scghost = p12.GetActorsByType("scghost")
				scfirebat = p12.GetActorsByType("scfirebat")
				scmarine = p12.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p13 and p7.IsAlliedWith(p13) then
				rmbo = p13.GetActorsByType("rmbo")
				e3 = p13.GetActorsByType("e3")
				e2 = p13.GetActorsByType("e2")
				e4 = p13.GetActorsByType("e4")
				e5 = p13.GetActorsByType("e5")
				e1 = p13.GetActorsByType("e1")
				e7 = p13.GetActorsByType("e7")
				shok = p13.GetActorsByType("shok")
				rae4 = p13.GetActorsByType("rae4")
				rae3 = p13.GetActorsByType("rae3")
				rae2 = p13.GetActorsByType("rae2")
				rae1 = p13.GetActorsByType("rae1")
				dog = p13.GetActorsByType("dog")
				scghost = p13.GetActorsByType("scghost")
				scfirebat = p13.GetActorsByType("scfirebat")
				scmarine = p13.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p14 and p7.IsAlliedWith(p14) then
				rmbo = p14.GetActorsByType("rmbo")
				e3 = p14.GetActorsByType("e3")
				e2 = p14.GetActorsByType("e2")
				e4 = p14.GetActorsByType("e4")
				e5 = p14.GetActorsByType("e5")
				e1 = p14.GetActorsByType("e1")
				e7 = p14.GetActorsByType("e7")
				shok = p14.GetActorsByType("shok")
				rae4 = p14.GetActorsByType("rae4")
				rae3 = p14.GetActorsByType("rae3")
				rae2 = p14.GetActorsByType("rae2")
				rae1 = p14.GetActorsByType("rae1")
				dog = p14.GetActorsByType("dog")
				scghost = p14.GetActorsByType("scghost")
				scfirebat = p14.GetActorsByType("scfirebat")
				scmarine = p14.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p15 and p7.IsAlliedWith(p15) then
				rmbo = p15.GetActorsByType("rmbo")
				e3 = p15.GetActorsByType("e3")
				e2 = p15.GetActorsByType("e2")
				e4 = p15.GetActorsByType("e4")
				e5 = p15.GetActorsByType("e5")
				e1 = p15.GetActorsByType("e1")
				e7 = p15.GetActorsByType("e7")
				shok = p15.GetActorsByType("shok")
				rae4 = p15.GetActorsByType("rae4")
				rae3 = p15.GetActorsByType("rae3")
				rae2 = p15.GetActorsByType("rae2")
				rae1 = p15.GetActorsByType("rae1")
				dog = p15.GetActorsByType("dog")
				scghost = p15.GetActorsByType("scghost")
				scfirebat = p15.GetActorsByType("scfirebat")
				scmarine = p15.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p16 and p7.IsAlliedWith(p16) then
				rmbo = p16.GetActorsByType("rmbo")
				e3 = p16.GetActorsByType("e3")
				e2 = p16.GetActorsByType("e2")
				e4 = p16.GetActorsByType("e4")
				e5 = p16.GetActorsByType("e5")
				e1 = p16.GetActorsByType("e1")
				e7 = p16.GetActorsByType("e7")
				shok = p16.GetActorsByType("shok")
				rae4 = p16.GetActorsByType("rae4")
				rae3 = p16.GetActorsByType("rae3")
				rae2 = p16.GetActorsByType("rae2")
				rae1 = p16.GetActorsByType("rae1")
				dog = p16.GetActorsByType("dog")
				scghost = p16.GetActorsByType("scghost")
				scfirebat = p16.GetActorsByType("scfirebat")
				scmarine = p16.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p1 and p7.IsAlliedWith(p1) then
				rmbo = p1.GetActorsByType("rmbo")
				e3 = p1.GetActorsByType("e3")
				e2 = p1.GetActorsByType("e2")
				e4 = p1.GetActorsByType("e4")
				e5 = p1.GetActorsByType("e5")
				e1 = p1.GetActorsByType("e1")
				e7 = p1.GetActorsByType("e7")
				shok = p1.GetActorsByType("shok")
				rae4 = p1.GetActorsByType("rae4")
				rae3 = p1.GetActorsByType("rae3")
				rae2 = p1.GetActorsByType("rae2")
				rae1 = p1.GetActorsByType("rae1")
				dog = p1.GetActorsByType("dog")
				scghost = p1.GetActorsByType("scghost")
				scfirebat = p1.GetActorsByType("scfirebat")
				scmarine = p1.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p2 and p7.IsAlliedWith(p2) then
				rmbo = p2.GetActorsByType("rmbo")
				e3 = p2.GetActorsByType("e3")
				e2 = p2.GetActorsByType("e2")
				e4 = p2.GetActorsByType("e4")
				e5 = p2.GetActorsByType("e5")
				e1 = p2.GetActorsByType("e1")
				e7 = p2.GetActorsByType("e7")
				shok = p2.GetActorsByType("shok")
				rae4 = p2.GetActorsByType("rae4")
				rae3 = p2.GetActorsByType("rae3")
				rae2 = p2.GetActorsByType("rae2")
				rae1 = p2.GetActorsByType("rae1")
				dog = p2.GetActorsByType("dog")
				scghost = p2.GetActorsByType("scghost")
				scfirebat = p2.GetActorsByType("scfirebat")
				scmarine = p2.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p3 and p7.IsAlliedWith(p3) then
				rmbo = p3.GetActorsByType("rmbo")
				e3 = p3.GetActorsByType("e3")
				e2 = p3.GetActorsByType("e2")
				e4 = p3.GetActorsByType("e4")
				e5 = p3.GetActorsByType("e5")
				e1 = p3.GetActorsByType("e1")
				e7 = p3.GetActorsByType("e7")
				shok = p3.GetActorsByType("shok")
				rae4 = p3.GetActorsByType("rae4")
				rae3 = p3.GetActorsByType("rae3")
				rae2 = p3.GetActorsByType("rae2")
				rae1 = p3.GetActorsByType("rae1")
				dog = p3.GetActorsByType("dog")
				scghost = p3.GetActorsByType("scghost")
				scfirebat = p3.GetActorsByType("scfirebat")
				scmarine = p3.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p4 and p7.IsAlliedWith(p4) then
				rmbo = p4.GetActorsByType("rmbo")
				e3 = p4.GetActorsByType("e3")
				e2 = p4.GetActorsByType("e2")
				e4 = p4.GetActorsByType("e4")
				e5 = p4.GetActorsByType("e5")
				e1 = p4.GetActorsByType("e1")
				e7 = p4.GetActorsByType("e7")
				shok = p4.GetActorsByType("shok")
				rae4 = p4.GetActorsByType("rae4")
				rae3 = p4.GetActorsByType("rae3")
				rae2 = p4.GetActorsByType("rae2")
				rae1 = p4.GetActorsByType("rae1")
				dog = p4.GetActorsByType("dog")
				scghost = p4.GetActorsByType("scghost")
				scfirebat = p4.GetActorsByType("scfirebat")
				scmarine = p4.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p5 and p7.IsAlliedWith(p5) then
				rmbo = p5.GetActorsByType("rmbo")
				e3 = p5.GetActorsByType("e3")
				e2 = p5.GetActorsByType("e2")
				e4 = p5.GetActorsByType("e4")
				e5 = p5.GetActorsByType("e5")
				e1 = p5.GetActorsByType("e1")
				e7 = p5.GetActorsByType("e7")
				shok = p5.GetActorsByType("shok")
				rae4 = p5.GetActorsByType("rae4")
				rae3 = p5.GetActorsByType("rae3")
				rae2 = p5.GetActorsByType("rae2")
				rae1 = p5.GetActorsByType("rae1")
				dog = p5.GetActorsByType("dog")
				scghost = p5.GetActorsByType("scghost")
				scfirebat = p5.GetActorsByType("scfirebat")
				scmarine = p5.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p6 and p7.IsAlliedWith(p6) then
				rmbo = p6.GetActorsByType("rmbo")
				e3 = p6.GetActorsByType("e3")
				e2 = p6.GetActorsByType("e2")
				e4 = p6.GetActorsByType("e4")
				e5 = p6.GetActorsByType("e5")
				e1 = p6.GetActorsByType("e1")
				e7 = p6.GetActorsByType("e7")
				shok = p6.GetActorsByType("shok")
				rae4 = p6.GetActorsByType("rae4")
				rae3 = p6.GetActorsByType("rae3")
				rae2 = p6.GetActorsByType("rae2")
				rae1 = p6.GetActorsByType("rae1")
				dog = p6.GetActorsByType("dog")
				scghost = p6.GetActorsByType("scghost")
				scfirebat = p6.GetActorsByType("scfirebat")
				scmarine = p6.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p7infantry()
	end)
end
p8infantry = function()
	if p8 and p8.IsBot then
		cityboteliteinstructor = p8.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p9 and p8.IsAlliedWith(p9) then
				rmbo = p9.GetActorsByType("rmbo")
				e3 = p9.GetActorsByType("e3")
				e2 = p9.GetActorsByType("e2")
				e4 = p9.GetActorsByType("e4")
				e5 = p9.GetActorsByType("e5")
				e1 = p9.GetActorsByType("e1")
				e7 = p9.GetActorsByType("e7")
				shok = p9.GetActorsByType("shok")
				rae4 = p9.GetActorsByType("rae4")
				rae3 = p9.GetActorsByType("rae3")
				rae2 = p9.GetActorsByType("rae2")
				rae1 = p9.GetActorsByType("rae1")
				dog = p9.GetActorsByType("dog")
				scghost = p9.GetActorsByType("scghost")
				scfirebat = p9.GetActorsByType("scfirebat")
				scmarine = p9.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p10 and p8.IsAlliedWith(p10) then
				rmbo = p10.GetActorsByType("rmbo")
				e3 = p10.GetActorsByType("e3")
				e2 = p10.GetActorsByType("e2")
				e4 = p10.GetActorsByType("e4")
				e5 = p10.GetActorsByType("e5")
				e1 = p10.GetActorsByType("e1")
				e7 = p10.GetActorsByType("e7")
				shok = p10.GetActorsByType("shok")
				rae4 = p10.GetActorsByType("rae4")
				rae3 = p10.GetActorsByType("rae3")
				rae2 = p10.GetActorsByType("rae2")
				rae1 = p10.GetActorsByType("rae1")
				dog = p10.GetActorsByType("dog")
				scghost = p10.GetActorsByType("scghost")
				scfirebat = p10.GetActorsByType("scfirebat")
				scmarine = p10.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p11 and p8.IsAlliedWith(p11) then
				rmbo = p11.GetActorsByType("rmbo")
				e3 = p11.GetActorsByType("e3")
				e2 = p11.GetActorsByType("e2")
				e4 = p11.GetActorsByType("e4")
				e5 = p11.GetActorsByType("e5")
				e1 = p11.GetActorsByType("e1")
				e7 = p11.GetActorsByType("e7")
				shok = p11.GetActorsByType("shok")
				rae4 = p11.GetActorsByType("rae4")
				rae3 = p11.GetActorsByType("rae3")
				rae2 = p11.GetActorsByType("rae2")
				rae1 = p11.GetActorsByType("rae1")
				dog = p11.GetActorsByType("dog")
				scghost = p11.GetActorsByType("scghost")
				scfirebat = p11.GetActorsByType("scfirebat")
				scmarine = p11.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p12 and p8.IsAlliedWith(p12) then
				rmbo = p12.GetActorsByType("rmbo")
				e3 = p12.GetActorsByType("e3")
				e2 = p12.GetActorsByType("e2")
				e4 = p12.GetActorsByType("e4")
				e5 = p12.GetActorsByType("e5")
				e1 = p12.GetActorsByType("e1")
				e7 = p12.GetActorsByType("e7")
				shok = p12.GetActorsByType("shok")
				rae4 = p12.GetActorsByType("rae4")
				rae3 = p12.GetActorsByType("rae3")
				rae2 = p12.GetActorsByType("rae2")
				rae1 = p12.GetActorsByType("rae1")
				dog = p12.GetActorsByType("dog")
				scghost = p12.GetActorsByType("scghost")
				scfirebat = p12.GetActorsByType("scfirebat")
				scmarine = p12.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p13 and p8.IsAlliedWith(p13) then
				rmbo = p13.GetActorsByType("rmbo")
				e3 = p13.GetActorsByType("e3")
				e2 = p13.GetActorsByType("e2")
				e4 = p13.GetActorsByType("e4")
				e5 = p13.GetActorsByType("e5")
				e1 = p13.GetActorsByType("e1")
				e7 = p13.GetActorsByType("e7")
				shok = p13.GetActorsByType("shok")
				rae4 = p13.GetActorsByType("rae4")
				rae3 = p13.GetActorsByType("rae3")
				rae2 = p13.GetActorsByType("rae2")
				rae1 = p13.GetActorsByType("rae1")
				dog = p13.GetActorsByType("dog")
				scghost = p13.GetActorsByType("scghost")
				scfirebat = p13.GetActorsByType("scfirebat")
				scmarine = p13.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p14 and p8.IsAlliedWith(p14) then
				rmbo = p14.GetActorsByType("rmbo")
				e3 = p14.GetActorsByType("e3")
				e2 = p14.GetActorsByType("e2")
				e4 = p14.GetActorsByType("e4")
				e5 = p14.GetActorsByType("e5")
				e1 = p14.GetActorsByType("e1")
				e7 = p14.GetActorsByType("e7")
				shok = p14.GetActorsByType("shok")
				rae4 = p14.GetActorsByType("rae4")
				rae3 = p14.GetActorsByType("rae3")
				rae2 = p14.GetActorsByType("rae2")
				rae1 = p14.GetActorsByType("rae1")
				dog = p14.GetActorsByType("dog")
				scghost = p14.GetActorsByType("scghost")
				scfirebat = p14.GetActorsByType("scfirebat")
				scmarine = p14.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p15 and p8.IsAlliedWith(p15) then
				rmbo = p15.GetActorsByType("rmbo")
				e3 = p15.GetActorsByType("e3")
				e2 = p15.GetActorsByType("e2")
				e4 = p15.GetActorsByType("e4")
				e5 = p15.GetActorsByType("e5")
				e1 = p15.GetActorsByType("e1")
				e7 = p15.GetActorsByType("e7")
				shok = p15.GetActorsByType("shok")
				rae4 = p15.GetActorsByType("rae4")
				rae3 = p15.GetActorsByType("rae3")
				rae2 = p15.GetActorsByType("rae2")
				rae1 = p15.GetActorsByType("rae1")
				dog = p15.GetActorsByType("dog")
				scghost = p15.GetActorsByType("scghost")
				scfirebat = p15.GetActorsByType("scfirebat")
				scmarine = p15.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p16 and p8.IsAlliedWith(p16) then
				rmbo = p16.GetActorsByType("rmbo")
				e3 = p16.GetActorsByType("e3")
				e2 = p16.GetActorsByType("e2")
				e4 = p16.GetActorsByType("e4")
				e5 = p16.GetActorsByType("e5")
				e1 = p16.GetActorsByType("e1")
				e7 = p16.GetActorsByType("e7")
				shok = p16.GetActorsByType("shok")
				rae4 = p16.GetActorsByType("rae4")
				rae3 = p16.GetActorsByType("rae3")
				rae2 = p16.GetActorsByType("rae2")
				rae1 = p16.GetActorsByType("rae1")
				dog = p16.GetActorsByType("dog")
				scghost = p16.GetActorsByType("scghost")
				scfirebat = p16.GetActorsByType("scfirebat")
				scmarine = p16.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p1 and p8.IsAlliedWith(p1) then
				rmbo = p1.GetActorsByType("rmbo")
				e3 = p1.GetActorsByType("e3")
				e2 = p1.GetActorsByType("e2")
				e4 = p1.GetActorsByType("e4")
				e5 = p1.GetActorsByType("e5")
				e1 = p1.GetActorsByType("e1")
				e7 = p1.GetActorsByType("e7")
				shok = p1.GetActorsByType("shok")
				rae4 = p1.GetActorsByType("rae4")
				rae3 = p1.GetActorsByType("rae3")
				rae2 = p1.GetActorsByType("rae2")
				rae1 = p1.GetActorsByType("rae1")
				dog = p1.GetActorsByType("dog")
				scghost = p1.GetActorsByType("scghost")
				scfirebat = p1.GetActorsByType("scfirebat")
				scmarine = p1.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p2 and p8.IsAlliedWith(p2) then
				rmbo = p2.GetActorsByType("rmbo")
				e3 = p2.GetActorsByType("e3")
				e2 = p2.GetActorsByType("e2")
				e4 = p2.GetActorsByType("e4")
				e5 = p2.GetActorsByType("e5")
				e1 = p2.GetActorsByType("e1")
				e7 = p2.GetActorsByType("e7")
				shok = p2.GetActorsByType("shok")
				rae4 = p2.GetActorsByType("rae4")
				rae3 = p2.GetActorsByType("rae3")
				rae2 = p2.GetActorsByType("rae2")
				rae1 = p2.GetActorsByType("rae1")
				dog = p2.GetActorsByType("dog")
				scghost = p2.GetActorsByType("scghost")
				scfirebat = p2.GetActorsByType("scfirebat")
				scmarine = p2.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p3 and p8.IsAlliedWith(p3) then
				rmbo = p3.GetActorsByType("rmbo")
				e3 = p3.GetActorsByType("e3")
				e2 = p3.GetActorsByType("e2")
				e4 = p3.GetActorsByType("e4")
				e5 = p3.GetActorsByType("e5")
				e1 = p3.GetActorsByType("e1")
				e7 = p3.GetActorsByType("e7")
				shok = p3.GetActorsByType("shok")
				rae4 = p3.GetActorsByType("rae4")
				rae3 = p3.GetActorsByType("rae3")
				rae2 = p3.GetActorsByType("rae2")
				rae1 = p3.GetActorsByType("rae1")
				dog = p3.GetActorsByType("dog")
				scghost = p3.GetActorsByType("scghost")
				scfirebat = p3.GetActorsByType("scfirebat")
				scmarine = p3.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p4 and p8.IsAlliedWith(p4) then
				rmbo = p4.GetActorsByType("rmbo")
				e3 = p4.GetActorsByType("e3")
				e2 = p4.GetActorsByType("e2")
				e4 = p4.GetActorsByType("e4")
				e5 = p4.GetActorsByType("e5")
				e1 = p4.GetActorsByType("e1")
				e7 = p4.GetActorsByType("e7")
				shok = p4.GetActorsByType("shok")
				rae4 = p4.GetActorsByType("rae4")
				rae3 = p4.GetActorsByType("rae3")
				rae2 = p4.GetActorsByType("rae2")
				rae1 = p4.GetActorsByType("rae1")
				dog = p4.GetActorsByType("dog")
				scghost = p4.GetActorsByType("scghost")
				scfirebat = p4.GetActorsByType("scfirebat")
				scmarine = p4.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p5 and p8.IsAlliedWith(p5) then
				rmbo = p5.GetActorsByType("rmbo")
				e3 = p5.GetActorsByType("e3")
				e2 = p5.GetActorsByType("e2")
				e4 = p5.GetActorsByType("e4")
				e5 = p5.GetActorsByType("e5")
				e1 = p5.GetActorsByType("e1")
				e7 = p5.GetActorsByType("e7")
				shok = p5.GetActorsByType("shok")
				rae4 = p5.GetActorsByType("rae4")
				rae3 = p5.GetActorsByType("rae3")
				rae2 = p5.GetActorsByType("rae2")
				rae1 = p5.GetActorsByType("rae1")
				dog = p5.GetActorsByType("dog")
				scghost = p5.GetActorsByType("scghost")
				scfirebat = p5.GetActorsByType("scfirebat")
				scmarine = p5.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p6 and p8.IsAlliedWith(p6) then
				rmbo = p6.GetActorsByType("rmbo")
				e3 = p6.GetActorsByType("e3")
				e2 = p6.GetActorsByType("e2")
				e4 = p6.GetActorsByType("e4")
				e5 = p6.GetActorsByType("e5")
				e1 = p6.GetActorsByType("e1")
				e7 = p6.GetActorsByType("e7")
				shok = p6.GetActorsByType("shok")
				rae4 = p6.GetActorsByType("rae4")
				rae3 = p6.GetActorsByType("rae3")
				rae2 = p6.GetActorsByType("rae2")
				rae1 = p6.GetActorsByType("rae1")
				dog = p6.GetActorsByType("dog")
				scghost = p6.GetActorsByType("scghost")
				scfirebat = p6.GetActorsByType("scfirebat")
				scmarine = p6.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p7 and p8.IsAlliedWith(p7) then
				rmbo = p7.GetActorsByType("rmbo")
				e3 = p7.GetActorsByType("e3")
				e2 = p7.GetActorsByType("e2")
				e4 = p7.GetActorsByType("e4")
				e5 = p7.GetActorsByType("e5")
				e1 = p7.GetActorsByType("e1")
				e7 = p7.GetActorsByType("e7")
				shok = p7.GetActorsByType("shok")
				rae4 = p7.GetActorsByType("rae4")
				rae3 = p7.GetActorsByType("rae3")
				rae2 = p7.GetActorsByType("rae2")
				rae1 = p7.GetActorsByType("rae1")
				dog = p7.GetActorsByType("dog")
				scghost = p7.GetActorsByType("scghost")
				scfirebat = p7.GetActorsByType("scfirebat")
				scmarine = p7.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p8infantry()
	end)
end
p9infantry = function()
	if p9 and p9.IsBot then
		cityboteliteinstructor = p9.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p10 and p9.IsAlliedWith(p10) then
				rmbo = p10.GetActorsByType("rmbo")
				e3 = p10.GetActorsByType("e3")
				e2 = p10.GetActorsByType("e2")
				e4 = p10.GetActorsByType("e4")
				e5 = p10.GetActorsByType("e5")
				e1 = p10.GetActorsByType("e1")
				e7 = p10.GetActorsByType("e7")
				shok = p10.GetActorsByType("shok")
				rae4 = p10.GetActorsByType("rae4")
				rae3 = p10.GetActorsByType("rae3")
				rae2 = p10.GetActorsByType("rae2")
				rae1 = p10.GetActorsByType("rae1")
				dog = p10.GetActorsByType("dog")
				scghost = p10.GetActorsByType("scghost")
				scfirebat = p10.GetActorsByType("scfirebat")
				scmarine = p10.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p11 and p9.IsAlliedWith(p11) then
				rmbo = p11.GetActorsByType("rmbo")
				e3 = p11.GetActorsByType("e3")
				e2 = p11.GetActorsByType("e2")
				e4 = p11.GetActorsByType("e4")
				e5 = p11.GetActorsByType("e5")
				e1 = p11.GetActorsByType("e1")
				e7 = p11.GetActorsByType("e7")
				shok = p11.GetActorsByType("shok")
				rae4 = p11.GetActorsByType("rae4")
				rae3 = p11.GetActorsByType("rae3")
				rae2 = p11.GetActorsByType("rae2")
				rae1 = p11.GetActorsByType("rae1")
				dog = p11.GetActorsByType("dog")
				scghost = p11.GetActorsByType("scghost")
				scfirebat = p11.GetActorsByType("scfirebat")
				scmarine = p11.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p12 and p9.IsAlliedWith(p12) then
				rmbo = p12.GetActorsByType("rmbo")
				e3 = p12.GetActorsByType("e3")
				e2 = p12.GetActorsByType("e2")
				e4 = p12.GetActorsByType("e4")
				e5 = p12.GetActorsByType("e5")
				e1 = p12.GetActorsByType("e1")
				e7 = p12.GetActorsByType("e7")
				shok = p12.GetActorsByType("shok")
				rae4 = p12.GetActorsByType("rae4")
				rae3 = p12.GetActorsByType("rae3")
				rae2 = p12.GetActorsByType("rae2")
				rae1 = p12.GetActorsByType("rae1")
				dog = p12.GetActorsByType("dog")
				scghost = p12.GetActorsByType("scghost")
				scfirebat = p12.GetActorsByType("scfirebat")
				scmarine = p12.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p13 and p9.IsAlliedWith(p13) then
				rmbo = p13.GetActorsByType("rmbo")
				e3 = p13.GetActorsByType("e3")
				e2 = p13.GetActorsByType("e2")
				e4 = p13.GetActorsByType("e4")
				e5 = p13.GetActorsByType("e5")
				e1 = p13.GetActorsByType("e1")
				e7 = p13.GetActorsByType("e7")
				shok = p13.GetActorsByType("shok")
				rae4 = p13.GetActorsByType("rae4")
				rae3 = p13.GetActorsByType("rae3")
				rae2 = p13.GetActorsByType("rae2")
				rae1 = p13.GetActorsByType("rae1")
				dog = p13.GetActorsByType("dog")
				scghost = p13.GetActorsByType("scghost")
				scfirebat = p13.GetActorsByType("scfirebat")
				scmarine = p13.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p14 and p9.IsAlliedWith(p14) then
				rmbo = p14.GetActorsByType("rmbo")
				e3 = p14.GetActorsByType("e3")
				e2 = p14.GetActorsByType("e2")
				e4 = p14.GetActorsByType("e4")
				e5 = p14.GetActorsByType("e5")
				e1 = p14.GetActorsByType("e1")
				e7 = p14.GetActorsByType("e7")
				shok = p14.GetActorsByType("shok")
				rae4 = p14.GetActorsByType("rae4")
				rae3 = p14.GetActorsByType("rae3")
				rae2 = p14.GetActorsByType("rae2")
				rae1 = p14.GetActorsByType("rae1")
				dog = p14.GetActorsByType("dog")
				scghost = p14.GetActorsByType("scghost")
				scfirebat = p14.GetActorsByType("scfirebat")
				scmarine = p14.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p15 and p9.IsAlliedWith(p15) then
				rmbo = p15.GetActorsByType("rmbo")
				e3 = p15.GetActorsByType("e3")
				e2 = p15.GetActorsByType("e2")
				e4 = p15.GetActorsByType("e4")
				e5 = p15.GetActorsByType("e5")
				e1 = p15.GetActorsByType("e1")
				e7 = p15.GetActorsByType("e7")
				shok = p15.GetActorsByType("shok")
				rae4 = p15.GetActorsByType("rae4")
				rae3 = p15.GetActorsByType("rae3")
				rae2 = p15.GetActorsByType("rae2")
				rae1 = p15.GetActorsByType("rae1")
				dog = p15.GetActorsByType("dog")
				scghost = p15.GetActorsByType("scghost")
				scfirebat = p15.GetActorsByType("scfirebat")
				scmarine = p15.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p16 and p9.IsAlliedWith(p16) then
				rmbo = p16.GetActorsByType("rmbo")
				e3 = p16.GetActorsByType("e3")
				e2 = p16.GetActorsByType("e2")
				e4 = p16.GetActorsByType("e4")
				e5 = p16.GetActorsByType("e5")
				e1 = p16.GetActorsByType("e1")
				e7 = p16.GetActorsByType("e7")
				shok = p16.GetActorsByType("shok")
				rae4 = p16.GetActorsByType("rae4")
				rae3 = p16.GetActorsByType("rae3")
				rae2 = p16.GetActorsByType("rae2")
				rae1 = p16.GetActorsByType("rae1")
				dog = p16.GetActorsByType("dog")
				scghost = p16.GetActorsByType("scghost")
				scfirebat = p16.GetActorsByType("scfirebat")
				scmarine = p16.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p1 and p9.IsAlliedWith(p1) then
				rmbo = p1.GetActorsByType("rmbo")
				e3 = p1.GetActorsByType("e3")
				e2 = p1.GetActorsByType("e2")
				e4 = p1.GetActorsByType("e4")
				e5 = p1.GetActorsByType("e5")
				e1 = p1.GetActorsByType("e1")
				e7 = p1.GetActorsByType("e7")
				shok = p1.GetActorsByType("shok")
				rae4 = p1.GetActorsByType("rae4")
				rae3 = p1.GetActorsByType("rae3")
				rae2 = p1.GetActorsByType("rae2")
				rae1 = p1.GetActorsByType("rae1")
				dog = p1.GetActorsByType("dog")
				scghost = p1.GetActorsByType("scghost")
				scfirebat = p1.GetActorsByType("scfirebat")
				scmarine = p1.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p2 and p9.IsAlliedWith(p2) then
				rmbo = p2.GetActorsByType("rmbo")
				e3 = p2.GetActorsByType("e3")
				e2 = p2.GetActorsByType("e2")
				e4 = p2.GetActorsByType("e4")
				e5 = p2.GetActorsByType("e5")
				e1 = p2.GetActorsByType("e1")
				e7 = p2.GetActorsByType("e7")
				shok = p2.GetActorsByType("shok")
				rae4 = p2.GetActorsByType("rae4")
				rae3 = p2.GetActorsByType("rae3")
				rae2 = p2.GetActorsByType("rae2")
				rae1 = p2.GetActorsByType("rae1")
				dog = p2.GetActorsByType("dog")
				scghost = p2.GetActorsByType("scghost")
				scfirebat = p2.GetActorsByType("scfirebat")
				scmarine = p2.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p3 and p9.IsAlliedWith(p3) then
				rmbo = p3.GetActorsByType("rmbo")
				e3 = p3.GetActorsByType("e3")
				e2 = p3.GetActorsByType("e2")
				e4 = p3.GetActorsByType("e4")
				e5 = p3.GetActorsByType("e5")
				e1 = p3.GetActorsByType("e1")
				e7 = p3.GetActorsByType("e7")
				shok = p3.GetActorsByType("shok")
				rae4 = p3.GetActorsByType("rae4")
				rae3 = p3.GetActorsByType("rae3")
				rae2 = p3.GetActorsByType("rae2")
				rae1 = p3.GetActorsByType("rae1")
				dog = p3.GetActorsByType("dog")
				scghost = p3.GetActorsByType("scghost")
				scfirebat = p3.GetActorsByType("scfirebat")
				scmarine = p3.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p4 and p9.IsAlliedWith(p4) then
				rmbo = p4.GetActorsByType("rmbo")
				e3 = p4.GetActorsByType("e3")
				e2 = p4.GetActorsByType("e2")
				e4 = p4.GetActorsByType("e4")
				e5 = p4.GetActorsByType("e5")
				e1 = p4.GetActorsByType("e1")
				e7 = p4.GetActorsByType("e7")
				shok = p4.GetActorsByType("shok")
				rae4 = p4.GetActorsByType("rae4")
				rae3 = p4.GetActorsByType("rae3")
				rae2 = p4.GetActorsByType("rae2")
				rae1 = p4.GetActorsByType("rae1")
				dog = p4.GetActorsByType("dog")
				scghost = p4.GetActorsByType("scghost")
				scfirebat = p4.GetActorsByType("scfirebat")
				scmarine = p4.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p5 and p9.IsAlliedWith(p5) then
				rmbo = p5.GetActorsByType("rmbo")
				e3 = p5.GetActorsByType("e3")
				e2 = p5.GetActorsByType("e2")
				e4 = p5.GetActorsByType("e4")
				e5 = p5.GetActorsByType("e5")
				e1 = p5.GetActorsByType("e1")
				e7 = p5.GetActorsByType("e7")
				shok = p5.GetActorsByType("shok")
				rae4 = p5.GetActorsByType("rae4")
				rae3 = p5.GetActorsByType("rae3")
				rae2 = p5.GetActorsByType("rae2")
				rae1 = p5.GetActorsByType("rae1")
				dog = p5.GetActorsByType("dog")
				scghost = p5.GetActorsByType("scghost")
				scfirebat = p5.GetActorsByType("scfirebat")
				scmarine = p5.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p6 and p9.IsAlliedWith(p6) then
				rmbo = p6.GetActorsByType("rmbo")
				e3 = p6.GetActorsByType("e3")
				e2 = p6.GetActorsByType("e2")
				e4 = p6.GetActorsByType("e4")
				e5 = p6.GetActorsByType("e5")
				e1 = p6.GetActorsByType("e1")
				e7 = p6.GetActorsByType("e7")
				shok = p6.GetActorsByType("shok")
				rae4 = p6.GetActorsByType("rae4")
				rae3 = p6.GetActorsByType("rae3")
				rae2 = p6.GetActorsByType("rae2")
				rae1 = p6.GetActorsByType("rae1")
				dog = p6.GetActorsByType("dog")
				scghost = p6.GetActorsByType("scghost")
				scfirebat = p6.GetActorsByType("scfirebat")
				scmarine = p6.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p7 and p9.IsAlliedWith(p7) then
				rmbo = p7.GetActorsByType("rmbo")
				e3 = p7.GetActorsByType("e3")
				e2 = p7.GetActorsByType("e2")
				e4 = p7.GetActorsByType("e4")
				e5 = p7.GetActorsByType("e5")
				e1 = p7.GetActorsByType("e1")
				e7 = p7.GetActorsByType("e7")
				shok = p7.GetActorsByType("shok")
				rae4 = p7.GetActorsByType("rae4")
				rae3 = p7.GetActorsByType("rae3")
				rae2 = p7.GetActorsByType("rae2")
				rae1 = p7.GetActorsByType("rae1")
				dog = p7.GetActorsByType("dog")
				scghost = p7.GetActorsByType("scghost")
				scfirebat = p7.GetActorsByType("scfirebat")
				scmarine = p7.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p8 and p9.IsAlliedWith(p8) then
				rmbo = p8.GetActorsByType("rmbo")
				e3 = p8.GetActorsByType("e3")
				e2 = p8.GetActorsByType("e2")
				e4 = p8.GetActorsByType("e4")
				e5 = p8.GetActorsByType("e5")
				e1 = p8.GetActorsByType("e1")
				e7 = p8.GetActorsByType("e7")
				shok = p8.GetActorsByType("shok")
				rae4 = p8.GetActorsByType("rae4")
				rae3 = p8.GetActorsByType("rae3")
				rae2 = p8.GetActorsByType("rae2")
				rae1 = p8.GetActorsByType("rae1")
				dog = p8.GetActorsByType("dog")
				scghost = p8.GetActorsByType("scghost")
				scfirebat = p8.GetActorsByType("scfirebat")
				scmarine = p8.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p9infantry()
	end)
end
p10infantry = function()
	if p10 and p10.IsBot then
		cityboteliteinstructor = p10.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p11 and p10.IsAlliedWith(p11) then
				rmbo = p11.GetActorsByType("rmbo")
				e3 = p11.GetActorsByType("e3")
				e2 = p11.GetActorsByType("e2")
				e4 = p11.GetActorsByType("e4")
				e5 = p11.GetActorsByType("e5")
				e1 = p11.GetActorsByType("e1")
				e7 = p11.GetActorsByType("e7")
				shok = p11.GetActorsByType("shok")
				rae4 = p11.GetActorsByType("rae4")
				rae3 = p11.GetActorsByType("rae3")
				rae2 = p11.GetActorsByType("rae2")
				rae1 = p11.GetActorsByType("rae1")
				dog = p11.GetActorsByType("dog")
				scghost = p11.GetActorsByType("scghost")
				scfirebat = p11.GetActorsByType("scfirebat")
				scmarine = p11.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p12 and p10.IsAlliedWith(p12) then
				rmbo = p12.GetActorsByType("rmbo")
				e3 = p12.GetActorsByType("e3")
				e2 = p12.GetActorsByType("e2")
				e4 = p12.GetActorsByType("e4")
				e5 = p12.GetActorsByType("e5")
				e1 = p12.GetActorsByType("e1")
				e7 = p12.GetActorsByType("e7")
				shok = p12.GetActorsByType("shok")
				rae4 = p12.GetActorsByType("rae4")
				rae3 = p12.GetActorsByType("rae3")
				rae2 = p12.GetActorsByType("rae2")
				rae1 = p12.GetActorsByType("rae1")
				dog = p12.GetActorsByType("dog")
				scghost = p12.GetActorsByType("scghost")
				scfirebat = p12.GetActorsByType("scfirebat")
				scmarine = p12.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p13 and p10.IsAlliedWith(p13) then
				rmbo = p13.GetActorsByType("rmbo")
				e3 = p13.GetActorsByType("e3")
				e2 = p13.GetActorsByType("e2")
				e4 = p13.GetActorsByType("e4")
				e5 = p13.GetActorsByType("e5")
				e1 = p13.GetActorsByType("e1")
				e7 = p13.GetActorsByType("e7")
				shok = p13.GetActorsByType("shok")
				rae4 = p13.GetActorsByType("rae4")
				rae3 = p13.GetActorsByType("rae3")
				rae2 = p13.GetActorsByType("rae2")
				rae1 = p13.GetActorsByType("rae1")
				dog = p13.GetActorsByType("dog")
				scghost = p13.GetActorsByType("scghost")
				scfirebat = p13.GetActorsByType("scfirebat")
				scmarine = p13.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p14 and p10.IsAlliedWith(p14) then
				rmbo = p14.GetActorsByType("rmbo")
				e3 = p14.GetActorsByType("e3")
				e2 = p14.GetActorsByType("e2")
				e4 = p14.GetActorsByType("e4")
				e5 = p14.GetActorsByType("e5")
				e1 = p14.GetActorsByType("e1")
				e7 = p14.GetActorsByType("e7")
				shok = p14.GetActorsByType("shok")
				rae4 = p14.GetActorsByType("rae4")
				rae3 = p14.GetActorsByType("rae3")
				rae2 = p14.GetActorsByType("rae2")
				rae1 = p14.GetActorsByType("rae1")
				dog = p14.GetActorsByType("dog")
				scghost = p14.GetActorsByType("scghost")
				scfirebat = p14.GetActorsByType("scfirebat")
				scmarine = p14.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p15 and p10.IsAlliedWith(p15) then
				rmbo = p15.GetActorsByType("rmbo")
				e3 = p15.GetActorsByType("e3")
				e2 = p15.GetActorsByType("e2")
				e4 = p15.GetActorsByType("e4")
				e5 = p15.GetActorsByType("e5")
				e1 = p15.GetActorsByType("e1")
				e7 = p15.GetActorsByType("e7")
				shok = p15.GetActorsByType("shok")
				rae4 = p15.GetActorsByType("rae4")
				rae3 = p15.GetActorsByType("rae3")
				rae2 = p15.GetActorsByType("rae2")
				rae1 = p15.GetActorsByType("rae1")
				dog = p15.GetActorsByType("dog")
				scghost = p15.GetActorsByType("scghost")
				scfirebat = p15.GetActorsByType("scfirebat")
				scmarine = p15.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p16 and p10.IsAlliedWith(p16) then
				rmbo = p16.GetActorsByType("rmbo")
				e3 = p16.GetActorsByType("e3")
				e2 = p16.GetActorsByType("e2")
				e4 = p16.GetActorsByType("e4")
				e5 = p16.GetActorsByType("e5")
				e1 = p16.GetActorsByType("e1")
				e7 = p16.GetActorsByType("e7")
				shok = p16.GetActorsByType("shok")
				rae4 = p16.GetActorsByType("rae4")
				rae3 = p16.GetActorsByType("rae3")
				rae2 = p16.GetActorsByType("rae2")
				rae1 = p16.GetActorsByType("rae1")
				dog = p16.GetActorsByType("dog")
				scghost = p16.GetActorsByType("scghost")
				scfirebat = p16.GetActorsByType("scfirebat")
				scmarine = p16.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p1 and p10.IsAlliedWith(p1) then
				rmbo = p1.GetActorsByType("rmbo")
				e3 = p1.GetActorsByType("e3")
				e2 = p1.GetActorsByType("e2")
				e4 = p1.GetActorsByType("e4")
				e5 = p1.GetActorsByType("e5")
				e1 = p1.GetActorsByType("e1")
				e7 = p1.GetActorsByType("e7")
				shok = p1.GetActorsByType("shok")
				rae4 = p1.GetActorsByType("rae4")
				rae3 = p1.GetActorsByType("rae3")
				rae2 = p1.GetActorsByType("rae2")
				rae1 = p1.GetActorsByType("rae1")
				dog = p1.GetActorsByType("dog")
				scghost = p1.GetActorsByType("scghost")
				scfirebat = p1.GetActorsByType("scfirebat")
				scmarine = p1.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p2 and p10.IsAlliedWith(p2) then
				rmbo = p2.GetActorsByType("rmbo")
				e3 = p2.GetActorsByType("e3")
				e2 = p2.GetActorsByType("e2")
				e4 = p2.GetActorsByType("e4")
				e5 = p2.GetActorsByType("e5")
				e1 = p2.GetActorsByType("e1")
				e7 = p2.GetActorsByType("e7")
				shok = p2.GetActorsByType("shok")
				rae4 = p2.GetActorsByType("rae4")
				rae3 = p2.GetActorsByType("rae3")
				rae2 = p2.GetActorsByType("rae2")
				rae1 = p2.GetActorsByType("rae1")
				dog = p2.GetActorsByType("dog")
				scghost = p2.GetActorsByType("scghost")
				scfirebat = p2.GetActorsByType("scfirebat")
				scmarine = p2.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p3 and p10.IsAlliedWith(p3) then
				rmbo = p3.GetActorsByType("rmbo")
				e3 = p3.GetActorsByType("e3")
				e2 = p3.GetActorsByType("e2")
				e4 = p3.GetActorsByType("e4")
				e5 = p3.GetActorsByType("e5")
				e1 = p3.GetActorsByType("e1")
				e7 = p3.GetActorsByType("e7")
				shok = p3.GetActorsByType("shok")
				rae4 = p3.GetActorsByType("rae4")
				rae3 = p3.GetActorsByType("rae3")
				rae2 = p3.GetActorsByType("rae2")
				rae1 = p3.GetActorsByType("rae1")
				dog = p3.GetActorsByType("dog")
				scghost = p3.GetActorsByType("scghost")
				scfirebat = p3.GetActorsByType("scfirebat")
				scmarine = p3.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p4 and p10.IsAlliedWith(p4) then
				rmbo = p4.GetActorsByType("rmbo")
				e3 = p4.GetActorsByType("e3")
				e2 = p4.GetActorsByType("e2")
				e4 = p4.GetActorsByType("e4")
				e5 = p4.GetActorsByType("e5")
				e1 = p4.GetActorsByType("e1")
				e7 = p4.GetActorsByType("e7")
				shok = p4.GetActorsByType("shok")
				rae4 = p4.GetActorsByType("rae4")
				rae3 = p4.GetActorsByType("rae3")
				rae2 = p4.GetActorsByType("rae2")
				rae1 = p4.GetActorsByType("rae1")
				dog = p4.GetActorsByType("dog")
				scghost = p4.GetActorsByType("scghost")
				scfirebat = p4.GetActorsByType("scfirebat")
				scmarine = p4.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p5 and p10.IsAlliedWith(p5) then
				rmbo = p5.GetActorsByType("rmbo")
				e3 = p5.GetActorsByType("e3")
				e2 = p5.GetActorsByType("e2")
				e4 = p5.GetActorsByType("e4")
				e5 = p5.GetActorsByType("e5")
				e1 = p5.GetActorsByType("e1")
				e7 = p5.GetActorsByType("e7")
				shok = p5.GetActorsByType("shok")
				rae4 = p5.GetActorsByType("rae4")
				rae3 = p5.GetActorsByType("rae3")
				rae2 = p5.GetActorsByType("rae2")
				rae1 = p5.GetActorsByType("rae1")
				dog = p5.GetActorsByType("dog")
				scghost = p5.GetActorsByType("scghost")
				scfirebat = p5.GetActorsByType("scfirebat")
				scmarine = p5.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p6 and p10.IsAlliedWith(p6) then
				rmbo = p6.GetActorsByType("rmbo")
				e3 = p6.GetActorsByType("e3")
				e2 = p6.GetActorsByType("e2")
				e4 = p6.GetActorsByType("e4")
				e5 = p6.GetActorsByType("e5")
				e1 = p6.GetActorsByType("e1")
				e7 = p6.GetActorsByType("e7")
				shok = p6.GetActorsByType("shok")
				rae4 = p6.GetActorsByType("rae4")
				rae3 = p6.GetActorsByType("rae3")
				rae2 = p6.GetActorsByType("rae2")
				rae1 = p6.GetActorsByType("rae1")
				dog = p6.GetActorsByType("dog")
				scghost = p6.GetActorsByType("scghost")
				scfirebat = p6.GetActorsByType("scfirebat")
				scmarine = p6.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p7 and p10.IsAlliedWith(p7) then
				rmbo = p7.GetActorsByType("rmbo")
				e3 = p7.GetActorsByType("e3")
				e2 = p7.GetActorsByType("e2")
				e4 = p7.GetActorsByType("e4")
				e5 = p7.GetActorsByType("e5")
				e1 = p7.GetActorsByType("e1")
				e7 = p7.GetActorsByType("e7")
				shok = p7.GetActorsByType("shok")
				rae4 = p7.GetActorsByType("rae4")
				rae3 = p7.GetActorsByType("rae3")
				rae2 = p7.GetActorsByType("rae2")
				rae1 = p7.GetActorsByType("rae1")
				dog = p7.GetActorsByType("dog")
				scghost = p7.GetActorsByType("scghost")
				scfirebat = p7.GetActorsByType("scfirebat")
				scmarine = p7.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p8 and p10.IsAlliedWith(p8) then
				rmbo = p8.GetActorsByType("rmbo")
				e3 = p8.GetActorsByType("e3")
				e2 = p8.GetActorsByType("e2")
				e4 = p8.GetActorsByType("e4")
				e5 = p8.GetActorsByType("e5")
				e1 = p8.GetActorsByType("e1")
				e7 = p8.GetActorsByType("e7")
				shok = p8.GetActorsByType("shok")
				rae4 = p8.GetActorsByType("rae4")
				rae3 = p8.GetActorsByType("rae3")
				rae2 = p8.GetActorsByType("rae2")
				rae1 = p8.GetActorsByType("rae1")
				dog = p8.GetActorsByType("dog")
				scghost = p8.GetActorsByType("scghost")
				scfirebat = p8.GetActorsByType("scfirebat")
				scmarine = p8.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p9 and p10.IsAlliedWith(p9) then
				rmbo = p9.GetActorsByType("rmbo")
				e3 = p9.GetActorsByType("e3")
				e2 = p9.GetActorsByType("e2")
				e4 = p9.GetActorsByType("e4")
				e5 = p9.GetActorsByType("e5")
				e1 = p9.GetActorsByType("e1")
				e7 = p9.GetActorsByType("e7")
				shok = p9.GetActorsByType("shok")
				rae4 = p9.GetActorsByType("rae4")
				rae3 = p9.GetActorsByType("rae3")
				rae2 = p9.GetActorsByType("rae2")
				rae1 = p9.GetActorsByType("rae1")
				dog = p9.GetActorsByType("dog")
				scghost = p9.GetActorsByType("scghost")
				scfirebat = p9.GetActorsByType("scfirebat")
				scmarine = p9.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p10infantry()
	end)
end
p11infantry = function()
	if p11 and p11.IsBot then
		cityboteliteinstructor = p11.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p12 and p11.IsAlliedWith(p12) then
				rmbo = p12.GetActorsByType("rmbo")
				e3 = p12.GetActorsByType("e3")
				e2 = p12.GetActorsByType("e2")
				e4 = p12.GetActorsByType("e4")
				e5 = p12.GetActorsByType("e5")
				e1 = p12.GetActorsByType("e1")
				e7 = p12.GetActorsByType("e7")
				shok = p12.GetActorsByType("shok")
				rae4 = p12.GetActorsByType("rae4")
				rae3 = p12.GetActorsByType("rae3")
				rae2 = p12.GetActorsByType("rae2")
				rae1 = p12.GetActorsByType("rae1")
				dog = p12.GetActorsByType("dog")
				scghost = p12.GetActorsByType("scghost")
				scfirebat = p12.GetActorsByType("scfirebat")
				scmarine = p12.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p13 and p11.IsAlliedWith(p13) then
				rmbo = p13.GetActorsByType("rmbo")
				e3 = p13.GetActorsByType("e3")
				e2 = p13.GetActorsByType("e2")
				e4 = p13.GetActorsByType("e4")
				e5 = p13.GetActorsByType("e5")
				e1 = p13.GetActorsByType("e1")
				e7 = p13.GetActorsByType("e7")
				shok = p13.GetActorsByType("shok")
				rae4 = p13.GetActorsByType("rae4")
				rae3 = p13.GetActorsByType("rae3")
				rae2 = p13.GetActorsByType("rae2")
				rae1 = p13.GetActorsByType("rae1")
				dog = p13.GetActorsByType("dog")
				scghost = p13.GetActorsByType("scghost")
				scfirebat = p13.GetActorsByType("scfirebat")
				scmarine = p13.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p14 and p11.IsAlliedWith(p14) then
				rmbo = p14.GetActorsByType("rmbo")
				e3 = p14.GetActorsByType("e3")
				e2 = p14.GetActorsByType("e2")
				e4 = p14.GetActorsByType("e4")
				e5 = p14.GetActorsByType("e5")
				e1 = p14.GetActorsByType("e1")
				e7 = p14.GetActorsByType("e7")
				shok = p14.GetActorsByType("shok")
				rae4 = p14.GetActorsByType("rae4")
				rae3 = p14.GetActorsByType("rae3")
				rae2 = p14.GetActorsByType("rae2")
				rae1 = p14.GetActorsByType("rae1")
				dog = p14.GetActorsByType("dog")
				scghost = p14.GetActorsByType("scghost")
				scfirebat = p14.GetActorsByType("scfirebat")
				scmarine = p14.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p15 and p11.IsAlliedWith(p15) then
				rmbo = p15.GetActorsByType("rmbo")
				e3 = p15.GetActorsByType("e3")
				e2 = p15.GetActorsByType("e2")
				e4 = p15.GetActorsByType("e4")
				e5 = p15.GetActorsByType("e5")
				e1 = p15.GetActorsByType("e1")
				e7 = p15.GetActorsByType("e7")
				shok = p15.GetActorsByType("shok")
				rae4 = p15.GetActorsByType("rae4")
				rae3 = p15.GetActorsByType("rae3")
				rae2 = p15.GetActorsByType("rae2")
				rae1 = p15.GetActorsByType("rae1")
				dog = p15.GetActorsByType("dog")
				scghost = p15.GetActorsByType("scghost")
				scfirebat = p15.GetActorsByType("scfirebat")
				scmarine = p15.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p16 and p11.IsAlliedWith(p16) then
				rmbo = p16.GetActorsByType("rmbo")
				e3 = p16.GetActorsByType("e3")
				e2 = p16.GetActorsByType("e2")
				e4 = p16.GetActorsByType("e4")
				e5 = p16.GetActorsByType("e5")
				e1 = p16.GetActorsByType("e1")
				e7 = p16.GetActorsByType("e7")
				shok = p16.GetActorsByType("shok")
				rae4 = p16.GetActorsByType("rae4")
				rae3 = p16.GetActorsByType("rae3")
				rae2 = p16.GetActorsByType("rae2")
				rae1 = p16.GetActorsByType("rae1")
				dog = p16.GetActorsByType("dog")
				scghost = p16.GetActorsByType("scghost")
				scfirebat = p16.GetActorsByType("scfirebat")
				scmarine = p16.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p1 and p11.IsAlliedWith(p1) then
				rmbo = p1.GetActorsByType("rmbo")
				e3 = p1.GetActorsByType("e3")
				e2 = p1.GetActorsByType("e2")
				e4 = p1.GetActorsByType("e4")
				e5 = p1.GetActorsByType("e5")
				e1 = p1.GetActorsByType("e1")
				e7 = p1.GetActorsByType("e7")
				shok = p1.GetActorsByType("shok")
				rae4 = p1.GetActorsByType("rae4")
				rae3 = p1.GetActorsByType("rae3")
				rae2 = p1.GetActorsByType("rae2")
				rae1 = p1.GetActorsByType("rae1")
				dog = p1.GetActorsByType("dog")
				scghost = p1.GetActorsByType("scghost")
				scfirebat = p1.GetActorsByType("scfirebat")
				scmarine = p1.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p2 and p11.IsAlliedWith(p2) then
				rmbo = p2.GetActorsByType("rmbo")
				e3 = p2.GetActorsByType("e3")
				e2 = p2.GetActorsByType("e2")
				e4 = p2.GetActorsByType("e4")
				e5 = p2.GetActorsByType("e5")
				e1 = p2.GetActorsByType("e1")
				e7 = p2.GetActorsByType("e7")
				shok = p2.GetActorsByType("shok")
				rae4 = p2.GetActorsByType("rae4")
				rae3 = p2.GetActorsByType("rae3")
				rae2 = p2.GetActorsByType("rae2")
				rae1 = p2.GetActorsByType("rae1")
				dog = p2.GetActorsByType("dog")
				scghost = p2.GetActorsByType("scghost")
				scfirebat = p2.GetActorsByType("scfirebat")
				scmarine = p2.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p3 and p11.IsAlliedWith(p3) then
				rmbo = p3.GetActorsByType("rmbo")
				e3 = p3.GetActorsByType("e3")
				e2 = p3.GetActorsByType("e2")
				e4 = p3.GetActorsByType("e4")
				e5 = p3.GetActorsByType("e5")
				e1 = p3.GetActorsByType("e1")
				e7 = p3.GetActorsByType("e7")
				shok = p3.GetActorsByType("shok")
				rae4 = p3.GetActorsByType("rae4")
				rae3 = p3.GetActorsByType("rae3")
				rae2 = p3.GetActorsByType("rae2")
				rae1 = p3.GetActorsByType("rae1")
				dog = p3.GetActorsByType("dog")
				scghost = p3.GetActorsByType("scghost")
				scfirebat = p3.GetActorsByType("scfirebat")
				scmarine = p3.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p4 and p11.IsAlliedWith(p4) then
				rmbo = p4.GetActorsByType("rmbo")
				e3 = p4.GetActorsByType("e3")
				e2 = p4.GetActorsByType("e2")
				e4 = p4.GetActorsByType("e4")
				e5 = p4.GetActorsByType("e5")
				e1 = p4.GetActorsByType("e1")
				e7 = p4.GetActorsByType("e7")
				shok = p4.GetActorsByType("shok")
				rae4 = p4.GetActorsByType("rae4")
				rae3 = p4.GetActorsByType("rae3")
				rae2 = p4.GetActorsByType("rae2")
				rae1 = p4.GetActorsByType("rae1")
				dog = p4.GetActorsByType("dog")
				scghost = p4.GetActorsByType("scghost")
				scfirebat = p4.GetActorsByType("scfirebat")
				scmarine = p4.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p5 and p11.IsAlliedWith(p5) then
				rmbo = p5.GetActorsByType("rmbo")
				e3 = p5.GetActorsByType("e3")
				e2 = p5.GetActorsByType("e2")
				e4 = p5.GetActorsByType("e4")
				e5 = p5.GetActorsByType("e5")
				e1 = p5.GetActorsByType("e1")
				e7 = p5.GetActorsByType("e7")
				shok = p5.GetActorsByType("shok")
				rae4 = p5.GetActorsByType("rae4")
				rae3 = p5.GetActorsByType("rae3")
				rae2 = p5.GetActorsByType("rae2")
				rae1 = p5.GetActorsByType("rae1")
				dog = p5.GetActorsByType("dog")
				scghost = p5.GetActorsByType("scghost")
				scfirebat = p5.GetActorsByType("scfirebat")
				scmarine = p5.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p6 and p11.IsAlliedWith(p6) then
				rmbo = p6.GetActorsByType("rmbo")
				e3 = p6.GetActorsByType("e3")
				e2 = p6.GetActorsByType("e2")
				e4 = p6.GetActorsByType("e4")
				e5 = p6.GetActorsByType("e5")
				e1 = p6.GetActorsByType("e1")
				e7 = p6.GetActorsByType("e7")
				shok = p6.GetActorsByType("shok")
				rae4 = p6.GetActorsByType("rae4")
				rae3 = p6.GetActorsByType("rae3")
				rae2 = p6.GetActorsByType("rae2")
				rae1 = p6.GetActorsByType("rae1")
				dog = p6.GetActorsByType("dog")
				scghost = p6.GetActorsByType("scghost")
				scfirebat = p6.GetActorsByType("scfirebat")
				scmarine = p6.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p7 and p11.IsAlliedWith(p7) then
				rmbo = p7.GetActorsByType("rmbo")
				e3 = p7.GetActorsByType("e3")
				e2 = p7.GetActorsByType("e2")
				e4 = p7.GetActorsByType("e4")
				e5 = p7.GetActorsByType("e5")
				e1 = p7.GetActorsByType("e1")
				e7 = p7.GetActorsByType("e7")
				shok = p7.GetActorsByType("shok")
				rae4 = p7.GetActorsByType("rae4")
				rae3 = p7.GetActorsByType("rae3")
				rae2 = p7.GetActorsByType("rae2")
				rae1 = p7.GetActorsByType("rae1")
				dog = p7.GetActorsByType("dog")
				scghost = p7.GetActorsByType("scghost")
				scfirebat = p7.GetActorsByType("scfirebat")
				scmarine = p7.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p8 and p11.IsAlliedWith(p8) then
				rmbo = p8.GetActorsByType("rmbo")
				e3 = p8.GetActorsByType("e3")
				e2 = p8.GetActorsByType("e2")
				e4 = p8.GetActorsByType("e4")
				e5 = p8.GetActorsByType("e5")
				e1 = p8.GetActorsByType("e1")
				e7 = p8.GetActorsByType("e7")
				shok = p8.GetActorsByType("shok")
				rae4 = p8.GetActorsByType("rae4")
				rae3 = p8.GetActorsByType("rae3")
				rae2 = p8.GetActorsByType("rae2")
				rae1 = p8.GetActorsByType("rae1")
				dog = p8.GetActorsByType("dog")
				scghost = p8.GetActorsByType("scghost")
				scfirebat = p8.GetActorsByType("scfirebat")
				scmarine = p8.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p9 and p11.IsAlliedWith(p9) then
				rmbo = p9.GetActorsByType("rmbo")
				e3 = p9.GetActorsByType("e3")
				e2 = p9.GetActorsByType("e2")
				e4 = p9.GetActorsByType("e4")
				e5 = p9.GetActorsByType("e5")
				e1 = p9.GetActorsByType("e1")
				e7 = p9.GetActorsByType("e7")
				shok = p9.GetActorsByType("shok")
				rae4 = p9.GetActorsByType("rae4")
				rae3 = p9.GetActorsByType("rae3")
				rae2 = p9.GetActorsByType("rae2")
				rae1 = p9.GetActorsByType("rae1")
				dog = p9.GetActorsByType("dog")
				scghost = p9.GetActorsByType("scghost")
				scfirebat = p9.GetActorsByType("scfirebat")
				scmarine = p9.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p10 and p11.IsAlliedWith(p10) then
				rmbo = p10.GetActorsByType("rmbo")
				e3 = p10.GetActorsByType("e3")
				e2 = p10.GetActorsByType("e2")
				e4 = p10.GetActorsByType("e4")
				e5 = p10.GetActorsByType("e5")
				e1 = p10.GetActorsByType("e1")
				e7 = p10.GetActorsByType("e7")
				shok = p10.GetActorsByType("shok")
				rae4 = p10.GetActorsByType("rae4")
				rae3 = p10.GetActorsByType("rae3")
				rae2 = p10.GetActorsByType("rae2")
				rae1 = p10.GetActorsByType("rae1")
				dog = p10.GetActorsByType("dog")
				scghost = p10.GetActorsByType("scghost")
				scfirebat = p10.GetActorsByType("scfirebat")
				scmarine = p10.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p11infantry()
	end)
end
p12infantry = function()
	if p12 and p12.IsBot then
		cityboteliteinstructor = p12.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p13 and p12.IsAlliedWith(p13) then
				rmbo = p13.GetActorsByType("rmbo")
				e3 = p13.GetActorsByType("e3")
				e2 = p13.GetActorsByType("e2")
				e4 = p13.GetActorsByType("e4")
				e5 = p13.GetActorsByType("e5")
				e1 = p13.GetActorsByType("e1")
				e7 = p13.GetActorsByType("e7")
				shok = p13.GetActorsByType("shok")
				rae4 = p13.GetActorsByType("rae4")
				rae3 = p13.GetActorsByType("rae3")
				rae2 = p13.GetActorsByType("rae2")
				rae1 = p13.GetActorsByType("rae1")
				dog = p13.GetActorsByType("dog")
				scghost = p13.GetActorsByType("scghost")
				scfirebat = p13.GetActorsByType("scfirebat")
				scmarine = p13.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p14 and p12.IsAlliedWith(p14) then
				rmbo = p14.GetActorsByType("rmbo")
				e3 = p14.GetActorsByType("e3")
				e2 = p14.GetActorsByType("e2")
				e4 = p14.GetActorsByType("e4")
				e5 = p14.GetActorsByType("e5")
				e1 = p14.GetActorsByType("e1")
				e7 = p14.GetActorsByType("e7")
				shok = p14.GetActorsByType("shok")
				rae4 = p14.GetActorsByType("rae4")
				rae3 = p14.GetActorsByType("rae3")
				rae2 = p14.GetActorsByType("rae2")
				rae1 = p14.GetActorsByType("rae1")
				dog = p14.GetActorsByType("dog")
				scghost = p14.GetActorsByType("scghost")
				scfirebat = p14.GetActorsByType("scfirebat")
				scmarine = p14.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p15 and p12.IsAlliedWith(p15) then
				rmbo = p15.GetActorsByType("rmbo")
				e3 = p15.GetActorsByType("e3")
				e2 = p15.GetActorsByType("e2")
				e4 = p15.GetActorsByType("e4")
				e5 = p15.GetActorsByType("e5")
				e1 = p15.GetActorsByType("e1")
				e7 = p15.GetActorsByType("e7")
				shok = p15.GetActorsByType("shok")
				rae4 = p15.GetActorsByType("rae4")
				rae3 = p15.GetActorsByType("rae3")
				rae2 = p15.GetActorsByType("rae2")
				rae1 = p15.GetActorsByType("rae1")
				dog = p15.GetActorsByType("dog")
				scghost = p15.GetActorsByType("scghost")
				scfirebat = p15.GetActorsByType("scfirebat")
				scmarine = p15.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p16 and p12.IsAlliedWith(p16) then
				rmbo = p16.GetActorsByType("rmbo")
				e3 = p16.GetActorsByType("e3")
				e2 = p16.GetActorsByType("e2")
				e4 = p16.GetActorsByType("e4")
				e5 = p16.GetActorsByType("e5")
				e1 = p16.GetActorsByType("e1")
				e7 = p16.GetActorsByType("e7")
				shok = p16.GetActorsByType("shok")
				rae4 = p16.GetActorsByType("rae4")
				rae3 = p16.GetActorsByType("rae3")
				rae2 = p16.GetActorsByType("rae2")
				rae1 = p16.GetActorsByType("rae1")
				dog = p16.GetActorsByType("dog")
				scghost = p16.GetActorsByType("scghost")
				scfirebat = p16.GetActorsByType("scfirebat")
				scmarine = p16.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p1 and p12.IsAlliedWith(p1) then
				rmbo = p1.GetActorsByType("rmbo")
				e3 = p1.GetActorsByType("e3")
				e2 = p1.GetActorsByType("e2")
				e4 = p1.GetActorsByType("e4")
				e5 = p1.GetActorsByType("e5")
				e1 = p1.GetActorsByType("e1")
				e7 = p1.GetActorsByType("e7")
				shok = p1.GetActorsByType("shok")
				rae4 = p1.GetActorsByType("rae4")
				rae3 = p1.GetActorsByType("rae3")
				rae2 = p1.GetActorsByType("rae2")
				rae1 = p1.GetActorsByType("rae1")
				dog = p1.GetActorsByType("dog")
				scghost = p1.GetActorsByType("scghost")
				scfirebat = p1.GetActorsByType("scfirebat")
				scmarine = p1.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p2 and p12.IsAlliedWith(p2) then
				rmbo = p2.GetActorsByType("rmbo")
				e3 = p2.GetActorsByType("e3")
				e2 = p2.GetActorsByType("e2")
				e4 = p2.GetActorsByType("e4")
				e5 = p2.GetActorsByType("e5")
				e1 = p2.GetActorsByType("e1")
				e7 = p2.GetActorsByType("e7")
				shok = p2.GetActorsByType("shok")
				rae4 = p2.GetActorsByType("rae4")
				rae3 = p2.GetActorsByType("rae3")
				rae2 = p2.GetActorsByType("rae2")
				rae1 = p2.GetActorsByType("rae1")
				dog = p2.GetActorsByType("dog")
				scghost = p2.GetActorsByType("scghost")
				scfirebat = p2.GetActorsByType("scfirebat")
				scmarine = p2.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p3 and p12.IsAlliedWith(p3) then
				rmbo = p3.GetActorsByType("rmbo")
				e3 = p3.GetActorsByType("e3")
				e2 = p3.GetActorsByType("e2")
				e4 = p3.GetActorsByType("e4")
				e5 = p3.GetActorsByType("e5")
				e1 = p3.GetActorsByType("e1")
				e7 = p3.GetActorsByType("e7")
				shok = p3.GetActorsByType("shok")
				rae4 = p3.GetActorsByType("rae4")
				rae3 = p3.GetActorsByType("rae3")
				rae2 = p3.GetActorsByType("rae2")
				rae1 = p3.GetActorsByType("rae1")
				dog = p3.GetActorsByType("dog")
				scghost = p3.GetActorsByType("scghost")
				scfirebat = p3.GetActorsByType("scfirebat")
				scmarine = p3.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p4 and p12.IsAlliedWith(p4) then
				rmbo = p4.GetActorsByType("rmbo")
				e3 = p4.GetActorsByType("e3")
				e2 = p4.GetActorsByType("e2")
				e4 = p4.GetActorsByType("e4")
				e5 = p4.GetActorsByType("e5")
				e1 = p4.GetActorsByType("e1")
				e7 = p4.GetActorsByType("e7")
				shok = p4.GetActorsByType("shok")
				rae4 = p4.GetActorsByType("rae4")
				rae3 = p4.GetActorsByType("rae3")
				rae2 = p4.GetActorsByType("rae2")
				rae1 = p4.GetActorsByType("rae1")
				dog = p4.GetActorsByType("dog")
				scghost = p4.GetActorsByType("scghost")
				scfirebat = p4.GetActorsByType("scfirebat")
				scmarine = p4.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p5 and p12.IsAlliedWith(p5) then
				rmbo = p5.GetActorsByType("rmbo")
				e3 = p5.GetActorsByType("e3")
				e2 = p5.GetActorsByType("e2")
				e4 = p5.GetActorsByType("e4")
				e5 = p5.GetActorsByType("e5")
				e1 = p5.GetActorsByType("e1")
				e7 = p5.GetActorsByType("e7")
				shok = p5.GetActorsByType("shok")
				rae4 = p5.GetActorsByType("rae4")
				rae3 = p5.GetActorsByType("rae3")
				rae2 = p5.GetActorsByType("rae2")
				rae1 = p5.GetActorsByType("rae1")
				dog = p5.GetActorsByType("dog")
				scghost = p5.GetActorsByType("scghost")
				scfirebat = p5.GetActorsByType("scfirebat")
				scmarine = p5.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p6 and p12.IsAlliedWith(p6) then
				rmbo = p6.GetActorsByType("rmbo")
				e3 = p6.GetActorsByType("e3")
				e2 = p6.GetActorsByType("e2")
				e4 = p6.GetActorsByType("e4")
				e5 = p6.GetActorsByType("e5")
				e1 = p6.GetActorsByType("e1")
				e7 = p6.GetActorsByType("e7")
				shok = p6.GetActorsByType("shok")
				rae4 = p6.GetActorsByType("rae4")
				rae3 = p6.GetActorsByType("rae3")
				rae2 = p6.GetActorsByType("rae2")
				rae1 = p6.GetActorsByType("rae1")
				dog = p6.GetActorsByType("dog")
				scghost = p6.GetActorsByType("scghost")
				scfirebat = p6.GetActorsByType("scfirebat")
				scmarine = p6.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p7 and p12.IsAlliedWith(p7) then
				rmbo = p7.GetActorsByType("rmbo")
				e3 = p7.GetActorsByType("e3")
				e2 = p7.GetActorsByType("e2")
				e4 = p7.GetActorsByType("e4")
				e5 = p7.GetActorsByType("e5")
				e1 = p7.GetActorsByType("e1")
				e7 = p7.GetActorsByType("e7")
				shok = p7.GetActorsByType("shok")
				rae4 = p7.GetActorsByType("rae4")
				rae3 = p7.GetActorsByType("rae3")
				rae2 = p7.GetActorsByType("rae2")
				rae1 = p7.GetActorsByType("rae1")
				dog = p7.GetActorsByType("dog")
				scghost = p7.GetActorsByType("scghost")
				scfirebat = p7.GetActorsByType("scfirebat")
				scmarine = p7.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p8 and p12.IsAlliedWith(p8) then
				rmbo = p8.GetActorsByType("rmbo")
				e3 = p8.GetActorsByType("e3")
				e2 = p8.GetActorsByType("e2")
				e4 = p8.GetActorsByType("e4")
				e5 = p8.GetActorsByType("e5")
				e1 = p8.GetActorsByType("e1")
				e7 = p8.GetActorsByType("e7")
				shok = p8.GetActorsByType("shok")
				rae4 = p8.GetActorsByType("rae4")
				rae3 = p8.GetActorsByType("rae3")
				rae2 = p8.GetActorsByType("rae2")
				rae1 = p8.GetActorsByType("rae1")
				dog = p8.GetActorsByType("dog")
				scghost = p8.GetActorsByType("scghost")
				scfirebat = p8.GetActorsByType("scfirebat")
				scmarine = p8.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p9 and p12.IsAlliedWith(p9) then
				rmbo = p9.GetActorsByType("rmbo")
				e3 = p9.GetActorsByType("e3")
				e2 = p9.GetActorsByType("e2")
				e4 = p9.GetActorsByType("e4")
				e5 = p9.GetActorsByType("e5")
				e1 = p9.GetActorsByType("e1")
				e7 = p9.GetActorsByType("e7")
				shok = p9.GetActorsByType("shok")
				rae4 = p9.GetActorsByType("rae4")
				rae3 = p9.GetActorsByType("rae3")
				rae2 = p9.GetActorsByType("rae2")
				rae1 = p9.GetActorsByType("rae1")
				dog = p9.GetActorsByType("dog")
				scghost = p9.GetActorsByType("scghost")
				scfirebat = p9.GetActorsByType("scfirebat")
				scmarine = p9.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p10 and p12.IsAlliedWith(p10) then
				rmbo = p10.GetActorsByType("rmbo")
				e3 = p10.GetActorsByType("e3")
				e2 = p10.GetActorsByType("e2")
				e4 = p10.GetActorsByType("e4")
				e5 = p10.GetActorsByType("e5")
				e1 = p10.GetActorsByType("e1")
				e7 = p10.GetActorsByType("e7")
				shok = p10.GetActorsByType("shok")
				rae4 = p10.GetActorsByType("rae4")
				rae3 = p10.GetActorsByType("rae3")
				rae2 = p10.GetActorsByType("rae2")
				rae1 = p10.GetActorsByType("rae1")
				dog = p10.GetActorsByType("dog")
				scghost = p10.GetActorsByType("scghost")
				scfirebat = p10.GetActorsByType("scfirebat")
				scmarine = p10.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p11 and p12.IsAlliedWith(p11) then
				rmbo = p11.GetActorsByType("rmbo")
				e3 = p11.GetActorsByType("e3")
				e2 = p11.GetActorsByType("e2")
				e4 = p11.GetActorsByType("e4")
				e5 = p11.GetActorsByType("e5")
				e1 = p11.GetActorsByType("e1")
				e7 = p11.GetActorsByType("e7")
				shok = p11.GetActorsByType("shok")
				rae4 = p11.GetActorsByType("rae4")
				rae3 = p11.GetActorsByType("rae3")
				rae2 = p11.GetActorsByType("rae2")
				rae1 = p11.GetActorsByType("rae1")
				dog = p11.GetActorsByType("dog")
				scghost = p11.GetActorsByType("scghost")
				scfirebat = p11.GetActorsByType("scfirebat")
				scmarine = p11.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p12infantry()
	end)
end
p13infantry = function()
	if p13 and p13.IsBot then
		cityboteliteinstructor = p13.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p14 and p13.IsAlliedWith(p14) then
				rmbo = p14.GetActorsByType("rmbo")
				e3 = p14.GetActorsByType("e3")
				e2 = p14.GetActorsByType("e2")
				e4 = p14.GetActorsByType("e4")
				e5 = p14.GetActorsByType("e5")
				e1 = p14.GetActorsByType("e1")
				e7 = p14.GetActorsByType("e7")
				shok = p14.GetActorsByType("shok")
				rae4 = p14.GetActorsByType("rae4")
				rae3 = p14.GetActorsByType("rae3")
				rae2 = p14.GetActorsByType("rae2")
				rae1 = p14.GetActorsByType("rae1")
				dog = p14.GetActorsByType("dog")
				scghost = p14.GetActorsByType("scghost")
				scfirebat = p14.GetActorsByType("scfirebat")
				scmarine = p14.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p15 and p13.IsAlliedWith(p15) then
				rmbo = p15.GetActorsByType("rmbo")
				e3 = p15.GetActorsByType("e3")
				e2 = p15.GetActorsByType("e2")
				e4 = p15.GetActorsByType("e4")
				e5 = p15.GetActorsByType("e5")
				e1 = p15.GetActorsByType("e1")
				e7 = p15.GetActorsByType("e7")
				shok = p15.GetActorsByType("shok")
				rae4 = p15.GetActorsByType("rae4")
				rae3 = p15.GetActorsByType("rae3")
				rae2 = p15.GetActorsByType("rae2")
				rae1 = p15.GetActorsByType("rae1")
				dog = p15.GetActorsByType("dog")
				scghost = p15.GetActorsByType("scghost")
				scfirebat = p15.GetActorsByType("scfirebat")
				scmarine = p15.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p16 and p13.IsAlliedWith(p16) then
				rmbo = p16.GetActorsByType("rmbo")
				e3 = p16.GetActorsByType("e3")
				e2 = p16.GetActorsByType("e2")
				e4 = p16.GetActorsByType("e4")
				e5 = p16.GetActorsByType("e5")
				e1 = p16.GetActorsByType("e1")
				e7 = p16.GetActorsByType("e7")
				shok = p16.GetActorsByType("shok")
				rae4 = p16.GetActorsByType("rae4")
				rae3 = p16.GetActorsByType("rae3")
				rae2 = p16.GetActorsByType("rae2")
				rae1 = p16.GetActorsByType("rae1")
				dog = p16.GetActorsByType("dog")
				scghost = p16.GetActorsByType("scghost")
				scfirebat = p16.GetActorsByType("scfirebat")
				scmarine = p16.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p1 and p13.IsAlliedWith(p1) then
				rmbo = p1.GetActorsByType("rmbo")
				e3 = p1.GetActorsByType("e3")
				e2 = p1.GetActorsByType("e2")
				e4 = p1.GetActorsByType("e4")
				e5 = p1.GetActorsByType("e5")
				e1 = p1.GetActorsByType("e1")
				e7 = p1.GetActorsByType("e7")
				shok = p1.GetActorsByType("shok")
				rae4 = p1.GetActorsByType("rae4")
				rae3 = p1.GetActorsByType("rae3")
				rae2 = p1.GetActorsByType("rae2")
				rae1 = p1.GetActorsByType("rae1")
				dog = p1.GetActorsByType("dog")
				scghost = p1.GetActorsByType("scghost")
				scfirebat = p1.GetActorsByType("scfirebat")
				scmarine = p1.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p2 and p13.IsAlliedWith(p2) then
				rmbo = p2.GetActorsByType("rmbo")
				e3 = p2.GetActorsByType("e3")
				e2 = p2.GetActorsByType("e2")
				e4 = p2.GetActorsByType("e4")
				e5 = p2.GetActorsByType("e5")
				e1 = p2.GetActorsByType("e1")
				e7 = p2.GetActorsByType("e7")
				shok = p2.GetActorsByType("shok")
				rae4 = p2.GetActorsByType("rae4")
				rae3 = p2.GetActorsByType("rae3")
				rae2 = p2.GetActorsByType("rae2")
				rae1 = p2.GetActorsByType("rae1")
				dog = p2.GetActorsByType("dog")
				scghost = p2.GetActorsByType("scghost")
				scfirebat = p2.GetActorsByType("scfirebat")
				scmarine = p2.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p3 and p13.IsAlliedWith(p3) then
				rmbo = p3.GetActorsByType("rmbo")
				e3 = p3.GetActorsByType("e3")
				e2 = p3.GetActorsByType("e2")
				e4 = p3.GetActorsByType("e4")
				e5 = p3.GetActorsByType("e5")
				e1 = p3.GetActorsByType("e1")
				e7 = p3.GetActorsByType("e7")
				shok = p3.GetActorsByType("shok")
				rae4 = p3.GetActorsByType("rae4")
				rae3 = p3.GetActorsByType("rae3")
				rae2 = p3.GetActorsByType("rae2")
				rae1 = p3.GetActorsByType("rae1")
				dog = p3.GetActorsByType("dog")
				scghost = p3.GetActorsByType("scghost")
				scfirebat = p3.GetActorsByType("scfirebat")
				scmarine = p3.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p4 and p13.IsAlliedWith(p4) then
				rmbo = p4.GetActorsByType("rmbo")
				e3 = p4.GetActorsByType("e3")
				e2 = p4.GetActorsByType("e2")
				e4 = p4.GetActorsByType("e4")
				e5 = p4.GetActorsByType("e5")
				e1 = p4.GetActorsByType("e1")
				e7 = p4.GetActorsByType("e7")
				shok = p4.GetActorsByType("shok")
				rae4 = p4.GetActorsByType("rae4")
				rae3 = p4.GetActorsByType("rae3")
				rae2 = p4.GetActorsByType("rae2")
				rae1 = p4.GetActorsByType("rae1")
				dog = p4.GetActorsByType("dog")
				scghost = p4.GetActorsByType("scghost")
				scfirebat = p4.GetActorsByType("scfirebat")
				scmarine = p4.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p5 and p13.IsAlliedWith(p5) then
				rmbo = p5.GetActorsByType("rmbo")
				e3 = p5.GetActorsByType("e3")
				e2 = p5.GetActorsByType("e2")
				e4 = p5.GetActorsByType("e4")
				e5 = p5.GetActorsByType("e5")
				e1 = p5.GetActorsByType("e1")
				e7 = p5.GetActorsByType("e7")
				shok = p5.GetActorsByType("shok")
				rae4 = p5.GetActorsByType("rae4")
				rae3 = p5.GetActorsByType("rae3")
				rae2 = p5.GetActorsByType("rae2")
				rae1 = p5.GetActorsByType("rae1")
				dog = p5.GetActorsByType("dog")
				scghost = p5.GetActorsByType("scghost")
				scfirebat = p5.GetActorsByType("scfirebat")
				scmarine = p5.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p6 and p13.IsAlliedWith(p6) then
				rmbo = p6.GetActorsByType("rmbo")
				e3 = p6.GetActorsByType("e3")
				e2 = p6.GetActorsByType("e2")
				e4 = p6.GetActorsByType("e4")
				e5 = p6.GetActorsByType("e5")
				e1 = p6.GetActorsByType("e1")
				e7 = p6.GetActorsByType("e7")
				shok = p6.GetActorsByType("shok")
				rae4 = p6.GetActorsByType("rae4")
				rae3 = p6.GetActorsByType("rae3")
				rae2 = p6.GetActorsByType("rae2")
				rae1 = p6.GetActorsByType("rae1")
				dog = p6.GetActorsByType("dog")
				scghost = p6.GetActorsByType("scghost")
				scfirebat = p6.GetActorsByType("scfirebat")
				scmarine = p6.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p7 and p13.IsAlliedWith(p7) then
				rmbo = p7.GetActorsByType("rmbo")
				e3 = p7.GetActorsByType("e3")
				e2 = p7.GetActorsByType("e2")
				e4 = p7.GetActorsByType("e4")
				e5 = p7.GetActorsByType("e5")
				e1 = p7.GetActorsByType("e1")
				e7 = p7.GetActorsByType("e7")
				shok = p7.GetActorsByType("shok")
				rae4 = p7.GetActorsByType("rae4")
				rae3 = p7.GetActorsByType("rae3")
				rae2 = p7.GetActorsByType("rae2")
				rae1 = p7.GetActorsByType("rae1")
				dog = p7.GetActorsByType("dog")
				scghost = p7.GetActorsByType("scghost")
				scfirebat = p7.GetActorsByType("scfirebat")
				scmarine = p7.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p8 and p13.IsAlliedWith(p8) then
				rmbo = p8.GetActorsByType("rmbo")
				e3 = p8.GetActorsByType("e3")
				e2 = p8.GetActorsByType("e2")
				e4 = p8.GetActorsByType("e4")
				e5 = p8.GetActorsByType("e5")
				e1 = p8.GetActorsByType("e1")
				e7 = p8.GetActorsByType("e7")
				shok = p8.GetActorsByType("shok")
				rae4 = p8.GetActorsByType("rae4")
				rae3 = p8.GetActorsByType("rae3")
				rae2 = p8.GetActorsByType("rae2")
				rae1 = p8.GetActorsByType("rae1")
				dog = p8.GetActorsByType("dog")
				scghost = p8.GetActorsByType("scghost")
				scfirebat = p8.GetActorsByType("scfirebat")
				scmarine = p8.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p9 and p13.IsAlliedWith(p9) then
				rmbo = p9.GetActorsByType("rmbo")
				e3 = p9.GetActorsByType("e3")
				e2 = p9.GetActorsByType("e2")
				e4 = p9.GetActorsByType("e4")
				e5 = p9.GetActorsByType("e5")
				e1 = p9.GetActorsByType("e1")
				e7 = p9.GetActorsByType("e7")
				shok = p9.GetActorsByType("shok")
				rae4 = p9.GetActorsByType("rae4")
				rae3 = p9.GetActorsByType("rae3")
				rae2 = p9.GetActorsByType("rae2")
				rae1 = p9.GetActorsByType("rae1")
				dog = p9.GetActorsByType("dog")
				scghost = p9.GetActorsByType("scghost")
				scfirebat = p9.GetActorsByType("scfirebat")
				scmarine = p9.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p10 and p13.IsAlliedWith(p10) then
				rmbo = p10.GetActorsByType("rmbo")
				e3 = p10.GetActorsByType("e3")
				e2 = p10.GetActorsByType("e2")
				e4 = p10.GetActorsByType("e4")
				e5 = p10.GetActorsByType("e5")
				e1 = p10.GetActorsByType("e1")
				e7 = p10.GetActorsByType("e7")
				shok = p10.GetActorsByType("shok")
				rae4 = p10.GetActorsByType("rae4")
				rae3 = p10.GetActorsByType("rae3")
				rae2 = p10.GetActorsByType("rae2")
				rae1 = p10.GetActorsByType("rae1")
				dog = p10.GetActorsByType("dog")
				scghost = p10.GetActorsByType("scghost")
				scfirebat = p10.GetActorsByType("scfirebat")
				scmarine = p10.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p11 and p13.IsAlliedWith(p11) then
				rmbo = p11.GetActorsByType("rmbo")
				e3 = p11.GetActorsByType("e3")
				e2 = p11.GetActorsByType("e2")
				e4 = p11.GetActorsByType("e4")
				e5 = p11.GetActorsByType("e5")
				e1 = p11.GetActorsByType("e1")
				e7 = p11.GetActorsByType("e7")
				shok = p11.GetActorsByType("shok")
				rae4 = p11.GetActorsByType("rae4")
				rae3 = p11.GetActorsByType("rae3")
				rae2 = p11.GetActorsByType("rae2")
				rae1 = p11.GetActorsByType("rae1")
				dog = p11.GetActorsByType("dog")
				scghost = p11.GetActorsByType("scghost")
				scfirebat = p11.GetActorsByType("scfirebat")
				scmarine = p11.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p12 and p13.IsAlliedWith(p12) then
				rmbo = p12.GetActorsByType("rmbo")
				e3 = p12.GetActorsByType("e3")
				e2 = p12.GetActorsByType("e2")
				e4 = p12.GetActorsByType("e4")
				e5 = p12.GetActorsByType("e5")
				e1 = p12.GetActorsByType("e1")
				e7 = p12.GetActorsByType("e7")
				shok = p12.GetActorsByType("shok")
				rae4 = p12.GetActorsByType("rae4")
				rae3 = p12.GetActorsByType("rae3")
				rae2 = p12.GetActorsByType("rae2")
				rae1 = p12.GetActorsByType("rae1")
				dog = p12.GetActorsByType("dog")
				scghost = p12.GetActorsByType("scghost")
				scfirebat = p12.GetActorsByType("scfirebat")
				scmarine = p12.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p13infantry()
	end)
end
p14infantry = function()
	if p14 and p14.IsBot then
		cityboteliteinstructor = p14.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p15 and p14.IsAlliedWith(p15) then
				rmbo = p15.GetActorsByType("rmbo")
				e3 = p15.GetActorsByType("e3")
				e2 = p15.GetActorsByType("e2")
				e4 = p15.GetActorsByType("e4")
				e5 = p15.GetActorsByType("e5")
				e1 = p15.GetActorsByType("e1")
				e7 = p15.GetActorsByType("e7")
				shok = p15.GetActorsByType("shok")
				rae4 = p15.GetActorsByType("rae4")
				rae3 = p15.GetActorsByType("rae3")
				rae2 = p15.GetActorsByType("rae2")
				rae1 = p15.GetActorsByType("rae1")
				dog = p15.GetActorsByType("dog")
				scghost = p15.GetActorsByType("scghost")
				scfirebat = p15.GetActorsByType("scfirebat")
				scmarine = p15.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p16 and p14.IsAlliedWith(p16) then
				rmbo = p16.GetActorsByType("rmbo")
				e3 = p16.GetActorsByType("e3")
				e2 = p16.GetActorsByType("e2")
				e4 = p16.GetActorsByType("e4")
				e5 = p16.GetActorsByType("e5")
				e1 = p16.GetActorsByType("e1")
				e7 = p16.GetActorsByType("e7")
				shok = p16.GetActorsByType("shok")
				rae4 = p16.GetActorsByType("rae4")
				rae3 = p16.GetActorsByType("rae3")
				rae2 = p16.GetActorsByType("rae2")
				rae1 = p16.GetActorsByType("rae1")
				dog = p16.GetActorsByType("dog")
				scghost = p16.GetActorsByType("scghost")
				scfirebat = p16.GetActorsByType("scfirebat")
				scmarine = p16.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p1 and p14.IsAlliedWith(p1) then
				rmbo = p1.GetActorsByType("rmbo")
				e3 = p1.GetActorsByType("e3")
				e2 = p1.GetActorsByType("e2")
				e4 = p1.GetActorsByType("e4")
				e5 = p1.GetActorsByType("e5")
				e1 = p1.GetActorsByType("e1")
				e7 = p1.GetActorsByType("e7")
				shok = p1.GetActorsByType("shok")
				rae4 = p1.GetActorsByType("rae4")
				rae3 = p1.GetActorsByType("rae3")
				rae2 = p1.GetActorsByType("rae2")
				rae1 = p1.GetActorsByType("rae1")
				dog = p1.GetActorsByType("dog")
				scghost = p1.GetActorsByType("scghost")
				scfirebat = p1.GetActorsByType("scfirebat")
				scmarine = p1.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p2 and p14.IsAlliedWith(p2) then
				rmbo = p2.GetActorsByType("rmbo")
				e3 = p2.GetActorsByType("e3")
				e2 = p2.GetActorsByType("e2")
				e4 = p2.GetActorsByType("e4")
				e5 = p2.GetActorsByType("e5")
				e1 = p2.GetActorsByType("e1")
				e7 = p2.GetActorsByType("e7")
				shok = p2.GetActorsByType("shok")
				rae4 = p2.GetActorsByType("rae4")
				rae3 = p2.GetActorsByType("rae3")
				rae2 = p2.GetActorsByType("rae2")
				rae1 = p2.GetActorsByType("rae1")
				dog = p2.GetActorsByType("dog")
				scghost = p2.GetActorsByType("scghost")
				scfirebat = p2.GetActorsByType("scfirebat")
				scmarine = p2.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p3 and p14.IsAlliedWith(p3) then
				rmbo = p3.GetActorsByType("rmbo")
				e3 = p3.GetActorsByType("e3")
				e2 = p3.GetActorsByType("e2")
				e4 = p3.GetActorsByType("e4")
				e5 = p3.GetActorsByType("e5")
				e1 = p3.GetActorsByType("e1")
				e7 = p3.GetActorsByType("e7")
				shok = p3.GetActorsByType("shok")
				rae4 = p3.GetActorsByType("rae4")
				rae3 = p3.GetActorsByType("rae3")
				rae2 = p3.GetActorsByType("rae2")
				rae1 = p3.GetActorsByType("rae1")
				dog = p3.GetActorsByType("dog")
				scghost = p3.GetActorsByType("scghost")
				scfirebat = p3.GetActorsByType("scfirebat")
				scmarine = p3.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p4 and p14.IsAlliedWith(p4) then
				rmbo = p4.GetActorsByType("rmbo")
				e3 = p4.GetActorsByType("e3")
				e2 = p4.GetActorsByType("e2")
				e4 = p4.GetActorsByType("e4")
				e5 = p4.GetActorsByType("e5")
				e1 = p4.GetActorsByType("e1")
				e7 = p4.GetActorsByType("e7")
				shok = p4.GetActorsByType("shok")
				rae4 = p4.GetActorsByType("rae4")
				rae3 = p4.GetActorsByType("rae3")
				rae2 = p4.GetActorsByType("rae2")
				rae1 = p4.GetActorsByType("rae1")
				dog = p4.GetActorsByType("dog")
				scghost = p4.GetActorsByType("scghost")
				scfirebat = p4.GetActorsByType("scfirebat")
				scmarine = p4.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p5 and p14.IsAlliedWith(p5) then
				rmbo = p5.GetActorsByType("rmbo")
				e3 = p5.GetActorsByType("e3")
				e2 = p5.GetActorsByType("e2")
				e4 = p5.GetActorsByType("e4")
				e5 = p5.GetActorsByType("e5")
				e1 = p5.GetActorsByType("e1")
				e7 = p5.GetActorsByType("e7")
				shok = p5.GetActorsByType("shok")
				rae4 = p5.GetActorsByType("rae4")
				rae3 = p5.GetActorsByType("rae3")
				rae2 = p5.GetActorsByType("rae2")
				rae1 = p5.GetActorsByType("rae1")
				dog = p5.GetActorsByType("dog")
				scghost = p5.GetActorsByType("scghost")
				scfirebat = p5.GetActorsByType("scfirebat")
				scmarine = p5.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p6 and p14.IsAlliedWith(p6) then
				rmbo = p6.GetActorsByType("rmbo")
				e3 = p6.GetActorsByType("e3")
				e2 = p6.GetActorsByType("e2")
				e4 = p6.GetActorsByType("e4")
				e5 = p6.GetActorsByType("e5")
				e1 = p6.GetActorsByType("e1")
				e7 = p6.GetActorsByType("e7")
				shok = p6.GetActorsByType("shok")
				rae4 = p6.GetActorsByType("rae4")
				rae3 = p6.GetActorsByType("rae3")
				rae2 = p6.GetActorsByType("rae2")
				rae1 = p6.GetActorsByType("rae1")
				dog = p6.GetActorsByType("dog")
				scghost = p6.GetActorsByType("scghost")
				scfirebat = p6.GetActorsByType("scfirebat")
				scmarine = p6.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p7 and p14.IsAlliedWith(p7) then
				rmbo = p7.GetActorsByType("rmbo")
				e3 = p7.GetActorsByType("e3")
				e2 = p7.GetActorsByType("e2")
				e4 = p7.GetActorsByType("e4")
				e5 = p7.GetActorsByType("e5")
				e1 = p7.GetActorsByType("e1")
				e7 = p7.GetActorsByType("e7")
				shok = p7.GetActorsByType("shok")
				rae4 = p7.GetActorsByType("rae4")
				rae3 = p7.GetActorsByType("rae3")
				rae2 = p7.GetActorsByType("rae2")
				rae1 = p7.GetActorsByType("rae1")
				dog = p7.GetActorsByType("dog")
				scghost = p7.GetActorsByType("scghost")
				scfirebat = p7.GetActorsByType("scfirebat")
				scmarine = p7.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p8 and p14.IsAlliedWith(p8) then
				rmbo = p8.GetActorsByType("rmbo")
				e3 = p8.GetActorsByType("e3")
				e2 = p8.GetActorsByType("e2")
				e4 = p8.GetActorsByType("e4")
				e5 = p8.GetActorsByType("e5")
				e1 = p8.GetActorsByType("e1")
				e7 = p8.GetActorsByType("e7")
				shok = p8.GetActorsByType("shok")
				rae4 = p8.GetActorsByType("rae4")
				rae3 = p8.GetActorsByType("rae3")
				rae2 = p8.GetActorsByType("rae2")
				rae1 = p8.GetActorsByType("rae1")
				dog = p8.GetActorsByType("dog")
				scghost = p8.GetActorsByType("scghost")
				scfirebat = p8.GetActorsByType("scfirebat")
				scmarine = p8.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p9 and p14.IsAlliedWith(p9) then
				rmbo = p9.GetActorsByType("rmbo")
				e3 = p9.GetActorsByType("e3")
				e2 = p9.GetActorsByType("e2")
				e4 = p9.GetActorsByType("e4")
				e5 = p9.GetActorsByType("e5")
				e1 = p9.GetActorsByType("e1")
				e7 = p9.GetActorsByType("e7")
				shok = p9.GetActorsByType("shok")
				rae4 = p9.GetActorsByType("rae4")
				rae3 = p9.GetActorsByType("rae3")
				rae2 = p9.GetActorsByType("rae2")
				rae1 = p9.GetActorsByType("rae1")
				dog = p9.GetActorsByType("dog")
				scghost = p9.GetActorsByType("scghost")
				scfirebat = p9.GetActorsByType("scfirebat")
				scmarine = p9.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p10 and p14.IsAlliedWith(p10) then
				rmbo = p10.GetActorsByType("rmbo")
				e3 = p10.GetActorsByType("e3")
				e2 = p10.GetActorsByType("e2")
				e4 = p10.GetActorsByType("e4")
				e5 = p10.GetActorsByType("e5")
				e1 = p10.GetActorsByType("e1")
				e7 = p10.GetActorsByType("e7")
				shok = p10.GetActorsByType("shok")
				rae4 = p10.GetActorsByType("rae4")
				rae3 = p10.GetActorsByType("rae3")
				rae2 = p10.GetActorsByType("rae2")
				rae1 = p10.GetActorsByType("rae1")
				dog = p10.GetActorsByType("dog")
				scghost = p10.GetActorsByType("scghost")
				scfirebat = p10.GetActorsByType("scfirebat")
				scmarine = p10.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p11 and p14.IsAlliedWith(p11) then
				rmbo = p11.GetActorsByType("rmbo")
				e3 = p11.GetActorsByType("e3")
				e2 = p11.GetActorsByType("e2")
				e4 = p11.GetActorsByType("e4")
				e5 = p11.GetActorsByType("e5")
				e1 = p11.GetActorsByType("e1")
				e7 = p11.GetActorsByType("e7")
				shok = p11.GetActorsByType("shok")
				rae4 = p11.GetActorsByType("rae4")
				rae3 = p11.GetActorsByType("rae3")
				rae2 = p11.GetActorsByType("rae2")
				rae1 = p11.GetActorsByType("rae1")
				dog = p11.GetActorsByType("dog")
				scghost = p11.GetActorsByType("scghost")
				scfirebat = p11.GetActorsByType("scfirebat")
				scmarine = p11.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p12 and p14.IsAlliedWith(p12) then
				rmbo = p12.GetActorsByType("rmbo")
				e3 = p12.GetActorsByType("e3")
				e2 = p12.GetActorsByType("e2")
				e4 = p12.GetActorsByType("e4")
				e5 = p12.GetActorsByType("e5")
				e1 = p12.GetActorsByType("e1")
				e7 = p12.GetActorsByType("e7")
				shok = p12.GetActorsByType("shok")
				rae4 = p12.GetActorsByType("rae4")
				rae3 = p12.GetActorsByType("rae3")
				rae2 = p12.GetActorsByType("rae2")
				rae1 = p12.GetActorsByType("rae1")
				dog = p12.GetActorsByType("dog")
				scghost = p12.GetActorsByType("scghost")
				scfirebat = p12.GetActorsByType("scfirebat")
				scmarine = p12.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p13 and p14.IsAlliedWith(p13) then
				rmbo = p13.GetActorsByType("rmbo")
				e3 = p13.GetActorsByType("e3")
				e2 = p13.GetActorsByType("e2")
				e4 = p13.GetActorsByType("e4")
				e5 = p13.GetActorsByType("e5")
				e1 = p13.GetActorsByType("e1")
				e7 = p13.GetActorsByType("e7")
				shok = p13.GetActorsByType("shok")
				rae4 = p13.GetActorsByType("rae4")
				rae3 = p13.GetActorsByType("rae3")
				rae2 = p13.GetActorsByType("rae2")
				rae1 = p13.GetActorsByType("rae1")
				dog = p13.GetActorsByType("dog")
				scghost = p13.GetActorsByType("scghost")
				scfirebat = p13.GetActorsByType("scfirebat")
				scmarine = p13.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p14infantry()
	end)
end
p15infantry = function()
	if p15 and p15.IsBot then
		cityboteliteinstructor = p15.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p16 and p15.IsAlliedWith(p16) then
				rmbo = p16.GetActorsByType("rmbo")
				e3 = p16.GetActorsByType("e3")
				e2 = p16.GetActorsByType("e2")
				e4 = p16.GetActorsByType("e4")
				e5 = p16.GetActorsByType("e5")
				e1 = p16.GetActorsByType("e1")
				e7 = p16.GetActorsByType("e7")
				shok = p16.GetActorsByType("shok")
				rae4 = p16.GetActorsByType("rae4")
				rae3 = p16.GetActorsByType("rae3")
				rae2 = p16.GetActorsByType("rae2")
				rae1 = p16.GetActorsByType("rae1")
				dog = p16.GetActorsByType("dog")
				scghost = p16.GetActorsByType("scghost")
				scfirebat = p16.GetActorsByType("scfirebat")
				scmarine = p16.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p1 and p15.IsAlliedWith(p1) then
				rmbo = p1.GetActorsByType("rmbo")
				e3 = p1.GetActorsByType("e3")
				e2 = p1.GetActorsByType("e2")
				e4 = p1.GetActorsByType("e4")
				e5 = p1.GetActorsByType("e5")
				e1 = p1.GetActorsByType("e1")
				e7 = p1.GetActorsByType("e7")
				shok = p1.GetActorsByType("shok")
				rae4 = p1.GetActorsByType("rae4")
				rae3 = p1.GetActorsByType("rae3")
				rae2 = p1.GetActorsByType("rae2")
				rae1 = p1.GetActorsByType("rae1")
				dog = p1.GetActorsByType("dog")
				scghost = p1.GetActorsByType("scghost")
				scfirebat = p1.GetActorsByType("scfirebat")
				scmarine = p1.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p2 and p15.IsAlliedWith(p2) then
				rmbo = p2.GetActorsByType("rmbo")
				e3 = p2.GetActorsByType("e3")
				e2 = p2.GetActorsByType("e2")
				e4 = p2.GetActorsByType("e4")
				e5 = p2.GetActorsByType("e5")
				e1 = p2.GetActorsByType("e1")
				e7 = p2.GetActorsByType("e7")
				shok = p2.GetActorsByType("shok")
				rae4 = p2.GetActorsByType("rae4")
				rae3 = p2.GetActorsByType("rae3")
				rae2 = p2.GetActorsByType("rae2")
				rae1 = p2.GetActorsByType("rae1")
				dog = p2.GetActorsByType("dog")
				scghost = p2.GetActorsByType("scghost")
				scfirebat = p2.GetActorsByType("scfirebat")
				scmarine = p2.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p3 and p15.IsAlliedWith(p3) then
				rmbo = p3.GetActorsByType("rmbo")
				e3 = p3.GetActorsByType("e3")
				e2 = p3.GetActorsByType("e2")
				e4 = p3.GetActorsByType("e4")
				e5 = p3.GetActorsByType("e5")
				e1 = p3.GetActorsByType("e1")
				e7 = p3.GetActorsByType("e7")
				shok = p3.GetActorsByType("shok")
				rae4 = p3.GetActorsByType("rae4")
				rae3 = p3.GetActorsByType("rae3")
				rae2 = p3.GetActorsByType("rae2")
				rae1 = p3.GetActorsByType("rae1")
				dog = p3.GetActorsByType("dog")
				scghost = p3.GetActorsByType("scghost")
				scfirebat = p3.GetActorsByType("scfirebat")
				scmarine = p3.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p4 and p15.IsAlliedWith(p4) then
				rmbo = p4.GetActorsByType("rmbo")
				e3 = p4.GetActorsByType("e3")
				e2 = p4.GetActorsByType("e2")
				e4 = p4.GetActorsByType("e4")
				e5 = p4.GetActorsByType("e5")
				e1 = p4.GetActorsByType("e1")
				e7 = p4.GetActorsByType("e7")
				shok = p4.GetActorsByType("shok")
				rae4 = p4.GetActorsByType("rae4")
				rae3 = p4.GetActorsByType("rae3")
				rae2 = p4.GetActorsByType("rae2")
				rae1 = p4.GetActorsByType("rae1")
				dog = p4.GetActorsByType("dog")
				scghost = p4.GetActorsByType("scghost")
				scfirebat = p4.GetActorsByType("scfirebat")
				scmarine = p4.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p5 and p15.IsAlliedWith(p5) then
				rmbo = p5.GetActorsByType("rmbo")
				e3 = p5.GetActorsByType("e3")
				e2 = p5.GetActorsByType("e2")
				e4 = p5.GetActorsByType("e4")
				e5 = p5.GetActorsByType("e5")
				e1 = p5.GetActorsByType("e1")
				e7 = p5.GetActorsByType("e7")
				shok = p5.GetActorsByType("shok")
				rae4 = p5.GetActorsByType("rae4")
				rae3 = p5.GetActorsByType("rae3")
				rae2 = p5.GetActorsByType("rae2")
				rae1 = p5.GetActorsByType("rae1")
				dog = p5.GetActorsByType("dog")
				scghost = p5.GetActorsByType("scghost")
				scfirebat = p5.GetActorsByType("scfirebat")
				scmarine = p5.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p6 and p15.IsAlliedWith(p6) then
				rmbo = p6.GetActorsByType("rmbo")
				e3 = p6.GetActorsByType("e3")
				e2 = p6.GetActorsByType("e2")
				e4 = p6.GetActorsByType("e4")
				e5 = p6.GetActorsByType("e5")
				e1 = p6.GetActorsByType("e1")
				e7 = p6.GetActorsByType("e7")
				shok = p6.GetActorsByType("shok")
				rae4 = p6.GetActorsByType("rae4")
				rae3 = p6.GetActorsByType("rae3")
				rae2 = p6.GetActorsByType("rae2")
				rae1 = p6.GetActorsByType("rae1")
				dog = p6.GetActorsByType("dog")
				scghost = p6.GetActorsByType("scghost")
				scfirebat = p6.GetActorsByType("scfirebat")
				scmarine = p6.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p7 and p15.IsAlliedWith(p7) then
				rmbo = p7.GetActorsByType("rmbo")
				e3 = p7.GetActorsByType("e3")
				e2 = p7.GetActorsByType("e2")
				e4 = p7.GetActorsByType("e4")
				e5 = p7.GetActorsByType("e5")
				e1 = p7.GetActorsByType("e1")
				e7 = p7.GetActorsByType("e7")
				shok = p7.GetActorsByType("shok")
				rae4 = p7.GetActorsByType("rae4")
				rae3 = p7.GetActorsByType("rae3")
				rae2 = p7.GetActorsByType("rae2")
				rae1 = p7.GetActorsByType("rae1")
				dog = p7.GetActorsByType("dog")
				scghost = p7.GetActorsByType("scghost")
				scfirebat = p7.GetActorsByType("scfirebat")
				scmarine = p7.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p8 and p15.IsAlliedWith(p8) then
				rmbo = p8.GetActorsByType("rmbo")
				e3 = p8.GetActorsByType("e3")
				e2 = p8.GetActorsByType("e2")
				e4 = p8.GetActorsByType("e4")
				e5 = p8.GetActorsByType("e5")
				e1 = p8.GetActorsByType("e1")
				e7 = p8.GetActorsByType("e7")
				shok = p8.GetActorsByType("shok")
				rae4 = p8.GetActorsByType("rae4")
				rae3 = p8.GetActorsByType("rae3")
				rae2 = p8.GetActorsByType("rae2")
				rae1 = p8.GetActorsByType("rae1")
				dog = p8.GetActorsByType("dog")
				scghost = p8.GetActorsByType("scghost")
				scfirebat = p8.GetActorsByType("scfirebat")
				scmarine = p8.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p9 and p15.IsAlliedWith(p9) then
				rmbo = p9.GetActorsByType("rmbo")
				e3 = p9.GetActorsByType("e3")
				e2 = p9.GetActorsByType("e2")
				e4 = p9.GetActorsByType("e4")
				e5 = p9.GetActorsByType("e5")
				e1 = p9.GetActorsByType("e1")
				e7 = p9.GetActorsByType("e7")
				shok = p9.GetActorsByType("shok")
				rae4 = p9.GetActorsByType("rae4")
				rae3 = p9.GetActorsByType("rae3")
				rae2 = p9.GetActorsByType("rae2")
				rae1 = p9.GetActorsByType("rae1")
				dog = p9.GetActorsByType("dog")
				scghost = p9.GetActorsByType("scghost")
				scfirebat = p9.GetActorsByType("scfirebat")
				scmarine = p9.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p10 and p15.IsAlliedWith(p10) then
				rmbo = p10.GetActorsByType("rmbo")
				e3 = p10.GetActorsByType("e3")
				e2 = p10.GetActorsByType("e2")
				e4 = p10.GetActorsByType("e4")
				e5 = p10.GetActorsByType("e5")
				e1 = p10.GetActorsByType("e1")
				e7 = p10.GetActorsByType("e7")
				shok = p10.GetActorsByType("shok")
				rae4 = p10.GetActorsByType("rae4")
				rae3 = p10.GetActorsByType("rae3")
				rae2 = p10.GetActorsByType("rae2")
				rae1 = p10.GetActorsByType("rae1")
				dog = p10.GetActorsByType("dog")
				scghost = p10.GetActorsByType("scghost")
				scfirebat = p10.GetActorsByType("scfirebat")
				scmarine = p10.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p11 and p15.IsAlliedWith(p11) then
				rmbo = p11.GetActorsByType("rmbo")
				e3 = p11.GetActorsByType("e3")
				e2 = p11.GetActorsByType("e2")
				e4 = p11.GetActorsByType("e4")
				e5 = p11.GetActorsByType("e5")
				e1 = p11.GetActorsByType("e1")
				e7 = p11.GetActorsByType("e7")
				shok = p11.GetActorsByType("shok")
				rae4 = p11.GetActorsByType("rae4")
				rae3 = p11.GetActorsByType("rae3")
				rae2 = p11.GetActorsByType("rae2")
				rae1 = p11.GetActorsByType("rae1")
				dog = p11.GetActorsByType("dog")
				scghost = p11.GetActorsByType("scghost")
				scfirebat = p11.GetActorsByType("scfirebat")
				scmarine = p11.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p12 and p15.IsAlliedWith(p12) then
				rmbo = p12.GetActorsByType("rmbo")
				e3 = p12.GetActorsByType("e3")
				e2 = p12.GetActorsByType("e2")
				e4 = p12.GetActorsByType("e4")
				e5 = p12.GetActorsByType("e5")
				e1 = p12.GetActorsByType("e1")
				e7 = p12.GetActorsByType("e7")
				shok = p12.GetActorsByType("shok")
				rae4 = p12.GetActorsByType("rae4")
				rae3 = p12.GetActorsByType("rae3")
				rae2 = p12.GetActorsByType("rae2")
				rae1 = p12.GetActorsByType("rae1")
				dog = p12.GetActorsByType("dog")
				scghost = p12.GetActorsByType("scghost")
				scfirebat = p12.GetActorsByType("scfirebat")
				scmarine = p12.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p13 and p15.IsAlliedWith(p13) then
				rmbo = p13.GetActorsByType("rmbo")
				e3 = p13.GetActorsByType("e3")
				e2 = p13.GetActorsByType("e2")
				e4 = p13.GetActorsByType("e4")
				e5 = p13.GetActorsByType("e5")
				e1 = p13.GetActorsByType("e1")
				e7 = p13.GetActorsByType("e7")
				shok = p13.GetActorsByType("shok")
				rae4 = p13.GetActorsByType("rae4")
				rae3 = p13.GetActorsByType("rae3")
				rae2 = p13.GetActorsByType("rae2")
				rae1 = p13.GetActorsByType("rae1")
				dog = p13.GetActorsByType("dog")
				scghost = p13.GetActorsByType("scghost")
				scfirebat = p13.GetActorsByType("scfirebat")
				scmarine = p13.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p14 and p15.IsAlliedWith(p14) then
				rmbo = p14.GetActorsByType("rmbo")
				e3 = p14.GetActorsByType("e3")
				e2 = p14.GetActorsByType("e2")
				e4 = p14.GetActorsByType("e4")
				e5 = p14.GetActorsByType("e5")
				e1 = p14.GetActorsByType("e1")
				e7 = p14.GetActorsByType("e7")
				shok = p14.GetActorsByType("shok")
				rae4 = p14.GetActorsByType("rae4")
				rae3 = p14.GetActorsByType("rae3")
				rae2 = p14.GetActorsByType("rae2")
				rae1 = p14.GetActorsByType("rae1")
				dog = p14.GetActorsByType("dog")
				scghost = p14.GetActorsByType("scghost")
				scfirebat = p14.GetActorsByType("scfirebat")
				scmarine = p14.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p15infantry()
	end)
end
p16infantry = function()
	if p16 and p16.IsBot then
		cityboteliteinstructor = p16.GetActorsByType("cityboteliteinstructor")
		for _,expert in pairs(cityboteliteinstructor) do
			if p1 and p16.IsAlliedWith(p1) then
				rmbo = p1.GetActorsByType("rmbo")
				e3 = p1.GetActorsByType("e3")
				e2 = p1.GetActorsByType("e2")
				e4 = p1.GetActorsByType("e4")
				e5 = p1.GetActorsByType("e5")
				e1 = p1.GetActorsByType("e1")
				e7 = p1.GetActorsByType("e7")
				shok = p1.GetActorsByType("shok")
				rae4 = p1.GetActorsByType("rae4")
				rae3 = p1.GetActorsByType("rae3")
				rae2 = p1.GetActorsByType("rae2")
				rae1 = p1.GetActorsByType("rae1")
				dog = p1.GetActorsByType("dog")
				scghost = p1.GetActorsByType("scghost")
				scfirebat = p1.GetActorsByType("scfirebat")
				scmarine = p1.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p2 and p16.IsAlliedWith(p2) then
				rmbo = p2.GetActorsByType("rmbo")
				e3 = p2.GetActorsByType("e3")
				e2 = p2.GetActorsByType("e2")
				e4 = p2.GetActorsByType("e4")
				e5 = p2.GetActorsByType("e5")
				e1 = p2.GetActorsByType("e1")
				e7 = p2.GetActorsByType("e7")
				shok = p2.GetActorsByType("shok")
				rae4 = p2.GetActorsByType("rae4")
				rae3 = p2.GetActorsByType("rae3")
				rae2 = p2.GetActorsByType("rae2")
				rae1 = p2.GetActorsByType("rae1")
				dog = p2.GetActorsByType("dog")
				scghost = p2.GetActorsByType("scghost")
				scfirebat = p2.GetActorsByType("scfirebat")
				scmarine = p2.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p3 and p16.IsAlliedWith(p3) then
				rmbo = p3.GetActorsByType("rmbo")
				e3 = p3.GetActorsByType("e3")
				e2 = p3.GetActorsByType("e2")
				e4 = p3.GetActorsByType("e4")
				e5 = p3.GetActorsByType("e5")
				e1 = p3.GetActorsByType("e1")
				e7 = p3.GetActorsByType("e7")
				shok = p3.GetActorsByType("shok")
				rae4 = p3.GetActorsByType("rae4")
				rae3 = p3.GetActorsByType("rae3")
				rae2 = p3.GetActorsByType("rae2")
				rae1 = p3.GetActorsByType("rae1")
				dog = p3.GetActorsByType("dog")
				scghost = p3.GetActorsByType("scghost")
				scfirebat = p3.GetActorsByType("scfirebat")
				scmarine = p3.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p4 and p16.IsAlliedWith(p4) then
				rmbo = p4.GetActorsByType("rmbo")
				e3 = p4.GetActorsByType("e3")
				e2 = p4.GetActorsByType("e2")
				e4 = p4.GetActorsByType("e4")
				e5 = p4.GetActorsByType("e5")
				e1 = p4.GetActorsByType("e1")
				e7 = p4.GetActorsByType("e7")
				shok = p4.GetActorsByType("shok")
				rae4 = p4.GetActorsByType("rae4")
				rae3 = p4.GetActorsByType("rae3")
				rae2 = p4.GetActorsByType("rae2")
				rae1 = p4.GetActorsByType("rae1")
				dog = p4.GetActorsByType("dog")
				scghost = p4.GetActorsByType("scghost")
				scfirebat = p4.GetActorsByType("scfirebat")
				scmarine = p4.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p5 and p16.IsAlliedWith(p5) then
				rmbo = p5.GetActorsByType("rmbo")
				e3 = p5.GetActorsByType("e3")
				e2 = p5.GetActorsByType("e2")
				e4 = p5.GetActorsByType("e4")
				e5 = p5.GetActorsByType("e5")
				e1 = p5.GetActorsByType("e1")
				e7 = p5.GetActorsByType("e7")
				shok = p5.GetActorsByType("shok")
				rae4 = p5.GetActorsByType("rae4")
				rae3 = p5.GetActorsByType("rae3")
				rae2 = p5.GetActorsByType("rae2")
				rae1 = p5.GetActorsByType("rae1")
				dog = p5.GetActorsByType("dog")
				scghost = p5.GetActorsByType("scghost")
				scfirebat = p5.GetActorsByType("scfirebat")
				scmarine = p5.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p6 and p16.IsAlliedWith(p6) then
				rmbo = p6.GetActorsByType("rmbo")
				e3 = p6.GetActorsByType("e3")
				e2 = p6.GetActorsByType("e2")
				e4 = p6.GetActorsByType("e4")
				e5 = p6.GetActorsByType("e5")
				e1 = p6.GetActorsByType("e1")
				e7 = p6.GetActorsByType("e7")
				shok = p6.GetActorsByType("shok")
				rae4 = p6.GetActorsByType("rae4")
				rae3 = p6.GetActorsByType("rae3")
				rae2 = p6.GetActorsByType("rae2")
				rae1 = p6.GetActorsByType("rae1")
				dog = p6.GetActorsByType("dog")
				scghost = p6.GetActorsByType("scghost")
				scfirebat = p6.GetActorsByType("scfirebat")
				scmarine = p6.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p7 and p16.IsAlliedWith(p7) then
				rmbo = p7.GetActorsByType("rmbo")
				e3 = p7.GetActorsByType("e3")
				e2 = p7.GetActorsByType("e2")
				e4 = p7.GetActorsByType("e4")
				e5 = p7.GetActorsByType("e5")
				e1 = p7.GetActorsByType("e1")
				e7 = p7.GetActorsByType("e7")
				shok = p7.GetActorsByType("shok")
				rae4 = p7.GetActorsByType("rae4")
				rae3 = p7.GetActorsByType("rae3")
				rae2 = p7.GetActorsByType("rae2")
				rae1 = p7.GetActorsByType("rae1")
				dog = p7.GetActorsByType("dog")
				scghost = p7.GetActorsByType("scghost")
				scfirebat = p7.GetActorsByType("scfirebat")
				scmarine = p7.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p8 and p16.IsAlliedWith(p8) then
				rmbo = p8.GetActorsByType("rmbo")
				e3 = p8.GetActorsByType("e3")
				e2 = p8.GetActorsByType("e2")
				e4 = p8.GetActorsByType("e4")
				e5 = p8.GetActorsByType("e5")
				e1 = p8.GetActorsByType("e1")
				e7 = p8.GetActorsByType("e7")
				shok = p8.GetActorsByType("shok")
				rae4 = p8.GetActorsByType("rae4")
				rae3 = p8.GetActorsByType("rae3")
				rae2 = p8.GetActorsByType("rae2")
				rae1 = p8.GetActorsByType("rae1")
				dog = p8.GetActorsByType("dog")
				scghost = p8.GetActorsByType("scghost")
				scfirebat = p8.GetActorsByType("scfirebat")
				scmarine = p8.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p9 and p16.IsAlliedWith(p9) then
				rmbo = p9.GetActorsByType("rmbo")
				e3 = p9.GetActorsByType("e3")
				e2 = p9.GetActorsByType("e2")
				e4 = p9.GetActorsByType("e4")
				e5 = p9.GetActorsByType("e5")
				e1 = p9.GetActorsByType("e1")
				e7 = p9.GetActorsByType("e7")
				shok = p9.GetActorsByType("shok")
				rae4 = p9.GetActorsByType("rae4")
				rae3 = p9.GetActorsByType("rae3")
				rae2 = p9.GetActorsByType("rae2")
				rae1 = p9.GetActorsByType("rae1")
				dog = p9.GetActorsByType("dog")
				scghost = p9.GetActorsByType("scghost")
				scfirebat = p9.GetActorsByType("scfirebat")
				scmarine = p9.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p10 and p16.IsAlliedWith(p10) then
				rmbo = p10.GetActorsByType("rmbo")
				e3 = p10.GetActorsByType("e3")
				e2 = p10.GetActorsByType("e2")
				e4 = p10.GetActorsByType("e4")
				e5 = p10.GetActorsByType("e5")
				e1 = p10.GetActorsByType("e1")
				e7 = p10.GetActorsByType("e7")
				shok = p10.GetActorsByType("shok")
				rae4 = p10.GetActorsByType("rae4")
				rae3 = p10.GetActorsByType("rae3")
				rae2 = p10.GetActorsByType("rae2")
				rae1 = p10.GetActorsByType("rae1")
				dog = p10.GetActorsByType("dog")
				scghost = p10.GetActorsByType("scghost")
				scfirebat = p10.GetActorsByType("scfirebat")
				scmarine = p10.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p11 and p16.IsAlliedWith(p11) then
				rmbo = p11.GetActorsByType("rmbo")
				e3 = p11.GetActorsByType("e3")
				e2 = p11.GetActorsByType("e2")
				e4 = p11.GetActorsByType("e4")
				e5 = p11.GetActorsByType("e5")
				e1 = p11.GetActorsByType("e1")
				e7 = p11.GetActorsByType("e7")
				shok = p11.GetActorsByType("shok")
				rae4 = p11.GetActorsByType("rae4")
				rae3 = p11.GetActorsByType("rae3")
				rae2 = p11.GetActorsByType("rae2")
				rae1 = p11.GetActorsByType("rae1")
				dog = p11.GetActorsByType("dog")
				scghost = p11.GetActorsByType("scghost")
				scfirebat = p11.GetActorsByType("scfirebat")
				scmarine = p11.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p12 and p16.IsAlliedWith(p12) then
				rmbo = p12.GetActorsByType("rmbo")
				e3 = p12.GetActorsByType("e3")
				e2 = p12.GetActorsByType("e2")
				e4 = p12.GetActorsByType("e4")
				e5 = p12.GetActorsByType("e5")
				e1 = p12.GetActorsByType("e1")
				e7 = p12.GetActorsByType("e7")
				shok = p12.GetActorsByType("shok")
				rae4 = p12.GetActorsByType("rae4")
				rae3 = p12.GetActorsByType("rae3")
				rae2 = p12.GetActorsByType("rae2")
				rae1 = p12.GetActorsByType("rae1")
				dog = p12.GetActorsByType("dog")
				scghost = p12.GetActorsByType("scghost")
				scfirebat = p12.GetActorsByType("scfirebat")
				scmarine = p12.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p13 and p16.IsAlliedWith(p13) then
				rmbo = p13.GetActorsByType("rmbo")
				e3 = p13.GetActorsByType("e3")
				e2 = p13.GetActorsByType("e2")
				e4 = p13.GetActorsByType("e4")
				e5 = p13.GetActorsByType("e5")
				e1 = p13.GetActorsByType("e1")
				e7 = p13.GetActorsByType("e7")
				shok = p13.GetActorsByType("shok")
				rae4 = p13.GetActorsByType("rae4")
				rae3 = p13.GetActorsByType("rae3")
				rae2 = p13.GetActorsByType("rae2")
				rae1 = p13.GetActorsByType("rae1")
				dog = p13.GetActorsByType("dog")
				scghost = p13.GetActorsByType("scghost")
				scfirebat = p13.GetActorsByType("scfirebat")
				scmarine = p13.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p14 and p16.IsAlliedWith(p14) then
				rmbo = p14.GetActorsByType("rmbo")
				e3 = p14.GetActorsByType("e3")
				e2 = p14.GetActorsByType("e2")
				e4 = p14.GetActorsByType("e4")
				e5 = p14.GetActorsByType("e5")
				e1 = p14.GetActorsByType("e1")
				e7 = p14.GetActorsByType("e7")
				shok = p14.GetActorsByType("shok")
				rae4 = p14.GetActorsByType("rae4")
				rae3 = p14.GetActorsByType("rae3")
				rae2 = p14.GetActorsByType("rae2")
				rae1 = p14.GetActorsByType("rae1")
				dog = p14.GetActorsByType("dog")
				scghost = p14.GetActorsByType("scghost")
				scfirebat = p14.GetActorsByType("scfirebat")
				scmarine = p14.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
			if p15 and p16.IsAlliedWith(p15) then
				rmbo = p15.GetActorsByType("rmbo")
				e3 = p15.GetActorsByType("e3")
				e2 = p15.GetActorsByType("e2")
				e4 = p15.GetActorsByType("e4")
				e5 = p15.GetActorsByType("e5")
				e1 = p15.GetActorsByType("e1")
				e7 = p15.GetActorsByType("e7")
				shok = p15.GetActorsByType("shok")
				rae4 = p15.GetActorsByType("rae4")
				rae3 = p15.GetActorsByType("rae3")
				rae2 = p15.GetActorsByType("rae2")
				rae1 = p15.GetActorsByType("rae1")
				dog = p15.GetActorsByType("dog")
				scghost = p15.GetActorsByType("scghost")
				scfirebat = p15.GetActorsByType("scfirebat")
				scmarine = p15.GetActorsByType("scmarine")
				for _,rmbo in pairs(rmbo) do expert.DeliverExperience(rmbo) end
				for _,e3 in pairs(e3) do expert.DeliverExperience(e3) end
				for _,e2 in pairs(e2) do expert.DeliverExperience(e2) end
				for _,e4 in pairs(e4) do expert.DeliverExperience(e4) end
				for _,e5 in pairs(e5) do expert.DeliverExperience(e5) end
				for _,e1 in pairs(e1) do expert.DeliverExperience(e1) end
				for _,e7 in pairs(e7) do expert.DeliverExperience(e7) end
				for _,shok in pairs(shok) do expert.DeliverExperience(shok) end
				for _,rae4 in pairs(rae4) do expert.DeliverExperience(rae4) end
				for _,rae3 in pairs(rae3) do expert.DeliverExperience(rae3) end
				for _,rae2 in pairs(rae2) do expert.DeliverExperience(rae2) end
				for _,rae1 in pairs(rae1) do expert.DeliverExperience(rae1) end
				for _,dog in pairs(dog) do expert.DeliverExperience(dog) end
				for _,scghost in pairs(scghost) do expert.DeliverExperience(scghost) end
				for _,scfirebat in pairs(scfirebat) do expert.DeliverExperience(scfirebat) end
				for _,scmarine in pairs(scmarine) do expert.DeliverExperience(scmarine) end
			end
		end
	end
	Trigger.AfterDelay(DateTime.Seconds(8), function()
		p16infantry()
	end)
end
